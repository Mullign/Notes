![[Alternating Iterative Refinement (Part 2).pdf]]
So, in terms of Gaussian mixture modeling, this is actually a pretty powerful idea, I guess. So I want to spend just a minute or two, as maybe an aside, talking about some related ideas to Gaussian mixture modeling. One of them that's kind of nice to know about is sometimes called kernel density estimation. The word kernel is used in so many different ways in machine learning, it's a very overloaded word. So the idea here is I give you a point cloud, I give you a collection of points, in the example in the lower left here, these blue points are just points on the number line. On the right hand side, I've got points in two dimensions. I would like to kind of find a probability distribution, so to speak, I'd like to find a continuous density function that kind of describes that point cloud, from which it is likely that the point cloud was sampled, if you think of it as a probability distribution. 

And one common way that we do this is we basically just draw a little Gaussian distribution, sometimes called, that's called a Gaussian kernel. The use of the word kernel here is basically kind of a distribution of weight centered at a particular point that kind of dies off with distance. So we take a little Gaussian kernel function, superimpose that at each point, and then add those all together. And what you're going to get is a continuous distribution function that does describe the shape of your underlying point cloud. So here I have kind of, the points are kind of concentrated in three areas, and so you get a trimodal distribution as a result. In fact, this is very, if you do this, it's very easy to now figure out how many clusters your dataset had, because, you know, the peaks in your density function that you estimate would typically correspond to cluster centers. In fact, there's a method of clustering called mean shift clustering that is based on this sort of idea. 

You basically, if you want to figure out what cluster you belong to, if you take a point at some location, and you want to ask what cluster do I belong to, you basically turn on gravity. You let all of the points attract you according to gravity, and so if you're near to a large group of points, then you'll get kind of pulled in that direction. So you simulate the impact of gravity and kind of keep moving the point until it comes to rest at one of these peaks, typically. So these peaks represent kind of gravity wells, if you think about things in terms of gravitational attraction of the points in your dataset. One downside of kernel density estimation is that the model you come up with for this continuous distribution function, it really isn't any simpler than the underlying dataset that you started with. You started with a dataset described by n points, and you're now modeling it, this continuous distribution, using a distribution that is a superposition of n Gaussians, and so the model is extremely complicated. That's usually not a good feature. 

Typically when you try and come up with a model that describes your underlying data, you would like the model to be much simpler than the underlying data, parametrized by many fewer things than the n points of data that you trained on to fit your model. And so in this case, it might make sense to use Gaussian mixture modeling to come up with a much simpler model that still is a good approximation to your original data. So maybe in this case, using three Gaussians would have given you the ability to come up with a mixture that produces a continuous distribution that is pretty close to what you would have gotten had you added up a Gaussian around every single one of those n different points. And so Gaussian mixture modeling can give you a nice approximation to what you might have gotten through something like kernel density estimation. OK. I'm now second guessing whether I should have embarked on another recursive aside, especially one that involves a topic that is somewhat deeper in machine learning. But I wanted to kind of draw a parallel with something on the previous slide in terms of Gaussian mixture modeling. 

So let's kind of look at the world of classification for just a second. So supervised machine learning. So suppose I have a data set of labeled training data, maybe in two different classes, and I would like to find in feature space a decision boundary that delineates between those two classes. A lot of times we use techniques in machine learning like support vector machines that will find a linear decision boundary between our two classes. But in many cases, our data just may not be linearly separable. There may not be a good hyperplane, a linear decision boundary that separates our two classes. So what are we to do in that case if we are trying to use a method like a support vector machine that is typically geared towards finding linear decision boundaries when there isn't one to be found? 

So in this example in the lower left, my two classes are not separable easily by any sort of line that I can draw through my data. I have to kind of draw a nonlinear decision boundary to delineate those two classes. So here we could adopt an approach for classification that is similar to what we just talked about with kernel density estimation. So imagine taking a Gaussian distribution centered on every single one of my data points and adding them all together. So I get kind of a continuous distribution. That's going to be the thing in the lower right here. The only difference is for one of my classes, I'm going to weight the Gaussians with a plus one coefficient. 

And with the other class, I'm going to weight all the Gaussians with a minus one coefficient. So those Gaussians point downwards instead of upwards. And so when I add all of those together, I get a continuous distribution with a big well in the middle that reflects kind of the negative contribution of those points in the middle. And then I get kind of a bunch of mountains on the outside that reflect kind of the contributions of the points on the other class on the outside. Where that continuous distribution crosses zero, that's a good decision boundary. It's a nonlinear decision boundary that would kind of correspond to this dotted line here in my figure. And so that's one way that possibly I could do classification. 

This is basically kind of a glorified version of nearest neighbor classification if you think about it, because the Gaussian distributions are basically, if you look at any point in space and figure out its value in this continuous distribution, that's basically just looking at the sum of the distances to all of your training data weighted in some sense by the plus or minus one if it's in one class or another. And so this is very, very similar to doing nearest neighbor classification. The downside of building a classifier this way is exactly the same as the downside of using kernel density estimation here. So our model is no simpler than the underlying data that we built it from. So if you have n points in your training data set, then here your model is basically described by a density function. Your decision boundary is the place where that density function crosses zero. But the density function is a function of all of your data points. 

There are kind of n terms added together that make up that density function. And that's not great because you would like to have a model for classification that is much, much simpler, ideally, than the very large training data that you use to build that model. So what we might do instead is we can use a kernel function. So this is a different use of the word kernel function than slightly different than from kernel density estimation. So here, the typical use of kernel function is a way that you can take your data and map it typically to a higher dimensional space. Usually you see a picture kind of like this in the middle, where you take your data that's not linearly separable in the original feature space, but if you apply a kernel function to that data, then you can map it to a higher dimensional representation in a higher dimensional space in which it magically becomes linearly separable. And so then there's a whole can of worms involved with how you pick this kernel function, what sort of properties does it need to satisfy and so on. 

In some instances, the kernel function actually maps your data points up to an infinite dimensional space in which you find a linear decision boundary between your two classes. And then you use that for classification. The algorithmic benefit of using these sorts of kernel methods, which you can plug into a wide range of machine learning methods like PCA or support vector machines, methods that are kind of traditionally designed to find linear answers, like linear decision boundaries. You can often use these kernel methods to kind of upgrade these methods to find nonlinear answers, nonlinear decision boundaries by kind of implicitly projecting your data up to a higher dimensional space, finding a linear answer in that higher dimensional space, and then projecting that back down and kind of looking at what that looks like in your original feature space. And the benefit of using these methods is oftentimes you don't actually have to do the explicit projection to a higher dimensional space. That kind of is done in an implicit fashion. So algorithmically, these techniques are quite intriguing, but they're also kind of tricky to understand. 

At least for me, they were hard to learn about when I learned about them in my own schooling. So I wanted to basically, I won't go into a tremendous amount of detail here because this is another topic in and of itself, but I wanted to kind of draw an analogy with the issue that we had on the previous slide here. So here we had a continuous density function that we had estimated with kernel density estimation. The downside was that it took too much, it was too complicated, too much space because the density function was a sum of n different things. And we remedied that by using a mixture model of a very small number of Gaussians. You can do the same thing here. So effectively, if you use a support vector machine, which I won't define, but it's a popular method of classification, typically with a linear decision boundary, but you can upgrade it with a kernel and a very common kernel that we use is a Gaussian kernel or a radial basis function kernel. 

If you do that, you are effectively approximating this continuous distribution kind of in the lower right. So instead of superimposing a little kernel function over every single point in your training data set, you're trying to find a small number of Gaussians that you can superimpose to approximate this thing. That small number of Gaussians correspond to what are called the support vectors of your support vector machine model. And so the SVM model here is kind of doing something very similar to what we've been talking about with our kernel density estimation and our Gaussian mixture modeling. It's trying to use kind of a small number of Gaussians to approximate this landscape in which the zero crossings corresponds to the nonlinear decision boundaries between your classes. This could very well be an entire lecture series in and of itself. I just wanted to kind of highlight a few more interesting things in the world of Gaussian mixture modeling that might be of interest. 

Okay, we're almost done with Gaussian mixtures and such. I wanted to kind of talk about at a higher level, the technique that we just used to do Gaussian mixture modeling, where we update kind of in alternation, our explicit model parameters. Those were the mus and the sigmas and the Ws. And we alternated between updating those and the Pijs, which were not explicit parameters in our model, but they're kind of these latent parameters that help us update the explicit model parameters. So knowing the Pijs makes it much easier to update the explicit model parameters and vice versa. So this type of alternating iterative refinement approach is sometimes called expectation maximization. It's a generic algorithm that appears in many different contexts, specifically in machine learning and in many different situations. 

If you'd like to fit things like hidden Markov models or Bayesian networks, oftentimes you use expectation maximization, an EM algorithm to do that. And it's very commonly also used kind of generically for mixture modeling. Oftentimes that means Gaussian mixture modeling because Gaussians are well-behaved and so it's easy to update their parameters. But you could conceivably build a high-level aggregate model that is a mixture of any type of model. So just take a weighted sum of a number of underlying models, take a superposition of those underlying models, and you can use expectation maximization in essentially the same way that we have been using it to learn the parameters of our mixture of Gaussians. You can learn a mixture of other types of models here. So this is really a more generic technique. 

It's not just Gaussian mixture modeling. This is kind of expectation maximization is kind of the term we use for this algorithm that alternates between updating your explicit model parameters and these sort of latent, hidden model parameters that aren't really parameters in your model, but that kind of help you with updating the explicit model parameters. Expectation maximization, it's kind of like dynamic programming. The name itself does not really suggest what the technique does. And so I'm not a fan of the name of the technique. I just prefer alternating iterative refinement, kind of where you alternate between updating explicit and latent model parameters. The word expectation, where does that come from? 

I think typically the explanation that I would give people here is that one of your steps is updating these probabilities, these latent parameters that are often probabilities. And as you know, a probability is the same thing as the expectation of an indicator random variable. So if you set things up mathematically using indicator random variables, the expectations of those indicator random variables are these probabilities. And so these two steps are sometimes called the E-step and the M-step. So the E-step is this expectation step where you update the latent parameters that often has the interpretation of updating these expected values of random variables that end up being the same as probabilities. And the M-step is a maximization step where you update the model parameters to give you your expectation. your maximum likelihood estimation. So there's a lot of really interesting mathematics going on underneath the hood in this expectation maximization algorithm, but to avoid getting into the weeds too much, I think I wanna kind of move on and talk about just a few more ideas in the world of alternating iterative refinement. 

So one nice idea that I like a lot is an idea that kind of takes us back to our discussion of ensemble methods that we briefly introduced when we talked earlier about just iterative refinement. So suppose I would like to find a clustering of say the nodes in a graph. And so I would like to take the nodes in a graph here and divide them up into two clusters in some reasonable way. And I want to talk about kind of what objectives you might use for that. There are lots and lots of different objectives that people have proposed that are quite reasonable for doing this sort of clustering in graphs. But suppose, for example, that I'm dealing with like an NP hard clustering objective. So I don't know how to exactly optimize it. 

What I might do is I might come up with a large number of locally optimal solutions. So maybe you start with a random solution, run iterative refinement until it converges to something and do that N times. And so I get N perspective clusterings. And it's not clear which one of those is the best. And I use some sort of an ensemble approach to improve the state of things. So what I'm gonna do is imagine taking all of those clusterings and kind of superimposing them on top of each other. And what I can kind of do then is I can reweight my graph. 

And so I'm gonna assign the weight of an edge in my graph to be the fraction of my clusterings in which X and Y belong to the same cluster. And so if in all of my clusters, two nodes are always in the same cluster, they're gonna have a very high weight edge, an edge of weight one joining them. If they're never in the same cluster, then they're gonna have an edge of weight zero joining them. So the weight of an edge reflects the propensity of two nodes to want to belong to the same cluster. So after I reweight my graph, I can then do the same thing. I can generate N clusterings again, but now I can use these kind of fictitious edge weights that suggest which nodes kind of want to belong to the same cluster. And so if I have a high weight between two nodes, it will be more likely that they end up in the same cluster according to whatever clustering algorithm I'm using. 

This can be kind of used generically with any clustering method really. And so here I use alternating iterative refinement. In one step, I generate my ensemble of clusterings, and then I use that to update the weights of my edges. And then I use those edge weights again to generate another ensemble of clusterings, ensemble of clusterings. And then I alternate back and forth until I eventually converge. When the weights all become binary, I'm basically done. Or if they get close enough to binary numbers, one or zero, that means that my clusterings are pretty much all agreeing with each other. 

And so kind of a nice way that you can use alternating iterative refinement together with kind of an idea that involves ensembling of solutions. And then finally, one last idea here that I think is quite nice to mention. So very commonly in data analytics, you have a data matrix. So that would be a matrix like A here where the rows of the matrix are your different data points. So N rows, each one is just a feature vector in D dimensions, or it describes an element of data. So maybe it's a signal of speech data or something. A lot of times you would like to, in some way, take that data matrix and factor it to expose some sort of underlying hidden latent structure. 

You wanna basically show that your data points could have been written as mixtures of lower level hidden kind of underlying sources or factors. So to give you kind of a few examples of that, suppose that each data vector maybe corresponds to a document in a text processing application. And the vector itself is kind of, so the columns of my matrix correspond to different terms, different words or phrases. And so each data vector is basically a very, very long vector. This is kind of a document term matrix, I guess, A. Each data vector is a very long matrix that kind of tells me the composition of a document in terms of its underlying words or terms. If I'm focused on kind of key words, maybe I don't care about the, you know, the and A and N, but maybe I care about the interesting words. 

So if I'm able to factor that matrix into kind of X times Y, where X is kind of a matrix of the mixture weights between the sources or the factors in Y, then these latent factors in Y might tell me something like, I don't know, subject specific vocabularies. So each data vector, each document, its vocabulary could possibly be expressed as a mixture of kind of more primitive underlying vocabularies from different domains. So maybe I have a data vector that's a document in a bioinformatics document. And so maybe it's a mixture of terminology from computer science and from biology. So maybe one of these underlying latent sources is a vector of terms that corresponds to computer science. And another one is a vector of terms that corresponds to biology. So I could maybe discover that sort of underlying structure and how these underlying source vocabularies are combined and mixed together to get my data points if I'm able to factor my matrix. 

In this particular application, I want to factor the matrix in a way such that the factors are non-negative. This is called a non-negative matrix factorization because I'd like to be able to interpret the resulting matrices in a certain way. So one of them is kind of the mixture weights and the other one is going to be these kind of non-negative underlying latent factors. So in many applications involving matrix factorization, you want to kind of look for a factorization that satisfies some sort of property. So you'd like the sources, the factors, the underlying factors to be maybe uncorrelated with each other. That's what you're basically looking for in PCA, principal component analysis or factor analysis, basically two sides of the same coin. A lot of times you're looking for underlying factors that are independent of each other. 

This actually in my own research is something that we often do. I do a lot of research involving brainwave data, EEG data. So you measure electrical voltages at different points on the scalp. That's unfortunately not measuring what's actually happening in your brain. That's measuring mixtures of signals that are originating from your brain. And so a lot of times what you do with EEG data is you take a bunch of data vectors, which each one is like the signal that you're getting from a particular electrode on your scalp. And you'd like to kind of figure out a likely way that those data vectors, those signals originated as mixtures of kind of more primitive lower level underlying signals inside your brain. 

And so in this case, you typically do ICA, independent component analysis. You look for independent signals because if you mix signals together, they're not independent anymore. And so those are not source signals. You're looking for kind of the original source signals inside the brain that contributed to these mixtures that you're measuring on the scalp. So there are lots and lots of different types of properties that you often look for when you try and factor a data matrix. You might look for just a sparse solution. So kind of a simple solution. 

You might try and find factorization that involves an X and Y that has a very small number of factors. So you might be trying to come up with like a low rank approximation of your original data matrix. So there are lots and lots of problems in data analytics that have this sort of structure. You're basically trying to take your data matrix and factor it somehow. In our current context, thinking about alternating iterative refinement, I think the key thing to do here is focus maybe on just non-negative matrix factorization. So here I wanna just factor A, which is a non-negative matrix itself into a product of two non-negative matrices. And this unfortunately is an NP-hard problem but you can actually approach it with alternating iterative refinement. 

And maybe I should remove my face from the picture here so I'm not overlaying part of the slide. So basically what we can do is like hold one of these matrices fixed, hold X fixed, and then solve an optimization problem to update the values in the other matrix. So we hold X fixed while we update Y, we then hold Y fixed while we update X. And even though the overall problem here is NP-hard, each of those two problems that we solve in alternation is much, much easier. Typically, if you make an objective function here that's kind of a least squares objective, then you can phrase each of those update problems as what's called a non-negative least squares problem. That's a much, much easier problem in convex optimization. So much easier to solve these smaller kind of individual problems as you're working your way towards a locally optimal solution of the overall problem. 

So these are, well, quite a few now, examples of alternating iterative refinement algorithms. So taking iterative refinement and just using it in a very common way where we kind of don't update the entire solution all at once, but alternating between updating pieces of the solution. So I think I'll stop here. I think we have now seen a lot of examples of iterative refinement. So hopefully this will be useful when you look at different problems that you might have to optimize. Thank you. Bye-bye.