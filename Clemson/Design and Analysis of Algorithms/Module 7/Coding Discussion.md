![[Coding Discussion module 7.pdf]]
Hard problems are common in the real world, and we do need to have effective algorithmic strategies for approaching them. In this module, the main two approaches that we use to address hard problems would be either using a heuristic approach that gives us a reasonably good solution with a reasonably fast running time, or as we talk about in this video, using a pruned exhaustive search to get the exact optimal solution but only for very small input sizes because we'll be searching over every possible solution. Hopefully, pruning the search as we go to make it run as quickly as we possibly can. This is sometimes called branch and bound search because we're branching over every possible solution and then bounding the search with pruning to try and make it run quickly. I thought this would also be a good exercise for our implementation discussion for this module because going through the motions of coding up a pruned exhaustive search, it's a helpful exercise to practice our skills with recursion and just understanding the structure of how we can search a large space of solutions. The problem that I have chosen for our discussion today is the famous traveling salesman problem, which we've talked about before. This is a known NP-hard problem, so we shouldn't expect to be able to find optimal solutions for very large instances. 

Here, just to remind you that input is a set of N cities. We'll be using points in the two dimensional plane to make it easier to conceptualize and visualize our solutions. But the input doesn't have to be points in the plane. All we really need is just pairwise distances between our cities as input. The goal is to find a tour that visits every city exactly once, returning back to where it started at the end. As just a fun aside, the picture here in the lower right. This is an example of a traveling salesman tour on 500 cities which I had given that input to a group of students as a challenge problem in the past, saying, hey, come up with the best solution you can for that set of 500 cities. 

It's probably too much to ask to find an optimal solution for input of that size, but the students actually did a pretty good job. Then later on in the week with that same group of students, we were going on an excursion to a planetarium just for fun, and I told them at the time, by the way, those 500 cities, they were actually the 500 brightest stars in the sky. You have just found a galactic traveling salesman tour that visits the 500 brightest stars in the sky, and we actually visualized their solutions on the giant planetarium dome. I'd worked this out with the operators of the planetarium in advance. That was a fun exercise involving traveling salesman tours. What do we do if we're faced with an input of the traveling salesman problem? Maybe the simplest approach that we could start with, and maybe from our implementation exercise perspective, we'll start here, but we could try and come up with a greedy solution. 

It's very easy to come up with greedy solutions for the traveling salesman problem. You start at some arbitrary city then from there, you go to the closest available city and then from there you go to the closest remaining city that you haven't visited yet and repeat until you've built a complete tour. This probably won't give you an optimal solution. As we'll see, we'll probably end up painting ourselves into a corner where the next step is inevitably going to have to jump a large distance to get to a city that we haven't visited yet. We could try and mitigate slightly this issue of non-optimality by trying different possible starting cities and maybe taking the best tour that we generate over this entire process. But this will hopefully at least give us a reasonably good solution. Later on in the module, when we talk about iterative refinement, this might be a good starting point for an iterative refinement process that takes a reasonable starting point and makes it better and better via some heuristic approach. 

Maybe this will be the starting point for our coding discussion. Then after that, we're going to consider using a pruned exhaustive search to explore every possible tool that we can generate. Here, the running time is not going to be very good. It's going to be N factorial because you're basically checking every possible permutation of the N cities and remembering which one was the best possible tool. The structure here is basically, you can think of it in this model of sequential decisions. We start at an arbitrary city, we'll start at city 0 as our starting point. Then we're going to loop over every possible next city. 

Then for each of those possible second choices for a city, we're going to loop over recursively all of the possible choices for the third city and so on, just remembering the best solution that we get as we go through that process. A nice way to think about this is using a tree of sequential decisions. If I go look at my whiteboard here, I've drawn out a tree where our starting point is city 0 and then from city 0, we have a branching decision where we have to figure out what is the next city that we visit? What's the second city that we visit? There's N-1 choices here for each of those choices. We then have N-2 choices for the next city and so on. You can view the process of enumerating through every possible tour as a process of traversing this tree of all possible options. 

By the time you get down to a leaf at the bottom of the tree, you will have generated a complete tour and you can basically calculate the length of that tour and remember the best one. There will be basically N factorial options at the bottom of this tree that we have to consider as possible tools and if we're in the middle of the search process. If we're say sitting on a node, that's not a leaf. That represents a partial solution that we've built up. This node 2 right here would represent the fact that our current tour basically starts with city 0 and city 2 and then remaining, we still have cities 1, 3, and 4 in play. Our tour would look like this. We've committed to the partial solution of zero and two, and then we're still trying to decide between 1, 3, 4. 

In our code actually, we're going to have a tour array that will look exactly like this. It'll have zero and two at the beginning because we've already committed to those. Then 1, 3, and 4 will be at the end of the array, and we'll be trying to basically cycle through all the options of those three remaining cities as we continue to explore the sub tree below the current node. This is the picture of where we're going. Let's write some code now and start to explore how to greedily and exhaustively compute solutions for the traveling salesman problem. I'll switch over to my coding view. Let's maybe start a file. 

How about tsp.cpp? That sounds appropriate. I'll go ahead with my usual header here, iostream using namespace std int main. We're going to be dealing with an input that is N points in the two dimensional plane representing our N cities. Maybe I'll make a point structure to hold a point. Struct point, and a point is going to be just an x-coordinate and a y-coordinate. We'll have maybe a vector of points. 

Let's just call it cities. That's going to be my list of all the cities. If I'm using a vector, then I'll have to include vector as a header file. I'm going to have, let's say maybe N as the number of cities. This is going to be a description of my input. Let me just generate that input randomly perhaps. How do I get N? Maybe I can actually pass in N on the command line. 

When I run my program, I can easily specify different values of N. I'll just pass in, and the way you do this in C++ or in C. You pass in the number of command line arguments and an array of those arguments, and I'll just convert to an integer, the first argument that's passed in on the command line. That's how we're going to get the number of cities, and then we'll just generate the coordinates of those cities randomly. I'll just loop up to N. I less than N, i++. Then for each city, I'll just generate it randomly. 

I'll just make a random point. I'm going to call drand 48, which gives me a random real number between 0-1, that'll be my x-coordinate and then I'll call drand 48 again to get another random number for my y-coordinate. Every single point that I generate is going to be just a random point in the unit square in two dimensions, and then I'm going to add that to my city's array, cities,push_back (p). I have now my list of N random cities. Let's go ahead and print those out. If I just again, loop over all of the cities in my instance here, I'm just going to print out for each one cities of i.x, and then maybe a space and then cities of i.y. Let's compile and run this just to build our code up piece wise here. 

If I compile tsp.cpp and I forgot my less than less than before cities of x. Here we go. If I run this a.out on maybe 10 cities then I get a list of the x and y coordinates of those 10 points to help us visualize our solutions. I've written a little visualizer, so I can actually pipe the output those 10 points into my little visualizer, and that's actually going to visualize what a tour would look like if I visit those 10 points in the order that I just specified, going back to the starting point at the end of the tour. As you can see, it's of course, not an optimal solution because this is just essentially a random ordering of those 10 points. If I ran this on 100 points, it would probably look even worse like a plateful of spaghetti. This is certainly not a good solution for the traveling salesman problem. 

We have a lot of ground to make up here. Maybe the first thing we should try and do is code up a greedy solution to see if we can at least get a reasonable solution that looks better than this plateful of spaghetti right here. Let's maybe focus on how we might do that. I'll go back to our code here. We would like to figure out a greedy solution. Let's basically well, to keep main uncluttered here, I'll just call a function that says greedy. How is that going to compute a greedy solution? 

How am I going to represent a tour actually? Maybe I'll just make a vector of integers. How about that? A vector of ints, and that'll be my tour. For example, the tour might be, what did I have on the previous board? It started with zero and two, and then it had the other possible cities in some order. The tour is just going to be a vector of ints N of them that specify the indices of the cities along my tour. 

All I'm going to do is maybe initialize this tour vector in the greedy function, and then I'll print it out at the end. Actually, when I print this out, I don't want to just print the cities in order, I want to print the cities in the order specified by my tour vector. I'll just put cities of tour(i). Whatever tour I come up with in my greedy algorithm, that's the one I'm going to print out. Let's initialize my tour vector. Maybe I'll just loop over all the cities, and I'll just say tour.push_back(i). I'm going to start with the tour that's 0, 1, 2, 3 up to N-1. 

Now I'm going to figure out how to make this into a greedy solution. I'm going to start with city 0. The first city is the correct one, city 0. I need to loop over all of the remaining cities and figure out what's the best greedy option for each remaining city. I'm going to loop over i = 1 up to N. For each of these, I'm going to find the best greedy choice for tour(i). What is the best choice for i index in this tour? 

I'm looking at index i here, trying to find the best option for a city to go here, and the options available are all of the cities from position i onwards. When i = 2, i is in this position, I will have committed to the first two cities, and I'll be looking in the set of remaining cities from i and beyond for the best remaining city. I can just swap that into position i and then I would step forward to position 3. Again, I'm trying to find the best city from the remaining set of cities and I'll swap that into position 3 and so on. I'm going to find the best greedy choice for tour of i and swap into position tour of i from remaining cities in positions i onwards. What's the best choice? I really want to minimize the distance from the last city on my tour. 

If I'm sitting here trying to find the best city to put in Position 3, then that's going to be the one that minimizes the distance with the one that I had in Position [i-1[ in Position 2. Minimize the distance from city in tour[i-1]. That's the last one that I added. All I need to do is basically a for loop that loops over all my choices and takes the best one. J is going to start at i because the options that I have to consider start at position i and go onward from there. Loop over all the remaining possible choices, and if this one has a better distance from the city in tour[i-1[, then I'll swap it into Position i. I don't have a distance function. 

Let's write a quick distance function, that'll take the distance between two cities. That hopefully shouldn't be too bad. Pause our thoughts there, so I'll make a distance function, that's going to take two indices of two cities and return the distance between them. That's going to be just the standard Euclidean distance in the plane. I'm going to have the x distance, which is the cities[i].x minus cities[j].x. That's the difference in their x coordinates, and then I'll find the difference in their y coordinates. Then the overall distance is going to be just the square root of the square of the x distance plus the square of the y distance. 

Standard Pythagorean theorem to find the distance between those points. I guess if I'm using square root, I need to make sure I'm keeping up with my headers here. Square root is defined in, I think, cmath. Going back to our greedy algorithm, we're interested in whether the distance from I guess tour[i-1] to this new point that I'm considering tour[j], If that's less than the distance from tour[i-1] to tour[i], which is the current contender for the next city that I choose, then I'm going to swap tour[i] with tour[j]. I think that implements the idea that we just talked about in our comments here. We're going to loop over all of the remaining cities from i onwards. Pick the one that has the best distance with the city in position i-1 in our tour, and swap that into position i. 

That should hopefully generate a greedy tour. Then if I go back after greedy is finished with executing, it'll print out that tour, and hopefully we will get something that looks more reasonable than that plateful of spaghetti from before. If I recompile, I need to put an int there. Let's see. Where int j = i. Recompile and rerun. This looks more reasonable. 

This is roughly what I would expect a greedy algorithm for the traveling salesman to look like. It is actually not a bad looking solution for the most part. It looks like it's stepping to the nearby cities whenever it can, but it does seem to occasionally paint itself into a corner and require a large step because there aren't any nearby cities at that point. This is a hallmark of greedy algorithms. They only look at the short term ramifications of their decisions. You're taking tiny steps to nearby cities, not realizing that you're trapping yourself in a place that doesn't have any good escape routes, and you have to take a big step as a result of that. You do lose something by not looking at long-term ramifications of the decisions that you're making. 

Let's now look at instead of this simple fast greedy approach, how would we generate every possible tool and take the best one, so heading down this recursive exhaustive enumeration route. If we go back to our code here, I'm going to use the same tour structure for doing this. What I'm going to do essentially is, let's go back to our picture of the whiteboard. We're going to basically try and implement this tree of sequential decisions. At any point in this search, we will have built up a partial tour. Maybe I'll use the same tour array to build up my tour, and I'll be trying to find the right city to choose for the next index, the working location in this tour. Let's maybe call this for lack of a better word index. 

That's the current location in the tour array that I'm looking at alternatives for it. I'm going to write a function, maybe I'll call it solve, and I'll pass in the current index. Initially, I guess, my index is going to be one because I'm going to be looking for the right alternative to put in Position 1 in my tour array. Remember, Position 0 is fixed. That's the initial starting city. That's always going to be there. I'm going to call solve initially on Index 1, and that's going to look at all of the possible choices that I can put in the one position in my tour array. 

Then for each of those, that's going to recursively call solve of two to look at all the possible options that I can put in Position 2 of my tour array. The snapshot that I've shown right here would be maybe what I would be looking at right after calling solve of two. I already have committed two cities, zero and two at that point, and I'm going to be looping over all of the possible options for what to put in Index 2 in my tour array. The options available to me are all of the options that are at the end of my tour array here. Cities 1, 3, and 4 in this example. I'll loop over all of those. For each one, I'll try temporarily swapping it into Position 2 in the tour array and then calling solve of three to try and loop over all of the remaining options that are still ahead of me in that array. 

That's where we're going with our code. Let's see if we can actually write that. I mentioned I'm going to call solve on one, that's going to start looking at all the options for Position 1 in my array. Here I'm going to have my recursive function solve and I'm going to give it an index. According to what I said, I'm basically going to loop over the structure here is that I've already figured out in some sense, tour of everything up to index-1, and I guess, I'm looping over all options. Looping over all options for tour[i] among the choices that are in tour[i] onward. Let's loop over all those options. 

I'm going to loop over, I guess I shouldn't write i here. It's really index. I'm going to loop over all choices of my tour, my next city from, I guess, position index onward in my tour array. For each one of those, I'm going to temporarily swap it into position index because that's the one that I'm trying to figure out the right setting for it. I'm going to swap tour of index with tour[i], to put that option in tour of index in that spot, and then I'm going to call solve on index+1. Then I think I'm going to swap it back so that as I'm considering options, I'm going to temporarily swap something into the index position, recurse, and then swap it back. That means that this function will leave the tour array in the same order that it found it, which is nice that when I call solve on index, I can expect that recursive call to solve isn't going to mess with the tour array. 

It's going to basically leave the tour array in the same state as when I called it. I won't get messed up in my search over different options or the alternatives that I'm considering for the index position here in tour. This is going to be recursively enumerating over all of my choices. I call solve of one, that looks at all the options to put in tour[1], and each of those calls solve of two, which will loop over all the possible positions options to put in tour[2], and so on. This will be recursive, so it does need a base case. What's a good base case? I guess that's when I reached the bottom of my tree and I have filled the entire tour array. 

If I guess if index=N, that means I've fully specified a complete tour, so at that point, I will return because this is a base case, and I need to figure out, is this the best tour that I've seen so far. Is this the best tour so far? If so, I need to remember that tour. Maybe I need to keep track of the best tour that I found over the entire recursive process. Maybe I have a vector of ints that's the best tour, and maybe I'll keep track of the length of that best tour, so best length. Maybe I'll start that out at some very large number so that over the course of the search, I'll find better and better tours and keep recording them as I find them. Here if the tour length of my current tour is better than the best length, then I will make a copy of my tour in best. 

I'll save the current tour is best, and I'll set the best length equal to the tour length. I'll just remember the best tour that I've ever generated. Whenever I hit a complete tour, I compare it to the best one I've found so far. I guess I need to understand the length of my current tour. I haven't computed that yet. What's a good way to get the length of the current tour? I could, I guess loop over the tour array and add up all the distances of the current tour or I guess maybe slightly more efficient, actually, why don't I just build that distance up and pass it along during the recursive process? 

Why don't I just pass along the length of my tour as I go? Then I don't have to spend time here in this base case looping over the entire tour array and recomputing from scratch the length of the tour every single time I hit a base case. I'll already have computed the length of the tour at that point. I'm going to start out when I initially call solve, I'm going to pass in a zero length tour because I only have City 0 in my tour array. I haven't really stepped anywhere yet. Then when I recursively call solve, then all I need to do is add to my current working tour length, the length of the step that I've just added under consideration here. I've just added a step from tour of index-1 to tour of index. 

I'm just going to add in the distance between those two cities between tour of index-1 and tour of index. Now as I recurse, I'm just building up the length of the tour as I go. By the time I hit my base case, the tour length will actually reflect the sum of the distances on every step that I have taken while building up that tour. I guess, actually, there's one step that I haven't reflected, which is the step that takes me back to the starting point. The actual length of the tour is not just tour length, it's tour length plus the distance between the first and the last elements of the tour array, so between tour[0] and tour[N-1]. I have to account for that very last leg, that last step that completes the tour and makes it into a cycle, and so I need to add that distance in to tour length when I consider it. Tour length plus that distance. 

Actually, one thing that I also probably need to do is when I print out the to, I want to print out the best to that I found instead of just the greedy tour that I found. Now we should have a recursive exhaustive search that loops over every possible tour, running in N factorial time, and it's going to compute the best one. We should get the optimal solution, but we'll only be able to do that for very small instances. Let's compile and run and see what happens. I definitely don't want to run this for size 100. Let's try maybe size 10, see what that does. That looks believable. 

I would expect probably not to be able to solve this for very large inputs. Eleven seems okay, what about 12? Because N factorial has some severe limitations on the input sizes that you can solve. Even for N = 12, things are slowing down quite a bit. Maybe I can give myself the best possible hope. I'll add optimization when I compile, and let's see if that helps. For 12, I can handle 12. 

What about 13? Thirteen seems to be slowing down. It looks like N = 13 is maybe about the limit of what I can handle with my just brute force recursive enumeration. Now I have to be smarter about pruning my search to make it run as quickly as possible to get past this N = 13 mark. What am I going to do to prune my search? Let's go back to our high level discussion here. We've done our exhaustive search, now I want to add in pruning. 

What do we typically do to make this work? I'm going to do my exhaustive search like I have done before. Do my exhaustive search, keep track of the best solution we've found so far. That we've done. That's sometimes called the incumbent solution. That's our running best solution. Then the key thing now is if you ever find a solution, even a partial solution that you've built up that really is hopeless, has no hope of dethroning the incumbent solution, then you want to prune the search and just return and don't even bother recursing any further into your tree. 

The standard picture of that looks like the following. Here's my tree of solutions that I'm searching, all my sequential decisions. I've already searched a good chunk of that tree, and I found an incumbent solution. The best solution I found so far is x star. The cost of x star is 100, for example. Now I'm continuing to search other solutions. Suppose I'm sitting on a partial solution x, the cost of that is 75. 

The cost of building up up to the point that I have is 75. I still need to explore options in x's subtree to figure out how to complete that partial solution. It's still going to cost me a bit to complete that solution. The key thing to being able to prune effectively is to get some reasonable lower bound on the cost of completing that solution. If I know that no matter how I choose to complete x, it's going to cost me at least 30, then all of the solutions in the subtree are going to cost me at least 75+30, 105. That's already worse than my incumbent solution, and so I can prune the search right here and now. If I have a crude way of getting a lower bound on my cost of completion, then that's going to allow me to prune the search as early as possible. 

The other thing that will help is as you search the tree, you also have to consider the order in which you branch over different solutions. Often we want to use the greedy ordering for that choice because if we do that, then we'll generate essentially the greedy solution as the first complete solution in our tree. That's going to be a decent solution. If we have a decent incumbent solution, that will lead to more effective pruning right off the bat. We're actually doing that. We ran the greedy algorithm to populate our tour array with the greedy ordering. That's the first order that we're going to consider. 

We are actually starting with effectively the greedy solution as our incumbent solution. Sometimes in other problems, this won't be relevant for traveling salesmen. It could be the case that as you explore different options, you actually reach one that's not even a feasible solution to your problem. The problem might have some constraints involved with it that you violate at some point. Of course, you should prune your solution anytime you reach an infeasible solution as well. This is called branch and bound search because every step, you're branching over different next possible decisions, and you're bounding the search as you go. We need to figure out what's a good way to prune our search. 

Let's maybe do something simple just to start with. If I go back to my coding view here, then how can we prove our search? I'll go back to my recursive solve function. I'm building up the length of my tour. Maybe a simple thing that I could do here is I'll just say, if the length of the tour that I've built up so far for the partial solution that I'm considering, if that is already worse than the best length of a tour that I've stored, then I can return. If I ever reach a point where the partial solution I've built up is already worse than the best complete solution I've found, I can stop. Well, at some point, I also want to consider this kind of lower bound on what it's going to cost me to complete my existing solution. 

For example, I know that I'm going to have to at some point, go back to the start. I know that I might have to go back, and at least the cost of completing my tour is going to involve the distance from the last city in the partial tour back to the starting point. I could look at the distance from tour of index -1. That's the last city on my tour, the tour of zero, because I know that at least the remainder of the tour is going to have to incorporate that amount of distance. If the cost of the tour that I've built up so far, plus some accrued lower bound on how much it's going to take to complete my tour, if that's already worse than the best solution I've found so far, then prune the search. What does that do in terms of my ability to solve larger problems? Thirteen was the stopping point from before. 

It looks like 13 is actually much faster now because we're pruning things from the search. Fourteen is actually quite fast, 15 is similarly quite fast. We're actually getting quite a bit of leverage out of just this simple tree pruning approach. Sixteen is not bad. I expect we're probably going to run out of luck before too long. Seventeen is starting to show signs of weakness. We can get up to maybe about n=17 with this level of tree pruning. 

To be able to make things run even faster, we have to come up with more clever ways to effectively prune our search. Remember that when we're sitting on a partial solution here, it's really about trying to find a good lower bound on what is going to be the cost of completing our partial solution. Is there some stronger bound we can put there that will let us prune more aggressively? I think there actually is. If I go back to my whiteboard picture, then let's look at this picture here where I've already visited cities zero and two. I'm still considering what cities I want to visit going forward. That's my partial solution. 

I've already visited. Here's City 0, here's City 2. My tour just looks like this. I've only taken one step of my tour, going from City 0 to City 2, and then the remaining cities, one and three, and four are still out there. I still have to choose among them, what's the right order to visit those cities. I still have a number of steps that I have to take in this tour. I have to take a step at some point to city Number 1. 

At some point, I have to take a step to city Number 3. At some point, I have to take a step to city Number 4. I guess at some point I also have to take a step back to city Number 0. What about the cost of those steps? I know at some point I'm going to have to take a step to City 1. At some point, I have to take a step to City 3, at some point, a step to City 4, I guess at some point, a step back to City 0. The best I could hope for with those steps would be if I step from the closest city to City 1 and from the closest city to City 3, and the closest city to City 4, if for every city, I find the distance to its nearest neighbor, and I add that up for all of the cities that I haven't yet stepped to, then that's going to give me a lower bound on how much remaining distance I'm definitely going to have to add to my current tour length in order to finish up the tour. 

I need to add the closest distance of City 1, the closest distance of City 3, the closest distance of City 4, and the closest distance of City 0. Let me go ahead and calculate maybe an array of closest neighbor distances of all of my cities, and I'll use that to help me make this more effective lower bound. I'll switch back over to my coding view here. What I wanted to do was come up with a vector of maybe doubles, I'll call it closest, that's going to just tell me the closest distance. Closest of i is the nearest neighbor distance for city i. That should be pretty easy to calculate. I'll just do that in my main function before everything gets started. 

I'm going to just loop over. Let's see. Closest is going to initially be a vector of doubles of length N. I'll initialize the distances to some big numbers to start with and then I'll loop over all the options and set them to the minimum closest distance as I do a double four loop here. I'll loop over all my cities, and I want to calculate closest of i. To do that, I'll loop over again all the choices for my nearest neighbor. For all of the choices, I guess, that aren't i because my nearest neighbor can't be myself, I'm just going to set closest of i to a running minimum, among all of these different choices and the distance between i and j. 

I've calculated the closest distance from city i to all other cities that are not city i. Now, I need to basically use this to get a lower bound on the completion cost of my tour. If I'm going back to my solve function, I'm going to prune the search if the current tour length of what I've built up so far. I'll replace this term here, plus my bound on completion cost. I'II call that completion length. If that's worse than the best solution I found so far, then I can prune. What is this bound on completion length? 

That's going to be the sum of the closest distance for all of the cities that remain in my tour. I think it might actually be the most convenient to also keep track of that as I go during the recursive process. I'll just pass that in as another parameter, completion length, because initially, that lower bound is going to be just the sum of the nearest neighbor distances of all the cities. If I just calculate the sum of all these closest values, I guess it'll be a double not an int. As I calculate these things, I'm going to say that, what do I call it? Total plus equals closest of i. I'm going to add up all of these closest distances for all the points, and that's going to be my initial bound on my completion length, just the sum of the nearest neighbor distances for everybody. 

Then as I recuse, every single time that I step to another city, that I add another city to my tour, I'm going to take out its closest distance because it's no longer added up into that completion length. I'm just going to take my completion length and subtract out the closest distance of the city that I just added into the tour, which is tour of index in this case. I'm actually going to be keeping track of this bound on completion as I rehearse. Every single step, I'm adding a city to my tour, which means I'm taking it out of the completion bound. I'm taking out its contribution to that sum of closest distances. I start with the sum of all the closest distances. This should give me a much more robust way of pruning my search with a much stronger bound on completion cost. 

Let's see what happens if we compile this. I guess we were able to solve n=17 before, so let's see how much more leverage we get from that additional lower bound. Seventeen is now pretty fast. That's good. How about 18? Eighteen is not bad. This extra lower bound does indeed let us prune the search much more effectively. 

Nineteen is starting to show a few signs of possibly slowing down a little bit, and I suspect 20 is going to have a bit of a pause. Not too bad. We're actually able to solve slightly larger instances with our more effective lower bound here. Twenty-one is thinking about what it's going to do for a minute there. We've actually managed to improve our situation quite a bit. We started with just n=12 or 13 with our bare bones recursive search that didn't do any pruning, and we've managed to extend that up to n= 20 or 21 and get still optimal solutions for the traveling salesman problem. This is probably a good point to finish things up. 

I think this illustrates nicely the power of pruning your searches whenever possible. The key though is to come up with effective bounds that can help you get a lower bound on completion cost so that you can prove as early as possible in the recursive exhaustive search process. I'll leave this here. Hopefully, this has been an informative discussion.