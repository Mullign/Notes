![[Dynamic Programming Examples Part 2.pdf]]
Our next problem is a true classic in the world of optimization with many applications across different domains. This is the Knapsack Problem. Here we're given a set of n items. Each one has a size and a value, and we'd like to find a maximum value subset of those items fitting into a capacity C knapsack. It's a very natural problem. It also interestingly turns out to be an NP-hard problem. We don't know how to solve it with a running time that is legitimately a polynomial running time. 

The running time of the algorithm we're going to come up with is going to be on the order of n times C. Because of this direct dependence on the knapsack size, that's why it doesn't technically count as a polynomial running time. That's not to stop people from using the algorithm. It's an eminently practical algorithm as long as C isn't too large. Let's come up with a formulation for solving this problem. This will be the first example of what we can call a two-dimensional state space of sub-problems as opposed to the more one-dimensional examples that we've talked about so far. One other important note, I'm going to have to assume that all of the item sizes are integers. 

We'll see why that's important in just a second. If I go over to my whiteboard view, then let's design our state space of sub-problems. It's going to be relatively straightforward. I'm going to have a sub-problem for every i, j pair, and the definition here is going to be this is the maximum value maybe using only a subset of items 1-i, and a capacity j knapsack. We have so far talked about one-dimensional sub-problem of state spaces, and the nature of those has usually been, what's the optimal solution up to this point? What's the optimal solution of a prefix of our full instance? That's really what's going on here just in two dimensions at the same time. 

We're saying, what's the optimal solution that we could get using only a prefix of our items and using only a prefix of our knapsack? Only j units of the knapsack capacity in only i of the items. How are we going to solve these problems? We can actually think of solving these problems as filling in a two-dimensional table of sub-problem solutions where the i dimension goes from 1-n, and the j dimension goes from 1-C. We're going to fill in this table row by row as we loop over all of our sub-problems. We're going to loop from i = 1-n, and then we're going to loop inside that for j = 1-C, and inside that, we'll just compute M(i, j). Filling in each of the i, j entries in this table. 

How do we compute of M(i, j)? That actually turns out to be relatively straightforward. What we're going to do is take the better of two possibilities, so the max of two options. The first option is if we don't use item i, and the second option is if we do use item i. I want to figure out the optimal solution in both of these two cases, take the better of the two, and that certainly should give me the optimal solution overall. If I don't use item i, that's actually the easy case because then I'm just left with the sub-problem of how I use optimally a subset of all the items up through i - 1 to fill a capacity j knapsack. I've already computed that optimal solution. 

That's just the sub-problem in the row directly above me that I've already solved. What if I do use item i? If I do use item i, then, well, I get to count its value towards my solution. I get V_i +, and now I need to figure out what's the residual sub-problem that I'm left with if I use item i. I'm still allowed to use any subset of the remaining i - 1 items, how much capacity is left. I guess I have j minus the size of i worth of capacity left. This is where it was important for the item sizes to be integer valued because now j - S_i is an integer. 

This does indeed map to a problem that I have solved before. You can imagine if the item sizes were not integer valued, then I'm not really discretizing in the capacity dimension properly, and this value here may not be an integer, so it may not really correspond to any sub-problem that I've already solved before. It's important to have discrete values along at least one dimension, here the size dimension. We could also have reformulated this with integer values in the value dimension as well. Maybe I'll let you think about how to do that as an exercise. But if we look at this formulation, it takes us constant time to compute the solution of every sub-problem, and I have n times C different sub-problems that I'm solving. That's what gives me my running time of order of n times C to solve the knapsack problem. 

Relatively straightforward formulation, just a two-dimensional state space of sub-problem solutions. Moving along, our next problem is also a very prominent problem, I guess, two problems that are very closely related to each other that have a large number of applications in several areas of practice in data analytics, computational biology, quite a few. The idea here, the motivation is that it's easy to calculate the similarity between two objects if you look at the cost of transforming one into the other. If it's easy to transform one object into another, those two objects were likely very similar to each other. There's a lot of ways we can measure similarity by basically measuring min cost of transformation. Depending on the types of object, that gives us a different flavor of problem. With the edit distance problem, we're trying to find the minimum cost of transforming one string into another string, where we can do operations like insert a character, delete a character, or modify a character, and each of those has a corresponding cost associated with it. 

In the dynamic time warping problem, we're trying to transform one time series into another time series by stretching it out or compressing it a bit so that the two time series align in an optimal fashion. Let's talk about these two problems one after the other and see how to solve them with elegant dynamic programming formulations. Maybe we'll consider edit distance first. The idea here is I have two strings, A and B. I have string A and string B. Let's say string A has length m and string B has length n, and I would like to transform A into B, and the operations I'm allowed to use when I do that are inserting a character, deleting a character, or modifying a character. Those have corresponding costs associated with them. 

Maybe to insert a character, the cost of each insert is going to be maybe C_i. If I want to delete a character, maybe the cost associated with that is C_d. Maybe if I want to modify a character, then the cost of that, I'll just say is C_m. Actually, the modify cost could even depend on what character I'm modifying to what, because it might be more or less expensive to transform some characters into some other characters. Maybe in computational biology if you're looking at two DNA sequences and trying to figure out how similar they are, it's probably more likely that certain DNA bases get replaced by other DNA bases if you're trying to think of ways that one sequence could have mutated into another sequence. What's the minimum cost of transforming A into B? We're going to use a very similar idea with the layout of our two-dimensional state space of sub-problems here. 

I'm basically going to look at a prefix of A, say up to position i, and a prefix of B up to say position j, and say, just what's the minimum cost of transforming that prefix of A into that prefix of B? My sub-problems are going to have the form maybe M(i, j), which is going to be the min cost to transform this prefix of A, so A of 1-i into B of 1-j. How do I transform just those two prefixes into each other, and then as I look at larger and larger sub-problems, eventually I'll solve the overall problem of transforming all of A into all of B. I'm going to have again, two nested four loops. I'm going to loop for i = 1-m. That's the size of A. Then inside that, I'm going to loop over all js from 1-n, that's the size of B. 

Inside this, I have to calculate M(i, j). How do I compute the optimal solution for transforming a prefix of A into a prefix of B? This is going to end up being a minimum of basically three different cases. Three different cases corresponding to insert, delete, and modify. I guess one case of modify that also is causing me not to have to do four cases here is I could leave a character alone if the character doesn't require modification. I could make that a special case of modify with the cost of zero if I'm transforming a character into itself, essentially. I need to think of how to write these cases. 

To do that, let's maybe draw a picture of this prefix of A up to position i, and this prefix of B up to say position j. I might have to actually use this picture a couple of times. Let me see if I can actually maybe copy and paste the same picture and not have to redraw it a couple of times. Let me see if I can make this actually work. My computer has been a bit slow here. Well, that didn't work superbly well. We'll make do with this. 

Let's consider transforming the prefix of A up to i and the prefix of B up to j. One option is that I could involve an insert in the way I'm doing this. The cost is going to involve the cost of inserting a character, and then what sub-problems will remain after I do this. The way this is going to look is I'm going to actually transform all of A up through position i into all of B, but only up to position j -1. Basically, the jth character of B is the character that got inserted in this transformation. I transform all of A up to position i into the part of B up to j -1, and then the jth character of B is what materializes as a result of that insert operation. If I want to look at similarly the situation where I had a delete, that's going to give me the cost of a deletion plus the residual sub-problem of transforming A up to position i - 1 into B up to position j. 

Here, what it looks like is I delete the ith character of A, and then I transform everything left in A into B up to position j. Then the final option is going to be, again, if I redraw this picture, A up to position i and B up to position j. Then I'm going to actually have a modify operation, C_m of two things, and the resulting residual sub-problems are going to be A up to i - 1 and B up to position j - 1. In this case, I look at these two last characters. That's what the modify is going to affect. The modify operation is going to change the ith character of A into the jth character of B, and so that's what actually goes here. This is going to be A(i) and B(j). 

Because I'm changing the ith character of A into the jth character of B, and then I am solving the residual sub-problem, the leftover sub-problem of transforming everything that's left into everything else that's left in B. These are my three cases, and if you look at the running times here, it still only takes constant time to solve every single one of these sub-problems. I have m times n of these sub problems. The total running time here is going to be just order of m times n to solve the edit distance problem. Not too bad. Let's maybe have a look at the related dynamic time warping problem. We'll actually develop a formulation almost the same for that problem. 

Let's go ahead and clear some space here. Our application here is going to be looking at two different time series. I'm going to have maybe a time series, let's call it f and g are my two time series, so f is going to be a time series of length m, and g is going to be a time series of length n. Very similar to our string example from before. Now I would basically like to align these two time series in the most likely way. Typically what this does is, f is a time series that represents some waveform, and then g is presumably a similar shape waveform, but it might be stretched out or squashed in areas, and we'd like to find the most likely way to make these different parts of these different time series correspond with each other. What we're going to do is basically for every single element in f and every element in g, we're going to draw a correspondence. 

Maybe the first two elements correspond with each other, and then maybe I have something like this picture. Basically every single element has to have a corresponding element that it's paired with on the other side in the other signal. The lines for these correspondences, they're not allowed to cross over each other, and so I need to basically figure out how to optimally align the time series f with the time series g. Basically trying to match up the values in f with the corresponding values in g that are the closest numerically to each other. That's my goal is to find this warping path or this correspondence between the elements of f and the elements of g. I'm not allowed to draw a crossing pair of these corresponding assignments here. My formulation is going to look quite similar in this case. 

I'm going to have a subproblem for every prefix of f and every prefix of g up to say position j, so my subproblems maybe are again of the form M(i, j). This is going to be the minimum cost of aligning. Say f of everything up to position i, with g of everything up to position j. Exactly the same structure in my subproblem decomposition here. Now, how do I compute M(i, j)? Again, I'm going to have two for loops for i = 1 up to n, and for j = 1 to n. Inside those two for loops, I'm going to calculate M(i, j). 

How do I do that? Well, if I'm going to align those subproblems, let's draw a picture of that. I'm going to have f of everything up to position i and g of everything up to position j. I know that if I'm aligning these things, then those last two items, the last value in f and the last value in g, those actually have to be matched with each other. I have to have a correspondence between those items. I know I'm going to have to pay the cost of aligning those two specific values. What is that cost going to be? 

That cost is usually just the difference between those two values. I'm going to have maybe the difference between f(i) and g(j). Maybe the absolute value of that difference. That's the cost of making those two values align with each other. Now, how do I make the rest of everything align? Well, options here might include, for the rest of the alignment, I could have maybe a case like this where g(j) actually aligns with other things in f, or I could have a case where f(i) aligns with other things in g, or neither of those two things could happen. I'm actually going to again have three possible cases. 

Where the three cases are actually quite similar to what we saw with edit distance. I'm going to have either M(i-1, j), M(i, j-1) or M(i-1, j-1). Those correspond to the three possible cases regarding what other alignments involve those final two items. Just to draw one of those pictures maybe. Because I'm running a little bit out of space. If we look at one of these cases right here, so let's look at the case of M(i, j-1), so what's going on there? I know that I have aligned the last characters. 

I know that I have aligned the ith character of f with the jth character of g. This subproblem is telling me M(i, j-1), that's saying that I'm aligning all of f, everything up to character i with g up to position j-1. That means that I'm also going to have this alignment right here, basically. The ith item in f is also going to be connected to the j minus first item in g because I'm aligning these subproblems next, basically after I peel off that last pair of items. I get a very similar formulation here for dynamic time warping as I would have for edit distance. Each subproblem takes me just constant time to solve, and I have m*n such subproblems, so my total running time again is going to be order of m*n. Lots of applications of this particular problem, if you're trying to match up waveforms that you expect to be similar or match trajectories of things that you expect to be similar. 

I've used this in my own research looking at brain wave data, trying to figure out if an EEG waveform measured on somebody's head is similar to a template that you would expect to indicate that they have a neurological disorder like epilepsy, for example. Lots and lots of different applications of these particular problems. We're now on our very last problem of the lecture. This one is a fun one. This is binary search, good old binary search, although with asymmetric guessing penalties. It'll cost us a different amount if we guess too high versus too low in our application of binary search. That will bias our choice of where we make our guesses to try and spend as little money as possible. 

We still, as with normal binary search, want to search a sorted array for a target value, but we want to minimize the amount of money that we spend. The exact way that we're going to phrase this is, what is the minimum amount of money that I need in my pocket so that I can guarantee that I find the target element, irrespective of where it lives in the array? You'll note that I could just run regular binary search for this application, and since every guess costs me no more than two, then my log N guesses from a normal application of binary search will definitely never cost me more than two log N. The answer here is never going to exceed 2 log N. But I need to come up with a formulation that gives me a guessing strategy that will minimize the amount of money I need to keep in my pocket to guarantee that I can successfully guess where the target is. Let's take a look at how we might formulate that problem. I have my array that I'm going to be searching. 

Well, what should I decide are my subproblems here? Maybe as binary search runs, we're going to be narrowing our search down to narrower and narrower intervals, and so a snapshot in the middle of that process, we're going to have some interval from i up to j that we've narrowed our search down to. Maybe I have a subproblem of the form, maybe M(i, j) is going to be the minimum amount of money I need in my pocket to be able to definitely locate the target element if it lives in the range i to j. What's the min amount of dollars that I need to guarantee finding the target within this range from A(i up to j). How would I then solve each of these subproblems in terms of smaller subproblems? What does a smaller subproblem mean here? The hardest biggest subproblem is M(1, n). 

That's the entire array. How much money do I need to guarantee finding the target element within the entire array? That's a big hard problem. The easier problems are actually the very small intervals here, actually M(i, i) that's really trivial. That's a base case, that's searching within an array of just size 1. What I'm actually going to do in terms of order of computation is I'm going to calculate basically all of the M(i, i)s, all the subproblems that represent intervals of length 1, then I'm going to calculate all of the M(i, i+1)s. That's all of the windows of length 2, and then all of the M(i, i+2)s, and so on. 

Basically, I'm looking at window sizes that increase and the very last problem that I solve is going to be M(1, n) the largest possible subproblem that I can consider. That's the order in which I solve these subproblems. Now how do I actually compute the optimal solution of one of these subproblems? What is M(i, j) written as some combination of, I guess M(i, j)s for smaller intervals. How does that work if I have this interval from i up to j, then I have to make a guess within that interval. That's how binary search works. I have to actually guess maybe some value of k within that range. 

Then the result of the binary search is going to be that my guess is either correct or it was too high or it was too low, and there's a different penalty for each of those two cases. Remember that if I have a guess that's too low, that costs me one dollar, and if it's too high, that's going to cost me two dollars , just to remember that. The outcome of my guess, if my guess is too low, that means that the resulting interval that I'm going to have to search goes from my guess k+1 up through j. If my guess was too high, then I'm going to be searching an interval from i up to k-1. Those are the two options that I might end up with. I don't really have any control over which of those two things is the result, so I have to make a worst case assumption that whichever of those two results happen is going to be the hardest possible situation for me to continue to resolve. What I'm going to actually try and do is pick the best possible guess. 

I'm actually going to minimize over my choice of k in this range between i and j. I'm going to try and pick the best guess, but then subject to that guess, I'm going to have to assume the worst case about which side I end up on if I guess too high or too low. I don't have any control over what the answer is, and so I'm going to assume the worst in terms of those two possible outcomes. The max over what happens if my guess was too low or too high. What are the costs in either of those two cases? Well, the cost, if I guess too low, that was one dollar, and then what's the residual subproblem that's left? I have M of, if I guess too low, then the resulting range goes from k+1 up to j. 

Then if I guess too high, that was a two dollar penalty, and the resulting range goes from i up to k-1. These are the subproblems that I have left. I spend one dollar, and then I have to have this much money left to be able to guarantee finding the target element within that resulting range, or I spend two dollars, and I have to have this much money left to be able to guarantee finding the element within the resulting target range. I take the max of the two of these because I have to consider in the worst case, I still need to have enough money, irrespective of which direction the too high or too low decision works out to be. This is my subproblem decomposition. What's running time is this going to lead me to? It so far doesn't look terribly good because well, the maximization is going to be just a constant time operation over those two quantities. 

But then I'm minimizing over a choice of a linear number of things, so computing the result of this min is going to take order n time. How many subproblems do I have? Well, I have a two dimensional state space of subproblems, so there's order n^2 squared subproblems, each of which takes order of n time to solve. At the moment, it would appear that my running time is order of n^3. Which is not a great result to start with. However, let's see if we can actually come up with a way to make it a little bit faster. One key observation here is that it actually doesn't matter what the range is in terms of the i value and the j value defining the range. 

All that really matters is the size of the range that I'm searching because it doesn't matter where the range is. All it matters is how many elements are in the range, and so I really don't need a two dimensional state space of subproblems at all, all I really need is just the size, let's say s of my range. I'm going to redefine my subproblems to be M(s) is the minimum amount of money needed to guarantee finding the target element within a range of size s within a sub array whose length is s. That will actually make things a little bit easier. Let me maybe clear off a little bit of space so that I can reformulate things. I'm actually now going to have just a single for loop for s = 1 up to n. I'm going to compute M(s). 

It's a little easier now to actually loop over all of my subproblems. What is the value of M(s)? This is the minimum amount of money that I need to guarantee that I can find the element that I care about within a range of size s. Again, I'm going to figure out what's the optimal place to make my guess? I'm going to guess somewhere within that range, a value of k between one and s, anywhere from the first position in the range up to the sth position in the range. Actually the same mathematics applies here. If you look at what can happen as a result of the guess, we have basically the same result, it's either too high or too low, and I don't have control over those two options, and so again, I'm going to have to have a maximization over the cost in both of those two cases. 

I'll actually end up with something very similar to what I had before. If I guess too low, the cost of that is one, and then how big is the resulting range size? That's this range right here. I guessed too low. Everything above K. I think the range size here is going to be size of I guess s-k is the size of the resulting range on the upper side. Then if I guess too high, that costs two, and then the resulting sub problem is of size k-1 because well, basically, it's everything from the beginning of the range up to k-1, our options in that case. 

Almost the same formulation, it's just now it's one dimensional because I really didn't need those two end points I and J. All I really cared about was ultimately the size of the range. Now if you look at the running time involved here, it still takes me order n time to solve each of these minimums, but I only have a linear number of total sub problems, and now my running time is down to order of n^2 which is quite nice. Now, we might be content to finish things at this point, but in keeping with this being the grand finale of our problems, let's see if we can get a little bit more mileage out of our efficiency here. Let's see if we can get the running time down even further. What I'm going to actually do now is take a very close look at the structure of this minimization /maximization, this calculation here where I'm computing the value of each respective sub problem. Let's maybe color code things. 

This function right here, I'm going to color in red, and this other function here, I'm going to color in green. Let's draw a picture of those functions on maybe a set of coordinate axes and see if we can understand a little bit about the structure of this problem that will let us solve things more efficiently. If I look at the red function, actually, let's look at the green function to start with. This is the m( k-1) function here. If I plot that, this is going to be the function 2+m( k-1). The important observation here is that is M increasing or at least not decreasing because if you increase the size of the range, it's going to take more money to search that range. We have a nice monotonicity property going on, where if you look at these sub problem solutions, they will increase over time,2+m( k-1), if this is an axis where I have all the possible values of K, then plotted as a function of k, that's going to be an increasing function. 

What about this red function, 1+m(s-k)? That has a negative dependence on K. That's actually going to be a decreasing function over values of K. This is the function 1+m(s-k )as a function of k. K goes up, this function actually goes down because s-k is decreasing. That's interesting. I have these two functions, and I'm looking at the maximum of those two functions. 

That's their upper envelope. That's basically if I trace out the max of those two functions, then that's this dotted line right here. I'm now looking at trying to find the value of K that minimizes that maximum. On my axis here on my K axis, I'm trying to find this optimal value of K, that is the minimizer of that envelope. You can see where that happens. That roughly is where the green and the red functions cross each other. This actually gives us a way that we can calculate that minimum more efficiently than doing it in order n time. 

I don't actually have to try all these different values of K, I can actually effectively binary search for that minimum. Because if I make a guess, maybe I guess here, for example, then all I need to do is evaluate the red and the green functions, compute 1+m(s-k),2+m(k-1) and I look at which function is on top. If the green function is taller than the red function at this point, I know my guess is too high. If I evaluate K at a point where the green function is below the red function, I know my guess is too low. I can actually binary search for the appropriate value of K that solves this first minimization problem. That's going to reduce the running time for solving that minimization problem down to just order of log of n. Now my overall running time is going to drop to n log n because each of my n sub problems only requires logarithmic time to solve because I can exploit this nice structure to binary search for the answer. 

It turns out we can actually do a tiny bit better than that? Because we're actually solving these sub problems for increasing values of S. You can imagine I've just computed m(s). Now I move on to computing of m(s+1). How does that problem look in terms of its difference with the problem that I've just solved? If I go from, computing m(s) to computing m(s+1)? How does this picture change as a result? 

Well, I guess the only thing that really changes, I'm still looking for the value of K that represents the optimal crossing point of these two functions. However, S s+1. This function right here, the S term is now going to be an s+1, that's going to slightly increase that argument. That's going to slightly move up the red function. All that's really going to change when I move from computing of m(s) to m( s+1) is this red curve is going to move up slightly. What's that actually going to do to the crossover point? The point right I'm looking for for the optimal value of K. If the red curve moves up a little bit, then the optimal solution for k is going to move to the right. 

The crossover point defining the optimal value of K that I'm looking for is going to move to the right. This actually gives me another nice bit of monotonicity, because as I consider larger and larger values of S, the minimizing value of K for those sub problems is only going to be monotonically increasing as I go. I don't even need to do the binary search. All I need to do is just try stepping k forward in lock step as I increase through these sub problems through different values of S. As I increase S, I'm going to try pushing K forward, and I won't push K forward so far that it goes too far. It's still going to basically track that crossing point as I go. But over all of my sub problems, K is just going to scan once across this entire axis, and that's going to be a total amount of just linear work over solving all of my sub problems. 

What that's going to do is effectively reduce the entire running time. We're solving all of these sub problems to just linear because I don't even really have to search much for K at all. Over all the sub problems, the searching over K represents just one pass across the K axis in total. Now I have a running time that's down to just order of n, which is much better than the order of n^3 that I started with. Maybe we'd be content with order n, but this is supposed to be our grand finale problem. Could we even do better than that? It turns out that yes, we actually can do better than that. 

And my figure here is getting so crowded that maybe I need to start redrawing things. What I'm interested in computing is, I'm interested in solving for these sub problems of one up through of n. I've already mentioned that this function, the amount of money I need to search a range of a certain size, that's actually an increasing function. I'm interested in of n. What is that value going to be? I'm trying to find m(n). Remember that when we talked about the problem originally, I said that m(n) was at most to log N. The amount of money I need in my pocket is at most twice log N because I could always have just run a binary search, and every single guess costs me either one or two. 

If every guess ends up costing me two, then the log n steps of my normal binary search would end up costing me to log n. I know that the optimal solution here is relatively small. It's at twice log n. I know that on this axis, on the y axis here, that's a really short axis. It goes up to at most two log n. Whereas the x axis here, that's a little bit longer. That actually goes up to the value of n that I care about. 

What I'm actually going to think about doing is reformulating the problem to probe. I'm trying to figure out this function, its behavior. But I'm going to actually probe it from the y axis point of view instead of the x axis point of view. My current formulation is computing of one, m of two, m of three, and so on. It's like computing the values of this function for increasing values of my S parameter all the way up to n. I'm doing n different calculations to compute solutions of those sub problems. I have n different sub problems. 

Instead, why don't I try and redefine my sub problems according to the y axis here, because there's much fewer sub problems in that direction, and so I'll probe the function that way. What that is going to look like is the following. Previously, my sub problems were of the form m(s). This is like the minimum amount of money needed to search a range of size S. What if I flip that around now? What if I make another type of sub problem? I'll say maybe how about R of what's a good variable name here? 

How about Little m. This is going to be the maximum size of a range that I can with money. In the first case, II tell you the size of the range and you tell me how much money you need to search that size of a range. Now I'm actually flipping that around. I say, I give you a certain amount of money in your pocket. How big of a range can you actually search with that amount of money? That's basically saying, if I have a point on the y axis here with a certain amount of money, then what range size can I actually search with that amount of money? 

I'm actually interested in solving those sub problems now. I'm going to say n (4)m=1 up to at most two log n. I'm going to actually compute the R of M values now. That's going to be probing the function by stepping up in the y axis here for every possible amount of money. What's the size of the range that I can search? The minute that size of the range goes bigger than n, that means I have the ability to search the full array of size N with the amount of money that I have in hand. I only now have a logarithmic number of sub problems that I need to solve. 

How do I compute the solution for r(m)? This is the max size of a range that I can search with an amount of money M. Well, how do I figure that out? I would like to figure out, I guess there's two alternatives every time you make a guess, you either pay $1 and end up guessing too low or you pay $2 when you guess too high. What is the max size of a range that I can search with money? Well, one option, if I guess too low, I'm going to have m-1 money leftover. I could search a range of this size if I happen to have guessed too low. 

Alternatively, if I guess too high, I guess, then I've spent two units of money, in that case, I can still search a range of size r (m-2). Then I also have the possibility that the element that I actually guessed was the right answer. I think the answer for how much of a range I can search with units of money, it's just 1+r(m-1+r (m-2). Because every single time I guess, I could actually have the right answer. That's one of the elements that I could have searched in my range. Then I have the possibility of searching ranges of size r(m-1) or r(m-2 ) in the events that I had guessed too low or too high. This actually is really good news because I can solve these sub problems in constant time per sub problem and I only have a logaritthmic number of these sub problems. 

This brings my total running time down to just order of log n. We're getting the final solution of my problem. I think the takeaway message here is that the formulation of your DP solution really does matter. You can sometimes formulate solutions in different ways, and sometimes that has a pretty substantial impact on running time. If you have a lot of sub problems in one formulation and not so many in another formulation. Other takeaway message here is that a lot of times dynamic programming solutions have a lot of very special structure to them. There's things like monotonicity or whatnot in your sub problems that you can exploit. 

As you're solving a sequence of sub problems, you may not have to redo from scratch each of these minimizations. You can reuse some of the work from solving the prior sub problem to make each subsequent sub problem solve much faster. There's a lot of things you can do to speed up dynamic programming algorithms. This just highlights a nice example of how that can happen for one very well structured problem.