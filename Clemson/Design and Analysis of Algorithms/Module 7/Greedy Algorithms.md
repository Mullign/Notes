![[Greedt Algorithms .pdf]]
If we are lucky, we might be able to solve an optimization problem very quickly and very simply using a greedy method. Greedy algorithms are incremental construction algorithms that typically build up a solution to an optimization problem, one element at a time, in every step, adding the element that seems to result in the greatest short term gain towards our solution. Let's spend a few minutes talking about how to design and analyze greedy algorithms. As a simple starting problem, let's look at a problem faced by cashiers everywhere, making change for a certain amount of money. Suppose I have a cash register that has a bunch of one cent coins, five cent coins, 10 cent coins, and so on, and an unlimited number of each one. I would like to make C total cents worth of change using the minimum possible number of coins. I can think about this process as basically building up a solution via a sequence of decisions. 

What's the first coin that goes into my solution? Then what's the next coin that goes into my solution, and so on? If I look at the first initial high level problem that I'm trying to solve, and I'd like to make C cents worth of change with a minimum number of coins, then after my first decision, I'm left with a smaller residual sub problem that in this case, happens to conveniently be of the same form as the original, it's just a smaller problem involving making change for a smaller amount of money, again, with an optimal number of coins. This is a very common framework for optimization problems where you can think about building up solutions via a sequence of decisions. A lot of the algorithmic approaches that we use for solving optimization problems, they fit into this framework. With a greedy algorithm, typically, what we're going to do is make this first decision based on some very simple greedy choice rule that results in the best short term gain for our solution. We're going to just use the biggest possible coin that we can. 

That's our greedy choice rule. We make that choice, we don't look back and we just proceed to solve the resulting sub problem that we land on after making that choice. With greedy algorithms, the choices that we make are usually irrevocable, we commit to them, and that's why they're so simple and so fast to execute. The challenge, of course, is with greedy algorithms, they only look at the short term gain of their solutions. They don't look at the long term ramifications of the decisions that they're making. In many situations, greedy algorithms may not actually reach an optimal solution. They often reach a reasonable solution and we often use them in practice, nonetheless. 

But in many cases, they're not actually optimal. Here's an example of a set of coin denominations for which greedy does not give you an optimal solution. If you have coins of values one, four, and five, and you'd like to make eight cents worth of change, then the natural greedy algorithm of always just using the biggest possible denomination would give you a solution with a coin of denomination five, and then you're stuck with coins of values one, one, and one. You use four coins versus the optimal two coins, if you had been a little bit more longer term focused and you used two coins of value four. Later on in the module, we'll talk about some other approaches also based on this sequential decision model. We'll talk about what happens if you'd like to explore exhaustively every possible solution to a problem. Here, instead of just making that first decision greedily, we will loop over all possible options for that first decision, and that will land on a number of different sub problems and we'll recurs on those sub problems, looking at all possible next decisions and so on. 

We're going to fan out and basically get a tree of all possible solutions that we could construct in a step by step manner. That's going to lead to a much slower running time. Hopefully we can speed things up by pruning our search, but we'll look into how to do that in our coding discussion a little bit later on. Then after that, we're going to talk about algorithms called dynamic programming algorithms that also operate in this sequential decision mindset. But the way they operate is they typically solve all the problems that we have in a bottom up fashion. They basically take this big problem, divide it up into a bunch of smaller sub problems and solve those smaller sub problems in an increasing order of size so that the very last problem you end up solving is the original big problem that you care about, so making C cents worth of change. The benefit of that is that when it comes time to now solve that final big problem, we already have solved all of these smaller sub problems that are reachable by our first decision. 

Maybe if I use 10 cent coin, then the number of coins that I need to make change for the residual sub problem after that is only seven. Now it makes it really easy to make that first decision because I've already figured out the longer term ramifications of that decision. I'll make that first decision so as to land on a sub problem whose remaining solution is as optimal as possible, as minimal as possible. Here I would use a five cent coin because I only need four more coins beyond that point. We'll talk a lot more about dynamic programming and examples of dynamic programming algorithms later on in the module. Let's take a look at some sample problems that are approachable by greedy methods. As a caveat, when you study algorithms in a class setting, you might see lots of problems that actually are successfully approachable by greedy methods. 

But in reality, it's really only a small handful. There aren't that many problems out there for which greedy algorithms actually do give you the optimal solution. That doesn't stop us from using these methods in practice. They often give you reasonable solutions, but just be aware that if you're looking for problems out there for which greedy algorithms work, they're few and far between, just as a caveat. Consider this problem. I give you a set of intervals on the number line, and you would like to select a subset of those intervals containing as many intervals as possible. I have a set of n possible classes that I could attend. 

Each one is described by the interval that the class is offered in terms of the time of the day. I'd like to take as many classes as possible. I just can't take two classes if they overlap in terms of the time window in which their lectures meet. Let's see if we can approach this with a greedy algorithm. The way a greedy algorithm here would operate is according to sequential decisions, you'd have to have some a greedy choice rule for picking the first interval to be part of your solution. Once you've picked the first interval, now you can disqualify all the other intervals overlapping with it and you have then a smaller sub problem consisting of all the remaining intervals. Again, you'd want to pick one of those greedily and so on. 

All we really need to do is figure out how to pick the first interval and then everything else will follow from that decision rule that we come up with. Usually, if a greedy algorithm works for solving a problem like this, the greedy decision rule for the algorithm we develop will be something fairly simple and natural. Usually it's weird if the greedy choice rule is something very out of bounds. It's usually something that makes sense and is simple and natural. For this problem, a couple of possible greedy choice rules come to mind. Maybe we pick the shortest interval first as part of our solution. Maybe we pick the interval that overlaps with the fewest other intervals. 

It causes the fewest conflicts. Maybe we pick the interval that starts the earliest, trying to pick somebody that's out of the way as much as possible, or the interval that ends the earliest. This is the usual process of designing a greedy algorithm. You typically list out a couple of natural choices for how you could pick the elements in your solution. Now you have to go about basically trying to disprove these things by coming up with counter examples showing that they don't work. In this case, it turns out that the one that ends up working is the interval ending the earliest. If we repeatedly just pick that interval, then we will actually always get an optimal solution. 

But the way that we arrive at that design decision, is by disqualifying all of these other greedy choice rules by looking for counter examples. If you're trying to maybe pick the shortest interval first, well, that doesn't work in our first counter example here, where if you pick the shortest interval, then you only get one interval in your solution, whereas you could have had two intervals in your solution. If you pick the one that starts the earliest, then that could be a really long interval that basically disqualifies you picking any other interval. Here, if I pick the one starting earliest, I would miss out on the ability to pick three intervals in the optimal solution. Actually, the one with the minimum overlap count is even a little trickier to come up with counter examples for, but here's one where I guess every single interval here has an overlap count bigger than two, except for the one that I've circled. That one has the minimum overlap count. It only overlaps two other intervals. 

I would have picked it first, that at that point, now I'm doomed to a solution that only involves at most three intervals, whereas the optimal solution would involve four intervals. A lot of times when you're trying to find the right greedy algorithm, a lot of it is just looking at possible counter examples to disqualify greedy choice rules. Then when you get down to one that seems to always work, then it's a matter of trying to actually justify that it is in fact going to lead to an optimal solution. When we do have a working greedy algorithm, the benefit is that usually these algorithms are very simple and very fast. They usually just involve sorting our input according to some criteria. If we want to look back at the algorithm we came up with using the interval that ends the earliest, then how would we actually implement that algorithm? Well, we could just take our intervals and sort them by ending time because we care about the interval that ends the earliest. 

What we would do is we would pick the interval with the earliest ending time and then that would disqualify all of the intervals overlapping with this one. If we maybe look at the ending time of that interval, then every other interval that overlaps would be disqualified. This one would be disqualified, this one, and this one. Now from the remaining intervals, again, we would want to pick the one that ends the earliest. I guess that would be this interval right here. Again, we can move our dotted line forwards to the end of that interval and disqualify any other interval that starts before that dotted line. Basically, this is just sorting the intervals and then scanning through them in linear time, picking out the intervals that work and skipping over any of the intervals that have been disqualified by earlier choices that we've made. 

A very simple end log and running time we can achieve for this greedy algorithm. Greedy algorithms when they do work, they're usually very simple and very fast and very easy to implement. The challenge with greedy algorithms typically is justifying to ourselves that they do, in fact, always produce an optimal solution. That's what I want to spend the rest of our discussion here on is how do we mathematically prove that a greedy algorithm will always give you an optimal solution. Usually, it's easy to come to the point where we can't find any counter examples, but still we need to go to another level of rigor to be able to fully justify that the algorithm always gives you an optimal solution. Even if this isn't the case again though, remember that in practice, people still use greedy algorithms with the understanding that they may not give you an optimal solution, but they'll usually give you a pretty good solution to a problem. Maybe let's think about our activity selection problem. 

How would we justify that this algorithm gives us an optimal solution? What we typically do here is we set up a proof by contradiction. We say, well, what if the greedy algorithm that we have in mind isn't optimal? What if there's some other optimal solution lurking out there that's better than what the greedy algorithm produced? What we do is we kind of compare the optimal solution, this hypothetical optimal solution to what the greedy algorithm would have done. At some point, the optimal solution would have made a different choice than the greedy solution because the two solutions are different from each other. The optimal solution would have made a non-greedy choice somewhere along its construction of the elements that it comprises. 

We have to look at that location where the optimal and the greedy solutions differ and hypothetically say, what if the optimal solution made a choice that aligned more with the greedy algorithms choice and see if that could actually improve our hypothetically optimal solution, contradicting our assumption that it was optimal in the first place. It's probably easier just to work through things at the Whiteboard to see how this approach works in action. If we want to argue that our greedy algorithm for interval selection is indeed optimal, then let's look at an optimal solution for the same problem. Here's an optimal solution. I'll put quotes around optimal. Let's suppose this is different from what the greedy algorithm produced, so the optimal solution consists of a set of intervals. Let's compare that to what our greedy algorithm would have produced. 

We line up the hypothetical optimal solution with our greedy solution under the assumption that our greedy solution isn't optimal. These two solutions are different from each other. They might actually agree up to a point. Maybe they use the same interval for a while, but at some point, the optimal solution and the greedy solution are going to make different choices. What's going to happen at that point is, remember the greedy solution always ends up choosing an interval that ends the earliest. If you look at the two intervals that differ, the greedy interval is going to have an earlier ending point than the optimal interval at that location. What we can do is basically say, well, what if the optimal solution instead had chosen the interval here that the greedy solution had chosen? 

That's always a valid choice because the greedy interval ends earlier than the optimal interval. It's not going to cause a conflict with of the future intervals in the optimal solution, and it's not going to cause a conflict with any of the past intervals, because those also appeared in the greedy solution. I can basically pivot in the interval that was used in my greedy solution into this supposedly optimal solution without changing the number of intervals in the optimal solution. Now I've extended the agreement between greedy and optimal, I can then keep doing the same argument and eventually, I will have effectively transformed the optimal solution into the greedy solution, thereby proving that the greedy solution was actually optimal to begin with. That gives me a contradiction to my original assumption that greedy wasn't optimal. That's the general high level structure that you often try and do with these types of proofs. You line up a hypothetical optimal solution with the greedy solution, look at the place where the two differ, and then say, well, what if the optimal solution had made the greedy choice at that location? 

Can I establish a contradiction using that argument? Let's maybe look at one or two other quick examples. A nice example of a greedy algorithm in the realm of graph algorithms is Kruskal's minimum spanning tree algorithm. What's a minimum spanning tree of a graph? We start with a graph. It's just a bunch of nodes connected by edges and each edge in the graph has a cost associated with it, a weight associated with it. I would like to find a spanning tree of my graph, that's a subset of the edges that connects everything together, but doesn't have any cycles in it, so it's like a minimal connected set of edges, and I'd like to find such a spanning tree of minimum total cost. 

Prototypical application might be if I'm building an electrical network that connects together a set of cities, I'd like to find the minimum cost way of doing that where every single edge here describes the cost of building a connection between a pair of cities. Kruskal's algorithm is maybe the most famous algorithm, one of the simplest algorithms for computing minimum spanning trees. This is a very well-studied problem in graphs. All you do for Kruskal's algorithm is you sort the edges on cost and you consider them one by one in order of cost and you try and basically add them into the spanning tree that you're building up one by one. The only time you would skip over an edge is if it would create a cycle with the edges that you've already added to your solution. In this case right here, I've added maybe the two. The negative seven would be the lowest cost, and then the two, then the three, and then the five, eventually though I'm going to hit some of the higher cost edges like maybe this cost 9 edge, I would not add that to the solution because that would make a cycle with the two and the five that I've already added to my solution, so a very simple algorithm. 

If you look at a snapshot in the middle of the algorithm, you will have built up a minimum spanning forest along the way towards building a complete minimum spanning tree. Each step of the algorithm is going to add an edge to this collection of components you've built up that will join two of those components together. In the next step of the algorithm, I guess I would add the edge of cost 10, which would merge these two components together. Then I would consider the edge of cost 13, and I would skip over that because that would have created a cycle and so on. That is Kruskal's algorithm. It's relatively easy to show that Kruskal's algorithm always produces an optimal spanning tree. Let's see if we can actually prove that using the same structure that we used for the activity selection problem. 

I'll switch back over to my whiteboard view. Let's say that I take all the edges in my graph, and I'm going to list them in increasing order of cost. I have maybe edge 1, and edge 2, and edge 3, dot, dot, dot. This is an increasing order of cost. The order of cost that Kruskal's algorithm would have considered those edges. Let's suppose for the purposes of contradiction that Kruskal's algorithm doesn't produce an optimal spanning tree. Let's, again, line up the supposedly optimal spanning tree with our greedy spanning tree, the one produced by Kruskal's algorithm. 

Let's look at which edges are used in those respective spanning trees. It might be the case that for a while, the optimal spanning tree and the greedy spanning tree use similar edges, so I'm scanning forward in cost. As long as the two solutions agree with each other, that's great. But at some point, since I'm hypothesizing that Kruskal's algorithm isn't optimal, there has to be some disagreement. There has to be some edge, let's call it e_i, where the optimal solution and the greedy solution made different decisions. Remember that Kruskal's algorithm always picks the minimum cost available at any given point. At this point of disagreement, we know that it's going to be the case that it's an edge that Kruskal's algorithm picked, and the optimal solution decided not to pick for some reason, maybe it thinks there's some better options available in the future using some higher cost edges. 

What we would typically do here is we would say, well, what if the supposedly optimal solution actually used that edge? What if it actually made a choice that aligned with what Kruskal's algorithm, the greedy algorithm, had done? Let's try adding edge e_i into this supposedly optimal solution. If you look at this again, optimal spanning tree, let's draw a picture of that. My spanning trees look like a plateful of noodles, basically. Here's a spanning tree of my graph. I'm going to take this edge e_i and add it in to this graph. 

Whenever you add an edge into a spanning tree, here's edge e_i, adding an edge to a spanning tree basically closes off a unique cycle. If you look at that edge plus the edges on the spanning tree, now I've formed a cycle. I would like to ask myself for the edges along that cycle, where do they live in this picture right here? I actually argue that at least one of those edges has to live after edge e_i, has to have a bigger cost than e_i. They can't all be before e_i. Why is that the case? Why can't all the edges on this cycle live in the regime of edges before e_i in my sorted ordering? 

Well, if they did, then all of those edges would also be present in my greedy solution and that wouldn't make sense because Kruskal's algorithm chose e_i and it chose all of those edges, so Kruskal's algorithm would have had a cycle. Remember that Kruskal's algorithm avoids picking any edges that would have created a cycle. I know that at least one of the edges in my optimal solution on that cycle has to be later on in my ordering. Let's call this edge maybe e_j. E_j is somewhere on the cycle. It has a higher cost than e_i. What I'm going to do is I'm going to remove that edge from the cycle, pivot it out of the cycle, and that gives me another spanning tree, where I've added e_i and I've removed e_j. 

What's the net cost of doing that? Well, I've added a cheaper edge and removed a more expensive edge. That's going to actually lower the cost of my supposedly optimal spanning tree. That would contradict the fact that it was actually more optimal than the greedy spanning tree, or it would contradict the fact that it was an optimal spanning tree, which was the assumption that I had originally started with. This is a simple way of proving that a greedy algorithm is optimal again. Sometimes we call this an exchange argument. You look at how this hypothetical optimal solution lines up with your greedy algorithm. 

When you look at the first place where they make different decisions, you can make some reasoning, an argument, that basically shows that if the optimal solution had made instead the greedy decision at that point, it would have led to a more optimal outcome than the optimal solution had to begin with. That's the standard way that we set these types of proofs up. Let's look at maybe one more final example. The world of scheduling actually has a lot of problems in it that are actually approachable with greedy methods. Let's look at one of these scheduling problems that has a well-known greedy solution here. Here I have a set of jobs, I've got n jobs. Each job has a weight, which is its level of importance, and each job also has a processing time. 

I would like to assign these jobs to one machine in some order so as to minimize the sum of their weighted completion time. High weight jobs, very important jobs, I want to complete earlier. If a high weight job completes late, it has a very high completion time, that's going to make my objective very unfavorable. My goal is generally to put the high weight jobs as early as possible. Although if they have large processing times, that might also have a very negative impact on my schedule. How do I sequence my jobs to minimize the sum of weighted completion times? Maybe a nice way to think about how to develop a greedy choice rule here is to look at a couple of special cases. 

Imagine for a second that all the weights are one. If all the weights are one, then now I just want to minimize the sum of completion times. If you think about that carefully, you'd want to just order the jobs by increasing order of processing time to minimize that. What if, on the other hand, all the processing times are one so these are just unit length jobs? In that case, it's pretty easy to see that you'd want to just order the jobs by decreasing order of weight to minimize the sum of their weighted completion times effectively. Given that, we'd like to come up with one greedy choice rule that does both of these things in those two special cases. One obvious choice that comes to mind is to order the jobs by the ratio of weight to processing time so that weight 1 over processing time 1 is at least as large as weight 2 over processing time 2 and so on. 

That does turn out to be optimal. Why? How can we prove that this greedy algorithm gives us an optimal ordering of our jobs? Well, let's again try and set up some of an exchange argument that would show this. If we go back to the whiteboard here, then let's look at a so-called optimal schedule of our jobs. Let's see if I can get control of my whiteboard again. Suppose I have supposedly optimal schedule of my jobs, and I compare that to a greedy schedule of my jobs. 

Again, I'm, for the purposes of contradiction, assuming that the optimal solution here is different from what the greedy algorithm had output. If I look at my optimal schedule, that involves the jobs in some order. Then I compare that to the greedy schedule at some point in the optimal schedule, I'm going to have an adjacent pair of jobs, say, job j and job j+1 for which they're not ordered the same way that the greedy solution would have ordered them. Let's look at maybe the first place where that happens. Remember that the greedy algorithm is ordering jobs in descending order of weight divided by processing time. Here's the greedy order. Weight 1 over processing time is at least weight 2 over processing time 2 and so on. That's the greedy order. 

In the optimal solution, if we didn't adhere to that order, then that means that the weight of job j divided by its processing time would have actually been less than the weight of job j+1 over its processing time. We know that if there's an optimal solution out there that's different from the greedy solution, it's going to have a consecutive pair of jobs in it that are related in this way with the ratio actually increasing instead of decreasing. Now, why does that lead to a contradiction? Why does that lead us to believe that our optimal solution wasn't actually optimal? Let's take a look at that. For simplicity, let's assume that this point in time right here, I'm going to call this time T, just to simplify some of the math that follows. Let's look at our objective. 

Remember, our objective is to minimize the sum of the weighted completion times of all of my jobs. That's going to be the sum of the weight of job 1 times the completion time of job 1 plus the weight of job 2 times the completion time of job 2, and so on. How does that objective change if I go into this optimal solution and I consider swapping these two jobs to make the optimal solution look a little bit more like the greedy solution? If you look at your objective, actually not a whole lot changes if you swap the order of just two jobs, because only those two jobs might possibly change in terms of their contribution to the objective function. Every other job has the same completion time as it did before and after the swap. All of the terms in my objective function stay the same. If I swap two consecutive jobs like this, there's only two terms that might be different. 

What were the contributions of just those two terms pertaining to jobs j and j+1? Before the swap, those two terms contributed, well, the weight of job j times its completion time, which I guess is going to be T plus the processing time of job j because the completion time is right here, that's t plus the processing time of j. Then job j+1 is going to contribute a term that is its weight times its completion time, which I guess is T+p_j+p_j+1. Those are what the two terms in my objective would look like before I did the swap. After you do the swap, what are those terms going to look like? Now if I have job j+1 first, followed by job j, then I guess, the weight of job j, its completion time is now going to be the same thing that used to be the completion time of j+1. T+p_j+p_j+1 plus the weight of job j+1 times its new completion time, which is just going to be T plus its processing time. 

What is the change in my objective if I make that swap? It's basically the after terms minus the before terms because all of the other terms in my objective are the same, so they cancel out if I'm looking at the difference and objective according to just making that swap. What is that going to be? If I multiply these things out, there's going to be a lot of cancellation, actually. All of the terms involving T, I think are going to cancel, so none of those terms are going to survive. Also, there's going to be quite a bit of cancellation. I think there's a w_j, p_j term both before and after, so that's going to cancel. 

There's a w_j+1, p_j+1 term that's going to cancel. I actually end up with only a couple of terms left. I'm going to have w_j*p_j+1-w_j+1p_j. That is the net impact on my objective of swapping these two jobs in the optimal solution were ordered contrary to what the greedy algorithm would have done. What can I say about that quantity right there? Well, I know that I've assumed that w_j/p_j is less than w_j+1/p_j+1. If I just cross multiply, then this tells me that w_j*p_j+1 is less than w_j+1*p_j. 

Well, this right here, that's the first term and this right here is the second term. I know that this quantity is actually negative. If I make this swap, I'm actually going to improve the value of the supposedly optimal solution, I'm going to lower its objective value. I'm trying to minimize my objective here. Making this swap would have actually given me a better solution, thereby contradicting the optimality of my supposedly optimal solution that I started with. Again, we've done this exchange argument where we have shot down the supposed optimality of this optimal solution differed from our greedy solution. This just shows a couple of examples of how we can work through a more rigorous mathematical proof that our greedy algorithm does indeed give us an optimal solution. 

Lots of other greedy algorithms can be similarly analyzed with the same approach.