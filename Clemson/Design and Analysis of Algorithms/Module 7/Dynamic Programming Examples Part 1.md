![[Dynamic Programming Examples Part 1.pdf]] 
>> Even though dynamic programming is at its core based on a relatively simple premise, I've noticed that nonetheless, students can still take a decent amount of time building up a reasonable level of comfort with the technique. And of course, as with anything, the more practice you get, the better you get at building dynamic programming algorithms. So our goal with this video is to spend some time at the whiteboard and just work through formulations of a couple of common dynamic programming algorithms to get more familiarity with applying the technique. This may end up being a slightly longer video, but hopefully at the end of the day it will be worth it. As a starting point, let's consider a relatively simple problem, this is the good old maximum sum subarray problem. Given an array, I'd like to find a contiguous subarray within it, whose sum is as large as possible. We actually studied this problem back when we talked about divide and conquer. 

I think when we built an n log, n algorithm for it here, we'll build probably an even simpler algorithm that runs in only linear time based on dynamic programming. So what would the formulation for this problem look like? If we take a look at our array here, then, as is typical, most of the time when your input is sequential like given in an array like this, a lot of times your dynamic programming subproblems are going to be of the form, you know, what's the best solution up to position j? Or what's the best solution for a prefix of my array? Or alternatively, you can usually formulate things the other way around and say, what's the best solution from position I onwards? What's the best solution for every given suffix of my array? Maybe, we'll stick with prefixes for now. 

It's pretty easy to define our subproblems, M of j is just going to be the best possible window I can find the window with maximum sum ending at position j. So the max sum of a window ending at position j. And it is important to clearly write out exactly what your subproblems mean, because otherwise there could be some subtle ambiguities that make it difficult to take the next step, which is writing a formula for computing each subproblem in terms of the smaller subproblems on which it depends. So those smaller subproblems in this case are going to be as follows. So I'm going to be looping over the array from one up to n, calculating the value of each of these subproblems. So M of j should only depend on m of things smaller than j that I already will have computed during my scan. So how do I compute M of j? 

That's actually relatively straightforward, there's only really two cases that I need to look at, and I'll take the better of the answers. For those two cases. Either the best window ending at position j is going to be a single element, just whatever is in the jth position of my array by itself, or it's what you get when you extend the best window that ended at position j minus one at the previous position. So I can take M of j minus one. That's the best window ending at the previous position, and I extend that by adding the j element. So one of those two has to be the case for the optimal window ending at j. And therefore I can calculate M of j and just constant time. 

And I'm doing this for a linear number of subproblems, so my total running time is going to be order n very easily. Where does the actual answer emerge, when I finish running this algorithm? I guess I don't know where the optimal window overall is going to end. And so for the overall answer, I'm probably going to have to take the max of all of the M of j over every possible endpoint j to figure out what's the best window overall. So not too bad. This is a dynamic programming algorithm for our very first problem the maximum sum subarray problem. I like this problem because it's relatively simple and it's nice to actually consider possible extensions of the problem or consider alternative solution techniques just to kind of highlight some other approaches that might be possible using other tools we have developed in this class. 

Remember when we talked about range queries and such, that one of the tricks that we developed was the ability to take the sum of any window by taking the difference of two prefix sums. So the sum of a between i and j is a prefix sum up to position j minus a prefix sum up to position i minus one. That kind of cancels out this part, leaving only the sum of the window that we care about. We could, if we wanted to, have actually built a solution on this insight instead of using our dynamic programming approach. So I think let's talk about that for 2 seconds, because it's nice to see alternative ways to approach the same problem. So the sum of everything between i and j is Pj minus one. So suppose we'd like to find the sum of the best window ending at j, the subproblems that we just developed with our dynamic programming algorithm. 

That's going to be Pj minus something, and if we want that to be as big as possible, we'd like to subtract the smallest possible PI minus one term here. So basically finding the maximum window ending at j, that boils down to finding the smallest possible P of i where i is less than j. And you can actually compute that pretty effectively by just keeping a running min of the prefix sum values as you scan across your array. And so when you reach j, you're going to take Pj minus whatever. That running minimum value was the best possible Pi prior to j, and that gives you the best window sum ending at j. So another interesting way that we could have solved effectively the same problem, and we could have actually extended that solution, or even our dynamic programming solution to handle even more interesting variations on the original problem. So imagine that we had asked not just for the best overall subarray, but we only maybe care about subarrays with some minimum length. 

We only care about long subarrays. So find me the best subarray with the highest sum among all subarrays having minimum length l. And you can extend either of these two approaches quite easily to still run in linear time and solve the problem. In this case, I've written the solution for using the prefix sum approach, and maybe you want to consider separately as an exercise, how you might extend the dynamic programming approach as well. Here's another nice extension. Suppose instead of looking for a subarray of maximum sum that I'd like to find a sub array of maximum average value. If you think about this problem actually for a few seconds, you realize that it's actually, as stated, a little bit too trivial, because the optimal solution is going to be just the one single element in your array that has the largest value. 

If you make a window bigger than that, it's just going to dilute the average and make it worse. So here, to keep things interesting, we really do need to insist on the lower bound on the length of our window. So the question would be find me a subarray of length, at least i, whose average value is as large as possible. Maybe a practical use case for something like this would be analyzing image data, looking for prominent rectangular sub regions that have a large or small average pixel value. So looking for very large bright or dark spots in the image. We've already solved this problem for sums. How do we solve it for the seemingly more difficult objective of an average? 

That seems like it might be more challenging, but there actually is an elegant way that we can address the problem. And this gives me, as another aside, a way to bring up another cool trick in optimization that is fairly broadly applicable. It's the idea of binary searching on the answer to a problem. So a lot of times it's difficult to come up with the precise numeric value of the optimal solution of a problem. But it is somewhat easier, to test if that optimal. Solution value is above or below a certain guess or a cutoff. And if that's the case, you're basically just one binary search away from zeroing in on the actual numeric value of the optimal solution. 

You just binary search on the value of x. If we'd like to see how that applies in our present context, then remember we're trying to find if there exists a sub array of length at least l, for which the average value is maximized. And so to binary search, all we need to do is figure out if the average value of such a subarray is at least x. So does there exist a subarray of length, at least L, for which the average is at least X? It turns out that's actually pretty easy to do. So I'm interested in finding does there exist a subarray, say, from position i up to position j, for which the average value is at least X? I can write the average value of a subarray as just the sum of the subarray divided by its length, and the length. 

Here I've written in kind of a weird way as one plus one plus one, and so on for a number of terms equal to the length of the subarray. So there's the same number of terms here in the denominator as there are in the numerator. So how do I figure out if there exists such a subarray satisfying this condition? Well, I can just rearrange terms and get an equivalent question. So that's equivalent to asking, does there exist a subarray of length at least i, for which the sum of the subarray is at least X plus X plus X? And so on, where there's the same number of terms on the right hand side as there are on the left hand side. And then one final rearrangement, I'll move all the X's over to the other side. 

Now it's equivalent to asking, does there exist a subarray of length at least l, for which, if I add up the elements in the subarray, each with x subtracted from them, that I get a quantity that's greater than or equal to zero. This basically reduces the problem back down to the sum version of the problem. I subtract x off of everything in my array, and then I find a maximum sum sub array of length at least l. If the answer is at least zero, then that means the answer to this original question was also yes, that there was a sub array of average value at least x. And so then I can continue with my binary search. So this is kind of neat. I get to basically use the sum version of the problem over a number of steps to binary search on the answer to solve the average version of the problem. 

So anytime you have an optimization problem with a ratio objective, this is a technique to keep in mind that could be useful for solving it. Okay, let's go back to some dynamic programming, and we're going to consider as our next example problem a very prominent problem in the world of graph algorithms. We're going to find shortest paths in a directed acyclic graph. We'll talk about graphs in detail in the next module. So this is kind of a glimpse into one of the types of problems we'll be thinking about then. But here, a directed acyclic graph, it's, well, it's a graph that consists of nodes and directed edges. And the key thing here is there are no directed cycles. 

And one nice feature of dags, as they're called, which we'll learn about actually in the next module, is that in linear time, you can take any directed acyclic graph and you can order its nodes according to what's called a topological ordering, such that all the edges point in a consistent direction. They all point from left to right. And so we're going to do that as a pre processing step. We'll see how to do that in the next module. But we're going to assume for now that the DAG has been topologically ordered, so all of its edges point from left to right. My goal is now to find a shortest path from the source node, which I'm going to assume is the leftmost node here, to every other node in the DAg. Every edge here has a cost associated with it, and the shortest path is just a path that minimizes the sum of the edge costs across the path. 

So how do I solve this problem using dynamic programming? I'm actually going to use a similar sort of formulation to what I've done in the past. So let's go ahead and see what that looks like. In my dag, I have the leftmost node is the source node, and then I have all of the remaining nodes. Let's call those nodes 1, 2, 3, and so on. And let's go up to, say, node j. My subproblems are going to have the same form as I looked at in the past. 

I'm going to say, what's the best solution? Kind of up to position j. In this context, that's like asking, what's the shortest path from the source to node j. So I'm going to compute the shortest path from the source to node 1, and then from the source to node 2, and then from the source to node 3 as I loop over my nodes. So basically I'll have maybe p(j) is my subproblem. That says the minimum cost of a shortest path. I guess that's redundant. 

So the minimum cost of an s to j path, I just like to compute the shortest path to every node as I scan across my topologically ordered dag. And so the overall structure of the algorithm is going to be a for loop from j equals one up to n. Inside that for loop, I'm just going to compute the shortest path to each node j in sequence. How do I do that? Well, how do I get a path that reaches node j? The only way that I can do that is I can extend a path that ended at some prior node I. And so what I'm going to do is look at all the possible penultimate nodes on a path to node j that go through nodes I that are earlier than j. 

This is where I'm using the topological ordering to my advantage. I've already calculated the shortest path to all of those prior possible nodes i. So what I'm going to do is basically minimize over all choices of nodes i such that ij is an edge in my graph. So these are all the nodes I that I could go to first on a shortest path from the source, and then take an extra edge that hops over to j. Any path to j has that sort of structure to it. And so what's going to be my shortest path? It's the basically choose the best such penultimate node I such that you minimize the cost of the shortest path up to I plus the cost of that extra edge that's going to carry you from I to j. 

So this is my dynamic programming formulation, just minimizing over all the preceding nodes I that have an edge into j of the shortest path, cost of getting to I plus the cost of the edge that goes from I to j itself. So what would be the running time of this algorithm right here? I would actually classify the running time as order of n plus m, where in usually when we talk about graph algorithms, n is the number of nodes in the graph and m is the number of edges in the graph. Because as we scan across our instance here, we're definitely looping over all the nodes. So that's the order of n part. But during this minimization, we're actually looking at every single edge once as we visit the node to which it's incoming. So we're also just visiting every single edge once during the entire scan. 

So this is basically a linear time algorithm. Its complexity is linear in the size of our graph. That's the same running time that it would take to do the topological sort at the beginning as a preprocessing step. So very, very efficiently, we can compute the shortest paths in a dag. It's one of the special cases of the shortest path problem that we can solve extremely quickly. So that's kind of nice to know. The other nice thing about shortest paths in dags is this is kind of a generic problem. 

If you think about what dynamic programming is in general, dynamic programming actually is kind of a computation on a DAG. Because if you think about all the different sub problems that you've decomposed your problem into, they actually have a relationship between them that is of, you know, the structure of a Dag because you're kind of computing solutions of large problems in terms of solutions of smaller subproblems. So if you make every subproblem a node And you draw edges from smaller subproblems to the larger subproblems that depend on them. That overall structure will be a dAg. And so, computations on DAGs, and sometimes even pathfinding problems on DAGs, that's a good way to kind of think about and visualize other dynamic programming algorithms. In fact, if you want a nice way to draw pictures of DP algorithms, then pretty much every single problem that we have talked about so far can be visualized as a shortest or a longest path problem in a dag. The longest path problem in a dag is pretty much the same as the shortest path problem in a dag. 

You just minimize versus maximize, that's the only real difference. So, for example, if we go back to the case of optimal making change, like the coin changing problem, this is an example of a dag that represents the situation where we'd like to make $0.08 worth of change. And the shortest path through that DAG actually corresponds to the configuration of coins that we're using to make $0.08 worth of change. Through the denominations of coins I have available are a $0.05 coin, a $0.02 cent coin, and a $0.01 coin. The five cent coin corresponds to all of these edges that hop me ahead by five units. So the nodes here correspond to all of my different subproblems. Remember, how to make change for $0.01, how to make change for two cent, how to make change for $0.03. 

Those are all the different states that I'm considering, all the different sub problems that I'm considering. And if I'm in the start node, I haven't used any coins yet. If I use a five cent coin, that hops me ahead to state number five, because now I've used $0.05. So if I look at the path in question here, I use a five cent coin and then a two cent coin and then a $0.01 coin to arrive at my ultimate destination, which is the node for eight total cents worth of change. And so the shortest path through this graph actually will represent a minimum number of coins that I use to achieve my target amount of currency. So if I want to now look at maybe the maximum sum subarray problem that we started our discussion with, I can also represent that as a calculation on a dag. So I just take my array and I turn it pretty much directly into a dag. 

So every single edge here corresponds to the value of an element in my array. And then I have a start node and an end node, the start node connects to everybody, and everyone connects to the end node. And so to get from the start node to the end node, I can jump into the array, follow the array for a while, and then jump to the end node. And I would like to find the longest such path, and that's going to correspond to the maximum sum I can get from a subarray in my original instance, if I'm interested in looking at the interval problem that we talked about in the previous video. So find a disjoint collection of intervals whose sum of lengths is maximized. I can turn that into a dag problem as well. Finding a longest path in a dag, where now the, I guess the edge costs or the edge weights in my dag are going to be analogous to the lengths of the intervals that those edges are connected to. 

And a path through this DAG is going to correspond to the set of intervals that I choose for my solution. So I can have an edge from one interval to another interval if they're disjoint from each other, and one of the intervals follows the other. So again, basically, a path through this dag, a longest path, would correspond to a set of intervals that are disjoint from each other that have maximum total length if I add their lengths up. If I'm interested in the longest increasing subsequence problem, another problem that we looked at in the previous video, I can turn that into a dag calculation involving the longest path. So again, I can jump from my start node to anywhere in the sequence, and I can jump from anywhere in the sequence to my end node, and I have an edge from any node to a node ahead of it, if that represents an increase in value. So now a path basically corresponds to an increasing subsequence. And so a longest path from start to end would correspond to a longest increasing subsequence. 

So I could have actually formulated all of these dynamic programming algorithms kind of generically as pathfinding problems in DAGs, as specifically shortest or longest path problems in DAGs. And I could have taken the formulation that we just came up with for shortest path problem in a DAg and applied that to all of these problems. So this just gives us kind of a nice generic way to think about dynamic programming algorithms, if you like to be able to visualize algorithms. I think kind of visualizing DP algorithms as computations on DAGs can be a useful way of kind of looking at how the subproblems are configured and how they relate to each other and such. So just another nice tool to kind of keep in the back of your mind.