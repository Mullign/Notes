![[Module Overview.pdf]]Welcome to Module 7, the first of two modules where we talk about algorithms for the expansive world of optimization. This module focuses on discrete optimization problems, and later on we'll talk about continuous optimization problems. Let's briefly highlight some of the key points that we'll discuss in this module. First of all, this module is all about optimization. Optimization, as you would expect, involves finding the best solution, usually minimizing or maximizing some objective value from a set of feasible alternative solutions. In the world of discrete optimization, typically the set of feasible solutions that we're optimizing over has some discrete or combinatorial character to it. We could enumerate potentially all of the feasible solutions and pick the best one. 

However, oftentimes there's an exponential number of potential solutions, and so we wouldn't just do that. We wouldn't just enumerate all of our possible solutions. We would want to use a more clever algorithmic approach to zero in on the best solution of our problem. Optimization is incredibly prevalent in a wide range of applications. A couple of quick notes. A lot of times you're asked to solve, say, a system of equations. Solving a system of equations is almost the same thing as optimizing. 

They're kind of two sides of the same coin. So, for example, if you want to optimize a convex function, that usually involves taking the derivative and setting it equal to zero and solving the resulting equation. So optimizing can boil down to solving a system of equations. And conversely, if you would like to solve a system of equations, that often involves minimizing the error in the system of equations. And so you can solve by optimizing. If you study things like machine learning or other sorts of data science problems where you're trying to fit a model to your data, that basically at the end of the day boils down to just optimization. So all of machine learning and AI at its core is basically optimization. 

You're trying to find the best setting of your parameters in your predictive model such that you fit your data as best as possible so that you can predict well on your training data, for example. So that's often called regression, trying to find parameters such that your model is a good fit to your data. So optimization shows up in a very wide range of applications. In this module, as I mentioned, we're focusing on discrete optimization problems and algorithms, and we'll focus more on continuous problems and algorithms in a later module. So if we look at the examples on this particular slide, the traveling salesman problem is a wonderful example of a discrete optimization problem. So here you're given a set of n cities and you'd like to find a tour of those cities, basically a path that visits one city, then the next city, and the next city, and so on, and eventually comes back to where you started. And that tour should ideally have minimum total length. 

So that's the traveling salesman problem, a very famous problem, and we'll actually use that as the example problem for our coding demonstration in this module. These other problems that I've highlighted are actually more continuous in nature, just to highlight the difference between discrete and continuous. And so your generic continuous optimization problem would be just minimize a continuous function. And so we'll see how to do that in a later module. Also, we can talk about problems like regression problems, where you'd like to find maybe a line that fits your data as well as possible, or more generally, find the settings of parameters of a model that fit the training data for that model. And typically, that ends up in the realm of continuous optimization as well. And so we will address those types of problems later on in the course. 

But some of the ideas that we talk about in this module will definitely have a relevance to continuous optimization as well. And so sometimes there's kind of a gray area where you're studying problems that have kind of a little bit of a character of both discrete and continuous nature to them. So what are we going to talk about as main topics in this module? So we'll start out by kind of talking about the difference between so-called kind of hard versus easy problems. We'll talk about the notion of an NP-hard problem and complexity classes of different problems. So we can kind of talk more easily about when problems are computationally tractable versus not tractable. And that's a big deal in optimization because many optimization problems are known to be hard to solve efficiently. 

And so in that situation, you often can't hope to produce an exact optimal solution, but you might want to come up with a heuristic that generates reasonably good solutions in a short amount of time. So we'll talk about some standard types of algorithms for approaching optimization problems. Two that are very well known are greedy algorithms, which are very fast and can give you very good solutions to a very wide range of optimization problems. They're also quite simple. But the set of problems for which greedy algorithms give you optimal solutions is kind of limited. So if greedy algorithms don't work, we'll also talk about and spend quite a while going through examples of a technique called dynamic programming, which is kind of a recursive way of characterizing solutions of optimization problems where you can solve a big optimization problem by solving kind of smaller instances, kind of recursive instances of subproblems embedded within it. So we'll see lots of examples of how to use dynamic programming to solve a wide range of optimization problems. 

In the event that we're trying to solve a hard problem, we will look at various heuristic methods. These are methods that are not guaranteed to give you the optimal solution, but that's nonetheless tend to work well in practice. And so things like iterative refinements for trying to approach these hard problems and get solutions that are reasonably good in a short amount of time. We'll also talk about if you want to get the optimal solution to a problem and you're willing to spend a decent amount of time, how to do some sort of an exhaustive recursive search with pruning to speed it up. This is called branch and bound search. And we'll actually do this in our coding demo where we'll try and come up with exact optimal solutions for small instances of the traveling salesman problem. So that should be fun. 

And then we'll also talk about a few other key ideas just in the space of optimization in general that show up quite often if you study optimization or if you're trying to develop optimization algorithms for different problems. So we'll talk about duality relationships between problems and also the notion of a relaxing a hard problem to an easier problem as also a means of generating heuristic solutions. And then finally, we'll have an optional enrichment lecture on the topic of approximation algorithms. So another way to address hard problems is to come up with a fast algorithm that gives you a provably close solution to optimal. And of course, the challenge here is proving that you're close to an optimal solution that you can't really compute because the problem is too hard. So there's some interesting mathematical ideas involved with these approximation algorithms. And then finally, accompanying this module is going to be our final programming assignment of the course where basically we will give you a hard problem. 

So it will generally be impossible to find the optimal solution of this problem. And so your goal is going to be, you know, in kind of an open-ended way, trying to apply the techniques from this module to generate as good a solution as you can come up with in a short amount of time. And so you can use techniques like relaxation and rounding, iterative refinement, greedy methods, and so on to try and, you know, deal with this hard optimization problem. And this is quite realistic because in the real world, there are lots of hard optimization problems out there that we need to come up with good solutions for. This problem will vary based on the semester. And so there won't be a video introduction to it, but just a handout that describes the nature of the problem and kind of the input and what the expected output is going to be. So hopefully this will be fun. 

You'll get to kind of explore different methods for addressing these sorts of hard problems. So hopefully this is an enjoyable module. We'll learn a lot in this module about optimization and even more later on when we focus on continuous optimization. But hopefully this will, you know, expand your algorithmic toolbox in useful ways so that you can solve a very wide range of problems that are quite relevant in many areas of practice.