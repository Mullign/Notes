![[Skip Lists.pdf]]
[MUSIC] For those who appreciate the broad utility of binary search trees but who are maybe less enthusiastic about the level of implementation required to keep them balanced, the skip list might be a good alternative data structure for you to consider. We very briefly introduced skip lists back in Module 1 as a way of remedying some of the shortcomings of a linked list. So instead of having to painstakingly walk down the linked list, instead you can take these express lanes that are built on top of the linked list to find an element much more quickly. So you can think of it as a way to build a better linked list. Or you can think of it as a way of basically achieving the same functionality that you get from a balanced binary search tree but based on a very different, and, in some cases people might say, simpler design. So the picture really does explain how a skip list operates. You start with a linked list in its lowest level. 

I guess if you're implementing a set, that linked list would be in sorted order. If you're implementing an arbitrary sequence, then it would just be in arbitrary sequence. And then on top of that linked list, you build sparser and sparser copies, each one having roughly half the number of elements as the level below. And that lets you find an element much more quickly. Imagine we're trying to implement a set so that our elements at the lowest level here are in sorted order. So if I'm trying to find the element 19, then what you do is you start at the top level on the far left-hand side, and you always try and move right in each step. So you try and move to the right, you try and move to the right, but if the next step to the right would overshoot, then instead you move down. 

So you have a series of rightward and downward steps, every step trying to make as much progress to the right as you possibly can. And we'll see in a minute that that takes just logarithmic expected time to implement the find operation. And almost every other operation takes logarithmic time and expectation as a consequence of that. You can also do rank-based access, just like you would in a balanced binary search tree, with the select operation. So here you're looking for the element that would be at position K, or rank K within your underlying linked list in the lowest level. To do this, we have to do something similar to what we did with the binary search tree. Remember, in a binary search tree, to implement the select operation, we had to augment subtrees with their sizes. 

And that gave us enough information to help navigate down the tree to an element of a specific rank. Here we do something similar. We'd have to augment every single, basically, long-range link with what we could call its skip count, telling us how many elements we skip over if we take that link. And then the select operation operates in much the same way as the find operation. To find an element of a specific rank, you start in the top level and you try and move as quickly as possible across the list. Every single time, you would take a long-range link, unless that would overshoot. And you can keep track of whether you would overshoot by keeping track of the rank that you're currently at by adding up the skip counts that you're following along that top-level list. 

So every time you try and take a link to the right that would take you too far, you instead move down, the same principle as the find operation. One of the really cool things about a skip list is how we use randomization to kind of keep it balanced, in a sense. Remember that every single level has to have roughly half the number of elements as the level below it for the operations to run efficiently. And there's a very simple way that we can ensure that by just flipping coins whenever we do inserts. So every time you insert an element into a skip list, you first use the find operation to locate the position in the lowest level list where the new element would need to go. And then you splice it into that lowest level list. Now the question is, should we also insert that element at higher levels? 

And the way you do that is just by flipping a fair coin. You flip the coin, if it comes up heads, you also insert the elements one level higher. And then you flip the coin again, and if it still comes up heads, you continue inserting it at higher and higher levels. And so every single element maybe has a one-half probability of being inserted one level higher, a one-fourth probability of being inserted two levels higher, and so on. And this probabilistically gives us what we want in terms of half the elements being in the next level, a quarter being in the level above that, and so on. In fact, now we can start seeing some mathematical things about skip lists. If you want to talk about the total number of levels in a skip list, it's easy to argue that that is logarithmic in expectation using the randomized reduction lemma. 

Because if you look at the number of elements per level, there's a one-half chance that that actually drops to half of what it used to be every time you move from one level to the next. And so the randomized reduction lemma tells you that in expectation, and, I guess, also with high probability, the total number of levels in a skip list is on the order of log n. We can also use the randomized reduction lemma to analyze the running time of the find operation, which we'll do in a second. That also is logarithmic in expectation, but once we have those, basically every other operation has a logarithmic running time and expectation as a consequence. So if you want to insert an element, that's basically the find operation followed by those random coin flips. If you want to delete an element, that's basically the find operation followed by splicing that element out from all of the levels on which it exists on. So basically, every single operation on a skip list has a pretty easy implementation. 

And its running time is going to be logarithmic in expectation as a consequence of the find operation and also the fact that we have a logarithmic number of levels. Let's analyze the find operation now. This is also very easy to do using randomized reduction. In fact, instead of analyzing the find operation, we're going to analyze the so-called unfind operation, just looking at what happens with find, only in reverse. Remember that with the find operation, you start in the upper left-hand corner and you try and move to the right whenever possible, otherwise moving down. Instead of doing find, if we do unfind, we start at the target element in the lowest-level list, and we try and move up whenever possible. And if we can't move up, we move left. 

So unfind is going to follow exactly the same path that the find operation followed, just only in reverse. So the expected time to unfind an element is exactly the same as the expected time to find an element. And we can analyze unfind very easily with randomized reduction. The key parameter to look at is the number of elements on our left. So if we're currently sitting on this 26 node, the yellow highlighted elements here are the elements on our left, I argue that the number of such elements follows a randomized reduction process. In every step of the unfind operation, there's at least a one-quarter probability that the number of elements on our left drops to at most half of what it used to be. That's actually pretty easy to show, it just requires two things to happen. 

So if we're sitting on this node right here, we want to ask, is the next step of unfind going to be an upward step? That just depends on the coin flip that happened at this location when the 26 was inserted. If we flipped heads when we were at this location, then we would have also installed another copy of the 26 one higher level than where we are right now, so we would have moved up. So we have to have a one-half probability that the 26 moved up, that we flipped heads at the 26. And then if that happens, you look at all the elements to our left, they also flipped coins when they were inserted. And so there's a one-half chance, at least, that no more than half of them decided to move up as well. So if both of those events happen, we move up and less than half of the elements on our left move up, which that happens with probably at least one-quarter, then we get the desired level of reduction in the number of elements on our left. 

So by randomized reduction, find and unfind both take logarithmic time in expectation. And as a consequence, basically every other operation on a skip list also takes logarithmic time and expectation. Skip lists, they were kind of designed as an alternative to binary search trees, but you can actually show that they're very closely related to binary search trees. In fact, if you go back and look at the picture of a skip list and kind of tilt your head by 45 degrees, it kind of almost looks like a binary search tree. And so maybe not surprisingly, there is a way, or there are several ways, in fact, that you can take a skip list and kind of transform it or map it, come up with a one-to-one mapping between the skip list and an equivalent, basically, variation on a binary search tree. And so that actually allows you to kind of port the balancing mechanism for a skip list to the world of binary search trees. And it gives you another way of randomly balancing a binary search tree with coin flips. 

It kind of looks similar to what a skip list is doing. You're kind of flipping coins and randomly rotating elements up in a binary search tree. I guess you can also port binary search tree balancing mechanisms to the world of the skip list. I'm not sure you'd ever want to build a red-black skip list or a splay skip list, but I guess, conceivably, it could be done. So just nice to kind of think about connections sometimes between the various data structures that we're thinking about. It's also nice to study skip lists in particular, because the main trick they use, using these longer-range links to give us, effectively, shortcuts to navigate to a destination more effectively. That does generalize and give us insight that we can apply to other problems in other data structures or other algorithmic situations. 

Let me tell you a couple of examples where I've seen the same idea being used effectively. So if we think about tree data structures, a lot of times a node will keep track of a pointer to its parent. But you could also keep track of a pointer to your grandparent and your 4x grandparent and your 8x grandparent, effectively building a skip list-type structure embedded vertically within your tree that lets you walk up the tree much more quickly. This could, for example, let you find your kth level ancestor very quickly. Your kth level ancestor is the node that is k steps above you in the tree. So you could navigate to that in only logarithmic time if you had these kind of pointers that go upwards at geometrically spaced out intervals. Another very common problem in trees is finding the so-called lowest common ancestor of two nodes, a and b. 

That's the very unique point in the tree where the search path down to a and the search path down to b diverge. And a very, very common node that we want to find in a wide range of different data structure applications, you can find the LCA of a and b in just logarithmic time using these long-range links, irrespective of the depth or balance of your tree. In the next module, when we talk about hash tables, we'll talk about distributed hash tables. If you have a really, really large dataset and you have to store it on multiple computers in a distributed network, how do you navigate quickly to the computer that houses a particular data element? We'll see the same idea being used, where you don't have to step individually from one machine to the next machine to the next machine. You can kind of take shortcut edges to find the target machine much more effectively. And then finally, if you want a nice example from the world of AI, take a look at deep neural networks, where you have networks consisting of many, many, many layers. 

The input of each layer comes from the output of the preceding layer. One challenge with very deep networks often is vanishing gradients during training. So you use backpropagation to propagate gradients back through the network and to adjust the weights in your network. But if you have many, many, many layers, the gradient information kind of decays away as you move backwards through successive layers. And to prevent that from happening, or to mitigate that, you can add these longer-range links in the network, where the output of one layer travels forward, kind of skips over a number of layers and feeds into a layer much farther ahead. And so that kind of gives the gradient information alternative shortcuts to travel backwards and reach those lower levels in the network much more effectively. So that helps with model training in deep neural networks, so many applications involving the same kind of core trick, I guess, that the skip list uses.