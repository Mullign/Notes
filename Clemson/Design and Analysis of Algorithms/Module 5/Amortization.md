![[Amortization.pdf]]
If I ever teach a class in just data structures, the topic I always start with is amortized analysis, because it is that important and that pervasive in the field of data structures. In fact, we've already talked about amortized analysis a little bit, way back in Module 1, simply because we needed to add that to our vocabulary right at the beginning of our course because we have already encountered and talked about several different data structures that involve amortization. In this video, we're going to revisit amortized analysis and amortized data structures and talk about them maybe in a little bit more depth, a little bit more properly, and talk about how to do amortized analysis of data structures. Maybe a good starting point for our discussion is to revisit the amortized data structure we talked about back in Module 1, that was the expanding array or vector. Very simple idea, you're just inserting elements, you're pushing elements onto the end of an array, and whenever that array reaches the size of the memory buffer you've allocated for it, you have to pause and do an expensive operation where you copy the entire array into an up-sized memory buffer. That's expensive, but that doesn't happen too often. This is the situation where amortization usually shines when you have a sequence of operations with very non-uniform performance characteristics. 

Most operations are fast, but you have an occasional slow operation. Here if you use worst-case analysis, it doesn't really faithfully describe what's happening because in the worst case, things are quite bad, you could have an expensive operation that causes a copy, but that just doesn't happen that often. Expensive operations are spaced out by intervening fast operations. If we average over a sequence of operations, then the running time looks a lot better, so it's a more faithful way to describe the running time of our data structure. That really motivates the definition of amortization, it's really a worst case analysis, but averaged over a sequence of operations. In this case, we showed back in Module 1 that if you do any sequence of k operations, k pushes into your array, the total time that takes is just order k. If you divide that out and look at the average running time over the sequence, it's constant, and so we say our running time is constant amortized in this case. 

Similarly, if you say your running time is order log n amortized, what that means is over any sequence of k operations, the total running time should be at most k log n. It's important to know that amortized analysis still is worst-case analysis, it's just looking at things averaged over a sequence of operations, and that smooths out the non-uniformity in this sequence of operations where some run quickly and some of them run more slowly. This is a much simpler way of looking at running time, and it's a more accurate way of characterizing the running time of these types of data structures where the performance is somewhat non-uniform. In the example of our expanding array, I could have described the running time as linear worst case, and that would have been correct, but it really doesn't properly, accurately characterize the running time. People would be scared of using the data structure if all I told them was, hey, this operation runs in linear worst case time. For all they know, every operation could run in linear worst case time. But if I tell them it's constant amortized, that gives a much clearer picture of what the true performance really is and it is a faithful description of the running time, it really does give you still a worst case bound just over a sequence of operations. 

This is also useful when you consider plugging in data structures into larger algorithms. A lot of times an algorithm will make a sequence of calls to a data structure, and in that setting, it really doesn't matter if the data structure does every operation in some amount of worst case time or in some amount of amortized time. If your data structure has an operation taking log n worst case time versus log n amortized time, it doesn't matter because if you do a sequence of k of those operations in both cases, the total running time of that sequence of operations is k times log n. Amortized data structures, as they're sometimes called in comparison to their so called worst case counterparts, where every operation runs in log n worst case time, they are just as good as building blocks for more sophisticated algorithms. The total running time they take is just as strong as their worst case counterparts. There are several ways we can perform amortized analysis. In fact, we have already used one of those back in Module 1 when we analyzed the expanding array. 

We used in that case, a method that's often known as aggregate analysis. It's probably the simplest way that we can do amortized analysis, motivated entirely by the definition of amortized running time. What we did was, we looked at a sequence of k operations, just k inserts into our array, we ascertained that that took order k time, and then we just divided by k to get the average running time over that sequence. That was our amortized running time. This method works well, anytime you can get an upper bound on the running time of an arbitrary sequence of k operations, you just divide by k and you have your amortized running time. The trouble is, this is relatively limited to simple situations because in most data structures, it can be hard to calculate a meaningful upper bound on running time of an arbitrary sequence of k operations. Imagine maybe k operations in a splay tree, not so easy to calculate the total running time that that would take. 

If we want to develop alternative methods, I'd like to go back to the vantage point that we also introduced earlier in Module 1 and in other places of looking at running time from the perspective of an element of data. There are two ways that are common for analyzing running time. One is chronologically, looking at the major steps your algorithm takes, or the major procedures that the algorithm invokes over time and another way is looking at how much time you spend per element of data. In this matrix here, I basically show the amount of work that you spend on each element in each step of the algorithm. In the first step, that's an inexpensive step, you just swap two elements, you do one unit of work to each element. In step 3, that's also an inexpensive step where you modify an element. Step 5 is an expensive step because we copy the entire set of all n elements, and so you spend one unit of work to each of them. 

If you look at the running time chronologically, major step by major step of the algorithm, the performance is very non-uniform and that's what sometimes makes it difficult to add things up. But oftentimes if you look column by column instead, if you look at the work done per element, then you get a simpler picture. Maybe you only spend a constant amount of work per element over its lifetime in the structure. That's what makes this per element analysis sometimes easier to do than if you add things up row by row. This is going to be the basis of our next method of amortized analysis. In fact, a hallmark of this method is that when you look at the work done per element, you don't care as much about when that work happens. We're really going to take advantage of that in the next method, which is sometimes called the accounting method, I guess. 

The general idea here is we're going to be time shifting when we account for work. That's the big idea. It's always going to be okay to account for work earlier than when it actually happens, but not later than when it actually happens. For example, if you have an insert operation and a delete operation, both costing one unit of work, then it's perfectly reasonable to time shift when you account for the delete and instead charge that upfront at the time of insert. You can mentally think that insert costs you two units and delete is now free, it costs you zero units because you've prepaid when the element entered the data structure for all of the work that's going to be done to that element over time. Why is this reasonable? We've invented a fictitious running time of two for insert and a fictitious running time of zero for delete. 

Those don't correspond to what you're actually spending for insert and delete. However, if you add up those fictitious running times over a sequence of operations, you will still get a valid upper bound on the total actual amount of work that you spend, and that only happens if you time shift and account for work earlier than it actually happens. Because if you look at a sequence of operations, you may have actually accounted for some deletions that haven't even happened yet so you're almost overcharging yourself for work that you haven't done yet. If we think about why this could be useful, a lot of times we have data structures that maybe stop here and there and do periodic garbage collection. The expanding array fits into that model as well, every now and then it pauses and does an expensive operation on a large set of elements. It's hard to predict when and where those expensive operations happen, though so if we instead just prepay for all of that work, if we just charge ourselves when elements are inserted into the structure, an amount that covers all of the subsequent work that's going to happen to them, then it doesn't matter when and where that work happens, we've already accounted for it. It's much simpler in that method. 

This gives us a method we call the accounting method, where we invent fictitious running times that basically represent time-shifted work. In the case of the expanding array, what we're going to do is we're going to charge ourselves three units for every insert. An insert actually only costs us one unit of work, but we're going to charge ourselves three units of work. One unit of work to actually pay for the here and now cost of the insert, and then two units of work that we're going to invest as a credit that will pay for future work that involves the element that we've just inserted. If I draw a picture of what's happening, I can think of the elements in the data structure. Some of them actually have two units of credit sitting on them that represents the amount of work we've prepaid on those elements, but that we haven't yet redeemed. When it comes time to do an expansion, what we see is that half the elements in our array must have been inserted since the last expansion. 

Those elements have two units of prepaid work on them. That's enough credit to pay for the expansion to happen. Two units of work on half the elements, is the same as, n total units of work. This accounting scheme basically lets us figure out when an expensive operation happens, who's paying for it? Who do we tax in some sense? Amortization is sometimes equated with taxation of elements. In this case, we could have what we call maybe a social security scheme for elements in our data structure where the younger and newer elements are really paying the way of the more senior elements who have already spent their credits. 

Every time an expansion happens, someone has to pay for it. In this case, it's the newer elements that have just freshly been inserted, they have credit sitting on them that we can now redeem and pay for the expansion. If we look at how we're doing our accounting scheme for running time, the actual cost is still the same. The data structure isn't changing, we're just changing how we're accounting for running time. We're accounting for it at a different point in time than when the work actually happens. Actually insert still only costs one and expansion still only costs n, but we're basically calling insert three. We're saying that's our fictitious amortized running time for insert. 

But if we use that, then that lets us basically say expansion costs you zero because you already prepaid all of your expansion costs with your inserts. This is very common in the real world. Amortization is a financial concept that you see in real life. Just to give you an example of the same situation, maybe in a concrete physical example, suppose you own a car, and every month, that car costs you $10 worth of maintenance or gas or what have you. There's a monthly cost that recurs of $10. But every December, you have to spend an extra $120 doing a full tuneup of the car to keep it running properly. If you had to describe to somebody how expensive this car is to you, you could say, well, it's $130 in the worst case, with the same analogy that we've seen with our data structure operations. 

But that doesn't really faithfully describe how much the car actually costs because it doesn't often cost you $130, there are a lot of months where it's much cheaper than that. What we can do is we can amortize our cost. We can take that expensive $120 worth of yearly maintenance and spread that across all of the months leading up to December so that we charge ourselves a fictitious, we set aside $10 and put it in the bank in every month leading up to December so by the time December happens, we have enough money in the bank to pay for that expensive charge. We've time-shifted when we're accounting for our payment basically. We're setting aside money in advance to pay for this anticipated expensive operation that's going to happen in the future. This is exactly what happens with amortized data structures. Here, this also illustrates why it's okay to time-shift things earlier. 

It's fine to build up a credit and put money in the bank to pay things off later. It's not good to pay things off after the fact, because then you go into debt. That doesn't work in the real world, either. Let's think about maybe adding an operation to the mix. So far our expanding array example is pretty straightforward, let's make it a little bit more interesting. Let's add a pop operation, the ability to remove an element from the end of our array. Maybe thinking of it more as a stack. 

We can push elements onto the end of the array, and we can pop elements from the end of the array. When you pop elements off the end of the array, the issue is that the array can shrink quite a bit and you could end up wasting memory. If you have a huge memory buffer allocated for the array, and you have only a small number of elements that you're storing, then you're using much more memory than you want to. If you pop a lot of elements out of the structure, you might want to actually downsize the structure, copy it into a smaller memory buffer so that you're not wasting space. The amount of memory that you're using should always be within a constant factor of the number of elements you're actually storing in the structure. Here's one proposal for what you could do in that case. Just as before, if the buffer fills up, then you double its size. 

Same strategy there. But maybe in terms of downsizing, if the buffer becomes less than half full, then we will shrink the buffer. We'll copy everything in the buffer into a memory buffer that's half the size as our original, and therefore, we'll only be spending at worst twice as much memory as we actually need at any given point in time. Is this a good idea? I guess the answer is given away on my slide here. I should have made it a two-part reveal. This is actually not good because imagine for a second if you have just done an expansion, right after an expansion, your buffer is exactly half full. 

Then if you pop an element at that point, you become less than half full and that causes a contraction. When you do a contraction, you're almost all full and then you can do a push operation which causes an expansion. If you alternate between pushes and pops in this borderline state, then you're going to have expensive operation after expensive operation. Every single push or pop is going to cause either an expansion or a contraction. Amortization like the slide says here, it can't save you from the situation where every operation is expensive. You really do need intervening cheap operations to average over if you want the expensive operations to get averaged out from the picture. Here's a better scheme. 

What we're going to do is the same thing when we do pushes. If the memory buffer fills up, we're going to double its size. On the other end, if we're going to remove elements from the structure, we wait until it gets one-quarter full before we do a contraction. If we pop elements out of the array and it drops down to being one-quarter full, then we contract its size and at that point, I guess, it would only be half full after an expansion or a contraction. In this picture right here, the middle part of the picture is the state that our buffer would be in right after an expansion or a contraction actually because right after you expand, the memory buffer is going to be half full. Right after a contraction, you're also half full as well. You'll notice that it takes a number of intervening fast operations, a couple of pushes or a couple of pops, before you can force an expensive operation to happen. 

That's really important to being able to achieve the fast amortized performance. How do we actually make the accounting work here? How do we time shift how we charge ourselves for these expensive operations? We're going to do the same thing with the push operation. We're going to charge ourselves three units for every push. That's going to be broken down as one unit to pay for the here-and-now cost of the actual push operation and then two units of work that we think of as a credit that we're investing on that element that we can use to pay off in the future. I like to actually think of it as actual money. 

I think of it as a $2 bill sitting on top of each element of data. That's the credit that we've prepaid, but we haven't redeemed yet. If the structure fills up, same picture as before. If the memory buffer completely fills up, then we do have enough credit to pay for the next expansion because half the elements will have two units of credit sitting on them. What about on the other side? With the pop operation, I'm going to overcharge every pop operation by one unit. My fictitious amortized cost for pop is going to be two, one to pay for the actual pop operation, and then another one that becomes a credit, maybe a $1 bill sitting on the empty spot in my data structure that I've removed the element from. 

If my data structure ever drops to a size of n/4, then I will have n/4 units of credit sitting in the data structure that I can use to pay for the contraction that I need to do at that point. I've designed my accounting scheme here so that it is like working backwards. I know on the horizon that I have these potential expensive operations, and so I have to overcharge the cheap operations just enough so that I know that by the time I build up to an expensive operation, I will have built up just enough credit to be able to pay for that expensive operation. Let's look at another nice example of this so-called accounting method for amortization where we're time-shifting how we account for work in expensive operations. Let's as our starting point go back to a stack and add to that stack a find-min operation. Now our stack will support hopefully three operations, push, pop, and find-min all in constant time. This is really easy actually to implement by just keeping a pointer to whoever is the current minimum in the stack. 

Then we also have to add a pointer from every element in the stack to the element that was formerly the minimum when that element was pushed. When one was added to the stack, three was the previous minimum, so one is going to point to three. When one is subsequently popped out of the stack, we can then reset our current minimum to point to the three. Very easy to create a so-called min stack. It is surprisingly more difficult to create a min queue. A queue augmented with a find-min operation where all of insert, delete, and find-min run in constant time. In the worst case, you can actually do this, but it is surprisingly messy and very complicated to implement. 

Much easier actually to achieve amortized running times here. It's important that we're dealing with a find-min operation not a remove min operation. We're not talking about a priority queue here. We're just talking about a regular queue that is just aware of who the minimum element is in that queue at any given point in time. This min queue data structure actually does have a lot of uses. We had talked in earlier video in this module about a problem where we want to slide a window across an array and keep track of a running minimum of the contents of that window, you could use a min queue for that application. At every step, you're pushing a new element onto one side of the queue and you're popping elements off of the other side of the queue and just calling the find-min operation as you go as the window scans across the array. 

If you did have a constant amortized running time available for all of those operations, then sliding the window all the way across your array would just take linear total time, full stop. Lots of applications if we can build this min queue. There's actually a nice way that we can build a min queue by essentially gluing together two min stacks at their bases. This also goes back to a trick that we talked about in a previous video where we talked about building two-sided data structures by combining two one-sided data structures. In fact, it was for the exact previous application. We built a minimum of a window by taking a prefix and a suffix min and gluing those together to get the min of an entire window. Here, what we're going to do is take two one-sided data structures, min stacks, and glue them together at their bases to get a two-sided data structure, which is the min queue. 

Elements here are going to be inserted on one side into the blue side, and they're going to be deleted from the yellow side. Then it's easy to know who the minimum is in the entire structure because these are both min stacks, so they both know who their respective minimum elements are. This works really well as a min queue except for the case where we try and delete an element and we have exhausted all of the yellow elements. If the yellow stack completely empties out and we'd like to then delete another element, that gets difficult because you can't really delete the base element of the blue stack. Instead, what we end up doing is an expensive operation. We're going to take the entire contents of the blue stack, hoop all of them, and then push them into the yellow stack. We could call this a transfer or a recoloring operation that takes linear time, but it doesn't happen too often. 

That's good news for amortization. Once we've done this, then we can proceed to do further deletes because now we have yellow elements available to delete. All of our operations run in constant worst-case time except for delete, which takes linear worst-case time in the event that it triggers one of these recoloring operations. We need to figure out how to shift around when we account for the work of these operations to basically prepay for this expensive case of delete. That's actually pretty easy to do here. Instead of saying that pushing into the blue stack costs me one, I'm going to charge myself two units. The amortized running time of insert is now going to be two. 

One of those is going to go towards the actual here-and-now cost of the actual insert operation. But then I have one unit of prepaid work left over as a credit that I can envision just being associated with each blue element. Picture a $1 bill sitting on every blue element that represents prepaid work that we've already accounted for with our overcharged version of insert. Now when it comes time to do the recoloring operation, I have exactly enough money to pay for that. I have n units of credit sitting on those blue elements. I can use that to pay for the recoloring operation. Of course, the yellow elements that result, they don't have any credit, but they don't need it because they're never going to be recolored ever again. 

If I look at the resulting running times, insert now has an amortized running time of two, and recoloring is now free in an amortized sense. It has an amortized cost of zero. It still actually costs you n units, but we're just mentally charging ourselves for those n units earlier in little bits and pieces as we insert the elements earlier on in the structure. We could have even changed around this accounting scheme even further. If you think about how much work each element is subject to during its entire lifetime in the structure, there's one unit of work when it gets pushed into the structure initially, one unit of work when it participates in a recoloring operation, and then later on one unit of time when it's part of a delete operation. Only three units of work ever spent on any given element. That should bode well for amortization, but we could also have just charged all three of those units upfront. 

We could have achieved an amortization scheme that looks like this where insert now has an amortized cost of three. The way that breaks down is one unit is used to pay for the actual insert leaving two units of credit sitting on all the blue elements. Then when we do a recolor, that takes one of those units away from each of the blue elements leaving still one unit of credit on the yellow elements that's enough to pay for the deletion. Now delete also has an amortized running time of zero because we've prepaid for it by slightly overcharging insert. This is how the so-called accounting method works for amortized analysis. It really is just time-shifting. When we account for work, we're accounting for some work earlier than when it actually happens. 

Yes, we are getting these fictitious amortized running times, but we'll see in a second that they're still valid in terms of giving us proper upper bounds on how much work we really do spend over a sequence of operations. Again, note the data structure is not changing. These amortized running times are made-up numbers that don't reflect the actual running times, but they still are really valid in a very real sense that we'll see in a second. If you ever read a research paper that discusses amortization, you're likely to see it discussed in terms of so-called potential functions. A lot of times when we do amortized analysis, we think of it in terms of this accounting scheme where we have credits floating around in the data structure associated with individual elements. But if you want to actually at the end of the day write it down in somewhat of a mathematically principled way, then oftentimes we use what's called a potential function to articulate the same idea. Let me explain how that works. 

This really is exactly the same thing. It's just being looked at from a different vantage point, maybe slightly more mathematically rigorous, using a little bit more terminology essentially for what we had done on the previous slide. We basically first define a potential function that tells us how much total credit is in our data structure at any given point in time. The potential function is just a function of the state of the data structure that tells you how much credit it should represent. Here, if you look at the potential function that would correspond to this accounting scheme, it would basically assign two units of credit to every blue element and then one unit of credit to every yellow element. Our potential function is twice the number of blue elements plus the number of yellow elements. Now that you have defined your potential function, you can very simply write a formula for the amortized cost of every operation. 

That is just its actual cost, the here-and-now true cost of the operation, plus whatever change in potential results from that operation. If we look at the same examples we looked at before, we reach the same conclusions because this is the same analysis that we just did, it's just looked at from a formulaic perspective. Insert has an amortized cost of 1+2, one for the actual here-and-now cost of the insert. Then what's the change in potential? Well, you're adding a blue element, so that increases your potential by two, so amortized cost is three. What about the amortized cost of recoloring? Well, the actual cost of recolor is n because you're doing order n work. 

A lot of times, by the way, we forego temporarily the use of asymptotic notation, and we just talk about costs being things like one or two or n rather than Order 1 or Order 2 or order n. That just simplifies the process of doing amortized analysis by pretending that one unit of work is basically the hidden constants in our O notation are all one. We can just assume that we're doing one unit of work or two units of work. At the end of the day, we would have to put back the O notation around everything, but the answers aren't going to change. Things that are constant are still going to be constant. Insert costs us three units of work. That's going to be a constant Order 1 at the end of the day. 

The amortized cost of recoloring, it's n units of work for the actual operation. But then there's a corresponding decrease in potential because n elements turn from blue to yellow, and so the potential will drop by n, that offsets the expensive cost of the actual recoloring and gives it an amortized cost of zero. You typically do have these two flavors of operations in any amortized data structure. You have the cheap operations that don't actually cost much that you slightly overcharge. They're injecting a little bit of money into the bank. The potential is going up when you invoke those operations. Then you have the expensive operations. 

They're actually paying for themselves by pulling money out of the bank to offset their high actual costs. This is how we express the exact same idea from the previous slide but using a potential function. Now we can see mathematically why it actually works when we use our fictitious amortized costs that we've come up with. Imagine that we look at any sequence of operations and we sum up their amortized costs. According to this formula, the amortized cost of an operation is its actual cost plus its change in potential. The sum of amortized cost is just the sum of their actual cost of those operations plus the sum of all the changes in potential. Something very pleasant happens when you do that. 

If you take the sum of all the changes in potential, maybe I'll write that as like Phi_1-Phi_0 for the first change in potential, where Phi_1 is the potential after the first operation, and Phi_0 is the potential before the first operation. Then the next operation, it's change in potential would be Phi_2-Phi_1. That's the Delta in potential, and so on. If you do this, you notice that you have a very pleasant cancellation happening here. This is called telescoping sometimes when you write out a long series and then you just have a massive amount of cancellation. I have a plus Phi_1 and a minus Phi_1. I have a plus Phi_2 and a minus Phi_2 and so on. 

When the dust settles, all you're going to end up with at the end of this chain, you're going to have a Phi_k if there were k operations. That's the final potential that was left over in the bank after you ran your sequence of k operations. Then I'm going to have, I guess, this term right here, this minus Phi_0. But that's going to be equal to zero because if your potential function is proper, it should start out at zero for your empty data structure. You can't magically start out with credit in the bank. Your potential should always be zero initially. I guess the only other rule about potential functions is that they should never go negative. 

They should always be non-negative. That reflects the fact that you're only pre-paying for expensive operations by accounting for work earlier. If you accounted for work later, that would correspond to a potential function going negative on you, and that's not allowed. If I go back and look at the sum of my amortized costs here, that's the sum of their actual costs plus the change in potentials, which I've just showed you adds up to basically the leftover potential after you've executed those k operations. Your fictitious amortized running times that we've made up, if you add those up, that does actually end up giving you a proper upper bound on the actual amount of time you spent on those operations. Yes, they're made-up numbers, but they still are valid in terms of giving you a worst-case upper bound on the running time of a sequence of operations. That's using potential functions. 

It really does reflect the same analysis that we just did though with the accounting method. If you're more comfortable using accounting analysis, I would recommend sticking with that. To me, this is the most comfortable way of doing analysis. Then if you have to make it a little bit more rigorous if you're writing a research paper or something, you can usually convert that into an equivalent potential function analysis. We'll work through a lot of examples in the next video where we talk about using both vantage points to do amortized analysis of several common more advanced data structures.