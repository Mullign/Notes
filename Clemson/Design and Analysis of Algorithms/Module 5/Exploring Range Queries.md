![[Exploring Range Queries.pdf]]

[Music] The core topic underlying the discussion in this video is the range query, asking a data structure to output all of its records matching a set of criteria. On its surface, that may seem a bit dull and mundane, but it really actually does open the door to an incredibly rich set of ideas in the design space of data structures. Talking about range queries will give us an opportunity to visit a very wide range of very elegant and exciting topics in the world of data structures. We'll get to review binary search trees and highlight their tremendous power and versatility. We'll get to go back and talk about multi-dimensional structures. In general, we'll just see a lot of very powerful and generalizable ideas in data structure design. This will be a fairly extensive video. 

I will definitely not feel offended if you have to pause halfway through and grab a snack. But hopefully, it will provide a lot of useful insight in data structure and algorithm design. Range queries come in many shapes and sizes. We can talk about range queries in sets, in sequences, in trees, in multi-dimensional point sets. There are many, many different options. The simplest ones that we'll consider first are the so-called one-dimensional range queries. That would be a range query in a set, effectively a bunch of points on a number line, asking for either account or us to enumerate the points within a certain range from A to B. 

We can also ask range queries on sequences, asking for some aggregate statistic of the range from index i up to index j. What's the sum of a range, what's the minimum or the maximum of a range? In higher dimensional spaces, we can output all of the elements that live in some geometric range. Tell me all the restaurants within one kilometer of my present location. You can ask range queries in databases. A database select out all the records that correspond to books with a page count between 500 and 800 and a price between 50 and 75. That would be a two-dimensional range query. 

There are lots and lots of different options. For our starting point, let's have a look at simple one-dimensional range queries in sets. Effectively, here we have n points on a number line, and we would like to either count or enumerate all of the points that live in a range between A and B. Here, I'd like to draw an important distinction between what we call static and dynamic data structures. In a dynamic data structure, we have insert and delete operations, so we'd be adding points and removing points interspersed with our range queries. In the static case, we build the data structure once at the beginning of time. We start with n elements, we build a data structure out of them, and then we hit it with queries. 

In the static case, you typically talk about what's the preprocessing time to build your initial data structure. Then, after that, what's the query time for answering each subsequent query? It turns out that in either case, we're going to be able to answer range queries in the same amount of time here. Basically, order log n time or order of k plus log n and output sensitive running time if we want to actually enumerate the k points in our range. That's inevitable because we have to spend order k time writing those points down. In fact, in the static case, we've already seen this problem before. This is a very trivial problem. 

You can solve this with just regular old binary search. All you do is you take your elements and you sort them as preprocessing step, that takes n log n time. Then you can just binary search for the end points of your range and either write out the k elements in the range or easily calculate the number of elements in the range in order log n time. The static case here is relatively trivial. In the dynamic case, we basically use a balanced binary search tree. That's our go-to structure for one-dimensional range queries, and our go-to picture for range queries in binary search trees is going to be this picture right here. We're going to see variance on this picture quite a bit throughout this video. 

This really does describe the way a range query looks in a binary search tree. The yellow elements here are the elements in the range between A and B. If you do a search from the root down to the left endpoint A and a search from the root down to the right endpoint B, then all the elements in between, in the subtrees hanging in the interior of those two search paths, belong to our target range. We may have to be a little bit careful because A and B themselves may not be in the range, so we might be interested in instead looking at the successor of A or the predecessor of B. We'll see this picture showing up quite often through the rest of our discussion. There are actually quite a number of ways to think about range queries in binary search trees. If we're interested only in counting the number of elements in our range, one very easy way to do that is just to take the difference of ranks between B and A. 

Being a little bit careful if B and A themselves may not necessarily be in the tree, we may have to adjust by plus or minus one in that case. Remember that the rank of an element is how many elements in the tree are smaller than it. The difference of two ranks tells you how many elements in the tree live between A and B. That's the number of elements in the range. An easy way if you want to enumerate the elements in a range is you could use what we call maybe successive successors. You start at the element that's the low-end point of the range, and then you just call the successor operation over and over again as you step through the elements in the range one by one in sorted order, eventually reaching the far side of the range. There's actually something interesting to talk about here in terms of running time. 

The successor operation, as you know, in a balanced binary search tree takes order of log n time for one isolated invocation of the successor operation. What if you do a series of calls to successor? If you do k calls to successor, stepping through the k elements in your range, you might think that that would take k times log n time because each individual call does indeed take order log n time. But something very interesting happens in an amortized sense here. It turns out that things are actually much more efficient than that. The running time ends up being k plus log n. There's a fixed overhead of log n, and then enumerating the k elements ends up taking basically constant time per element in an amortized sense. 

That's actually not too hard to see because if you call successor over and over and over again, you end up essentially following the same path that an in-order reversal of your tree would have followed. If you think about what calls to successor would be doing, if they enumerated all these yellow elements, they're starting at A, they're ending at B, they're going to be visiting those two outer search paths. That's like what explains the plus log n part because those search paths have log n elements on them. Then they're going to be stepping through all of these interior yellow search trees that are containing the bulk of the elements in our range. On those search trees, it's taking just linear time to step through all of the elements because we're effectively just in order traversing those trees, and in order traversals take just linear total time. That explains where the plus k is coming from in that running time. Some other nice ways to deal with range queries in binary search trees. 

If you have a split operation, you can call split twice to entirely split the range you care about out of the tree. That can be extremely nice if you want to somehow just isolate the range into a single subtree. If you want to count the number of elements in the subtree, then maybe just look at its root. Hopefully, you've augmented your subtrees with subtree sizes. The one root element of that subtree would tell you the count of how many elements are in the range, and it would be trivial to enumerate them with an in-order traversal of that subtree. You can achieve a similar effect with a splay tree. If you splay the two elements that are flanking your range, that will also isolate your range into a single subtree, and you can do the same thing in that case. 

Lots of ways that you can manage range counting or enumeration queries in binary search trees. There's actually yet another way that I'd like to advocate. This one is nice because it generalizes well, not just to binary search trees, but multi-way search trees and multi-dimensional search trees. It's a very generic approach that we can use to think about stepping through a recursive decomposition of our elements and outputting all the elements that live in a particular range. We could call this like a pruned traversal, maybe. We're going to do essentially an in-order traversal of our binary search tree. But as we do that, we're going to prune the search anytime we wander outside the range. 

We avoid visiting parts of the tree that are irrelevant for the range that we care about. If we start with just an in-order traversal, the code here is not colored basically. If N is NULL return, that are simple base case, and then for every element that we visit in the tree, we look at that element and say, well, is it in the range? If so, we counted as being part of the range or we printed out, whatever we're trying to do with our range query. We examine the elements that we see as we step on each individual node because each node represents one element of data, and then you just simply recursively query your children. This query is going to fan out and visit the entire tree. It would do that if we didn't include any sort of pruning whatsoever. 

It's very important that we prune the search anytime it ends up in a situation that allows for effective pruning. This red case here is really the crucial one. If you ever land on a node whose subtree is completely disjoint from the range query that you care about, you can prune the search. You don't have to keep searching in that subtree at all. Anytime you wander outside of the range, you can prune the search. That's these red X’s in the diagram here. Also, if you're doing, say, range counting, anytime you land on the root of one of these yellow subtrees, that means that now everything in your subtree is within the range that you care about. 

You don't want to continue traversing that subtree and counting up the answers one by one, you just simply look at the root of that subtree that you hopefully have had augmented with the subtree size, and so you can stop then and there and also prune the search. Effectively, all you visit, as you're traversing the tree, is just these outer search paths. Anytime you step off of one of those outer search paths, the search gets pruned. That's why the search takes just log n time, say, for a range counting query. Now, you can actually do exactly that, pruned recursive search of your tree in other types of trees. Let's think about the multi-dimensional case. We'll go back to a kd-tree, which we talked about in our last video lecture, and we'll try and answer a multi-dimensional range query. 

Here I have a set of points in two dimensions, and I'd like to output all the points that live inside of a rectangle inside of a query box. I can do exactly the same thing here. We start at the root of our kd-tree, and we just simply start traversing the kd-tree. Using exactly the same template, if you ever reach a node in the kd-tree whose subtree represents a region of space completely disjoint from the query that you care about, then you can prove the search. Similarly, if you ever reach a node in the kd-tree, whose subtree lies entirely within the query box, then there's no need to continue traversing, you can just return the size of that subtree and stop traversing. Exactly the same procedure works in basically any sort of tree that represents a recursive spatial decomposition. That would include things like kd-trees, things like quad trees, things like binary space partition trees. 

This very simple template for traversing a tree in a prune fashion is actually quite general and quite powerful. In this figure right here, this is a nice figure because it really shows what the footprint of the tree is that's visited as a result of all of this pruning. In a kd-tree, every node represents a rectangular region of space. As you walk down the tree, those rectangles are getting further and further subdivided so that at the leaf level in the tree, you have the smallest possible rectangles. Every leaf in the tree represents a tiny little rectangle containing one point, and that's what I have pictured here in this figure. I've shaded all of the rectangles that correspond to the leaves that I visited while searching for the elements in this rectangular range. You can see that that fanned out and visited, well, the footprint that it visited did, of course, include all of the range, but it didn't actually stray too far outside of the range. 

There's a lot of the kd-tree that was not visited as part of that range query, which means it was hopefully a lot faster than an overall traversal of the kd-tree would have been. What's the running time if we continue looking at the multi-dimensional case? We've talked about two prominent of higher-dimensional generalizations of binary search trees. We've talked about kd-trees, and we've talked about quad trees. We've also talked about binary space partition trees, but kd-trees and quad trees are probably the most common structures that you see used for these sorts of range query applications. It would be interesting for us to know how fast do these range queries run in the multi-dimensional case. In the one-dimensional case, everything runs in log n time because binary search trees do pretty much everything in log n time. 

News is not so good, unfortunately, in the multi-dimensional case, and due to the curse of dimensionality, it gets worse and worse as dimensionality goes up. I guess to start with, the news is just not that great for quad trees in general. In general, a range query over a rectangular region in a quadtree could take linear time, which is not great considering you could have answered that range query by just simply enumerating your endpoints and checking each point one by one to see if it lived inside of the range. Quad trees really don't give you any theoretical leverage in terms of answering multidimensional range queries. In practice, they may still work reasonably well. You can imagine that this order n only happens in certain very adversarial situations that probably don't show up that much in practice. Maybe this is still a viable structure to use in practice. 

The kd-tree actually does have an interesting story behind its running time. In two dimensions, if you'd like to enumerate the points inside of a query rectangle using a kd-tree, the running time for that is k plus the square root of n as an upper bound, instead of k plus log n with the one-dimensional binary search tree. The running time actually gets worse and worse as dimensionality increases. It's k plus n to the one minus one over d. If we think about the two-dimensional running time is n to the one-half, the square root of n, then in three dimensions, it's n to the two-thirds, and then n to the three-fourths, n to the four-fifths, and so on. It gets closer and closer to n as the dimensionality increases. These are not particularly effective data structures in high-dimensional spaces. 

There's actually some very interesting things to say about the kd-tree. I guess, one qualification for this running time is that to get this running time to work, the k plus square root of n, the kd-tree has to be what you could call perfectly balanced. That means every single node has to be a perfect split into a half-size subproblem on one side and a half-size subproblem on the other side. We really can't tolerate any deviation from perfect splits. We have to always split the tree on basically a median value into a subtree of size n over two and a subtree of size n over two. In regular binary search trees, we can be a little bit of less careful and balance them, say, using randomization. If you build a binary search tree randomly, then every operation will still take order log n time just like it would if you balanced it perfectly. 

That's not actually the case with kd-trees. It's rather interesting if you actually try and build a kd-tree at random. Basically, you insert the points into a kd-tree in random order, then the running time you get for these two dimensional range queries is a bit bizarre. Instead of n to 0.5, the square root of n, you get something like n to the 0.562, which is actually n to the square root of (17-3)/2. This to me, this is really weird because why would Mother Nature do this to us? We seem to be dealing with a very natural, very simple data structure. Why on earth would the running time work out to be something so bizarre if you build it in such a natural way just by adding elements in random order? 

Why the square root of 17? The math here is actually quite complicated to prove this. I won't go into more detail. I just wanted to highlight something that is one of those weird situations and algorithms where we get a very complicated running time. That is actually the right answer. That's a tight result. What I probably should do at this point is go back to the square root of n result and explain where that comes from because it's a little bit mysterious. 

Where does root n come from in the kd-tree? There's actually a relatively simple explanation for how we get square root of n as the query time in the kd-tree. This also highlights a useful principle in terms of running times when you traverse trees. If I have a binary tree and I start at the root, and I take steps going down the tree, and every single step I take goes to one child, that means you basically walk down the tree, and that will usually take log n time if your tree is indeed balanced. A lot of times, the process that you see here is, when you step to a subtree, then the size of your effective subtrees is reducing in a multiplicative way. If you walk down the tree, always visiting just one child, that's good. That's efficient. That takes just order log n time. 

What if, instead, you visit both children? If you're starting at the root and every step you take, you recurse on both children. Well, now you're effectively doing an in order traversal. You're traversing the entire tree. This is going to take you linear total time. Another way to think about that, if your tree is perfectly balanced is that you start with one node at the root, and then you step down to its two children, and then to the four grandchildren, and so on. Every single level you step down, you're doubling effectively the footprint of the number of nodes in that level. 

The tree is going to have height log of n, log base two of n because it's perfectly balanced. You have log base two of n doublings. That means when you reach the leaf level, the number of leaves is going to be something like two to the power of the log base two of n, and that is synonymous with n. That's another way of looking at the fact that you have n elements at the leaf level that you reach if you simply just traverse the entire tree. That's relatively obvious. What effectively is happening with a kd-tree is that you're actually alternating between those two cases. At even levels of the tree, you're recursing both ways to both children. 

At odd levels of the tree, you're only recursing one way to one child. If you look at that process and how it fans out, then you only double the size of your footprint in the tree every two levels. How many doublings happen if you trace this traversal all the way down to the leaves? Well, you only double on half of the log base two of n levels. The number of leaves you end up visiting at the end of the day is two to the one half of log base two of n. That is synonymous with square root of n. That's where the square root of n comes from. 

It comes from this sort of process. Why do we get this sort of process? It turns out that, well, let's look at the pruned traversal of our kd-tree as we answer a range counting query. We have a range counting query over some rectangular range, and we start at the root of the kd-tree, and start traversing it, and we prune our search in two possible ways. If the search wanders outside of the query region, we prune it. If the search actually wanders fully inside the query region, we also prune the search because no need to continue searching, we can just return our subtree size. What that means is that the only places where the search continues to build out are along the boundaries of the query box here. 

If you wander inside the query box, the traversal stops. If you wander outside the boundary, the query stops. The query continues to visit smaller and smaller, more refined parts of the tree only in the regions around the boundaries of the query here. That's where we end up wasting all of our time. Because anytime you step away from that, you don't traverse any farther, the traversal stops. All you really care about, if you want to count up the total amount of work spent by the query, that's actually dominated by the number of leaf regions that you visit around the boundary of the query. If you look at this picture here, how many rectangles does the query intersect around its boundary? 

That's going to actually be the dominant factor in the running time of your search. To get a handle on that, we can actually look at the four sides of our rectangle separately, and you can say, well, if you were just searching for a horizontal line or a vertical line, how many leaf regions would that intersect? That will give you an upper bound on the complexity of the number of rectangles that you hit with each of the four sides of your query rectangle. Now we have a simpler problem to think about. Imagine that I'm searching in a kd-tree for just, say, a horizontal line like this red line right here. What would that do? What parts of the tree would that end up visiting? 

Well, every time you hit an x node, an x node is a split in the x direction, if you're trying to visit all the parts that are touched by this red dotted line like the top part of our query, you have to actually traverse both ways because the line actually is going to go on both sides of any x split. The x splits are useless. You have to go both ways. But if you hit a node in the next level that's a y split, then line is either going to be either above or below since it's a horizontal line, so you only go one direction on those nodes. If you're searching for a horizontal line, then every x split, you go two ways, every y split, you go one way, vice versa if you're searching for a vertical line, and those lines effectively give you bounds on how much complexity you encounter when you're searching the tree with respect to the four sides of the query region. That's roughly what's happening in terms of the running time of a kd-tree and why you get square root of n in that running time. If you're building the tree randomly, then you can imagine that this doesn't quite work out because the height isn't exactly log n and the math gets actually quite a bit more complicated in that situation. 

Let's go back to databases. Remember that you can think about databases as just a set of points in a high dimensional space. If your n points in the database are each characterized by d individual fields, then we're talking about points in d dimensional space. If you issue a query in a database, very often, that is a range query. Here's an example of a range query written in SQL, in structured query language, which is a very common language that we use for interacting with databases. Here, I'm trying to select out all the books in my table on books that satisfy three different criteria. This is actually going to be a three-dimensional arranged query effectively because I have conditions that govern three of the different fields in my database here. 

The price has to be less than 100. Like your x coordinate has to be less than 100, the publication date has to be at least 1990, so your y coordinate, effectively, has to be at least 1990 , and the page count, your z coordinate, has to be between 500 and 750. This does carve out a three-dimensional box in space, and I'd like to count or enumerate all of the points inside of that box. If you think about how you might want to do that, you could use a data structure like a kd-tree or a quadtree to handle that as a legitimate multidimensional query. But more commonly, we basically break this up and use just one-dimensional queries to help speed things up with databases. A lot of times, we will build what's called an index on individual coordinates. Maybe we build an index on the page count field. 

That would basically be a binary search tree or more commonly a B-tree, which we'll talk about later in the module, where we basically just order all of the records in the entire database just by page count. Then we might build a separate index, a one-dimensional index on publication date and another index on price. We actually are indexing the same database in multiple ways using multiple one-dimensional indices. Now, if you'd like to find the answer to one of these multi-dimensional queries, you have to figure out what one-dimensional index is going to be the most helpful. Conveniently, you can actually do range counting very effectively using those one-dimensional indices. Very quickly, you can figure out that maybe there are 10 million records that match the price less than equal to 100 criteria, and 100 million records that match the publication date criteria, but only 100,000 matches for the page count condition. What we probably would want to do in this case is we would use the index, the B-tree or the binary search tree connected with page count, to actually pull out all the records, the 100,000 records that match according to that dimension. 

Then we can just, more in a brute force fashion, step through those records and check for each one individually if it matches the other two conditions. That's probably closer to what actually is happening in a real-world database than running a legitimate two or three-dimensional query using a kd-tree or a quad tree. You'd imagine if you used kd-trees or quad trees, you would have to build a lot of different structures on different combinations of dimensions. It would get a little bit unwieldy. Typically, you would only use kd-trees or quad trees if you have a small dimensional point set and you know that you're doing range queries on certain combinations of dimensions quite often in that point set. [Music