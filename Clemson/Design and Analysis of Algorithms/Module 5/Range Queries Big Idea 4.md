![[Range Queries Big Idea 4.pdf]]
[Music] Idea number 4. This is another way that we can get at a range by basically taking a difference of two pre-computed prefix quantities. Instead of taking a prefix and a suffix and gluing them together, here we take a difference of two prefix quantities. We'll see this idea a lot. For example, when we study string hashing, we'll be able to compute a hash code that represents a sub-sequence of a larger string by taking a difference of two pre-computed prefix hash codes, essentially. In the simple examples I have here, one of them we have seen before, but we want to compute the sum of the elements in the range within an array. We can easily do that in constant time if we've pre-computed an auxiliary array of prefix sums, which is basically an array of sums up to every element in our array. 

The sum of the contents of the range between i and j is just the prefix sum of everything up to position j, minus the prefix sum of everything up to position i-1, that cancels out everything before the range, and leaves you with just the contents of the range summed up. You can actually do this in higher dimensions as well. Suppose I have a matrix of numbers, and I would like to compute in constant time the sum of any sub-rectangle of that two-dimensional matrix. How can I do that? What's the analog of a prefix sum in this two-dimensional situation? It turns out there's a couple ways you could do it. To me, the easiest way is to think of a prefix sum as an upper left quadrant sum. 

I'm going to first pre-compute another matrix where each entry in that auxiliary matrix of upper left quadrant sums represents the sum of an upper left quadrant from one point in my original matrix. It contains the sum of everything above it and to the left, like I've pictured here. If I look at that one entry in my auxiliary array, it gives me the sum of all these entries, including the rectangle I care about, and a lot of stuff that I don't care about because it's above and beyond the rectangle. Can I subtract off a few things to cancel out things in the right way and leave myself with just the contents of the rectangle? Maybe I can subtract off this upper left quadrant sum. That's going to cancel out a fair number of the elements that I don't care about. I guess I still have these elements down here that I have to cancel out. 

Now I can subtract off this upper left quadrant sum as well. Now I've canceled out, I think these elements, and I've canceled out these elements, but I've double canceled out the elements in this region and only added them in once. I then have to re-add the elements in this upper left quadrant to add and subtract them all in a way that totally cancels out. This will leave me with the sum of only the contents of my rectangle. Basically by adding and subtracting a constant number of these pre-computed upper left quadrant sums, I can isolate the sum of any specific rectangle. Very powerful idea that you can take the sum of a range by taking a carefully chosen combination of prefix sums that cancels out in just the right way to leave yourself with the sum of the ranges that you care about. Let's look at an interesting use case of that in multi-dimensional range queries. 

Imagine now that I have a set of points in two dimensions, and I would like to answer what I could call three-sided range counting queries in two dimensions. My queries are going to have a definite left and right side, and they're going to have a definite top, but they're going to extend to infinity in the minus y direction. I would like to, for queries of that shape, be able to count the number of points in the query region. This actually allows me to bring up another important distinction between types of queries and data structures. We've talked about static versus dynamic. Another important distinction is offline versus online queries. In the offline case, you are basically given all of the queries you're going to have to answer in advance, and you can address them in a batch fashion. 

That's usually a lot easier than the online case where you have to build your data structure, not knowing what queries it's going to receive, and then you get hit by the queries. Offline problems, they're usually much easier, and that's certainly the case here. Imagine that you're given your end points along with a set of queries that you'd like to answer over those end points. What you can use here is a good old sweep line approach, but we're going to first sort the points on the y dimension and step forward in increasing order of y. That's what I've drawn here with our green sweep line. Every time you reach the top of one of your queries, you can now answer that query quite easily if you have stored your points in an x-ordered binary search tree as you've been going. We're scanning through our points in increasing order of y, adding the points one-by-one into a binary search tree. 

Remember that typically when we have a sweep line approach in one dimension, we keep track of a data structure that's organized by the other dimension by x. We're going to store all of our points that we see in an x-ordered binary search tree. Whenever we reach the top of one of our queries, then all we need to do is an x range query in the binary search tree, because the binary search tree already holds all the points less than the top of the query. All we need to do now is isolate the points that match the left and the right sides of the query. That's a standard one-dimensional range query, the same type that we've been talking about earlier quite a bit. We could answer that by say, using a splay tree and doing two splay operations to isolate the contents of that range into a single subtree. That would easily let us count the number points in that subtree by just looking at the root, which is presumably augmented with a subtree size. 

This would give us a very easy approach for answering all of our queries in log n total time. I guess the total time for this entire operation that sweeps over all of our points and answers all of our queries would be like number of points plus number of queries times log n time, because every point gets added into the structure. That's an insert operation that takes log n time, and every query involves a range query in your binary search tree. That also takes log n total time. Unfortunately, this is only an offline data structure. We have to know all of the queries in advance, because as we're sweeping across the point set, we have to pause every time we hit the top side of a query, and then we can answer the query. If you don't know what the queries are going to be in advance, you don't know where to stop the sweep line and when to query the binary search tree for different ranges. 

It might seem like this approach is totally ineffective for online queries. It turns out, however, there's a very cool idea that basically lets us use this offline data structure to answer online queries. That's the idea of persistence. It's what we're actually going to talk about in our enrichment lecture in this particular module. The idea with persistence is basically we're going to add a rewind function to our data structure. We can actually query it at historical points in time. We can say, "Hey, data structure, remember when the sweep line was here, what would you have answered this particular range query with at that prior point in time?" 

Persistent data structures, they let you issue historical queries. That basically lets you turn an offline data structure like this into an online data structure. You just sweep across your point set, not knowing any queries. But then when a query comes along, you're allowed to rewind the sweep line effectively to the point where it would be relevant for answering that query, and then you can issue a range query into the data structure. Persistence is an incredibly powerful idea in data structure design. You can imagine it's like an infinite level undo. If you want to store all of the changes you make to a document in a persistent data structure, then you can go back to any point in the past and resurrect any prior version of the document, and answer queries about any prior version of the document. 

Very very powerful idea, and actually not that complicated to implement. We'll talk about how to do that in our enrichment lecture. Maybe shameless plug for the enrichment lecture in this module. Given that we can answer online or offline three-sided range queries, what about four-sided range queries. Queries over some box that has four defined sides, not just three defined sides. Now we go back to our idea of difference of two prefix sums because we can represent any four-sided query region as the difference between 2, 3-sided query regions. We can take a three-sided range counting query for this region and subtract out the answer in this three-sided range counting query region, and that will leave us with only the count in that four-sided query region. 

Now we would have a method for answering four-sided range queries in only log n time if we employ persistence and all of that in the right way. [Music]