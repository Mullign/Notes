![[Range Trees.pdf]]
[Music] Brian Dean: That's actually the direction we're going to go is we're going to basically be augmenting every single subtree with an entire ordering of all of its points in the other dimension. You can almost think about this as taking an x-ordered tree and augmenting it with y-ordered trees. It's not as crazy as it sounds, so let's talk about how this works. This develops a data structure that we call a range tree, and it's a really clever way of actually building a range searching data structure that composes dimensions together. It's going to have an outer data structure that corresponds to our first dimension x, and then that's going to be augmented with data structures that corresponds to the other dimensions. First, we're going to build a data structure on our first dimension on x. That's going to be a regular old binary search tree. 

For simplicity, I'm going to use a segment tree. I could have used a binary search tree, but I'll just use a segment tree. I'm going to take the ordering of all my points by x. That's going to be stored in a sequence, and I'm going to build on top of that sequence a perfectly balanced binary tree, that's basically a segment tree. I'm going to augment every interior node with enough information to be able to do effective x searching in this structure. Maybe the x minimum and x maximum of the range covered by every interior node I would augment it with. Now, I can basically use this structure like an x searching structure. 

If I would like to find the set of points that matches the x range of my query, I can effectively do that using the same picture that we've seen umpteenth dozen times already in this discussion. You do the query from the root down to the left end point of the range and the query from the root down to the right end point of the range just in terms of x. That's going to isolate all of the points in your sequence that match the x part of your query, the points in this infinite vertical strip. They're going to be the ones in this contiguous range, which is conveniently covered by a logarithmic number of subtrees in my data structure. All I need to do is augment the roots of these subtrees in a way that lets me filter out the points in that range that also match on y. How am I going to do that? Well, every single subtree is going to be augmented with a y-ordered list of all the points in that subtree. 

This is what the data structure is going to look like. The root is going to be augmented with a list of all the points in the entire structure ordered by y. Its two subtrees are going to be augmented with, I guess, half the points, the ones in those respective subtrees ordered by y, and so on. This is using quite a bit of space, but not a tremendous amount of space. How much space is actually being used here? It's actually only order n log n total space because if you think about it, every single point is being stored once at each level in the structure. Each point is really being stored only in a logarithmic number of places. 

We're only still using n log n space. There's a space query time trade-off that tends to happen with multidimensional range searching structures. If you're content to use more space, you can get a lower query time as that's going to happen here. This is also a nice way to think about this structure is it's the recursion tree of merge sort. If we start with our points being ordered by x, and we run merge sort on those points, sorting them by y, then ultimately at the root, we will reach a sorted ordering by y. But basically this tree depicts what happens with merge sort. We take pairwise elements and we merge them together so that now we have lists of length 2 that are sorted by y, and then we merge those together pairwise to get lists of length 4 that are sorted by y. 

Then at the very end, we get one big list sorted by y. You could call this a merge sort tree or something. The proper name is a two-dimensional range tree here. It's an outer tree on x, our first dimension, but it's augmented with information on the other dimension on y. Now how do we answer a range query using this structure? Again, we're going to isolate the x part of the query region by searching for the range in x covered by this tree, just like we did before. But now we can use the augmented information at every subtree grout to figure out the points that also match on y, because we can just do a binary search in those sorted lists to find in each of these subtrees, the points in y that also match our query. 

Since we're doing a logarithmic number of binary searches, we're doing a binary search in each of those augmented y-ordered lists, we're going to get a running time of log squared of n basically because we're doing a logarithmic number of binary searches. We get a total query term of k + log^2 n. The k is for the enumeration part. If we're just doing counting, it would just be log^2 n. Unfortunately, dimensionality is not kind to us as it rarely is with these problems. If you go up to d dimensions, then what happens here? This structure actually does naturally generalize to higher dimensions. 

What you end up with is an outer x structure again, but it's augmented not with simple arrays, but with (d -1)-dimensional range trees. This entire picture here, this is a two-dimensional range tree. In a three-dimensional range tree, you basically have an outer x-ordered tree augmented with these two-dimensional range trees. It gets a little bit hard to think about perhaps. But it's just a nice way to build a structure by layering dimensions on one at a time. You basically have an outer tree in the first dimension augmented with (d-1)-dimensional structures at each of the nodes in that tree. That explains why the running time is logged to the d(n), because every layer adds another factor of log n because you have this extra logarithmic number of queries to these (d-1)-dimensional structures to answer a query. 

The space bound is also not particularly nice in terms of how it scales with dimension. It scales as n log^d(n) in space. There's one small improvement we can get. We can shave off one log factor in the query time using a technique called fractional cascading where you basically do a binary search in the top ordered list, and that gives you enough information to effectively avoid having to actually do binary searches in the lower lists because all the elements are in the top ordered list after binary searching them, you effectively have done enough work to know the answers in these lower ordered lists. If you keep a few extra pointers around, you can avoid doing all these binary searches in the lower ordered lists and just do one binary search instead of a logarithmic number of them. We can save one log factor with fractional cascading, but that's really the limit to how much that can help us out. The range tree maybe gives us the best theoretical bounds that we have available for searching in multidimensional spaces, but you can see these still do not scale very well with dimensionality. 

These would only be effective still in very low dimensional spaces. That's basically, I guess, the ultimate data structure that showcases the power of augmenting another data structure. Instead of just augmenting a tree with values, we're augmenting trees with trees effectively and getting something very powerful as a result. One very final idea that I wanted to mention before closing things off is all of these topics that we've talked about in the world of range queries, we can actually now double the number of problems under consideration by considering the inverse of a range query, which is known as a stabbing query. In a range query problem, you have a bunch of data points as effectively and you're trying to answer queries involving ranges over those data points. In a stabbing query problem by contrast, the data elements are actually ranges. Maybe the data elements are intervals or rectangles or something, and the queries involve a point. 

Basically, if I look at this particular query point, what ranges in my data structure are stabbed by that query point, what ranges contain that query point. We can take any range query problem and turn it on its head and make it into a stabbing query problem. If you're worried about not having enough problems to think about, now you have twice as many problems that you can address because you can think about the stabbing versions of many of these range query problems. A lot of those can actually be turned back into equivalent range query problems. The one on the slide here we actually talked about before, so I won't go into the details. A lot of times a stabbing query problem is itself a well disguised range query problem. Yes, we've talked about range queries a lot throughout this video, but I think the goal was really to use range queries as a mechanism to highlight lots of other interesting ideas in data structure, design, and analysis. 

Hopefully, now we are experts in not only range queries, but in lots and lots of other topics related to range queries, binary search trees, multidimensional search structures, and so on. Hopefully, this has been insightful, and I think this is a good time to wrap things up. [Music]