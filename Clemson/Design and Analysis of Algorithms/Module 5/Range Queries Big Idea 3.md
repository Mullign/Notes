![[Range Queries Big Idea 3.pdf]][Music] Alright, we're on to big idea number three. So here let's go back to our friendly segment tree. We could also make this work in a binary search tree and we'll see kind of both approaches. But here I would like to do what we call a range update. So we've been talking about range queries, where we actually ask the data structure to give us aggregate information about a query range. What if we would like to do a batch update of all of the elements in a particular range in our data structure? So in this case, our data structure represents a sequence, and I would like to support two operations, maybe just retrieve the value of the ith element of the sequence, and then a batch operation that updates all the elements in a range to a common value. 

So like set all the elements in this range to the value seven. And I would like to do that much faster than just individually stepping over those elements and setting them one by one to the value seven. So we can also achieve this quite easily using a treeification of our sequence. Here I've got a segment tree. What I'm going to do is basically step through the logarithmic number of subtrees that cover my range that I care about, and I'm going to put a little augmentation on the root of each of those subtrees. That augmentation is basically going to say, treat everybody in this subtree as if it has the value equal to v. And so with those augmentations in place, I kind of know that anytime I access an element in this tree, I'm going to walk down to that element from the root and therefore I will pass by any of these relevant augmentations. 

And that will let me take note that I should treat any element in this subtree as if it's been replaced by v. Even though I'm not physically updating the individual elements, I have kind of this blanket augmentation that applies to an entire subtree of elements. So this is one way that you can very efficiently do a batch update of a large number of elements in a data structure without actually updating those elements. You just put an augmentation in the data structure that applies to that range of elements. There is some complexity here, though, because later on I could go in and issue another one of those range update queries that involves elements in kind of an overlapping range here. And so I'm going to end up with, maybe, conflicting augmentations that would tend to confuse me if maybe this subtree right here, where I'm setting everything equal to v, maybe in a subsequent operation, I take kind of half those elements and I set them to something else. And so how does my overall augmentation saying everything is set to v, how does that jive with another augmentation that says, no, I have to augment only part of these elements to be some other value? 

And so this brings us to another very common technique. Sometimes it's called lazy propagation. The idea is basically, if at any point during your operation with this data structure, if you examine a node that has one of these augmentations on it, you basically just push the augmentation down to the children of the node, and you do that first before anything else. And so I kind of like to think about it as you're walking through the data structure and you have this kind of fleeting glimpse of an augmentation, but before you can do anything, it runs away and kind of stays one step ahead of you and moves down to the children of that node. So if you're, say, walking from the root down the tree, then these augmentations kind of, they run away from you as you walk down the tree. So every time you're about to step on a node, the augmentations on that node, they kind of scurry ahead and land on the children of that node instead. So you basically never see the augmentations ever, because they always stay one step ahead of you. 

And if an augmentation ever makes it all the way down to the bottom of the tree, then you can actually modify an individual element that you reach with that augmentation. That's where the process would stop. So this, basically, prevents you from ever having conflicting augmentations, because you never bump into an augmentation ever. So if you're putting the augmentations in place for another update operation, they won't run into the augmentations from a prior update operation because you'll just never see those augmentations. They will kind of keep advancing through the tree, and they'll stay out of your way. So this is kind of a clever approach that we can use to do batch updates and still not get confused when we have to do multiple of those batch updates that conflict with each other. We can also use the same approach in a binary search tree. 

We've talked about it here kind of in the context of a segment tree. But in a binary search tree, what would we do, well, if we want to do a range update, then there's a couple of pictures of how we could represent that range in the stereotypical picture of what that range looks like in a binary search tree. We would again walk across that range, and we would actually physically update all the elements on the search path that belong to our range. Those would actually be updated to v. But then the subtrees that are in the interior here of our range, we would just apply an augmentation to the roots of those subtrees, much like we did with the segment tree. And there's a logarithmic number of such roots. So if we just apply those augmentations, it takes you log n time to do the batch update to those operations. 

We could also use a splay tree or a set of splits to isolate the range we care about into a single subtree as we talked about earlier. And in that case, it's a little bit cleaner because then you just attach one augmentation to the root of that one subtree, saying everything in this subtree is to be treated as if it is now set to the value v. So that's how you apply the augmentations. You still have to do lazy propagation, because if you don't, then the augmentations can move around. Like if you're rebalancing the tree with rotations, the augmentations can move around in a way that would invalidate them. So imagine that you have a rotation that you're trying to do, maybe a right rotation here, and the root node here, y, has an augmentation on it. So if you imagine doing the rotation, now, that augmentation would be on node y, but it would no longer be applied to node x. 

Which isn't correct because the original augmentation was supposed to have been applied to everything in y's subtree. So prior to doing this rotation, we would have to push the augmentation down so it no longer is on y, and we would also push the augmentation down so it no longer is on x. It would actually live at the roots of the subtrees A, B and C, and the augmentation would be cleared from x and y, allowing us to do the rotation about x and y without any sort of negative consequence. So that also happens if we use this sort of lazy propagation. If you're doing the rotation between x and y, you have to look at x and y. And remember, the very first thing that happens if you look at a node, is that pushes augmentations down to its children. So the augmentations will automatically be cleared before you ever have a chance to rotate the edge that connects x with y. 

So that's lazy propagation and range updates. [Music]