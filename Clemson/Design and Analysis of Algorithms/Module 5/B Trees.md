![[B Trees.pdf]][MUSIC] Brian Dean: In many applications involving massive datasets or very large databases, our data may just not fit in main memory. Maybe it only fits, say, on disk. And that has some very substantial implications for how we design effective data structures. We actually spoke a little bit about these issues back in Module 1 in our enrichment lecture, when we talked about external memory models and cache obliviousness and related topics. Here, this motivates a discussion of a data structure called a B-tree, which is a relative of the binary search tree with a higher branching factor. B-trees are incredibly prevalent in practice, and they specifically are designed to be effective in situations involving very large data sets, maybe that reside on disk instead of in main memory. So our motivation here is looking at the structure of real-world memory systems. 

They're typically hierarchically organized with a very, very small but very fast cache memory sitting next to the processor. And if you can't find what you're looking for in the cache, then you may have to go to main memory, which is a lot slower to access, but a lot larger. And if you can't find what you're looking for in the main memory, you may have to go to disk, which is even larger but much, much slower. Every time you move down layers of the hierarchy, the accesses do slow down dramatically. Disk may be orders of magnitude slower to access than main memory, so it really makes a huge difference where your data is located. This can have some pretty substantial performance implications for, say, a binary search tree. If most of the binary search tree is stored on disk, because if you have, say, 1 billion records, log base, two of a billion is about 30. 

So that would mean a typical find operation might involve looking at 30 nodes as it walks down the tree. And if those nodes are all located in various places on disk, that's going to be 30 disk accesses, which is quite slow compared to what you would get if you were mostly looking in main memory. Another hallmark of these hierarchical memory systems is that typically data is transferred not in single elements between layers, but in blocks. So if you go out to disk and pull in data, you actually pull in an entire page of data, an entire block of consecutive elements on the disk that gets pulled into the main memory, same thing between main memory and cache. So you're pulling in maybe one k or two k or 4k worth of elements all at once. And we can exploit this in our data structures because it's just as expensive to transfer an entire block of data as it is to transfer one element of data. That's really what the B-tree is designed to exploit. 

So with the B-tree, instead of the situation with a binary search tree where you have a single key stored in a node and two children, we have very fat nodes that store multiple keys, typically in just a simple array. And so there's actually a lower bound and an upper bound on the valid size of a node. So if you look at any node in a B tree, it's allowed to have between B-1 and 2B-1 keys. And the number of children is actually going to be one larger than the number of keys. So the number of children can range between b and two b because there's going to be one child subtree between every consecutive pair of keys. If you look at this example right here, if you look at the child that would be hanging off of this node between the keys five and eight, the binary search tree property would dictate that that child subtree would have all of the keys between five and eight. So the binary search tree property generalizes in a pretty natural way to the case of a B tree. 

The root node in a B-tree is special. It actually doesn't have a lower limit. So it could have as low as just one key and only two children. We typically choose the value of B so that the size of a node basically fits in one of our memory blocks that we'd be transferring between, say, disk and main memory. So that if you have to pull in a block from disk, you can pull in an entire node in one fell swoop in just as much time as it would take to pull in one individual element of data. So it's very common to find extremely large values of b in the hundreds or thousands, depending on whatever block size your memory is dealing with. Here's an example of a B-tree. 

For at least a small value of b, b equals two, where each node is allowed to have between two and four children. Because of that, this is sometimes called a two-three-four tree. It's a special case that we sometimes look at. But remember, in practice, b is very commonly set to very large numbers in the hundreds or even thousands. And because of that, that makes a b-tree incredibly shallow. The depth of a b tree is on the order of log base b of n, instead of the log base two of n that you would have with a binary search tree, because as you move down the b tree, every level has a factor of at least b, more nodes than you had in the previous level because the branching factor is so high. So these are incredibly shallow data structures. 

That's good news, though, if you're accessing memory on disk, for example, because you don't have to make that many accesses as you walk down the tree. If we go back to our example of storing 1 billion keys, if our branching factor is 1000, if b equals 1000, log base v of n, in this case is just three compared to what we had with the binary search tree, which was more like 30. So three disk accesses is a lot better than 30 disk accesses. And moreover, you often very commonly store the top levels of the b-tree in main memory because there are many fewer nodes in the top levels of the b tree. The vast majority of the nodes live in the lowest level or two. And so oftentimes a B-tree only involves maybe just one or two disk accesses to be able to pull in whatever record you are looking for, so much more efficient in this sort of external memory situation. Most operations on a B-tree are very easy to implement in order of B log base B of n time by generalizing the corresponding operation on a binary search tree. 

So the binary search tree property generalizes in a natural way, and most operations on a binary search tree generalize very naturally to B-trees. For example, if you want to find an element, you start at the root and walk down the tree. And every single time you inspect a node, that instead of taking constant time, now takes you order of B time, because the size of a node involves on the order of B different keys. But once you have looked at the node, you can figure out which child you have to step to next. And the tree itself is relatively shallow, so you only have to look at log base B of n nodes on your way down the tree. And so that's where this running time of b times log base b of n comes from. You inspect log base B of n nodes, but each node inspection is now more expensive. 

It takes order b time, and this is the same pattern that you get with almost every other operation in a b-tree. We'll specifically talk about insert and delete in a minute, because those have to make sure that the b- tree kind of stays balanced in a sense. One actually useful feature to keep in mind here in terms of the balance of a b-tree is we do ensure that every single leaf in the b tree has the same depth. So the B-tree is leveled at the bottom, it is balanced, and we will have to make sure that we maintain that property when we do inserts and deletes. But all of the major operations on a B-tree are quite straightforward to implement. If you look at this running time, order of B times log base B of n, remember, that the log base B of n part is really the important part. That's how many block transfers you're doing from disk. 

The order b part is usually almost regarded as free because that's just looking at the contents of a node that's already been pulled into main memory. So that's much cheaper than the block transfer itself. So the log base B of n term is really what we're trying to optimize for in this setting. Okay, now let's work towards how we keep a b tree balanced by modifying insert and delete appropriately. It's actually relatively straightforward to do that, and one important operation to develop along the way is a generalization of the notion of a rotation. Remember, with binary search trees, we typically balance them by shifting elements around using rotations. And we can do rotations in b-trees as well. 

They look a little bit more interesting than with a binary search tree because they take an element and kind of rotate it up through the parent into an adjacent node. So if I do a rotation here involving the 63 and the 82, so the 63 is the rightmost element in a node, and then the 82 is kind of a key stored in my parent. If I do a rotation about those two elements, the 63 moves up into the parent taking the place of the 82, and the 82 then moves down and becomes the first key in my adjacent sibling. And then I have a symmetric rotation that I could do in the other direction. So rotations in B-trees allow me to basically share keys with my siblings. You can donate a key to your sibling or you can steal a key from your sibling. I guess which outlook you adopt may depend on your upbringing as a child and how well you got along with your siblings, but this can be useful as a way of shifting around keys in the B-tree to help with balancing things out as you're doing inserts and deletes. 

Let's now take a look at inserts and deletes. So if you do an insert and a delete, the first rule is that we always do inserts and deletes in leaves. So we start at the lowest level of the b-tree. Anytime we do inserts and deletes, that's not a problem for inserts, because when you insert a new key into the structure, you walk down the structure kind of like you were finding the key, and you eventually will end up in a leaf node where you want to insert the new key for a delete. If you end up trying to delete an element that is not in a leaf, then we do kind of the same thing we did back when we talked about binary search trees and deletion from binary search trees using that kind of ugly method for delete, where we swapped first with a predecessor or successor. So it turns out that if you're trying to delete a key from a non-leaf node, then your predecessor or your successor will be in a leaf node. So you can swap with your predecessor or successor first and then delete from a leaf node location. 

So the only issue that happens if you're inserting or deleting that is a bit interesting, is you could cause a node to become too big or too small, because each node is bounded by the upper bound and the lower bound on how many keys it is allowed to have. And so that's really the only situation we have to address when we're doing inserts and deletes. And addressing that is actually relatively straightforward. All we end up doing is kind of splitting nodes when they get too full and joining together nodes when they get too under full. So if we look at insert, for example, if we insert a new key into a node and that makes the node too full. So it has two b keys. Remember, 2B-1 was the upper limit. 

So if you go one above the upper limit and you have two b keys, then what you end up doing is splitting the node, and that will basically turn one node into two nodes. Whenever you split a node, that actually donates a new key to your parent, because the parent needs one more key, because now it has one more child to accommodate as well. So splitting a node donates a key to the parent. And conveniently, this is why we have that sort of factor of 2 worth of valid ranges built into the size of a node. If you have an overfull node and you split it, you end up with two nodes that are just about at their lower capacity level, but they're still in the valid range. So splitting a node gives you two nodes that are within the range of what's allowed for a B-tree node. The fact that we've donated a key to the parent means now that the parent might also have to split itself if it is now over full. 

And so doing an insert can actually set off a cascading chain of splits that maybe goes all the way up to the root of the b-tree. And if you split the root of the B-tree, that actually increases the depth of a B-tree. That's the only way that a b-tree gets taller is if you split the root, if we look now at the delete operation, everything is kind of symmetric to that. So if you're trying to delete from a node, and that makes the number of keys stored in that node too small, remember that b minus one was the lower limit on how many keys can be stored in a node. So if you go down to b minus two keys, that's too small. And so in that case, you think about trying to join with an adjacent sibling. If you have a very large sibling, you may not even need to do a join. 

You could instead steal a key from that sibling using a rotation operation. So that's your first course of action as you look at your siblings and you ask, can I steal a key from one of those siblings? Does the sibling have excess keys, one of which I can steal? If not, that means your siblings adjacent to you are also kind of at the lower limit for how many keys are allowed. And so if you join with one of those nodes, then you'll be very close to the valid upper limit of how many keys are allowed after joining. Because when you do join two siblings together, you have to steal a key from your parent to kind of make the math work out, because now the parent has one fewer node in its list of children, so it also needs one fewer keys as a result. So joining together two nodes steals a key from the parent that could again make the parent too small, and for it to be joined with one of its siblings. 

And this can set off a cascade of joins that goes up the B-tree and eventually causes a join to happen at the root, which would join together two children of the root. And if the root only has one key, that can make the entire b-tree one level shallower as a result. And this is the only way that a bee tree can decrease in height is if you have a cascade of joins that goes all the way up to the root. So that's the insert and the delete operation, relatively straightforward, actually, to kind of keep a b- tree balanced, to keep it so it satisfies the valid range of sizes that we impose on the nodes in a B-tree. And one nice consequence of this, if you remember back to our discussion when we talked about red black trees and all of the very complicated special cases that you have to deal with. If you're trying to balance a binary search tree that's a red black tree, it turns out that there's kind of a one to one mapping between red black trees and two-three-four trees. So any red black tree, you can transform into an equivalent 2-3-4 tree, and vice versa. 

And it turns out that the red black balancing mechanism is actually equivalent to what's happening in a 2-3-4 tree if you run the standard B-tree balancing mechanism that we just talked about with inserts and deletes. So you could argue that maybe we don't need to emphasize red black trees as much, because there's a much simpler way of looking at what is effectively the same balancing mechanism. So this is the B-tree, very, very useful data structure, very common in a wide range of areas of practice involving large datasets, databases, things like that. So it's a good data structure to know about, because you will likely encounter them quite often in your computing careers. [MUSIC]