![[Amortization Examples.pdf]]
[MUSIC] Not surprisingly, the best way to become comfortable with amortized analysis is to simply work through plenty of examples of prominent amortized data structures. In this video, we'll do exactly that. We'll have a look at a handful of amortized data structures, many of which are data structures we've seen before, or that have strong connections with data structures we have seen before in this course. As a warmup, let's consider a simple problem that shows up in many different situations. Say I have 20 different elements and I'd like to look at every possible combination of those elements. So I'd like to enumerate all the 2 to the 20th subsets of my set of 20 elements. There turns out to be an extremely lightweight and simple way of doing that. 

I can just take an integer counter and step it from zero to two to the 20th minus one. And if I look at the binary representation of that counter, it'll have 20 bits in it corresponding to my 20 elements. The counter will actually cycle through every possible combination of zeros and ones on its way from zero to two to the 20th minus one, allowing me to basically enumerate every possible subset. You can think of this length 20 binary number as a binary incidence vector, so to speak, of the set in question. So a one bit represents presence of an item, a zero represents the absence of that item. So how is this connected with amortized analysis? Well, how quickly can we actually step forward from one value of the counter to the next value of the counter if, say, we represent it as an array of zeros and ones? 

This actually brings us to perhaps a textbook example of amortization. This might actually be the first problem studied in a data structures course that talks about amortization. So this is the problem of how you increment a binary counter. So I have a binary counter stored in an array of 0s and 1s, and I would like to effectively increment that counter. So starting with the rightmost digit, I flip 1s to 0s until eventually I hit a 0 that flips to a 1. That's kind of what happens in the process of incrementing a binary counter, and that can be expensive if your binary number ends with a large run of ones. However, on average, as you step through many, many, many different values of the counter, on average, it turns out you only flip two bits. 

So let's analyze this. It's actually very easy to analyze this situation. All we do is we associate one unit of credit with every one bit in our counter, because the expensive part of an increment is flipping all these one bits to zeros, there's only a single zero bit that gets flipped to a one bit. And so we have the flexibility to overcharge ourselves for flipping the 0 to a 1. We're actually going to charge two units for that one unit to flip the bit to a one, and then one extra unit to install a credit on that one bit. So that's how the credits are going to appear on our one bits. And now the one bits can pay their own way when they later on, flip back to zeros. 

And so basically, the amortized cost of flipping a zero to a one, we call that two. And then the amortized cost of flipping the 1 back to a 0 is going to be free. So that's our amortization scheme. Every single increment of the counter therefore only costs us two units in an amortized sense, because all of these one bits that flip themselves back to zeros, those are free. If you wanted to kind of write a potential function that described exactly the same accounting scheme, the potential of the data structure is just the number of one bits in our counter. And so, if you want to look at the amortized cost of one of our increment operations, suppose we have an increment operation that flips exactly b one bits to zero bits. So what is the amortized cost? 

That's the actual cost plus the change in potential that results. The actual cost is b plus one, because we flipped b one bits to zeros, and then we flipped a zero to a one. So there's b+1 bits that we're flipping. That's the actual cost of the operation. What's the impact on potential? Well, a single zero flips to a one, so that adds one to our potential. And then b bits flip from 1 to 0, that decreases the potential by b. 

And the B's here offset and cancel out, and you're left with two. So at its heart in this class, all we really do is add one plus one to get two in very fancy ways. This is also a nice example of a phenomenon that happens fairly often in amortization that sometimes is called recursive slowdown. So if you look at the patterns involved with incrementing a binary counter, the rightmost bit, the least significant bit, that actually alternates in every single increment, it goes from 0 to 1 to 0 to 1. If you move one position over, that bit only changes every other increment, it's 0011-0011. The next position changes even less frequently. Only one out of four, four increments and so on. 

So by the time you get to the most significant bit, it very, very rarely changes. One way you can kind of think about this that's kind of equivalent, is that this process is kind of being driven by the least significant bit. It, in every iteration, involves one unit of work. It's flipping zero to one to zero to one. But then some of that work kind of propagates to higher order bits sometimes. And so we can kind of think about if we amortize it the right way. Every single flip of the least significant bit kind of propagates half a unit of work to the next position over. 

Because every two increments, there's one unit of work involved in the next position over, and every increment of the least significant bit propagates a quarter of a unit of work to the third least significant position, and so on. And so you can kind of think of each original unit of work at the right hand side of our number and are propagating to an exponentially decaying amount of work that gets spent at higher levels, deeper and deeper within the structure. And because 1/2 plus 1/4 plus 1/8 and so on, that adds up to at most 1, that's one extra unit of work at most, that each original bit flip is going to propagate to and cause. That's where we get two from via this alternative way of thinking about things. So one unit for the original flip, and then 1/2 plus 1/4 plus 1/8 for all of the downstream work that is going to be caused by that original flip. So this is called recursive slowdown, because as we do, operations kind of on one side of the data structure, they do propagate through the data structure, but their influence attenuates as that happens. And so it doesn't add up to a huge amount for each original operation. 

There's a wonderful illustration of this in an art piece that used to be, or maybe still is in the MIT museum, called machine with concrete by Arthur Ganson. It's basically a series of gears. And so on one side, there's a motor that's turning the first gear by, you know, quite a fast rate of speed, but the gears are slowly decreasing the, the rate of speed as you move through this succession of gears. And the final gear is embedded in a block of concrete. Just to illustrate that you're doing a lot of work on one side of this structure, but very little of it, at the end of the day, finally propagates all the way through to the other side. So this is kind of the same phenomenon that we see with our binary counter. We're flipping bits very fast on one side, but very little work is propagating all the way to the other side of the binary counter. 

Why did I spend so much time on recursive slowdown? I think a really beautiful example of that is seen in our good friend the the B-tree, which we've also talked about in this module. So let's take another look at the B-tree and figure out where amortized performance actually shows up in the B-tree. So remember that when you do inserts and deletes in a b tree, you're always doing inserts and deletes, starting at the leaf level of the b tree. And if you insert. Into a node, and that makes the node too full, have too many keys in it. That could split the node and donate a key to its parent, which makes the parent more full. 

And eventually this can set off a cascade of splits that maybe goes all the way up to the root. If you're unlucky, the opposite happens with join. If you delete from a leaf node, that could make it too under full, and that can cause it to join with one of its siblings and steal a key from its parent, which makes the parent smaller, and then the parent might have to join with the sibling. So deletion can set off a cascade of joins that goes all the way up the tree. So that was the operation of a B tree. And remember that every single node in the B tree has a range of valid numbers of keys. It can hold between b minus one and two b minus one keys. 

Otherwise it's out of range and would have to cause a split or a join. Now, if you were paying very close attention when we talked about B trees, you might have actually noticed that this valid key range actually is kind of one larger than it really seems to need to be. Because if you, for example, split an over full key, so two b is one more key than you should have. If you split that, then you actually get two nodes of size B and B minus 1. Well, b minus one is the lower limit. So you actually have kind of one unit of slack in one of these two children's. You could have made your range a little bit smaller, actually. 

And the same phenomenon happens with join. Basically, when you join together a minimally full and an under full key node, what you end up with is a node that has two b minus two keys, which is actually one less than the upper limit. It actually would have also worked if we had defined the valid range of number of keys per node to be one smaller. So maybe instead of B minus 1 to 2B minus 2, sorry, to 2B minus 1, we allow anything from B minus 1 up to 2B minus 2. So a slightly smaller range. If we do that, things look almost more perfect. If you split an over full node, you end up with two minimally full nodes. 

And if you join together a minimally full and an under full node, you end up with a node that is exactly at its upper limit. So why didn't we define the b tree this way? This seems a lot cleaner in terms of how the ranges line up. However, it turns out you get a similar phenomenon to what we ended up with when we had a bad mechanism for resizing our expanding buffer. Remember back when we talked about resizing an expanding array? We doubled it whenever it became full. And then we thought about, well, maybe we could have it whenever it became half full. 

And that was a bad idea, because then you could have a series of doublings and halvings that happened in rapid succession as you're doing an insert and a delete. And an insert and a delete. The same sort of thing can happen here. If you have, if you're unlucky enough to have a cascade of splits that goes all the way up to the root as a consequence of an insert, then the next delete can basically cause all of that to undo itself. It can cause all of those splits to rejoin, and then you can have another insert that causes another cascade of splits that goes all the way up to the root, because you have this kind of perfect lineup between the upper and the lower bounds. So you can have actually a sequence of inserts and deletes alternating back and forth, each of which causes a tremendous cascade of splits and joins to happen. And so it actually is tremendously advantageous to make the range just one larger, to introduce just a tiny bit of slack that actually has some pretty profound implications in terms of the amortized performance of the B tree. 

So it turns out if you do that, if you just make the range one element larger, then it turns out that your insert and delete operations, they only cause a constant amortized number of splits and joins. I guess phrased differently, as long as you make the value of b appropriate to the block size in your memory model, then every single insert or delete causes only a constant number of amortized memory writes. And that's really, really useful, because memory writes are particularly expensive in most memory systems. Memory reads maybe not so much, but you really want to minimize the number of writes to memory. So to get a sense of how that works, let's look at this kind of abstract picture of our B tree in the very lowest level, that's where we do our inserts and deletes. If we have our range expanded kind of by one element, then we can't actually cause splits and joins to happen back to back. We have to actually do two inserts or two deletes to cause the next split or join. 

So every split or every join that we do at this lowest level requires two inserts or two deletes. We can't just get them to happen. Back to back, because we have this extra unit of slack built into the range of valid number of keys per node. And so that kind of slows down the rate of splits and joins in the lowest level. Splits and joins in the lowest level cause inserts and deletes one level up, and therefore can also cause splits and joins one level up. But if we slow down the splits and joins at the lowest level, that's like slowing down the inserts and deletes one level up. And because of that one unit of slack now one level up, we actually need a total of most four inserts and deletes or, and slash or deletes to cause the next split or join to happen one level up. 

And if you go two levels up, the same sort of idea applies. You actually need at least eight inserts and deletes at the bottom of the tree to cause a split or a join or a consecutive pair of splits and joins to happen two levels up in the tree. So you're kind of getting this recursive slowdown phenomenon where you're doing a lot of work at the bottom of the tree with inserts and deletes, but very little of it is propagating up the tree, or it kind of propagates at increasingly slower rates up the tree. And that's why we only have a constant amortized number of splits and joins happening as a consequence of every original insert or delete if you want. And this slide looks a bit terrifying I guess. If you want, you can actually do a proper amortized analysis to show this. And there are lots of ways we could have assigned, you know, an allocation of credit to our different nodes to make this work. 

Here is one approach that I found that looks relatively simple to me, at least. What I'm going to do is assign credit to nodes. And there's only four states of a node in which it will be assigned credit. So it will be either overfull by one element that's kind of a transient state. So if you enter that state, you get split. An overfull node is going to have three units of credit. A maximally full node that's at its upper bound that'll have one and a half units of credit. 

A under full node that's also a transient state because that has to be joined with one of its neighbors that'll have one unit of credit, and then a minimally full node will have 0.5 units of credit here. The units here are normalized to how much work it takes to do a split or a join. So one unit of credit corresponds to the amount of work it takes to do a single split or a single join operation. Now we just have to make sure that the numbers, they line up properly. So if we have an overfull key for example that we're splitting, that over full key is going to have three units of credit sitting on it. According to our accounting scheme, the actual split operation that it undergoes, that costs us one unit of actual work. And so we have two units of credit left over. 

It looks like half a unit is going to have to go to one of the two children because that will be minimally full. And then if you look at how split works, split donates a key to your parent that could cause your parents to transition to a state where it's either at its upper limit. Or where it becomes over full. And to do that, because those states require 1.5 and three units respectively, that might require an additional 1.5 units to be donated to the parent node, which we have exactly left over if we've spent one and 0.5 out of three. And so the same sort of thing happens with join. You can verify that the math works out. You start with an under full node that has one unit of credit sitting on it. 

The actual join operation takes only one unit of credit, and then you are left with a node that is one less than the upper bound. But you might also need to add some extra credits to the situation for the parent, because you're taking away a key from the parent. And so the parent might actually need an extra half a unit of credit that actually comes from your other source, note here, which is minimally full, which has that half unit of credit. So it's a little bit complicated, but you just have to kind of play around with the numbers until everything lines up. If your potential function looks like this, what is the ramification in terms of the amortized running time of an insert or a delete? Well, insert basically adds an extra key to a leaf node. And so if you look at what that does to the potential function, at worst the potential is going to go up by, I guess, 1.5. 

If you insert a new key into a node. And so the amortized cost of insert in terms of the number of splits and joins it might cause is going to be just plus 1.5 beyond the original cost of one that you originally paid for the insert. Same thing with a delete. If you delete a key from a node, then according to our potential function, that might cause you to have to contribute an extra 0.5 worth of potential because you might create a node that's minimally full or under full. And so that's only a small constant amount of additional credit that you have to pay for each insert or delete operation. So they still have a constant amortized running time in terms of number of splits or joins that they cause. Okay, so that was interesting. 

We got to see how b trees have actually some very interesting amortized performance. Now let's go back and take a look at a data structure that's a close relative of the randomly mergeable binary heap, which we studied back when we talked about randomization. We're now entering again the world of priority queues. So I'd like to be able to maintain a set of elements where I support an insert operation and a removemin operation. Those are my two fundamental operations, and I'm going to use a data structure for storing my elements, which is pretty much exactly the same data structure that we used with the randomly mergeable binary heap. I'm just going to use a heap ordered binary tree, so nothing special at all. It's just a bunch of individually allocated nodes of memory, each one has a left and a right child pointer, and it satisfies the heap property. 

So what we talked about with the randomly mergeable binary heap is that if you can support a fast merge operation, then you can do basically every other operation as a consequence. So you can insert into the structure by merging with the new one element tree, and you can do a remove min by taking out the minimum, which is the root, and then merging its two subtrees back together. So all we need to be able to do is merge two trees together in logarithmic time, and everything else kind of falls out as a consequence of that. With the randomly mergeable binary heap, there was a very simple way that we could do a merge in logarithmic expected time by flipping coins and remember kind of the picture that went along with that. If you're trying to merge two heaps together, say h one and h two, and let's suppose h one has the smaller root, so its root is going to end up as the root of the merged heap. Then all you do is you think about taking h2 and recursively merging it either into your left or your right subtree of h1. And the way we made that decision with the randomly mergeable binary heap was we just flipped a coin. 

Heads we merge left, tails we merge to the right. And we showed that in expectation, that completes the merge operation in just a logarithmic number of steps. So that's the randomly mergeable binary heap. So we decide between these two cases, merge left or merge right at each step of the merge process by flipping a fair coin. I'd like to kind of think of that fair coin as sitting on the root of h1, because that's really the root node that has to make a decision between its left and right child. In fact, let's think of there being a coin on every single node in our heap. And so every step of the merge process involves the relevant node that has to choose between its left and right children just flipping its coin to figure out which direction it should direct the merge to go. 

So it turns out there is a close relative of the randomly mergeable binary heap called a skew heap. It's a very cool, very elegant data structure, and it is extremely closely related. The only difference is that instead of flipping the coin at the root here to figure out if the merge should be directed left or right, you flip the coin over. In other words, you just toggle the state of the coin. So if I do a merge into the left subtree and I flip the coin over, the next time h1 is involved with a merge, it will direct the merge into its right subtree and then flip the coin over. The next time it's involved with a merge, it will again alternate and direct the merge into its left subtree. And so this kind of, if you imagine, gives you a little bit of balance, because you're kind of alternating between merging left and right at every node, and that over time is actually going to give you a pretty nice amortized result. 

It turns out that it makes merge run in just login amortized time, and as a consequence, insert and remove-min will also take just login amortized time. It turns out also that we don't need the coins. We can actually make this whole thing work without even augmenting any node in the structure with additional state. Like we don't need to keep track of the current coin value for every node, what we typically just do is we always merge to the right, and then we just flip the two children. We swap the order of the left and the right children, so that the next time you merge into a child of h one, you're effectively merging with what used to be the left subtree. But you've swapped children, so now it's on the right hand side. It doesn't matter at all. 

By the way, with a heap, which child is on the left and which child is on the right. That's a big deal for a binary search tree. But in a heap like this, you can swap your left and right child, and that doesn't really make much of a difference to the heap property. So that's all we do is we always merge to the right and then after we do that, you swap your children. So we don't actually need to maintain any extra state in the tree to implement a skew heap. So it's one of these kind of brilliant little self adjusting data structures, maybe kind of like a splay tree. It was invented by the same computer scientists, actually. 

So how do we argue that the merge operation in a skew heap takes only login amortized time? That's a bit interesting, and it's not obvious why that's the case, actually. So let's kind of go through the details of maybe how we would reach that conclusion if we were trying to design an amortization scheme that works. So, what I'm first going to do is designate every node in the skew heap as either light or heavy, and that's based on the relationship of the sizes of your left and right subtrees. So this is an example of a light node, because your right child is the lighter of your two subtrees. That means that if you have a step of merge where you're merging into h1, you're going to merge, remember with the right subtree, you're merging with the smaller subtree, the lighter subtree in that case. And so I'd call that kind of a light node and a light step of the merge that would happen if h1 was involved with the merge in one of its. 

Steps. Heavy is the opposite of that. So your right subtree would be bigger than your left subtree if you're a heavy node. And so if you think about a merge process, you have a number of steps, each of which looks like this. It's either going to be a light step or a heavy step if it involves merging with a light node or merging with a heavy node. And so the total amount of time it takes to do a merge is l plus h is just the number of light steps plus the number of heavy steps. And so we kind of need to get a handle on how many of each of those types of steps we're going to expect to see. 

It turns out that the number of light steps is bounded by at most order log n, by most 2 log n. And that's easy to see because every time you take a light step, then, you know, originally I was doing a merge between h one and h two. If I do a light step, then now my recursive sub problem is between the right subtree, the smaller of the two subtrees of h one and h two. So one of my two input arguments in the merge process has shrunk to at most half of its original size. And that can happen at most a logarithmic number of times with each of the two input arguments of the merge. And so we get an upper bound of at most 2 log n on the number of light steps. This is our familiar situation where walking down the tree, if you step to the smaller subtree, you can only do that a logarithmic number of times. 

So the number of light steps is actually okay. It's bounded by order log n. That doesn't worry us. What does worry us is the number of heavy steps that could be part of a merge operation, and that could be a fairly large number. So what do we do if we have a large number of heavy steps during a merge process? Well, we have to pay for those heavy steps somehow. Let's pay for them using potential, using credit. 

So what I'm going to do is insist that every single heavy node has to have a dollar sitting on it, has to have a unit of prepaid work sitting on it that came from somewhere. So we need to figure out how that credit gets installed. But if every heavy node has a unit of credit sitting on it, then I can use those credits to pay for all the heavy steps in my merge. So that's how I pay off the merge. I have a logarithmic number of light steps, and then the heavy steps pay for themselves. Now, conveniently, when I take a heavy step of the merge, if you look at this picture with the heavy step, I'm merging with the bigger subtree on the right, that's going to make my right subtree even bigger. So this node is going to get even heavier because its right subtree gets even more mass added into it. 

But then remember that I flip the order of my left and right subtract subtrees. So after I take a heavy step of the merge, that node that used to be heavy actually becomes light. And that's super convenient because light nodes don't need credit on them anymore. And I've in fact used up the credit that was on this heavy node. So every single time I take a heavy step, that heavy node becomes light. And that's perfect because I no longer need to keep credit on that particular node. What about light steps? 

I guess if I have a light step, light steps, it turns out could become heavy. So a node that is light, that's on the merge path, that could turn heavy, maybe it stays light, it's hard to know. But every single light step could possibly create a heavy node. And that worries us because heavy nodes need to have credit sitting on them. So every single time I take a light step, I need to overcharge myself by an extra one unit to install credit on that node if that node becomes heavy. So we had at most 2 log n light steps. That means that I also need to charge an additional two log n for all of those light steps to possibly install credit on the nodes should they become heavy. 

And so my total amortized running time for merge is going to be something like 4 log n. It's going to be 2 log n for the actual light steps, another 2 log n to install credits on the nodes that have become heavy as a result of the light steps. And then the heavy steps pay for themselves so they're basically free. So that's my accounting scheme for analyzing the amortized running time of a merge. If I wanted to look at the same thing with a potential function, my potential function would just be the number of heavy nodes in the structure, and I could go through a standard potential analysis using that potential function. What if we want to consider some of the extended operations beyond just insert and remove-min that many priority queues also support? Here really the key operation is delete deletion of an arbitrary element, not just the minimum sitting at the root. 

For example, if you want to do a decrease key or an increase key, you could do that by just deleting something and then re-inserting it with a new key. So all I really want to know how to do is support deletion of an arbitrary element. And that's actually pretty easy with the merge operation, because to delete an element from this tree, I can replace it with the merge of its two child subtrees. So no problem there. What I do worry about though, is that deleting an element is going to effectively cause a decrease in subtree size for all of the elements, all of the nodes on the path from that element up to the root. So I'm actually changing potentially the subtree sizes of a large number of nodes in this structure that might, in some cases create heavy nodes that weren't there before. And according to my amortization scheme, heavy nodes need to each have a $1 credit sitting on them. 

And so anytime I create a heavy node, I have to pay for that, I have to overcharge my current operation to install that credit. And so my concern here is that maybe the delete operation is going to create a ton of heavy nodes and therefore its amortized cost will be very high because I have to pay for the installing of all of those new cryptocurrencies credits. So that's a possible concern. This also highlights the fact that if you are using a particular amortization scheme, say for the merge operation, then every single operation in your data structure has to kind of be cognizant of that method of how you're allocating credit. Every single operation, you have to worry, does that operation add credit? Does it remove credit? They all have to participate in the same underlying amortization scheme. 

So how can we deal with this situation? There's actually kind of maybe two ways. One of them is to actually show that this actually isn't a problem. If you walk from any element up to the root and consider the potential trouble spots. These are nodes that could possibly become heavy as a consequence of decreasing their subtree size by 1. It turns out that there's at most a logarithmic number of such troublesome nodes, because anytime you hit one of those troublesome nodes on your way up to the root, that represents a situation where your subtree size would have at least doubled. I'll let you look at the exact draw a picture behind that to convince yourself. 

But it's relatively easy to see that you can only have a logarithmic number of such problematic nodes. And so yes, you may have to install credit on some of the nodes, but only a logarithmic number. So the amortized running time of delete is at worst going to be plus log n from what the original actual time of delete was. Another thing that we can do, and this is really what I wanted to have an opportunity to bring up, is we could do a lazy delete. This is something that's extremely common in data structures, where when you're asked to delete an element, you don't actually delete it. You just leave it where it is, but you just mark it as having been, like, deactivated or deceased, so that you don't interact with it in a way that confuses things moving forward. And that fixes our problem of accidentally creating heavy nodes. 

Because we don't change any subtree sizes, we don't actually remove things from the tree, so we're not changing any subtree sizes. There are no new heavy nodes, so that removes our. Concern regarding the amortization scheme for merge, but it does create a potential problem later on with removemin, because now when you call removemin and pull an element out of the tree, what if that element is a deactivated element? In that case, you have to pull another element out of the tree and keep pulling an element out of the tree as long as you see deactivated elements. Eventually you'll finally do a remove-min and you pull out a legitimate element from the tree that's not been deactivated. So remove-min can slow down. In fact, we're really just kind of postponing the inevitable with a lazy delete. 

We're marking the element but not deleting it. The actual deletion happens later when we call remove-min and the element is sitting at the root, and then it actually gets deleted. And so here we really are just time shifting when we account for that running time with lazy deletion, what you would do is at the time when you, when you specify deleting the element, you're just going to leave a credit on that element, I guess a logarithmic amount of credit, in this case enough to pay for the eventual remove min down the road, that actually deletes the element. So again, we're going to be using amortization to justify why lazy delete should only kind of cost a logarithmic amount in an amortized sense. There's one other issue that can happen with lazy deletion, and that's that your data structure can get clogged up with a lot of deleted elements and you could end up wasting space as a result. So what's a standard way that you can deal with this? Well, you can use good old fashioned garbage collection. 

You can keep a count of how many deactivated elements are in the structure, and if that count ever reaches, say, half the number of nodes in the structure, so half the elements in your tree have been deactivated, then you can pause and basically just do a wholesale rebuild of the entire structure, leaving out the deactivated elements. So cleaning up all of the deactivated elements with one kind of batch operation, if that takes you only linear time, which it does here with the O(n), then you can pay for that by installing an extra two units of credit every time you deactivate an element. So every deactivated element has a $2 bill sitting on it that is earmarked for paying for eventual garbage collection. And if you ever do have to garbage collect, then half the elements in the structure are deactivated, they all have two units of credit. That's enough to pay for the garbage collection step. So this gives us some good opportunity to really showcase how the accounting method makes it actually quite easy to do amortized analysis in these sorts of situations, like garbage collection, lazy deletion, and whatnot. Maybe one final small subtlety that I wanted to mention in this case. 

I said leave log n credit on an element to pay for its eventual deletion. But what does n mean here? N is usually the size of the data structure. But if I'm leaving credit to pay for an eventual deletion, if I'm time shifting when I account for work, am I talking about n as of the present point in time or n in the future when the element is going to be deleted? There's a bit of ambiguity there. And so I think in this sort of situation, I would probably take n to be an upper bound on the value of n over the entire course of the existence of the data structure, just to kind of make sure that no matter how I timeshift or which point in time that log n is applied, it will still be enough credit to pay for the deletion of that element. Okay, we've reached the end of our standard examples that I wanted to discuss. 

There are many others that I could have discussed. One possibility would be talking about splay trees. As I mentioned when I talked about splay trees earlier, I'm not sure actually doing the analysis of splay trees leads to any further insight in why they have their remarkable performance guarantees. In fact, maybe one way to see that is the fact that when you do amortized analysis of splay trees, the amount of credit you end up leaving on a node is the log of its subtree size, kind of. Basically, what would be the height of its subtree if it were balanced, I guess. But you can imagine trying to move around credits to maintain that accounting mechanism. That can be a bit complicated mathematically. 

So I'm not going to go through the details of splay tree. I'm not sure that there's a huge amount of value in doing that, it's basically just manipulating a bunch of logs, mathematically. One other mechanism, though, that is also related in terms of being a balancing mechanism based on amortization is, you can kind of rebalance a tree in a batch fashion. So you do inserts and deletes in the tree. And if you ever notice that you've done a lot of inserts or deletes in one area of the tree, then you just do a batch rebuild and rebuild that part of the tree in a balanced fashion. So the way you typically would implement that is you just keep a counter on every node. And every time you do an insert or a delete, you know, walking down the tree to insert or delete a new node, you increment counters as you go. 

So every node has a counter of how many inserts and deletes have happened in its subtree. And if that counter grows large relative to the size of its subtree, like at least one-third of the subtree or one-fourth of the subtree, many different constants here can work, then you just do a wholesale batch rebuild of that subtree. And that takes you linear time, but you can pay for that by all of the intervening inserts and deletes that have happened every single time you've incremented the counter on a node. You could also add some credit to the node. For example, if you rebuild when one third of the mass of the subtree has been disturbed, then by adding three units of credit every time you walk down through a node, that would be enough to pay for rebuilding a subtree. And so this is a very common way that we do balancing of trees in kind of a batch amortized fashion. And this could end up being the method of choice for some very complicated tree structures, for example, KD trees and such, where most of our existing balancing mechanisms don't really apply. 

It's very hard to do things like rotations in a KD tree, for example. If you're looking for other good examples of amortized data structures to read about, I would recommend reading about disjoint set data structures and how they're used in algorithms like, say, Kruskal's minimum spanning tree algorithm. This is a wonderfully rich kind of area of amortization, that there's all sorts of variations on disjoint set data structures, and almost all of them have some sort of interesting amortized performance to talk about. So definitely a fun area to look at. I've omitted them from our discussion here, because in terms of the number of applications they have, they're somewhat limited, I think. So I think there are more pressing and more prominent data structures that we should be prioritizing in our discussion here. But in our enrichment lecture, we will also see amortization showing up again, when we talk about persistence. 

Kind of the idea that we can time travel with a data structure and ask it historical queries. And amortization plays a very large role in some of the common ways that we implement persistence. So lots of other good examples of amortization out there if you would like to read further or study further on the technique. One of the very last thing that I wanted to mention is that we've talked about amortization in a fair amount of detail. Now, we've also talked about randomization in a fair amount of detail. You can often get a similar effect with both techniques. So imagine again that you're doing kind of this batch rebuild or garbage collection with a data structure. 

The amortized viewpoint on that would often look like, every n steps, rebuild the data structure so that it doesn't get too unbalanced or too clogged up with garbage. The randomized outlook would be, every single operation, you flip a coin and you rebuild with probability 1 over n. So in both cases, you're still kina getting a constant amount of either, well, amortized work in the first case or expected work in the second case. And so the effect is similar, actually using, amortization or randomization for these sorts of batch updates. I just wanted to kind of point out the similarity of using these two kind of parallel techniques. But hopefully amortization is less mysterious now. It's definitely a challenging technique, with many examples being quite complicated for things like say, splay trees. 

But at its core, the idea is pretty straightforward, so it's a good technique to be familiar with. [MUSIC]