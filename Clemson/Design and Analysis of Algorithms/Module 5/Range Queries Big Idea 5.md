![[Range Queries Big Idea 5.pdf]]
[Music] Brian Dean: Final idea. I did tell you that this would be a fairly substantial video lecture. We've gone through quite a few ideas along the way. This final idea is something that we have seen before. It's the idea that you can make a data structure more powerful by just augmenting it with useful information. Of course, the downside is you have to keep that useful information up to date, whenever you modify the data structure if you have inserts or deletes, for example, but augmenting a data structure, simple idea, but it leads to a lot of powerful application. Here, for example, suppose I would like to answer, you could call them three-sided range emptiness queries. 

The same three-sided range queries in two dimensions that I had before, only now instead of just counting the points, it's a much more simplistic query. I just want to know is a three-sided query region empty or not? Are there points in it? I don't even want to count. I just want to know, is the count zero or not zero, essentially. If I would like to answer that query, all I really need is a single binary search tree that's been appropriately augmented. I'm going to have an x-ordered binary search tree, that I store all of my points in, and of course, if the data structure is x-ordered, then I can use two splits or two splays to isolate the contents of at least any x range into the subtree that I care about. 

I don't want to get ahead of myself here quite yet. If I look in this subtree here, then this subtree is going to contain all the points that match the x extent of the range. Now all I want to answer is the question, are there any points in this subtree with a y coordinate less than the top of my range? Because that will answer whether this range is empty or not. I'm really interested in the point in this subtree with the minimum y coordinate, and whether that minimum y coordinate lives below the cut off of the top side of my range. Here, all I need to do is augment every node with the minimum y coordinate in its subtree, because looking at that minimum y coordinate, comparing that to the top element of the range, that will tell me if the range is empty or not. Very easy to answer certain types of range queries, if you just simply augment your data structure appropriately. 

For our final grand finale example of augmentation doing some amazing things, let's go back to the case of answering four-sided range queries. I would like to count, let's say, range counting queries in two dimensions with four sides. I'd like to count the number of points in one of these query boxes. Again, using the same idea as before, if I had a binary search tree that was only ordered on x, then I could do two splays or two splits to isolate the x contents of my query into, one subtree. In order log n time, I can isolate all the points that match just the x part of my query into one subtree. That looks like this picture here with this infinite vertical strip. This infinite strip contains all the points that match just the x part of my query. 

Now all I need to do is take that set of points represented by this subtree that I've conveniently been able to find very quickly, and I have to filter that set of points on y, so that now I'm left with only the points in the original query box. How do I do that? This data structure is an x-ordered binary search tree. There's no information about y in it whatsoever. How am I supposed to filter out the points in this subtree, picking out quickly only the ones that match the y range of my query? It would be really nice if I had some mechanism for doing a range query on y, within this subtree. But the trouble is to do a range query on y, it would seem that I would need some ordered list of points that's ordered on y or a binary search tree that is ordered on y. 

That's actually the direction we're going to go, is we're going to basically be augmenting every single subtree with an entire ordering of all of its points in the other dimension. You can almost think about this as taking an x-ordered tree, and augmenting it with y-ordered trees. It's not as crazy as it sounds. [Music]