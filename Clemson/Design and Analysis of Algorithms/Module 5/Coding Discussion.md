
![[Coding Discussion.pdf]]



![[q20WYUUsQxywkWDDLcwE6Q_392e60cf62834b73aaef77b50d2ab2f1_20240726CPSC8400_0504coding_MP4_720.mp4]]


#include <iostream>
#include <vector>
using namespace std;

const int N = 4000000, D = 2;
double P[N][D];  // P[i][j] is jth coordinate of the ith point

struct Node {
  int index;  // refers to P[index][...]
  int size = 1;
  Node *left = nullptr;
  Node *right = nullptr;
};

void update_size(Node *root)
{
  // update root size since left or right size might have gone up
  root->size = 1;
  if (root->left != nullptr) root->size += root->left->size;
  if (root->right != nullptr) root->size += root->right->size;
}

Node *insert(Node *root, int index, int dim)
{
  if (root == nullptr) {
    Node *n = new Node;
    n->index = index;
    return n;
  }
  if (index == root->index) return root;
  if (P[index][dim] < P[root->index][dim])
    root->left = insert(root->left, index, (dim+1)%D);
  else
    root->right = insert(root->right, index, (dim+1)%D);
  update_size(root);
  return root;
}

struct Box
{
  vector<double> low, high;
};

bool point_in_box(int index, Box &b)
{
  for (int j=0; j<D; j++)
    if (P[index][j] < b.low[j] || P[index][j] > b.high[j]) return false;
  return true;
}

bool boxes_overlap(Box &b1, Box &b2)
{
  for (int j=0; j<D; j++)
    if (b1.high[j] < b2.low[j] || b1.low[j] > b2.high[j]) return false;
  return true;
}

// does big contain small?
bool box_contains(Box &big, Box &small)
{
  for (int j=0; j<D; j++)
    if (small.low[j] < big.low[j] || small.high[j] > big.high[j]) return false;
  return true;
}

int rq_count(Node *root, Box &qbox, Box &bbox, int dim)
{
  int n = 0;
  if (root == nullptr) return 0;

  if (!boxes_overlap(bbox, qbox)) return 0;
  if (box_contains(qbox,bbox)) return root->size;

  if (point_in_box(root->index, qbox)) n = 1;

  //  replace high range of bbox in dimension dim with root value
  double old = bbox.high[dim];
  bbox.high[dim] = P[root->index][dim];
  n += rq_count(root->left, qbox, bbox, (dim+1)%D);
  bbox.high[dim] = old;

  old = bbox.low[dim];
  bbox.low[dim] = P[root->index][dim];  
  n += rq_count(root->right, qbox, bbox, (dim+1)%D);
  bbox.low[dim] = old;  
  
  return n;
}

int main(void)
{
  Node *root = nullptr;
  for (int i=0; i<N; i++) {
    for (int j=0; j<D; j++)
      P[i][j] = drand48();   /// random real number in [0,1]
    root = insert(root, i, 0);
  }
  cout << "Yay!  Built tree!\n";
  vector<double> low(D,0.25), high(D, 0.75);
  vector<double> zeros(D,0.0), ones(D,1.0);
  Box qbox = {low, high}, bbox = { zeros, ones };
  for (int k=0; k<2000; k++)
    rq_count(root, qbox, bbox, 0);
  cout << "FAST done\n";
}

[MUSIC] Brian Dean: It's often surprisingly not that much more difficult to build a multi-dimensional data structure than its one-dimensional counterpart. As hopefully we will see in our coding discussion at the moment involving multi-dimensional range queries. Our multi-dimensional range query data structure of choice at the moment will be a kd-tree. Partly because it's a relatively simple data structure very closely related to the original one- dimensional binary search tree. And also because we have an upcoming programming assignment that will involve heavily the use of kd-trees. So I thought it would be helpful for people to see an implementation involving kd-trees before that gets going. So our goals with this exercise are relatively straightforward. 

Our first goal is simply to build a kd-tree, and we're going to try and make our code work in any dimensional space. Although we'll focus just for simplicity on a two dimensional set of points in the example that we're going to write here. And then once we've built our kd-tree, we're going to use it to answer range counting queries over rectangles. So given any rectangular region, how many points are contained within that rectangular region, so relatively straightforward. The tricky part I think is going to end up being making those range queries run quickly. So adding the appropriate amount of pruning to speed them up and avoid searching unnecessary parts of the kd-tree. If you remember from our standard diagrams of kd-trees, every single node in a kd-tree holds a point and represents also kind of a rectangular region of space. 

So as you're traversing a kd-tree and trying to find all the points that live inside of a rectangle. Occasionally your traversal might land outside of that range, in which case you don't have to keep exploring and traversing the kd-tree. You can stop, you can prune the search. Furthermore, if you end up in a rectangle, I guess completely inside your query rectangle, then you also probably don't need to further recurse onto smaller and smaller rectangles. You can basically say every point at that point is going to be inside my query region and just return the size of my subtree, for example. So there's a couple of optimizations that we can do to speed up these searches fairly dramatically. So let's go ahead and get started. 

To begin with, instead of starting with a blank slate, I'm going to start with the code that we wrote in the last module for a binary search tree. Because I really do want to highlight how the kd-tree is really just a baby step more than a regular binary search tree. So I'm going to copy bst.cpp into kd.cpp as my starting point and then edit kd.cpp. So this is my starting point. This is the binary search tree code that we wrote before it was used to store a set of integers. So every single node just has an integer key and keeps track of its subtree size. The only code that I've kept here is the insert function because we're going to be modifying that and then not using any of the other functions from our binary search tree. 

So remember the way we called this was we started with an empty tree. So node *root = nullptr; empty binary search tree. And then we called the insert function by saying root = insert(root). And then the key that we were inserting and the insert function returned back a pointer to the new root of the tree post insert. So how are we going to modify that insert function and the structure of our tree so that now it represents not a binary search tree, but a kd-tree. So every single node in a kd-tree doesn't just hold an integer key, it holds a d-dimensional point. And so it might seem necessary to store something like an array of in five dimensions, maybe an array of five doubles or something. 

So you have an entire point represented in each node. Instead of doing that, that's a little bit heavy. I think what I'd rather do is something more lightweight. I'm going to store the actual points outside of the data structure as we commonly do. And then every single node in the data structure can be more of a lightweight thing that just indexes into my array of points that I'm storing on the side. So instead of keeping the points explicitly represented in the nodes, maybe I'll just make an array here about- I'll keep end points- How many points? A large number of points. 

Let's go for it. About maybe 4 million points. 4 million, how about that? And then I'll be operating in two dimensions. We can always change the number of points and the number of dimensions if we wanted to. I'll try and make sure everything stays in terms of capital N and capital D. So all we have to do is change one number and everything will change appropriately. So I'll just make a global array of N by D that holds all of my points. 

This is my data matrix. And so basically p[i][j] is going to be the jth coordinate of the ith point. So every row in this matrix is, is a point, effectively. So now in my node structure I don't have to actually store all of the coordinates of a point. I can just store an index into my data matrix. And so this particular node refers to the point P of index and whatever data is stored in its respective coordinates. So that also is nice, because when I'm inserting something into the structure, I can still just pass an int, which is now an index, into my point double dimensional matrix. 

So I'm just passing in an index here instead of a key, basically, but I'm still passing an integer, which is kind of nice. So if I go back to the main function, the interpretation here is that I'm inserting into the data structure point 17 instead of the value 17. So I guess that means anywhere I had the value k that kind of turns into an index. So let me just make those cosmetic changes. I renamed it index instead of key. We'll have to make a few more changes here, but let me just kind of make the obvious changes first. Okay, I've gotten rid of all the keys and the k's and they're all indexes now. 

Okay, excellent. One other thing that's a hallmark of kd-tree based code is that you're cycling through dimensions. Remember, the root represents kind of the root holds a point, but also represents a split on x. And then the next level down in the tree, you split on y, and then you split on z, and you basically keep advancing to the next dimension until you run out of dimensions. Then you have to cycle back to the dimension you started in. And so almost any function for a kd-tree is going to have one extra parameter added onto it, and that's the current dimension. So every time you call that function, you're going to start out in dimension zero, the x dimension, the first dimension. 

And then when the function recurses, it's always going to recurse in a cyclic fashion, advancing the dimensions. So it's gonna recurse on dimension dim+1 for the next level down in the tree. And since we want to make sure that we cycle back when we run out of dimensions, we often add a mod capital D here to the end,. So that if you're in dimension D minus one and you advance instead of going to D, which would be too far, you cycle back around to zero. So I'm going to add this and you're going to see this sort of pattern in just about any function that goes around and traverses through a kd-tree. All right, so far we haven't really changed the structure of insert very much at all. It looks essentially the same as the insert function in a binary search tree. 

The only line I think that I have to change is this one, the one that decides if I go left or right with my insert. Do I delegate the insert to the left subtree or to the right subtree? Previously I checked if the key that I was inserting was less than the key stored at the root to go left or right. And I'm basically doing that here as well. It's just now the decision is being made in the current dimension because every single node represents an entire d-dimensional point. And so, I'm looking at that point only focusing on the one current dimension. So, like at the root, I'm looking at the x dimension. 

The next level down, I look at the y dimension, and so I'm looking at p of index dim. So index is the index of the point that I'm inserting, and dim is the current dimension. So in the current dimension is the point that I'm inserting less than the point at the root, which is p of root index dim. Okay, that's it. That's the only change that I think I need to make to create a kd-tree instead of creating a binary search tree. So, it's basically just adding on bracket dim and just looking at the current dimension instead of just comparing directly a specific key value and then cycling through those dimensions as I continue to recurse through the tree. So, let's maybe just compile this to make sure that I didn't break anything too badly yet. 

Okay, so we're compiling, let's build a kd-tree out of our 4 million points that I promised to create. So, I'm just going to make a bunch of random points. So, what's a good point set to use here? So, we're working in two dimensions. Maybe I'll just put a bunch of points in a unit square. So going from zero to one in x and going from zero to one in y. So, we'll just have a whole bunch of points spread across the unit square uniformly. 

That'll be our data set that we're going to deal with here. So, I'm just going to generate a bunch of random points and store them in my kd-tree. So let's do that. So, loop over all the points. I'm going to fill in that data matrix basically with random values. It's j=0, j<D, j++. Okay, and then I'm just going to set p of ij = drand48(), I think is the function in C that gives me a random real number in the range 0 to 1. 

So every coordinate of every point is being set to a random number between zero and one. So that will indeed populate my data matrix with points that live in that unit square, essentially. And then I guess once I've generated each point, I can go ahead and insert it into the kd-tree. So, I've generated the ith point and then I can tell the kd-tree to insert the indexed ith point starting in dimension zero. So this should basically build my kd-tree. So, after this, maybe I need an appropriate exclamation of success. “Yay, built tree.” 

Okay, so let's maybe compile and run and see how long it actually takes to build a kd-tree on 4 million points. Maybe that was a bit ambitious, we'll see. So, if I recompile and I run and now we wait, how long does it take to build that kd-tree? At least I'm inserting effectively random points, you know, points in random. That wasn't too bad. Okay, we'll keep that. Normally if you're building a tree like this, you have to worry about keeping it balanced. 

And one way to keep it balanced, of course, is to keep it random. Conveniently, by the way, I've generated my points completely at random, then the tree should end up relatively balanced. So balance won't be a concern of us at this point. Okay, so we have completed our first goal. We've built a kd-tree. Now we need to do a range query in that kd-tree over a rectangular range. So what's a good rectangular range over which to do our query? 

How about we do the following? So, since we have 4 million points, if I define a rectangular range by going from kind of like 0.25 up to 0.75 in each dimension, so 0.25, 0.75. So I'll kind of define a box that has those upper and lower points and I'll ask how many points are inside that box. So that range represents exactly half of the extent in x and half of the extent in y. So, it's a quarter of the entire unit square. So the total number of points in that range should be roughly one quarter of all of my points because they're uniformly spread across the entire rectangle. So, I should expect about a million points to be the answer to a range query over that range. 

So that gives us at least a ballpark sense of what we should be expecting as our output. Let's define that range and issue a query over it. So how do I define that range? I need some sort of like a box structure that I'm going to be like my query box is going to be something. So how do I define a query box that I'm going to do a range query over? And it has to be something that works in d-dimensions. So, I guess a query box is defined by a range in each dimension, kind of a low to a high in each dimension. 

So maybe I'll make a vector, typing is hard, of maybe low ends of the range and high ends of the range. So the low vector is all the low endpoints and the high vector is all the high endpoints. Each vector is going to have length d, I guess. Did I include, if I'm using vectors I probably need to include, I need to include the vector header here. So, #include <vector>. All right, so that's my structure for a box. If I want to now create that particular query box, that's going to be the low ranges and the high ranges where each of those is a vector. 

So, a vector of doubles. The low range was going to be D numbers that are all initialized to 0.25 and the high range is going to be D numbers that are all initialized to 0.75. Okay, so that's my query box and now I can just issue a range query. I want to count the number of points in that box. So let's ask. I'll pass along the root pointer of my tree. So, in this tree, count the number of points in this query box. 

And I probably need to start with the dimension 0 as I always do with kd-tree based functions. So, this is going to be my range query function. Let's write that function. So, my range query function is going to return a count of how many points are inside that box, I pass in the root of the tree, I pass in the query box. I'm actually going to, let's pass that in by reference. Otherwise I'm going to be making a copy of the query box whenever I call this function. And then when it recurses it's going to be passing copies of the box to itself. 

And that's going to take a lot of overhead here. I can basically just pass a pointer behind the scenes and passing it around as a reference. And this is my current dimension. Okay, so inside this function, this is going to be a recursive function that kind of traverses through the tree, hopefully only the parts of the tree that represent the parts that are inside my query and it counts up all the points that live inside that query. So I'm going to have some sort of a counter that counts how many total points and at the end, you know, returns that number. And then somewhere in here I'm going to have a base case probably, you know, if the root is a null pointer then certainly there aren't any points in that. And then I'm going to be recursing at some point. 

So, I'm going to call range query count on root->left perhaps. And I'm going to pass in the same query box, and I'm going to pass in dimension +1. And I'm probably going to recurse to the right as well. So, I'm just kind of writing some bits and pieces of code that I think I might need eventually here. So let me back up and kind of think like step-by-step what this function is going to have to do. So, we have our base case, if it's an empty tree, return null, that's fine. Now we typically have to do something with the root node itself. 

Remember that every node in a kd-tree represents a point and that root node is no exception. It represents a point. And maybe that point is inside our query, maybe it's not inside our query, but we probably need to tally that point up if it lives in our query. So, if that point is inside the query box, let's see the roots point is root index. That's the index of the root point. If that's inside the query box, then I'm going to count that as one answer to my query, right? So, I left unspecified a function I'm probably going to have to write here that tells me if a point's inside of a box. 

Actually, let me just write that function. That shouldn't be too hard. Hopefully. point_in_box and I passed it in as an integer index. The point is referred to as its index in the overall point array. And then the box is a reference to a, let's just call it b, box b. Okay, so a point is inside a box if it's inside the low to high ranges of that box in every single coordinate. So I have to loop over all of the coordinates going from zero up to d. 

And if the point is outside range in coordinate j, return false. So, if I'm out of bounds, if I'm not in the coordinate range in one coordinate, then I'm definitely not in the box. Otherwise, if I pass that test for every coordinate, then I am in the box. All I have to do now is just make sure that the point in question is inside the low to high range of the box in this particular coordinate. So, the point in question here is p of index, and I'm looking at this particular coordinate, which is j, and that has to be inside the low to high range. So that has to be at least as large. Or I guess I'm testing if I'm outside here, actually. 

So, if it's outside, if it's less than the low range b.low of j, or if it's above the high range. So, if it's outside on either side. Or if P of index j is bigger than b.high of j, then I'm out of range and I return false. Make sure I have enough parentheses there, otherwise return true if I'm not out of range on any of the ranges. Okay, so I've properly tallied up whether or not the contribution of the root node itself is an answer to my query. So n starts out at zero and only becomes one if the root node counts. And then if I recurse to the left or to the right, then I can just add up into my running tally however many points the left and the right subtrees told me we’re in the query region. 

So actually, at this point I think I have a working function. It's going to be dead slow because it's currently traversing the entire tree. And for every single node in the tree, just testing if that single point is inside the query box or not and just tallying all those things up. But this should still give me a valid answer and maybe this is a good first step. I can run this and see what I get. Let's print that out actually, hopefully we get a number around a million if we run this. So, if I compile for j, I forgot to type the word int, I think we can all agree that doesn't really count as a bug. 

That's just a minor omission. Compile and then we run and it took us what, a couple of seconds to build the tree. And then we're going to get an answer which is hopefully about a million points inside the box. And it is about a million points, 1,000,212 points. So that is a believable answer. And given the simplicity of the code so far, I think that looks relatively good. So actually, how quick is that query code? 

It's a little hard to know if you're just making one query. So maybe I can make, let's make a couple queries. I'm going to just loop, let's say 20 times. Why not? And I'll just run the same query 20 times and print out the same answer 20 times. But just to give myself a sense of how quick the individual queries are here. So, recompile and rerun. 

So how fast am I thinking these queries are going to take? I build the tree and then I'm going to see that 1,000,212 number appear line after line. So this is how fast our queries are running. Maybe just under a second, one or 2 per second so there's 20 queries. Okay, so that's the time to beat. We need to now figure out how to optimize our queries, hopefully by pruning the search so we don't visit the entire tree during each query. So what are the standard ways that you can prune a search like this so that you don't visit the entire tree. 

So, we basically probably just need some more base cases. So first, we're testing if the root point is inside the box. But now what are some other appropriate base cases that we could think about? I guess we could have. There are actually two that come to mind. So one is if we have basically wandered outside the query region entirely, then there's no point in continuing on to recurse. If we go back and kind of look at the picture on our slides here, remember that every single node not only stores a point, but also represents kind of a rectangular region in space. 

You can almost think of that as the bounding box of the subtree represented by that node. If that node's bounding box is completely disjoint from the query region. So if I go to the left here and then I go left of y one. So this “A” region right here is completely disjoint from the yellow query rectangle. So, once I reach subtree A, I don't have to keep recursing into subtree A anymore because none of these points are going to be answers. So I can stop right then and there and prune the search. So if the bounding box of my subtree is completely disjoint from the box that represents my query, I can stop. 

So that's a good base case. Let's maybe do that and then we'll come back and think about one more useful base case as well. Okay, so if I've wandered outside the query region entirely, the way we had phrased that was if kind of, if boxes are disjoint, my bounding box and the query box basically, then I can just return at this point. And I guess n is going to be zero in that case because my point will certainly not be in the query. So that would be one possible optimization, one way to prune the search. Now I see that I have a little bit more code that I have to write because now I don't have a mechanism for keeping track of my bounding box. I'll probably need to do that. 

And I also need this box’s disjoint function. Let me write the box’s disjoint function. That to me, looks maybe easier than the bounding box thing. So, box’s disjoint, and I'll pass in box and I'll pass by reference just to make it faster, b1 and box b2. So how do I tell if two boxes are disjoint? I probably, I suspect I'm again going to have to loop over each of the individual coordinates. If two boxes are disjoint, that means they must be disjoint in- Does it mean that they're disjointed all of their coordinates, or in at least one of their coordinates? 

I think they have to be disjoint in all of their coordinates. So actually, probably a better way to think about this. Let me actually write it as boxes overlap, in my mind somehow, that's an easier concept to write in code. So, the fact that they're disjoint means that they don't overlap. So maybe I'll write it as boxes overlap, not. So if the boxes don't overlap the query box and my bounding box, then I return 0, that's a base case. So now how do I test if two boxes overlap or not? 

So, they only overlap if they overlap in every dimension. So I can cycle through the dimensions and test if each dimension overlaps. And if every dimension overlaps, then I'm going to return true at the end. So only if one of the dimensions didn't overlap do I return false. So, if there's no overlap in dimension j, then I return false because that means that the boxes don't overlap if one of the dimensions was disjoint. So now I just need to write the logic for no overlap and dimension j, that shouldn't be too bad. So either box b1 was kind of below the other range or above it. 

So maybe if b1's high point in this dimension was less than b2's low point in that dimension, b2.low[j]. Or if b1's low point in this dimension is greater than b2's high point in this dimension, that means that their ranges did not overlap and I return false. Okay, so that's my overlap check. Now I need my bounding box actually. So how do I keep track of my bounding box as I guess I continue to recurse through the tree? This is going to be something that I might also have to pass along as a parameter in addition to the query box. So I'm going to pass along a reference to my bounding box as I traverse through the tree. 

So that's going to get passed to every recursive call, I guess. In fact, I'm going to have to modify the bounding box as I recurse because the bounding box is getting progressively smaller as I keep recursing into my sub trees. What's the initial bounding box actually? The initial bounding box when I first call the query function, Is going to be the entire unit square, I guess. So maybe I'll just define that box. I'll make a vector of zeros and a vector of ones. Those are the upper and the lower ranges of my initial bounding box. 

Okay, so I've created my initial bounding box. It ranges from zero to one in every coordinate and I'm passing that into my range count function. Now, my range count function takes the bounding box and passes that along to my children. But I do need to modify my bounding box every time I recurse. So let's maybe take a look at what that picture would look like. So if I go back to my drawing tablet here. So if I look at my coordinate axes, my initial bounding box is this, 0 to 1, 0 to 1 unit square, I have all of my different points. 

So let's say that here's this red point here, that's the root point, that's the point that the root splits on. And remember, the root splits on x, the first dimension. And so it's going to kind of split according to a cutoff in the first dimension, in the x dimension, and I'm recursing to the left initially. So what happens to my bounding box? Initially your bounding box is that entire square, the entire unit square. But now the bounding box is going to be restricted by the cutoff point of the root in x. So my bounding box is going to have to have its high point in dimension 0 replaced by the x coordinate (the dimension 0 coordinate) of the root. 

And similarly, when I recurse to the right, the same thing is going to have to happen. But my bounding box is going to have to have its low point replaced by the x coordinate of the root. So let's see if we can implement that in code. So if I go back to my coding view here. Then before I recurse to the left, I wanted to do what? Replace a high range of bounding box in dimension dim with the root value. That's kind of what I'm trying to do. 

So in code that would be saying that the bounding box.high in the current dimension is going to get replaced by the root point, which is p[root] index in the current dimension. Okay, and then similarly, before I recurse to the right, I'm going to update the low value of my bounding box in this dimension to the roots value in this dimension. So, restricting the bounding box based on the cutoff point stored at the root. One thing that I probably also need to do, I don't want these changes to be cumulative. So, when I recurse to the right, I don't want the high value of the bounding box affected. I want the original high value of the bounding box. So I want to put back the old value. 

So maybe I need to actually store the old value of this thing and then put it back after I recurse. This is actually something we do fairly often with recursive functions is you save a value, you kind of temporarily modify it for the purpose of recursing. So we modify our bounding box to recurse left, but then we put back the old bounding box. And then we modify it temporarily to recurse to the right, and then we revert it back to what it used to be. So again, I'm going to, before I recurse to the right, save the old value of the low end of that range and then I'll put it back at the end. You might ask why you need to do that. In this case, because this is the last thing we're doing in the function. 

So who cares if you modify the bounding box, but we're passing it by reference. So whoever called us, we want to be polite and put the bounding box back in the same form that we found it for that person. Whoever called us, we don't want to pass back bounding box that's been modified in a weird way. So I'm going to set this back to old. Okay, so that was a little bit of a chore, but we have now maintained, hopefully, our bounding box as we are traversing through the tree. And if that bounding box ever becomes disjoint from the query box, we can bail out. Okay, and actually I can probably move this check even earlier. 

I'm not sure that makes a huge difference because n is not going to be set to one if we're completely disjoint from the roots point. So let's go ahead and compile and run. One nice thing about these sorts of optimizations is they shouldn't change the answer, right? They should only make it run faster. We should still get as an answer to this 1,000,212 number. In fact, just so we can represent or remember kind of what kind of speedup we've gotten. Let's copy the current executable to maybe a new executable, I'll call it slow. 

So that we can always run the old one and run that maybe side by side with the new one to see how much of a speedup we're getting. So if I recompile kd.cpp and I rerun, then it takes a second or two to build the tree, but hopefully it runs a little bit faster. If we can remember the speed that it ran out before, which I guess we can run slow, uh oh 00000. I don't believe that should be the right answer. So, it seems like the pruning code is being too aggressive here if the boxes don't overlap. So there are two things that could be going wrong. Either the boxes overlap function is not working correctly or we're not maintaining our bounding box correctly. 

So either one of those two things could be the problem. Let's take one more look at the box overlap function here. So we go through all of our dimensions, so, if b1's high is less than b2's low, or if b1's low is bigger than b2's high, return false. So that means if b1 and b2 are disjoint in this dimension, then we return false, because they definitely don't overlap in that case. But if they pass all of these cases, then we return true. So that's actually fine. And then we can check the other situation. 

So we update the high point of our bounding box in the current dimension. Before we recurse to the left and we pass that bounding box along, we update that to the roots value in this dimension. Then we do the same thing for the low point when we recurse to the right. That actually looks reasonable. So what is not happening? Are we initializing our bounding box correctly? [LAUGH] Yes, I had typed 0 for the lows and the highs. 

Okay, that was very silly. So our initial bounding box was not set correctly. It was set to 0 by 0 box instead of a 0 by 1 box. Okay, let's try this now. That was a fun bit of debugging. So hopefully now we will get that 1,000,212 number if we see the answer to our queries. And it looks like they're actually running a little bit faster. 

So that's also good. That's nice to see. So is there anything else we can do in terms of potential speed up? I think there's maybe one other possible optimization that we can add. Another base case here. If we are disjoint from the query entirely, then we can just stop at this point. But we can also say that if the query box contains our bounding box entirely, that means that we've wandered to a place where our entire subtree now lives inside the query. 

There's kind of no point recursing onwards through our subtree because every single point in our subtree at this point will be an answer. And we know the size of our subtree. So in this case, I can just return root->size. That's the size of our subtree. Every single point in the subtree will be an answer. No need to count them one by one at this point. So all I need to do is write kind of a box contains function. 

If query box contains bounding box, then return root size. So let's see, if box_contains(Box). Let's maybe say that I have the big box and the small box, so big should contain small. Okay, and I called those with the right order, qbox and bbox. So if the query box contains our bounding box. All right, so how do I implement this? I think, again, we're going to have a loop over all of our coordinates and we're going to have to, in the same sort of structure, basically say, is there some sort of disqualifying condition? 

Will the box not be contained? If so return false, and if it makes it past all of those, then I'll be returning true. So what's the disqualifying condition here? Small is not contained in big. If (small is outside big in the jth dimension), if there's some dimension where it's not contained, if kind of small wanders outside the range of big, then we're not overall contained within that box. So small is outside the big box in the jth dimension. So how do I represent that? 

So I could say if small.low in the jth dimension is less than big.low in the jth dimension, or if small.high in the jth dimension is bigger than big.high in the jth dimension. So if small kind of extends out beyond the big interval in the jth dimension, then we're not contained. Otherwise we are. Okay, so now I should be able to recompile. And I just typed, that's another typo. Okay, let me fix that real quick. Type in h instead of a j. 

Recompile and rerun. Again, we'll rerun it, and then we'll see if we can somehow measure the speed up here that we're getting. So we're building the tree, and that seems to have run much faster. That's good. How much faster did that run, actually, compared to the original? I've kind of forgotten how slow the original slow one ran. So maybe what I'll do here is I'll run a lot of queries because we're running much, much faster here. 

So I'll run a whole bunch of the same identical query, let's run 100 times more of those queries, and then finally I'll print out “FAST done”. So this will basically stress test the fast solution. It looks like it's correct. It's getting the same answer. So that's good. At least if I run the original slow program in parallel alongside my now faster program, then now I've just launched them. They're both running in the background at the same time. 

They're both building their KD trees. It's like a foot race. They're both running simultaneously. They built the tree. Okay, and now we're off to the races. The slow program is doing their slow queries. That's what we're seeing here, 20 of them. 

And the fast program is chugging along, and at some point “fast” is done. So actually they finished right about at exactly the same time. So I would say we have roughly about a 100 times speed up in the optimizations that we've done with fast versus slow. So that's pretty dramatic. So definitely worthwhile to take the time to prune these sorts of searches effectively. So I guess that’s basically it, we've accomplished our second goal here. We've done fast range queries in d dimensions in a kd-tree with appropriate pruning. 

Hopefully this is useful when you write some kd-tree code for your next assignment. But hopefully this has been useful in just understanding how multi-dimensional search structures in general can appropriately generalize a one dimensional version of the same structure without too much headache. So I'll leave that with you, and good luck with your own multidimensional search structure implementations. [MUSIC] 

[MUSIC] Brian Dean: It's often surprisingly not that much more difficult to build a multi-dimensional data structure than its one-dimensional counterpart. As hopefully we will see in our coding discussion at the moment involving multi-dimensional range queries. Our multi-dimensional range query data structure of choice at the moment will be a kd-tree. Partly because it's a relatively simple data structure very closely related to the original one- dimensional binary search tree. And also because we have an upcoming programming assignment that will involve heavily the use of kd-trees. So I thought it would be helpful for people to see an implementation involving kd-trees before that gets going. So our goals with this exercise are relatively straightforward.  Play video starting at ::46 and follow transcript0:46 Our first goal is simply to build a kd-tree, and we're going to try and make our code work in any dimensional space. Although we'll focus just for simplicity on a two dimensional set of points in the example that we're going to write here. And then once we've built our kd-tree, we're going to use it to answer range counting queries over rectangles. So given any rectangular region, how many points are contained within that rectangular region, so relatively straightforward. The tricky part I think is going to end up being making those range queries run quickly. So adding the appropriate amount of pruning to speed them up and avoid searching unnecessary parts of the kd-tree. If you remember from our standard diagrams of kd-trees, every single node in a kd-tree holds a point and represents also kind of a rectangular region of space.  Play video starting at :1:34 and follow transcript1:34 So as you're traversing a kd-tree and trying to find all the points that live inside of a rectangle. Occasionally your traversal might land outside of that range, in which case you don't have to keep exploring and traversing the kd-tree. You can stop, you can prune the search. Furthermore, if you end up in a rectangle, I guess completely inside your query rectangle, then you also probably don't need to further recurse onto smaller and smaller rectangles. You can basically say every point at that point is going to be inside my query region and just return the size of my subtree, for example. So there's a couple of optimizations that we can do to speed up these searches fairly dramatically. So let's go ahead and get started.  Play video starting at :2:14 and follow transcript2:14 To begin with, instead of starting with a blank slate, I'm going to start with the code that we wrote in the last module for a binary search tree. Because I really do want to highlight how the kd-tree is really just a baby step more than a regular binary search tree. So I'm going to copy bst.cpp into kd.cpp as my starting point and then edit kd.cpp. So this is my starting point. This is the binary search tree code that we wrote before it was used to store a set of integers. So every single node just has an integer key and keeps track of its subtree size. The only code that I've kept here is the insert function because we're going to be modifying that and then not using any of the other functions from our binary search tree.  Play video starting at :2:59 and follow transcript2:59 So remember the way we called this was we started with an empty tree. So node *root = nullptr; empty binary search tree. And then we called the insert function by saying root = insert(root). And then the key that we were inserting and the insert function returned back a pointer to the new root of the tree post insert. So how are we going to modify that insert function and the structure of our tree so that now it represents not a binary search tree, but a kd-tree. So every single node in a kd-tree doesn't just hold an integer key, it holds a d-dimensional point. And so it might seem necessary to store something like an array of in five dimensions, maybe an array of five doubles or something.  Play video starting at :3:45 and follow transcript3:45 So you have an entire point represented in each node. Instead of doing that, that's a little bit heavy. I think what I'd rather do is something more lightweight. I'm going to store the actual points outside of the data structure as we commonly do. And then every single node in the data structure can be more of a lightweight thing that just indexes into my array of points that I'm storing on the side. So instead of keeping the points explicitly represented in the nodes, maybe I'll just make an array here about- I'll keep end points- How many points? A large number of points.  Play video starting at :4:21 and follow transcript4:21 Let's go for it. About maybe 4 million points. 4 million, how about that? And then I'll be operating in two dimensions. We can always change the number of points and the number of dimensions if we wanted to. I'll try and make sure everything stays in terms of capital N and capital D. So all we have to do is change one number and everything will change appropriately. So I'll just make a global array of N by D that holds all of my points.  Play video starting at :4:48 and follow transcript4:48 This is my data matrix. And so basically p[i][j] is going to be the jth coordinate of the ith point. So every row in this matrix is, is a point, effectively. So now in my node structure I don't have to actually store all of the coordinates of a point. I can just store an index into my data matrix. And so this particular node refers to the point P of index and whatever data is stored in its respective coordinates. So that also is nice, because when I'm inserting something into the structure, I can still just pass an int, which is now an index, into my point double dimensional matrix.  Play video starting at :5:34 and follow transcript5:34 So I'm just passing in an index here instead of a key, basically, but I'm still passing an integer, which is kind of nice. So if I go back to the main function, the interpretation here is that I'm inserting into the data structure point 17 instead of the value 17. So I guess that means anywhere I had the value k that kind of turns into an index. So let me just make those cosmetic changes. I renamed it index instead of key. We'll have to make a few more changes here, but let me just kind of make the obvious changes first. Okay, I've gotten rid of all the keys and the k's and they're all indexes now.  Play video starting at :6:16 and follow transcript6:16 Okay, excellent. One other thing that's a hallmark of kd-tree based code is that you're cycling through dimensions. Remember, the root represents kind of the root holds a point, but also represents a split on x. And then the next level down in the tree, you split on y, and then you split on z, and you basically keep advancing to the next dimension until you run out of dimensions. Then you have to cycle back to the dimension you started in. And so almost any function for a kd-tree is going to have one extra parameter added onto it, and that's the current dimension. So every time you call that function, you're going to start out in dimension zero, the x dimension, the first dimension.  Play video starting at :6:54 and follow transcript6:54 And then when the function recurses, it's always going to recurse in a cyclic fashion, advancing the dimensions. So it's gonna recurse on dimension dim+1 for the next level down in the tree. And since we want to make sure that we cycle back when we run out of dimensions, we often add a mod capital D here to the end,. So that if you're in dimension D minus one and you advance instead of going to D, which would be too far, you cycle back around to zero. So I'm going to add this and you're going to see this sort of pattern in just about any function that goes around and traverses through a kd-tree. All right, so far we haven't really changed the structure of insert very much at all. It looks essentially the same as the insert function in a binary search tree.  Play video starting at :7:39 and follow transcript7:39 The only line I think that I have to change is this one, the one that decides if I go left or right with my insert. Do I delegate the insert to the left subtree or to the right subtree? Previously I checked if the key that I was inserting was less than the key stored at the root to go left or right. And I'm basically doing that here as well. It's just now the decision is being made in the current dimension because every single node represents an entire d-dimensional point. And so, I'm looking at that point only focusing on the one current dimension. So, like at the root, I'm looking at the x dimension.  Play video starting at :8:13 and follow transcript8:13 The next level down, I look at the y dimension, and so I'm looking at p of index dim. So index is the index of the point that I'm inserting, and dim is the current dimension. So in the current dimension is the point that I'm inserting less than the point at the root, which is p of root index dim. Okay, that's it. That's the only change that I think I need to make to create a kd-tree instead of creating a binary search tree. So, it's basically just adding on bracket dim and just looking at the current dimension instead of just comparing directly a specific key value and then cycling through those dimensions as I continue to recurse through the tree. So, let's maybe just compile this to make sure that I didn't break anything too badly yet.  Play video starting at :9:5 and follow transcript9:05 Okay, so we're compiling, let's build a kd-tree out of our 4 million points that I promised to create. So, I'm just going to make a bunch of random points. So, what's a good point set to use here? So, we're working in two dimensions. Maybe I'll just put a bunch of points in a unit square. So going from zero to one in x and going from zero to one in y. So, we'll just have a whole bunch of points spread across the unit square uniformly.  Play video starting at :9:35 and follow transcript9:35 That'll be our data set that we're going to deal with here. So, I'm just going to generate a bunch of random points and store them in my kd-tree. So let's do that. So, loop over all the points. I'm going to fill in that data matrix basically with random values. It's j=0, j<D, j++. Okay, and then I'm just going to set p of ij = drand48(), I think is the function in C that gives me a random real number in the range 0 to 1.  Play video starting at :10:11 and follow transcript10:11 So every coordinate of every point is being set to a random number between zero and one. So that will indeed populate my data matrix with points that live in that unit square, essentially. And then I guess once I've generated each point, I can go ahead and insert it into the kd-tree. So, I've generated the ith point and then I can tell the kd-tree to insert the indexed ith point starting in dimension zero. So this should basically build my kd-tree. So, after this, maybe I need an appropriate exclamation of success. “Yay, built tree.”  Play video starting at :10:45 and follow transcript10:45 Okay, so let's maybe compile and run and see how long it actually takes to build a kd-tree on 4 million points. Maybe that was a bit ambitious, we'll see. So, if I recompile and I run and now we wait, how long does it take to build that kd-tree? At least I'm inserting effectively random points, you know, points in random. That wasn't too bad. Okay, we'll keep that. Normally if you're building a tree like this, you have to worry about keeping it balanced.  Play video starting at :11:14 and follow transcript11:14 And one way to keep it balanced, of course, is to keep it random. Conveniently, by the way, I've generated my points completely at random, then the tree should end up relatively balanced. So balance won't be a concern of us at this point. Okay, so we have completed our first goal. We've built a kd-tree. Now we need to do a range query in that kd-tree over a rectangular range. So what's a good rectangular range over which to do our query?  Play video starting at :11:45 and follow transcript11:45 How about we do the following? So, since we have 4 million points, if I define a rectangular range by going from kind of like 0.25 up to 0.75 in each dimension, so 0.25, 0.75. So I'll kind of define a box that has those upper and lower points and I'll ask how many points are inside that box. So that range represents exactly half of the extent in x and half of the extent in y. So, it's a quarter of the entire unit square. So the total number of points in that range should be roughly one quarter of all of my points because they're uniformly spread across the entire rectangle. So, I should expect about a million points to be the answer to a range query over that range.  Play video starting at :12:30 and follow transcript12:30 So that gives us at least a ballpark sense of what we should be expecting as our output. Let's define that range and issue a query over it. So how do I define that range? I need some sort of like a box structure that I'm going to be like my query box is going to be something. So how do I define a query box that I'm going to do a range query over? And it has to be something that works in d-dimensions. So, I guess a query box is defined by a range in each dimension, kind of a low to a high in each dimension.  Play video starting at :13:7 and follow transcript13:07 So maybe I'll make a vector, typing is hard, of maybe low ends of the range and high ends of the range. So the low vector is all the low endpoints and the high vector is all the high endpoints. Each vector is going to have length d, I guess. Did I include, if I'm using vectors I probably need to include, I need to include the vector header here. So, #include <vector>. All right, so that's my structure for a box. If I want to now create that particular query box, that's going to be the low ranges and the high ranges where each of those is a vector.  Play video starting at :13:46 and follow transcript13:46 So, a vector of doubles. The low range was going to be D numbers that are all initialized to 0.25 and the high range is going to be D numbers that are all initialized to 0.75. Okay, so that's my query box and now I can just issue a range query. I want to count the number of points in that box. So let's ask. I'll pass along the root pointer of my tree. So, in this tree, count the number of points in this query box.  Play video starting at :14:14 and follow transcript14:14 And I probably need to start with the dimension 0 as I always do with kd-tree based functions. So, this is going to be my range query function. Let's write that function. So, my range query function is going to return a count of how many points are inside that box, I pass in the root of the tree, I pass in the query box. I'm actually going to, let's pass that in by reference. Otherwise I'm going to be making a copy of the query box whenever I call this function. And then when it recurses it's going to be passing copies of the box to itself.  Play video starting at :14:47 and follow transcript14:47 And that's going to take a lot of overhead here. I can basically just pass a pointer behind the scenes and passing it around as a reference. And this is my current dimension. Okay, so inside this function, this is going to be a recursive function that kind of traverses through the tree, hopefully only the parts of the tree that represent the parts that are inside my query and it counts up all the points that live inside that query. So I'm going to have some sort of a counter that counts how many total points and at the end, you know, returns that number. And then somewhere in here I'm going to have a base case probably, you know, if the root is a null pointer then certainly there aren't any points in that. And then I'm going to be recursing at some point.  Play video starting at :15:29 and follow transcript15:29 So, I'm going to call range query count on root->left perhaps. And I'm going to pass in the same query box, and I'm going to pass in dimension +1. And I'm probably going to recurse to the right as well. So, I'm just kind of writing some bits and pieces of code that I think I might need eventually here. So let me back up and kind of think like step-by-step what this function is going to have to do. So, we have our base case, if it's an empty tree, return null, that's fine. Now we typically have to do something with the root node itself.  Play video starting at :16:4 and follow transcript16:04 Remember that every node in a kd-tree represents a point and that root node is no exception. It represents a point. And maybe that point is inside our query, maybe it's not inside our query, but we probably need to tally that point up if it lives in our query. So, if that point is inside the query box, let's see the roots point is root index. That's the index of the root point. If that's inside the query box, then I'm going to count that as one answer to my query, right? So, I left unspecified a function I'm probably going to have to write here that tells me if a point's inside of a box.  Play video starting at :16:41 and follow transcript16:41 Actually, let me just write that function. That shouldn't be too hard. Hopefully. point_in_box and I passed it in as an integer index. The point is referred to as its index in the overall point array. And then the box is a reference to a, let's just call it b, box b. Okay, so a point is inside a box if it's inside the low to high ranges of that box in every single coordinate. So I have to loop over all of the coordinates going from zero up to d.  Play video starting at :17:11 and follow transcript17:11 And if the point is outside range in coordinate j, return false. So, if I'm out of bounds, if I'm not in the coordinate range in one coordinate, then I'm definitely not in the box. Otherwise, if I pass that test for every coordinate, then I am in the box. All I have to do now is just make sure that the point in question is inside the low to high range of the box in this particular coordinate. So, the point in question here is p of index, and I'm looking at this particular coordinate, which is j, and that has to be inside the low to high range. So that has to be at least as large. Or I guess I'm testing if I'm outside here, actually.  Play video starting at :17:56 and follow transcript17:56 So, if it's outside, if it's less than the low range b.low of j, or if it's above the high range. So, if it's outside on either side. Or if P of index j is bigger than b.high of j, then I'm out of range and I return false. Make sure I have enough parentheses there, otherwise return true if I'm not out of range on any of the ranges. Okay, so I've properly tallied up whether or not the contribution of the root node itself is an answer to my query. So n starts out at zero and only becomes one if the root node counts. And then if I recurse to the left or to the right, then I can just add up into my running tally however many points the left and the right subtrees told me we’re in the query region.  Play video starting at :18:44 and follow transcript18:44 So actually, at this point I think I have a working function. It's going to be dead slow because it's currently traversing the entire tree. And for every single node in the tree, just testing if that single point is inside the query box or not and just tallying all those things up. But this should still give me a valid answer and maybe this is a good first step. I can run this and see what I get. Let's print that out actually, hopefully we get a number around a million if we run this. So, if I compile for j, I forgot to type the word int, I think we can all agree that doesn't really count as a bug.  Play video starting at :19:24 and follow transcript19:24 That's just a minor omission. Compile and then we run and it took us what, a couple of seconds to build the tree. And then we're going to get an answer which is hopefully about a million points inside the box. And it is about a million points, 1,000,212 points. So that is a believable answer. And given the simplicity of the code so far, I think that looks relatively good. So actually, how quick is that query code?  Play video starting at :19:58 and follow transcript19:58 It's a little hard to know if you're just making one query. So maybe I can make, let's make a couple queries. I'm going to just loop, let's say 20 times. Why not? And I'll just run the same query 20 times and print out the same answer 20 times. But just to give myself a sense of how quick the individual queries are here. So, recompile and rerun.  Play video starting at :20:24 and follow transcript20:24 So how fast am I thinking these queries are going to take? I build the tree and then I'm going to see that 1,000,212 number appear line after line. So this is how fast our queries are running. Maybe just under a second, one or 2 per second so there's 20 queries. Okay, so that's the time to beat. We need to now figure out how to optimize our queries, hopefully by pruning the search so we don't visit the entire tree during each query. So what are the standard ways that you can prune a search like this so that you don't visit the entire tree.  Play video starting at :21:1 and follow transcript21:01 So, we basically probably just need some more base cases. So first, we're testing if the root point is inside the box. But now what are some other appropriate base cases that we could think about? I guess we could have. There are actually two that come to mind. So one is if we have basically wandered outside the query region entirely, then there's no point in continuing on to recurse. If we go back and kind of look at the picture on our slides here, remember that every single node not only stores a point, but also represents kind of a rectangular region in space.  Play video starting at :21:39 and follow transcript21:39 You can almost think of that as the bounding box of the subtree represented by that node. If that node's bounding box is completely disjoint from the query region. So if I go to the left here and then I go left of y one. So this “A” region right here is completely disjoint from the yellow query rectangle. So, once I reach subtree A, I don't have to keep recursing into subtree A anymore because none of these points are going to be answers. So I can stop right then and there and prune the search. So if the bounding box of my subtree is completely disjoint from the box that represents my query, I can stop.  Play video starting at :22:16 and follow transcript22:16 So that's a good base case. Let's maybe do that and then we'll come back and think about one more useful base case as well. Okay, so if I've wandered outside the query region entirely, the way we had phrased that was if kind of, if boxes are disjoint, my bounding box and the query box basically, then I can just return at this point. And I guess n is going to be zero in that case because my point will certainly not be in the query. So that would be one possible optimization, one way to prune the search. Now I see that I have a little bit more code that I have to write because now I don't have a mechanism for keeping track of my bounding box. I'll probably need to do that.  Play video starting at :23:3 and follow transcript23:03 And I also need this box’s disjoint function. Let me write the box’s disjoint function. That to me, looks maybe easier than the bounding box thing. So, box’s disjoint, and I'll pass in box and I'll pass by reference just to make it faster, b1 and box b2. So how do I tell if two boxes are disjoint? I probably, I suspect I'm again going to have to loop over each of the individual coordinates. If two boxes are disjoint, that means they must be disjoint in- Does it mean that they're disjointed all of their coordinates, or in at least one of their coordinates?  Play video starting at :23:44 and follow transcript23:44 I think they have to be disjoint in all of their coordinates. So actually, probably a better way to think about this. Let me actually write it as boxes overlap, in my mind somehow, that's an easier concept to write in code. So, the fact that they're disjoint means that they don't overlap. So maybe I'll write it as boxes overlap, not. So if the boxes don't overlap the query box and my bounding box, then I return 0, that's a base case. So now how do I test if two boxes overlap or not?  Play video starting at :24:16 and follow transcript24:16 So, they only overlap if they overlap in every dimension. So I can cycle through the dimensions and test if each dimension overlaps. And if every dimension overlaps, then I'm going to return true at the end. So only if one of the dimensions didn't overlap do I return false. So, if there's no overlap in dimension j, then I return false because that means that the boxes don't overlap if one of the dimensions was disjoint. So now I just need to write the logic for no overlap and dimension j, that shouldn't be too bad. So either box b1 was kind of below the other range or above it.  Play video starting at :24:54 and follow transcript24:54 So maybe if b1's high point in this dimension was less than b2's low point in that dimension, b2.low[j]. Or if b1's low point in this dimension is greater than b2's high point in this dimension, that means that their ranges did not overlap and I return false. Okay, so that's my overlap check. Now I need my bounding box actually. So how do I keep track of my bounding box as I guess I continue to recurse through the tree? This is going to be something that I might also have to pass along as a parameter in addition to the query box. So I'm going to pass along a reference to my bounding box as I traverse through the tree.  Play video starting at :25:43 and follow transcript25:43 So that's going to get passed to every recursive call, I guess. In fact, I'm going to have to modify the bounding box as I recurse because the bounding box is getting progressively smaller as I keep recursing into my sub trees. What's the initial bounding box actually? The initial bounding box when I first call the query function, Is going to be the entire unit square, I guess. So maybe I'll just define that box. I'll make a vector of zeros and a vector of ones. Those are the upper and the lower ranges of my initial bounding box.  Play video starting at :26:28 and follow transcript26:28 Okay, so I've created my initial bounding box. It ranges from zero to one in every coordinate and I'm passing that into my range count function. Now, my range count function takes the bounding box and passes that along to my children. But I do need to modify my bounding box every time I recurse. So let's maybe take a look at what that picture would look like. So if I go back to my drawing tablet here. So if I look at my coordinate axes, my initial bounding box is this, 0 to 1, 0 to 1 unit square, I have all of my different points.  Play video starting at :27:8 and follow transcript27:08 So let's say that here's this red point here, that's the root point, that's the point that the root splits on. And remember, the root splits on x, the first dimension. And so it's going to kind of split according to a cutoff in the first dimension, in the x dimension, and I'm recursing to the left initially. So what happens to my bounding box? Initially your bounding box is that entire square, the entire unit square. But now the bounding box is going to be restricted by the cutoff point of the root in x. So my bounding box is going to have to have its high point in dimension 0 replaced by the x coordinate (the dimension 0 coordinate) of the root.  Play video starting at :27:51 and follow transcript27:51 And similarly, when I recurse to the right, the same thing is going to have to happen. But my bounding box is going to have to have its low point replaced by the x coordinate of the root. So let's see if we can implement that in code. So if I go back to my coding view here. Then before I recurse to the left, I wanted to do what? Replace a high range of bounding box in dimension dim with the root value. That's kind of what I'm trying to do.  Play video starting at :28:28 and follow transcript28:28 So in code that would be saying that the bounding box.high in the current dimension is going to get replaced by the root point, which is p[root] index in the current dimension. Okay, and then similarly, before I recurse to the right, I'm going to update the low value of my bounding box in this dimension to the roots value in this dimension. So, restricting the bounding box based on the cutoff point stored at the root. One thing that I probably also need to do, I don't want these changes to be cumulative. So, when I recurse to the right, I don't want the high value of the bounding box affected. I want the original high value of the bounding box. So I want to put back the old value.  Play video starting at :29:18 and follow transcript29:18 So maybe I need to actually store the old value of this thing and then put it back after I recurse. This is actually something we do fairly often with recursive functions is you save a value, you kind of temporarily modify it for the purpose of recursing. So we modify our bounding box to recurse left, but then we put back the old bounding box. And then we modify it temporarily to recurse to the right, and then we revert it back to what it used to be. So again, I'm going to, before I recurse to the right, save the old value of the low end of that range and then I'll put it back at the end. You might ask why you need to do that. In this case, because this is the last thing we're doing in the function.  Play video starting at :30:1 and follow transcript30:01 So who cares if you modify the bounding box, but we're passing it by reference. So whoever called us, we want to be polite and put the bounding box back in the same form that we found it for that person. Whoever called us, we don't want to pass back bounding box that's been modified in a weird way. So I'm going to set this back to old. Okay, so that was a little bit of a chore, but we have now maintained, hopefully, our bounding box as we are traversing through the tree. And if that bounding box ever becomes disjoint from the query box, we can bail out. Okay, and actually I can probably move this check even earlier.  Play video starting at :30:45 and follow transcript30:45 I'm not sure that makes a huge difference because n is not going to be set to one if we're completely disjoint from the roots point. So let's go ahead and compile and run. One nice thing about these sorts of optimizations is they shouldn't change the answer, right? They should only make it run faster. We should still get as an answer to this 1,000,212 number. In fact, just so we can represent or remember kind of what kind of speedup we've gotten. Let's copy the current executable to maybe a new executable, I'll call it slow.  Play video starting at :31:19 and follow transcript31:19 So that we can always run the old one and run that maybe side by side with the new one to see how much of a speedup we're getting. So if I recompile kd.cpp and I rerun, then it takes a second or two to build the tree, but hopefully it runs a little bit faster. If we can remember the speed that it ran out before, which I guess we can run slow, uh oh 00000. I don't believe that should be the right answer. So, it seems like the pruning code is being too aggressive here if the boxes don't overlap. So there are two things that could be going wrong. Either the boxes overlap function is not working correctly or we're not maintaining our bounding box correctly.  Play video starting at :31:59 and follow transcript31:59 So either one of those two things could be the problem. Let's take one more look at the box overlap function here. So we go through all of our dimensions, so, if b1's high is less than b2's low, or if b1's low is bigger than b2's high, return false. So that means if b1 and b2 are disjoint in this dimension, then we return false, because they definitely don't overlap in that case. But if they pass all of these cases, then we return true. So that's actually fine. And then we can check the other situation.  Play video starting at :32:36 and follow transcript32:36 So we update the high point of our bounding box in the current dimension. Before we recurse to the left and we pass that bounding box along, we update that to the roots value in this dimension. Then we do the same thing for the low point when we recurse to the right. That actually looks reasonable. So what is not happening? Are we initializing our bounding box correctly? [LAUGH] Yes, I had typed 0 for the lows and the highs.  Play video starting at :33:2 and follow transcript33:02 Okay, that was very silly. So our initial bounding box was not set correctly. It was set to 0 by 0 box instead of a 0 by 1 box. Okay, let's try this now. That was a fun bit of debugging. So hopefully now we will get that 1,000,212 number if we see the answer to our queries. And it looks like they're actually running a little bit faster.  Play video starting at :33:29 and follow transcript33:29 So that's also good. That's nice to see. So is there anything else we can do in terms of potential speed up? I think there's maybe one other possible optimization that we can add. Another base case here. If we are disjoint from the query entirely, then we can just stop at this point. But we can also say that if the query box contains our bounding box entirely, that means that we've wandered to a place where our entire subtree now lives inside the query.  Play video starting at :34:4 and follow transcript34:04 There's kind of no point recursing onwards through our subtree because every single point in our subtree at this point will be an answer. And we know the size of our subtree. So in this case, I can just return root->size. That's the size of our subtree. Every single point in the subtree will be an answer. No need to count them one by one at this point. So all I need to do is write kind of a box contains function.  Play video starting at :34:29 and follow transcript34:29 If query box contains bounding box, then return root size. So let's see, if box_contains(Box). Let's maybe say that I have the big box and the small box, so big should contain small. Okay, and I called those with the right order, qbox and bbox. So if the query box contains our bounding box. All right, so how do I implement this? I think, again, we're going to have a loop over all of our coordinates and we're going to have to, in the same sort of structure, basically say, is there some sort of disqualifying condition?  Play video starting at :35:19 and follow transcript35:19 Will the box not be contained? If so return false, and if it makes it past all of those, then I'll be returning true. So what's the disqualifying condition here? Small is not contained in big. If (small is outside big in the jth dimension), if there's some dimension where it's not contained, if kind of small wanders outside the range of big, then we're not overall contained within that box. So small is outside the big box in the jth dimension. So how do I represent that?  Play video starting at :35:53 and follow transcript35:53 So I could say if small.low in the jth dimension is less than big.low in the jth dimension, or if small.high in the jth dimension is bigger than big.high in the jth dimension. So if small kind of extends out beyond the big interval in the jth dimension, then we're not contained. Otherwise we are. Okay, so now I should be able to recompile. And I just typed, that's another typo. Okay, let me fix that real quick. Type in h instead of a j.  Play video starting at :36:42 and follow transcript36:42 Recompile and rerun. Again, we'll rerun it, and then we'll see if we can somehow measure the speed up here that we're getting. So we're building the tree, and that seems to have run much faster. That's good. How much faster did that run, actually, compared to the original? I've kind of forgotten how slow the original slow one ran. So maybe what I'll do here is I'll run a lot of queries because we're running much, much faster here.  Play video starting at :37:10 and follow transcript37:10 So I'll run a whole bunch of the same identical query, let's run 100 times more of those queries, and then finally I'll print out “FAST done”. So this will basically stress test the fast solution. It looks like it's correct. It's getting the same answer. So that's good. At least if I run the original slow program in parallel alongside my now faster program, then now I've just launched them. They're both running in the background at the same time.  Play video starting at :37:39 and follow transcript37:39 They're both building their KD trees. It's like a foot race. They're both running simultaneously. They built the tree. Okay, and now we're off to the races. The slow program is doing their slow queries. That's what we're seeing here, 20 of them.  Play video starting at :37:52 and follow transcript37:52 And the fast program is chugging along, and at some point “fast” is done. So actually they finished right about at exactly the same time. So I would say we have roughly about a 100 times speed up in the optimizations that we've done with fast versus slow. So that's pretty dramatic. So definitely worthwhile to take the time to prune these sorts of searches effectively. So I guess that’s basically it, we've accomplished our second goal here. We've done fast range queries in d dimensions in a kd-tree with appropriate pruning.  Play video starting at :38:26 and follow transcript38:26 Hopefully this is useful when you write some kd-tree code for your next assignment. But hopefully this has been useful in just understanding how multi-dimensional search structures in general can appropriately generalize a one dimensional version of the same structure without too much headache. So I'll leave that with you, and good luck with your own multidimensional search structure implementations. [MUSIC]  1:22 / 38:54 1.25x Coding Discussion Save note Transcript Notes Downloads Transcript language: English​Interactive Transcript - Enable basic transcript mode by pressing the escape key You may navigate through the transcript using tab. To save a note for a section of text press ⌘ + S. To expand your selection you may use ⌘ + arrow key. You may contract your selection using shift + ⌘ + arrow key. For screen readers that are incompatible with using arrow keys for shortcuts, you can replace them with the H J K L keys. Some screen readers may require using ⌘ in conjunction with the alt key Play video starting at ::4 and follow transcript0:04 : Added to Selection. Press [⌘ + S] to save as a note Like Dislike Report an issue : Added to Selection. Press [⌘ + S] to save as a note

---