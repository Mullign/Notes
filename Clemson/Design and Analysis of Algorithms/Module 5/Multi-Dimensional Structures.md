![[Multi-Dimensional Structures.pdf]][MUSIC] Our good friend the binary search tree generalizes quite well to representing multi-dimensional data. And that's a good thing because applications involving multi-dimensional data are just about everywhere. To give a few simple examples, think for a second about a database. Suppose you have a database with n records that represent books. Each book is characterized by a page count and a publication date. Then those records are essentially just points in two dimensional space. If your records instead are characterized by d different fields, they would be points in d dimensional space. 

And so you can very easily think about databases and operations on databases from a geometric standpoint, if you want a very modern application that involves very high dimensional data. Of course, AI comes to mind because you're manipulating huge data sets, typically in very, very high dimensional feature spaces. High dimensional data actually does pose quite a few challenges from a data structure perspective. A lot of the data structures that we have are really only effective in low dimensional spaces, and we typically focus on the low dimensional regime when we talk about multidimensional data structures, not because the high dimensional regime is unimportant, but simply just because there are no good data structures in many situations in the high dimensional case. So moving forward, we will focus most of our attention here, at least in low dimensional settings. As I mentioned, problems involving multidimensional data are just about everywhere, including places where you may not expect to find them originally. So maybe a few quick examples of that. 

In this problem here, the input is a set of N intervals on the number line, and we'd like to preprocess those intervals and build a data structure that can handle what we call stabbing queries. Given a query point x, I'd like to count or maybe enumerate all of the intervals that contain x that are stabbed by x. And we can turn this into a multidimensional problem by basically taking every interval which is represented by its start point and its ending point. And we can plot it as a point in two dimensions where the axes represent start point and end point. And so now if you'd like to find all of the intervals stabbed by x, they're the intervals that start earlier than x and that end later than x. So we can turn this into an upper left quadrant kind of query problem in two dimensions. If you want to look at another simple example, suppose I have an array now as input, and I would like to pre-process the array and build a data structure to handle queries of this form. 

Basically in any subarray, tell me all the values that are at most, some threshold. And I can easily turn that into a two dimensional set of points as well by using the location, the index within the array as your x coordinate. And your y coordinate is going to be the value of the element in the array. So this is kind of like building a stem plot, I guess. You basically kind of lift the array up into a two dimensional data set. So if the array has the values 3314 and so on, those get lifted up to points at heights 3314 and so on. And so now if I'm interested in all of the elements in the array that are within a certain range within the array, and at most a certain value, that's kind of like a three sided range query over that set of two dimensional points. 

It has a well defined left and right side and a well defined top, but it extends down to negative infinity in the minus y direction. So many ways that we can turn problems into two dimension or three dimensional problems and apply multi-dimensional data structures to solve them. Perhaps the most direct generalization of the binary search tree to the multidimensional case is the binary space partition tree. So here we recursively subdivide space with just straight linear cuts. Those would be lines in two dimensions, planes in three dimensions, or hyperplanes in higher dimensional spaces. If I focus on 2d for simplicity here, then the root node here is associated with a line that divides space into two half spaces, and the left and the right subtrees correspond to those two half spaces. You can use the structure to hold a bunch of points. 

You can use the structure to hold other geometric objects. In this picture, I'm storing line segments in the binary space partition tree. If it is the case that one of those objects crosses over the dividing line stored at the root, then you do need to split that object into two pieces, one on each side, and store each of those two pieces, respectively, in the two subtrees of the root. So one prominent application of the binary space partition tree is in 3D graphics. Actually, if you store a scene that you'd like to render in a binary space partition tree, then you can actually render it properly from any viewpoint by just traversing the tree, which is kind of clever. A lot of times when you want to render something properly, you have to render it from the back to the front, so that objects that you render later on will properly occlude objects that you drew further behind. And you can do that here based on whichever viewpoint that you're rendering from. 

What you do is you look at which side of the route you're on, and you render the other side first, and then you render the side that you're on, and you just recursively traverse the tree in that manner. And that will actually give you a back to front ordering of all of the elements in the scene from whatever viewpoint you're at. So, lots of nice applications of binary space partition trees. One note about binary space partition trees, they're a little different maybe than standard binary search trees in that they're leaf oriented. Lots of times we build trees in this way where the data itself is only stored at the leaf nodes in the tree. And the interior nodes, the non leaf nodes, they're augmented with enough information to be able to navigate down the tree, just like you would a binary search tree. So maybe, for example, the root node here we might associate with a cut point, kind of like we did with the binary space partition tree. 

That tells us enough information about how to go left or right based on what we're looking for. Or maybe we would augment a node in the tree with the range of values stored in its subtree. Either one of these is enough information to help us navigate down the tree to find a particular element. So this is a fairly common way that we organize information in trees. One note, it might seem like we're wasting space by only storing information in the leaves and not utilizing the interior nodes as well. But as long as every interior node is a branching node, you can easily show, via a simple induction proof that the number of interior nodes in any tree is upper bounded by the number of leaves. So you're not really wasting a tremendous amount of space by storing information only in the leaves. 

Okay, a very common special case of the binary space partition tree is known as a kd-tree or a k-dimensional tree. And here what we do is we still decompose space with straight cuts, but those cuts are parallel to the coordinate axes, and we also kind of cycle through dimensions as we make those cuts. So the very first cut that we make at the root corresponds to a line or a plane or a hyperplane that cuts space according to an x cutoff. So points with smaller x coordinates than the cutoff go to the left, points with bigger x coordinates than the cutoff go to the right. And then if you move down the tree to the next level, the next level down, we make cuts in the Y dimension. So we have kind of a y threshold and points above or below that go left versus right. If we were in three dimensions, the next level would be a z cutoff, and so on. 

And what you do when you when you run out of dimensions is you cycle back around to the first dimension. So in two dimensions, we would alternate x y x y. In three dimensions, it would be customary to rotate x y z x y z and so on. You don't have to do that. There are alternatives, but that's the standard way that a kd-tree typically is organized. So if you build a kd-tree on endpoints, typically you do actually store points in nodes here. So they could be leaf oriented or not. 

But typically, the root would actually represent a point, and the x coordinate of that point would serve as the dividing line between the left and the right subtrees. So you can use these structures for a wide range of applications. You can use them to search for points in multiple dimensional spaces. You can use them for very quickly kind of finding collisions or objects in a region of space. So, one interesting application, this is actually a scene that was rendered by a student at my university a few years back using a ray tracer. And with ray tracing, one of the common tasks, you're trying to simulate the paths taken by rays of light. You're very often trying to figure out, what does this ray intersect with? 

If I have millions and millions of objects in my scene, and I'm casting a ray through that scene, I would like to very quickly figure out what object does that ray intersect with? And by storing the scene in a kd-tree, you can more easily restrict your search for what objects you might be hitting with that ray to a much smaller subset of objects than just all of the objects in the entire scene. So that's one nice application of kd-trees. Another one, maybe from the world of AI and machine learning, would be in identifying nearby points to a target point. You could use this for nearest neighbor classification or k-nearest neighbor classification. So, in this example here, I've plotted a set of people, and each person is characterized by a feature vector of dimension two. So they're all points in the two dimensional plane. 

The two features are height and weight. So those are the two axes here. And the data are classified into two classes. You have males and females. So males are the squares. They tend to be taller and heavier than females. And so if you have an unclassified object, a person coming along who's not in your data set, and you know their height and weight, and you'd like to guess, are they male or female? 

Guess what class they belong to? Then a very standard way to do that is with k-nearest neighbor classification. So you can find your K nearest neighbors in space and maybe take a majority vote. And that's your guess about what class that person would belong to. So I wanted to actually go into a little bit more detail about how k-nearest neighbor classification, or k-nearest neighbor search would work in a kd-tree, especially because our upcoming programming assignment is exactly going to be this. It's going to be building a kd-tree and then doing k-nearest neighbor classification using that kd-tree. So I wanted to go through the details just to make sure that would be helpful for the upcoming assignment. 

So imagine that I have a bunch of points stored in a kd-tree, and I give you a query point p here, this star, and I would like to find, say, the nearest neighbor of point p. So how do I do that in my kd-tree? Well, I'm going to basically traverse the tree in kind of an informed way and prune that traversal. Prune the search anytime I realize that I'm visiting a part of the tree that can never lead to the discovery of a better neighbor than what I've found so far. So I'm going to keep track of a running global best neighbor so far, and I'm going to keep improving that as I traverse through the tree. So to start with, I visit the root. The root actually represents a point, and so I'm going to see is that point my new nearest neighbor. 

So possibly update my new nearest neighbor in response to the point stored at the root. Then I have to decide, do I go left or right? And since the point I'm looking for is on my right, it's on the right of the cutoff specified by the root, I'm going to go that direction first. So it kind of goes in that natural direction from the root and recursively find the nearest neighbor in that subtree. When I'm done, then I will have visited my right subtree. I will have discovered the nearest neighbor on the right hand side of the root, and let's call that distance to the nearest neighbor here, D, now that I've done that, I basically have to make a judgment call about whether it's worth visiting the other subtree. If you want tree data structure to run quickly, then what you really want to do is only visit one of your two children each time you recurse, because then you're on track for a logarithmic running time. 

If you only walk down the tree every step, visiting just one child, if you visit both of your children all the time, you're going to have a much larger running time, because your search is going to fan out and visit a much larger footprint within the tree. So is it worth visiting the other subtree? Is it even possible that I might find nearer neighbors within that subtree? And so typically what we do here is we just look at the distance from our query point to the cutoff line, the one specified by the point at the root. And we typically only look in the one dimension represented by the root. So just the distance in x in this case. So I'm calling that quantity b here. 

So I know that any point that lives in the left subtree of the root has to be at least b distance away from my query point. And if d is less than b, that means that there's no way a point in my left subtree is going to become the new nearest neighbor because I've already found a point closer than this distance b. So if d < b, I can kind of prune the search and avoid visiting my left subtree. Otherwise I have to visit the left subtree because there could conceivably be points in that left subtree that are closer than d to me. So if you look at what happens as a result, you kind of fan out through the tree and you only end up visiting a very small footprint within the tree. This is kind of pictured here on the right. Another way to think about KD trees is that every single node represents a rectangle in space, its subtree represents a rectangle in space. 

And so the collection of all those rectangles is what I've pictured here. And the shaded rectangles are the ones that I have fanned out and visited while trying to find the nearest neighbors of point x. So you can see that you only involve visiting a small number of nodes in the tree. You can kind of prune the search any time you wander too far away from your target point. You can easily generalize this if you want to find not just your nearest neighbor, but say your k nearest neighbors, your seven nearest neighbors, your 70 nearest neighbors, and so on. So the main difference is instead of maintaining your best neighbor as you do the search, you maintain a set of your best k neighbors. And you typically organize that in the structure of a priority queue, because if a new neighbor is added into that set, then who gets evicted? 

But the one that's currently the worst one in that set, the one that's the farthest away from you in that set. So you probably use a max priority queue keyed on distance from the query point. So that if you do add a new node to that set, you know who to evict and actually who to compare with to see if it's worth evicting that point. So as you traverse the tree, you're going to do essentially the same sort of traversal as before. If the query point is on the right of the root, then you first traverse the right subtree and then possibly the left. If it looks like there could still be options in the left subtree. And that's also easy to determine because once you've traversed the right subtree, then you'll have a set of the k-best neighbors you've already found on the right hand side of the root. 

All you have to do is figure out could there possibly be points on the left that are still interesting to look at? And you can make that same sort of comparison we made before, we let b be this bound on distance to the cutoff given by the root. So in just the one dimension represented by the root in the x dimension, what's the distance between the roots cutoff and the query point? That's a lower bound on distance from the query point to anybody on the other side of the root in the left subtree. And if the worst of our k-best nearest neighbors is already less than that distance, that means there's no way there's a point in the left subtree that's going to be better than any of the k-nearest neighbors we've found so far. So again, we can make a simple comparison between this distance b and d, which is kind of the worst of our k-nearest neighbors we found so far. And that determines if we have to recurse to the left and also visit our left subtree. 

So again, this method can involve only searching a very small amount of the tree and kind of prune the search any time you wander too far away from the query point. While we're on the subject of machine learning classification in kd-trees, it's probably good to point out another very prominent classifier in machine learning that effectively is a kd-tree, and that's a low depth decision tree. So here I've shown an example for classifying, again, males versus females. So the root node here would have a cutoff based on weight. If you're heavier than that cutoff, you're deemed to be a male. If you're lighter than that cutoff, then the next cutoff you look at is one in height and so on. So at the root there's an x split, and then the next level down there's a Y split, and so on. 

These are used very commonly in machine learning applications. A single decision tree may not do a great job just by itself, but a lot of times these are used in ensemble methods with techniques like boosting to build very strong classifiers. You build a series of decision trees, each one of which is designed to kind of focus on getting right the points that the other decision trees you've added so far don't get right. And so you can build up this ensemble of decision trees, take kind of a weighted majority vote amongst what they all recommend as a classification, and that typically results in a very strong, very high accuracy result for your classifier. Finally, we have one more type of recursive spatial subdivision to add to our lineup here, and that's sometimes called a quadtree. So it's a very natural idea as well. You're basically, instead of partitioning space by cuts into two pieces, you're actually partitioning based on cuts into four quadrants. 

Now. So we pick a central point, typically defined by the median x and the median y, although we could pick a random x value and a random y value from our scene as well, and we cut space into four quadrants at that point. So we actually end up with a tree with a branching factor of four. The root branches into four pieces, each corresponding to a quadrant, and then those pieces are recursively decomposed into four quadrants, and so on. You can do many of the same things that we had done with kdtrees and binary space partition trees. Here you can search for points. You can do near neighbor queries, those types of applications. 

I'm not a huge fan of quadtrees myself, because they really don't generalize well in terms of dimensionality. In three dimensions, you have what's called an oct tree, because you've decomposed space into eight octants at each step. So the branching factor is now eight. If you go to four dimensions, the branching factor is 16. And so the branching factor actually increases exponentially with dimension. In ten dimensions, you have a branching factor of 1,024, which is not great. So these might work well in maybe two or three dimensions, but I definitely would not consider them in higher dimensional applications. 

Just like our previous trees, though, building them is relatively fast. They only take a linear amount of space when built, and the height is going to be logarithmic. And this is true with any of the other trees that we've considered as well, as long as you're dividing in a relatively balanced fashion at each step. So you either pick kind of a median value to divide on, or you pick a random value to divide on. That'll give you a height that's either logarithmic in the worst case, or logarithmic in expectation or with high probability if you're picking random points for your dividing points. So many alternatives if you would like to look at multidimensional generalizations of a binary search tree. [MUSIC]