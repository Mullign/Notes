![[Binary Search Tree Balancing.pdf]]
Dean: Most binary search tree operations can be implemented in a straightforward recursive fashion. Really the hard part about implementing a binary search tree is usually keeping it balanced, although this is, of course, very important for the performance of the tree. In this video, let's talk about how we keep binary search trees balanced, looking in particular for methods that are simple and straightforward to implement. Many mechanisms for balancing have been proposed over the years. Most of them modify the insert and delete operations so that if the tree gets out of balance, the tree is then restructured to be more balanced. Usually, that restructuring happens via a very simple local modification called a rotation. As pictured here, rotations come in two symmetric forms. 

You talk about rotating an edge X, Y in the tree, so a right rotation about the edge X, Y would look like moving from the left picture to the right picture. Then a left rotation would move you back from the right picture to the left picture. If you look at the in-order traversal of your tree before and after the rotation, it's exactly the same. For example, you have the subtree A, followed by X, followed by the subtree B, followed by Y, followed by the subtree C. That's the same in-order traversal you would get after you did a right rotation. In-order traversals are not affected by rotations. If you're storing a sequence in your binary search tree, that sequence is not affected by doing a rotation. 

Rotations are safe to do as a way of restructuring a tree, but they don't affect the binary search tree property in any adverse way. Many of these balancing mechanisms use rotations to shift elements around in a tree to restructure it and keep it balanced. Now, you can divide all the balancing mechanisms in the world into three main classes. The worst case balancing mechanisms, keep the height always upper bounded by order log n. That might look preferable to the other mechanisms because it's a very strong bound. On the downside, these tend to be the most complicated methods. They usually have a lot of special cases. 

They're really painful in some cases to implement. It's interesting because one of these, the red-black tree seems to me to be a very popular approach in practice, maybe because it appears in a lot of algorithms textbooks, but it's one of the most complicated methods you can possibly find. It's actually at the bottom of my list of favorites. One of the only data structures that I'm not really a fan of. You can also look at amortized mechanisms where the height can actually be more than log n at certain points in time, but if you average over a sequence of operations, everything comes out in the wash and you get an average running time of order log n. Starting with an empty tree, if you do k operations, the total running time is k times log n. Every operation takes log n amortized time. 

You can also look at, and these might be my favorites, randomized balancing mechanisms, exploiting the equivalence between randomized quicksort and building a binary search tree in random order. These mechanisms keep the binary search tree always in a state as if you have built it randomly from scratch, which we already know is a state in which it will be balanced with high probability. Lots of different alternatives to choose from. In this discussion, I'll pick and choose a few examples from each of these classes just to showcase the variety of different approaches that we have available. Let's maybe look at one of these worst case mechanisms. This is called an AVL tree, named after the last names of the inventors of the structure. The way that we're going to set this up is very stereotypical for many of these mechanisms. 

We're going to define some property. But here, the property is called the height balance property. For a red-black tree, it'll be the red black tree property. All we need to do is show that if that property holds, then the tree is balanced. Then the property can serve as a proxy for balance, and all we need to do is maintain that property. With an AVL tree, these are also called height balanced trees, all we need to do is maintain the height balance property, that says that for every node in the tree, if you look at the heights of its two subtrees, they differ by at most one. That intuitively feels like the tree should be reasonably balanced, and you can actually prove with a straightforward induction proof that if you are height balanced, you are balanced. 

Your overall height is on the order of log n. All we need to do is maintain the height balanced property, and we can always do that after each insert or delete by a number of carefully chosen rotations so that the running time of insert and delete is still logarithmic in n. That's the high level picture. There are two key details to fill in here. One of them is proving that if you're height balanced, then you're balanced. We'll do that first, and then we'll show how to apply some carefully chosen rotations to restore the height balanced property when it breaks. First of all, suppose you have a tree that is height balanced, you can actually show that it's balanced with a simple induction proof, and it follows a fairly stereotypical line of attack, I guess, for showing that the tree has to be balanced. 

What we're going to do is prove the following claim that if you have a height balanced tree of a certain height, it has to have a lot of nodes, basically exponentially many nodes in its height, which means the height can't get very big because the size of the tree grows exponentially in the height. If you have a tree that if you add layers in height, suppose the size of the tree doubled every single layer you added, you could only add a logarithmic number of levels. Same thing is happening here, except instead of doubling, we're growing at an exponential rate where the base of the exponent is actually this magical golden ratio that you see in many different mathematical applications, the 1.618. That comes from a connection with Fibonacci numbers, which are part of the induction proof that we're going to do. We basically want to show that if you have a height balanced tree of height h, then it must have at least F_h nodes, F_h being the h^th Fibonacci number. That's not that hard to show. Let me switch over to my whiteboard, and we can actually work through that proof. 

Remember the Fibonacci numbers are defined very straightforwardly. The first couple of Fibonacci numbers are these, and to find each Fibonacci number, it's the sum of the previous two Fibonacci numbers. The nth Fibonacci number is given by the sum of the n minus first and the n minus second Fibonacci number. A simple way to define them recursively. Now, suppose we have a height balanced tree, what that means is, if you look at a particular element in the tree, say that element has a height of h, then if you look at the two subtrees of e, their heights can differ by at most one. My picture is a little bit confusing here. Let me see if I can redraw this in a cleaner way. 

One of your two subtrees, I'll draw this little bracket out here, the height of e is h. That means that one of e's subtrees has to itself have height h-1, and the other subtree would have, I guess height, either h-1 or h-2. Those are the only alternatives for the height of the other subtree if e's height is h. In e's subtree, I'm going to show that the number of elements is at least F_h, the h^th Fibonacci number. To do this, I can actually use induction. I know that one of my subtrees has a height of h-1, so I can invoke induction to argue that that subtree has to have at least F_h-1 elements. Then the other subtree has a height of h-1 or h-2, so by induction, it has to have at least F_h-2 elements. 

The total number of elements in the entire tree rooted at e is going to be the sum of those two things. When you add up F_h-1 and F_h-2, I guess even plus one for counting e itself, you're going to get the F_h^th Fibonacci number. That's the induction proof. It's actually relatively straightforward. It's known that the Fibonacci numbers grow at an exponential rate. F_h is on the order of the golden ratio raised to the h^th power. Indeed, the number of elements in a tree grows exponentially in its height, and that's basically all you need to show. 

That means that the height can't grow more than on the order of log n, because otherwise, you'd end up with more than n elements in your tree, and that would be a contradiction. Height balance implies balance. Now we need to show that once we have broken the height balance property with an insert or a delete, we can restore it with some carefully chosen rotations. I'm just going to look at the case of insert because that's not too bad. Delete has a couple of more complicated special cases. It's characteristic, as I mentioned, for these worst case algorithms to be a little bit messy in terms of special cases. If I was teaching a class on this, not expect my students to have to memorize all of these special cases, that's not really that important. 

They can always be looked up if they have to be. But there are lots of special cases usually with these types of mechanisms. Let's suppose we've done an insert, and we've just inserted this yellow element in the tree, and that has broken the height balance property somewhere. Somewhere above that newly inserted element, a node is going to no longer satisfy the height balance property because if you look at its two subtrees, now their heights differ by more than one. That would be node z in my picture right here. The left subtree is two more than the right subtree in height. Every time you insert an element, the way we're going to make this work is you walk back up the tree, and you are going to augment the nodes in your tree with their heights, so you know what the heights of all the nodes are. 

When you reach a node, these two subtrees differ by more than one in height, then you stop. Basically, walk up from the newly inserted element until you reach an unhappy element like z here, and that's the place that we're going to focus on fixing the broken height balance property. How are we going to do that? There's really just two main cases here. Suppose that we're looking at essentially the same picture, z is that unhappy element. That's the first element where we violated the height balance property, walking up from the newly inserted element. X is the left child of z. 

There's a symmetric case to this. Imagine for a second that the newly inserted element is in subtree A in this picture right here. That's the easy case because just doing a single right rotation on the x to z edge effectively raises up subtree A and alleviates the height imbalance, and makes everything happy again. That's the easy case. If you imagine that the newly inserted element was in subtree B instead, it's going to have to be in A or B because we're presuming that we inserted into the left subtree of Z, and that created this imbalance with the left subtree being much taller than the right subtree. If the newly inserted element were in B, you can see why that would be a problem because when you do this rotation, B's overall depth doesn't change. Doing this rotation wouldn't help if the element was dangling off of B in the same way that it is off of A right here. 

In that case, we have to look at two special subcases that can happen if the element was inserted into the left or the right subtree of B. But it turns out that irrespective of which is the case, you can do a left rotation on this X to Y edge, and that will bring you back into the former case, where you can then do a right rotation on the edge involving the grandparent z. That's roughly the configuration of rotations that you need to do to fix things in the case of an insert. There's a symmetric set of cases when I guess the right subtree of z was the one that was too deep. Then there's a lot of cases to deal with when you delete. These balancing mechanisms do get a little bit messy when you try and actually figure out the exact sequence of rotations that you need to do to restore whatever property it is. There are some balancing mechanisms even, one of them is called a weight balance mechanism, where it's so complicated that I think the original research paper that described the technique actually had errors in it that nobody noticed for a very long time just because it was so complicated. 

Briefly, if you want to see the high-level outlook on another one of these worst case mechanisms, we can talk about what red-black trees are. This is another mechanism that you do find implemented pretty broadly in practice. But to me, this is the most complicated mechanism out there just in terms of the number of special cases. I would probably steer clear of it if I had to choose between the different approaches. Here we have a red-black property where you can read what it is on the slide. Basically, every node is colored red or black, and there's two key things that have to happen. If you look at any root to leaf path, the number of black nodes on every such path has to be the same, and you can't have any two adjacent red nodes. 

If you start looking into that property, it starts making sense why that implies balance. The same number of black nodes implies that every root to leaf path has similar length, and you can't put too many red nodes on those paths because the red nodes have to be spaced out by black nodes. You can't have too many root to leaf paths that differ in length by a huge amount. That's roughly kind of why things are balanced. You can prove with a similar induction proof, that the red-black tree property implies balance. Then you go through a very messy sequence of cases to show how you can restore the red-black tree property with a series of rotations and recolorings anytime it breaks. I'll leave further details to the viewer if they're interested. 

You can definitely read up on all of the gory details if you want. My goal here is to showcase simpler mechanisms so that you don't have to get mired into all of these different special cases. One interesting way that you can do that in the case of a red-black tree is you can actually notice that the red-black tree is isomorphic to another type of balanced tree. When we study B-trees shortly in this class, it turns out that there's a one-to-one mapping between a certain type of B- tree called a 2-3-4 tree, and a red-black tree. Whatever the complicated mechanism is that's balancing a red-black tree, an equivalent mechanism is also balancing a B tree, and it looks much simpler from the vantage point of a B-tree. Since we're going to study B-trees, we're implicitly effectively studying red-black trees indirectly that way. I think the B-tree balancing mechanism looks a lot simpler. 

It's a lot easier to think about. Another bit of evidence that maybe we don't need to directly study red-black trees because they're pretty much equivalent to other balancing mechanisms that we will study that look much simpler. Moving on to my favorite balancing mechanism, this is the randomly built binary search tree. We've already mentioned when we introduced binary search trees and their equivalents with QuickSort, that if you build a binary search tree by inserting its elements in random order, then the binary search tree will come out balanced with high probability. That's wonderful. The trouble is the user, whoever is inserting and deleting things into your binary search tree, may not have it in mind to insert things in random order. They may want to insert elements in a very specific order that's not random. 

How do we leverage this fact that randomly built binary search trees are balanced for an arbitrary binary search tree that's having inserts and deletes done by a malicious adversary, perhaps even? It turns out you can actually inject a little bit of randomness into your structure over time during inserts and deletes, and just give the structure a little random kick whenever you do an insert or a delete and keep it in this state, which is as if it had just been built randomly from scratch. I don't have to keep saying as if it had just been built randomly from scratch. I'll call that the randomized binary search tree or the RBST property. That means that the data structure probabilistically is behaving as if it was just built by inserting its elements in random order from scratch. We're going to keep the tree in that state. It will always be in a state that is balanced with high probability. 

How do you actually characterize a tree that is in this state that satisfies the RBST property? It's actually really easy. The key property is that if you look at any subtree, then within that subtree, every element has an equally likely shot at being the root of that subtree. In the overall tree, every element is equally likely to be at the root. That makes sense because if you were to have built a binary source tree randomly from scratch, every element is equally likely to have been the first one inserted, and that's the element that would have been inserted and made the root. This is the property that we're going to try and maintain, the fact that within any subtree, every element has an equally likely shot at being the root. That guides us in how we have to modify our insert and delete functions, we just have to make sure that property is maintained. 

Suppose we're inserting a new element into our tree. We're inserting an element to bring the size up to n, then from n-1. How do we do that? Well, that new element, the new nth element has to have as much of a shot at being the root of the final tree as any of the former elements. Formerly, all of the n-1 elements in the tree were equally likely to be at the root. Now we're putting a new element in, and we want to give that element just as much of a shot at being at the root as all the other elements. The way to do that is we flip a biased coin, and with probability 1/n, we make the new element the root. 

We insert it at the root. That element has a 1/n chance at being the root, and all the other elements, they still have an equally likely chance of remaining at the root. Every element is going to have a one in n chance after doing this at being the root element. How do we actually install an element at the root of our binary search tree? There's a few ways you could do it. You could actually insert it like normal, which is drop it in at a leaf and then rotate it up and up until it eventually becomes the root. Or equivalently, you can actually call split? 

You can split your tree apart on the value of the element you're inserting. That will create two binary search trees, one with elements less than the thing you're inserting, one with elements bigger than the thing you're inserting, and you can just make those two subtrees the subtrees of the new element that is now becoming the root. There are a couple ways to do this. That's insert. What about delete? You'll notice with insert, that was relatively straightforward. There weren't any crazy special cases, subcase A and subcase B of special case. 

It's quite clean to analyze this structure. Basically, there's a one in n chance that you insert at the root. Otherwise, you just rehearse on your two subtrees. Within those subtrees, there's also a biased coin that gets flipped and the element might get installed at the root. As the element is being inserted, you're walking down the normal insert path, you would normally be walking down. But at every step, there's a chance that you stop and install the element at the root, and then that's the end of the insert function. What about delete? 

Remember that the clean way to delete from a binary search tree is to replace the element being deleted by the join of its two subtrees. If you have an element e that you're trying to delete, then you can take its two subtrees, T_1 and T_2 and join them together. Conveniently, these two subtrees have disjoint key ranges. That's crucial for the join operation to work successfully. How did we join these two subtrees together? There were two cases that we could choose between at every recursive step of the join operation. One of the cases involves T_1's root remaining the root of the joined tree, and we take T_2 and recursively joined it into T_1's right subtree. 

T_1's right subtree becomes the result of what you get when you recursively join its right subtree with T_2. Symmetrically, you could have also left t two's root as the root of the overall joined subtree, and you're thinking in this case of recursively joining T_1 into T_2, T_2's left subtree. T_2's new left subtree becomes what you get when you recursively join together T_1 with T_2's left subtree. You have to make this choice at every step of the recursive process in the join operation. Eventually, you hit a base case where you're merging a tree with things in it with an empty tree, and that's an easy base case. Sometimes I've heard this operation called zip, because if you actually look at what happens through the entire recursive process, it is like you're zipping a zipper to join two trees together, just like the split operation is sometimes called unzip. Now we have this interesting choice. 

We can do the join this way or this way in each step, how should we make that choice? Well, since we're trying to preserve the RBST property, it's important that every element has an equal shot at being at the root of the joined tree. If say T_1 was twice as big as T_2, we actually have to choose the left case with twice as much probability as the right case because there's twice as many elements in T_1 that want to have an equal shot at being at the root as the number of elements in T_2. Basically, the RBST property helps us make this choice. We just flip a coin and with probability proportional to the size of T_1, we invoke the left case, and with probability proportional to the size of T_2, we invoke the right case. That is the right balance factor to ensure that the resulting root is equally likely to be anybody in the joined tree. By induction, the same holds in all of the recursive sub problems of the RBST property is maintained throughout the entire process. 

This is the entire balancing mechanism. It fits on one slide. It's actually not that complicated. This is the one that we're going to try and implement in our coding discussion in this module because I think it may be the simplest, conceptually at least, to code up. One more mechanism that I wanted to talk about, and it's another very interesting one. This is an example of amortized balancing, and it's something called a splay tree. It's a remarkable data structure. 

It's also very simple, almost magically simple. The idea here is, we're going to design a balancing mechanism that gives you an amortized bound on performance. The tree may not always be balanced, but over a sequence of operations, it's on average behaving as if it was balanced in terms of the overall performance. Here's maybe some motivation for the direction of thought that leads to a splay tree. Imagine for a second that every time you access an element in a binary search tree that you rotate that element all the way up to the root, making subsequent accesses to that element faster. This could be a heuristic you could employ in binary search trees. If you don't access a lot of elements that often, if there's a small core set of elements that you access frequently, then maybe you want to rotate those elements up to the root so that they're very close to the root in the future, so that future accesses move quickly. 

It turns out that that might be a good idea in practice, but in theory, at least in terms of the running time performance, it doesn't actually improve the bottom line. There are access patterns where every access is basically bad. For example, if you have a binary search tree that is a very degenerate left path like this and you access the Elements 1, 2, 3, 4, 5, 6, 7 in order, then each one rotates up to the root. You actually, at the end of the day, end up with exactly the same shape of a tree. If you cycle through the same access pattern, every single access is basically taking you on the order of n time. You're not really contributing to anything that makes the tree more balanced. However, remarkably, a small variation on this idea does improve dramatically the balance of the tree. 

If instead of just rotating an element up to the root, we use what's called a splay operation, which is a rotate up to the root with one extra special case in it. As you're rotating an element up to the root, if you look ahead of you by two steps, so if I'm element x, I look two steps ahead of me. I'm going to be rotating with y and z as my next two steps. If those two steps are in line, like both left children or both right children, I do the next two rotations out of order. I rotate the parent grandparent link, and then I rotate the link with my parent, like what's pictured here. If I'm x, the next step would be to rotate the y-z link, and that would move me to this middle tree picture, and then I would rotate the x-y link, moving me to the right tree picture, and I'd continue onwards from that point onwards. When I say display a node, that just means rotated up to the root, but with this simple modification on that procedure. 

Anytime you look ahead by two steps and they're in line, you do the next two steps out of order. That's all. That's the only difference between this and the previous rotate to root heuristic. That seems to actually anecdotally make a big difference on the balance of the tree. It seems to leave a flatter tree in its wake, I guess, if you want to think of it that way. So if you look at the same degenerate left path tree shape, if you splay the Element 1 up to the root using these double rotations, then you end up with a much flatter tree shape in the wake of that operation. It turns out that you actually have enough here to actually prove that you get a stronger bound as a result of using this splay operation. 

This leads you to the splay tree. It's a very simple binary search tree where basically anytime you access an element, you splay it. To make a splay tree, all you do is you take the find operation, and after you're done finding an element, you splay it just for good measure up to the root. If you insert an element, you insert it like you normally would at a leaf in the tree, and then you splay it up to the root. If you want to delete an element, then you splay the element up to the root and then replace it with the join of its two children. We'll talk about join and split in just a second. Those are very natural operations on a splay tree. 

Splay trees, sometimes they're called self-adjusting trees because they just blindly follow this local restructuring heuristic, this splay rule. Without really looking at things in tremendous detail, they're not augmented with extra information. The splay tree is not noticing that it's out of balance and trying to correct that. It is just blindly applying this very simple restructuring rule, and it turns out that that is enough to give you order log n amortized running time performance for all of the major operations on a splay tree. Starting with an empty tree, if you do k operations, the total running time of that sequence of k operations will be k*log in. That's the definition of amortization for a running time to be order log n amortized. These structures, they're really cool and almost magical, I would say, because at least as far as I'm aware, there isn't a whole lot of very strong intuition about why the performance is as it is, the best I know of is this leaving a flattened tree in your wake anecdotal intuition. 

But I don't think there's much strong intuition on why. The math certainly works out, and I could take 15 minutes and walk through all the mathematics proving that the amortized running time is order log n. But I'm not sure at the end of the day if you do the math, then you have any more intuition about why the running time is order to log in. You have a lot of equations and at the end, the final equation has a log in in it, but to me, at least there's still intuition lacking in this play tree. That's one thing maybe that the research community should work on. But really cool data structures. There are some operations actually that splay trees are very adept at. 

Splits and joins are in particular very natural operations on a splay tree. If you want to split a splay tree on some value, then you basically splay that value up to the root and then just detach one of your child subtrees. Very easy to split a splay tree, very easy to do a join as well. If you'd like to join two trees together, you can say joining T_1 and T_2. I could splay the maximum in T_1 up to its root. If you splay the max up to the root, it has it has no right child because it's the maximum. You can just attach T_2 as its right subtree. 

Or you can do the mirror image. You can splay the minimum and T_2 up to the root and attach T_1 as its left child. All of these operations also run in log n amortized time. Splay trees are very flexible versatile structures versus other binary search trees that are a little more rigid and how they stay balanced. There's also a lot of remarkable properties you can prove about the performance of splay trees. Let me tell you maybe one or two just to really convince you that splay trees, there's something very magical going on with these structures. This is called the static optimality theorem. 

It turns out that if you know in advance a sequence of accesses that you're going to make in a binary search tree, that's the same thing as saying you know the frequencies with which you're going to be accessing elements, then you can actually build what's called an optimal binary search tree, as long as you're focusing on building a static, a tree that's not going to change its shape over time. If you want to build a fixed shaped tree, you can actually build the optimal fixed shaped tree for that access sequence, for those frequencies of access. This is an example of such a tree. I have scraped a bunch of recipe websites and pulled out very common ingredients for cooking along with their access probabilities. How common is it for salt to be in a recipe or for pepper or for milk? If you build an optimal shaped binary search tree out of those ingredients based on those access frequencies then this is what you get. It turns out that the expected number of nodes that you look at for a random access made with these particular frequencies, the expected number of nodes you access is about 3.5, which is remarkable because most elements here live at a depth of about four or five in the tree. 

It has to be a binary search tree, so it still has to satisfy the binary search tree property, but you're trying to put frequently accessed elements near the root so that in expectation, you're minimizing the number of elements you have to look at for a random access. Suppose you've made this static, optimal binary search tree, the theorem here says that splay trees basically match that performance to within a constant factor, even though the splay tree doesn't know in advance the access sequence or the frequencies with which you're going to be accessing the elements. The splay tree just learns, I guess, the right shape that it should take, because over time, if you do access elements often, they will end up being splayed up to end up near the root. That as an example of what we previously talked about is just a heuristic actually leading to a strong provable bound. Splay trees are within constant factors of optimal when you compare them against the optimal static tree that you could have built to handle an access sequence. Now, one of the biggest open questions in all of data structures today actually is whether or not the dynamic version of that claim is true. This is the dynamic optimality conjecture. 

It basically conjectures that. Splay trees are constant factor competitive against any binary search tree that handles any access sequence. If you take any access sequence, you can design of the best possible binary search tree for it, including even binary search trees that change their shape with rotations in anticipation of the elements that are coming up in the access sequence. This is the binary search tree that knows the future. It knows all the elements you're going to ever access, and it carefully does rotations over time so that it optimal it basically uses the minimum possible amount of time to process that access sequence. The claim is that a splay tree, even though it doesn't know the future, will still be within a constant factor of the running time of that optimal binary search tree. We don't know if this is true. 

I think a lot of people maybe suspect that it is true, and some progress has been made in the research community, but there are some very fascinating questions out there still surrounding splay trees and binary search trees.