![[Sweep Line Algorithms.pdf]]
[MUSIC] Dean: In this module, we've introduced the binary heap and we've introduced the binary search tree, but so far, mostly in terms of just what their functionalities are. We haven't really seen examples of these structures being used to solve actual problems. And so I thought this would be a good opportunity to introduce a prominent technique in algorithmic problem solving that often makes elegant use of data structures like binary heaps and binary search trees. And that's the technique sometimes called a sweep line approach. It's very prominent in computational geometry in particular. The main idea is you sort your input and scan across the input in that sorted ordering, maintaining some sort of a data structure so that you can maintain some useful state of your problem as you're scanning across. And that will help you solve the problem kind of as you go. 

There are lots of examples of sweep line or sort and scan algorithms out there. So I thought we would kind of look at a couple of examples of this technique just to kind of see how the data structures we've developed can actually be used in interesting ways. So here's a simple warm up. I give you a collection of intervals on the number line, and every interval has an associated value alongside it. What I'd like to do is find the point of overlap where the sum of those values is maximized. So I guess in this example, the answer would be ten. At the point where the interval with value four overlaps the interval of value six. 

This is actually a pretty easy problem to solve with a sort and scan or sweep line approach. What you can do is take every single interval and take its starting and ending point and take all of those two endpoints and sort them. So take all the interesting x coordinates in the instance, sort them all, and then just scan through all of those x coordinates one at a time. So imagine this little kind of green dotted sweep line, kind of scanning across the instance from left to right, pausing briefly when it hits one of these interesting points. All you're really doing here is scanning through an array of length two n that has been sorted that contains all of these interesting points. So as you're visiting these points, every time you hit the leading point of an interval, you increment a running count of how much value is kind of active at that point. And when you hit the trailing edge of an interval, you decrement your running count of how much value is active. 

So you basically just keep track of how much active value you have as you scan across. And remember when that number was the highest. So that's a very simple example of a sweep line algorithm. This would run an n log n time because of the initial sort. The actual scanning part is very fast, would just be linear. So let's look at a couple of other examples. We'll actually see a couple of problems here that we have studied before. 

I think there's a lot of value in looking at the same problem from different types of solution approaches. So this is the skyline problem that we, I think, used as an example of divide and conquer back in module three. We could also solve this with a scanning approach, a sort and scan approach, or a sweep line approach. So here the problem is to find the area covered by a set of rectangles that all live on a common base. So imagine they're buildings on the skyline of the downtown of a city, and you want to figure out the total area they cover. So what you could do is something very similar to the preceding approach. You could take all of the interesting x coordinates in your input. 

So that would be the leading and trailing edges of all the rectangles and sort them all, sort those two n values, and then just loop over those interesting x coordinates, kind of scan across your instance from left to right, pausing when you hit one of those interesting x coordinates. And so every time you hit the leading edge of a rectangle, you're going to kind of insert that rectangle into an appropriate data structure. And every time you hit the trailing edge of a rectangle, you're going to delete that rectangle from your data structure. So your data structure is going to kind of maintain the active set of rectangles in the cross section represented by the sweep line as it moves across the scene. What's an appropriate data structure? Well, we want to kind of track the tallest building as we go, so maybe a priority queue would be appropriate because that can always track, say, the maximum of the set of heights of the rectangles that are currently active. A binary search tree would also work because it also supports a find-max operation. 

But we basically want a data structure that handles the y ordered part of the problem. The initial sort is on x. Usually if you're scanning across in x, your data structure has something to do with the other dimension, with the y dimension. So here we use a data structure to kind of track the maximum y of our active buildings as we scan across. Total runtime is also n log n because we sort, that's n log n. Almost all of these algorithms are going to be n log n because of the initial sort. But during the scanning process, we also have to make a number of calls to our data structure. 

So N inserts and N deletes because each rectangle gets inserted once and deleted once. So that'll also be N log N if the inserts and deletes all individually cost log N. And then 2N, I guess find max calls as we're scanning across and kind of tracking the tallest building. Here's another nice one. I take a collection of line segments in the two dimensional plane and each line segment is either horizontal or vertical just for simplicity. And I would like to count how many intersection points are created by overlaying all of these different line segments on top of each other. Now it's worth noting that the answer here can be quadratic, can be on the order of N squared. 

If you have maybe N over 2 horizontal segments and they all intersect with N over 2 vertical segments, you can get a lot of intersections. And so you have to be careful when you approach a problem like this to not count the intersections one by one because any algorithm that you devise that counts the individual intersections is going to be doomed to take quadratic time because there could be a large number of intersections. We have to kind of count them in batches somehow and we can do that with a sweep line approach. So again, imagine you've taken all of the interesting x coordinates in your scene and you've sorted them. So that would include the left and right endpoint of every horizontal segment. And for every vertical segment it just has one x coordinate. So we throw that into a big bag. 

We sort all the x coordinates and we scan across as we have been doing. What we're going to do is keep a data structure that kind of tracks the active horizontal segments as we go. So that would be these light blue segments based on the current cross section of this snapshot right here. So as you're scanning across, every time you hit the leading edge of a horizontal segment, you're going to add it to our data structure. Every time you hit the trailing edge you're going to delete it. So the data structure will, at all points in time, hold kind of the currently pending or active horizontal segments. And since we're scanning an x, our data structure is probably going to be y ordered to handle kind of the y part of the problem. 

So we'll use a binary search tree balanced, ordered by y. And so if the y coordinates of our active segments here, I guess are 1, 3, 5 and 7, those are the values that are going to be stored in our binary search tree. When we hit a vertical segment, now we want to count intersections. We want to basically say how many active horizontal segments live between the minimum and the maximum y of that vertical segment that we just hit. And that's essentially a range query. In our binary search tree we're saying how many elements in the binary search tree live in a certain range. We'll talk about range queries in a bit slightly later in the future, but that's relatively easy to do in logarithmic time with a variety of approaches. 

You could, for example, kind of look up x and y where they would live in the tree. They may not actually be in the tree, but look at maybe the closest elements and take the difference of their ranks. That would give you one approach for solving the problem, but there are many others that we'll see when we talk about range queries. So this also can be implemented in just n log n total time. When we were in module two. We introduced this problem when we were talking about average case analysis of algorithms. This is the problem of identifying points that are non-dominated. 

We kind of talked about this in the context of multi objective optimization. So in two dimensions, you have this set of points. A non-dominated point is one. Well, it's not dominated, I guess it's probably easier to define what a dominated point is. So this blue point is dominated because there's another point that's strictly better than it in both coordinate axes. So there's a point above it and to the right, essentially. So how do we count the number of points that are not dominated? 

I won't actually go through the details because we actually talked about these exact algorithms when we introduced the problem earlier. So we introduced two sweep line approaches for solving this problem, one of which scanned forward in x and used a priority queue. Or you could also use a binary search tree that ran an N log N time. Or you could scan backwards in x and just keep a running maximum. We connected this, remember, with the number of times you expect to reset a running maximum in a randomly permuted array. So both of these approaches run in N log N time. The second approach, this one right here, actually the N log N just comes from the sort, the actual scanning process is only linear. 

So we've already seen actually a fair number of examples already in earlier modules that we could classify as sweep line algorithms. Let's maybe introduce a new problem based on this same input structure. So again, I have a collection of points in two dimensions. Suppose for every point I'd like to calculate what I could call its domination count. That's the number of points it dominates. That's basically a count of how many points are in its lower left quadrant. So how could we solve that problem efficiently with a sweep line algorithm? 

I guess as a hint, every algorithm in this discussion will be a sweep line algorithm. The hard part, of course, is when you see a new problem and you're not in the middle of a discussion on sweep line algorithms. So it's not obvious that the answer is going to be a sweep line algorithm. So in this case, we can also make short work of this problem with a sweep line algorithm. Suppose we sweep again forward in x. This problem is kind of symmetric in x and y, so we could have also swept in the y direction and it would have worked just fine. But we'll stick with sweeping in the, in the x direction. 

Usually for these types of two dimensional geometry problems, you either sweep on x or on y. You typically don't sweep in more exotic ways, like diagonally or something. There are some problems where you kind of sweep around radially, an instance that we don't have any examples of those in our current discussion. So if I sweep forward in x, then I'm going to be adding the points that I encounter into a data structure. It's going to be a y ordered binary search tree again, because the data structure typically handles the dimension that's kind of complementary to the one that you're sweeping along. So if I pause the algorithm right when I have added a point, this yellow point to the data structure, that means the points in the data structure are all the points on my left, because I've been sweeping from left to right. And I'm interested in at this point in time calculating the domination count of this yellow point. 

That's basically saying, hey, data structure, how many y coordinates are you storing that are smaller than my y coordinate? Because you already have kind of filtered out the points on your left, now you just need to filter out the points below you as well as being on the left. And that's actually the rank operation in a binary search tree. Remember, the rank of an element is essentially, it's where that element is in the in order traversal of a binary search tree, which is equivalent to how many other elements are smaller than you. So this is basically just sweeping forward on x, keeping a binary search tree ordered on y with calls to rank. So you can actually kind of at a high level, describe these algorithms extremely concisely. This also takes N log N time because of the sort. 

And then you're inserting n things into a binary search tree as you go. That's N log N you're calling rank N times. That's another log N per call to rank. So the whole thing adds up to N log N. Now, once we have solved a problem like that, I can introduce a lot of other problems that are strikingly similar to it. So we've already say, solve the domination count problem. Let's look at a couple of other problems and show that there's actually a lot of commonality between those problems. 

So this kind of hopefully drives home the point that we don't need to reinvent a new algorithm for every problem. We can exploit similarities between problems and transformations between problems, to use maybe the same algorithm or the same algorithmic idea to solve multiple problems. So imagine for a second that I give you a set of intervals, and for every interval I would like to count the number of other intervals in my input that are 100% completely contained within it. So compute maybe what we could call the containment count of every interval. How is that related to the domination count problem? It's actually extremely similar if I think about intervals. An interval is characterized by two numbers, the starting point of the interval and the ending point of the interval. 

So intervals, to me, they're points in two dimensions. They're fundamentally characterized by two parameters. So let's maybe make the starting point of an interval its x coordinate and the ending point its y coordinate. If I want to draw a picture of that, perhaps, then let's make some coordinate axes here. And my x-axis is going to be like the start location of an interval, and the y-axis is going to be the ending location of an interval. And so now I can just plot an interval as a point. So the x y coordinates are kind of where it starts, comma, where it ends. 

So all of my intervals actually end up as points in the 2D plane. That's another way to think of my collection of intervals in a different kind of geometric context. I guess the ending point is always bigger than the starting point. So all of the intervals would kind of live above the y = x line. But now if I look at a point, say this green point right here, where are the intervals, i.e. the points that are contained within that interval. So those would be the intervals. They have to start later than me and end earlier than me. 

So they have to be, I guess they have a higher starting point and a lower ending point. So they're to my right and they are below me. So basically they're all the points in my lower right quadrant. And so this is exactly the domination counting problem. It's just, I guess, counting points in my lower right quadrant instead of my lower left quadrant. But that's a mirror image of the same problem. So if you look at the problem the right way, it is indeed, exactly equivalent to the one that we had formerly introduced. 

What about counting inversions in an N-element array? The concept of an inversion is actually quite important. It's basically a pair of elements in an array that are kind of out of order with respect to what they should be if the array were sorted. So this pair of 9 and 4, that pair is an inversion, because if the array were sorted, then the 4 would come before the 9. But here the 4 comes after the 9. So this pair is an inversion. If you have a sorted array, there are no inversions. 

I guess if you have a reverse sorted array, it has the maximum possible number of inversions and choose two, because every pair of elements is an inversion. Counting inversions is sometimes useful. The inversion count of an array is sometimes a proxy for how unsorted the array is, and sometimes you can even write algorithm running times in terms of inversions. For example, insertion sorts running time is essentially just N plus the number of inversions in your array. So sorting an array that's nearly sorted, that has a small number of inversions is quite fast with insertion sort. How might we count the inversions in an N-element array? Let's maybe draw another picture similar to the one that we just drew. 

So if I take an array. Maybe, let's say, 1, 4, 3, and 2. Then I would like to count the inversions in that array. So what are the inversions? I guess 4 and 3. That's an inversion. 4 and 2, that's an inversion, and 3 and 2. So this array has three inversions. 

How can I maybe convert this into a problem that looks similar to the problems before? Well, an array can be thought of as a two-dimensional point set. You can basically draw a little coordinate axes over your coordinate axes over your array so that the x coordinate is your position within the array and your y coordinate is your value. So I'm kind of making a stem plot. One goes up to position one, four goes up to position two, three goes up to position three, and two goes up to position two. So I get a set of two-dimensional points. And now I'm interested in, well, how many inversions are there? 

Well, if I look at one of those points, like the point that corresponds to the four in my array, then what elements form inversions with the four? Well, they're elements that are to my right in the array, so bigger x coordinate, but they're smaller than me in value, so they're less than me. So again, this would be all the points in my lower right quadrant, exactly the same problem that we just looked at with intervals. So you can use the exact same sweep line approach to solve all of those problems. What about the last problem on my list of examples here? Count the number of pairs of crossing segments in this kind of bipartite matching, where I have the elements one through n listed on one side and then one through n listed in some other order on the other side. I connect one with one, two with two, three with three, and so on. 

And that creates a certain number of crossings between those different segments. So is that problem the same as the other problems? And indeed I think it is. So again, if we go back to our whiteboard, let me just redraw that picture just so that we have a copy of it here. So I had one, two, three and four on the left. I had two, three, four and one on the right. And one was connected with one, two with two, three with three, and four with four. 

So there were three total crossings. How can I maybe make a picture of this in two dimensions? Well, I guess, let me say 1234 on the x axis. Maybe that represents your position in the left hand side ordering, and then 1234 on the y axis, that represents your position on the right hand side. And so if I want to take each one of those line segments each line segment here is characterized by two numbers. Kind of like your position on the left side and your position on the right side. So the line segment that I've just highlighted, on the left hand side, it is in position 1. 

On the right hand side it's in position 4. So I can put a point here in the one, comma, four position. This segment, maybe this green segment that goes from three to three. On the left hand side it is in position three. On the right hand side it's in the second position. And so that would be a point at three, two. And so now, how do I connect this with domination, counting or inversions? 

Well, if you look at a segment like this segment, the one right here, the one that was the one to one segment, what crossings will that create? Well, that'll create a crossing with any other segment that is to my right, kind of ahead of me in one of those orderings. But underneath me, which is before me in the other ordering. So that segment kind of would have to cross me at some point. So again, I'm counting all the points in his lower right quadrant as the number of segments that would intersect with that segment. So it is the identical same problem in all of these cases. So the point here is, basically, look for commonalities in problems. 

And that way, you don't have to reinvent the wheel and devise a brand new algorithm for every single problem. Okay, moving right along. It seems like in every module it's obligatory for us to introduce some instance of this kind of domination radius or longest line of sight problem. We've seen it actually in module one, module two, module three. And now we'll see it again in module four. So we've seen this kind of mostly phrased as a domination radius problem. So given a collection of heights of individuals standing in a row. 

For every individual, you'd like to calculate the left and the right kind of line of sight length, the domination radius, until you reach somebody taller than you. The longest line of sight problem is kind of looking for the longest such line of sight. And so, in the last module, we looked at a divide and conquer algorithm for the longest line of sight problem. And then in Module 1, we actually devised a relatively sophisticated algorithm for solving the domination radius problem in linear time. That algorithm actually could be interpreted as a sweep line algorithm. If you want to go back and look at that algorithm, it does have flavors of being a sort and scan or sweep line algorithm. And then in Module 2, we looked at the same problems from an average case perspective. 

We looked at kind of the naive quadratic time algorithm that looks left and then looks right. That normally runs in N squared time in the worst case. But we looked at that in the average case and said that, that runs in N log N time in expectation because of connections with randomized quicksort. And actually, because now we know that randomized quicksort and randomly built binary search trees are the same thing, you can actually develop some very rich connections between that problem and binary search trees, as well as quicksort. So there's a lot of really cool stuff you can talk about with just this one problem. But since we're now talking about sweep lines, let's see if we can add one more algorithm to the mix. And solve, maybe, the domination radius problem with a simple sweep line approach. 

So what we're going to do here is, let's sweep downwards. So I'm going to sort all of the individual input elements in decreasing order by height and visit them in that order. And as I do that, I'm going to keep a binary search tree ordered by x by the index within the array because that's the complementary direction to the one that I'm scanning. So as I'm scanning, I'm inserting the elements, tallest down to the shortest, into a binary search tree. So if I take a snapshot on the algorithm kind of in the middle of the execution of the algorithm where the green sweep line is, I've already hit these yellow elements. They're the ones taller than the sweep line, they're already in the binary search tree. Their positions, position two, five, nine and twelve, they are stored in the binary search tree. 

And now it's easy to see what operation we need to use in the binary search tree to find your left and right neighbors on lines of sight. So if you insert someone into the binary search tree, you just need to call predecessor and successor to find the next smallest and next largest index of an element. That would be the lines of sight. When you look to the left and look to the right, conveniently everybody shorter than you that you're going to be looking over the top of the sweep line hasn't even hit them yet. So they're not even in the picture. So the only people in the picture at this moment are people taller than you, the ones that will block your line of sight when you look to the left and look to the right. So you could characterize this algorithm as an initial sort in N log N time. 

And then you're scanning downwards, and for every element, you're inserting it into a binary search tree kind of ordered on x, and you're calling predecessor and successor. All of those operations run in log N time on a balanced binary search tree. So again, we're going to get N log N as our total running time. Okay, final problem. I thought we would revisit a classic in computational geometry. This is a problem that you can solve in many, many interesting different ways, relatively efficiently in low dimensions. Like most computational geometry problems, as dimensionality goes up, this problem gets harder and harder. 

So in two dimensions, I would like to find the closest pair of points among endpoints in the plane. So it's a very standard problem, lots of interesting ways you can approach it. There's a very famous divide and conquer algorithm for solving the problem, if you want more examples of good divide and conquer algorithms. But here, let's see if we can develop A sweep line approach for solving the problem. And I think I'm just going to do this one at the whiteboard because it's easier to explain that way. So if we go back to our whiteboard, and let's create some space here, we're going to have our set of points that we're sweeping across. So lots of points in our point set and we're going to be sweeping across those points. 

And as we go, we're going to keep track of the closest pair of points we've found so far. So at the end of the algorithm we will have discovered the globally closest pair of points. So let's imagine our sweep line is kind of sweeping across the problem. For lack of a better idea, I'm going to just sweep in the positive x direction. So sort my points on x originally and then scan across in that order. This problem is relatively symmetric, like X doesn't really mean anything special compared to Y. So I probably could have swept in the Y direction and everything would have also worked out. 

So I'm going to sweep forward in X direction every time I hit a new point. I'm going to compare that point with all of the preceding points I've already seen. So compare that to this guy, compare it to this guy. Basically calculate the distance of that point to all of the other points that I've already swept across and see if I find any shorter distances than the best distance I've found so far. Because as I sweep, I'm going to be keeping track of the best distance, the closest distance I've found so far, let's say the closest distance is equal to capital D. That's the best distance I've found so far. So every time I see a new point, I try and figure out can I update capital D to something smaller by comparing that point to everyone I've already scanned across so far, we've described an algorithm that runs in quadratic time because every single point is going to get compared to a linear number of other points. 

So we need to bring a little bit more insight to the picture. And one of the bits of insight that we can look at here is you don't actually have to compare against everybody because a lot of the points in our instance are going to be so far away that there's no hope that that's going to, you know, reset D to something closer. In fact, if you draw maybe kind of another trailing sweep line at a distance of D behind the original sweep line, then you know that only the points in this kind of vertical strip are interesting to compare against because only they have the ability to be within D of the new point. Anybody who is further to the left kind of passed the sweep line, the trailing sweep line, they're farther away than D units, so they're never going to be candidates for resetting our closest distance. So all we really need to worry about, let me kind of redraw this picture just so it doesn't get too cluttered. All we really need to worry about are the points kind of in this vertical strip as we go. So we have a whole bunch of points and we're scanning across. 

Here's our green sweep line right here. And we're going to actually have another, think of it as another sweep line, actually that kind of trails behind the original sweep line at a distance of, of D, where D is the closest distance we found so far. And so really the interesting area that we need to consider is kind of the area between those two sweep lines. And so as we encounter every single point, we're only going to have to compare that point to the points that are kind of inside this vertical strip. They're the only possible contenders for points that could reset the running minimum distance. And so we have to compare every new point to only those points. And so what I'm going to do actually is I'm going to maintain the elements in this strip kind of in a binary search tree. 

And it's going to be ordered by y coordinate, because I'm scanning in the x direction. So your binary search tree is probably going to be ordered by y. So maintain all of the points in that strip in a binary search tree ordered by y. What that means is you're inserting points one by one when the original sweep line hits them. But you're also kind of tracking this trailing sweep line and that's moving forward. As long as the original sweep line moves forward, the other sweep line kind of moves in lockstep with it. And that's causing points to be deleted from your binary search tree when they kind of fall out of that strip. 

So you're both doing inserts and deletes to keep up to date the contents of that strip. And now every time you hit a point, you just want to compare it to all of the other points in that strip, but probably the ones that are close to you. In fact, you don't need to within that strip, if you kind of scan down a distance of D and up a distance of D, then you don't need to consider any points, you know, outside of that range because they will certainly be farther than D away from you. All you really need to compare yourself against are the points in kind of a D by 2D rectangle, this rectangle that is right next to the point that you've just added to the structure. And how many points do you have to compare against? It turns out there can't be that many points inside that rectangle because we know that as we've been scanning, we've been keeping track of the closest pair of points. If you had a million points in this D by 2D rectangle, inevitably two of them would be closer than D. 

And we know that can't be the case because D is supposedly the closest pair of points we've found so far. So there can only be at most a constant number of points, like some number like seven or eight or something that you can even fit inside this little rectangle that are the ones you have to actually compare against. Otherwise, if you had too many points, there would inevitably be a pair closer than D, and you know that that isn't possible. So what we're going to do is we're going to just compare this point against its successor and then its successor's successor. We're going to just keep calling successor to kind of step upwards through the chain of points that we see in this strip, and then we call predecessor to step downwards through the chain of points that we see in this strip, stopping when we get farther than D ahead of us in y or behind us in y. And there's only a constant number of points we have to compare against in each of those cases. So that's our algorithm. 

We're basically sorting, that's n log n. We're doing n inserts and n removals from our binary search tree, that's another n log n. And then every point gets compared to a constant number of additional points which you generate by moving upwards and downwards with predecessor and successor calls from that point. And so that's another n log n. So the entire algorithm actually just runs in n log n time. This one's a little bit more sophisticated than the ones we've seen before, but also a nice illustration of how powerful the sweep line approach can be. And how we can really leverage kind of the operations of a binary search tree to make this run efficiently.