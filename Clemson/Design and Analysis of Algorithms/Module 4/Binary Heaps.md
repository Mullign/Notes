![[Binary Heaps Slides.pdf]][Music] Dean: The binary heap is a very simple and very widely used data structure, one that is taught in almost any undergraduate data structures course, and one that is definitely good to have in your repertoire. So in this video we're going to review key details of binary heaps. So the binary heap it's a type of priority queue. We introduced priority queues as a class of data structures back in, I think, module two, when we talked about the randomly mergeable binary heap, which is a relative of the binary heap. So we're going to talk about here a priority queue is basically a dynamic set of elements that allows you to always reach in and extract the highest priority element and remove it next. And we usually by convention define that as the minimum element in the in the structure. We can have just as well defined it as the maximum. 

So the mandate for a priority queue is that you must support two main operations: an insert operation, and a remove min operation, or remove a max if that was your convention. Most priority queues also support some extended operations. For example, the ability to delete an arbitrary element, not just the min, or to change the key of an element already in the structure. Although I guess you wouldn't have to implement those if you already had insert and delete, because you could just delete an element and reinsert it with a new key. Priority queues and sorting are very closely related. You can sort with a priority queue by calling insert n times and then remove min n times, and as a consequence, at least in the comparison based model, one of insert or remove n must have a worst case running time of at least log n Otherwise that would allow you to circumvent the n log n lower bound on comparison based sorting. But one other interesting note about something that priority queues often cannot do is most priority queues don't support an efficient method for finding an element. 

Once you've inserted it into the structure, it's kind of not unlike the human digestive system. The only time you see the element again is when it reemerges on the other side when you call remove min and the element comes out. So that raises an interesting question. How do you actually get your hands on an element if you'd like to delete it or change its key once it's been inserted into the structure? And that actually lets me- I mentioned a subtle but important point about how, data is often organized in our memory. A lot of times, records of data are very big, very complicated things. Maybe if I'm storing records related to students at the university, every student has, you know, biographical information, their entire transcripts, their registration history. 

There's a lot of information that goes along with every single record. Most of it is irrelevant to a priority queue that is only indexing this information. Or maybe just one key. And so oftentimes you don't store the entire records of data in the data structure themselves. You instead, store in the data structure kind of lightweight nodes that have just the relevant key, and then a pointer to the full record of data which is parked somewhere else in memory, probably somewhere that it's not even going to move that often. And this makes things much more efficient, because the data structure can kind of shuffle around those lightweight small nodes and not worrying about moving the giant, clunky original records of data. For the same reason, a lot of times, if you're sorting large, complicated records instead, you might want to sort a lightweight array of pointers to those records instead of the original giant records, because you're moving a lot less memory when you do that. 

This also gives us a solution to the problem I just mentioned of. How do you access an element? Once it's been inserted into the priority queue, you can just keep bidirectional pointers. So every record in the data structure has a pointer to the giant record of, that it represents kind of on the side. And the giant original record of data has maybe a pointer into the data structure so you can access elements from the outside world. So just an important note- we often do this. Sometimes we build several data structures on top of our original records of data. 

So we might have a couple of lightweight indexing structures, maybe, a few priority queues, a few binary search trees, all kind of built on top of the same, original massive set of data records. So very common to organize things in memory this way. Okay. There are a lot of ways to build priority queues. And I think we mentioned some of the trade offs involved in our previous video. So I won't go into a tremendous amount of details here, but just, you know, reminding people that if you choose a very primitive way of implementing a priority queue, like just a regular old array or a linked list, that's not great in terms of worst case efficiency, depending on how it's implemented, either insert or remove min is going to be very slow. And so we typically look for ways to kind of more balance those running times. 

And there are lots of ways we can do that and achieve pretty much a logarithmic running times for insert and for remove min. That's usually the best we can hope to do. We saw how to do that in expectation with the randomly manageable binary heap. We will see how to do that in this data structure, the binary heap. It supports insert and remove min both in log n time. An interesting feature actually of the mergeable binary heap that we talked about before is that it also supported a merge operation. If you kind of look at more advanced data structures for priority queues, many of them are- they support merging. 

And they're actually kind of fundamentally based on merging as a as a key operation. That's actually not the case with the binary heap that we talk about here. It's a little more of a rigid data structure that doesn't want to merge very, very well. So if you go off and study more, kind of exotic implementations of priority queues, you'll see merging appearing quite often amongst the operations that these things tend to support. We'll also see, some additional priority queues throughout this class. In fact, in this module we will talk about balanced, binary search trees. Those can also serve as priority queues. 

They support insert and remove min and log n time, although they're not quite as lightweight maybe as the binary heap. So if all you need is a priority queue, the binary heap might be a better alternative, maybe a better hidden constant in the big O running time. We'll also see relatives of the binary search tree, things like B-trees and skip lists that could also serve as priority queues if needed. And then in the next module, when we talk about amortized analysis, we will see an elegant example of amortized analysis in what's called a skew heap, which is another type of mergeable heap that supports all major operations in log n amortized time, including merging. So lots of exciting things, still on the horizon to talk about with priority queues. But we should probably now focus on the data structure at hand, here the binary heap. So it's a very simple data structure. 

The interesting the most interesting thing about it is that the actual implementation in memory is nothing more than an array of elements, but the way we think about those elements is kind of in a tree structure. So we imagine that the, elements making up a binary heap, they form a heap ordered binary tree that is almost complete. And so let me go over each of those conditions. So it's a binary tree. So every single node has, you know, 1 or 2 children are left child and right child. It's almost complete, which means that all of the levels in the tree are completely filled in, except possibly the last level, which is only filled in up to some point. And that's very important because you can actually take an almost complete binary tree and you can map it to an array or actually store it in an array using what's called a level order encoding. 

So imagine kind of you put the root first and then you just read out from left to right the next level, then the next level, then the next level. And that's the contents of the array and memory. It's just each successive level, ordered from, from left to right. In that representation, you can actually jump around within the array with a simple kind of mathematical manipulation on the index that you're at. So if you'd like to go to your left child and you're sitting at index i, you just moved to index 2i. Or if you want to go to your right child, you move to index 2i + 1. If you want to move to your parent, you go to, you know i/2 with the floor. 

Those formulas will change a little bit. If you're treating your array as being zero based or one based, just FYI. But the ability to to do this to jump around in the array as if we were moving around in the tree, means that we don't really lose anything by storing the elements in an array instead of in this tree format. So we think of the elements as if they're stored in a tree, but they're actually stored in memory in a much more lightweight fashion in just a plain old array. And yet we still move around in the array, kind of as if we pretend like we're moving around in the tree. We usually move to your parent or your children, things like that. And this is why the data structure is such a nice, lightweight data structure. 

We don't have to represent it like we often represent binary trees, which often involve kind of a bunch of individually allocated blocks of memory and pointers to to other blocks of memory. There's no pointers or anything like that. It's just an array. Alright? It also satisfies the heap property. So we talked about that as well. When we talked about the randomly mergeable binary heap of the heap property basically means that the parent is always less than or equal to the child. 

So if you walk up the structure, the keys get smaller and the minimum is always sitting at the root in an obvious place. And so this is why the the heap property is so often used in priority queues, because you want the minimum to be in an obvious place in the structure. So that's the structure of a binary heap. All we need to do now is figure out how to support our main operations, how to insert an element, how to remove the minimum element, and so on. Those operations are just going to interact with the array representation of the heap here. But we're going to kind of think of the operations in terms of how they change the structure of the tree, kind of our mental picture of the data structure as a tree. So it turns out that every fundamental operation on a binary heap can be implemented in terms of two even more fundamental kind of meta operations, called sift-up and sift-down. 

Although these do sometimes go by other names. I've heard sift-down sometimes called either heap of fire or down heap and sift-up, I've heard called up heap, which I really don't like. That sounds like what you do when you're sick. But these are very intuitive operations. They move elements up or down within the heap to try and restore violations of the heap property. So sift-up basically fixes a violation of the heap property between an element and its parent by repeatedly swapping with its parent, kind of moving an element up in the heap. As long as the element is smaller than its parent, which is a no no, you keep swapping with the parent until you've restored the heap property between you and your parent. 

So this kind of takes light elements and lets them kind of drift or bubble upwards in the heap. I guess if you want a physical analogy for what's happening. Sift-down, on the other hand, takes a big heavy element and kind of by gravity pulls it down towards the bottom of the heap by repeatedly swapping it with, I guess, your smallest child, which is the one you'd want to swap with if we want to, to try and restore the heap property after seeing a violation between an element and one of its children. So, these two operations make it very easy to implement all of the remaining operations in just log n time. Either of these two operations runs in just log n time, because the height or the depth of a of a binary heap is log n. And so that's the largest distance you would have to travel with either of these two operations. So how do we implement say insert? 

It's very easy now. To insert a new element, you just drop it in at the end of your array, which corresponds in your mental picture as a tree, to kind of placing it in the next open slot in the bottom row of the tree. And the only problem now is, is the heap property could be violated with the parent. And so you fix that by calling sift-up, which will cause the element to repeatedly swap upwards with its parent until you now satisfy the heap property again everywhere. So that's insert, easily runs in log n time. What about removing the minimum? So the minimum is sitting in an obvious place. 

It's at the root. But you can't just take out that element and leave a gaping hole in the structure. You have to find some other element to kind of put in that location. So what you often do is you swap the root element and the very last element in the structure. So you kind of the root element, the minimum, the one you're trying to remove kind of moves to the very end of the array where you just kind of leave it there sometimes, but it kind of thing you think of it as kind of falling off the end of the array, basically. So the array shortens by one. And now you have replaced the root with what used to be the element at the end of the array. 

And that might cause a violation of the heap property with the children of the root, which you then fix by calling sift-down. And so all you do is you take this, this new element that you have installed at the root and sift it down, and that kind of swaps it with appropriate children as it moves down in the heap. And eventually you have restored the heap property. So that also takes log n time. And in general, you can implement all of your major operations using an appropriate combination of sift-up and sift-down. So if you decrease the key of an element that makes it smaller, that might cause a violation with the parent. So you have to call sift-up after that. 

If you increase the key of an element that makes the element bigger, that might cause a violation with one of the children. And so you have to call sift-down. So it's usually pretty straightforward to figure out the right combination of sift-up and sift-down needed to fix the heap property after you've broken it by changing a key or inserting or deleting an element. All of these run in log n time, quite straightforwardly. Okay, so we've actually basically talked about the binary heap. That's all there is to building a binary heap. It's actually really easy to write in code as well. 

But there are a few interesting kind of factoids about the binary heap. One of them centers on how efficiently you can actually just build a heap out of n elements, in kind of a batch fashion. There are two kind of prominent ways you could do this. One is using insert, which is based on sift-up, where you could basically, you know, start with the elements in arbitrary order in the array that you're going to turn into the array that represents your heap. And you just basically call insert on each element which at the end of the day, also is equivalent to looping over the elements. Going forward. You loop over element one, two, three, four, five in the array and you just call sift-up on each one. 

Because remember to insert an element, all you do is insert it by adding it to the end of the array and calling sift-up on it. So if I want to build a heap out of n elements that are just already sitting in this array, I just loop forwards over the array and call sift-up on each one. That's equivalent to inserting them all into the heap. Another way that I could have done it is I can actually loop backwards over the elements and call sift-down on each one of them in sequence, the last one being the the root element in the heap. So I start with an arbitrary ordering of elements, and I look backwards and call sift-down. That's more of a kind of a bottom up method of building the heap. Surprisingly, that also builds a valid heap. 

And so it's interesting to look at the running times of these two kind of alternative approaches. The first approach, where we call sift-up n times, that could, in the worst case, run in n log n time, because every element might be sifted all the way up to the root. I guess if you started with a reverse sorted array, that would be the worst case, because every single element you insert would become the new minimum and would sift all the way up to the root in log n time. So this could take n log n time. But it turns out if you randomly scramble your array beforehand, i.e. if you insert elements in random order. This is what randomized incremental construction, then the running time actually drops to just linear in expectation and we’ll prove that in a second. With the other method, the bottom up method based on sift-down that actually runs in just linear worst case time, surprisingly. 

So it's kind of interesting. You can build a heap really quickly in just linear time on any set of n elements. The trouble is to extract the elements in sorted order. It still takes n log n time, because removing the min over and over again, that still takes log n time for operation. So why does this bottom up method where you call sift-down on the elements one by one from end to one? Why does that actually work? So if you think about what sift-down does, sift-down kind of takes a heap where the only possible violation of the heap property occurs at the root, and it fixes that. 

If you call sift-down on the root, then that restores the heap to being a valid heap. That's kind of what sift-down was designed to do. And that's basically what you're doing. If we look at the structure of our heap, the bottom up construction method is, is looping backwards over our array. calling sift-down on all the elements. So in the tree picture we're looping backwards over that bottom level calling sift-down on everything that does nothing because they're already at the bottom of the heap. But then we loop backwards over the next level. We loop backwards over the next level. 

We're calling sift-down as we go. And by induction, we're kind of building up larger and larger sub heaps as we go. So by the time we reach an element, we will already have created valid heaps out of its two subtrees. And calling sift-down on that element therefore makes an even bigger heap, including that element as well. So the very last element you operate on is the root. By the time you reach the root, you will have created valid heaps out of both of its subtrees. And then calling search down on the root fixes that, any possible violations of the heap property and gives you one big heap. 

So that's why it works. Why does it run in just linear time? So that's actually also pretty easy to see. In fact, let me go back to this, this same picture here. How much work do we spend? Per element in this picture. So we're calling sift-down on every single position in the heap. 

How much total work is that? Well, in the bottom row of the heap, you really only spend kind of one unit of work calling sift-down on the on all those elements, because there's nowhere for them to go. In the next row up, sift-down takes at most two minutes of work because there's elements. They have a height of, say two. They can only move, you know, down one level, the next level up, sift-down on those elements maybe only takes at most three units of work because there's only, you know, three levels for them to move down at that point. And this is kind of the key observation. As you move up the heap, it gets more and more expensive to call sift-down. 

But there are far fewer elements as you move up the heap to call sift-down on. So the root is the most expensive, but there's only one root. There's a whole bunch of elements at the very bottom of the heap, but those are really cheap. So that's why things are going to add up to being just linear. So we could get kind of a crude upper bound on things, by just saying, you know, in the very lowest level, there's at most n elements, we could probably get a stronger, you know, kind of a tighter bound than that. But I'll just just for simplicity, say at most n elements in the very bottom row of the heap, then when you move up a level, I'll say there's that most n over two elements in the second lowest level and at most n over four in the third lowest level, the number of elements you have as you move up in the heap and the amount of work done, increases. So there's one unit of work done to all the elements. 

And the lowest level, two units to the, at most done to the elements in the second lowest level, then three, then four, and so on. So if we add all of that up, we get a very interesting series. We get kind of n for the lowest level, then 2(n/2) for the next level than 3(n/4), 4(n/8), we get a series that actually has both kind of an arithmetic component and a geometric component. And so all we need to do is add that up to get our answer. And that does actually add up to something that's on the order of n. How do you add up a series that looks a little bit daunting like this? It's not just a regular old geometric series. 

It's kind of an interesting one. However, we can still apply it successfully or kind of go to method of shifting to address this kind of series. So, what we can do is multiply the series by one half, which is kind of the common ratio of the geometric part. And that shifts over the terms of the series. So n turns into an n/2, the 2(n/2) turns into a 2(n/4) and so on. And then you can subtract the series kind of from itself and get some very helpful cancellation that turns the series effectively into a regular geometric series, which you can then again sum with the shifting technique. So this is a wonderful technique if you don't want to memorize formulas for adding up series that have kind of a geometric character to them. 

So basically, if we build a heap in a bottom-up fashion, we get a running time that's order n, which is nice. But we also mentioned that we get a linear running time in expectation if we build a heap by inserting elements in random order with shift-up. And so why is that the case? That's kind of a nice thing to look at as well, because we can revisit some of the ideas we talked about when we analyzed randomized algorithms. So with the previous analysis, we basically took the sum of the heights of all the elements and showed that that was linear. That was how much work we did, because the height of an element is how much work you do sifting it down. So how can we use this, this fact to help us analyze the algorithm that uses sifting up instead of sifting down to do all of our inserts, but in random order. 

So let's look at maybe one location in the heap, say the location were currently occupied by this this five element right here. This has a height of three. And so the number of elements in my subtree is at most 2^3, at most eight if I have a height of three here. So I'm interested in how much work I do at this location. And I'm going to figure that out and then sum that over all the locations. And that will give me my expected running time for inserting elements kind of in random order and calling sift-up on each one. So what's the expected amount of work I spend on a generic location in the heap like this, say, with, with a height of h? 

Here h is three. Well, the only way that I do work here is, when I'm inserting elements in this subtree, they might get shifted up through that location. So only the elements in the subtree, kind of the ones that start out in the subtree, have the capability of even causing work to happen at this location. If they sift-up to this location. So there's kind of at most 2^h elements that could cause work to happen there. And what does that process look like? Every element that causes work to happen here, you know, shifted up to that location and kind of reset this running minimum. 

This element here, is kind of a running minimum of the elements in this subtree as they are introduced and shifted up, because every element that is smaller than all the other elements so far will shift up and change the element in that yellow location. And so this is exactly an example of the process of how many times do you reset a running min of a, in a randomly set of ordered set of elements? We're basically, you know, scanning through the random contents of the subtree and every element that gets, you know, shifted up to this location resets to the minimum that was in this location. How many times is that happen? So we know that resetting a random, running minimum happens only a logarithmic number of times in expectation. So what's the logarithm of the number of elements in the subtree? The log of two to the h is just h. 

And so the expected amount of work we do on a height h element is just order h. And so again, if you sum up the expected work at every location in the structure, that's going to be essentially the sum of the heights which is on the order of n. So just a nice way to kind of revisit some of the the things we've already looked at in terms of analysis techniques. Okay. One final note about heaps. There is a very simple and very elegant sorting algorithm based on the binary heap, called heap sort, which is it's quite simple. All you do is you take the elements you'd like to sort, you put them in your array and in some arbitrary order, and then you build a heap out of that array, and you can do that however you want. 

I would say maybe using the bottom up method, because that's relatively simple. And that runs in just linear time. Once you've built a heap out of your elements, you just call remove min n times. And remember how remove min works when you call remove min. If I, go back to to the picture of remove min, it swaps the minimum to the end of the array, where it kind of you just kind of leave it there afterwards and forget about it. But if you keep calling remove min, then what's going to happen? The minimum gets swapped to the end of the array, and then the next minimum will get swapped to the second to last position in the array, and the third minimum to the third to last position. 

You're going to end up actually with a reverse sorted array after n calls to remove. Then kind of just as a side effect of calling removing n times. And so this will sort I guess in reverse order. So, what's the running time. Well, it's clearly n log n because it just involves n calls to remove min after you've built your heap. So, a very, very simple algorithm for sorting based on a binary heap. And it actually has a lot of very favorable characteristics. 

So compared to merge sort, this might be a preferable algorithm because it runs in place. You're only moving elements of data around within the array that encodes your binary heap. You're not using any substantial additional storage. Also, if you want to compare to, say, quicksort, this is nice because it's a deterministic order n log n algorithm. There's no randomization involved with quicksort. To get a deterministic n log n running time involves kind of a very heavy median finding algorithm in a high hidden constant, and a lot of complexity. This is a lot more lightweight. 

So it has a lot going for it. Compared to other fast sorting algorithms. So those are just a few nice properties and applications of heaps. And so hopefully you'll find them useful in your future algorithmic endeavors. [Music]