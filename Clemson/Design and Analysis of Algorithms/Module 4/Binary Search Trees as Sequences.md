![[Binary Search Trees as Sequences.pdf]][MUSIC] Dean: In our discussion of binary search trees so far, we have tended to emphasize their very prominent uses as sets or as maps. But the versatility of the binary search tree doesn't end there. They can be used in many other ways. And in this video, I wanted to really highlight another way of interacting with binary search trees that gives us what we call rank based access, and how that leads to another very powerful use case of the structure for implementing a sequence. So to do this, we need to add another operation to the lineup of operations supported by the binary search tree. That is the select operation, the operation that identifies the element of a particular rank within the tree. The element of rank k is the element that would be at position k within the n order traversal of the tree. 

So k = 0 would be the minimum, k = n- 1, the maximum, k = n over 2 would be the median. Here, I'm using zero-based indexing. I could just as well use one based indexing if I wanted to. So this is the same selection problem that we talked about in the preceding module when we talked about selection and Quickselect. So hopefully, we'll see a lot of familiar concepts in our discussion here that follows. A lot of times, we implement not only the select operation, but also kind of its inverse, which we could call the rank operation that basically tells you the rank of a given element. So point at an element in the tree and say, where would that element live? 

At what position would it live within the inorder traversal of the tree? These are very powerful operations. They can be used in many different ways. A simple example of something useful you get from the select operation is you can easily draw a random sample from a binary search tree by just selecting for an element of a random rank between 0 and n- 1. So to implement the select operation, we do need to augment our tree with extra information. This is something we very commonly do with data structures. We augment the data structures with some extra useful information. 

The downside of doing this is we have to make sure we keep that information up to date whenever we modify the structure of the tree. So whenever you do inserts and deletes, you have to make sure you keep this extra information up to date. Conveniently, we can do that as we do inserts and deletes without too much extra headache with most common types of augmentations on a binary search tree. Here, I'm just going to augment every single node with the size of its subtree. This is a very common augmentation, so every node knows how many nodes live in its subtree. And the convenient thing about that is that basically lets me navigate to the element of a given rank very easily. So starting at the root, all I really need to know if I'm trying to select for the element of rank k. 

Is the root the answer, or is the answer going to be on the left side of the root or on the right side of the root? If I can make that determination, then I can effectively binary search walk down the tree to find the element that I'm looking for. And the key to doing that is realizing that we can easily figure out the rank of the root. The rank of the root is where it lives in an inorder traversal. And so what gets printed before the root in an inorder traversal? The left subtree. So basically, the size of the left subtree is the rank of the root. 

That's the number of elements preceding the root in an inorder traversal. And so the first thing we do with the select operation is we look at the size of our left subtree. That gives us the rank of the root. Let's call that r. And if the rank we're looking for, k, is equal to r, then the root itself is the answer. If the rank we're looking for, k, is less than r, we can recursively select for that element in the left subtree. If the rank we're looking for is bigger than r, then we can recursively select in the right subtree. 

The only catch, and this catch exists also in the quick select algorithm, is that if you recurse to the right, you need to modify the rank that you're looking for when you recurse. Because locally, within the right subtree, you'll be looking for a different rank than you would have been looking globally within the entire tree. That's because the root and the left subtree have now left the picture. So you effectively have to adjust your rank by subtracting out the contributions of that many elements, essentially. So just don't forget to do that if you're implementing the select operation. This runs in order log n time in a balanced binary search tree. It's very similar to the find operation. 

You're navigating your way down the tree just using the subtree sizes as a guide as you go. The rank operation is also very easy to implement. All you do is to figure out the rank of an element, you need to figure out how many elements are smaller than it, because that will tell you its position within the inorder traversal. So one thing you can do to figure out the number of elements smaller than e is, just walk from e up to the root and look at how many elements are kind of on your left as you go. Because due to the binary search tree property, smaller things are on the left. So all of these subtrees hanging off the path up to the root, they all contain smaller elements. And in fact, some of the elements along the path up to the root are smaller than e as well. 

You don't have to actually count one by one these elements, because remember, we've augmented elements with subtree sizes. So all you have to do is inspect the roots of those subtrees as you walk up towards the root and add up the total number of elements less than e. That'll give you the rank of e in log n time in a balanced binary search tree. Okay, let's talk about now this other use case of the binary search tree as a sequence. If we go all the way back to Module 1, we introduced arrays and linked lists and talked about how they are very kind of primitive ways of representing sequential data, and both of them had pretty substantial drawbacks. The array was really good at indexing into a particular element, given its index, and modifying that element, but it was really bad at doing inserts and deletes in the middle of the array. The linked list had kind of a very different set of trade offs. 

It was very bad at indexing into a particular element because you had to walk painstakingly down the linked list to reach that element. Now, conveniently, a binary search tree gives you an alternative way of representing a sequence that doesn't have any of these weak spots. It will basically support all of these major operations in log n time. The ability to index into the sequence, to modify an element, to do inserts and deletes anywhere in the sequence, all of that we can do in logarithmic time using a balanced binary search tree. So the binary search tree, or maybe one of its relatives, that we'll learn a bit later, like skip lists or b trees. This might be the ideal way to actually implement a sequence, depending on the application. To do this, we need to actually kind of revisit the binary search tree property, and we're going to change it around. 

Change the interpretation of the binary search tree property, so that the sequence that we're storing is going to end up being the inorder traversal of our binary search tree. So instead of elements on the left being less than you and elements on the right being bigger than you, the interpretation of the binary search tree property here is going to be, there's some element of our sequence sitting at the root. The elements preceding that element are going to be in the left subtree, and the elements after that element in the sequence are going to be in the right subtree. So the binary search tree property here isn't value-based anymore. You won't have smaller things on the left and smaller things on the right. You have things before you on the left and things after you on the right. And all of our access now to this binary search tree is going to be using the select operation to find the element that would be at position k within the inorder traversal. 

That's kind of at position k within the sequence encoded by the tree. We no longer can use the find operation because elements aren't really ordered by value, right? There aren't smaller things on the left and bigger things on the right. So find is kind of now meaningless. We only use rank based mechanics, rank based access to implement the major operations of selecting an element, or inserting an element at a particular rank, or deleting an element at a particular rank. Here's an example of what a binary search tree might look like if used to store an arbitrary sequence. So I've taken a totally arbitrary sequence, 3.141,592. 

Fun fact, I actually won a nerd pageant back at MIT for reciting lots of digits of pi, a point of pi. But so here, I've stored the digits of pi in a binary search tree. So if I do an inorder traversal of the tree, it will spit out exactly that sequence. So I have an arbitrary element of the sequence five stored at the root. I think that's the second five in the sequence. There are a couple of 5s, but everything preceding that is in the left subtree, everything after that is in the right subtree. And I definitely need to make sure that my elements are augmented with subtree sizes because we're going to have to use the select operation to do all of the accesses to this structure. 

So that's the way the binary search tree looks when you're using it to implement a sequence instead of the use cases of a set or as a map. So we really have now two very high level, very different ways that we use binary search trees. We can either use them as a set or as a map. And in those cases, we have two ways of accessing elements. We have kind of value based access using the find function, or we have rank based access using the select function. So we can either say, find me the element of a particular value, or we can say find me the element at a particular rank. Both of those are used when the binary search tree is being used as a set or as a map. 

If the binary search tree is instead being used as a sequence, we're kind of using this slightly different interpretation of the binary search tree property. In that case, then we only use a rank based access. We only use the select operation because find doesn't make sense anymore. What about some of the other operations on binary search trees that we introduced in the context of a set or a map? Things like predecessor, successor, min, max, split and join. Those also have very nice interpretations when you think about them in the context of a binary search tree for representing an arbitrary sequence. So predecessor and successor, those just step to the preceding in the next element in your sequence. 

That's an incredibly useful operation to have. Min and max would just jump to the beginning and the end of your sequence. Split and join, that's actually also very useful if you split a sequence on some index that basically pulls it apart into two sequences, each represented by its own binary search tree. And then you can use the join operation to take two sequences, each represented by its own binary search tree, and to glue them together kind of end to end, into one sequence represented by a binary search tree. Using that, you could achieve functionality like, say, cut and paste. Maybe if the sequence in your binary search tree represents the document that you're storing in a text editor, then you can use a couple of split and join calls to splice out part of the sequence and then splice it back in at another place, all happening in just log n time per splice. So some very powerful ways that we can interact with sequences using these operations. 

So to recap, I think, to me, at least, I kind of like to think of the binary search tree philosophically as just a data structure that treeifies a sequence. It fundamentally serves to encode a sequence horizontally in its in- order traversal from left to right. In the event that you're using the binary search tree in more of its traditional role as a set or a map, the sequence represented by the binary search tree is just the sorted ordering of the elements that you're storing in the structure. But in either case, the binary search tree is really just kind of storing a sequence and giving you a powerful way of interacting with that sequence. And many more advanced data structures are based on this ability to interact with sequences in kind of these powerful ways. So the binary search tree, this kind of gives us a nice high level outlook on the binary search tree and also connects it with a lot of common algorithms. Binary search is just the find operation. 

The binary search tree itself is basically quicksort. It is basically the recursion tree of the quicksort process, the select operation that we talked about at the beginning of our discussion, that's basically quickselect. And so the binary search tree has a lot of nice, rich connections with common algorithms. One other quick note, I guess, the use case of a binary search tree to represent a sequence. Sometimes, you will hear people calling that an order statistic tree, because the element of rank k is also known as the element, the kth order statistic within the tree. And so sometimes, people differentiate those two use cases using that different terminology for the binary search tree representing a sequence. To me, it's the same structure, it's the same underlying mechanics. 

We balance the trees in the same way. So I call them both binary search trees. But you'll hear the other term used perhaps in practice as well. [MUSIC]