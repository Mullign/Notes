![[Binary Search Tree Basics Slides.pdf]][Music] Dean: If you ever find yourself marooned on a desert island with only one data structure, then you'd better hope that one data structure is a binary search tree, known for its tremendous versatility. Because of that versatility, we are going to spend quite a bit of time in this module talking about binary search trees, starting here with the basics. To begin with, a binary search tree can be used to implement most of the major classes of data structures. They can be used to implement sets, they can be used to implement maps, they can be used to implement sequences, which we'll see shortly. They can be used to implement priority queues. They're incredibly versatile data structures. If we focus here on sets and maps, these are two classes of data structures that are, well, they're just everywhere. 

They're extremely prominent in algorithm design, in software. A set, for example, is maybe the most primitive type of data structure you can imagine. It's just a dynamic collection of elements, supporting insert and delete, where you also have a fast find operation that can tell you if an element is present in the data structure or not. A map extends that functionality by storing not just a set of keys, but a set of key value pairs. Every key in the structure is augmented with an extra value that you can get at by searching for the associated key. A lot of times languages have sets and maps built into them. A lot of times with maps, you can actually access them using this associative array notation, where you can say A bracket k to refer to the value associated with key k in a map named A. 

This looks like an array access, but is usually doing something very different than an array access. For example, it would, in the case of a binary search tree, be asking you to look up a key k in a binary search tree and basically reach over and do something with the associated value of that key k in the binary search tree. Tremendous power and versatility with binary search trees. Let's focus on sets and maps for this discussion, and we'll see in a future video how binary search trees, in particular, can also implement sequences, which is also a very powerful idea. If you were to implement a set or a map not using a binary search tree, you could go all the way back to your primitive data structures like arrays, linked lists, arrays that you keep in sorted order. Maybe you think keeping the array in sorted order would be beneficial because then your find operation would be fast because you could use binary search to find an element. But then again, insert and delete would be slow because you have to scoot elements over to keep the array in sorted order. 

Basically, any way you try and use an array or linked list, you're going to have a very slow running time for one of the fundamental three operations in a set. Insert, remove, or find, those are your three fundamental operations, and any incarnation of an array or linked list is not going to be efficient, uniformly across the board on all these operations. There will be an Achilles heel operation that runs slowly. Binary research trees, on the other hand, can do all of these operations quite efficiently in log n time per operation. We'll see also shortly there are some relatives of binary search trees called skip lists and B-trees that basically give you similar performance bounds, logarithmic performance bounds. In an upcoming module, we'll also talk about hash tables, also a very powerful and very general data structure. They actually give you constant time bounds for insert, remove, and find. 

That may even look more preferable just looking at the table here, but I guess some caveats. A hash table is a RAM data structure. It is operating in a more powerful model of computation than a binary search tree, which is comparison based. Binary search trees are doing the best with the model of computation that they've been given. Logarithmic running times are about as good as you can hope for with a binary search tree. Also, hash tables are very simple, very lean data structures. They can do insert, remove, and find really fast, and that is it or that is essentially it. 

A binary search tree can do insert, remove, find, and a whole host of additional extended operations. They really are like the Swiss Army knife of data structures. They can be used in very many different ways in algorithmic problem solving. What is a binary search tree? It's a simple concept. Basically it's a binary tree, so it consists of a bunch of individually allocated nodes and memory. Each node keeps track of a key, I guess possibly also a value if you're storing a map, and a pointer to your left and right children. 

Possibly, you might keep a pointer to your parent depending on whether that's needed for your application or not. A binary search tree satisfies the so-called binary search tree property, which means if you look at any node in the binary search tree, elements in your left subtree are smaller than you and elements in your right subtree are larger than you. That makes the tree, I guess, ordered horizontally from left to right. The small elements are on the left and the large elements are on the right. We often talk about balanced binary search trees. What I mean by that is basically that the height is order log n, and it doesn't mean perfectly balanced. We just recently talked about binary heaps that are ideal tree shapes that are completely filled in on every level and perfectly balanced. 

Binary search trees, that's a little bit too much to ask for, so we won't insist that they are perfectly balanced. If you look at the root, you won't always have half the elements on both sides of the root, but they'll be balanced enough, so the height is logarithmic in n, the number of elements in the tree. The binary search tree property, it basically is one of two major ways. We typically organize data in trees, maybe three major ways. But the binary search tree property imposes a horizontal ordering of the elements in our tree. Another prominent property that we've already seen is the heap property, which is a vertical ordering property. Those are two very common properties. 

Another one that I was just thinking of is sometimes you have what's called a try, where you're storing strings in the root to leaf paths in your tree. Those are probably the most common ways that we structure data in trees, either horizontally, vertically, or storing things in root to leaf paths within a tree. Binary search trees, we're going to be focusing on the horizontal aspect here. Now, almost every major operation in a binary search tree has a simple recursive implementation because binary search trees are recursive structures. If you look at your left subtree, that's a binary search tree. If you look at your right subtree, that is also recursively a binary search tree. You can implement typically most operations on a binary search tree very efficiently and very simply with recursion. 

You don't have to worry too much about overflowing your system stack depth because the recursion depth here is just the depth of the tree, which typically is going to be logarithmic if you keep the tree balanced. For example, to find an element in a binary search tree, then we use our usual recursive mentality. We have some base cases involving just looking at the root element. We just have to do actual work on the root element, saying, hey, was the root element the key that we were looking for? If so, that's what we should be returning. Otherwise, we're going to delegate the work that remains to recursion. We're going to say that's recursion's problem. 

If you have children perhaps, this will be a fun concept, delegate it to your kids in the case of a binary search tree. Let your kids handle the request. If the answer is not the root, then let your children sort out the rest of the details. Call find recursively on the appropriate child by comparing yourself to the key at the root and going left or right. This is actually nothing more than binary search in data structure form, if you think about it. This is why these are called binary search trees because they really are representing the essence of what binary search does. If you want to insert into a binary search tree, you can write very similar code. 

If the tree is empty, you just return a new single node, otherwise you just pass the request onto your children. If you're inserting a node less than the root, your left child can deal with it. If you're inserting a value bigger than the root, your right child can deal with it. Most operations in binary search trees have this structure. You do something where you look at the root and then recursively delegate to your left or right child as appropriate. Now, it does matter a lot, whether you keep your tree balanced, whether the height is logarithmic or not. Most operations on binary search trees have running times that are on the order of the height of the tree. 

If you're balanced, the running time is going to be logarithmic. If you're not balanced, the running time is going to be a lot worse. If you just insert, say, elements one through n without any attempt to keep your tree balanced, what you can end up with is a very degenerate looking tree. You can end up with a tree that looks like just a long path, basically a glorified linked list that has 1, 2, 3, 4, 5, 6 all the way down to n, all is going down the right spine of the tree. The depth of a tree here would be linear if you don't take any proactive steps to keep the tree balanced, and that would be very slow. The running time to insert n elements, if you don't make any attempt to keep things balanced, that could actually be quadratic because every single insert could take you linear time in the worst case. We do need to pay attention to keeping trees balanced, and that's usually the hard part with binary search trees, is keeping them balanced. 

There are maybe half a dozen prominent mechanisms out there for ensuring that you keep a tree balanced. Every time you insert and delete in the tree, you have to restructure the tree so that it stays appropriately balanced. That's usually the tricky part of implementing a binary search tree. The other operations are usually quite straightforward and recursive. There is actually one nice property or fact that we can keep in mind that will help us with balance. If you insert elements in random order into a binary search tree, think about randomized incremental construction essentially of a binary search tree, then it turns out that your binary search tree will end up balanced with high probability. That's quite nice. Actually, my favorite mechanism for keeping a binary search tree balanced is based on this fact. 

It actually always injects the right amount of randomness into the tree so that if you look at the tree at any point in time, it is probabilistically behaving as if you had just built it randomly from scratch according to this process of inserting its elements in random order. Therefore, the tree at any given point in time is always balanced with high probability. We'll see that it's actually very easy to maintain the tree in that state, even as you insert and delete elements. That'll be one of our most interesting mechanisms to preserve balance in the tree. Another really nice insight that also has to do with random building of binary search trees is that binary search trees are essentially the same thing as Quicksort. The binary search tree is really the recursion tree of Quicksort in some sense. If you look at all the pivots chosen by Quicksort, they form a binary search tree. 

If you look at all the comparisons made, when you build a binary search tree, those are the same comparisons that Quicksort would have made. Let's look at that a little bit more carefully because this is a very, very useful correspondence that will let us analyze binary search trees very easily, given that we have already spent a lot of time analyzing Quicksort and randomized Quicksort. If we look at say randomized quicksort, then what happens in terms of all the comparisons we make? We do a partition operation, where we pick a pivot element and everyone gets compared to the pivot element and partitioned into two subproblems; everything less than the pivot goes left, everything bigger than the pivot goes to the right. Then again, within those subproblems, you pick pivot elements and partitions happen, everyone gets compared to the pivots and partitioned onto the left or the right. What's the corresponding picture now with a binary search tree, especially when you build in random order? I pick some random element and I insert it into the tree. 

That becomes the root of the tree. That's analogous to the first choice of a pivot. Now everything else I insert into the tree will get compared to the root on its way into the tree. Elements that you insert into the tree, they get compared to the root and they follow a path down the tree until eventually they end up being inserted as a leaf. Every single element will still get compared to the first pivot, to the root just over time instead of right up front as it did with randomized Quicksort during the first partition operation. The comparisons that we do when we run randomized Quicksort or when we build a binary search tree by inserting elements in random order, they're the exact same comparisons probabilistically. Everyone gets compared to the root, and then everyone who went left they get compared to the root of the left subtree and so on. 

Because of this direct correspondence, we can actually say quite a bit about analyzing one of these structures given that we've already analyzed the other. For example, we can say that a randomly built binary search tree is balanced with high probability. Why can we say that? Well, when we analyzed randomized Quicksort, we looked at that from a per element perspective, we looked at a generic element of data, and we argued that that element of data over time over different partitions starts belonging to smaller and smaller subproblems. Initially, it's in a big problem of size n that's in a smaller subproblem and then a smaller subproblem. But the sizes of those subproblems satisfied the conditions of the randomized reduction lemma. That led us to conclude that we expected each element to only appear in a logarithmic number of subproblems, or even better each element with high probability will only appear in a logarithmic number of subproblems. 

From a binary search tree perspective, that means that the depth of a generic element in a randomly built binary search tree will be log n or log n with high probability according to exactly that same randomized reduction analysis. If the depth of one generic arbitrary element is order log n with high probability, you can extend that result to all n of your elements by taking a union bound over all n elements. The tools that we built when we studied probability theory are incredibly useful here. With high probability, all of the elements have a depth of order log n, so the entire tree has a depth of order log n with high probability. We have just argued that a randomly built binary search tree is balanced with high probability using what we already have learned about randomized Quicksort. What else do we like to do with trees? One thing we commonly do with binary trees is we traverse them. 

We walk around in the tree in some well-specified ordering. Maybe the most common type of traversal is called an in-order traversal. It's a recursive traversal where you recursively traverse the left subtree, then you print the root, then you recursively traverse the right subtree. Because of that ordering, the inorder traversal will just print out the contents of the tree in sorted order. Even better, it will do so in just linear time, irrespective of whether the tree is balanced or not. The inorder traversal, basically, you can think of it as like walking down every edge. Once in every edge, it walks back up exactly once. 

It really just takes a linear amount of time walking around the tree, irrespective of the shape of the tree. If you have built a binary search tree, you can print out its contents in sorted order in just linear time, which means it probably must require you to take n log n time to build a binary search tree because it's a comparison based data structure and otherwise that would let you circumvent the lower bound on comparison based sorting. That's why inserting into a binary search tree really must take log n time. Otherwise, we could sort faster than we ought to be able to. There are a number of other very prominent types of traversals that we see for trees. Many of them apply not only to binary trees but to higher branching factor trees as well. The preorder and postorder traversals, basically, with a preorder traversal, you first visit the root and then you visit the children of the root recursively. 

With postorder of the root is the last thing you visit. You recursively visit all of your children's subtrees and then you visit the root. Postorder traversals are extremely common when you're doing calculations on binary search trees. Maybe you want to count up how many nodes were in a binary search tree. If you traverse it in postorder, then you can sum up the total number of nodes as you go. By the time you finally reach the root at the end, you already will have computed the sizes of all the subtrees of the root, allowing you then when you visit the root, to just add those numbers up and get the size of the entire tree. Bottom up computations on trees are very natural to do with a postorder traversal. 

Another traversal you sometimes see, which is equivalent, I guess to a depth-first search of a tree, if you're familiar with that notion from graph theory, it's an Eulerian traversal. It's called that because if you double every edge in the tree, it's equivalent to an Eulerian tour of the tree in the graph theory sense. It's basically a traversal that you can think of that's walking around the perimeter of the tree. It really does walk down every edge once and up every edge once. That is actually used in quite a number of advanced data structures. There are lots of ways to traverse trees and to use that to good effect. It's nice, I guess later on, we're going to focus on the duality between a sequence and a tree. 

Trees often represent sequences. If you do an inorder traversal of a tree, that spits out the contents of the tree in some sequence, in this case, a sorted ordering of the elements in a tree. You can go from tree to sequence by traversing the tree. Later on, we'll look at the reverse viewpoint of going from sequence to tree by representing a sequence in a tree. It's a useful thing to start thinking about that trees represent sequences basically, and the traversal is a way that you can go from a tree to its corresponding sequence. I've already mentioned connections with binary search trees and sorting. Given a binary search tree, you can easily sort by just doing an inorder traversal to print out its contents in sorted order. 

This, you could call it maybe binary search tree sort, if you want to give it a name, but insert n elements into a balanced binary search tree that will take you (n log n) time and then traverse the tree. Very easy to sort with a binary search tree. Given a binary search tree, you can easily find the smallest and the largest element present in the tree. To find the minimum in the tree, all you need to do is walk down the left spine of the tree until you can't walk left any further and that's going to be the minimum. Symmetrically for the maximum, you would walk down the right spine of the tree. You can easily find those elements in order of height time or order log n time if your tree is balanced. Now, because you can easily find the minimum, a binary search tree can serve as a priority queue, because you can find the minimum easily, you can then remove the minimum, you can insert elements. 

A binary search tree does almost anything that a priority queue would need to do. You might ask, why do we study priority queues as a separate class of data structures? All the different priority queue implementations, binary heaps and randomly mergeable binary heaps, all of these different ways of implementing priority queues why do we need them when we also have a binary search tree that just one data structure that does everything? The answer there is it depends, but like with the binary heap that we've also talked about in this module, that's a much lighter weight data structure that's much simpler to implement and probably a little bit faster if you're just doing priority queue operations than a binary search tree because with a binary search tree, there's a little bit of extra machinery that you need to include to keep it balanced, and that makes the efficiency maybe just one notch lower than what you get with a true priority queue. Two other useful operations in a binary search tree are the predecessor and successor operations. If you're given a pointer to some element in a binary search tree, you can easily find the element that would be immediately before it or after it in an inorder traversal in the sorted ordering of all the elements represented by the tree. These are symmetric operations. 

Let me tell you how predecessor works and successor is the mirror image of that. According to the binary search tree property, where is your predecessor in the tree? That's the element that would be printed out right before you if you did an inorder traversal. What does that look like if I'm trying to find the predecessor of element e? Well, in an inorder traversal, I would print out the contents of e's left subtree right before I print out e. The maximum in my left subtree, that is my predecessor. Given that I have a maximum operation, I can easily just run that on my left subtree to find my predecessor. 

Now, sometimes it could be the case that e doesn't have a left subtree. Where's your predecessor in that case? Well, in that case, I guess there's a few ways you can think about it. One is if you walk up the tree from e, it's the first time you have a left parent, I guess, where you're the right child of your parent. That's your predecessor. You can also think symmetrically your predecessor is the element for which you are the successor, and by symmetry, the successor of an element would be the minimum in your right subtree. If you think about it, the blue element here is the element for which you are the minimum in its right subtree. 

Because if you look at the right subtree and then walk down the left spine of that right subtree, that's what e would be the minimum in that right subtree. You can easily find adjacent elements in the space of all the elements of a binary search tree. One useful note, I guess, that is a common misconception, your predecessor and successor are not always directly attached to you in a binary search tree. They could be somewhat distant. That's perfectly okay. But we have these operations that can find those elements still in order log n time in a balanced tree. Given that we have the predecessor and successor operations, this leads us to one possible way that we can delete an element from a binary search tree. 

There are a couple. This oddly seems to be one of the more prevalent ones that I see being taught in classes, and I think this is a little bit of a clunkier way to delete an element, but it's worth talking about because you're probably going to see it talked about if you talk about deleting from a tree with someone who's taken a data structures class somewhere. The idea here is deleting a leaf node from a tree is very easy. Leaf node has no children, so you can just delete the node. If you have a node with just one child, very easy to delete as well. If I want to delete this 2, I can just replace it with its one child. It's very easy to delete a node if it has only one child or no children. 

The tricky things to delete are the nodes with two children. The standard trick here is if you have two children, you can show that your predecessor and your successor both have at most one child. They can't have two children because then they wouldn't be your predecessor or successor, I guess, there'd be some element in between. In this case, you can pick one or the other and swap with your predecessor or successor. Let's pick my successor here. The successor of 8 would be 11. I would swap the 8 and the 11. 

Your successor is actually a safe element to put here with respect to the binary search tree property, and that relocates the 8 to a place where it's easier to be deleted, a place that has at most one child. That's one way that you can delete from a binary search tree. We'll see another better way in my opinion in just a second. Another nice feature of binary search trees, what happens if you search for an element that's not there? If I search this tree for 14, there isn't a 14, so the find operation will make its way down the tree and land on a node that isn't 14 obviously because 14 isn't there, but it's close to 14. It's actually the predecessor or the successor of 14 in value space. You can think about implementing two flavors of the predecessor and successor operations. 

One flavor would take a pointer to an element in the tree and ask what is its predecessor or successor? Another flavor would be a find operation where you give it a value and say, hey, find me this element if it's in the tree, but if it's not in the tree, find me the element with the next smallest or the next largest key. That's easy to do by a small modification of the find operation. This is actually quite powerful. If you think about hash tables with their constant time performance, they can't do this. They will just tell you the element you're looking for isn't in the structure, but they won't say, well, how about this nearby element instead. A lot of functionality here, if you ever go to a library catalog to look up a book if it's not present in the library, a lot of times what you will get is a list of nearby titles alphabetically that you can page through. 

That's the functionality that you would get with the predecessor and successor operations. It lets you explore nearby elements in the tree. We can also answer what are called range queries, which we'll talk more extensively about later on, the queries that you might find in a database system that say printout or count all the elements that live in a certain range. There's a lot to say about those types of operations. You can use predecessor and successor queries to step through a range to enumerate the contents of a range in response to one of those queries. Finally, I wanted to talk about two operations that I think don't usually get as much attention, and they really should when people study binary search trees. These are wonderful, elegant operations called split and join. 

They're inverses of each other, and they do what the name suggests. The split operation takes a binary search tree and splits it on some value into two smaller trees. If you split on a value k, then it pulls apart the tree into two binary search trees. One has keys less than k, the other one has keys bigger than k. The join operation does the opposite. It takes two binary search trees where the key ranges of the trees have to be disjoint from each other. They can't overlap in their key ranges. 

It's hard to join together binary search trees that overlap. But if the key ranges are disjoint, you can actually join those two trees together very efficiently into just one tree. Sometimes I've heard these operations called zip and unzip, because if you actually look at what they're doing recursively, it almost looks like you're taking a tree and just unzipping a zipper to separate it into two separate trees or taking two trees and zipping them together along their spines. We'll actually implement some of these operations in our coding discussion in this module to hopefully make them more concrete. But these are also very easy to implement in a recursive fashion. If you'd like to split a tree on some value k, then possibly, you have to actually take some action at the root itself if the root maybe is the value k. But more often than not, you can just delegate that to one of your child subtrees. 

If k is less than the value of the root, then you can just tell your left subtree recursively to split itself. It will dutifully do that by recursion, and that gives you the result of your split. You've got this one piece that gets split out of the tree, and then the other piece stays behind as your new left subtree, and that's the right hand side of the split. Very easy actually to implement operations like split and join recursively. Now that we also have join, that gives you, perhaps, in my opinion, a more elegant way to delete an element from a tree that doesn't have all these special cases about if you have one child or two children. To delete an element, you just replace it with the join of its children. This is something that we use a lot in tree data structures. 

We've seen in our example of a randomly mergeable binary heap. To remove the minimum, you basically replace the element you removed with the merge of its two children. It's a very prominent concept in tree data structures. How do you join those two child subtrees together? Conveniently, if you do delete an element, the two child subtrees of that element are disjoint in their key ranges, which remember that's important for the join operation. How do you join two trees together? You actually have a choice to make. 

You can do this recursively. You can think about either joining. Suppose those trees are called T_1 and T_2. You can either think about recursively joining T_2 into T_1's right subtree, or symmetrically, you can think about recursively joining T_1 into T_2's left subtree. In code, this is just one line of code where you just recursively call join in either case. The only worry here is if you choose poorly between these two cases, then there may be efficiency concerns to worry about. When we talk about balancing and in particular randomized balancing, we're going to choose between these two options with a biased coin flip, and we'll have a very simple way of deleting an element and of joining two trees together that preserves this random structure in our tree. 

These are a couple of the standard operations in binary search trees. Through the rest of the module, we'll not only see how to keep a tree balanced, but we'll also look at trees in more depth and see how to use them in algorithmic problem solving as well. These will hopefully become very familiar objects by the end of our module. [Music]