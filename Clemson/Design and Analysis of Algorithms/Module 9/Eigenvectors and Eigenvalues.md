![[Eigenvectors and Eigenvalues.pdf]]All right. So we've talked a lot about solving linear systems. I'd like to switch gears now and talk about one other major class of problems that also has a tremendous number of applications in practice. And that class of problems involves finding eigenvectors and eigenvalues of matrices. Also something you spend a lot of time in typically if you take a linear algebra class. One of my pet peeves about math classes that teach linear algebra is they often teach you about kind of what an eigenvector and an eigenvalue are, but they don't tell you all the cool things you can do with these quantities. And there are so many interesting things, interesting problems that you can approach with eigenvectors and eigenvalues that they're wonderful entities to study mathematically and algorithmically. 

So in our discussion here, we'll basically just be able to scratch the surface of types of problems that you can address with eigenvectors. So just as a super, super quick review, eigenvectors and eigenvalues come in pairs. You say that lambda and X are an eigenvalue and eigenvector pair if multiplication of your matrix A by X gives you lambda X. So basically an eigenvalue and eigenvector of a matrix are kind of a stationary distribution. Sometimes we think of that about X as kind of a stationary distribution because multiplication of that vector by the matrix only rescales it by lambda, by the eigenvalue. It doesn't change its direction. So that's kind of, that's one way to think about or to define an eigenvector. 

It's a vector for which multiplication by your matrix only rescales the vector. It doesn't change its direction. In general, if you have an N by N matrix, you'll be able to find N eigenvalue, eigenvector pairs for that matrix. And there is a lot of fine print. There are cases where this doesn't happen and I won't go into the details, but kind of in general, this is what you expect to happen. And the nice case, especially is when your matrix is a symmetric matrix. And in practice, many of the applications where we look for a solution using eigenvectors and eigenvalues, they involve symmetric matrices conveniently. 

So in that case, your eigenvectors are very well behaved. They are real valued and they're all orthogonal to each other. So they form an orthogonal basis of space. If your matrix is not symmetric, then all bets are off. Your eigenvectors may not be orthogonal. They might have complex numbers in them that's a lot more complicated perhaps. So we'll tend to focus on symmetric matrices or matrices that are closely related to symmetric matrices such that they inherit some of these nice properties. 

Also, I guess worth noting, if you have a non-square matrix, it doesn't have eigenvectors and eigenvalues per se. Eigenvectors and eigenvalues are really only defined for square matrices. If you have a rectangular matrix, you can define something similar. You can talk about its singular vectors and singular values. Singular vectors come in kind of left and right flavors. So there's kind of two sets of singular vectors, and they are the eigenvectors of A transpose A and AA transpose, both of them conveniently being symmetric matrices. And those two matrices have the same sets of eigenvalues. 

So a lot of problems for which eigenvectors are the solution, they can be generalized to situations where singular vectors are the solution in a non-square situation. We'll see a few examples like that in a second. Okay, one nice thing about eigenvectors is that you can compute them quickly. At least you can compute certain eigenvectors very quickly using a technique called the power method. It's one of the simplest algorithms you could ever imagine. It basically just takes a vector and keeps multiplying it by a matrix over and over and over again. That's all it does. 

Why does that actually work? Let's maybe think about eigenvectors in another way that kind of characterizes what they do when you multiply a vector by a matrix. So if I switch over to the whiteboard, suppose I take a matrix and I multiply it by a vector X. What does that do to the vector X? That gives you a new vector as a result. But if we look at what that does, then let's take our vector X right here, and let's look at the eigenvectors of our matrix A. And if A is well-behaved, if it's a symmetric matrix, then its eigenvectors are going to be orthogonal to each other. 

So maybe I have one eigenvector, and then I have another eigenvector that's going to be orthogonal to it, so kind of a right angle here. So what does multiplication of A by X do? Well, I can kind of decompose X into the amount of X that kind of lies in the direction of one of those eigenvectors, and then the amount of X that lies in the direction of the other eigenvector. So X, I can kind of write it as a sum of components that live in the directions of the different eigenvectors, and those components get scaled by the respective eigenvalues. So the component of X in the direction of the first eigenvector, that gets scaled by lambda 1, by the first eigenvalue. The component of X that lives in the direction of the second eigenvector, that gets scaled by the second eigenvalue. And so if you have a really, really big eigenvalue attached to some eigenvector, that direction gets scaled by quite a bit. 

So now imagine for a second that maybe this first eigenvector that I've drawn is the one that has the biggest eigenvalue. So multiplying by X is going to emphasize that direction. It's going to stretch the part of X in that direction by a lot more than the parts of X in other directions. And so if I keep multiplying by X, this vector X by A, over and over and over again, that direction, the eigenvector with the biggest eigenvalue, that is going to become even more pronounced relative to the other directions. And that's basically what makes the power method work, is it kind of stretches out your vector in the direction of the most prominent or the principal eigenvector the most. So what we do is we start with an arbitrary vector, we multiply the matrix A by it, and then we normalize the vector to have unit length. We have to normalize because otherwise the length of the vector might just blow up on us. 

If, say, the eigenvalue is 2, then we're doubling at each step and we don't want this to blow up. So we basically renormalize the length of our vector at each step. And so the contribution in the direction of the principal eigenvector will dominate as we continue to do this matrix vector multiplication. The other contributions in the directions of the other eigenvectors, they'll die out. So this is called the power method, and it's a very simple way that we can calculate the principal eigenvector, the one that is paired with the largest eigenvalue, the one that has the biggest magnitude. And conveniently, it does turn out that for many problems where eigenvectors are the answer, the eigenvector in question that you really care about is the principal eigenvector. And so you can calculate that really quite easily and quite efficiently with the power method. Even if you need to know other eigenvectors, a lot of times the ones you need are like not just the principal eigenvector, but a couple of top eigenvectors, the ones that are paired with the biggest eigenvalues. 

And you can use still power method like techniques to calculate those by basically kind of using the power method to find the biggest eigenvector, the principal one, and then you can run the power method again. But you can kind of in each step subtract off the contribution in the direction of the principal eigenvector. And that leaves you with the second largest eigenvector, the one paired with the second eigenvalue and so on. So you can actually adapt the power method to calculating not just the principal eigenvector, but the couple, you know, top eigenvectors. You can also adapt it to calculating the lowest eigenvector, the one with like, say, the smallest eigenvalue. By modifying your matrix A, there are some simple modifications you can do to a matrix that will like reverse the order of its eigenvalues. And so what used to be the smallest becomes the biggest. 

And so running the power method on that would give you basically what used to be the smallest eigenvector. So there are various ways you can adapt the power method to find not just the principal eigenvector, but others if needed. Let's talk about a couple of applications now where the principal eigenvector or other eigenvectors tell you some very interesting information about a graph or a data set. One very interesting connection here is with random walks in graphs. And so if I have a random walk in a graph, then suppose I start in this upper left hand node right here. So random walks are usually characterized by kind of a probability distribution over where you are in the graph. So we start out with a probability distribution that tells us that we are definitely in that upper left node. 

There's a probability of one of being there and zero of being anywhere else. After one step, then you basically follow a random outgoing edge from wherever you are. And so there's a one third probability of being in any neighbor of that upper left node after the first step of your random walk. After the next step of your random walk, you would take each of those one thirds and you would divide them up in equal measure and send those equal measures out along all of the outgoing edges and kind of redistribute the mass in your probability distribution. Everything will still add up to one in that probability distribution. It just gets kind of redistributed in each step of the random walk. Whatever mass lives on a node that gets sent out in equal proportion along the neighbor to the neighbors of that node. 

So you still have a total of one unit of probability across the entire graph. One interesting thing about a random walk is that under appropriate conditions, if you run the random walk long enough, it loses track of where it started and it will always converge to what's called a stationary distribution. It'll converge to the same probability distribution over all the nodes. If you run the random walk long enough, there is going to be a five out of 35 chance that you'll be in this node right here, irrespective of where you started. So what conditions are necessary? Well, the graph has to be fully connected. That kind of makes sense because otherwise it would matter where you started. 

It would matter what connected component you started in. And it can't be bipartite because otherwise it would matter whether you started on the left or the right hand side of the graph. You know, you'd alternate back and forth in each step. So if those conditions are satisfied, your random walk will always converge to a stationary distribution. What does that have to do with eigenvectors? Well, this stationary distribution ends up being an eigenvector because you can describe the evolution of each step of a random walk as a matrix vector product. You can take a matrix, which is called the walk matrix of your graph. 

The entries in that matrix basically tell you the probability of stepping from one node to another in a given step of the walk. And so taking one step of the walk evolves your probability distribution X by multiplying it by the walk matrix. And so if you run this random walk for a long enough time, you reach this stationary distribution, which is characterized by multiplying by the walk matrix doesn't change it. And so this stationary distribution is an eigenvector of the walk matrix paired with an eigenvalue of one. It turns out that any matrix of this sort where the rows or the columns add up to one, that's called a Markov matrix, and it has a lot of really nice properties. But one of those properties is that it always has an eigenvalue of one, and that's the one whose eigenvector is the interesting one. That's the one that's basically our stationary distribution. 

I guess I should probably say that there is always fine print here. So remember, there are conditions that have to hold in order for this stationary distribution to hold. But all of the other eigenvectors of this matrix, they tend to be paired with eigenvalues that are smaller in magnitude than one. So that's why if you keep evolving your random walk by essentially multiplying it by the walk matrix over time, you're basically running the power method and you don't even have to renormalize your distribution because of the fact that the total sum is always going to be one when you multiply by the walk matrix. So you're basically running the power method here. All of the other eigenvectors are kind of decaying away, leaving only the principal eigenvector, which is the stationary distribution when multiplied by the matrix. So what sort of interesting applications are there here? 

Well, the stationary distribution of a random walk has a really nice interpretation as telling you centralities of different nodes in the network, because if you imagine, you know, trying to measure how important is a node in a network in terms of its overall connectivity, one way to measure that is you can say that a node is important if a random walk frequently would go through that node because it somehow would be centrally well connected in that case. And so if you do a random walk, compute the stationary distribution, that kind of gives you a score that you can assign to each node that reflects how central that node is in the connectivity structure of your graph. It turns out that if your graph is an undirected graph, then the stationary distribution really only just tells you the degrees of your nodes. The degree is the number of neighbors. So the numbers attached to the nodes in a stationary distribution in this case are just going to be proportional to the degrees of our different nodes. So the upper left node has three neighbors. And so its entry in the stationary distribution is three out of thirty four. 

This node right here has four neighbors, and so its entry is four out of thirty four and so on. They all add up to one. If you add up all the degrees, you get thirty four in this case. So in the undirected case, this principal eigenvector maybe doesn't tell us anything super exciting. It just tells you the node degrees in the directed case, though. You get something very interesting. This is actually basically the Google PageRank algorithm. 

So the numbers in this stationary distribution tell you basically your so-called PageRank score tells you how important the node is in the context of your directed graph. And this is used with Google basically to rank the web pages in the World Wide Web in response to a search query. So if you type in a search query, it finds all the. pages that match your query and presents them to you in order with the largest page rank score first, with the thought being that that's the web page that someone surfing the web randomly would wander through most often. So it has to be prominently linked to. And it's probably very important because of that. So there's one other wrinkle with the random walk that the Google page rank algorithm does. It doesn't just do a random walk on the worldwide web, where each web page is a node. 

And you follow a random outgoing link at each step. In each step, there's a small probability that you teleport to a random web page. The goal of this algorithm is to kind of model a random web surfer. In each step, your random web surfer either just clicks on a random link in your given page or types in a new random page into the URL bar and effectively teleports to a brand new page on the worldwide web at random. And so that's really the random walk that the Google page rank algorithm is considering. It takes the stationary probability that you get with the principal eigenvector of that walk, which you can compute. Even though the worldwide web is huge, you can still compute that effectively using the power method. 

And that gives you a good score that measures the importance or centrality of every web page. So eigenvector is very important. Launched a multi-billion dollar company, Google. So they are very effective at measuring centrality in networks. If you look at other eigenvectors in a graph like this, so the principal eigenvector is actually kind of interesting. Of the walk matrix, it involves usually non-negative numbers that you interpret as centralities. The other eigenvectors that are not the principal eigenvector, they end up being orthogonal to this one. 

So they have actually positive and negative entries. And those actually end up having a really nice interpretation as cluster pairs. So if I look at that same network and look at one of the non-principal eigenvectors, I actually end up getting what could be a reasonable split of the network into one cluster here on the left and another cluster on the right if I take the positive entries in the eigenvector versus the negative entries in the eigenvector. And the reason an eigenvector like this does tend to reveal this sort of potential split into two clusters is because of this interpretation of an eigenvector as kind of being stationary when multiplied by the walk matrix. Remember that when you multiply this vector by the walk matrix, it redistributes the weights given by these numbers by taking each one of those weights and distributing it in equal proportions out to the neighbors. And so if you have a graph that divides up into two very pronounced clusters, then it will make sense for an eigenvector to reflect that because the positive weights in one cluster will kind of mix around and reinforce each other. And the negative weights in the other cluster will mix around and reinforce themselves when you multiply by the matrix. 

But there isn't much crosstalk because the two clusters don't have many edges going between the clusters. And so you don't get a whole lot of cancellation between the positives and the negatives. And so because of the fact that this vector is stationary with respect to our walk matrix, that means that the positives and the negatives kind of form these kind of self-reinforcing communities, i.e. clusters. And so you can actually use eigenvectors to find clusters in graphs as well. There's also a really nice clustering objective function called the normalized cut clustering objective. It's similar to the ratio cut objective that I think we introduced maybe back in module 7. But it has a nice interpretation in terms of random walks. 

It tries to find a partition of your graph into two clusters so that you kind of minimize the probability of stepping between those two clusters in any given step of your random walk. And it turns out that to solve the normalized cut problem, it's an NP-hard problem, unfortunately. You can actually relax it to a continuous version of that problem, where the relaxed problem, its solution is actually an eigenvector. So it's a very closely related problem to what we've been talking about here with random walks and eigenvectors. You can do a lot of things in this same sort of kind of space of ideas. A really nice algorithm that I would recommend having a look at is sometimes called the HITS algorithm. It basically is kind of doing a similar eigenvector-based analysis of centrality in a directed graph, although it kind of gives you a separate answer for ingoing versus outgoing connectivity. 

So it kind of basically says that every web page can serve two roles. It can be a hub, which is kind of a web page that has a lot of high-quality outgoing links. So that's kind of a page that has a lot of centrality on the outgoing side. Or it can be an authority. That's a web page that has a lot of high-quality content that might have a lot of high-quality incoming links. And so that's a page that has a lot of kind of a high degree of centrality on the incoming side. And so using the singular value decomposition, which I remember told you has kind of two sets of eigenvectors, you can actually separately analyze the incoming versus outgoing centrality of each page. 

You get separate kind of page rank scores, if you will, characterizing each web page in terms of how well it serves as a hub and of how high quality are its outgoing links versus how well it serves as an authority, how high quality are its incoming links. So lots of really cool analysis that you can do with that algorithm. And you can also look at these pairs of singular vectors from a clustering perspective. You can find bi-clusters in your network where kind of the hub and authority scores mutually reinforce each other. So a lot of really nice network analytics that you can do with eigenvectors and eigenvalues. Another nice application, you can use eigenvectors for image segmentation. This is really just looking at clusters and graphs in another way. 

So I can turn an image into a graph by basically making every pixel into a node and connecting it to nearby nodes. And of course, the weight of the connection would have something to do with the difference in pixel value, so maybe a very high weight if the pixels differ in intensity by quite a bit. And now if I want to find two clusters in this network, that might involve segmenting a foreground image from a background image. And so this is just an example of my cat and how I've turned the cat into a graph and found a couple of the eigenvectors of that graph and color coded this image according to those eigenvectors and which parts of the eigenvectors are positive versus negative. Because remember that those non-principle eigenvectors, each one suggests a two-way split of your graph. And so if you look at eigenvector 2, 3, and 4 in terms of prominence, 2 and 3 don't quite tell you the answer you're looking for. You get a prominent horizontal split of the image and a prominent vertical split of the image. 

But eigenvector 4 actually correctly captures the split between the cat and the non-cat, the foreground from the background. There's a lot of really cool mathematics that you can look at for these sorts of problems relating eigenvectors and graphs. There's an entire area of mathematics, in fact, called spectral graph theory, which is the study of graphs from the perspective of their eigenvectors and eigenvalues. So lots and lots of really cool applications to look at in that space. One other nice idea here is the following. Suppose I ask you if there was a way that you could assign weights to the nodes of a graph in such a way that kind of conveys the same vibe as maybe a Gaussian distribution would around some point in space. So I'd like to have high weight on x and on x's neighbors and maybe kind of dying off as you get further and further away from x. 

What would be a reasonable way to come up with such a distribution? One very simple way is you can do a random walk in your graph but modify the random walk. So in each step, there's a small chance that you reset and teleport back to your starting point x. And so what that would do is it would basically kind of bias the stationary distribution to have a lot more weight concentrated around x. There's a lot of ways you can actually view this sort of random walk. Sometimes it's viewed as like a diffusive process where I've heard it described as kind of you knock over a can of paint at x. And some of the paint sticks to x. 

And some of it oozes over to the neighbors of x and sticks there. And then some of that oozes over to the neighbors of the neighbors. And you end up with a distribution of paint that is highly concentrated around x that has basically that's another way of looking at this same process of doing a random walk that occasionally resets itself and jumps back to x. Similar mechanics actually to the Google PageRank random walk is just with the Google PageRank random walk. Every time it resets or teleports, it teleports to a random place. And here we're teleporting back to x. For that reason, sometimes this is called personal PageRank because you're basically running the PageRank algorithm. 

Just instead of teleporting to a random page on the entire internet, you always jump back to your home page. That's your starting point. And so what you end up with as a stationary distribution is a distribution that's much more highly concentrated around your starting node x. So what sort of applications could you address with this type of algorithm? Well, let me just tell you one or two. If you want to build, say, a recommendation system, you can model that as a bipartite graph with users on one side and purchases on another side that the users have made. And so each user is connected to the nodes representing that user's purchases. 

And so now, suppose you'd like to recommend to a user what things they should purchase that they may not have purchased before. So what nodes are kind of similar in terms of connectivity on the purchasing side to a given user? So one way you could do this is exactly as we just talked about. You could run a random walk that keeps resetting back to the user, so to node x, and look at the stationary distribution of that random walk. Presumably, that will assign high weight, especially to the purchases of that user, but also to purchases that are well-connected with the user. So maybe they are things that were purchased by other users that made similar purchases to user x. And so you could use these types of ideas to build a recommendation system. 

What if you have the very common problem in data analytics of having to impute missing values in a data matrix? So I have a data matrix here of basically n data points, n objects, each of which is characterized by d features. And so this is a very common way that we represent data as a data matrix A. And suppose we're missing some of the values in A. This is very common. How do you impute what those missing values might be? Well, you can basically visualize this data matrix as a bipartite graph with objects on one side and features on the other side, so n nodes on the left and d nodes on the right. 

The weight of every edge is going to be the value in the A matrix corresponding to the connection between that object and that feature. And if you'd like to impute the weight on a missing edge, then what you might do is run one of these diffusion kernels and figure out how much weight. If you run, say, a personalized PageRank algorithm from x, you can measure how much weight ends up at y. And that might tell you how strong of a connection there should be between x and y. This is measuring some level of indirect connectivity between x and y. So a couple of perspective applications there. One final thing. 

I know we've talked about a lot of ideas so far, but let me mention just one last idea that is good to know about when you're dealing with eigenvectors. A lot of times, you end up with an objective function in a problem that's what's called a quadratic form. In linear algebra terminology, it looks like x transpose Ax or x transpose Ay if it's a rectangular sort of objective function. This is a very natural form of kind of an objective that basically takes the sum in the rectangular case over all pairs of products between entries in x and entries in y weighted by the entries in A. So this is basically kind of a generic quadratic form that just takes all possible pairs of products between entries in x and y, but the A's are the coefficients of those entries. And it adds them all up. And so you can describe that very concisely in linear algebra terms as x transpose Ay. 

If you ever are faced with a quadratic form like this, and you have to minimize it or maximize it subject to the constraints of x and y being unit vectors, then it turns out that the answers are going to be eigenvectors. So for example, if you want to maximize x transpose Ax with x being a unit vector, the optimal solution is just the principal eigenvector x, the largest one. If you want to minimize, you use the smallest eigenvector and so on. So in the rectangular case, you'd use singular vectors instead of eigenvectors. So there are lots and lots of problems out there that either exactly have this sort of objective function, or they can be relaxed to a form that involves this sort of quadratic objective function. So you may have heard of principal component analysis. That involves an objective function that directly looks like this. 

And so the answer is an eigenvector. There are lots of different objective functions for graph problems. We talked about earlier ratio cuts, normalized cuts. There's another prominent one called modularity cuts in graphs. All of these are NP-hard problems that can be relaxed to a quadratic form objective function. So an eigenvector will solve that relaxation. So it's just good to know that if you encounter an objective of this form, then basically the answer is going to end up being an eigenvector. 

One example that we have already seen before of that is one-dimensional embedding of a data set. So remember, if you have a matrix Aij whose entries tell you the similarities between data elements, so Aij is how similar element i is to element j, suppose you would like to embed your data elements on a one-dimensional number line. So element i is going to be assigned to position x i. And the goal in doing this is to minimize the kind of data elements that you're going to embed. of weighted squared wire length objective. So you take the squared distance between two data elements, you weight that by how important the connection between those two elements, that connection is, and then you add that up over all pairs. And so if you have a pair of elements that are very similar, so AIJ is very large, then you definitely want to put them close together on the number line, because otherwise you're going to contribute a very large penalty towards your objective. They're going to have a very large squared error between them. And so this is a very common objective function for basically dimensionality reduction or for embedding data in one dimension. 

One thing we do have to be careful about here is you want to impose some additional constraints to make sure that the trivial solution of assigning every element to the same point is not allowed. And so we're going to insist that our embedding has zero mean, so all the points are embedded on the number line in a way that averages to zero, and unit variance, and so the sum of the squares of the x's, that has to be one. So there has to be some spread. And it turns out that this problem can be rephrased in a way that involves a quadratic form as the objective. You can basically rewrite it as X transpose LX, where L is the so-called Laplacian matrix of your original data matrix A. This Laplacian matrix is a very common thing that shows up in a lot of graph optimization algorithms. And so it ends up that you can solve this problem, since you're trying to minimize, by taking actually not the smallest eigenvector, but the second smallest eigenvector. The smallest eigenvector is kind of degenerate, and you have to rule it out because you want to have a solution that has zero mean, and that means orthogonal to the vector of all ones, which is the degenerate lowest eigenvector. So I won't go into too much detail, but it's just nice to know that a problem we have seen before, we studied this back in module 7, when we talked about iterative refinement and relaxation, a problem we studied before has another interpretation as, you know, basically having an objective value that is a quadratic form. So its optimal solution is indeed going to end up being an eigenvector. And in module 7, we showed that there was another interpretation of finding that eigenvector using essentially a modification of a power method that involves trying to, in each step, replace every single node with the average of the positions of all of its neighbors. And so there are some really nice connections between lots of the topics that we've talked about so far. So I just wanted to give a couple of examples of types of problems here, where eigenvectors show up and play a prominent role. You can even generalize this one-dimensional embedding situation to multiple dimensions. So here's an example where I've turned my face into a graph, and I've embedded it in two dimensions by using numbers from the principal eigenvector as x coordinates, and numbers from the second most dominant eigenvector as y coordinates. And so you can kind of see that this does indeed give a decent embedding, even in higher dimensional spaces as well. So just a couple of interesting ideas involving linear systems. We've seen a lot of them in this video, and I don't expect everyone to be able to, you know, be able to use them, you know, right out of the gate, because there's a lot of details. But at least, hopefully, this gives you some ideas for types of algorithms you might be able to apply to whatever problems you're coming across.