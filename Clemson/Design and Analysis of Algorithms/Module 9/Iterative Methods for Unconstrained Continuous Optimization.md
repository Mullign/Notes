![[Iterative Methods for Unconstrained Continuous Optimization.pdf]]This video really lies at the heart of the present module covering the core topic of how we can use iterative refinement-based algorithms to solve continuous optimization problems. We're going to focus here on the case of unconstrained optimization, so optimization over all space. We're not going to have any side constraints governing which values of x are feasible versus infeasible. And there's quite a bit one can say just about this topic. Our goal here is to stay at a relatively high level and do kind of a survey of prominent methods and variations on those methods that people tend to use in practice. We'll try not to get too bogged down in the mathematics along the way because there are lots and lots of subtle details that you can get into with these types of problems, like how do you tune the parameters, as with many other types of optimization methods. The high level picture here, though, is quite simple. 

We're just doing iterative refinement, so we have a guess x for our current solution and we want to make x better and better. So if you're trying to minimize a function, you want to move in a direction that kind of is downhill with respect to the function. So the only questions you have to ask at each step are what direction to move in, presumably a direction that moves downhill, and how far to move, what's the step size that you move in that direction. And that's it. And there's a lot that goes on in choosing the answers to those two questions. What we'll kind of see is that there's a continuum of methods that on one side involve a very large number of low quality but fast steps. And so you're taking these kind of micro steps toward the optimal solution, but they're very, very fast, but each step may not be of terribly high quality. 

On the other end of the spectrum, you're taking a smaller number of steps, but each step is slower to calculate, but higher quality. And so that would be something like, say, Newton's method, which we'll talk about in a second. So one of the other ways that you can slice and dice these techniques is based on how easy it is to calculate derivatives of the function that you're trying to optimize. So as we've talked about in our introduction to the module, if you can't calculate derivatives, you typically fall back to techniques like the Nelder-Mead algorithm, that's the simplex search algorithm, or maybe genetic algorithms or other sort of population-based approaches. That is a pretty popular technique if you can't calculate derivatives easily, and that's actually quite common with complicated objective functions. There are also objective functions that may not be differentiable everywhere, and we'll see how to address those a little bit later on in this video. If it's easy to calculate first derivatives, then we often use kind of the workhorse of this video is going to be a technique called gradient descent, where you basically use the first derivative of the function to locally approximate it as a line or as a hyperplane, more generally in higher dimensions. 

And that suggests a direction of descent, and that tells you which direction to move in. So gradient descent is actually where we're going to spend most of our time in this video, but that assumes that you can calculate first derivatives or gradients in higher dimensions relatively easily, and there are lots of techniques for doing that. Calculating the gradient is just a mechanical operation. So even if your function represents something very complicated, like a deep neural network, there are automated ways of calculating derivatives, even for complicated functions like that. On the other end of the spectrum, where we take a smaller number of higher quality steps, we have something like Newton's method, where here we use second derivatives. So here, you'd only want to use this method if it's easy to calculate the second derivative of your function. But here, using the first and second derivative, you locally approximate your function around your current guess as a parabola, as a quadratic function, and then that suggests a direction of movement. 

In fact, you could even minimize that quadratic to figure out where you want to jump to next. Kind of the minimum point of that quadratic. Or you could just move in a direction that is towards the minimum point of that quadratic. So these are two very prominent approaches. The general idea, though, is you use a couple of terms from the Taylor expansion of your function to locally approximate it as a linear or a quadratic function, and that suggests a direction of movement. And the mathematics here isn't too bad. If you know calculus, then the formulas down below will be pretty straightforward, how you can kind of locally approximate a function using its derivative or its second derivative. 

If you've studied vector calculus, then the formulas look very similar for how you can approximate the function locally as a linear function or as a quadratic function. But I won't go too much into the mathematics, because the goal here is to kind of give you more of a high-level sense of how these techniques work. Let's start out by talking a little bit about Newton's method and its pros and cons. So here, you're again taking a small number of high-quality steps, because each step is modeling the local structure of your function more faithfully than, say, gradient descent would do. The bad news is each step is actually quite expensive from an algorithmic perspective, because you actually are modeling your function as a quadratic function, and so minimizing a quadratic in d dimensions basically boils down to solving a linear system with d variables and d equations. And so the running time you're looking at for that is something kind of like d cubed, or slightly less if you use fancier methods for solving linear equations. But that is a pretty heavy calculation, and that's not going to scale well if your dimensionality is extremely large. 

So Newton's method might just be off the table outright if you're in a very high-dimensional space. On the positive side, in terms of good news, Newton's method converges extremely quickly, especially when you're getting very close to the optimal solution. Oftentimes we analyze the performance of Newton's method by assuming that our function is a quadratic or something similar to a quadratic. Actually if your function is a quadratic, then Newton's method would minimize it in a single step. But if your function is well approximated by a quadratic, then Newton's method converges extremely fast once you arrive at the vicinity of a locally optimal solution. We call this actually quadratic convergence, which is another one of those confusing terms. Let me switch over to the whiteboard and kind of talk about what quadratic convergence means. 

It means that if you have an error at any given point in time, say your error is 10 to the minus 3, then in the next step the error inherent in your guess is going to be the square of that, which is 10 to the minus 6. So that's why we call it quadratic convergence. But if you think about it in terms of the number of digits of accuracy in your solution, you've gone from kind of three digits of accuracy to six digits of accuracy. You've actually doubled the number of digits of accuracy in your solution. And that's a very, very strong guarantee. Every step of Newton's method as you get very, very close to your optimal solution doubles the number of digits of accuracy in your solution. So you have an exponential increase in digits of accuracy as you perform successive steps. 

So very, very fast convergence of Newton's method as you get close to the optimal solution and as your function is well approximated by a quadratic. Before that, kind of in the initial steps of Newton's method, kind of anecdotally it doesn't do as well as when it's really close to the optimal solution. And so sometimes what people do is they maybe start out with something more like gradient descent and they switch over to using Newton's method as you get closer and closer to the optimal solution. So there's an algorithm called the Levenberg-Marquardt algorithm, which basically moves in a direction that is kind of a weighted average of the direction suggested by gradient descent and the direction suggested by Newton's method. The direction here for Newton's method would be the direction towards the minimum of the quadratic that we're using as an approximation of our function. And we adjust the weights over time so that as you get closer and closer to the optimal solution, we use more of the Newton direction and less of the gradient descent direction. So this is Newton's method, a fairly popular method, especially in low dimensional spaces. 

But for extremely high dimensional problems, it may not be the algorithm of choice. And for that, we have to switch over to using gradient descent, which we're going to basically focus on for the rest of this video. Gradient descent really is the workhorse of continuous optimization. Almost any algorithm that you see in the wild for doing continuous optimization is probably based on some sort of variant of gradient descent. The idea here at a high level is very simple. So the gradient vector of a function at a particular point basically just points you in the direction of the fastest increase of the function. So if you move in the direction of the gradient, you're moving uphill as quickly as you possibly can at a given point x. 

Typically, we want to minimize just by convention, so we're going to move in the negative gradient direction. So the direction here is obvious. We move in the minus gradient direction at our point x. And so the real question is what sort of step size to use. And the figure here in the bottom right kind of shows you what can happen as a consequence of using good versus bad step sizes. On the left hand side, this is the trajectory that you might take. If you take step sizes that are maybe a little bit too large, you kind of end up overshooting a little bit and zigzagging towards the optimal solution. 

And on the right hand side, you take much more conservative step sizes. And so you kind of converge along a slightly smoother trajectory, although you might end up taking more steps along the way. So there is maybe a trade off to consider here. Another note just about this picture, you'll see pictures like this a lot in optimization where we kind of picture a high dimensional function in terms of its contours or level sets. What we're looking at here is really a function of two variables. So it's a function of x and y that is kind of drawn in the third dimension, you know, out of the plane of the slide here. So the middle picture really illustrates that. 

So here I have a quadratic function like f of x is like x squared plus y squared. And that quadratic function, if you look at the contours of it just in the two dimensional plane, you kind of get these concentric circles. So the minimum of the function is right in the middle. And then these contours kind of tell you basically curves along which the function has a constant value. And based on those contours, you can kind of see what the gradient would look like. The gradient is actually going to be perpendicular to those contours. There are two qualitatively different types of functions that you can think about here in terms of the contours. 

So the contours could be very circular. That's actually a good case. That's the kind of the well-conditioned case where your condition number kappa is close to one. Remember, we defined what the condition number of a function is in our discussion of convexity. In the case that you have very elongated contours, that's the situation where your condition number is much larger than one. And optimization tends to get harder and harder in that situation. We'll end up seeing a lot of convergence bounds that depend on kappa. 

And so the more circular the contours of your function, the easier it is to move in the direction of an optimal solution. That should actually kind of make sense because the gradient points in a direction perpendicular to the contours. So if you start optimizing this very circular contoured function, the gradient is going to point right in towards the origin, and you're going to head in the right direction right from the outset. In this more elliptical contour situation, what you see with the zigzag case here is that the gradient doesn't point you directly towards the optimal solution in the middle of the graph, but instead kind of bounces you around a little bit as you go. And so optimization is definitely harder in the less well-conditioned case. One other thing to note, just in terms of the mathematical conditions for optimality, once you finally reach your minimum point, at that point, the gradient is going to be zero. The gradient being zero, you can think of that as kind of an optimality condition for optimization with gradient descent. 

That's when you stop. And if you have a convex function, that's good news because remember that with the convex function, a locally optimal solution is a globally optimal solution. So if you're optimizing a convex function and you reach a point where f of x, the gradient of f, is equal to zero, then you have indeed reached an optimal point globally on the function. Not just locally, but globally. In the non-convex case, things are a little bit more nuanced. So if you reach a point where the gradient equals zero, then there are a couple of situations that that could actually represent in the non-convex case. That could be a local minimum, it could actually be a local maximum, it could be some sort of like a saddle point. 

So there are a couple of different possibilities beyond what you would get with the convex case, which is only the first of these cases, the global minimum and local minimum case. So in the non-convex case, we call a point where the gradient is zero a stationary point. That's a point where gradient descent would get stuck because the gradient is zero. It doesn't suggest any direction of movement. So technically, if you're optimizing a non-convex function and you start out at one of these kind of points, you're unlucky enough to start out at one of these stationary points, you won't move. And so you might need to give gradient descent a little kick to get it moving. Any of these points, actually, if you move a tiny epsilon in any direction, you're going to get a meaningful gradient and be able to kind of escape and move towards a locally minimum solution. 

So the only sort of basin of attraction for gradient descent really is this first case right here. So even in a non-convex function, gradient descent will generally converge to a locally minimal solution like this first case right here. But it's kind of good to know that these other cases are strictly, you know, they're technically possibilities for the situation where the gradient is equal to zero. All right. The challenge with gradient descent, pretty much the only challenge, is step size calculation. How much do you move in the direction of the negative gradient? And there are two prominent options that people usually consider here. 

One, and maybe the easiest, is just use a fixed step size. So our update equation is that the next point we move to is our current point minus some step size alpha times the gradient. So how do we choose alpha? Just pick a fixed constant and use that throughout. And one benefit of this actually is that as you get closer and closer to the optimal solution, the magnitude of the gradient gets smaller and smaller because the gradient is getting closer to zero. And so actually the amount of movement actually does start to kind of shrink as you get closer to the optimal solution. This is actually one of the misconceptions that I originally had when I learned about optimization. 

I was worried that if you always used a fixed step size, then as you get closer and closer to the optimal solution, you need to take smaller and smaller steps. And that actually does happen kind of automatically with a fixed step size alpha because the gradient itself is getting smaller as you approach an optimal solution. So this is a very common situation. How do you pick the fixed step size in this situation? It turns out that using kind of the curvature of the function is the right idea. So when we talked about convexity, we introduced this parameter capital M, which is an upper bound on the second derivative of F. In the multidimensional case, it's a bound on the largest eigenvalue of the Hessian matrix of F. 

That number kind of characterizes the curvature of F. It basically, remember the second derivative is the rate of change of the derivative. And so if you're bounding the second derivative, you're bounding how much the derivative can change as you move around in space. And so if M is a big number, that means if you move around in space, the derivative is going all over the place. If M is a small number, the derivative isn't changing that much as you move around in space. And so if the derivative is moving around all over the place, you need to take smaller step sizes. You need to be less aggressive with gradient descent. 

If the gradient isn't changing that much over space, you can afford to take larger step sizes. And so setting the step size to one over M is kind of the right thing to do. It ends up mathematically. You can actually prove this. We won't go into the details of that. It turns out that you don't have to use one over M. You could use any constant over M, like 0.5 over M or whatnot. 

And you'll get the same theoretical guarantees. But kind of one over M is the kind of ideal to use here. The trouble is a lot of times you don't know M because who knows what the second, the Hessian matrix and its largest eigenvalue are going to be for a very, very complicated function F. And so that's the drawback of this calculation right here is kind of, it's based on a parameter that's kind of hard to know. But that is kind of mathematically the ideal setting of the parameter to use. Perhaps more common is to use an adaptive step size calculation. So once you have figured out the direction of movement, you can basically now reduce your problem to a one-dimensional problem because you're only considering movement along one direction. 

So you can kind of basically look at a slice of your function through that dimension. You're kind of projecting your function along that one dimension and considering now just the one-dimensional optimization problem of moving, how much do I move in that one dimension? So your one dimension is alpha, basically your step size. And now in that one dimension, you're kind of, you have a simpler picture where you need to figure out what the step size ought to be. So let me draw a picture at the whiteboard and show you maybe one common way that we approach this one-dimensional optimization problem to figure out the step size in the direction of the negative gradient. We follow something called the Armijo rule, typically in that sort of situation using what's called backtracking line search. So if I draw a picture of my function, so my function, I'm gonna start out at the origin. 

That's basically the point that I'm currently at, and I'm gonna translate things. So the current value of, I'm currently at a point X and the Y value kind of represents the value of my function and the X axis here indicates the direction that I'm moving in. And so this is really kind of the alpha axis because the larger I set alpha, the further I move along that axis. And my function I know is decreasing if I move in this direction, but it may not be decreasing forever. So basically this is a plot of my function. If I move in the direction of the negative gradient and I'm trying to pick the step size alpha. So what is kind of the slope of my function initially? 

This is kind of at my current guess X, I know that moving in the direction of the negative gradient is moving downhill, but what is the actual slope at which I'm moving? That actually is governed by the magnitude of the gradient at this point X. So the magnitude of the gradient tells you how steep the function is at point X. And so that tells you basically the slope of the function at point X. So the bigger the magnitude of the gradient, the more the function slopes downwards at this point. And so we would like to, when we move in this direction, we would like to be able to achieve a relatively good decrease in value over our next step if the gradient is indeed large. The gradient kind of tells you what you can expect to achieve in your next step. 

And we know that kind of right around our current guess, the slope of the function really is exactly determined by the magnitude of the gradient. The thing is, as you move farther and farther away, you may not be able to achieve kind of the amount of decrease suggested by the magnitude of the gradient. So what we do is we pick maybe some sort of a constant fraction of that slope. So I'm gonna draw a line through the origin, maybe at half of that slope, so a little bit less aggressive and I'll be content if I can end up anywhere underneath this kind of aspirational line, basically, that has a slope of just half of the slope that the gradient initially tells me. So if I can end up anywhere underneath that line, that means that I'm kind of achieving a relatively good decrease relative to the amount of distance that I've traveled. And so maybe now let's talk about backtracking line search. So what I'm gonna do is just pick a relatively aggressive value of alpha. 

I'm gonna try and move a large distance. And I realized that, oops, that large distance doesn't work well because I'm above my aspirational line. So what I'm gonna do is dial it back a little bit, maybe reduce it by some constant fraction. So I'm gonna guess maybe half of what I used to guess or two thirds of what I used to guess, keep kind of dialing it back according to some multiplicative reduction. And as I do that, I'm guaranteed eventually to end up at a point for which the function is below the line. So if I look at the value of the function at these different points at which I've evaluated, then aha, at this point, I finally am below the aspirational line. So actually I wouldn't have taken that last step. 

The point that I've highlighted would be the value of alpha that I would choose because the amount of decrease that I would get from using that value of alpha would be at least kind of proportional to what I could have achieved ideally using the slope indicated by the gradient. So I basically just try and use an aggressive step size because I wanna move as far as I can, as fast as I can. But if that doesn't give me a decent decrease, then I have to keep dialing it back until I reach a point where I have a decent decrease in value relative to what you could expect given the magnitude of the gradient. Okay, I think I have belabored that enough. So that is how you adaptively calculate the step size that you move with. Now let's talk about convergence rates that you get using either of those two step sizes. So I'm going to assume that I follow one of these two patterns. 

I either use a fixed step size with a step size of one over M or some constant over M where the constant is between zero and one, or that I use an adaptive step size that satisfies the Armijo rule. If that's the case, then you can actually mathematically prove some decent bounds on convergence for gradient descent. And we won't go into the math here. It's not terribly complicated, but I want to kind of just highlight the structure of the results instead. So if F is a strongly convex function, this is where the results are the best. That's the case where you have a strictly positive bound, lower bound on the second derivative or on the smallest eigenvalue of the Hessian matrix is strictly positive in the multidimensional case. In this situation, we get linear convergence. 

And what that means is basically that your error rate, the difference between the function at your current point at time T versus the function at the optimal point, that decreases exponentially. So basically your error is kind of a negative exponential function that drops off quite rapidly with each successive time step. And the condition number does show up in this bound. So probably the most common way that we articulate this convergence rate is that to decrease the error to an epsilon fraction of what it used to be, you need to take a number of steps, a number of iterations, which is proportional to kappa to the condition number of your function and to the logarithm of one over epsilon. So this is actually a pretty decent bound and it really does depend quite crucially on the condition number of your function. So this is, and remember, we've talked about this type of bound before. This is what we call linear convergence because you're adding digits of accuracy kind of at a linear rate to your answer. 

Now, if your function happens to be convex, but not strongly convex, so the lower bound on the second derivative basically is potentially zero, then remember in that case, your condition number isn't actually well-defined because it's kind of, it would involve division by zero. So your bounds here don't depend on the condition number and they are a bit weaker. Basically the convergence rate in this case, you can prove is kind of one over T where T is the index of time. And so you can kind of make this a little bit more explicit by kind of saying, you know, your error at any given time T is on the order of one over T times M, the curvature times D, which is the square distance that you are from the optimal point at the outside of your algorithm. But usually what people do is they just write this as order one over T to kind of illustrate the rate of convergence over time. And that is a weaker bound than the kind of exponential rate of convergence that you would get in the strongly convex case. Now you can improve things a little bit. 

So gradient descent can be sped up using a couple of different methods or we call accelerated gradient descent. And the math here underneath the hood gets quite complicated. So if you want to really test your mathematical skills, try understanding the proofs of these methods because they can be quite intricate. Some of these methods, even the trajectories they take might even increase the function value temporarily along the way. So there is a little bit of complexity underneath the hood in analyzing these things. But again, the high level idea is not too bad. What we're gonna do is basically add an extra term to our update that kind of adds a momentum to our gradient descent trajectory. 

If you go back and look at maybe this picture right here where you zigzag back and forth, the trouble is that you're kind of overshooting a little bit too much. And so if you add a little bit of momentum that kind of smooths out the path that you end up taking. So the update rule here depends on the exact sort of accelerated gradient descent method that you're gonna use. One that's maybe easy to understand is called the heavy ball method, which kind of imagine that the ball, the guests that you have has some mass to it. So it doesn't want to just change direction on a whim. It kind of keeps going a little bit in the last direction that it was moving. So your update step here is, you know, yes, you still move in the direction of the negative gradient like you have before, but you also add to that some fraction of the vector that describes the last step you took. 

So whatever direction you took in the last step, you kind of add that in a little bit into the next step. So you add a little bit of momentum. So the ball rolls downhill, but it also has some momentum to it, some mass to it. The heavy ball method can work well in the right circumstances, but knowing the right parameters here for alpha and beta can be a challenge. And there are actually a few corner cases where it actually doesn't do well. It can actually fail to converge in some situations, even on convex functions. And so probably the recommended approach for accelerated gradient descent is to use a method called Nesterov's method, which is similar in terms of the update equation. 

So the updated value of X for the next guest, you also add the same momentum term, but you apply a step size that depends not just on the negative gradient, but kind of the negative gradient plus the momentum. So that's just kind of an interesting small tweak on the step that you take here. So it turns out that you can get very similar bounds with Nesterov's method to a well-tuned implementation of the heavy ball method, but Nesterov's method is maybe a little bit safer in terms of possible corner cases that can really throw things off. If you set the parameters correctly, and that's kind of the trick with these methods is knowing how to set the parameters. There's a lot that can be said about how to tune the parameters properly, and I won't go into the details of that. That's kind of the tricky part, but you can look that up if you need to to find good ways to set the parameters. So basically the bottom line is that accelerated gradient descent will improve the convergence bound in the strongly convex case from kappa times log one over epsilon to square root of kappa times log one over epsilon. 

And so you've taken the square root of the condition number of your function. That can be a really big deal if your function is poorly conditioned. If your condition number is a million, that reduces it to a thousand. So that could be a substantial increase in efficiency. And if your function is only convex, remember the result there was a little bit weaker. We had a bound of one over T for convergence in that case, that turns into one over T squared. So that is also a substantial improvement in efficiency. 

So if possible, it's nice to be able to try and use these sorts of acceleration techniques with gradient descent. But with that, you know, you get a little bit of additional complexity in how you tune the parameters and how you can analyze these methods. Very common, especially in areas like machine learning and data science is a variation. called stochastic gradient descent. So the idea here is that very, very often when you're trying to fit a model to data, your objective function is kind of an error or loss function that is basically a sum over a whole bunch of terms, each one of which relates to one data point. So if I'm trying to solve a linear regression problem, you know, fit a line to a bunch of data points, my error function is of this form. I basically sum up the error on each individual data point. How well did my perspective line fit every single one of my data points? 

So I'd have a term in this summation for every data point that tells me the per data point loss or error in my model fit. It turns out that taking the gradient of a function, that's a linear operation. So gradient distributes over a sum. So it's very easy to find the gradient of your function at any point X by just taking the sums of the gradients of these kind of per data point loss functions. The trouble is if your data set involves millions and millions of data points, the summation is over millions and millions of terms. And so it can be expensive to evaluate for every single step of gradient descent. So what we do with stochastic gradient descent is we just pick one of these terms at random and we move in that direction, the direction suggested by that gradient. 

So in terms of model fitting, imagine that you're trying to fit a line to a bunch of data points. You just pick one data point and adjust your line as suggested by that one data point. And then you pick another data point at random and adjust your line as suggested by that data point. So you don't kind of move the line according to the global consensus of what all the data points are telling you, but you just kind of move a little bit in a direction suggested by one of the data points in your input set. And so that's stochastic gradient descent. This is on the very far end of the spectrum of taking very lightweight steps, but many, many, many of them. So it's very cheap to compute each update here, but the updates are usually a very low quality because you're gonna be moving all over the place. 

The thing is, even though you're kind of probably seeing a lot more noise in the direction of movement, the expected direction of movement is still that of the actual full gradient. Because if you take the expectation over the data point of the direction of movement, remember the expectation is just an average of all of the different possibilities that your random variable can take. And that is the full gradient because the gradient is just the sum of these little gradients of the per element gradients. So the average, the expected direction of movement is the full gradient. And that's kind of why stochastic gradient descent still is working. It's, you know, you're taking noisier steps, but on average, they're still pointing you in the right direction. In fact, this approach still works even if your objective function doesn't look like this, but if you just have a mechanism for basically estimating the gradient at any point. 

So suppose you only have a way of drawing kind of a noisy estimates of the gradient at any point X, as long as the expectation of those estimates is reasonable, is in the direction of the actual gradient, then the same results for stochastic gradient descent work. They actually don't depend on the number of data points in this sum, which is quite remarkable. So the bottom line here in terms of convergence, it is a little bit weaker, the bounds you get for convergence with stochastic gradient descent versus normal gradient descent. It turns out that if you tune things the right way, and I'm skipping over a lot of fine print again, in terms of parameter tuning, it turns out basically the best results you get are one over T and one over square root of T, where the strongly convex and the just plain old convex cases. So these are quite a bit weaker than with regular gradient descent, but it is nice that we can actually get bounds that have maybe a similar form as with regular gradient descent at least. It turns out that you can actually get bounds of the exact same form, one over T and one over square root of T, if you use what's called sub-gradient gradient descent or sub-gradient stochastic gradient descent. Let me mention this briefly because there are some functions that may not be differentiable everywhere, there may not actually be a gradient everywhere. 

You can have maybe some points where there's kind of a kink in the function that makes it not smoothly differentiable, but you can still kind of apply in general techniques from gradient descent if we use what's called a sub-gradient. So let me just draw a picture to illustrate what I mean in that case. So if I have a function, say that decreases down to a point, and then I have kind of a kink in the function. So at that lower kind of apex of the function, it's not differentiable because you need to have kind of a smooth function to have it be differentiable. So there isn't really a gradient at this lower point in the function. So what do you do instead? Well, remember that the gradient basically at any point kind of defines a lower bound of your function. 

It's kind of a supporting hyperplane at that location. It's tangent to your function. And it turns out there are several supporting hyperplanes that would work at one of these kinks in your function. So I could draw a hyperplane like this. I could draw a hyperplane like this. I could draw a hyperplane like this. Any of these are valid sub-gradients basically. 

So a sub-gradient basically corresponds to a vector that would give you a lower bound on your function. And if your function is not differentiable at a point, there could be several choices for sub-gradients. If it is differentiable, there's only one choice, the gradient. So if you use a gradient descent, but instead of using the gradient, you use the sub-gradient or a sub-gradient in situations where the gradient is not available, then that also tends to work. But the convergence bounds are a bit weaker and they are of the same form actually that you get here with stochastic gradient descent. And you can actually combine the two. You can do sub-gradient stochastic gradient descent as well. 

So lots and lots of different variations are possible on the idea of gradient descent. So if we maybe move on, let's just talk about a couple of remaining ideas in this space. And we're gonna see a couple more later on in future videos as well. So when we talked about iterative refinement in the module on discrete optimization, we talked about the idea of alternating iterative refinement where you kind of freeze part of your solution and update the other part and then alternate back and forth. Applied to gradient descent, that gives you something sometimes called coordinate descent or block coordinate descent if you optimize several coordinates at once. So here the idea is you basically pick a dimension and move in that one dimension and then pick another dimension and move in that other dimension and alternate through the dimensions in maybe a cyclic way or maybe you pick a dimension randomly. Sometimes you might pick a small subset of dimensions to optimize over, fixing your location in the other dimensions. 

And you can actually use the gradient to suggest what dimensions to use. So if you look at the gradient vector, you could look at which dimensions are of large magnitude in the gradient vector to kind of suggest which dimensions along which you can move where that'll make the most difference to your objective value. So that's one way that you can maybe simplify your problem and again, take a larger number of maybe lower quality steps but less expensive steps to compute. Another idea that we'll see when we talk about solving linear systems is a method called the conjugate gradient method where again, you're kind of optimizing along one dimension and then you pick another direction to optimize over but you try and pick a direction that's kind of perpendicular to the first direction because you've already found a good solution along the first direction. Now you wanna find a good solution in directions perpendicular to that first direction and you proceed by finding directions of optimization that are kind of perpendicular to the directions that you've used before. So we'll talk a little bit more about that when we talk about solving a linear system. It's a very popular method for solving linear systems in kind of an iterative fashion. 

We also talked about the distinction between radiant descent, which involves a small, I guess a large number of lightweight steps versus Newton's method which involves a small number of heavier steps. There are a couple of methods that kind of fit into the space between these two types of methods. Sometimes they're called quasi Newton methods that maybe don't involve an update that's quite as expensive as a full Newton update where you don't quite minimize a full quadratic but you can do something that kind of approximates that using information from the gradient. And there are a couple of methods that fit into this space. I won't go into detail but you might run across a few of them if you're looking up different popular methods for optimization. And then the last thing that we will talk about a little later on in the module as well is what happens if you add a little bit of randomness to the mix, you know, gradient descent, there's no randomness involved with it. You just follow the negative gradient. 

But what if you follow the negative gradient and maybe add a little bit of additional noise to the step that you take? It turns out something quite nice can happen there. You can actually end up sampling from functions if you use an approach similar to that. This also kind of maybe brings back memories of like simulated annealing where you are kind of interpolating between moving around completely at random and moving around in a meaningful direction of descent that optimizes your function. And so you can do something similar with gradient descent which we'll talk about later on. So there are lots of interesting ideas in this space, just variations on kind of our core topics of gradient descent, Newton's method, and in general just iterative refinement for continuous optimization. So thanks for watching. 

I hope you enjoyed this video. If you did, please give it a thumbs up. And if you have any questions, please leave them in the comments below. And I'll see you in the next video. Bye.