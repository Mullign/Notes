![[Solving Linear Systems.pdf]]Problems involving linear systems are important. They are likely involving a huge chunk of the world's computing power. And since many of these types of problems involve algorithmic ideas related to those that we've been talking about for continuous optimization, we've opted to include in this module a video where we talk about, at least at a high level, some of the main ideas, algorithmically, that are used for manipulating linear systems. It will definitely be to your advantage, for this video at least, if you have had some prior background with linear algebra, although to make things as understandable as possible to a broad audience, we have tried as hard as possible to kind of keep things here at a high level and not to get too far into the weeds mathematically. So the goal here is to maybe just survey some of the major techniques out there so that you have a sense of what you need to look up in further detail if you need to use one of these particular techniques. Just in terms of motivation, linear systems really are everywhere in applications ranging from data science to engineering and beyond. So in data science, for example, if you're trying to fit a model to your data, very often we choose a loss function or an error function to minimize that is quadratic. 

For example, with linear regression, you're trying to fit a line to your data points to minimize the sum of the squared errors on those data points. Why do we often use quadratic error functions? Well, if you want to minimize a quadratic, you take its derivative or its gradient, set it equal to zero, and that gives you a linear system. So minimizing a quadratic involves solving a linear system. We saw this again with Newton's method. If you recall, we locally approximate a function as a quadratic and minimize that quadratic, which involves solving a linear system to figure out the direction of movement as we're optimizing a nonlinear function. In engineering, we often have very, very large scale finite element models trying to figure out how a large mechanical system might behave. 

This picture right here actually is from some joint research with my wife, who's a bioengineer, where we were trying to build a mechanical model of a cell based on microscopy images of that cell. So we could predict if you pushed on the cell with, say, an atomic force microscope, how it would respond mechanically. This involves solving a very large system of equations. You discretize the cell up into a bunch of tiny little compartments, write down a system of equations for how those compartments relate to each other mechanically. And solving such a system typically involves basically lots and lots of iterations where you're solving very large and very sparse linear systems. So linear systems, they're really everywhere in science and engineering and artificial intelligence even. So it's very useful to have a sense of types of algorithms that are in play when you're dealing with linear systems. 

The basic problem here is basically solving an n by n linear system. So I have n variables and n equations, so a square linear system, and I would like to find its solution. We typically write this as AX equals B in matrix vector notation. And so we're going to spend a little bit of time in this video talking about how to solve a linear system. There are many different approaches that we can use for doing this. But before we get into that, let's maybe talk about the cases where it's not actually a square system, but a rectangular system. So what if you have a tall, skinny system in terms of the underlying matrix A? 

So that would be a system with many more equations than variables. So here your system is over-constrained, and it's unlikely that you'll be able to find a setting of your variables that satisfies all of those equations. With n variables and n equations, you typically have just one unique solution. But if you have more equations than variables, you likely don't have any solutions to the system. This is actually quite common. What do we do in that situation? Well, we try and find the best solution we can, which minimizes the error in the system. 

And again, we choose a quadratic error function. So if we're trying to solve AX equals B, the error inherent in our solution is AX minus B. It's the length or the norm of that vector. And we typically look at the squared length of that vector to make it a quadratic objective function. To minimize that quadratic, we take its derivative, its gradient, set it equal to zero. That gives us a linear system, which ends up actually being a square system. So it's A transpose AX equals A transpose B. And so solving that system will give us the least squares solution to our original system. 

And so now we're back in square linear system territory, and we can use the techniques that we're going to talk about for solving square linear systems to address this. The only numerical challenge or issue here is that this system here with A transpose AX equals A transpose B, it's kind of more poorly conditioned than the original system. The condition number is kind of the square of what it was for the original system, which isn't great numerically. But we'll kind of talk a little bit more about those types of issues a little bit later on. What if we have a lot of variables and not a lot of equations? So in that case, we have a lot of degrees of freedom in our system, not enough to kind of lock in a unique solution. So we have an entire vector space of solutions typically in this setting. 

What do we do? We want to find maybe the best solution. And so usually here, we end up with a minimization problem where you add an objective function to the mix, and you'd like to minimize or maximize that objective subject to AX equals B. So there are many solutions now of AX equals B, and we just use that as the set of constraints in our optimization problem. So these are types of situations that can arise if you're dealing with a linear system that may not necessarily be square. On the same note, if you are trying to optimize and you have a set of linear constraints, this often brings you into linear programming territory. So one of the most simplistic models for optimization is linear programming. 

The word programming here, remember, doesn't mean like computer programming. It means more kind of optimization, formulating and planning an optimization problem. So a linear program is a mathematical optimization problem where you have a linear objective function and a bunch of linear equality and inequality constraints. So that's what you would get in this previous situation if your objective function F was a linear function. So if you study optimization in school, usually the very first class you take is going to focus on linear programming, because it's one of the kind of major classes of optimization problems that we actually know a lot about. We have effective algorithms for solving linear programs. They can be solved in polynomial time, which is nice, using a variety of different algorithmic approaches. 

But linear programs, there's just a lot you can say about them, and I probably shouldn't say too much here, because there's a lot of other topics we need to talk about. But they kind of lie at the boundary between discrete and continuous optimization. They are continuous optimization problems, because you're optimizing over a continuous set of possibilities. But because of the nature of this problem, you're kind of trying to maximize a linear objective function, which means you're trying to move as far as possible in one direction in space, subject to these linear inequality constraints, which constrain you to be in kind of a convex polyhedral set of feasible solutions. Because of the nature of this problem, you always end up with an optimal solution which is at a corner point of this polyhedron of feasible solutions. So there's only kind of a discrete set of possible options for where the optimal solution could lie. That's why it's also similar to discrete optimization in some ways. 

And there are some algorithms for linear programming, like the simplex algorithm, that move towards an optimal solution by iterative refinement, by kind of pivoting from one corner point to another corner point, until you eventually reach the optimal corner point of your feasible region. There are many different algorithms out there for solving linear programs, and lots of really good software packages for doing this. So I'd recommend, if you are faced with a linear program, use a well-established software package for solving it, instead of reinventing the wheel and trying to implement from scratch an existing algorithm. But there are lots of nice algorithms here. Linear programming also is a situation where there's a really nice setup with duality that you can use. All linear programs have dual linear programs, and that's used in a lot of different algorithmic approaches for solving different types of problems. So linear programming is a very big space that you end up studying when you look at optimization problems. 

So definitely worth some time to investigate if this is an area of possible interest. Let's move back now to the problem of solving a square linear system. So I have a linear system with n variables and n equations. And I would like to find the exact solution of that problem. If you take a linear algebra class, then the go-to technique that you will typically be taught here is Gaussian elimination, where you basically solve the first equation for the first variable, and then you plug that into the remaining equations to kind of eliminate that variable from those equations. It reduces your n by n system down to a system with n minus 1 variables and n minus 1 equations. And then you keep repeating this process. 

And it takes you quadratic time to actually eliminate every variable from the mix. So it actually takes n cubed total time to work your way down to a system with just one variable and one equation that you can then just outright solve. And then you kind of plug the answer in reverse order to kind of recover the solutions of the other variables. It's a relatively straightforward technique. The trouble is it's just very slow. n cubed time is not very fast, especially for the enormous linear systems that we do typically encounter in practice. So while this may be the method of choice in a math class on linear algebra, in practice we often need to use different types of techniques for solving linear systems just because n cubed is too slow. So let's talk about kind of solving linear systems and related problems and other approaches we might end up needing to use if Gaussian elimination is not fast enough. 

So first of all, there are several very important problems in the space of kind of solving linear systems that are, they all end up being equivalent in terms of their algorithmic complexity. So there is solving a linear system. That's one of them. There is matrix multiplication. That's another one. And there is inverting a matrix. Inverting a matrix gives you another way of solving a linear system, because if you have a system AX equals B, you can solve it by taking the inverse of A and setting X equal to A inverse times B. So it turns out all three of these problems can be shown to be essentially equivalent to each other. 

If you can solve one of these problems quickly, you can solve all three of them quickly. They basically have the same running times. So whatever the running time fate of one of these problems is, so is the fate of the other problems. And so a lot of times we study maybe matrix multiplication, trying to make a faster matrix multiplication algorithm. There's a good reason for doing that, because if you can speed up matrix multiplication, you can also speed up solving linear systems and inverting matrices, which are incredibly important problems. So lots of very important problems are essentially equivalent in terms of their algorithmic complexity. You can show this with reductions, where you can reduce one problem to the other. 

And I won't go into the details of how that is done, but it's nice to know that these problems are all related to each other. So a fast algorithm for one would mean a fast algorithm for all of them. Unfortunately, we don't have super fast algorithms for any of them. The best we essentially know is, well, Gaussian elimination is our starting point, n cubed time. And then we have some kind of divide and conquer and related methods that we talked about back in divide and conquer, Strassen's algorithm and the like, that are kind of n to the two point something, but that's still not terribly efficient. So we do have, you know, a decent number of tools in our algorithmic arsenal for exactly solving linear systems and these related problems. But in practice, we end up seeing linear systems that are incredibly large. 

The thing with those incredibly large systems is that they're often very sparse. They have a lot of zeros in their A matrix. And so it turns out that there are a lot of techniques out there that are more effective for solving large, sparse systems in more of an iterative fashion to get an approximate answer. And that's the direction that we're going to be heading in now. We're going to leave behind the world of kind of finding an exact solution with techniques related to maybe Gaussian elimination. And we're going to focus more on iterative methods for these large, sparse systems. If you do have a sparse system, let's talk about that for just a minute. 

We often associate with a large, so a large sparse system to start with is a system AX equals B, where the A matrix has mostly zeros in it. There aren't that many non-zero entries. That's what makes it sparse. The word sparse here means the same thing as when I used the word sparse in the context of our study of graph algorithms. And in fact, we often associate with a matrix like this, we associate a graph with it by basically considering the weight on an edge from I to J to be the entry AIJ. So if there's a zero in the matrix, there's no edge. There's only an edge for the entries in the matrix that are the non-zeros. 

And those entries tell us the weights of those respective edges. And so if you make a graph out of your matrix, then it means really the same thing for the graph to be sparse as it does for the matrix to be sparse. And so that's a good way to think about sparsity for matrices. How do we represent sparse matrices? Well, similar to sparse graphs, we basically just keep a list or maybe a hash table of just the interesting entries, the non-zero entries in the matrix. And that will use considerably less space than if we stored this as a two-dimensional array, as we would use for a dense matrix. So instead of N squared space, we'll only use order of M space. where m is the number of non-zeros, that's the notation that we typically use to denote the number of non-zero entries in our matrix, just like we used m to denote the number of edges in a sparse graph. 

So we can do a lot of things very quickly on sparse matrices. Maybe the most prominent operation here is multiplying a vector by a sparse matrix. We can do that in only linear time in the number of non-zero entries in the matrix, so order of n plus m time. A lot of times you'll see that written as just order m, because we typically assume m is kind of the dominant factor here. So the running time for a matrix vector multiplication really only depends on how many non-zeros you have in your matrix, because you're just taking the vector that you're multiplying and kind of dot-producting it with all the rows of your matrix, and you only have to consider the contributions that arise because of the non-zero entries in your matrix. You can just loop over all of those entries and accumulate what would be the answer in your result vector very easily. We actually will be using matrix vector multiplication as a key part of many of the iterative methods that we're about to talk about. 

Many of them are based on just repeated instances of matrix vector multiplication. So if you can do that quickly, as you can with a sparse matrix, then that's really good news. All right, we now have a couple of methods that I wanna talk about for doing kind of basically iterative methods for solving large sparse linear systems. And so let's kind of try and introduce these at a high level and maybe go into a little bit of mathematical depth, but not too much. All right, so the first one of these is maybe the simplest of all of them. It's called Richardson iteration. And the mathematical motivation for this is basically that we're kind of trying to come up with an approximation of A inverse by almost writing it out as a Taylor series expansion. 

So remember that you wanna solve a linear system, AX equals B, the solution of that, you could get exactly by inverting A, by setting X equals to A inverse B. You can actually approximate A inverse with Taylor series expansion. Let me maybe go to the whiteboard just to kind of motivate that because that's actually relatively straightforward. If I have, and this actually lets us review geometric series as well. If I have a geometric series, one plus A plus A squared plus A cubed and so on, what does that add up to? Remember that a long time ago, we introduced a kind of a shifting technique for adding up geometric series. And so the answer here ends up being one over one minus A, which you could also write as one minus A to the negative one power. 

It turns out that the exact same thing happens if you, instead of having just a number A, if you have a matrix. So if I have one plus A, capital A, plus capital A squared and so on, a geometric series involving matrices, then that also adds up via the shifting technique to identity matrix minus A inverse. Identity matrix is just the matrix with ones down the diagonal and zeros elsewhere. And so you can actually now basically replace A with I minus A, and you can get this approximation that the inverse of A is approximately going to be identity plus identity minus A plus identity minus A squared and so on. So you can make a polynomial basically that approximates the inverse of A. Based on trying to approximate that, Richardson iteration, basically in every iteration, takes a vector X, which is your current guess for the solution. And it adds to that kind of the current error in that solution. 

So you're trying to solve AX equals B. So the error involved in that is B minus AX. That's what you actually add to your guess X in each iteration of Richardson iteration. You can also rephrase that as setting the next iterate to B plus identity minus A times your former guess for X. And that kind of highlights maybe the, how that relates to this formula here. After K iterations of Richardson iteration, you basically built up an approximation of A inverse involving K terms. So if you look at the structure of this formula, every iteration here basically just involves a matrix vector product. 

You have the matrix identity minus A, and you're multiplying your current guess X by it. That gives you a vector. You add that to B, and that gives you your next guess, X prime. And so you just do a bunch of matrix vector products. Each one takes you order N plus M time or order M time. And how many of those iterations do you need to do? So you can actually get a pretty good bound on that for the special case where A is a positive definite matrix. 

This is a very common special case in practice it ends up. And so a lot of the methods that we analyze, we look at them in this special case where A is positive definite. We also do this because this ends up being the case where we can actually do an effective analysis in many cases. There's less you can say if A isn't nicely structured like this. So I won't define positive definiteness. So that's something to look up or something hopefully that you have seen if you've taken a linear algebra class. It's a very common condition that we look at for matrices. 

So if A is a positive definite matrix, then you get an epsilon reduction in error after on the order of condition number times log of one over epsilon steps. The form of this should look very familiar. This is actually the convergence guarantee that we got with gradient descent earlier in continuous optimization. And there's a good reason for this because there's actually a way that we can regard Richardson iteration as a form of gradient descent, which we'll talk about in just a second. So the value of kappa, the condition number here, it's defined in a very similar way to condition numbers that we have seen in the past. It's a ratio of the largest to the smallest eigenvalue of our matrix A. Just as in continuous optimization, remember that our condition number was a ratio of the largest to the smallest eigenvalue of a Hessian matrix of our objective function. 

So I won't go into too much detail there, but we will see in just a minute kind of the connection between this and gradient descent. But if you want to reduce your error by down to an epsilon fraction of what it used to be, it takes this many steps, each of which takes you only linear time. So relatively efficient. And your error here conveniently can be of many different forms. You can have an error that basically measures how far X is away from the optimal solution, or you can have an error term that measures kind of the error in the entire system, you know, B minus AX. So how far is B away from A times X? Or you can look at the squares of those terms. 

It turns out that all of those are reduced to an epsilon fraction of their initial values after this many steps. So let's move on and kind of look at a couple of other prominent methods for solving linear systems in an iterative fashion. A very popular and commonly cited method is Gauss-Seidel relaxation, which is also called a coordinate descent method. We actually talked about this one briefly when we covered alternating iterative refinement, because this type of method basically fixes some of the coordinates, some of the values in your solution, and then updates the others. So it kind of updates parts of your solution in a cyclic fashion, one part at a time. So this happens in a very straightforward fashion. If you want to solve a linear system, you start with some guess X, which is probably not the right answer, and then you just update its coordinates in a cyclic fashion. 

And so you update X1, the first coordinate, by basically solving the first equation. You fix all the other parts of X, and you just update X1 using the first equation. And then you look at the second equation, fix all the variables except X2, and use the second equation to update X2, and so on. You just update the variables in a cyclic fashion using the different equations. There's a nice picture behind this where you're basically kind of fixing the coordinates one at a time. So in two dimensions, finding the solution of a linear system is like finding the intersection point of two lines. And so in the first iteration, we basically take our guess, which is a point that is definitely not the intersection, and we snap it to the first line. 

You update the first coordinate so that it satisfies the first equation. So now you're on line one. And then you update the second coordinate, that's the Y coordinate, so that now you're on line two, so that it satisfies equation two. And you keep doing that. Update the first coordinate so it satisfies line one, and so on. And as you can see, that will eventually spiral in and converge towards the correct answer, the intersection point of those lines. If you're not careful, of course, and you do the updates in the wrong order, or if your matrix doesn't have the right structure, you can actually end up spiraling outwards instead of inwards, and that's bad news. 

So you do have to be careful that the conditions in your system are right to support convergence. And one condition that does guarantee convergence is if your matrix is positive definite again. So in general, it's good news if your matrix is positive definite. If not, you have to be a lot more careful using these types of methods. If you make one pass over all of the coordinates, one update pass that updates all the coordinates, that takes you linear time, order of N plus M time. So this is a very efficient method because every pass over all the coordinates takes linear time, and hopefully you don't have to do too many of those passes in order to converge to a reasonable solution. There's another sort of, almost kind of dual to the Gauss-Seidel relaxation, another method that we can also use to iteratively solve linear systems. 

And it's actually based on a somewhat more general principle. So suppose you would like to find a point that lies in the intersection of a number of convex sets. So if I have a couple of convex sets like this rectangle and this kind of oblong set right here, the dark shaded region is their intersection. It's always the case that if you intersect a bunch of convex sets, you get a convex set as their intersection. How do you find a point that lies in that intersection? It turns out one easy way to do that is by alternating in a cyclic fashion where you just project yourself perpendicularly onto the different sets. So I project myself onto set one and then onto set two and then onto set one. 

That's the trajectory taken by this point here, kind of a zigzag pattern. You can see that it converges towards the intersection of all of those sets. You can actually phrase solving a linear system in the same framework because every single one of your lines or hyperplanes in the system you're solving is in and of itself a little convex set. And you're looking for the intersection of all of those things, which is basically one point. So here I've shown you an example in two dimensions where you're intersecting two lines, but this works in higher dimensional spaces as well. All you do is you just project onto the different linear equations, all those constraints in your system one by one. So I project myself onto the first line, onto the second line, onto the first line, back and forth. 

And you keep projecting yourself in a perpendicular manner, and that will eventually converge to the intersection of those lines. This works quite well, unless you're in kind of a poorly conditioned situation where the lines are nearly parallel, in which case you can end up bouncing back and forth a lot and doing a whole bunch of iterations. So this is another relatively straightforward method that goes by the name of Katchmar's method that you do see sometimes for solving linear systems. And it's maybe similar to kind of in spirit to Gauss-Seidel relaxation, just instead of updating coordinates one at a time, you're taking your point and kind of projecting it onto constraints one at a time. But I won't go into too much detail here. So you have to kind of, usually you either cycle through constraints or you pick a constraint randomly to project onto in each step. Even with Gauss-Seidel relaxation, sometimes you don't necessarily cycle through constraints, but you might pick a, sorry, cycle through variables, but you might pick a random variable to update in each step. 

So there are variations on these different methods. All right, so now let's look at actually some connections with optimization. So, so far we've talked about a lot of methods, but none of them have kind of looked very similar to what we've talked about so far in this module with continuous optimization. It turns out that solving a linear system, at least a positive definite linear system, is directly connected with continuous optimization. Because if you want to solve a system AX equals B, where A is positive definite, it turns out that that is exactly the same thing as minimizing a quadratic. If you're minimizing the quadratic F of X is one half of X transpose AX minus B transpose X, that's kind of a quadratic form involving X, then to minimize that function, what would you do? You would take its gradient and set it equal to zero. 

The gradient of this function is AX minus B. Setting that equal to zero gives you AX equals B, which is your linear system. So a solution in solving this linear system that basically gives you the optimality conditions for minimizing this quadratic function. Again, minimizing a quadratic function involves solving a linear system. So we're just exploiting that connection here kind of in reverse. If you want to solve a linear system, you can rephrase that equivalently, at least in the positive definite case, as solving a quadratic or minimizing a quadratic function. And so now we can actually use all of the tools at our disposal for minimizing quadratics for continuous optimization. 

We can use those for solving a linear system. And so the first idea that maybe comes to mind is what do we do just gradient descent on this quadratic function? That basically is equivalent to Richardson iteration that we just talked about. And you'll remember that the guarantee we got for convergence with Richardson iteration, you know, the condition number times log of one over epsilon that basically was the exact same guarantee that we got with gradient descent in the strongly convex case. And that's not an accident because that is exactly what's happening here. It turns out you can show that the two are more or less equivalent. And so Richardson iteration basically is gradient descent when you kind of look at it through the lens of this equivalence of solving a linear system being equivalent to minimizing a quadratic. 

It's also nice to know that the condition number here. kappa really means the same thing that it did when we talked about gradient descent. Remember back then it was a ratio of two eigenvalues of a of a Hessian matrix of second partial derivatives. Here it's a ratio of eigenvalues of your matrix A, but that actually means the same thing in this context. So the condition number here ends up meaning the same thing as well. There's one other method that is very popular for solving linear systems that is based on this equivalence with optimization of a quadratic, and that's called the conjugate gradient method. I'll kind of describe it here at a high level because the math gets not super complicated, but it's just a lot of detail that I don't think we have the time to really go through it properly. So I'll kind of talk about it at a high level here, and it's relatively easy to look it up and code it up if you need to use it. It's nice because it's equivalent basically to these accelerated gradient descent methods that we talked about earlier in the module. And remember that the convergence guarantees you get from accelerated gradient descent are the same as with gradient descent, just involving the square root of the condition number. That can be a really big deal if your condition number is quite large. And so the the running time we're going to get with the conjugate gradient method is basically this many steps, you know, square root of condition number times log one over epsilon, where each step basically involves a matrix vector type product. So it's going to be a linear time for each step. So how do these steps work? It's basically you're moving in the direction of the gradient to start with. So you have kind of a one-dimensional, you know, line search problem in the first iteration. So kind of minimize taking a step in some optimal fashion in the direction of the gradient. In the next iteration, you have to pick a new direction to search along. And the key with the conjugate gradient method is that you want that next direction to be kind of orthogonal in a way to the first direction. It may not be exactly orthogonal. The condition for orthogonality involves the matrix A in an interesting way. 

But basically you're going to be searching in the second iteration in a direction that is kind of, well, I'll keep using the word orthogonal to the first direction, but it's defined in kind of a way that involves the matrix A. So it's not exactly perpendicular, but kind of perpendicular with respect to the matrix A, if that makes sense. Because you've already optimized in the first direction as well as you could have, now we're optimizing in the second direction that kind of is orthogonal to the first. And then the next direction you choose is going to be one that's kind of orthogonal with respect to A compared to those first two directions and so on. And so after, in theory, after you've done this n times, where n is the dimensionality of your space, you should have found the exact optimal solution. But the trouble is this process where you have kind of a Gram-Schmidt orthogonalization process, this is known to be fairly unstable. And so you actually have round off errors that accumulate over time. And so in practice, you're not going to find the exact optimal solution after n steps, but you kind of will expect to just converge towards an optimal solution over a number of steps. But very popular method for solving a linear system and also just for continuous optimization in general, if you're just trying to minimize a quadratic function, but exploiting this connection between solving a linear system and minimizing a quadratic function.