![[Hash Tables.pdf]]
We finally reached the very last data structure that we'll be talking about in this course, and it's an important one. If you recall a while back, I made a list of the top three data structures that I'd like everyone to remember has the end of the course. The Number 1 spot on that list really has to be the binary search tree. Also on the list was the binary heap because of its prevalence in a wide range of applications. But Number 2, probably, and maybe even contending for the Number 1 spot would have to be the hash table. Hash tables are incredibly powerful and prominent data structures. You definitely want to know how they work. 

What do hash tables do? They typically represent sets or maps, and so the core operations would be insert, remove, and find. We've already seen sets and maps being implemented using binary search trees and their relatives. We typically the performance guarantees are logarithmic. That's because binary search trees and their relatives are comparison based data structures. Here, we're going to be operating in the RAM model of computation, so we're going to be assuming the things we store in our data structure are integers. We're storing integer keys in our data structure. 

Because of that, we will be able to move past logarithmic times and get things down to Order 1 to constant running times for the core operations of insert, remove, and find. There is a bit of find print about whether it's going to be constant amortized or constant expected or just constant anticipated. A lot of times we build hash tables using hash functions that are heuristic and they tend to work well in practice, even though they don't have a theoretical guarantee attached to them. We anticipate things taking constant time, even though mathematically we can't prove that that would be the case. We often use hash tables as maps as well as sets, and we often do this in a way that employs associative array notation. In the programming language, I might say that the grade of Brian equals 100. What that's actually doing underneath the hood is it's looking up an element in a hash table with a key of Brian and setting the associated value of that record to 100. 

You can also emulate very large and very sparsely filled arrays with that. We've seen this done before in some of our past coding discussions. Here if I run this line of code, using a hash table to emulate this array, then I don't actually need a giant array, I just need a hash table that uses an amount of memory proportional to the number of records really being stored in the array. This is looking up a record in the hash table with a key of 99999 and assigning its associated value to three. We really don't lose anything on performance here, because remember with an array, this would take constant time, and with a hash table, we anticipate every access, also taking constant time as well. Hash tables give us incredibly powerful ways of implementing or emulating these types of sparsely filled, large virtual arrays. As a starting point, we can build something that basically gives us constant performance for insert, remove, and find, and works perfectly well for very small integer keys. 

Remember our keys that we're storing in the structure are now integers because we're in the RAM model of computation. Suppose that we use as our data structure, just a simple array of Booleans. They're just true or false. To indicate the presence of an item in the data structure, we just turn that corresponding element to true. If you want to insert five into the data structure, you set A of five to true. If you want to remove five from the data structure, you set A of five to false. This gives us a very trivial constant time implementation for insert, remove, and find. 

The only drawback, and it's a pretty severe drawback is space usage. This works really well if you're dealing with very small integer keys. But if you're dealing with very large integer keys, then you need an incredibly large underlying array to be able to build this data structure, and it would probably take you a very long time to initialize that array as well. To remedy that situation, we use a much smaller underlying array to store our values, and we use what's called a hash function to take a key and map it to where it needs to go in that array. The whole challenge of building an effective hash table is coming up with a reasonable hash function that spreads our data around in the table, usually in a haphazard fashion to avoid sending everything to the same place. Because if you send too many things to the same place, you get what are called collisions, and that will slow down the operation of your hash table. A lot of designing hash tables coming up with a good hash function, and also dealing with collisions when they happen. 

Because here we are taking a very large space of potential keys and mapping them down to the space of indices within our table via this hash function. Inevitably, when you map a large range down to a small range, you will have collisions. Collisions do happen in hashing. They don't break a hash table, they just slow it down, and we need to think of ways of effectively managing collisions. In the rest of our discussion today, we're going to focus a lot on how we manage collisions, and also we'll talk about how we design effective hash functions. Let's talk about collisions to start with. There are two probably high level ideas for managing collisions that are probably the most popular. 

They go by what look like unfortunate names that describe methods of torture, so probing and chaining. With probing, we store the elements that we're trying to represent in our hash table. We typically store them in the hash table itself. Anytime you try and insert a new key into the hash table and realize that the place that it hashes to that you'd like to insert to is already occupied, then you just simply scan forwards from that point until you find the next empty space in the hash table, and that's where you put the element. That's called sequential probing, where you just scan sequentially forward. If you reach the end of the table, you wrap back around to the beginning of the table. To make this work effectively, we need to make sure that the table is large enough that it has a lot of empty spaces in it. 

If you're storing 100 elements, you might want the table to have some constant factor larger than 100 worth of space in it so that you guarantee that you do have enough empty spaces that you're likely to hit one relatively soon as you are scanning forward. This is also why we try and map things into the table in a haphazard way. If all the elements get hashed to the same place in the table, this is going to be very, very slow. But if you hash the elements to arbitrary locations in the table, it's more likely that when scanning forward, you're going to find an empty space relatively quickly. We also sometimes look at more sophisticated probing patterns rather than just scanning sequentially, and we'll talk about that in just a second. Another approach that's pretty common is using chaining, where you don't typically store elements directly in the table, but every entry in the table is the head pointer of a link list that holds all of the elements colliding in a particular cell. Here, if you want to insert into the table, then you just hash the key that you want to insert, that tells you where in the table it should go, and then you just prepend that element to the link list in that cell of the table. 

This gives you a constant worst case running time for insert, so very straightforward to insert. If you'd like to find an element, then you just hash it as you did before, and then just search the corresponding link list attached to that table cell. As long as your hash function does a good job spreading elements of uniformly around the hash table and not sending everything to the same place, then performance will be pretty good here. If you send everything to the exact same place in the table, then congratulations you've just built a glorified link list, and performance will not be so good. A couple of useful notes on probing-based hash tables. As I mentioned, sometimes people do advocate using more sophisticated probing patterns instead of just scanning sequentially. You might have a hash function of two arguments, one of which is the key, and the other of which is the step in your probing sequence, so 1, 2, 3, 4, and this hash function would output a series of locations in the hash table that you would attempt to put the element. 

Whenever a spot is full, you move on to the next potential location. This could jump around in a more haphazard way throughout the table and hopefully build, I guess it would have less of a tendency maybe to build large clumps of consecutive elements that you might get with sequential probing. The hope here is that this will lead to an improvement in performance. However, it's not a foregone conclusion that that would be the case because jumping around haphazardly through memory is about the least cash friendly thing that you can do. Sequential probing, even though it might tends to generate long consecutive clumps of elements, it's very cash friendly just scanning sequentially through your table. You might have to do some computational testing to see which approaches work best for your hash table. Another important thing and I've made this bug myself in production code, don't do this is, if you delete an element from a probing-based hash table, you don't want to just simply reset that cell of the table to unused because that might break subsequent calls to find. 

If I look up the Element 1 after deleting this element at Position 5, then I definitely want to keep scanning all the way forward to find where the one is. If I reset the table cell five to empty, unused, then my find operation would stop scanning at that point. It would break the find operation. What we typically do is when we delete an element, you mark that table cell not as unused, but you mark it with maybe some an indicator, we call it sometimes a tombstone marker that allows you to reuse that table cell if you're doing an insert, that when you're doing a find operation, you continue scanning past that location. That makes it so the find doesn't break. There are quite a few interesting ideas in the space of probing-based hash tables that help manage collisions in effective ways, especially that try and minimize the running time of a find operation by trying to make sure that find operations in particular run very quickly. I wanted to just mention a few of these. 

A few are more advanced, they require a little bit of additional machinery underneath the hood to operate properly. But I think it's interesting just to see the types of ideas people have come up with in this space. To start with if we have a collision in a probing-based hash table, a whimsical way to think about that is it's an epic battle between two elements. There's an incoming element that wants to take over this location, but there's already an incumbent element sitting in that location. There's some a competition between these two elements. One of them wins and gets to stay in that location. The other one loses and has to move on and continue the probing sequence. 

Then that's going to possibly involve challenges with other elements if there are further collisions downstream. How do we deal with that? How do we construct interesting ways that will make the find operation tend to run quickly? One idea, maybe is I'll talk about these out of order. Robin Hood hashing, they all have cute names, tends to favor the weary well traveled elements. If you have an element that has already taken many steps in its probing sequence, that element would tend to win when compared with an incumbent element that hasn't in its probing sequence had to travel very far. If we implement this, the idea, the hope is that you won't have elements that end up very far away from where they has. 

Because the farther away from where you hash you get, the more likely you're going to be the one that wins one of these challenges and kicks somebody else out. This will hopefully make find run a little bit faster because elements don't typically travel too far from where they hashed. If we look at Hopscotch hashing, another acute name. The idea here is you try and keep each key close to where it hashes in the table. Within some distance Delta of where it hashes, and if all those positions are occupied, then you try and it'll kick somebody out, that person will move forward so that they stay within Delta of where they hashed, and that result in kicking somebody else out. You end up with this forward chain of kickouts until eventually, hopefully you reach an empty spot for somebody to end the process. It's reminiscent of the game of Hopscotch, I guess. 

If you look at everyone's kicking out and jumping forwards to make room for the new element. Again, the hope is that find will then run faster because every element is guaranteed to be relatively close to where it hashes. Another final method that I wanted to mention, that goes by the name of cuckoo hashing. The name comes from the fact that the European cuckoo is apparently a bird that is known for taking over the nest of other birds. Here you have this phenomenon applied to hashing. Usually, the way cuckoo hashing is implemented is you have two hash tables and two hash functions, and every given key has exactly two places where it could reside. It could be in the first table at the place where it hashes under the first function, or in the second table according to where it hashes in the second function. 

You can actually describe the possible locations as a bipartite graph between your two tables where every key is an edge that joins together the two possible places where a key could reside. Here if you try and find a key, it's very fast because there's only two places the key could be. Again, we're speeding up the find operation. If you insert a key, things get interesting though, because well, if one of your two possible locations is vacant, you can just insert at one of those vacant positions. But if both of them are occupied, then just like the European cuckoo bird, you are going to uneremoniously take over one of those positions and kick out whatever key was in that location. That key, of course, has an alternate location it could move to. It will move over to that alternate location, and it will take over that alternate location, possibly kicking somebody else out, and you get a chain of kickouts. 

If you're extremely unlucky, that chain can actually get stuck in an infinite cycle. Typically, cuckoo hashing has some counter built in where If you have enough kickouts in a row, you simply give up and just rebuild the entire table from scratch after reselecting different choices for your hash functions. A lot of these approaches have that possibility, where if it seems impossible to actually satisfy these conditions, you have to just give up and rehash everything. The hope is that that doesn't happen too often, so you still have decent amortized performance. Some of these methods involve a bit more complexity underneath the hood. I just thought it was nice to showcase a few different ideas in the design space of probing-based hash functions. Now we've reached an unfortunate point in our discussion where you must be subjected to one of my data structure jokes. 

As you guys know, I'm the director right now of Clemson School of Computing, basically a department chair role. Every time I run a faculty meeting, I've started a tradition where I start the faculty meeting with a silly joke, usually about an algorithms or data structure topic. One of the recent jokes that I won't say how many people laughed at it, but was one that I'm very proud of. It's a hash table walks into a bar, and the bartender eventually notices the hash table at the far side of the bar, walks up and says, you ordered and the hash table says, no. That's the joke. I can only presume that at the other end of this video, you're laughing at the joke. Feel welcome to pause the video and go tell your friends the joke as well if you want. 

But of course, why this is funny, and it is indeed funny, I assure you, is that hash tables are known as unordered sets. They're known as unordered data structures. The whole point of hashing is to spread information around in an unstructured haphazard way throughout the table. That's what makes hash tables fast. If everything gets mapped in a predictable way that always targets the same place in the hash table, then performance is going to be a lot worse. The whole point of hashing is to avoid structure, to avoid order. That actually explains partly the name hashing. 

If you think about the word hash, where does that come from? Well, in the world of food, a hash is this jumbled up mixture of many different ingredients, usually involving potatoes and whatnot. But the word hashing as a verb means to mix up or to jumble things up, and that really does convey the essence of what the hashing technique involves. It's trying to jumble up your data and it's interesting that by making things completely almost random and arbitrary, you get better performance with hashing. How do we do this with an actual hash function? What are best practices for designing hash functions? Here we'll talk about just heuristics for coming up with good hash functions that tend to work well in practice. 

In the next video, we'll talk about in a slightly more mathematically principled way the design of what we call universal hash functions that actually carry strong theoretical guarantees. Here we won't actually have guarantees on running times, but these will tend to perform well in practice as long as we avoid some common pitfalls. If we look at some common choices for hash functions here, maybe the simplest is you just take your key, mod the size of your table. A lot of hash functions at the end of the hash function, you find mod the size of the table because at the end of the day, you have to output an index within the range of your hash table. If you just take your key mod the table size, that tends to work pretty well unless you have some reason to expect that your keys are maybe spaced out by multiples of the table size because then they would end up hashing to the same place. Maybe one example of a situation where that might somewhat happen is if your table size is 256, oftentimes table sizes are powers of two like this, and your hashing keys that happen to be IP addresses, like an IPV4 address is a four byte integer, where each byte is a meaningful part of that address. But if you hash an IP address using this hash function, you're basically extracting the last byte. 

You're completely ignoring the first three bytes of the key and only looking at the last byte. Oftentimes with IP addresses, that last bite is like.1 or.2 or.3, it's a small integer. In this case, you might be putting too much pressure on the first the early part of your hash table. This also illustrates that you should make sure that you use the entire key. If you have a hash function that's key mod 256, it's only going to use the last byte, the low order eight bits of your key. It's going to completely ignore the rest of the key. That's usually not a good thing. 

Your hash function should use the entire key, and it should also use the entire table. There's another example that we'll see in a second where if you're not careful, your hash function might output always an even number. Maybe it only hashes to the even spots in your table. Make sure that you use all the bits of your key and that you use the entirety of your hash table as well. Another very simple example would be this one right here. You basically take your key and you scale it by m/C. The size of the table divided by the maximum possible key. 

This hash function intuitively, it just linearly rescales the range of all possible key values down to the range of all possible table indices. That's probably a bad idea, I would think for a hash function. Because oftentimes you're hashing things that are sequential in nature, you'd hash x and then you'd has x+1 and then x+2. In a hash function like this, very nearby values would tend to get compressed down and hash to the same place in your table. That's probably not a good idea. You typically want nearby values in key space like x, x+1, x+2 to hash two very different locations in your table, because otherwise, bad things can happen in very common use cases in practice. A very common situation that's often used as a heuristic hash function, you basically apply some linear transformation to your key. 

You take the key times a+b for some coefficients, a and b, and then take it the table size. This can work quite well, as long as you avoid the situation I talked about a second ago where maybe you always output an even number. If a and b and m are always even, then you're always going to output an even number. Be careful to avoid that structure. A lot of times people recommend choosing prime numbers for these parameters to try and avoid always outputting some multiple two or multiple three or something like that. These are some common choices that we use for heuristic cache functions. There's no guarantees using any of these. 

But if you avoid the pitfalls that we talked about, then usually performance will be fine. You would anticipate constant running time, well, as long as the size of your table is appropriate for the number of elements that you're storing in the table. For example, if we go back and we look at a chained hash table like this, then what's the performance of a find operation? Well, it depends on how long these link lists are. What you want to have happen is you want this underlying array to be roughly the same size as the number of elements you're storing in the table so that on average, the length of these lists is roughly constant, and you don't have to spend that long with the find operation. With a probing-based hash table, it's important, like I said before, to leave enough extra breathing room in the table so that you do have plenty of empty spaces to make inserts run quickly. The running time of a find operation is basically the same as the running time of an insert operation in this case. 

The longer an element travels during insert, the longer the probing sequence during a find operation is going to take. In both of these cases, it's important that the underlying array representing your hash table be sized appropriately. You would like it to be roughly on the order of the number of elements in your table for a chained hash table, and you'd like it to be maybe some small multiplicative factor larger than the number of elements you're storing in the case of a probing-based hash table. To do that, we often use amortized rebuilding. The same approach that we talked about with an expanding array when we talked about amortization. If your hash table gets to full, then you allocate another underlying array, maybe twice the size, and you rehash everything into that new array. Rehashing is necessary because if you look at most hash functions, they end with mod the table size. 

If you change the size of a hash table, that changes where things are located within the hash table. You really do need to rehash and reinsert whenever you build a different sized hash table. Then if you delete a lot of elements, then you often downsize your hash table as well just to make sure you're not wasting space. The goal is to keep the size of the hash table locked at some constant factor times the number of elements you're actually storing in the table to make sure that you're memory efficient, as well as being efficient in terms of inserts and deletes and finds. Because it only takes a linear amount of time to rebuild your table, this doesn't impact the performance of inserts and deletes too much. It's just going to add an extra constant amortized time to every call to insert or delete, using the same amortized analysis that we went through when we talked about expanding and contracting tables. That's something that we want to make sure we do with almost any hash table because we often don't know in advance how many elements are going to be stored in the table. 

The table has to expand on demand as needed.