![[Coding Discussion .pdf]]![[CDaNR9U5Q26Ni5QfnAnVNw_bd0773826cf64fe29a346f324c6dddf1_20240802CPSC8400_0605coding_MP4_720.mp4]]
For our coding demo in this module, I thought we would work through some simple exercises involving hashing and hash tables. One, to illustrate some common use cases of these techniques, and two, to hopefully showcase how easy it is to actually write to implement a hash table. Let's talk about maybe our goals for the exercise. Let's maybe take as input a file that contains one million strings, one string per line, and those strings are going to be English words. My goal for the first part of the exercise is to remove the duplicates because there certainly are not one million distinct English words. I want to remove all of the duplicates from the file and count how many distinct words are really present in that file. Very standard application that we often use hashing for there. 

Then goal number two should be hopefully fun. We're going to find all pairs of words that are anagrams of each other. Two words or anagrams, if you can rearrange the letters in one word and get the other word, or I guess, equivalently, if you sort the letters in both words and that makes them equal to each other. That is our set of goals for this exercise. Let's go ahead and start writing some code. Hopefully this will be a relatively straightforward exercise. If I switch over to my coding view here, let's take a look at this input file. 

If I look at maybe the last bit of manywords.text. You can see that this file just contains an arbitrary assortment of random English words. How many lines are there? WC -l counts the number of lines in a file. There are indeed one million lines, one million strings in this file. How many of them are distinct? I guess I could actually also solve this with the Unix command line, so I could sort the file. 

That will actually group together all of the instances of each word in one contiguous block. Then if I pipe that through uniq, that will actually compress down every block of identical lines into just one line, and then I can run that through the word count program, and that tells me the number of lines at the end is 53,021. That should be the answer that we're hoping to get at the end of our program. Everybody remember that number, 53,021. Let's go ahead and let's write some code and implement some hashing. I'll start with my usual iostream and using namespace, if I can actually type, std. What I'm going to do is just read in a bunch of strings from standard input. 

While I read in a string from standard input, what do I do with that string? I guess, I have to now put it in a data structure of some sort. Maybe just to get us started, let's put all of our strings into a linked list. This will be easy to write because we've already written a lot of link list code. I wanted to also draw attention to how easy it is to move from having a linked list to a chained hash table. A chained hash table is really not much more than just an array of linked lists, so we should be able to take a very small step to get from a linked list to a hash table. If we're going to write a link list, we need a node structure to represent each node of a link list. 

It's going to have, I guess, a string valued key and a node valued next pointer, node *next. Then maybe I'll write my usual insert function to insert at the beginning of that link list. Node *insert. This will be the same function that we wrote, I think back when we practiced using a link list. I'll pass in the head pointer of the list and a string key that I like to insert, and I'm just going to create a new node. I'm going to allocate a new node, initialize it, so its key is going to be k, and its next pointer is going to be everything in the list currently, so the current head pointer of the list and then just return that new node that I've created. I should be able to add all of the strings that I read in into a link list by creating a link list. 

An empty link list and then every single string that I read in, I just insert it into the link list. So far, this code should run relatively quickly, just makes a link list of length one million out of all of the contents of my file. If I wanted to only put the distinct strings into my link list, I could write something like this. I could say, if not find maybe head,S, then I insert into the link list. If I had a find function, which I would need to write, then only if the string is not already in the list, do I insert it into the list. That's going to basically populate the list only with the distinct strings. The trouble is, this is not going to be particularly fast because well, every single string I read in, I'm calling find on it, which is going to, in linear time, walk down the link list, trying to find if that string is present or not in the link list. 

This code right here is going to be quadratic, which is not a good thing to have if my input size is one million. We don't want to do this. We want to use instead a hash table. That'll be a much faster way to store and call find on our strings. I guess, let's go ahead and write the find function just so that we have some complete code here. Boolean find is going to take the head pointer of the list and the string that we're trying to search for, and it's simply going to walk down the link list and see if the string is present. I'll just use a for loop for that, so node *n is equal to head as long as n is not equal to the null pointer in each step stepping to the next node of the list. 

Then as I walk down the list, if I run into a node whose key is equal to k, and I return true, because I have found the string that I'm looking for. Otherwise, if I get to the end of the list and haven't found it, return false. That's my fine function. Here we should actually have a working program. I just suspect it may not be the fastest program in the world. Let's actually see how fast it is. I'll make an integer n that counts how many things I actually insert into my link list. 

At the end, I will output n. This should actually print out, what was it, 53,021. Let's go ahead and compile and run. This is our link list based approach. I run my program and I pipe in manywords.text, and it's going to take a while. It's going through one million words, and for every single word, it's scanning all the way down to the end of the link list and trying to figure out, is that word present in my link list? Not good so far in terms of efficiency. 

Always good though to have a baseline solution that we can compare against to make us feel good we're making our algorithm run faster. Let's go ahead and see if we can instead of a link list, use a chained hash table. What is the difference between a chained hash table and a linked list? I would argue that instead of a single head pointer, all we have here is an array of head pointers. I'll just make an array of head pointers of size, let's call it table size, maybe how big should our table be? I'll just hard code the table size here for simplicity at maybe, I don't know, how about 10,000? Because when we cheated earlier and saw that there should be about 50,000 distinct words, that would mean that on average, each link list in our hash table would have about five words in it. 

That's not too bad. Instead of just one global link list, I have an array of link lists, one for every cell in my hash table. What's the other ingredient that you need if you're building a hash table? You need a hash function. I need to write my hash function that's going to take a string S and return the hash of S. Maybe return a table index in the range from zero up to table size minus one. Return a valid index within my table. 

How can I compute the hash of a string? I'm going to use exactly the same technique that we talked about in the previous video on polynomial hashing, so we're going to use effectively Horner's rule and loop over the letters in my string, and for each one, keep a running hash value that I multiply by some randomly chosen x, and then I add in the next character. In code, that's remarkably simple, actually. Maybe I'll start my running hash at zero. At the end, I'm going to return that. Then all I do is loop over my string and add up, accumulate that running hash. Loop over all the characters maybe in my string length, i++. 

Then for every character, I'm just going to update my hash to what I get when I take maybe hash. I have to multiply by some arbitrary x. I don't know, I'm just going to pick a number out of thin air, 173. Why not? Then I add in the next character of my string. S(i). That's how I update my hash at each step. 

I should probably also after each step, take the result module of the table size. My numbers stay in a reasonable range. Every number that I get after each step is going to be reduced back down to this range between zero and table size minus one. At the end when I return a value of hash, it is indeed a valid index within my table. This is very common when you accumulate a running hash like this, after every intermediate calculation, you take the result module of the table size to keep your numbers in a reasonable range. Actually, there's one other subtlety that sometimes crops up when you write a function like this that you sometimes need to be careful about. You want to make sure that you're never overflowing things to make negative numbers, because if you accidentally get a negative number here, a negative number mod the table size actually turns out to be negative. 

Mathematically, it shouldn't, but in a computing system, negative number mod something ends up being negative, and that's bad news if you're trying to return a non-negative integer, which is your index into the table. We need to make sure that this never overflows and turns negative on us. I think we're going to be okay because we're always reducing things mod the table size, so the value of hash is going to be at most 10,000. I'm multiplying it by 173. That's still going to fit comfortably within an int. If we were paranoid about overflow though, we could possibly do things like work with unsigned ints, for example, that will never go negative on us. That's just one consideration that sometimes crops up when you're writing a function like this, just be aware of the possibility of negative numbers causing strange bugs in your code. 

As we read in the strings from the input, now we can figure out for each string where it hashes in our table. I can call the myhash function on my string, and that will give me the location in the table to which it hashes. Instead of inserting into this global link list, now all I do is I just insert it into the appropriate link list in my hash table, the one to which it hashed. When I call find, I again, want to just see if I find the element that I care about in the specific link list that it hashed to. This is the beauty of a chained hash table. If you already have a linked list implementation sitting around, you're basically just adding bracket Hs on top of your head pointers because now instead of dealing with the head pointer of one global list, you're just looking at the head pointer of the specific list that you hashed to, and you have an array of head pointers instead. Actually, relatively lightweight to implement a chained hash table. 

This code should now just work and it should output the right value of n, and it should actually run much faster. If I recompile and rerun, then boom. It prints out the right answer instantly, pretty much, so hashing really does work. It's a very effective technique. We have finished our first goal for the exercise. Maybe I'll copy hash.cpp into hash1.cpp here. I'll make a copy of the program that solved our first goal because we might modify it now to solve the second goal, which is finding all the pairs of anagrams in our dataset. 

Let's go back and look at how we might do that. I think I liked what we did last time. We did the slow approach and then the faster approach. Maybe let's do the same thing with anagrams. What I'm going to do is maybe I'll make one big link list of all the distinct words, and we'll work with that first. That should be a slow approach that uses one big link list, and then we'll try using a hash table instead to see how we can speed things up. I'll make a link list of all the words, and it's going to start out as null pointer. 

Every time I insert into my hash table, I will also insert into this big link list of words, so words = insert(words, s). Now I've built a link list of all of my words, and now I'm going to go through and just try and find what pairs of words are anagrams. Let's do this the slow way first. I'm going to just loop over every pair of words in that link list and compare them and see if they are indeed anagrams of each other. Let's loop over that link list. I'm going to start out. Let's maybe say a is going to start out at the head pointer of the link list. 

Loop as long as we're not the null pointer and a = a->next. That's going to loop over all the words. Then within that, I'm going to loop over all the other words. Maybe all the words after this one because I don't want to consider pairs twice like pair x, y and then pair y, x. Maybe I'll start this one out at a->next. I look at all the words after a. That's word b. 

Now all I need to do inside this double four loop that's looping over every possible combination of words is I just need to compare, are these two things anagrams? If anagrams, and I'll pass in the keys of these two nodes. If they're anagrams, then I'll just print them out, a->key space b.->key. I guess I need to actually write a function that tests of two things or anagrams. That hopefully isn't too bad, so bool anagrams. I'll pass in two strings, a and b. How do we test if two things are anagrams? 

I could actually use hashing here, if we wanted to. You can build a hash table that has all the characters in a and then loop over b and maybe remove things from that hash table, and if you end up with an empty hash table, then a and b were anagrams of each other. Although that's probably overkill for testing if two short strings are anagrams of each other. Maybe a lightweight simple approach here is just sort a and b. Sort the characters in both strings and see if that makes them equal. I could just say something like sort. I think the sort function you pass it like a.begin, that's the pointer to the first character in a, and a.end, which is a pointer to the end of a, and I do the same thing for b. I'm going to sort b, and then I'm just going to return whether that made them equal, so return whether a is equal to b. 

Very simple approach to test if two strings are anagrams. I guess, if I'm using the sort function, I probably need to include something up here. I think it's algorithm if I'm using the sort function. Now, I should be printing pairs of words that are anagrams, but probably doing so rather slowly because I'm using one giant link list, and I have an n squared algorithm that just loops over all pairs of elements in that giant link list of size 53,000. Let's maybe compile and run and see what this looks like as a baseline. If I run this, then I should start getting anagrams, which I'm getting. You'll notice that they're showing up in reverse alphabetical order. 

I guess, that probably means that my original file must have had words in increasing alphabetical order at some point because when I built my link list, it would have been built backwards because I was inserting things at the beginning of the link list. But that is convenient because it helps us gauge progress. We're only in the Ws and we have to get all the way back down to the As. You can see that this definitely it's not fast. How can we make it faster, basically? If we go back to our code now, instead of iterating over this monstrous list of all the words, all the words also exist in our hash table. Let's instead iterate over our hash table. 

The trouble is, if I want to find anagrams, then how do I make sure that words that are anagrams of each other end up in the same bucket of my hash table? Because otherwise, if I only look locally in each bucket of the hash table, I won't find anagrams. I need to have some way of ensuring that when I drop these words into buckets in my hash table, it would be a feature if anagrams collide because then all I have to do is search within each bucket for all of the anagrams in that bucket. If you recall back when we did, I think it was bucket sort. We did this large scale problem decomposition where we took a big problem and we could have just run a bubble sort, a quadratic time algorithm on the big problem. What we did instead was we broke the problem up, we decomposed it into a number of smaller buckets and we ran a quadratic time algorithm locally within each bucket. That's pretty much what we want to do here. 

I want to design a hash function that forces anagrams to collide. In each cell of my hash table, I'll get all the anagrams that hash there, possibly other words as well. I still need to do all pairs of comparisons within locally each bucket, but the buckets are going to be much smaller than the original global list. Things should run much faster. Let's go back to my hash function. How do I make sure that anagrams collide? Actually, one easy way to do that, perhaps, is before hashing my string, I'll just sort it because remember that anagrams sort to the same thing. 

I will basically transform every string into this canonical sorted format by sorting its characters, and then I hash it. That will make sure that anagrams hash to the same place in my table. That part is at least done. Now, I just need to loop over, I guess, my hash table and locally run this n squared algorithm in each bucket of my hash table. I'm going to loop over every single cell of my hash table. Loop over i = 0 all the way up to the table size. Then within each cell, what am I going to do? 

I'm going to just loop over a double four loop over the contents of the link list in that cell. That was what? That was the head pointer in cell i. That's all I need to do, I think. I'm just looping over every single cell in the hash table, and then within that cell, I have that n squared algorithm, but n is very small here because it's just looping over just the contents of that cell. This is a really interesting, almost it looks wrong because I added another layer of for loops, and yet this should make my program run faster. Let's compile and run and see the damage here. 

We compile, we run, and it's done. I wonder how many different anagrams we got actually. Wc -l. We got 3,036 pairs of anagrams. Pretty cool. Hashing definitely runs quickly. There's a reason that hashing is so prevalent in practice because hash tables, they really are very effective at solving a wide range of problems. 

I'll leave this here, and hopefully this has been informative about how to implement hashing and hash tables.