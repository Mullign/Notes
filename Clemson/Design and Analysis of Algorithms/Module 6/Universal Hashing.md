![[Universal Hashing.pdf]]In this video, we'll discuss how to build a class of hash functions for which it will actually be possible to prove strong theoretical guarantees on running time performance that hasn't been possible yet. In the preceding video, we went over kind of best practices for designing heuristic hash functions that tend to perform well in practice. This is the slide that we went over in the last video, and that leads us to basically this state of affairs in terms of running times. Insert and remove. In our hash tables, those take constant amortized time because of our resizing of the hash table. And then the find operation is basically cross your fingers, and hope for the best. And if you use a decent hash function, usually you'll get constant time performance. 

But our goal here is to actually get a provable guarantee that is constant in expectation. As a starting point, let's go back and look at our two major ways of resolving collisions with probing or with chaining. And let's talk about running times of each. So remember, with probing, the running time of insert and the running time of find are basically the same thing, because it takes the same amount of time to find an element as it did to insert it originally, the running time of delete is a bit interesting. Typically, when you delete an element, you have to first find it and then delete it. Just to make things as simple as possible, we typically separate those two functionalities, and this is characteristic of analyzing many data structures. When we talk about the running time of delete, we talk about just the time for delete, not the time to find and delete the element. 

So here, the time for just deleting an element is constant if you factor out the running time for find and only analyze that under the find operation. So we would really like to hear analyze the running time for find/insert with a probing-based hash table. In the case of chaining, insert and delete both take only constant time, I guess amortized because we're resizing the table again and again, delete is just the deletion part. It doesn't count the time it takes to find an element. So here the interesting case is analyzing the running time of find, because to find an element, you have to hash that element and search the linked list of all the colliding elements in that particular cell of the table. So it all boils down to how long that linked list is. Hopefully not very long, no matter how you resolve collisions. 

It turns out that if you want to be able to write any sort of reasonable performance guarantee for find, you absolutely cannot use a deterministic, that is non random hash function. Because assuming the space of all keys is quite large relative to the size of your hash table, no matter what deterministic hash function you use, there will always be a bad set of keys lurking out there that your malicious adversary could have fed to your hash table, turning it into basically like a glorified linked list in the case of chaining. So using deterministic hash functions will never lead us to a good performance guarantee. For find, we'd have to use randomization in some way or another. So what if we go to the other end of the spectrum and we consider using a fully random hash function, we're just going to hash everything totally at random. What that means is for every possible key, we have to write down and remember where it would be hashed in the table, and that's going to be just a random destination. If we do this, then the performance actually turns out to be great. 

We're effectively just throwing balls into bins at random. This is a situation we've analyzed quite a bit when we've analyzed randomized algorithms and talked about linearity of expectation. It's easy to show that on average, say, if you're using chaining, then you expect to find n over m balls landing in every single bin, in every single cell of the hash table. So the expected running time of find is just going to be one plus n over m. I write the 1+ just because of the time it takes to hash the element initially. So this is great. We typically do assume that m is on the order of n because of our table resizing. 

That means that kind of the load factor, as this is called, of our table, is constant. And so the total expected running time of find is constant, which is nice. And you can actually show the same thing with a probing based hash table. It takes a little bit more mathematical work, so I won't go into those details. But performance is great if we hash at random. The fatal flaw with this, the downside is it takes a tremendous amount of space, because for every possible key, you would have to write down the randomly chosen destination to which that key would hash. You can't just pick a new random destination every single time you hash the key, because every time you hash the key, it has to go to the same place in the table. 

So this would take a tremendous amount of space to represent a fully random hash function, as much space as we would have needed for a direct access table. So there would have been a much easier solution to our problem if we're willing to spend that much space. What does end up working well is a compromise solution where we build a partially random hash function. It's a hash function with a small number of baked in parameters that we choose randomly when our algorithm initializes itself. And that gives us just enough randomization to be able to prove our strong performance guarantees. And now we're ready to talk about the core topic of this video, which is universal hashing. We say that a hash function is universal if the probability of two different keys colliding under that hash function is just one over the table size, and some definitions allow two over the table size, or three over the table size will be fine with any of those flavors of definition. 

Note that this is actually the same guarantee that you would get if you were mapping things completely at random. If you just map elements randomly into the table, the probability of a collision between two elements is also one over the table size. And there is a lot in common between random mapping and universal hash functions, which we'll cover in just a second. A slightly more specific definition is strong universality, which actually implies universality. So here the probability of, I guess if you have two different keys and two locations in the table, the probability of those two keys mapping to those two locations in the table is one over the table size squared. Or in some flavors of the definition, a constant over the table size squared, and we'll be fine with either of those. This also is the same probability that we would get if we were mapping things completely at random. 

And in fact, one nice way to think about kind of what strong universality really means is it basically is a random mapping that gives you pairwise independence, but nothing higher order than that. So it's a mapping that behaves as if it's a random mapping. As long as you only look at two elements at a time, those two elements behave as if they were being mapped completely at random. But if you add maybe a third element to the mix, now there could be some dependencies based on where the first two elements were mapped. So strong universality is basically a random mapping that just gives you pairwise independence, but no higher order levels of independence in where things get mapped. Now, the punchline at the end of the day is that if you use universal hashing or strongly universal hashing, then find runs quickly. The expected running time of find is constant. 

It's 1+n/m. And remember that we always keep m bounded by theta of n by resizing our table. So this will work out to a constant in expectation. So that's the big deal with using universal hashing, it will be very easy to show in a second that I guess in a chained hash table, probing is something we'll have to talk about in a second. At least in a chained hash table, universality makes find run quickly in expectation. All right, this is a bit of an intimidating slide, but I wanted to show you a few examples of Of universal and strongly universal hash functions. There are several such functions that people use. 

Depending on your application, one may be easier to deal with than the other, but let me kind of go through and talk about a few of these. The first function here, this is sometimes called a linear hash function. It's very similar to the heuristic hash functions we talked about earlier. You're basically taking some sort of a linear function of your key, and then you take it modulo an arbitrary large prime number and then mod your table size. This one is not too bad to use. Your parameters a and b are basically the ones that are randomly chosen during the initialization of your algorithm. The only thing that's a bit of a pain is you have to come up with a large prime number to be able to use this function. 

And there are good algorithms for generating large prime numbers, but that's a step that makes this function a little bit maybe less pleasant to use if you don't care to generate a large prime number as part of your hashing scheme. There are a couple of other simpler alternatives. Perhaps this second alternative is actually quite nice. All you do is choose the parameters here, a and b, randomly, and then you just take this ak plus b, this linear function of your key modulo mC divide by C and then take the floor. So that's actually not too bad, especially if m and C are powers of 2, which is often the case, because then taking things mod mC and divided by C, those can be done with simple bitwise operations. And so this can actually be a very fast hash function to evaluate in practice. And speaking of bitwise operations, this third example is also quite interesting. 

This is sometimes called tabulated hashing. The idea here is that we are going to actually use fully random hash functions, but over a much smaller domain. So we're going to break our key up into small locks of bits. If you think about the larger key being written in binary, we're going to break it up into b blocks. So maybe it could be just the individual bits written out, b of them, or maybe two bits at a time, or maybe three bits at a time. If you're hashing maybe an IP address, a IPV4 address, you could maybe break it up into four blocks of eight bits each. And then you're just going to hash each of those blocks in a fully random way. 

So we're going to make a fully random lookup table, essentially, that acts as a hash table for each of those small blocks of the piece of your original key. And so we're basically going to fully randomly hash the first run of bits in our key and then fully randomly hash the next block of bits, and then fully randomly hash the next block of bits and so on, with different tables that are randomly chosen during initialization. And then we just xor all of those results together to get the mapping for the entire key. So it's called tabulated hashing because it relies on basically these random lookup tables that act as kind of very, very small, fully random hash functions. And this also can be shown to be strongly universal. It's actually a very practical way of hashing. If we go back to the example of hashing an IP address, for example, you could make four tables that each hash one byte of your IP address. 

And so that's basically a table of size 256 that tells you where each of the 256 different options for that byte would be mapped in your table. So we just build four of those and Xor all of the results together, and that gives you a strongly universal mapping for the entire IP address. And then at the bottom here, I've shown a function that is universal, but not strongly universal. That's perfectly fine in terms of the running time guarantee that you get for find, as we just showed. But this is much simpler to evaluate if you want something that's also a very lightweight function that doesn't take too much effort to evaluate, especially if M and C are powers of two things like division and mod. M can be done with very fast bitwise operations, and so this type of function can be evaluated very quickly. Typically, if you have a universal hash function, it's actually very easy to show that the find operation in a chained hash table runs in constant expected time. 

This is just a simple direct application of linearity of expectation. Remember that the running time of find boils down to the length of the list that you're searching with the find operation. So you could say the running time of find is order one to do the hashing, and then plus l, where l is the length of the list that you end up searching. And so all we need to do is calculate the expected value of l, the length of the list that we're searching. And we're actually going to focus on an unsuccessful call to find one that searches the entire list, because that'll give us an upper bound on the expected time we spend for a successful call to find. So let's analyze the expected value of L. To do that, we can write L as a sum of indicator random variables, each one is going to tell us one if the key that we're searching for, k, collides with one of the keys in our table. 

Because every key in our table that collides with k is going to be an element in the linked list that we end up searching. And so all we need to do, by linearity of expectation, is calculate the sum of the expectations of these indicator random variables. Remember, the expected value of an indicator random variable is the probability of the event for which it serves as an indicator, the probability of a collision between two different elements. And they're different because we have an unsuccessful call to find. And so, by the definition of universal hashing, that probability is 1 over m, or order of 1 over m. So if you add that up over n indicator random variables, you get that the expected length of the list that you search is n over m, which is constant as long as n and m are within constant factors of each other. To recap where we are with universal hashing, we now have insert and delete running in constant amortized time, and find running in constant expected time if we use chaining to resolve collisions. 

And so now this opens the door to solving a wide range of problems much faster than we would have previously been able to solve them using prior data structures like binary search trees or whatnot, that were in the comparison model. So here I've shown a couple of very standard, very prominent problems that actually have an n log n worst case lower bound in the comparison model, that where you can solve the problem in linear expected time with a universal hash table. So the two problems here, one of them is element uniqueness. Given n elements, are they all distinct, or are there duplicates present? And then the other problem is basically looking at standard set operations. Given two sets of elements, find their intersection, find their union, find their difference, all of these problems actually end up having a worst case lower bound of n log n in the comparison model. You can't hope to solve them any faster than that using things like binary search trees. 

But if you have a universal hash table, you can actually solve them in linear expected time. Just to give one prototypical application and we'll do exactly this when we talk about our coding discussion later on in the module. Suppose you have a list of elements, and you'd like to just remove duplicates from that list, leaving only one of each distinct value. That's easy to do with a universal hash table, because all you're doing is inserting the elements one by one into the table. So there's at most n inserts. And right before you insert an element, you call find on that element to see if it's already present in the table. And if it is, you don't insert a duplicate copy. 

And so this is going to give you n finds, which each one takes constant time and expectation. So by linearity of expectation, that's only order n time in expectation. And then we have n inserts at most, each one of those takes constant amortized time. And so in total, that will take linear time. So, linear plus linear in expectation will give us a total running time of linear in expectation for solving that problem. >> And a wide range of very simple problems can be similarly solved. These are also a staple of technical job interviews, things like, you know, remove the duplicates or tell me how many distinct values are present in a list, things like that. 

So hashing is, if you're in a job interview and you don't know the answer, then guess hashing basically, because that's often the answer for a lot of standard job interview questions. So far, all of our analysis has been specific to chained hash tables. What about hash tables that use probing instead? It turns out that the analysis there turns out to be a bit more complicated, which is why we often focus on chained hash tables instead. With a probing based hash table, it turns out you can get constant expected performance for insert and find. However, that requires something beyond just strongly universal hash functions. We actually need hash functions that support 5-wise independence instead of just pairwise independence. 

And we can do this, we can build slightly more sophisticated hash functions that have that level of independence built in. But one way to do that would be, say, generalizing our linear hash functions. Instead of taking a linear function like ak+b mod prime, mod the table size, we can take a higher order polynomial function with more parameters baked into it, and then take that mod prime, mod of the table size. And if the polynomial has five parameters baked into it, we actually get 5-wise independence. It's no accident that the number of kind of free parameters that we're choosing randomly that are baked into our hash function, that gives us the level of independence. Maybe to see that if we go back to our linear hashing function here, that has just kind of two random parameters, a and b, that we had chosen, and that's only pairwise independent. And you can kind of see that by suppose you knew where two keys were mapped. 

So you kind of knew the output of this hash function for two different keys. That almost gives you kind of two equations and two variables. Your kind of unknown variables are this, a and b parameter. So if you knew where two keys were mapped, you could almost, I guess you could in many cases, solve for a and b, and then that would fully determine where any other key would be mapped. And so, similarly, if you have five parameters baked into your hash function and you knew where five things were mapped, then you could reverse engineer what those parameters were, and then the 6th thing would be fully determined at that point. So this is kind of the story on performance of probing-based hash tables. It's a little bit more complicated in terms of the underlying analysis and mathematics. 

But at least you can still get constant performance for insert and find. All right, one final thing I wanted to mention. I wanted to kind of go back and revisit a topic that we've talked about, I think, in module two, when we talked about bucket sort and linearity of expectation and all of our different tools for doing randomized analysis. So here we were throwing balls into bins, and we were looking at the expected number of colliding pairs of balls when we did that. And that's extremely relevant to hashing, because, of course, balls and bins and collisions is exactly what happens when you have a hash table and elements collide. So, when we talked about, in particular, the so called birthday paradox, we were talking about how many collisions do you get when you throw n balls into 365 bins to see how many shared birthdays you have in a set of people. And so when we did the math here, it was actually quite straightforward. 

We looked at the expected number of pairwise collisions you get when you throw n balls into m bins. And that was just a decomposition into indicator random variables. One, for every pair of elements, there's n. Choose two different pairs of elements, and each pair of elements had a probability of one over the table size of colliding. And so you multiply those together, linearity of expectation tells you approximately you expect N squared over 2M pairwise collisions when you map your elements into the table. And that was part of our analysis of earlier, when we talked about the birthday paradox. It turns out that the mathematics here still applies if we talk about universal hashing, because the probability of a pair colliding is still one over the table size, even with universal hashing. 

And so we get the exact same bound on the expected number of pairwise collisions with a universal hash function, as we would have had if we had just randomly mapped everything into the table. And because of that, now we can actually talk about what would it take to build a hash function and a hash table that has zero collisions. So, knowing this bound right here, what if we actually go with a table size of n squared? If you map N elements into a hash table universally, and that hash table has size N squared, then the expected number of pairwise collisions is N squared divided by 2N squared, or one-half. So we only expect half a collision if we map N elements into an enormous table of size N squared. And now we can actually kind of use all of our wonderful probabilistic tools. So the expected number of collisions is at most one half. 

So what's the probability that you have at least one collision. That's the same as saying, what's the probability that you have more than twice as many collisions as what you expect? And so, using Markov's inequality, you can show that the probability of one collision or more, that's at most one half. And so, flip that around. The probability that you have no collisions, the probability that you managed to be lucky enough to build a collision free hash table, that's at least one-half. So if you map N elements into a size N squared table, you have at least a one-half chance of that mapping being collision-free. And so now what if I keep doing that mapping over and over until I finally get a collision free hash table? 

Since there's a probability of at least one half of being successful on each of those attempts, then this is expected trials until success. The number of times I rebuild my table in expectation is at most two. And so what that means is, in expectation, both in time and in space, I can build a collision free hash table, at least statically, on n elements in quadratic time, in n squared time and space in expectations. So that's a fairly interesting result, although it's maybe not that useful because quadratic time and space is quite large. It turns out, though, that you can actually get the expected time and space down to just linear with another cool idea that lets us review another thing that we've talked about in the past, which was essentially the idea behind bucket sort and large scale problem decomposition. So here the idea is, I'm going to build a static hash table on n elements where find is going to run in constant, worst case time. Effectively, there'll be collision free. 

And here's what I mean by that. I'm going to hash n elements into a size n universal hash table. That's kind of my top-level structure. And then, for all the elements that collide in a cell, instead of resolving them with chaining, I'm going to resolve them with the previous slide. I'm going to resolve them by building a collision free hash table, that the size of that hash table is going to be the square of however many elements landed in that cell. So if three elements landed in a particular table cell, I will actually build a size three squared collision free hash table on those elements. And the time it takes to do that is just quadratic in the number of elements that land in that cell in expectation. 

So the total time it takes to actually build this structure is going to be an order n to basically hash everything into the original universal hash table. And then the expected value of the sums of the squares of the numbers of elements that land in each respective bucket in my top level hash table. Remember now, back when we analyzed bucket sort, we actually had an expression for running time that was exactly this. It was basically the expected value of the sums of the number of elements squared that ended up in each of the different buckets that we sent our elements into. And it turns out that the exact same mathematics from the analysis of bucket sort applies here. We were able to kind of equate this sum of squares with the total number of pairwise collisions, which in this case, in this top level table, is on the order of n. And so it turns out that in only linear expected time and space, we can build this two-level structure where it's kind of a hash table where we resolve collisions with another level of hash table, although that second level is designed to be collision-free. 

And so using this structure, we can actually do finds in constant, worst case time, because we hash an element that leads us into a cell in our top level structure, and then we can do a collision free lookup in the secondary structure. So this is sometimes called a perfect hash table because find runs in only constant time. This case only helps us if we have a static problem where we want to build a hash table on n elements and then just issue find queries on it. But with a little bit of pain and suffering, you can also make it dynamic and support inserts and deletes as well. So there's a lot of interesting generalizations and extensions that you can do with hashing. I like this problem in particular, this kind of static perfect hash table construction, because it lets you very nicely review many of the probabilistic tools that we had developed. We're using Markov's inequality, we're using linearity of expectation, we're using expected trials until success. 

So it's a really good problem to kind of work through in terms of the math, just to make sure that we're comfortable with all of our different forms of probabilistic analysis. So that closes our discussion of universal hashing. Very, very powerful technique. So now we can actually write strong running time guarantees for all of our hashing based algorithms.