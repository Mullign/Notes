![[Matchings.pdf]]
The final class of problems we've chosen to discuss in this module is the class of matching problems, a very extensively studied class of problems, both from a graph theory perspective and a graph algorithms perspective. Matching problems show up in many important applications in practice, and they also involve quite a few clever algorithmic ideas, many of which are based on the techniques we've already talked about in this module. So hopefully this serves as an appropriate grand finale topic for our discussion of graph algorithms. A good starting point for talking about matchings is talking about the fundamental problem of finding a maximum cardinality matching in a bipartite graph. So here I have a bipartite graph, there are nodes on the left and nodes on the right. And in that graph, I would like to match up as many nodes as possible. Every edge in the graph tells me a perspective pairing that I could make between a node on the left and a node on the right. 

And say here, the bold set of edges represents one possible matching in my graph. Every node can be matched to at most one other node. That's kind of the main condition that defines what a matching is. There are some extended versions of matching problems that we'll see later on that do allow multiple matchings of a node, but most standard matching problems only allow a node to be matched to one other partner. You can also consider matching problems in general graphs, they don't have to be bipartite. And there are decent algorithms for solving those types of problems, but they're a bit more complicated. The algorithmic story is much simpler in the bipartite case. 

And in practice, you do come across bipartite matching problems very often. So we're going to stick with bipartite problems for our discussion here. So here I have in bold, a matching of size four, I would like to match up as many people as possible. Would it be possible to create a matching of size five? And how would we algorithmically calculate the largest possible matching? It turns out, that's actually easy to do by converting my matching problem into an equivalent maximum ST flow problem with a very standard, very simple transformation. All I do is I add a dummy source node and connect that to all the nodes on the left hand side. 

I add a dummy sink node and connect every node on the right hand side to the sink node. Let's give every edge in this graph unit capacity, and then just solve a max ST flow problem. And if you do that, then basically the structure of the flow that you calculate corresponds to a maximum matching. If you look at the paths taken by your max flow as they kind of go through this bipartite region, then those paths kind of indicate the edges that your max matching would want to include. So a very simple way that you can solve a max matching problem using a max flow algorithm. If you use your standard augmenting path algorithm for max flows, the running time is actually not too bad either, because it takes linear time to find each augmenting path in your graph. And each augmenting path increases the value of the flow by one, because you have unit capacity edges. 

So basically each augmentation is going to push one additional unit of flow. The most amount of flow you can push in this graph is n units, because then all of your nodes would be matched up. So at most, n augmentations are necessary before you reach a maximum flow. So the total running time here is order of m times n. Slightly better running times are possible by using more advanced algorithms, but we're going to kind of advocate this as our standard way of solving a matching problem for now. There's actually kind of a nice way to look at the augmenting path algorithm for max flows in the context of finding a matching. So maybe I'll switch to the whiteboard and kind of animate that process, because I think it'll be helpful also in just understanding the augmenting path algorithm for max flows as well. 

So here's a very simple bipartite graph, and I would like to find a matching in that graph. There's kind of two choices. The best matching, of course, just matches straight across and matches both nodes. And there's also maybe a less good choice that matches along that one diagonal edge and only manages to match with one edge. So let's turn this into a flow problem and see what happens if I try and solve it with an augmenting path algorithm. So I introduce a dummy source node and connect that source node to the two nodes on the left hand side. I introduce a dummy sink node, and I connect that sink node to all of the nodes on the right hand side. 

And then I have the edges in the original bipartite graph. I direct those from left to right. So here's the flow network that I want to find a max flow in. All edges have unit capacity. And so I'm going to find an augmenting path in that graph as the first step. So maybe that augmenting path uses these edges that I'm highlighting in red. That's an augmenting path from the source to the sink. 

The max that I can push through that path is one, because every edge has unit capacity. So I'll push one unit of flow through that path. And that kind of effectively adds this diagonal edge to my matching, because now if you look at the tentative flow that you've built up, it uses that diagonal edge. So in the context of kind of looking at things from what I've done to my matching, I've kind of added one edge to my matching so far. Now what happens? Now I have to look for another augmenting path. And this is where things get interesting from the flow perspective, because remember that when you search for augmenting paths, you do so in the so-called residual network of the flow you've built up. 

So if I have, for example, an edge in my graph, and that edge has a capacity of seven, and I'm already sending a flow of three through that edge, what does that look like in the context of my so-called residual network? In the residual network, well, that edge only has a capacity now of four, because I can send only four more units additionally through that edge in the forward direction. And I also have introduced kind of a dummy backwards edge that has a residual capacity of three. If I send flow in the reverse direction through that kind of dummy residual edge, that basically corresponds to undoing or decreasing the flow that I've already sent in the forward direction. So when I'm looking for augmenting paths, this is kind of what I look at for this edge. I look at, you know, it has a capacity of four in one direction and three in the other direction. In my case, all of my edges have unit capacity. 

So here I have an edge that has a capacity of one. And so what happens when you send one unit of flow through that edge? Well, basically, in the residual graph, the forward residual capacity is zero, because I've used up, I've saturated that entire edge. So there actually is no forward residual edge anymore. All I have is a backward residual edge of capacity one, that if used, effectively corresponds to undoing the flow in the forward direction. So in unit capacity situations like this, sending a unit of flow through a unit capacity edge basically reverses the edge in my residual graph. And so if I now draw the new residual graph that I get after that first augmentation, I'll redraw it just to kind of make it not so cluttered. 

Then I have my, my augmenting path. And the edges on that path basically are reversed from what they were in the original graph. So those edges now flip backwards. And then I have all the other edges that were there to begin with. Okay, so now, that's my residual graph that I'm looking for an augmenting path again. And so now, what's the augmenting path that I find? I think there's only one augmenting path available to me. 

That's going to be the path that uses, I guess, these edges right here, it's going to go across the graph, then back along the diagonal edge that I've reversed, and then across the top edge and then into the sink. So kind of that the set of blue edges that I've just highlighted. So what is that effectively going to do to my flow? I am using one of those reverse residual edges, the diagonal edge is a reverse residual edge. So sending flow through that edge is effectively undoing the flow that I'd sent earlier. And so that actually is kind of like saying in my matching, I get rid of that edge. And instead, I'm replacing it with the two new edges that now carry flow in my in my graph. 

So every single time I augment, it actually increases the cardinality of my matching by one edge. Here, I took away one edge and added two edges, sometimes you might take away seven edges and add eight edges, but you're always, you know, adding one more unit of flow, which means somehow the matching has to have increased in size by one. So what you end up with sometimes is, you know, you can end up with kind of a large kind of wholesale shift. If I looked at an example that was kind of similar to this, maybe I had a matching that I had tentatively computed using these three edges, and by augmenting on a path that kind of followed all of those in the reverse direction, I might actually have have, you know, discarded those three edges and added, you know, four new edges to my matching. So that's kind of the consequence of what could have happened with an augmentation in a larger example. So every single time I augment, I add one more edge effectively to my matching. And so there's at most n augmentations. 

The total running time is order of m times n, because each augmentation just takes me linear time. Okay, just wanted to kind of drill down and highlight how the augmenting path algorithm looks. And it's also good just to kind of animate the augmenting path algorithm and understand how things work mechanically with the residual graph. Let's now also bring in some information that we learned about with max flows. We learned that max flow equals min cut, and that actually manifests itself in a nice way in the context of our bipartite matching problem. Because remember that we kind of, we solved a max flow problem to get a max matching. So maximum matching and max flow are kind of equivalent things. 

It turns out that min cut can be equilibrated with something called a minimum vertex cover in a bipartite graph. So there's a nice duality relationship here I'd like to highlight. In fact, in any graph, it turns out there's a weak duality relationship between the size of a maximum matching, that's always no larger than the size of what's called a minimum vertex cover or a minimum node cover. And in a bipartite graph, we actually have strong duality, we have equality between these two things, as a consequence of max flow and min cut. Min vertex cover we're going to see in a second is basically the same thing as a min cut in this situation. So what is a minimum vertex cover or a minimum node cover? A node cover is just a set of nodes in your graph that covers all the edges. 

So every edge has to have at least one endpoint in the cover. And so if you look at these red nodes right here, in the original bipartite graph, those are a node cover because every edge in my bipartite graph has at least one of its endpoints colored red. So we would generally like to find minimum node covers in graphs, that's kind of a very common optimization problem, like you'd like to put a minimum number of police stations in your city so that you cover all the roads in the city with police stations or something like that. So these red nodes corresponds to a minimum vertex cover in this example, in my bipartite graph. And that actually also kind of corresponds to what a minimum cut would be in my expanded flow network. So if I look at these red edges that kind of correspond to the red nodes in my minimum vertex cover, those red edges actually are a minimum cut. If I take those out, I've actually separated S from T. There's no path that goes from S to T anymore. 

And so I have a minimum cut of the same size as the vertex cover that I've found in my bipartite graph. So we can use the duality between max flows and min cuts to show another useful and helpful duality between maximum matchings and minimum vertex covers in bipartite graphs. That also helps us with establishing optimality of these things. If you'd like to prove that your maximum matching is indeed a maximum matching, you can do that by demonstrating a vertex cover of matching size. If you'd like to prove that a vertex cover is minimal, you can do that by finding a matching of the same size. So just a lot of nice relationships between these sorts of combinatorial ideas. Okay, moving forward. 

I'd like to highlight now how we can take the basic ideas we've just established, how to turn a matching problem into a flow problem, and kind of push things a little bit further and solve some slightly more interesting, more advanced problems. So think about the following load balancing problem. I've got a set of jobs. Each job has just unit size. And I'd like to process those jobs on a number of machines, but because of the job and machine configurations, each job may only be processable on some of the machines. And so I have a bipartite graph whose edges tell me which jobs can be processed by which machines. And what I'd like to do is come up with an assignment of jobs to machines that is somewhat equitable. 

It doesn't overload any one machine to a huge extent. And so I would like to minimize the load on the most highly loaded machine. That's a very common objective in load balancing. And very easy to actually extend our previous kind of idea of turning this kind of matching problem into a flow problem so that we can solve this. So what I'm going to do here is create a dummy source node on the left-hand side, connect that to all the jobs with unit capacity edges. But then on the machine side, I'm going to connect all the machines to a dummy sink node with edges having some common capacity T. So here I'm kind of making a guess that hopefully If I give all the machines capacity T, that's enough to schedule all of the jobs. 

So I'm going to find a max flow in this network. And if that flow is able to saturate all of the edges out of the source, then that flow will give me a valid assignment in the bipartite region here between the jobs and the machines where every single machine is assigned at most T jobs because I can send T units of flow into each machine that then gets routed into the sink. But I can't send more than T units of flow. I can't assign more than T jobs to each machine. If an assignment is possible, then the value of T that I had guessed was definitely high enough. So maybe I can try decreasing the value of T and again, solving a flow problem to see if now I can find an assignment that uses even less capacity with all the machines. And so the structure of my solution here would be a binary search over the value of T. 

And at every step, I'm going to solve a flow problem to see if the particular value of of all the jobs is possible. So there are lots of related problems that we can still solve using this standard kind of flow-based transformation. In fact, if we want to kind of open up the space of problems even more that we're talking about, we can kind of generalize the problems along a couple of dimensions. So in the last slide, we talked about how we can start thinking about matching not just unit-sized things to unit-sized things, but matching maybe non-unit-sized things to unit-sized things. So maybe when students arrive at a university, they get matched with dormitories and every student can only be matched with a certain subset of dormitories based on maybe their gender or maybe which class they're a part of. And so here I'm matching unit-sized things with non-unit-sized things. Every dormitory has a capacity of how many students it can accept. 

And so the flow network for solving this problem would look very similar to the one on the previous slide. I'd have a dummy source node connected to all the students with unit capacity edges, and I'd connect all the dormitories to a dummy sync node with capacities that reflect the capacities of the dormitories. And a max flow would basically allow me to find an assignment of students to dormitories or realize that such an assignment is not possible. I could even consider assigning non-unit-sized things to non-unit-sized things. Maybe I have jobs of various sizes and I'd like to assign those to machines of various capacities. And in this situation, you could even further subdivide based on whether the jobs can be split up and partially assigned to several different machines or whether each job has to be assigned kind of unsplittably in its entirety to just one machine. So there are lots of different variations you can consider based on kind of one-to-one versus one-to-many and whatnot. 

It turns out that our standard flow transformation by turning this into a flow problem, that basically just works for almost all of these, except for the unsplittable version of the many-to-many problem. That actually ends up being an interesting NP-hard problem, actually one I've looked at in my own research quite a bit. So as long as you're not trying to insist that the jobs are assigned unsplittably, everything works well and our standard flow transformations can handle most of these types of problems. Another direction that we can generalize along is instead of just trying to assign as many people as possible, instead of trying to match as many people as possible, we can now introduce costs or values into the picture and try and ask for a matching or an assignment of minimum cost or maximum value. So let's go back to the one-to-one example here. Suppose I associate with every edge cost that tells me how much it costs me to assign someone on the left to someone on the right. And in this situation, maybe I'd like to match everybody, but at minimum cost, or maybe I assign a value to every single node, to every single edge, sorry, that indicates the value that I get from a particular pairing. 

I'd like to find a matching of maximum total value. So there are a lot of interesting problems we can look at that have kind of costs and values associated with the matching, the assignment that I come up with. And that's the direction that our discussion is going to head now. So we're gonna now consider another very fundamental problem called the minimum cost perfect matching problem. A perfect matching is a matching where everybody's matched. So the input here is going to be a bipartite graph with every edge present. So it's not gonna be like the graphs we had before where only some of the edges were present in the bipartite graph. 

All of the edges are present, just some might be very expensive to use. And so I wouldn't want to include them in my matching. And so what I'm gonna do to describe my input here is I'm gonna provide a matrix that basically tells me all of the edge costs in my bipartite graph. So the IJ entry in this matrix tells me the cost of matching node I on the left with node J on the right here. So matching node two with node one costs me $5. And so I have a five in the two comma one position of my matrix. Given this input, I would like to find a perfect matching, a matching where everybody's matched, but at minimum total cost. 

And it turns out that this type of problem shows up in a very wide range of areas of practice. Just to give a couple and focusing on kind of the areas related to the data analytics, which has kind of been an area of emphasis in this course. Suppose you have two frames from a video in which you're kind of tracking objects. Maybe I have cells under a microscope or I'm tracking people in a video as they walk across an intersection or something. So the objects in my video move. And so if I look at these two different frames, then I would like to figure out the most likely correspondence between the objects in the first frame and the objects in the second frame. And so that's a very obvious sort of matching problem where you can make a bipartite graph describing the nodes on the left-hand side or the objects in the first frame, the nodes on the right-hand side or the objects in the second frame, and the weights or the costs tell you something about how similar were those objects or how close are they in the two frames. 

And an optimal matching would tell you maybe the most likely correspondence between those objects, kind of which object in the first frame is most likely to correspond to an object in the second frame. This kind of more abstractly can be a nice way of just computing similarity between objects. Like when we talked about dynamic programming and edit distance, we advocated that a nice abstract way to calculate the similarity between object A and object B is to calculate like the minimum cost of transforming A into B. Another nice way to calculate the similarity between A and B is to find the optimal kind of correspondence between the sub parts of A and the sub parts of B. If A and B are kind of made up of little sub objects, then you can use a matching problem to find the correspondence between those objects and thereby compute the similarity between A and B. A similar situation to that might be with clusterings in graphs. If I have clustering A and another clustering B, how similar are those two clusterings? 

That could actually give you a proxy for how similar are the two graphs. You could find the clusterings in the graphs and if the clusterings are similar, maybe the graphs were similar. And again, you can use a matching problem to find the best possible correspondence between the clusters in clustering A and the clusters in clustering B. That's kind of what I've shown here on the right-hand side. This is a figure I've stolen from a master's thesis of one of my students from a few years ago, where she was looking at the way a clustering in a network changed when you added more and more clusters. So if you only have two clusters, then what happens if you add a third cluster to the mix? And then what happens if you add a fourth cluster to the mix? 

It's kind of interesting. We were looking at the stability of the clustering. So if you add a new cluster, does that basically kind of cannibalize one cluster and turn it into two? Or does it completely blow up the clustering structure that you have and everything gets reassigned in a very ad hoc way to the new set of clusters. And so you can visualize these sorts of things with kind of these alluvial or Sankey diagrams that matchings actually could play an important role in kind of how you lay these out for visualization. But lots of applications of these matching problems to kind of figure out the optimal correspondence between elements in two different sets. This can kind of be a nice tool for your toolbox. 

If you're a data scientist, this is maybe not a common tool that data scientists know about because you have to have a pretty deep level of algorithmic knowledge to kind of understand how matching algorithms work. But I think that matching algorithms are kind of maybe an underappreciated tool that has a lot of applications in areas like data science. So finally, let's talk about how we can algorithmically approach the min-cost perfect matching problem, also called the bipartite assignment problem. It goes by several different names. So it turns out we can actually use the same flow-based transformation that we had used before. We basically take our original problem, and I've reused my figure from before, which is kind of misleading because here you do have the same number of nodes on the left and on the right, as opposed to what I had on my figure. But we introduce a dummy source node and a dummy sink node. 

We connect the source to everything on the left. We connect everything on the right to the sink node. But now what do we want to compute? Instead of finding a max flow, which would just try and match as many people as possible, well, we know everyone can be matched because the bipartite region of our graph is a complete graph. There's every single edge is present. Just some of the edges are more expensive than others. It turns out that what we want to find here is actually a minimum-cost max flow. 

We still want to find a max flow that'll force everybody to be matched, but we want to find a max flow whose total cost is minimized. The cost of a flow is just the sum of the costs of the edges that we're using in our flow, in the unit flow case. And it turns out that kind of what you might hope would work does work in this situation. You can solve this problem using our straightforward augmenting path algorithm. The only difference is that as you find augmenting paths in the residual graph, you use a shortest path for each augmentation. So you use Dijkstra's shortest path algorithm to find each subsequent augmenting path. And it takes only n augmenting paths, just the same as before, because every augmenting path basically increases the size of your matching by one, and at most you have n things to match. 

And so you basically end up with a running time of kind of n times Dijkstra, because each augmenting path requires the application of Dijkstra's shortest path algorithm. If you remember back to our discussion of Dijkstra's shortest path algorithm, the running time could vary based on the type of priority queue that you used with Dijkstra's algorithm. Here, we have basically a dense graph. Every edge is present in the bipartite region of this graph. And so it makes sense to use the version of Dijkstra with just an unsorted array as the underlying priority queue. So basically a trivial priority queue with Dijkstra's algorithm. In that case, the running time ended up being just n squared because we have n squared edges in our graph. 

And so each invocation of Dijkstra's algorithm will take n squared time, and we do n augmentations. That's why the total running time is gonna be n cubed. So there's actually one additional subtlety though, which is great because it lets us review some of the important topics that we've talked about so far in this module. When we augment through this graph, remember that augmenting on an edge kind of introduces a reverse residual edge. That actually creates kind of a threat in terms of possibly introducing negative cost edges into our graph. And so let me draw a picture of that just to highlight how that can happen and how we can address that using ideas we've already talked about. So let's go back and look at this example of an edge in my graph and say it has a capacity of seven and it has a flow on it of maybe three. 

So in that situation, remember in the residual network, what does that look like? That looks like an edge that has now a forward capacity of only four because I can push four additional units of flow through that edge and I've introduced a reverse residual edge, kind of a dummy residual edge in the backward direction that has a capacity of three. And using that edge corresponds to kind of undoing the three units of flow that I've sent in the forward direction. What if I now have costs in this picture? What if my original edge cost me $2? So in a flow problem, it's quite common to have edge costs as well as capacities. So if I have a $2 edge and I'm sending three units of flow through it, that would be six total units of cost usually in a cost-based flow problem. 

But what happens in the residual network here? My forward edge still has a cost of $2, but the reverse residual edge, since using that is equivalent to removing flow that I've already sent, the cost of that edge ends up being negative $2, the negation of the forward cost. So even if your network has non-negative costs to begin with, which we typically assume with these sorts of assignment problems, you can easily end up with negative edge costs the minute you start augmenting because that introduces reverse residual edges of negative cost. This is one situation where you can end up with negative costs in graphs. Remember when we talked about shortest paths, we said that in many areas of practice, you're lucky you have non-negative edge costs and so you can use Dijkstra's algorithm. However, if you have negative edge costs, you have to fall back to using much slower shortest path algorithms like the Bellman-Ford algorithm, for example. And that's a concern here because after you start augmenting, you're going to introduce negative cost edges into your graph, these kind of dummy reverse residual edges. 

And it seems like subsequent augmentations are gonna be much slower because they're gonna require the use of slower shortest path algorithms. We still want to augment on shortest paths, but now we have negative edges in our graph. So how do we deal with that? Remember the trick that we used when we talked about Johnson's all pairs shortest path algorithm, which was converting to the use of reduced edge costs. Remember that if you've already solved a shortest path problem, you've assigned cost labels to all the nodes of your graph and you can use those to reweight the edge costs in your graph in such a way that all of the edge costs are now non-negative. And that's gonna be the saving grace here. That's gonna be the trick that we use to keep our edge costs non-negative because we basically find a shortest path, we augment along it. 

And you think that, oh no, that might introduce negative edge costs. But as long as we kind of use the cost labels from the shortest path algorithm that we just solved, if we use those cost labels to reweight our edges and calculate reduced edge costs, then that gives us a new set of edge costs that are gonna end up being non-negative. I guess another way to think about that, just kind of with the goal of reviewing kind of what we know about reduced edge costs is if you solve a shortest path problem, and I think we had a figure that looked just like this when we talked about all pairs shortest paths. If you solve a single source shortest path problem, that gives you a tree of shortest paths leaving the source node. And if you look at the reduced costs of all the edges in your graph, based on the cost labels that you compute from that shortest path algorithm, the reduced cost of all the edges on that shortest path tree, they're all gonna be zero. So shortest paths. actually have reduced cost zero. They have no slack in them. 

They're tight edges, so to speak. Only edges that are not part of shortest paths have a positive reduced cost. So that's one way that you can actually tell if an edge is part of a shortest path is if it has a zero reduced cost. So if you calculate shortest paths and then use your cost labels that you get to kind of change your edge costs to being reduced costs, then now you have non-negative edge costs and the cost of the edges on shortest paths, they're zero. And so one nice feature about that is if you augment, remember we augmented on a shortest path, that means that in terms of reduced cost, we're only gonna be sending flow through edges that have zero reduced cost. And so the reverse residual edges are gonna have zero reduced cost because negating zero gives you zero, at least as long as you only look at things from the perspective of using reduced cost. That's the original cost and have offset by plus or minus the cost labels at the end points of the edge. 

And so that's really the trick here that makes things run quickly. We find a shortest path, we augment on it, and then we transform our edge weights so that they're now reduced costs and therefore non-negative. And then again, that lets us use Dijkstra's algorithm to find the next augmenting path. Because remember that using reduced edge costs preserves shortest paths, paths that were shortest paths beforehand, stay shortest paths when you transform your edge costs into reduced edge costs. And so that lets us use Dijkstra's algorithm for every single iteration of our augmenting path algorithm. We just have to keep recomputing reduced costs as we go, because the structure of our graph keeps changing. Our residual graph edges are being removed and added because every time you augment, that's basically flipping edges around in the residual graph. 

And so that's kind of changing the structure of the graph. So you do have to recompute reduced costs after every invocation of your shortest path algorithm to keep your reduced costs up to date. So that is basically the structure of the algorithm that we're gonna use for calculating a minimum cost, perfect matching in a graph of bipartite assignment here. This algorithm goes by the name commonly of the Hungarian algorithm. It was named this kind of to acknowledge the early contributions of several prominent Hungarian mathematicians in the area of matching. There's kind of a strong tradition of excellence in graph theory and matchings from that area of the world. So if you're trying to look up this algorithm, look up the Hungarian algorithm. 

It runs an N cubed time because it's basically N times Dijkstra's algorithm. And each invocation of Dijkstra's algorithm runs an N squared time. My one gripe maybe with how the Hungarian algorithm is described, if you look it up online, for example, is that very few places actually tell you kind of the underlying insight of why the algorithm works. It's a very mysterious algorithm. If you just read through the description of the algorithm, it's not clear at all why it works. And the insight is that it's really just augmenting on shortest paths in a residual graph of kind of an expanded network that's kind of a flow network that you've created from your assignment problem. So knowing about flows and shortest paths and reduced costs gives you kind of the underneath the hood view of why this is actually working. 

And usually you don't see that if you just look up a description of the algorithm or look at the code that represents the algorithm. It's not actually that hard to implement. It's a fairly short algorithm by any code. So just wanted to mention that as kind of our final algorithm of the module. So now we can solve a variety of different types of matching and assignment problems. And hopefully that will be useful in applications you encounter as you move forward with your computing career.