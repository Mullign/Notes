![[Module 8 overview.pdf]]
Welcome to Module 8, where we talk about graph algorithms, a very prominent subfield of study within the world of algorithms, and for good reason, because graphs themselves are so ubiquitous within computing. They're very powerful models capable of representing almost any type of network. And so the algorithms that we learn about in this module will have very broad applicability to a very wide range of different situations involving graphs and networks. In this video, we'll briefly talk about some of the major graph problems that we'll be addressing in this module, as well as some high-level terminology and notation, just so that we can talk about graphs using the right words. So we've actually seen graphs earlier in this course on a couple of occasions, but let's formally introduce them here. So a graph is basically a modeling construct within computing that we use to model things that have a network structure. And so a graph consists of a set of nodes and a set of edges that connect between pairs of nodes. 

Sometimes nodes are called vertices. I like to call them nodes because that's a simpler word. And sometimes edges are called links or arcs. Sometimes edges are directed, so they are drawn with an arrow. They have a direction from one node to another node. And sometimes edges are undirected. So in a transportation network, for example, a directed edge might correspond to a one-way street. 

And so a graph that's built from directed edges is called a directed graph, or sometimes a digraph for short. A graph that's built from undirected edges is called an undirected graph. And by default, the word graph itself typically implies just an undirected graph. So if you're talking about a directed graph, usually you want to use the word directed just for clarity. So graphs are everywhere, as I mentioned, within computer science. They are capable of modeling a very wide range of different types of networks, social networks, communication networks, transportation networks. Lots and lots of different types of problems can be modeled with graphs and addressed with graph algorithms. 

There are lots of different special types of graphs. In fact, there's an entire branch of mathematics called graph theory devoted to the study of properties of different types of graphs. And so if you really want to get into the weeds in the study of graphs, have a look at graph theory. There are some very esoteric results in the field, but also some very prominent and very intriguing results in the field of graph theory. We're focused a little bit more on the practical side with our study of graph algorithms. So we might bring in a couple of results from graph theory when useful, but we'll mostly focus on the algorithmic side of graphs in this class. So on the algorithmic side of things, one thing to take note of is that there are different flavors of graphs, different special types of graphs out there, and some of them are easier to handle from an algorithmic perspective. 

So a lot of times a problem might be difficult to solve on a general graph, but maybe it's easier to solve on a special type of graph, like maybe a tree or a bipartite graph. So just to give you a sense of a couple of common special cases of graphs that you might encounter. So a path is a very simple type of graph. It's just a series of nodes connected by edges in a path. A cycle is the same thing, except there's now an edge from the first to the last node that closes it up into a loop. A tree is a very common sort of graph, and in fact, we've encountered trees a lot in the world of data structures. Sometimes we differentiate between trees being used for data structures and trees in the sense of a graph by calling a graph tree sometimes a free tree. 

But a tree is a type of graph that is completely connected, so every node can reach every other node via some path, and there are no cycles in the tree. So it's kind of a minimally connected type of graph. And trees in the world of graphs typically don't have a root node designated like you often see with data structures. So a very common pre-processing step when you're dealing with trees as graphs is you pick a node and designate that node as the root, so you kind of root the tree, and that orients the tree so that now you can talk about for every node who is its parent and who are its children, and think of it a little bit more like we've been thinking about trees from the world of data structures. So trees are very prominent objects when we talk about graph algorithms. Lots and lots of graph algorithms get much easier when you try and run them on a tree, because there are no cycles to deal with and things like that. Bipartite graphs. 

Also a very prominent special case of graphs, a bipartite graph, basically the nodes can be partitioned into two sets. We sometimes draw them so that one set is on the left and one set is on the right, and all the edges go from one set to the other. You don't have any edges that connect from a node on the left to another node on the left. Sometimes these are called two-colorable graphs, because you can color the nodes with two colors so that every single edge connects two different colors. There's a very prominent type of algorithmic graph problem called a coloring problem, where given a graph, you would like to color its nodes with usually the smallest number of colors possible so that every edge connects two differently colored nodes. There's a famous result in graph theory that planar graphs, which we'll talk about in a second, can always be colored with at most four colors. But it's actually a very hard problem in general to figure out the minimum number of colors needed to color a graph. 

One special case being with bipartite graphs, we can pretty easily figure out in linear time if a graph is bipartite or not, i.e. two-colorable. So bipartite graphs are used for a very wide range of applications. We'll talk about some of them in a few minutes, mostly involving things like matchings, where you're trying to match the elements on the left-hand side with the elements on the right-hand side, and where edges tell you valid pairs that you can match up. Another common type of graph that you'll sometimes encounter is a planar graph. So a planar graph is a graph that can be drawn in the two-dimensional plane in such a way that none of its edges cross. And so this graph that I've shown right here actually is a planar graph. This also highlights that there's an important distinction between what the graph actually is. 

A graph is really just a combinatorial structure. It is the nodes and the connections between the nodes that make the edges of the graph. How you draw the graph is kind of irrelevant to that. A graph can be drawn in many different ways. And a graph is planar if there is a way to draw it such that there are no edges that cross. So I could, for example, take node 3 and maybe kind of move it up above node 1. And if you do that, then you wouldn't have any crossings between the edges. 

So a graph is planar if there is a planar drawing of the graph that is possible. Another thing about how you draw graphs is you don't have to use straight edges. You can draw curved edges. There's an entire field of study involving graph drawing. How do you make kind of aesthetically pleasing drawings of graphs? What sort of algorithms can you use to lay out a graph geometrically on a two-dimensional grid so that the edges tend to have some nice structure to them? Part of that might be testing if a graph is planar, which you can also do with efficient algorithms. 

So there are lots of special types of graphs out there. And many of these admit faster algorithms if you're trying to look for ways to speed up your different graph algorithms. There are lots and lots of problems out there involving graphs. And we're going to talk about some of the basics in this module. So maybe let's review just a couple of maybe the most prominent types of problems on graphs. So one type of problem that we will talk about quite a bit in this module is the shortest path problem. So the shortest path problem is just how to get from point A to point B in a graph as efficiently as possible. 

So typically, you have a weighted graph or a graph with every single edge having an associated cost or weight. A lot of times in graphs, we associate numbers with nodes or with edges. A lot of times those numbers are called the weights of the nodes or the edges. I like to usually, instead of using the word weight, I'll usually use the word cost if we're trying to minimize something because cost implies that you want to get the cost as low as possible. Or maybe I'll use the word value if you're trying to maximize something because clearly value implies a quantity that you're trying to maximize. So here we have a weighted graph, a graph with costs on all of its edges. And you would like to find a minimum cost path from one designated node to another designated node. 

There are lots of variations on shortest path problems. Maybe you want to find the shortest path from one node to every other node or from every node to every node. Maybe you have versions of a shortest path problem in an unweighted graph where you don't have costs on the edges. You just like to find a path that involves the smallest number of edges possible, just a minimum hop path. So lots of variations on shortest path problems. We've talked a little bit about minimum spanning trees earlier because we talked about Kruskal's algorithm for finding a minimum spanning tree. It's a famous greedy algorithm. 

So we won't actually spend a ton of time in this module talking about minimum spanning trees, but it's another very prominent graph problem just good to know about. So the idea here is I give you a graph with costs on its edges, and you would like to find a spanning tree embedded within the edges of the graph. So find me a subset of the edges of the graph that forms a tree, so no cycles and fully connected, and that tree has to span the entire graph. So it has to connect together all of the nodes of the graph. So if I want to find a minimum cost way to build a power grid that connects together a bunch of cities, then this might be the problem I want to solve. So minimum spanning tree is a very famous problem, especially from an algorithmic perspective, because we know how to solve it in many different ways very efficiently, for example, with Kruskal's minimum spanning tree algorithm. Matchings. We will spend a good bit of time in this module talking about matching problems. 

So these are typically posed on bipartite graphs, although they can also be posed in a non-bipartite setting where you just want to match up the nodes in the graph. So in a bipartite setting, you have two sets of nodes. You know, the set on the left, we call that maybe A, and the set on the right, which we'll call B. And you would like to match up the nodes from A with the nodes in B. And edges tell you valid pairs of nodes that you can match up. Lots and lots of different objectives in this setting. So the most common, the simplest one is probably just match as many nodes as possible. 

And each node can be matched to at most one other node. You can't have multiply matched nodes. There are some versions of the problem that do allow multiple matchings, and we'll talk about those. But the simplest and most standard versions of matching problems basically involve every node can be matched along an edge to at most one other node. And you want to match up as many people as possible. So we'll talk about some algorithmic approaches for approaching matching problems. And then finally, we'll talk about kind of a very broad class of problems involving flows in networks. 

They're related closely to what you could call routing problems in networks. So here, the numbers on the edges of your network typically indicate capacity for how much of some commodity you can ship through that edge. If this is maybe a transportation network or a communication network, how much data can you send in terms of bandwidth through a given edge? Sometimes you also have costs associated with edges for how much it costs to ship things through a given edge. And these sorts of flow and routing problems typically involve commodities that you'd like to optimally route through a network. And you're given source nodes where the commodities start out and sink nodes where the commodities have to end up. And you'd like to find a way of routing usually as much as possible through the network or routing a given amount of commodity through the network at minimum cost. 

There are lots and lots of variations on those types of problems. But basically involving how you can route commodities through a capacitated network in the most efficient way possible. There are lots of problems on graphs that also you can study from more of kind of a data analytics perspective. A lot of times you're given a data set that itself has an underlying graph structure. So maybe you're studying social networks, which consists of nodes and edges that represent friendships between people. And you'd like to solve some sort of problem on that social network, maybe find clusters of well-connected individuals or something. There are lots of interesting problems that you can pose on graphs from kind of a data analysis perspective. 

So just to highlight a couple, and we'll see a few approaches for dealing with these problems later on in the module. So similarity or connectivity questions. So how well connected are a pair of nodes in your graph? So this is especially interesting if a pair of nodes is not directly connected by an edge. So in this graph down here, X and Y are not directly connected, but you might still be interested in measuring how similar they are via indirect connectivity. So you could, for example, use a shortest path problem as a method of measuring connectivity between two nodes. Now, maybe you would state that two nodes are similar if they're connected by a short path. 

But that may not be the world's best measure of connectivity, because even if two nodes are connected by a short path, you might say that they're better connected if there are lots of paths that connect between those two nodes. So many short paths. And so we'll talk a little bit later in the module about ways we can kind of characterize well-connectedness between nodes. well-connected. You can also talk about clustering problems in graphs. So clustering is a very prominent problem. You can study it in generic data sets, but also in graph-structured data sets. Can you break up the nodes of a graph into well-defined clusters in a way such that you have a lot of edges within clusters and not so many edges that go between different clusters? 

So lots of different ways to think about and approach those types of problems. And then finally, I want to highlight one other common problem in graphs from a data perspective, which is centrality. So this is kind of identifying nodes that are well-connected in the context of the graph structure that we're talking about. So in a social network, for example, these would be your influencers. In a network that kind of talks about maybe a disease spread, a central node would be a node where if a disease originated there, it would typically spread very rapidly through the rest of the network. Maybe in terms of information propagation in a social network, if you start a rumor at a very well-connected central node, that rumor will propagate very readily towards the rest of the network. So measuring centrality is extremely important. This is the key problem that Google basically solved with its Google PageRank algorithm, because you can think of the World Wide Web as a giant graph. It's a directed graph where every single node is a web page, and every single node has directed edges out to all the web pages that it links to with hyperlinks. And highly central nodes in that graph are web pages that have some sort of high degree of importance. 

They're the ones that you'd like to return as the result of a query. So lots and lots of interesting questions, highly relevant to a wide range of different applications, that you can think about from a kind of a data analysis. So just a couple other quick points on terminology. So we've talked about nodes and edges. We also want to talk about paths and walks in graphs. So a path is basically kind of what it sounds like. It's just a series of nodes connected by edges. And we use the word walk for the same thing. It's a series of nodes connected by edges. 

The difference is that a path is usually considered to be simple. It doesn't repeat the same node, so it doesn't have any loops in it. Whereas a walk is allowed to contain, you know, multiple copies of the same node, so it can actually loop around a bit. So just a small distinction between path and walk. In a directed graph, paths and walks are usually interpreted as being directed entities. And so you're only allowed to take a path if the edges you're following on that path are consistently directed in the direction that you're following them. So you can only follow a one-way street in the appropriate direction. All right. We've talked about what a cycle is as a special case of a graph, but it also, you can talk about a cycle as basically just a part of a graph if you just have a loop that you're following at the cycle. And then we have a notion of what's called a connected component in a graph. So graphs don't have to be fully connected. A graph could consist of multiple pieces that are disjoint from each other. 

Those are called its connected components. So within a connected component, you can walk from any node to any other node. You have full connectivity. So there exists a path of edges that will take you from any node to any other node within a connected component. But if you have two nodes in different connected components, there's no way for one to reach the other, because the components don't have any, there's no edge that connects between those connected components. So one of the first algorithms that we will talk about is one that basically discovers the connected components of a graph. In fact, if we go back and look at special graph types, we talked about trees. If you have a graph that consists of multiple disjoint trees, that graph is called, not surprisingly, a forest. So each tree would be a connected component of that graph. 

So what are some other kind of just terminology, common bits of terminology that are worth knowing about? So in a directed graph, the notion of a connected component turns into what's called a strongly connected component. So two nodes are in the same strongly connected component if you can get from one to the other and back on directed paths. So you have kind of complete directed connectivity between all pairs of nodes in a strongly connected component. And so one nice thing about graphs is you can take any graph and partition it up into its connected components. You can take any directed graph and actually partition it up into its strongly connected components. And we'll see how to do all of that algorithmically later on in the module. 

How do you actually represent graphs in memory? That's important to think about. There are two very common ways that we do this. One is called using an adjacency matrix, which is basically just an n by n matrix where n is the number of nodes in the graph. And the matrix has a one to indicate the presence of an edge between one node and another here. So here I have an edge from one to two. So that means there's a one in the one comma two position of my adjacency matrix. And so you can use these matrices to represent directed or undirected graphs. I guess in an undirected graph, the matrix would be symmetric. So you'd have an edge from one to two if you also have an edge from two to one. Probably the most common way that we represent graphs is using what are called adjacency lists. And this is a much more space efficient way of representing graphs where you basically for every node just keep a list or an array or some more elaborate data structure like maybe even a hash table that basically tells you all the neighbors of that node. And this could be used for directed or undirected graphs. In directed graphs, sometimes you have a list of your outgoing neighbors or your incoming neighbors. Maybe you keep those two lists separate. But adjacency lists are nice because the amount of space required to store a graph using adjacency lists is just proportional to the number of nodes and edges in the graph. And so you can store very, very large graphs using adjacency lists very efficiently in memory. They're ideal for what we call sparse graphs. There's a good distinction to be made between a graph being dense and a graph being sparse. So a dense graph has a lot of edges. The most edges you could have in a graph would be n choose two if every pair of nodes is connected by an edge. And so that would be a quadratic number of edges on the order of n squared edges. That would be a fully dense graph. We sometimes call a graph a complete graph if every possible edge is present in the graph. On the other hand, most graphs in the real world tend to be quite sparse. I mentioned earlier the World Wide Web. You know, there are billions and billions of nodes, web pages in the World Wide Web, but not too many outgoing links from every single web page. And so for very large and very sparse graphs, the adjacency list representation is much preferred in terms of its memory footprint versus an adjacency matrix. 

So good to know about the distinction between dense and sparse graphs. A lot of times that's important also for algorithm choice. There might be some algorithms that work better in dense graphs and some algorithms that work better in sparse graphs. All right, so now that we've covered the basics of what graphs are and what words we use to talk about different features of graphs, let's briefly review some of the topics we'll talk about in this module. So we will cover a lot of common algorithms for analyzing graphs. We'll start with basic algorithms for analyzing connectivity. And the big algorithm here is called depth-first search. It's kind of a Swiss army knife in terms of it can solve about half a dozen common problems in the world of connectivity. Are nodes x and y connected by a path? If so, find a path from x to y. 

Determine the connected components of a graph or the strongly connected components of a directed graph. Does the graph contain a cycle? There are lots of different kind of basic connectivity questions that depth-first search can very easily answer. So we'll talk about those first. We'll then switch gears and talk about finding shortest paths in graphs, not just whether two nodes are connected, but what's the shortest path to get from one node to another node. And there are lots of flavors of shortest path problems out there. We'll talk about maybe half a dozen kind of core algorithms that are very common for finding shortest paths, depending on the nature of the problem. If you're trying to find a path from say one node to every node, or from every node to every node, depending on whether your edge costs are non-negative, or could have negative values, or if edge costs are all one, there's a lot of special cases of shortest path problems. You can also think about these problems in special types of graphs, like what about a shortest path problem in a directed acyclic graph? We talked about that when we talked about dynamic programming, for example. So we'll visit the shortest path problems, spend a bit of time talking about those. We will then do a coding demo where we implement basically depth-first and breadth-first search to highlight how easy these algorithms actually are to implement in code, and how they can be used to solve very simple types of graph problems. 

And then we'll switch gears and talk about flows and matchings. So we'll focus on the famous max flow problem, which we briefly introduced earlier when we talked about discrete optimization, because it has a famous dual problem called the min-cut problem. And we'll see how flows can be used to solve matching problems in a wide range of other types of applications. And then finally, we'll have our optional enrichment lecture, where we'll talk about what we call a stable matching problem, or a stable marriage problem sometimes. It's another really wonderful example of kind of the intersection of algorithms and game theory. The idea here is, again, you're trying to find a way to match up entities in a bipartite graph, but instead of characterizing the quality of a matching in terms of kind of a numeric objective function. Like maybe it gives you three units of value if I match these two individuals up, and I want to maximize the value of all the pairs that I match up. Instead of that, all I have are preference lists. So my first preference is to be matched with this person. My second preference is to be matched with this person. And that leads to a very different notion of what makes a matching good. You have to use kind of ideas from game theory to characterize what a good matching is in that sort of preference-based setting, versus kind of your more traditional setting from optimization, where you have kind of an explicit numeric objective that you're trying to minimize. So these are some of the main topics we'll talk about in this module. And then finally, accompanying this module will be a second design and analysis homework assignment. It will be similar to the first in terms of, you know, you'll have a list of a short number of problems, fairly open-ended, that you will want to design algorithms to solve them, analyze their running time, and such. The structure will be very similar, and of course you can use all of the tools and ideas that we've talked about to date in the class to address those problems. The two main topics that will be on this assignment, maybe receiving a bit more emphasis because they're recent topics, that weren't on the first analysis assignment would be discrete optimization and graphs. So you can expect to probably see those topics being very prominently featured in several of the problems that we include in that assignment. So hopefully a lot of fun things to talk about in this module, and well, let's go get started.