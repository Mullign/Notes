![[Topological Sort and Strongly-Connected Components .pdf]]
Let's now continue our discussion of extensions of depth-first search working towards what I think is one of the coolest and possibly least obvious algorithms in the realm of fundamental graph algorithms. It's going to be an algorithm for partitioning up the nodes of a directed graph into its strongly connected components. In order to get there we're going to start with the topic of topological sorting of a directed acyclic graph or DAG. We've actually I think seen DAGs a bit earlier in the preceding module. We showed how to find the shortest path from one node to every other node in a DAG in just linear time using dynamic programming. And in fact dynamic programming is very closely related with DAGs because oftentimes the sub-problem relationships in a dynamic program have a DAG structure because large sub-problems to solve them you have to first solve smaller sub-problems on which they depend. And so the dependencies between sub-problems if you kind of model that as a directed graph you end up with a DAG. Lots of other applications involve DAGs. For example scheduling applications when you have precedence constraints where you have to do one task before you do another task that ends up being a DAG because you typically don't have cycles in your precedence constraints otherwise that would make it impossible to perform the tasks. So DAGs are a very important class of directed graphs and anytime you see a DAG a very common pre-processing step algorithmically involves topological sorting of that DAG. 

Ordering the nodes of the DAG in a so-called topological ordering. What that means is basically coming up with an ordering of the nodes in your DAG such that all the edges are directed in one consistent way say from left to right. So this is a short example of a DAG here on the slide and one of its topological orderings that shows you how you can get dressed in the morning. So these are the precedence constraints about which articles of clothing you have to put on before putting on other articles of clothing. There can be multiple valid topological orderings for any DAG but this is a very useful pre-processing step just as sorting is a useful pre-processing step for many other algorithm questions. Remember we often sort first to ask questions later. If you're dealing with a directed graph a directed acyclic graph then topological sorting is almost always a kind of a pre-processing step that you should keep in mind especially because we can do topological sorting very quickly in just linear time. This is actually an if and only if a graph a directed graph can be topologically sorted if and only if it is a DAG because if there is a cycle it's impossible to come up with a topological ordering because there would always be one edge that points the wrong way in that cycle. So how do we topologically sort? There are a couple of ways we can do it. Let me tell you first how you can do it without depth-first search and then we'll kind of see how you can do it in a clever way with depth-first search. So one kind of very natural way to do topological sorting is you basically look for what you call a source node. That's a node that has no incoming edges only outgoing edges. That's a node that's kind of safe to put first in the topological ordering. So if we look at our example right here underwear is a source node because there is no prerequisite that has to be done before underwear. So that would be a safe node to put first. There are actually several source nodes here. We've actually chosen socks as a source node that we've put first in our topological ordering. So basically identify a node with no incoming edges a source node put it first and repeat and take that node out of the graph find a new source node and and so on. If you implement this carefully you actually can make it run in just linear time. Now a couple of interesting questions here. What if you can't find a source node? It turns out that your graph is a DAG if and only if it has at least one source node. DAGs always have source nodes. If you can't find a source node that means your graph has to have a cycle with it. And there's a very easy justification for that. So just pick any node in your graph. 

If it's a source node you're done. Otherwise if it's not a source node it has an incoming edge. So step backwards on that incoming edge and repeat. So basically what we're going to do is just kind of walk backwards arbitrarily through our graph. Anytime we find an incoming edge to our current location we step back along some such edge. And we keep doing that until eventually we hit a source node. If we don't hit a source node since our graph has only a finite number of nodes that means we have to have gotten stuck in a cycle. So basically a directed graph has to either have a source node or it has to have a directed cycle. So very easy to kind of prove that DAGs have source nodes. So this is one way that we could do topological ordering. I guess it would also give us another way of doing cycle finding in directed acyclic graphs. If you kind of try and topologically sort a directed graph and fail, the only reason you would fail is because the graph had a directed cycle. So most of the algorithms that we're going to talk about for topological sorting they can also be used to find directed cycles as a failure case. Okay now let's look at another way we can do topological sorting with depth-first search. And this kind of piggybacks on an extension that we talked about earlier for cycle detection where we actually record for every single node during the depth-first search process its so-called discovery time. That's kind of there's a global clock and every time we step on a node for the first time we record the time stamp of when we step on that node. We're also going to record the so-called finishing time of each node. 

So that's the time at which depth-first search is finally finished processing that node. It's visited everything reachable downstream from that node and finally steps out of that node. That's the finishing time of each node. And so we want to record basically the discovery time and the finishing time of every node during the depth-first search process. That's not going to add substantial overhead to the depth-first search process. Of the two of these time stamps it turns out that the finishing time is really the crucial one for topological sorting because it turns out that if you perform a full depth-first search of your graph then just outputting nodes in reverse order of finishing time is sufficient to do topological sorting. Why is that? Why is reverse ordering by finishing time sufficient to do topological sorting? So we'll see that in just a second but there's a couple ways you can actually make this into an algorithm that topologically sorts. One is that you can just print out a node when it finishes and that's going to print out the nodes in increasing order of finishing time so you'll get a reverse topological ordering when you do that. 

Or you can run depth-first search and calculate the finishing times of all the nodes. Finishing times are going to be very small integers so you could then sort as a post-processing step using something like counting sort and that would run in just linear time. So several ways you can actually use depth-first search to do topological sorting in linear time. So why is it the case that this reverse ordering by finishing time is a topological ordering? So there's one kind of key claim basically that we have to kind of establish and that will show us why this works. So suppose you're given a DAG and you look at some edge in that DAG from i to j. We would like to show that if you run the preceding algorithm where you sort in reverse order by finishing time this edge should end up pointing from left to right and that if that holds for every edge then we'll have a valid topological ordering. And so all we need to do is show that for every single edge in our graph, say from i to j, the finishing time of i has to be bigger than the finishing time of j. 

That means that the edges can end up pointing in the right direction once we order the nodes in decreasing order of finishing time. And so let's show that that actually does happen when you do a full depth-first search of a DAG and there's only two cases to look at. One is whether you visit node i first in your full depth-first search or if you visit node j first. So to look at those two cases let's switch over to the whiteboard. So I have an edge that goes from i to j in my graph and I'd like to show that the finishing time of i is going to end up bigger than the finishing time of j. So how do I do that? Well what if I visit node i first? If I visit node i first during the full depth-first search process then i is going to be kind of pending while I visit everything downstream from i and that includes j. And so during the process of visiting i I'm going to go off and visit j and everything kind of reachable downstream from j and then finally j is going to finish and then after that i is going to finish. So kind of visiting j and everything downstream of j that kind of happens as a nested sub-process of visiting i. So in that case indeed the finishing time of node i is going to end up bigger than the finishing time of node j. So what if the reverse happens? What if we during the process of our full depth-first search what if we visit j as the first of the two nodes between i and j? So in that case you know full depth-first search is going to from j go off and visit everything downstream of j. So visit all of the nodes that you can reach from j but importantly that set of nodes does not include i because otherwise there would be a directed cycle in your graph because there's an edge from i to j. So if there was a directed path from j back to i that would mean there's a directed cycle and there can't be such a cycle if we're assuming our graph is a DAG. So what that means is from j we visit everything downstream from j and then j finishes and then later on in our full depth-first search we again encounter i and visit everything downstream of i and then finally i finishes. So again in this case i finishes later than j. So in both cases the finishing time of i is bigger and so that if you come up with a topological ordering that is in descending order by finishing time that edge from i to j is going to point in the correct direction from the right. So that's why this algorithm actually works. So we have now a couple of ways of doing topological sorting in linear time one of which is conveniently based on depth-first search. So now let's turn our attention to the more interesting algorithm in this video. So this is the um the algorithm there's a couple algorithms actually that can do this. We're going to focus on one of them that kind of leverages what we've just talked about with topological sorting. So we would like to take a directed graph not necessarily a directed acyclic graph but just any directed graph and partition it into its strongly connected components. And this is kind of a generalization of what we do with undirected graphs. You know with an undirected graph you can very easily use a full depth-first search to partition it up into its connected components. It turns out that every directed graph can also be partitioned up into its strongly connected components and within any strong component you have complete directed connectivity. 

So every node can reach every other node within a strong component along a directed path. But if you look at two nodes that are in different strong components you only have kind of one way potentially directed connectivity but not both ways. Maybe you can reach one from the other but not vice versa. So actually if I'm going to skip ahead and just kind of show a picture here that tells you how you can think about the strong components in any directed graph and then we'll kind of come back to the algorithm itself. So if you take a directed graph and you do partition its nodes up into their strong components here's a picture of kind of what you end up getting. So I have like strong component one, strong component two, strong component three is boring it has just one node in it. But if you do that that means that within each strong component you have complete directed connectivity you can reach any node from any other node on a directed path. But what does the graph itself look like if you kind of regard every strong component itself as one giant node as like a super node. If you step back and you look at the graph of strong components then it turns out that is a DAG because if that graph had a cycle in it a cycle of strong components then you could merge those strong components into an even bigger strong component. And so this is kind of a nice way that you can actually regard any directed graph as a DAG of its strong components. So this is nice because I mentioned earlier that DAGs are well-behaved graphs algorithmically you can topologically sort them and algorithms on DAGs oftentimes are easier and more efficient than algorithms on general directed graphs. But this gives us a way that if you first find the strongly connected components of a directed graph as a pre-processing step that almost turns the graph into a DAG. If you kind of look at the graph at the level of you know treating each of its strong components as a super node then what you get then has a DAG like structure. So there's some nice structure here that we can exploit with many different types of algorithms. So let's look at how we can actually take a directed graph now and partition it up into its strongly connected components. This actually is the running time is very easy because it just involves two applications of a full depth first search and each of those takes the linear time. So what we're going to do is run full depth first search while we record the finishing times of all of the nodes of our graph just like we did before. Then mysteriously we're going to reverse all of the edges in our graph and then we run full depth first search a second time. But the key is in that second application of full depth first search the order in which we process the nodes of our graph is in decreasing order of finishing time according to the first full depth first search. And that might seem oddly familiar because that's the exact same order that we used when we topologically sorted earlier. 

And there is actually a very strong connection with topological sorting as we'll see in a second. So it turns out that in this second application of full depth first search, you know full depth first search launches a couple of individual depth first searches each of which discovers a collection of nodes. Those collections of nodes will be the strong components of our graph. And so why on earth does this actually work? There's actually a really nice intuition behind this algorithm. So let's let's switch to the picture and kind of talk through it in terms of the example that we have on the next slide. So remember the algorithm, first run a full depth first search, reverse all the edges and then run a second full depth first search, but processing the nodes in reverse finishing time from the first full depth first search. 

So if we again go back to our picture of a graph where we can regard the graph as a DAG of its strong components, then it turns out that what that previous algorithm is doing is basically topologically sorting this DAG. So let's kind of talk through that. It turns out that you remember that nice property that if you have an edge from I to J, then the finishing time of I is always gonna end up bigger than the finishing time of J. That can actually be generalized to using actually essentially the same argument that I had before. If you have a strong component with an edge to another strong component, so strong component A and strong component B, where A has an edge to B, then it turns out that the biggest finishing time in A, that's gonna be larger than the biggest finishing time in B. So that kind of generalizes what we had before. It's just not a single node with an edge to a single node. 

It's if you have a strong component with an edge to a strong component, then the largest finishing time in the first strong component will be bigger than the largest finishing time in the second strong component. What that means is when we run our second instance of full depth-first search, what node are we going to visit first? We're visiting the nodes in decreasing order of finishing time according to the first depth-first search. So if you look at all these strong components, in which strong component will we find the node with the highest finishing time? So like strong component one versus strong component two. Well, there's an edge from one to two. That means the biggest finishing time has to live in strong component one. 

Overall, we will find the node with the highest finishing time in a strong component that is a source in this DAG of strong components. So like strong component one in this example would have the node that has the highest finishing time overall. And that's the very first node that we're going to visit during our second invocation of full depth-first search. And so the way this algorithm works is it's going to visit somebody in strong component one first and launch a depth-first search there. And that depth-first search is going to fan out and visit all of strong component one because there's complete directed connectivity within any strong component. So our second call to full depth-first search, it will visit all of strong component one, but it won't be able to escape because remember conveniently, we have reversed all the edges of our graph. Reversing the edges of your graph actually doesn't affect the strong components because it just kind of reverses all the paths within those strong components. 

You can still get from anywhere to anywhere in a strong component. So the strong components stay the same, basically, if you reverse the edges of your graph, but now what used to be a source component turns into a sink. You can't get out from strong component one now because you reversed all of the edges. It used to have edges going out of the strong component with all of those being reversed. Now you can't get out from strong component one. So your full depth-first search, it will start in a source node, this source strong component, visit all of that component, and then it won't be able to escape. And so that will find all the nodes in strong component one. 

What you're basically doing is you are basically just topologically sorting this DAG of strong components using the first algorithm that we talked about for topological sorting. That is, find a source node, remove it, and find another source node, remove it, and so on. Because the order in which our second full depth-first search is examining nodes is basically, you know, because of this property, it will, because of the property here, it will always find a source strong component to examine first. It will plan out, visit all the nodes in that component. It won't be able to escape because we've reversed all the edges in our graph. And so it will peel off that strong component and then proceed to new strong components that have become sources, and then it'll find the nodes in those components and so on. So basically, this algorithm is topological sort. 

It's doing a topological sort, but of the directed acyclic graph of strong components. And the method of topological sorting is kind of the first method that we mentioned, where it starts with a source component, identifies that source component, and then moves on to identify the others. So very clever and very non-obvious algorithm for partitioning up the nodes of a graph into its strong components in just linear time. So this is probably the least obvious extension of depth-first search to solve an important connectivity problem in a graph. ♪♪