![[Shortest Paths and Dijkstra's Algorithm.pdf]]
Certainly, no discussion of shortest paths would be complete without talking about Dijkstra's famous shortest path algorithm, perhaps one of the most well-known algorithms of all time. In this video, we'll talk about the structure of Dijkstra's algorithm, and also analyze its running time and correctness. In terms of context, we're talking about the single source shortest path problem here, so we'd like to find the shortest path from a designated source node to every other node in a directed graph. Every edge in our graph has an associated cost, and we're making the important assumption here that all of those edge costs are non-negative. Conveniently, in practice, that's a very common occurrence. For example, in a transportation network, if the cost of an edge represents, say, the travel time to travel down that edge, maybe the travel time down a highway, that's always a non-negative quantity, otherwise you'd be traveling backwards in time. It's convenient that this is actually a common occurrence in practice, because Dijkstra's algorithm is incredibly fast. 

It runs in nearly linear time, whereas the alternative, if you have edges with negative costs, you have to fall back to other algorithms that are much, much slower, for example, the Bellman-Ford algorithm or other alternatives. It's good that most graphs that we're going to encounter in practice have non-negative edge costs, a very crucial assumption for Dijkstra's algorithm to work. At the heart of Dijkstra's algorithm, and many other shortest path algorithms for that matter, is a drive to satisfy the so-called triangle inequality. Let's take just a minute and refresh our memory about what the triangle inequality is, both in elementary geometry and in the context of shortest path problems in graphs. So if we look at first regular old high school geometry, take a triangle, and let's say that the lengths of the sides of that triangle are A, B, and C, the triangle inequality tells us that the sum of A plus B, the sum of any two sides of our triangle, has to be at least as large as the length of the other side, the third side. If that wasn't the case, if this was a less than, that would mean that the third side of length C would be just kind of too long to connect everything together, to close up the triangle. If you tried to draw a picture of the triangle in that case, you'd have something like, you know, here's A and here's B, and then the third side of length C would be so long that it would be impossible to get the triangle to close up. 

So that's the triangle inequality in regular old geometry. What does it look like in the context of graphs and shortest paths? Well, if I have a directed graph, then take any three nodes in that graph, let's call those nodes just 1, 2, and 3, and let's look at a shortest path from 1 to 2, say the cost of that shortest path is A, and then look at a shortest path from 2 to 3, let's say that the cost of that path is B, and then finally look at a shortest path from 1 to 3, and let's say the cost of that is C. So just as in the example above, we have to always have A plus B being at least as large as C. If that wasn't the case, what goes wrong? If this is a less than, then that means that C couldn't possibly be the correct shortest path cost from 1 to 3, because an even shorter path would be available of cost A plus B. We could kind of go from 1 to 2 on the on the path of cost A, and then from 2 to 3 on the path having cost B, and that would be even shorter than our supposedly shortest path from 1 to 3 of cost C. 

So the triangle inequality is a very important idea when we discuss shortest paths, and many algorithms are based on kind of trying to ensure that the triangle inequality is satisfied. Let's take a look at how that kind of manifests itself in the context of a single source shortest path problem. So in this case, we typically form a triangle based on every single directed edge. So if we have an edge from, say, i to j, then we make a triangle with i, j, and the source node, and the three sides of the triangle. So one of them is going to be the shortest path from s to i of cost C i. So C i is the cost label associated with node i, the length of the shortest path to get from the source to i. That's one of the things we want to compute in our shortest path algorithm. 

We also have the shortest path from s to j of cost C j, and then we have the actual cost of the direct edge from i to j. So here, the exact same triangle inequality that we just looked at tells us that it must be the case that C of i plus the cost of the edge from i to j, that has to be at least as large as C j. If that wasn't the case, if this was a less than, then this cost label C j couldn't possibly be correct because there would be an even shorter path available to j that went via i, taking the shortest path from s to i, and then the direct edge from i to j. That would be an even shorter path than C j. So this is the triangle inequality in the context of a single source shortest path problem. It turns out that if you have a set of cost labels that reflect the costs of actual paths in your graph, and if those satisfy the triangle inequality for every single edge, you can actually prove that those cost labels are optimal. The triangle inequality actually serves as an optimality condition for the single source shortest path problem. 

All we have to do is find cost labels that satisfy the triangle inequality, and we're done. And so based on that, we can come up with a subroutine that you see in a large number of different shortest path algorithms, including Dijkstra's shortest path algorithm. It's basically a subroutine that looks for a violation of the triangle inequality and uses that to improve, i.e. lower, some of our cost labels. So if you find an edge, say i to j, that violates the triangle inequality, we have a less than here, whereas it should be a greater than or equal to, what we said that meant was that C j couldn't possibly be the correct shortest path distance to node j because there's an even shorter path available that goes via node i. We can take the shortest path from s to i of cost C i, and the direct edge from i to j of cost i j, and that's even better than the cost that we tentatively have represented by C j. So in that case, we can improve C j by setting it equal to the left-hand side here, and then we also set the predecessor of node j to i to indicate that our new best shortest path we've found so far goes via the edge i j in order to get to node j. So this is a subroutine that I like to call the act of tightening the edge i j because it's kind of tightening down the cost label of node j, kind of almost pulling it closer to the source node. 

There's a nice physical analogy that sometimes people use for shortest paths where you actually think of your graph as a physical model made up of balls and strings. So the nodes are balls, and the edges are strings, and to compute shortest paths, all you do is suspend the graph by holding it from the source node, and you let everything else fall according to gravity, and the distance that every single node falls before it finally catches is equal to the shortest path from the source node. I have an interesting paradox that sometimes people say, well, I've just computed shortest paths in constant time using gravity. So how is that for a computational model, perhaps? That's much better than, say, order n or order m. It seems to be kind of constant. So that's something interesting to think about, perhaps. 

So in that case where you have kind of this gravity model of shortest paths, I think the word tightening makes sense because you're basically using the triangle inequality to kind of pull the distance label of one of your nodes closer to the source. The string represented by the edge from i to j is kind of catching and becoming tight and forcing that distance label to decrease. So confusingly, if you read several other texts on algorithms, this procedure is sometimes called relaxing the edge from i to j. I think there are historical reasons for why that's the case, but I think that's maybe not the best word that describes what's going on here. So I'll call this particular procedure tightening an edge, basically checking if the triangle inequality is violated, and if it is, improving the cost label of node j and then also setting the predecessor of j to i. So if we have this subroutine, we can build quite a few different shortest path algorithms. In fact, a very simple generic way to calculate shortest paths is you just start out with an initial set of cost labels where the cost of the source is zero, and the cost of every other node starts out as infinity, and then you just simply keep tightening edges over and over again until they all satisfy the triangle inequality. 

And at that point, you will have reached an optimal solution. This is sometimes called a generic label correcting algorithm for shortest paths. The only trouble with it is that it could run in exponential time because there are some very bad cases out there where you can actually tighten edges over and over again and end up spending a huge amount of time basically tightening the same edge over and over multiple times. Dijkstra's algorithm actually is a bit more clever. It tightens every edge at most once, and so it's much more efficient. So let's take a look at how Dijkstra's algorithm works. Initially, we set the cost labels as we just did. 

So we set the cost label of the source to zero, and we set every other cost label to infinity. A good value of infinity here actually is typically n times the largest edge cost, because remember that the shortest path from the source to any other node can have at most n minus one edges in it. And so if you use n times the largest edge cost, that's larger than any shortest path distance will ever be. So that's kind of a good value to use as infinity in this situation. So what we're going to do now is something very similar, actually, in spirit to what breadth-first search did. Remember that with breadth-first search, we basically fanned out from the source node, and we visited all the nodes in our graph in order of their distance from the source node, kind of in a layer-by-layer fashion. We're going to do the same thing with Dijkstra's algorithm. 

Just instead of using a queue as the mechanism for keeping track of the nodes, we're going to visit, we're going to use a priority queue. But we're still going to be visiting all the nodes in kind of one sweep outward from the source node, visiting them one by one in order of their distance or cost from the source node. So we're going to build a priority queue on the nodes in our graph, keyed on their cost labels. And in every iteration of Dijkstra's algorithm, it's going to basically select the node that we haven't yet visited, having the smallest cost label in our priority queue. So initially, that's going to be the source node, and then it's going to be the node closest to the source node, and so on. So we basically visit the node that has the smallest cost label in our priority queue. When we do that, we remove that from the priority queue, so we don't visit it ever again, and we just tighten all of its outgoing edges. 

That might allow us to improve the cost labels downstream along those edges. And I think probably the best way to see how the algorithm works is just through an animated example. It's actually a very simple and very intuitive algorithm. We'll come back and talk about the running time in just a second. So let's look at an animation that explains how Dijkstra's algorithm works. Initially, remember, we set the cost label of the source to zero, and the cost labels of all of the other nodes, these yellow values here, to infinity. And we, in our first iteration, extract from our priority queue the node that has the smallest cost label. 

So that'll always start out being the source node. When we do this, we kind of fix the cost label of that node. Sometimes Dijkstra's algorithm is known as a label setting algorithm, because every time you visit a node, you basically kind of fix permanently its cost label. It will never change after that point. In fact, it will be the correct shortest path distance at the point in time when the node is visited. So we freeze the cost label of the source at zero, and then we try tightening all of the outgoing edges from the source. And that basically changes their cost labels to nine and two, based on the edge costs leading to those respective nodes. 

Now, in the next step, we look at all of the remaining nodes, and we choose the one having the smallest cost label. So that'll be node three. Its cost label is two. So we visit that node. And in so doing, we kind of freeze its cost label. And we also note that two here actually is the correct shortest path distance to get to node three. So node three is taken care of, and we now look at its outgoing edges and try tightening them. 

And if you look at the edge that goes from, say, three to one, now we can use that edge to improve the cost label of node one, because we have a better shortest path now available to get to node one via node three that basically involves a cost of two, plus this edge cost of four. That's a total cost of six, which was better than the tentative shortest path cost that we had assigned to node one beforehand. So we tighten the outgoing edges of node two, and then we move on and again, choose from among the remaining nodes, the one that has the lowest cost label. That'll be node one in this case. And so now node one is fixed. So six is indeed the correct shortest path cost for getting to node one. We tighten the outgoing edges of node one and so on. 

So that's basically how Dijkstra's algorithm operates. The next iteration is going to choose node four because it now has the lowest cost label. We tighten its outgoing edges and so on until we have eventually visited every single node in the entire graph. And then we're done. So Dijkstra's algorithm visits every node once, and it tightens every edge exactly once when the source of that edge is visited by Dijkstra's algorithm. So now we can kind of think about what the running time of Dijkstra's algorithm would end up being. It turns out that the running time of Dijkstra's algorithm is basically determined by... the running time of the operations of the priority queue that you use with Dijkstra's algorithm, because, well, what are the key operations that you do with that priority queue? 

You're basically visiting every node once. Whenever you visit a node, you're pulling out of the priority queue the node that has the minimum cost label. And so that's a remove min operation in your priority queue. You're doing that n times, basically. So we have n remove mins, and then we're also tightening every single edge. There's m edges. When you tighten an edge, that could cause a cost label to decrease. 

And so that would be a decrease key operation in our priority queue. So we basically have n remove mins and up to m decrease key operations. And so now we just have to figure out for whatever priority queue we're gonna use, you know, what's the running time associated with those two operations, and then we can add up the total. A very common case here is using a binary heap as our priority queue, because then every major operation runs in order log n time. And so we have n log n time for the remove mins, and m log n time for the decrease keys. The m log n term dominates, and so we typically would quote the running time of Dijkstra with a binary heap as order of m times log n. That's a very good running time if your graph is, say, very large and very sparse, because it's nearly linear in the number of edges in your graph. 

Order m would be linear. That would be the best you could ever hope for. Here we just have order m times an extra log n factor. So that's actually quite fast. It's maybe not quite as fast as we could hope for in a dense graph. If m is close to n squared, as you would have in a dense graph, then this running time would look more like n squared log n, and that's actually not the best that you could hope to achieve. What you could do in that case, if you have a very dense graph, is use a very primitive form of priority queue, just storing your cost labels in an unsorted array. 

If you do that, removements are super slow because you've got to search all of the cost labels to find the minimum. So those removement operations take order n time. There's n of them, and so that's going to take you order n squared time for all the removements. But the decrease keys are now fast because you're just changing a value in an array from one thing to another smaller thing. That takes constant time for each decrease key. And you're doing that order m times. Remember, the m here is also kind of something similar to n squared. 

So this also takes on the order of about n squared time. And so your total running time here is just order n squared. And so that's typically the running time that you would hope to achieve in a dense graph. That actually is linear time in the size of your graph. Now, there's one type of priority queue that kind of achieves the best of both worlds. That's called a Fibonacci heap. It's one of the more advanced types of priority queues that we didn't really cover when we talked about priority queues because it's a little bit complicated, not terribly bad. 

But it has the nice feature that it can support a decrease key operation in only constant amortized time. In fact, Dijkstra's algorithm was one of the motivating applications that served as a motivation for developing the Fibonacci heap because you're doing so many decrease key operations, you want those to run extremely fast. And so the Fibonacci heap can do decrease keys very quickly in constant amortized time. That makes the entire running time of the algorithm just M plus N log N. The M is because of all the decrease keys and the N log N is because of all of the removement operations. So this is extremely close to linear time. In fact, it is linear time plus an extra N log N. 

And that kind of seems natural because you are visiting all of the nodes in increasing order of their distance or cost from the source. So this algorithm is kind of sorting along the way. So it's maybe not surprising that we see an N log N as part of its running time. M plus N log N, the running time that you get with a Fibonacci heap is basically the fastest known running time in a reasonable model of computation for solving the single source shortest path problem. The downside of using a Fibonacci heap, I guess I should also mention that this M plus N log N is at least as good as the other two running times here, order N squared and order of M log N. So it kind of wins in both a sparse and in a dense graph. The only downside is because the Fibonacci heap is so complicated, there are hidden constants in play that make the Fibonacci heap actually not really that preferable in practice. 

I've actually done a couple of computational tests and I have not been able to find basically any graph for which the Fibonacci heap actually runs faster in practice compared to just a regular old binary heap. So just kind of a word of caution, sometimes even though the theoretical running time of an algorithm may look a smidgen faster than one other algorithm, it actually matters how simple the implementation of the algorithm and the data structures are because some algorithms are much leaner than others. So I would probably recommend using a binary heap with Dijkstra's algorithm, especially if the graph you're dealing with is very large and very sparse. There are of course many other choices that you could use for your priority queue. In particular, if you have a graph with small integer edge costs, then you can use priority queues that are designed to work with integers. And in that case, you might get a kind of a RAM-based algorithm that has a running time that actually is input sensitive. It kind of depends on the magnitude of your edge costs. 

And so for example, if C is the largest edge cost in your graph, you might get a running time that actually has a dependence directly on C. Just like other algorithms we've seen, like for example, sorting integers with counting sort, the running time depended on how large those integers were. And so there are lots of different alternatives for the priority queue that you can plug into Dijkstra's algorithm. And therefore there are lots of possible running times that you can end up with as a result of running Dijkstra's algorithm. Last thing to talk about, let's talk a little bit about correctness. Why is it that Dijkstra's algorithm does in fact compute shortest paths? So I think the key claim here is that basically when Dijkstra's algorithm visits a node, when it pulls out a node from the priority queue, it chooses the one that has the minimum cost label and visits that node, the cost label of that node will at that point in the algorithm be the correct shortest path distance from the source to that node. 

And so why is that the case? If we can argue that we're basically done because the Dijkstra's algorithm as it visits the nodes of our graph, we're kind of fixing them one by one and kind of permanently labeling those nodes with their correct shortest path distances. So one way to think about it is as we're running the algorithm, there are kind of two sets of nodes. The ones we've already visited, let's call that set S, the ones that are kind of permanently labeled. According to our claim, those are the ones that we have labeled with their correct shortest path distances. And then there's the remaining nodes. Let's call that set T. 

Those are the ones that are still kind of in play. Their distance labels, their cost labels might still be decreasing a bit over time as we continue to tighten edges. So why is it the case that, and I guess in every iteration of Dijkstra's algorithm, we take the node in T that has the minimum cost label and we effectively move it to S. So why is it that that node has to have a correctly set cost label? Why is the cost label of that node the correct shortest path distance to that node? Let's maybe see if we can justify that really quickly at the whiteboard. So if we look at our graph, remember we have these two sets, S and T. 

S is the set of all the nodes that we have already visited. And we're going to assume that they have correct shortest path cost labels assigned to them. So the source node is in set S. The nodes in T are all the remaining nodes. And so their shortest path cost labels might still be changing over time as we continue to tighten edges. And let's suppose that I'll just label one of these guys. This is node I. 

This is the one that has the minimum cost label. So the minimum value of C of I over all of the nodes in T. That's the one we're going to choose in the next iteration of Dijkstra's algorithm and effectively include that node in S. So why must the shortest path cost label C of I attached to node I, why must that be correct at this point? Well, the claim here is that not only are the shortest path cost labels of the nodes in S correct, but for the nodes not in S, for the ones in T, I claim that the shortest path cost labels, the C of I's, those represent the cost of the best possible path to get to node I using only nodes in S as intermediaries. So if I look at all possible paths that kind of only go through S and then kind of hop directly out to node I, among all such paths, C of I represents the shortest of those paths. So C of I is kind of the shortest path among all the paths having that particular structure. 

If there was a shorter path to node I, it would therefore have to have a different structure than that. It would have to actually basically go through set S for a bit and then jump out to some other node in T and then go through T and eventually come back to node I. So if there was a shorter path out there, it would have to have this kind of green structure, but that green structure has to be longer than the shortest path indicated by C of I. Remember C of I is the minimum cost label of all the nodes in T. So whatever node, let's call it J, the green path jumps to on its way around to node I, the cost label of J has to be already at least as large as the cost label of I, because by definition, I had the minimum cost label in all of T. And so since we know that all of our edge costs are non-negative, we know that the remainder of our path has to have a non-negative cost. And so the total cost of the green path here is going to be at least as large as C of I. 

That means that C of I has to be the shortest path overall to node I. There can't be any other path that's shorter because if there was such a different path, it would have the structure of that green path. And we just argued that that green path can't possibly have a shorter total length than C of I. So that's kind of the rough structure of an argument that we would make to justify that at this point in time, C of I is indeed the correct shortest path label for node I. And so when we include that in S, we continue with our condition that all the nodes in S have correct shortest path labels attached to them. So that's kind of an argument for correctness of Dijkstra's algorithm. And that basically wraps up our discussion of Dijkstra's algorithm. 

So it's a very prominent, very widely used algorithm for calculating shortest paths in graphs. Thank you. Thank you.