![[Design Principles Constraints Slides.pdf]]
So let's look at constraints. Constraints actually restrict the possible actions that can be performed by users on the system. So constraints of the design principle helps prevent users from selecting incorrect options. And physical objects can be designed to constrain things with regards to user input. So there's only one way that you can insert a key into a lock, right? So that physical example of a constraint here, a computing relevant example could include external slots on a computer that have been designed to only allow a cable or a card of a certain type to be inserted in a certain way to prevent any erroneous mistakes with regards to a incorrect cable or a card to be inserted there. For example, HDMI slots are designed in such a way that there are wider end and on end, and there is a narrower end on the other. So it provides some implicit feedback as to what kinds of cables need to be inserted there. It's certainly not a USB cable that needs to go into the HDMI, right? So in which direction the video cable needs to be inserted is made explicit through that constraint. So USB cables have to have also been provided with explicit directionality. And the cable only fits into a particular slot that's meant for the USB cable in a particular direction. So there again, is another example of constraint, especially physical constraint. Here are such examples. On the other hand, for instance, an RS 232 cable can only be inserted in a specific direction because the wider end is on the top end and the narrower end is on the bottom end. So it provides a constraint as to in which direction the cable must be plugged in. It's logical, unambiguous design. However, there could also be ambiguities in the sense that if you have two different plugins that are very similar to one another, and the instruction regarding the plugin are provided in a different panel in the bottom, so one particular plugin has to do with the mouse and the second has to do with the keyboard, but these labels are not provided immediately next to the plugin itself. It's actually provided on a different panel. So here in this case, it's actually confusing to the user as to which particular plugin corresponds to the mouse and which one corresponds to the keyboard. Is it the top or the bottom connector? How to design them more logically? You may want to provide direct adjacent mapping between the icons and the connector, as is shown in the image A, and the image B provides color coding to associate the connectors with the labels. So the green connector could be associated then with the mouse and then the red connector could be associated with the keyboard.