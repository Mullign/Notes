![[Design Principles Affordances Slides.pdf]]Finally, let's talk about affordance as a very important design principle. This refers to an object giving clues about the way that it can be used and the way that users can interact with that object. For example, and to define this further, it refers to the attributes physical characteristics of an object that allows people to know how to use it, what type of actions to enact on it. For example, a mouse button invites pushing, the left mouse button is oftentimes located directly under the index finger, and the right mouse button is located directly below the middle finger. And each of these buttons are designed in such a way, often such that they they show that there is actually a dimple on that button, that the that fits the shape of that index finger and the middle finger explicitly. And this is how it is meant to be used. So the physical characteristics of the buttons invites an appropriate finger based touch and push on that button. A door handle, for instance, affords pulling and a door knob, for instance, affords rotating and a door bar, for instance, affords pushing. So Norman in 1988, use these terms to discuss the design of everyday objects. And oftentimes, it basically refers to the interplay or the relationship between the user and the object. So essentially, the physical properties of the user and the object conveyed to the user how it is meant to be used, what types of actions are meant to be enacted on that object, chairs are meant to sit on chairs afford being able to sit on scissors afford being able to hold in a particular way and to cut object, etc, etc. knives afford cutting, and are meant to be held in a particular way, where the shape of the handle informs the user of how it is meant to be handled. 

From the time it has been introduced, it has been much popularized in interaction design society. And it's been discussed how it has affected the design of interface objects as well. scroll bars afford moving up and down. And icons, for instance, afford clicking buttons, for instance, afford clicking, as well as, for instance, the scroll bar can be moved upward or downward in order to be able to scroll up or down in a document. And these are all coined by Donald Norman as having virtual affordances, because they are at the interface level. And the design of these interface level elements such as buttons, scroll bars, and icons, etc, all have rendered information properties that convey how it can potentially be used. So affordance simply has to do with how the physical and virtual properties of icons entities provide explicit information to the user regarding how they are meant to be used. What does affordance have to offer interaction designers interfaces are virtual and do not have affordances like physical objects like chairs or knives or scissors, etc. So interface elements such as buttons and scroll bars and text areas have virtual affordances, while physical objects have physical affordances. So Norman argues that it does not make sense to talk about interfaces in terms of real affordances. Instead, interfaces are better conceptualized as perceived affordances. That is learned conventions of arbitrary mapping between action and effect on the interface. scroll bars are meant to be scrolled up or down. And you know, option boxes are meant to be selected. For instance, a scroll wheel is meant to be turned up or down. buttons are meant to be pushed. So these are perceived affordances that one learns through arbitrary mappings between actions of scrolling, clicking, selecting moving up or down, and the effect on that interface. Some mappings are better than others. 

An activity that we might perform in order to understand virtual affordances a bit more better is if you look at some of these different virtual interface icons below. And what do the following screen objects afford? What if you were a novice user? Would you know what to do with them? So let's start from the icon on the left. For example, this is a button that one would know from prior experience actually takes you to the previous module or refers to a previous object and to view them. Perhaps a previous video or a previous music file would be evoked using this button. The next button doesn't quite tell you what you know it actually refers to but it could be the start of a software. And the button in yellow doesn't tell you anything specifically about what it's supposed to do. So it's just an empty button that is just has a unique color. So this button does not provide it doesn't even, you know, give you any virtual affordances that it is in fact a button. It just shows a text area or a rectangular box with a different color as compared to the background. Next you have a interface element, which is actually a scroll bar. And if you had never seen one of these before, it also requires some amount of experience in order to understand the mapping between the action and what the interface element actually the information that it provides. So the scroll bar is interesting in that typical scroll bars have two different modes of operation. So you have an elevator button that you can actually scroll up or down in order to peruse the document. And then second, you have up and down arrows that you can push in order to have much more fine grained control over the scrolling.