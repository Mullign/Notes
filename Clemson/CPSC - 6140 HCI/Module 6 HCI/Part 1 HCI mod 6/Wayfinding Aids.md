Other types of input devices and interaction metaphor examples is the use of wayfinding aids. Wayfinding aids are essentially aids that allow user to understand where they are spatially. So the interaction metaphors here not only are used towards processing the input of a user, such as gesture-based input or any kind of spatial input from tracking systems that the user wears, or tracking systems plus buttons that are on the user's hand, and then processing it towards specifying input from the user to the 3D simulation or the virtual environment, but also interaction metaphors can consist of how the virtual reality simulation provides information back to the user. So in that direction, an example is the wayfinding aid. So the wayfinding aid is rendered in the bottom right-hand portion of the screen here. This is basically a top-down view of where the user is in the virtual environment. As you can see here in this video, the red dot with two arrows emanating from it show the horizontal field of view of the user in the virtual environment, and then the red dot specifies where the user is. 

And the task here is that they have to essentially walk around a virtual hospital ward and inspect and view certain agents as they're walking about. And so this wayfinding aid shows a you-are-here pointer that's specified by that red dot, and then other dots that are moving about show where there are virtual agents, which are basically AI-controlled virtual humans that represent virtual healthcare workers and virtual patients that are walking about. On top of that, you have interaction metaphors that allow you to open as you make an observation of two agents interacting with one another, such as a healthcare worker and a patient, and you want to record some observation, you can essentially pull up an interaction widget that consists of a smartphone application. So in the virtual world, you can pull up a smartphone screen, and then you can specify certain observations there. So the interaction metaphor here for specifying user input is through this smartphone application window that is rendered in the virtual world on demand. And then the information provided back from the virtual environment is updates to the scene as the user is walking around. You have the wayfinding aid that allows for spatial knowledge acquisition and for spatial orientation of the user and the objects that surround him. 

And you also have, for instance, a view of the agents that are walking about.