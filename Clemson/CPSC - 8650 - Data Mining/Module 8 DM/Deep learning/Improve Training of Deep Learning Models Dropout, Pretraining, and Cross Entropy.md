​With the large number of neurons and a lot of depths and a lot of layers, ​there's a big problem of overfading because you learn so many features. ​So dropout is another way to prevent the overfading. ​So the idea is at each approach, okay, and given a law of dropout rate, ​say, law equals 0.5, we drop out. So we randomly drop out some non-output unit, okay? ​So and then with the unit is dropped out, we're not going to do the backpropagation on those ​dropped out nodes. We'll do the propagation on the dropout network, okay? Therefore, with this ​dropout chain, then we scale the final model parameters with the learning from the dropout ​network, okay? Now why does dropout work? Dropout can be viewed as a regularization technique. 

​So it forced the model to be more robust to the noise. Essentially, you try to drop out. ​Basically, you add a noise, okay, and then more generalizable features. ​Dropout, it is different from the bagging, okay? Bagging, well, each base model is trained ​independently on the bootstrap samples. The dropout, the model parameters of the current ​dropout network are updated based on the previous dropout network. So you can see there's a difference, okay? 

​But both of them try to make the model to be more robust to the noise, okay? ​And that technique to help the training the deep neural network is pre-training. ​And the pre-training is the process of initializing the model in the suitable region, okay? ​So, in general, the most popular method of pre-training is called the gradient-supervised ​pre-training. And we preset the model parameters layer by layer in a graded way. ​So we started with a simple model, okay? And then ​add one additional layer at each time. For example, this is the original model to pre-train, right? 

​So we will train first iteration, we will train the first hidden layers, okay? So then we ​add the new layer, okay? And then we train the new layer. And then another, and then we add ​more hidden layers we train, okay? And then after that, we have a fine-tuned process. ​Now there are other pre-training strategies such as unsupervised pre-training based on the ​auto-encoder, okay? And then we can have a hybrid strategy with the auto-encoder plus ​gradient-supervised pre-training. And then there's another method called pre-training for transfer ​learning, okay? Then, as we know that in the past, we often, you know, ​use the mean square error for training, you know, mean square error to train the ​traditional neural network, right? So for the mean square error, the error term is the delta is ​O times one minus O and times O minus T, right? So you can see that the decaying problem there, ​mean square error, okay? And so we can also use the cross-entropy to measure the ​disagreement between the actual T and the predicted target, okay? And so cross-entropy ​basically is using the negative T log O minus, the loss is one minus T log one minus O. So ​then our error becomes O minus T. So this is the one way that, ​so you can see the difference. There is no decaying there, okay? 

​Okay, so those are the methods to improve the training, okay?