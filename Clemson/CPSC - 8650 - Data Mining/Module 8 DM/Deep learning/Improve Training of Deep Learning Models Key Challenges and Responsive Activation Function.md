​So, what are the challenges for deep learning? ​There are key challenges. ​So in this, we will study the key challenges for training the deep learning models. ​And then we study the responsive activation functions, adaptive learning rate, dropout, ​boundary training, and cross-entropy. ​So those are techniques to improve the deep learning training, okay? ​So what are the key challenges? ​And so the key challenge here is that solving the optimization problem in deep learning ​we want to achieve the global minimum, right? 

​So the first challenge is optimization and try to minimize the approximated training ​error, okay? ​And so we use the stochastic gradient descending to find the model parameters. ​So that's what we do in the neural network. ​Now because it is non-convex in general, right? ​So how do we find a high-quality knockout optima? ​Because when we apply the approximated training gradient descending to find the model parameter, ​we may end up finding the knockout minima. ​So if the knockout minima is a high-cost knockout minima, it's not good, so we should get to ​the low-cost knockout minima or achieve the global minima, okay? 

​And secondly, since we are training with the minimized approximate training errors, right? ​So what we really want is to minimize the generalization errors, not just the knockout ​minima, okay? ​So we train the knockout minima and then we may end up with an overfitting problem that ​when we apply the real data, we take the training data set and then we name the knockout optimal ​data error, okay? ​So because we minimize the training error, but it does not apply to general data. ​So the general data, we still have a large error. ​So how do we mitigate the overfitting? ​So to address those two problems, well, we need first to use different activation functions there. 

​So looking at the traditional sigma activation function, that output is the sigma of i of ​input, right? ​And then input is 1 over 1 plus e negative i, and that converted to 0 and the 1 there. ​So when we apply optimization, well, we compute the derivative, right? ​So it's a partial derivative of o and regarding to the i will become o times 1 minus o. ​So given the error of the unit is become delta j equal to o j and 1 minus o j times ​the o j minus target, right? ​So you can see there is a decaying problem here because there is a saturation of sigma ​activation function when o j is 1 or near the 0, okay? ​When o j is 1, it's saturated because the o j times 1 minus o j become near 0 or o j ​equal to 0, that decaying is become 0. 

​So both derivative and error will cost be 0, okay? ​So that's the major problem. ​If you do the back propagation and because of these changes, so the gradient will vanish. ​So we have a gradient vanishing problem, okay? ​So the back propagation was stuck or take the non-time to terminate, okay? ​So we need a more responsive active functions such as ratified linear unit. ​So that is a Reynold activation function used more generally used in the deep learning network, ​right? 

​So what is the Reynold function, activation function? ​The Reynold output o is equal to as a function of input, right? ​And if the output input is i, so the input is i, it will be i. ​If i is greater than 0, if otherwise less than 0 or equal to 0, we will just make it ​0. ​So this has introduced the nonlinearity and also with the gradient, you can see that partial ​derivative of o over i is equal to 1 if i is greater than 0, okay? ​And then partial derivative of o over i is equal to 0. ​So in terms of the error, that error is 0 if the unit is inactive because i is less ​than 0. 

​Otherwise, we basically aggregate the all error terms from the units in the next high ​layer the unit is connected to because you transform the error up, okay? ​So this will not have any decaying, therefore, we avoid the gradient vanish unlike in the ​saturated sigmoid activation function where when oj approaches to 1 or 0 that the derivative ​of error becomes very close to 0, right? ​So the gradient becomes vanishing. ​So we use the responsive activation function here. ​There are several activation functions and ReNO somehow become the most popular one nowadays. ​Of course, there are some modifications based on the ReNOs such as Dickey ReNO or ReNO, okay? 

​So, what are the challenges for deep learning? ​There are key challenges. ​So in this, we will study the key challenges for training the deep learning models. ​And then we study the responsive activation functions, adaptive learning rate, dropout, ​boundary training, and cross-entropy. ​So those are techniques to improve the deep learning training, okay? ​So what are the key challenges? ​And so the key challenge here is that solving the optimization problem in deep learning ​we want to achieve the global minimum, right?  Play video starting at ::58 and follow transcript0:58​So the first challenge is optimization and try to minimize the approximated training ​error, okay? ​And so we use the stochastic gradient descending to find the model parameters. ​So that's what we do in the neural network. ​Now because it is non-convex in general, right? ​So how do we find a high-quality knockout optima? ​Because when we apply the approximated training gradient descending to find the model parameter, ​we may end up finding the knockout minima. ​So if the knockout minima is a high-cost knockout minima, it's not good, so we should get to ​the low-cost knockout minima or achieve the global minima, okay?  Play video starting at :2: and follow transcript2:00​And secondly, since we are training with the minimized approximate training errors, right? ​So what we really want is to minimize the generalization errors, not just the knockout ​minima, okay? ​So we train the knockout minima and then we may end up with an overfitting problem that ​when we apply the real data, we take the training data set and then we name the knockout optimal ​data error, okay? ​So because we minimize the training error, but it does not apply to general data. ​So the general data, we still have a large error. ​So how do we mitigate the overfitting? ​So to address those two problems, well, we need first to use different activation functions there.  Play video starting at :3:6 and follow transcript3:06​So looking at the traditional sigma activation function, that output is the sigma of i of ​input, right? ​And then input is 1 over 1 plus e negative i, and that converted to 0 and the 1 there. ​So when we apply optimization, well, we compute the derivative, right? ​So it's a partial derivative of o and regarding to the i will become o times 1 minus o. ​So given the error of the unit is become delta j equal to o j and 1 minus o j times ​the o j minus target, right? ​So you can see there is a decaying problem here because there is a saturation of sigma ​activation function when o j is 1 or near the 0, okay? ​When o j is 1, it's saturated because the o j times 1 minus o j become near 0 or o j ​equal to 0, that decaying is become 0.  Play video starting at :4:45 and follow transcript4:45​So both derivative and error will cost be 0, okay? ​So that's the major problem. ​If you do the back propagation and because of these changes, so the gradient will vanish. ​So we have a gradient vanishing problem, okay? ​So the back propagation was stuck or take the non-time to terminate, okay? ​So we need a more responsive active functions such as ratified linear unit. ​So that is a Reynold activation function used more generally used in the deep learning network, ​right?  Play video starting at :5:37 and follow transcript5:37​So what is the Reynold function, activation function? ​The Reynold output o is equal to as a function of input, right? ​And if the output input is i, so the input is i, it will be i. ​If i is greater than 0, if otherwise less than 0 or equal to 0, we will just make it ​0. ​So this has introduced the nonlinearity and also with the gradient, you can see that partial ​derivative of o over i is equal to 1 if i is greater than 0, okay? ​And then partial derivative of o over i is equal to 0. ​So in terms of the error, that error is 0 if the unit is inactive because i is less ​than 0.  Play video starting at :6:41 and follow transcript6:41​Otherwise, we basically aggregate the all error terms from the units in the next high ​layer the unit is connected to because you transform the error up, okay? ​So this will not have any decaying, therefore, we avoid the gradient vanish unlike in the ​saturated sigmoid activation function where when oj approaches to 1 or 0 that the derivative ​of error becomes very close to 0, right? ​So the gradient becomes vanishing. ​So we use the responsive activation function here. ​There are several activation functions and ReNO somehow become the most popular one nowadays. ​Of course, there are some modifications based on the ReNOs such as Dickey ReNO or ReNO, okay? ​So, what are the challenges for deep learning? ​There are key challenges. ​So in this, we will study the key challenges for training the deep learning models. ​And then we study the responsive activation functions, adaptive learning rate, dropout, ​boundary training, and cross-entropy. ​So those are techniques to improve the deep learning training, okay? ​So what are the key challenges? ​And so the key challenge here is that solving the optimization problem in deep learning ​we want to achieve the global minimum, right?  Play video starting at ::58 and follow transcript0:58​So the first challenge is optimization and try to minimize the approximated training ​error, okay? ​And so we use the stochastic gradient descending to find the model parameters. ​So that's what we do in the neural network. ​Now because it is non-convex in general, right? ​So how do we find a high-quality knockout optima? ​Because when we apply the approximated training gradient descending to find the model parameter, ​we may end up finding the knockout minima. ​So if the knockout minima is a high-cost knockout minima, it's not good, so we should get to ​the low-cost knockout minima or achieve the global minima, okay?  Play video starting at :2: and follow transcript2:00​And secondly, since we are training with the minimized approximate training errors, right? ​So what we really want is to minimize the generalization errors, not just the knockout ​minima, okay? ​So we train the knockout minima and then we may end up with an overfitting problem that ​when we apply the real data, we take the training data set and then we name the knockout optimal ​data error, okay? ​So because we minimize the training error, but it does not apply to general data. ​So the general data, we still have a large error. ​So how do we mitigate the overfitting? ​So to address those two problems, well, we need first to use different activation functions there.  Play video starting at :3:6 and follow transcript3:06​So looking at the traditional sigma activation function, that output is the sigma of i of ​input, right? ​And then input is 1 over 1 plus e negative i, and that converted to 0 and the 1 there. ​So when we apply optimization, well, we compute the derivative, right? ​So it's a partial derivative of o and regarding to the i will become o times 1 minus o. ​So given the error of the unit is become delta j equal to o j and 1 minus o j times ​the o j minus target, right? ​So you can see there is a decaying problem here because there is a saturation of sigma ​activation function when o j is 1 or near the 0, okay? ​When o j is 1, it's saturated because the o j times 1 minus o j become near 0 or o j ​equal to 0, that decaying is become 0.  Play video starting at :4:45 and follow transcript4:45​So both derivative and error will cost be 0, okay? ​So that's the major problem. ​If you do the back propagation and because of these changes, so the gradient will vanish. ​So we have a gradient vanishing problem, okay? ​So the back propagation was stuck or take the non-time to terminate, okay? ​So we need a more responsive active functions such as ratified linear unit. ​So that is a Reynold activation function used more generally used in the deep learning network, ​right?  Play video starting at :5:37 and follow transcript5:37​So what is the Reynold function, activation function? ​The Reynold output o is equal to as a function of input, right? ​And if the output input is i, so the input is i, it will be i. ​If i is greater than 0, if otherwise less than 0 or equal to 0, we will just make it ​0. ​So this has introduced the nonlinearity and also with the gradient, you can see that partial ​derivative of o over i is equal to 1 if i is greater than 0, okay? ​And then partial derivative of o over i is equal to 0. ​So in terms of the error, that error is 0 if the unit is inactive because i is less ​than 0.  Play video starting at :6:41 and follow transcript6:41​Otherwise, we basically aggregate the all error terms from the units in the next high ​layer the unit is connected to because you transform the error up, okay? ​So this will not have any decaying, therefore, we avoid the gradient vanish unlike in the ​saturated sigmoid activation function where when oj approaches to 1 or 0 that the derivative ​of error becomes very close to 0, right? ​So the gradient becomes vanishing. ​So we use the responsive activation function here. ​There are several activation functions and ReNO somehow become the most popular one nowadays. ​Of course, there are some modifications based on the ReNOs such as Dickey ReNO or ReNO, okay? ​So, what are the challenges for deep learning? ​There are key challenges. ​So in this, we will study the key challenges for training the deep learning models. ​And then we study the responsive activation functions, adaptive learning rate, dropout, ​boundary training, and cross-entropy. ​So those are techniques to improve the deep learning training, okay? ​So what are the key challenges? ​And so the key challenge here is that solving the optimization problem in deep learning ​we want to achieve the global minimum, right?  Play video starting at ::58 and follow transcript0:58​So the first challenge is optimization and try to minimize the approximated training ​error, okay? ​And so we use the stochastic gradient descending to find the model parameters. ​So that's what we do in the neural network. ​Now because it is non-convex in general, right? ​So how do we find a high-quality knockout optima? ​Because when we apply the approximated training gradient descending to find the model parameter, ​we may end up finding the knockout minima. ​So if the knockout minima is a high-cost knockout minima, it's not good, so we should get to ​the low-cost knockout minima or achieve the global minima, okay?  Play video starting at :2: and follow transcript2:00​And secondly, since we are training with t: Added to Selection. Press [⌘ + S] to save as a note Like Dislike Report an issue : Added to Selection. Press [⌘ + S] to save as a note Like Dislike Report an issue : Added to Selection. Press [⌘ + S] to save as a note