​Now, the next advantage of CNN is the translation equivalent. ​So intuition is that the same input at different location ​gives the same output, right? ​So fgx to the gfx equal to the gfx. ​You don't say, oh, they moved, and they are not, the cat. ​A cat at the upper right corner and at the lower left corner ​of the image will produce the same output. ​University of Illinois, at the start of the sentence ​and at the end of the sentence, produces the same output. ​That's the main idea. 

​So you should have the translation equivalent, OK? ​So we send the image of in, in. ​You move the image right by l pixels, ​and they should have the same after you ​apply the filter over it, OK? ​So basically, your feature will just move. ​Your feature will just move by l with the same filter k. ​So that's why we use the CNN. ​Now, CNN, except the convolutional layer, ​it has also a pooling layer. 

​The pooling layer essentially is a subsampling process ​where the pooling layer should pool the hidden unit ​in the same neighborhood so that, first, it ​introduces the invariance to the local translation. ​Second, it reduces the number of hidden units in the hidden ​layer, because you pool it, and you actually subsampling, OK? ​So a convolutional layer usually has multiple kernels. ​They are applied separate to the input of volume. ​So you have four depths, OK? ​So you have many neurons, right? ​Basically, each one of them will produce a 2D feature map. 

​Then your multiple feature maps are organized in a 3D tensor, ​OK? ​So you have a 3D tensor. ​The 3D tensor has depth, OK? ​And then you need to follow with nonlinear activation ​functions such as the ReNO, OK? ​And then you do the pooling to reduce ​the size of the feature map that get the eventual output. ​So you can see the input volume, apply the kernels, ​get the feature map, and then you ​do the nonlinear activity to introduce ​the nonlinearities, and then do the pooling, OK? ​That's a typical convolutional layer.