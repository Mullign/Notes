​In this video, we are going to study convolutional neural networks. ​What is convolutional neural networks? ​First, what is convolution? ​Convolution is to compute the feature maps using a sliding kernel over the input, and ​you compute the weighted sum locally. ​So like in this left side, this is a one-dimensional kernel, k, okay? ​And the input is 10, 50, 30, 40, 50, 80, 90, 80, okay? ​So we aim this center of the kernel at the designed input, and then we sum the neighboring ​input with the kernel weight, include itself, and then we get the result as the feature ​map result. 

​Now for this edge input, we had an actual zero, therefore we get three values, because ​this is a three, zero by three, one by three kernel, okay? ​So this is the formula for compute the kernel, the feature map value based on the kernel. ​You can see that we only look at the nearby input, okay? ​And then this on the right side is a two-dimensional convolution, where it has the kernel of three ​by three, two-dimensional kernel, so again we map this middle or the center of the kernel ​map into the input field, then we compute the weighted sum of those neighboring input, ​you get the result of three, okay? ​So of course, in a convolutional neural network, using the kernel to do the convolution, we ​call it a convolutional layer, okay? ​So this is a convolutional layer example, okay? ​First of all, that you have an input value in this red, it has the input that may have ​multiple depths, for example, multiple channels, okay? 

​So a kernel will move over that input value, okay? ​So there are many, many neurons, maybe like two or three neurons, depending on the depth ​of your output, okay? ​So each neuron in the convolutional layer is connected only to a knuckle region. ​That knuckle region is the size of the kernel, okay? ​And we'll look at the four depths. ​Now if you notice in this line here, we didn't draw any weight, because each neuron will ​connect to the same field, but not share the same weight. ​They have different weights, okay? 

​And so therefore, your output has different depths, okay? ​So then for each neuron, we'll act as the same, but it's just the weighted summation. ​Maybe have certain values plus an activation function. ​Actually, renal activation is generally applied in the convolutional neural network, which ​will provide a non-linearity. ​Now you can also use other activation functions, depending on your application, okay? ​But the problem here, like the only difference from the traditional neural network is that ​it is restricted to the input only on that field. ​As a 3x3 kernel, then that's a non-input, okay? 

​The neuron will look at this non-input. ​Now look at this. ​This is the kernel example, okay? ​You can see that convolutional kernel here, and look over this input, and then connect ​it to the nearby non-input, okay? ​And then the result is negative 8. ​This is called a convolution, okay? ​Now, because on the edge, you may have, for this edge input, you may miss some neighbor ​input, so we pair them as zeros here, okay? 

​So pairing on the input value with zero, therefore, the reason we pair this is because ​if we don't pair this, we cannot calculate this edge pixel. ​If we pair them, the resulting feature space will have the same dimension as the original ​input, okay? ​All right, so, apparently, there are three hyperparameters that control the size of the ​output value of the convolutional layer, and the depth, okay? ​The depth is how many neurons. ​So each neuron has their own feature map, has their own filters, okay? ​The depth of the output environment controls the number of neurons in the layer that connect ​to the same region on the input value, okay? ​So that same region, okay, that's 3 by 3, 5 by 5, okay? 

​And then there is another thing, because when we have this kernel here, and then we move ​our kernel over this input, so how far, how much distance for every movement? ​If we move one pixel by pixel, we call this a stride. ​So you just move one pixel, then we call the stride equal to one. ​If we move two pixels, then stride by two. ​So the stride, actually, if we, in this case, move by one, then our size, the output size ​will equal, with the padding, our output size will equal to the same, well, output size ​will be equal to the original input size. ​Now, if we move by two, stride two, so then, well, instead of getting one, then our next ​will get moved to this one here, then our next will move to this one, so our output ​size becomes three now, 3 by 3. ​So you can see, original has 5 by 5, now we have 3 by 3. 

​So you can see the padding, right? ​So if you don't have the padding here, and then we can only cover this size, if we don't ​have a padding here, we cover this size, then when we stride to this, well, our size becomes ​two by two. ​So you can see that the stride, the original input size, the kernel size will determine ​the output volume size, okay? ​So the stride control how depth columns around the ways I hide are allocated. ​If the stride is one, then move the filter one pixel at a time. ​This will lead to a heavy overlap because we get the receptive fields overlapped. ​They usually use, you know, leave a little bit gap, okay, then what you can do is you ​take s equal to one, two, okay, then your stride is two. 

​And usually you don't go over three, okay, it's very real. ​All in all, a greater stride means a smaller overlap of receptive fields. ​Of course, you will have a smaller spatial dimension on the output volume. ​But usually when you have a larger stride, you may not cover the details sometimes, okay? ​And then sometimes it is convenient to pad the input with zeros or other values. ​Sometimes you can do an average of the region on the border of the input volume. ​The size of this padding is another parameter, okay, another hyperparameter. 

​So padding provides control to output this volume spatial size. ​And so sometimes you want to preserve the exact spatial size of the input value, ​then you have to do the padding, okay? ​We call that a stem padding, so it depends on your kernel size. ​And if your kernel size is three, you want to pad by one. ​Well, overall, of course, it also depends on your stride here. ​So the spatial size of the output volume is w, w is the width of your imagery, okay? ​And minus k, k is your kernel size, and then plus 2p divided by s plus one. 

​Now, this number must be an integer. ​If this number is not an integer, then the strides are incorrect. ​And the neuron cannot tell to fit across the input volume in a symmetrical way, okay? ​So let's go back to look at this convolutional kernel. ​As we say, you know, if we have a stride of one, and with the padding one here, ​and then the resulting spatial map is five by five, okay? ​However, if you, with the same padding one, then you have a stride of two, ​with a kernel size three, then the output volume will be five minus three plus 2p, right? ​Plus two, five minus three plus two divided by two equal to two plus one. 

​So the output will be three. ​So if you look at this, we move one, and then we get this one, ​then stride is two, we get this one, then another stride of two, we get this one. ​So basically, the output becomes three. ​And then we move down here, get this one, stride by two, this one, stride by two, get this one. ​So it's three by three. ​So that's the formula. ​It means the formula works. ​w minus k plus 2p, okay? 

​In this video, we are going to study convolutional neural networks. ​What is convolutional neural networks? ​First, what is convolution? ​Convolution is to compute the feature maps using a sliding kernel over the input, and ​you compute the weighted sum locally. ​So like in this left side, this is a one-dimensional kernel, k, okay? ​And the input is 10, 50, 30, 40, 50, 80, 90, 80, okay? ​So we aim this center of the kernel at the designed input, and then we sum the neighboring ​input with the kernel weight, include itself, and then we get the result as the feature ​map result.  Play video starting at :1:21 and follow transcript1:21​Now for this edge input, we had an actual zero, therefore we get three values, because ​this is a three, zero by three, one by three kernel, okay? ​So this is the formula for compute the kernel, the feature map value based on the kernel. ​You can see that we only look at the nearby input, okay? ​And then this on the right side is a two-dimensional convolution, where it has the kernel of three ​by three, two-dimensional kernel, so again we map this middle or the center of the kernel ​map into the input field, then we compute the weighted sum of those neighboring input, ​you get the result of three, okay? ​So of course, in a convolutional neural network, using the kernel to do the convolution, we ​call it a convolutional layer, okay? ​So this is a convolutional layer example, okay? ​First of all, that you have an input value in this red, it has the input that may have ​multiple depths, for example, multiple channels, okay?  Play video starting at :3:14 and follow transcript3:14​So a kernel will move over that input value, okay? ​So there are many, many neurons, maybe like two or three neurons, depending on the depth ​of your output, okay? ​So each neuron in the convolutional layer is connected only to a knuckle region. ​That knuckle region is the size of the kernel, okay? ​And we'll look at the four depths. ​Now if you notice in this line here, we didn't draw any weight, because each neuron will ​connect to the same field, but not share the same weight. ​They have different weights, okay?  Play video starting at :4:10 and follow transcript4:10​And so therefore, your output has different depths, okay? ​So then for each neuron, we'll act as the same, but it's just the weighted summation. ​Maybe have certain values plus an activation function. ​Actually, renal activation is generally applied in the convolutional neural network, which ​will provide a non-linearity. ​Now you can also use other activation functions, depending on your application, okay? ​But the problem here, like the only difference from the traditional neural network is that ​it is restricted to the input only on that field. ​As a 3x3 kernel, then that's a non-input, okay?  Play video starting at :5:11 and follow transcript5:11​The neuron will look at this non-input. ​Now look at this. ​This is the kernel example, okay? ​You can see that convolutional kernel here, and look over this input, and then connect ​it to the nearby non-input, okay? ​And then the result is negative 8. ​This is called a convolution, okay? ​Now, because on the edge, you may have, for this edge input, you may miss some neighbor ​input, so we pair them as zeros here, okay?  Play video starting at :6:8 and follow transcript6:08​So pairing on the input value with zero, therefore, the reason we pair this is because ​if we don't pair this, we cannot calculate this edge pixel. ​If we pair them, the resulting feature space will have the same dimension as the original ​input, okay? ​All right, so, apparently, there are three hyperparameters that control the size of the ​output value of the convolutional layer, and the depth, okay? ​The depth is how many neurons. ​So each neuron has their own feature map, has their own filters, okay? ​The depth of the output environment controls the number of neurons in the layer that connect ​to the same region on the input value, okay? ​So that same region, okay, that's 3 by 3, 5 by 5, okay?  Play video starting at :7:20 and follow transcript7:20​And then there is another thing, because when we have this kernel here, and then we move ​our kernel over this input, so how far, how much distance for every movement? ​If we move one pixel by pixel, we call this a stride. ​So you just move one pixel, then we call the stride equal to one. ​If we move two pixels, then stride by two. ​So the stride, actually, if we, in this case, move by one, then our size, the output size ​will equal, with the padding, our output size will equal to the same, well, output size ​will be equal to the original input size. ​Now, if we move by two, stride two, so then, well, instead of getting one, then our next ​will get moved to this one here, then our next will move to this one, so our output ​size becomes three now, 3 by 3. ​So you can see, original has 5 by 5, now we have 3 by 3.  Play video starting at :8:49 and follow transcript8:49​So you can see the padding, right? ​So if you don't have the padding here, and then we can only cover this size, if we don't ​have a padding here, we cover this size, then when we stride to this, well, our size becomes ​two by two. ​So you can see that the stride, the original input size, the kernel size will determine ​the output volume size, okay? ​So the stride control how depth columns around the ways I hide are allocated. ​If the stride is one, then move the filter one pixel at a time. ​This will lead to a heavy overlap because we get the receptive fields overlapped. ​They usually use, you know, leave a little bit gap, okay, then what you can do is you ​take s equal to one, two, okay, then your stride is two.  Play video starting at :10:10 and follow transcript10:10​And usually you don't go over three, okay, it's very real. ​All in all, a greater stride means a smaller overlap of receptive fields. ​Of course, you will have a smaller spatial dimension on the output volume. ​But usually when you have a larger stride, you may not cover the details sometimes, okay? ​And then sometimes it is convenient to pad the input with zeros or other values. ​Sometimes you can do an average of the region on the border of the input volume. ​The size of this padding is another parameter, okay, another hyperparameter.  Play video starting at :11: and follow transcript11:00​So padding provides control to output this volume spatial size. ​And so sometimes you want to preserve the exact spatial size of the input value, ​then you have to do the padding, okay? ​We call that a stem padding, so it depends on your kernel size. ​And if your kernel size is three, you want to pad by one. ​Well, overall, of course, it also depends on your stride here. ​So the spatial size of the output volume is w, w is the width of your imagery, okay? ​And minus k, k is your kernel size, and then plus 2p divided by s plus one.  Play video starting at :11:59 and follow transcript11:59​Now, this number must be an integer. ​If this number is not an integer, then the strides are incorrect. ​And the neuron cannot tell to fit across the input volume in a symmetrical way, okay? ​So let's go back to look at this convolutional kernel. ​As we say, you know, if we have a stride of one, and with the padding one here, ​and then the resulting spatial map is five by five, okay? ​However, if you, with the same padding one, then you have a stride of two, ​with a kernel size three, then the output volume will be five minus three plus 2p, right? ​Plus two, five minus three plus two divided by two equal to two plus one.  Play video starting at :13:6 and follow transcript13:06​So the output will be three. ​So if you look at this, we move one, and then we get this one, ​then stride is two, we get this one, then another stride of two, we get this one. ​So basically, the output becomes three. ​And then we move down here, get this one, stride by two, this one, stride by two, get this one. ​So it's three by three. ​So that's the formula. ​It means the formula works. ​w minus k plus 2p, okay? ​In this video, we are going to study convolutional neural networks. ​What is convolutional neural networks? ​First, what is convolution? ​Convolution is to compute the feature maps using a sliding kernel over the input, and ​you compute the weighted sum locally. ​So like in this left side, this is a one-dimensional kernel, k, okay? ​And the input is 10, 50, 30, 40, 50, 80, 90, 80, okay? ​So we aim this center of the kernel at the designed input, and then we sum the neighboring ​input with the kernel weight, include itself, and then we get the result as the feature ​map result.  Play video starting at :1:21 and follow transcript1:21​Now for this edge input, we had an actual zero, therefore we get three values, because ​this is a three, zero by three, one by three kernel, okay? ​So this is the formula for compute the kernel, the feature map value based on the kernel. ​You can see that we only look at the nearby input, okay? ​And then this on the right side is a two-dimensional convolution, where it has the kernel of three ​by three, two-dimensional kernel, so again we map this middle or the center of the kernel ​map into the input field, then we compute the weighted sum of those neighboring input, ​you get the result of three, okay? ​So of course, in a convolutional neural network, using the kernel to do the convolution, we ​call it a convolutional layer, okay? ​So this is a convolutional layer example, okay? ​First of all, that you have an input value in this red, it has the input that may have ​multiple depths, for example, multiple channels, okay? : Added to Selection. Press [⌘ + S] to save as a note Like Dislike Report an issue : Added to Selection. Press [⌘ + S] to save as a note