​Now, for a convolutional neural network, you must have at least one convolutional layer. ​But usually you have multiple convolutional layers, layer 1, layer 2, layer 3. ​So each layer of convolution can use as the output of the previous layer's convolutional ​layer can be an input of the next convolutional layer, OK? ​So the output then feeds into the next convolution and feeds into the next convolution. ​Well, and the last convolutional output volume often be flattened to make it a fully connected ​neural network. ​Basically, the output volume and then flatten them into all the neurons and then create ​a fully connected network. ​Then generate the final classification or extract the final features, whatever, OK? 

​So the number of layers are important because, as we know, we are looking at the features ​locally, right? ​So you have more layers, you can see a wider field, OK, receptive field. ​So the higher layer is able to learn from a larger receptive field, right? ​So that's why in the early 1990s, DaneNet has only eight layers. ​Nowadays, it is common to have 100 plus layers with DaneSnap like a ResNet, OK? ​So because they can see more. ​And this is a typical example of a convolutional neural network for image recognition. 

​And basically, it's just looking at images of input of handwritten digits, OK? ​So 0, 1, 2, 9, 0, 1, 2, 3, 4, 5, 9, OK? ​So basically, what it has is use a 5 by 5 kernel, and the input is paired to 28, OK, ​with the edge is paired. ​And then, of course, the output will become 24 by 24 times N, N1, and that's the channel, ​OK? ​Then we do the max pooling, the max pooling 2 by 2. ​The max pooling, you can see that the pooling here is actually with the 2 by 2 pooling, ​you reduce the size by half, OK? ​You reduce the size by half. 

​Now the pooling can have also strides, OK? ​Now you can stride by 1. ​Usually the pooling 2 by 2, you have a stride by 2 so that you can reduce the size, OK? ​Now, so you reduce the size 12 by 12. ​Then on this 12 by 12, you apply the 5 by 5 kernel again, and of course, you reduce ​the 5 by 5 kernel on that, you know, without the padding, without the padding, the size ​will become 5, W is 12 minus 5, then plus 1. ​So basically, it's 8 by 8. ​And then you do a max pooling on this, so it becomes 4 by 4 times N2. 

​Now, then you flatten, you flatten them into a single input, and then create a connected ​layer, OK? ​And then you do a, and with the, and then with the Reno activation, and then you create ​another layer. ​So this is the hidden layer, OK? ​And three units, then with another layer of output, 0, 1, 2, 9. ​So each output, and then, of course, this fully connected, you can do, when you train, ​you can use a dropout to train, OK? ​For, you know, every iteration training, you drop out half, 50% of dropout. ​So you will get a 0, 1, 2, 3, so as the class is output. 

​So this is a convolutional neural network here, OK?