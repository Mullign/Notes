​In this video, we're going to study recurrent neural networks. ​And when we're handling the sequences, it is better to design the recurrent neural networks. ​So at each time step, the input and the previous, the hidden state, are fed into the network. ​So you have a time here. ​So like in this network here, you start with a hidden state. ​And then you also have an input, like Illinois is the land of Lincoln. ​So basically, your input is Illinois as the first input with the initial hidden state. 

​And then there's a time as a weight matrix. ​And then also has a weight matrix U for the input. ​Then the next token is, and it's made as an input. ​And then the next hidden state is the result of the previous hidden state, ​and the weighted on the previous hidden state. ​And then you get the current hidden state. ​So that means ht is equal to a function of uxt, the current input, ​plus wht minus one. ​That's the previous hidden state. 

​So you can see the hidden state is kept throughout the entire process, ​passed along the sequence, and then passed as a bias. ​Finally, when the sentence is fed through the network, ​and then you have the hidden state of five, h5, ht, ​and its output is a weighted output, vht plus a bias. ​That's the yt, that's the y hat five. ​And then you apply the loss functions, and then you generate the two target values. ​So you can see this is a recurrent neural network. ​So let's look at the difference between the recurrent neural network ​and the feedforward neural network. ​Given the feedforward neural network, ​so basically they have a hidden state, ​but every hidden value is equal to a fraction of the previous hidden value plus the bias. 

​But they have different weights and different hidden values. ​In this case, the key difference is that you apply the same hidden to hidden weight matrix w. ​Not like a different layer has a different weight matrix, ​and they are calculated, they are adjusted based on backpropagation, ​or they are trained by backpropagation, right? ​So the feedforward neural network, you have different weights. ​The recurrent neural network, the hidden to hidden weight matrix is w, the same. ​And also the input to hidden weight is the same matrix u. ​So it's the Illinois, and then there's the u, okay? 

​And then also the bias at the different layer in the feedforward neural network is maybe different, ​but in the recurrent neural networks, the bias vector is the same, a, okay? ​So that is the true difference, okay? ​And also, if you look at a sequence, we can view a sequence as a one-dimensional grid data. ​Then what you can do is you can take a one-dimensional CNN, ​but the problem is that one-dimensional CNN will only look at the local information by convolution. ​Say, if you use a three-by-three kernel, and then you only get the local information. ​However, sometimes a sentence has a non-dependency, right? ​Say, you cannot capture the longer-term dependency, like Illinois or Lincoln. 

​So that is a non-dependency, so you cannot do that with the one-dimensional CNN ​because the feature map only has a partial relationship there. ​So that's the main differences, okay? ​So you can see that RAN is specifically designed to deal with the sequences, ​especially for sequence data with context relationship between the input, different times input, okay? ​So there are several different types of RANs. ​The first RAN is called RAN without output. ​So given any input, x1 to xt minus 1, xt, xt plus 1, X capital T, ​and just transform the hidden state, okay? ​So every time, just convert it to a hidden state. 

​Then your final result may be just extracted out from the hidden state there. ​And then the second one is with, given the RAN, you will generate a sequence of output, ​y1, y2, y3, yn, y plus yt, okay? ​It's used in image capturing where you have to do a vector of images, okay? ​And also you can have, in this hidden state, it's hidden from time 1 to time 2, time 3, time 4. ​You can also apply a backwards hidden state. ​So instead of just from time 1 to T, you can start time T to 1. ​Well, so that way, you combine the time. 

​So your input is a combination of hidden state 1 and hidden state h1 and q1. ​So q1 actually contains all the information from the T, T minus 1, T minus 2, blah, blah, blah, goes on. ​And then, so it includes the previous information and the future information. ​So this is a bidirectional RAN, okay?