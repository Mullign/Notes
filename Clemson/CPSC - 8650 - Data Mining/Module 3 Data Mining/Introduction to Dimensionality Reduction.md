![[Introduction to Dimensionality Reduction Slides.pdf]]
Now we get to the dimensionality reduction. So what is dimensionality reduction? So dimensionality reduction is like we have attributes, right? We have many, many attributes. So unlike the data reduction, we focus on each attribute to reduce the data value or reduce the number of data values. In the dimensionality reduction, what we want to do is to reduce the number of attributes, OK? So reduce the number of attributes. 

Therefore, when we do the data mining, we use less attributes. Because when you have a large number of attributes, data mining algorithms face an issue called the dimensionality curse of the dimensionality, OK? So there are three different dimensionality reduction methods. One is principal component analysis. We will study that. And then we will study the attribute subset in action. And then we will also discuss the nonlinear dimensionality reduction method. 

OK, so dimensionality reduction, as we said, we want to address the curse of dimensionality. Because when dimensionality increases, data become increasingly small. So if you use the density or distance measure of the point, and you will see there is no clusters, or it is hard to identify the outline, right? So your algorithm, apply the algorithm to this high-dimensional data, become melanin, because you cannot find those clusters, or you cannot find the outline. However, if you combine some subspaces, then the possibility of finding those outliners or clusters become exponentially increased, OK? So dimensionality reduction is trying to reduce the number of attributes or random variables based on the current attribute with the relationship with our objective, OK? So we want, of course, to obtain a set of most important variables. 

You don't want to get a set of variables are irrelevant to our objective, OK? So using the dimensionality reduction, we will avoid the curse of dimensionality. We will eliminate the irrelevant features and reduce the noise. And then we will reduce the time and the space required in the database. And then also, we can easier to visualize the data, because you have a large space, then it is large dimension space. It is hard to visualize, OK? So let's get to individual methods, OK? 

And dimensionality reduction method, you have feature selection. So what you do is you take the original variables, remove some of irrelevant features or attributes, OK? Or feature extraction, well, you transform data in the high dimensional space to a space of fewer dimensions. Or maybe you just combine several features together. So this is kind of dimension transformation, OK? So let's get to some typical dimensionality reduction methods. First is principal component analysis. 

So the principal component analysis is trying to find a subset of original variables that are most important for your data mining task. And then attribute subset selection is that we will select the subset attributes based on their importance. Now, principal component analysis is you actually come up with the new attribute by using PCA method, OK? And then you can also have a non-linear dimensionality reduction. We will discuss that. Thank you.