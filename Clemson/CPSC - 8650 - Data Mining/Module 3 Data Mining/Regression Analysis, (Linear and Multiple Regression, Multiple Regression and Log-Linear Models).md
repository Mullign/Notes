![[Regression Analysis, (Linear and Multiple Regression, Multiple Regression and Log-Linear Models).pdf]]
Now, parametric data reduction, one of the popular method is called the regression, okay? So regression analysis, so there is a connect name, a connective name for the techniques for the model and analyze the numerical data, which consists of values of a dependable, dependent variable. So that's a dependent variable. Then all the other variables, so are called independent variable, okay? So the dependent variable is also called as a response variable or measurement. And then the independent variable called the explanatory variable or predictor, okay? So we try to estimate the parameters to make that data fit this assumption, okay? 

So that means the assumption is on the dependent variable relationship with the explanatory variable relationship, okay? So dependent variable has certain relationship with independent variable, okay? So most commonly used is the best paid is evaluated by using the least squares method, but you can also use other method, okay? So this regression analysis method can also be used in prediction, okay? For example, doing the focusing on the time series data or do inference or do the hypothesis testing or modeling the causality relationship, okay? Let's first get some concept on the regression. Regression can be a linear regression and multiple linear regression as well. 

And then we can also that we have a nonlinear regression as well, okay? So linear regression try to fit the data into a straight line. So the assumption is the data follow the straight line. So in this, we have, say, assume we have only one independent variable, x. Then our dependent variable, y, will be the non-y equal to wx plus b, wb are two parameters, okay? So we often call it two regression coefficients. Now, x can be a set of variables, then there'll be some like w1, x1, w2, x2, w3, x3, plus b, okay? 

That's called a multi-regression. So you will use the least square criteria, okay? So you also have, you must have some known value, okay? So those known value has the x and the y. So those are the samples, okay? So then you take those samples to estimate the wb, and therefore, using the least square criteria where each unknown sample distance to the line, the sum of distance to the line become minimized. So that's the least square, okay? 

Now you can also assume that the data follow a nonlinear model. So, well, the data is modeled by a function, which is a nonlinear combination of model parameter. And they're dependent on the one or more independent. So your variable is one or more than one independent variable, and then your dependent variable is just one value. Your dependent variable is dependent on those independent variables follow the nonlinear model, okay? So most often you can fit this method data using a successive approximation. So you try to change the parameters so that the data is getting closer and closer to the model. 

So those, let's use two examples, okay? One example for the log data, so you can see that y equals b0, that's the bias, you can call it b0, and b1, x1. So what we want to model is estimate b1, b0, b1, and b2. So response variable or dependent variable y will be modeled in a linear function of multiple dimension vector of x1, x2, okay? Similarly, for the nonlinear function, if you apply the kernel function, actually you can transform those nonlinear functions into a linear function, okay? And the nonlinear model, so basically that is a nonlinear function tried to transfer into a linear function. What it does is take the form of a function whose logarithm is a linear combination of parameters of the model. 

So the original data is not linear, but take the logarithm, become the linear, okay? So you estimate the probability of each point, which is a tuple, in a multi-dimensional space for a set of discrete attributes based on a small subset of dimensional combinations, okay? So this method can be used in either dimensional reduction and data smoothing as well.