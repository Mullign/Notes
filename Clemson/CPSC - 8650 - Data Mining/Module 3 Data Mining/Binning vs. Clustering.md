![[Binning vs. Clustering.pdf]]Without supervision, you may have some issues. For example, taking the equal depths or equal distances, that if your data is in category, for example, this is the first upper left graph is the data scattered part of the data. You can see that the blue data is around 5. And then KM data is around 7 on x-axis. And then yellow data is around 10 on x-axis. And then orange data is around 15 on x-axis. OK, x-axis. 

So you can see that this is the original data. Then what do you want to do? Say, for example, I want to do an equal width. Equal width, so we will partition the data from 0 to 5, 5 to 10, 10 to 15, and 15 to 20. So you can see you will have to cut, you put blue, which is category, into bin 1 and bin 2. And then you put yellow into bin 2 to bin 3. You put orange into bin 3 and bin 4. 

OK? However, it is not normal for you, because you split the value into two bins. That's the problem. That's the category data problem. OK? And you can also say that in the bin 2, you have three categories of data. It doesn't matter if you use equal width or use equal frequency. 

You always have similar problems, because equal width does not consider the natural distribution of the data. So what you can do is you can use another unsupervised message, which is called the class tree. Now, this study, later on, we will study K-means class tree algorithm. And then if you use the K-means class tree algorithm, you can actually create three different bins. Well, one bin, so the boundary is clearly cut those four different categories of data. OK?