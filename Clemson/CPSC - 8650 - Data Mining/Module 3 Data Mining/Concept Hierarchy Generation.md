![[Concept Hierarchy Generation Slides.pdf]]Discretization can also use concept hierarchy method and try to generate the concept hierarchy. So this is usually applied to the nominal or ordinal data, but it can also apply numerical value through the automatic method. So concept hierarchy also organizes the concept or attribute values hierarchically so it can't use it with one dimension in the data warehouse. So it is how concept hierarchies facilitate drilling and lowering in data warehouse to view the data in multiple granularity. So what it does is try to recursively reduce the data by connecting and replace the known level concept and into a higher level concept. For example, you get the numerical value for age, then you try to generate a higher level concept such as youth, adult, senior, and then or you can generate the other higher level concepts such as degree, senior, and so on. So concept hierarchy is trying to recursively reduce the data. 

So you build a higher level concept, and then you merge them into the groups, and then you further merge the groups. Say for example, you get the youth, you can merge the adult and the senior into a higher level concept. Now concept hierarchies are usually specified by domain experts. So you need to know the data and the concept, or the data warehousing designers say, I want to merge those data into the group. Now of course, you can also automatically form the concept hierarchy by analyzing the data value. For example, you can see that in that attribute, the data are forming several groups, and then you name those groups. For example, in our previous data set, then we can do a cluster analysis to form a higher level concept, and then we can automatically do it. 

Say around 5, around 7, around 10, around 15, those are newer concepts, which is automatically created, right? Use this discretization method to do it. Or for the nominal data, and then we can also form the nominal, say if you have the data, so you have professors going up, and then you can do it at a different, higher level. Now this may need to be done by both a mix of domain experts plus the data value, but in the numerical and nominal data, you may just be able to do it. So automatic, for example, you have a nominal data value such as gene terms, then you can come up with, for the numerical value, you can use the bin or you can use the other discretization method. For the nominal data, such as gene terms, if you can come up with a gene similarity measure, and then you can group that, and then generate a concept at a higher level, okay? And for the other part, you can also do that based on the schema level of users as experts. 

For example, you have a state, and you can have a city, a state, and a country. So you know the state is greater than the city, so the city is straight within the city, and the city within the state, and the country. So this is the user expert, okay? Then most of those are just like, hey, I use a concept of hierarchy generation and using the domain expert information, okay? Now you can also automatically generate the hierarchy by a number of distinct values, which, for example, you have, say, within the dataset, you have a different concept, and then the number of distinct values on the street is less than the number of distinct values in the city, and then less than the number of distinct values in the country. So they automatically form the hierarchy, okay? So, and of course, you can group the data, okay? 

For example, based on the, say, you have a city, and then you based on the city location and to group their range. Or you can take the map and map them into the different place. For example, Iberia, Champaign, and Chicago can be grouped into Illinois, right? And you can also just specify a partial set of attributes, okay? For example, you take a hierarchy from street to city, done.