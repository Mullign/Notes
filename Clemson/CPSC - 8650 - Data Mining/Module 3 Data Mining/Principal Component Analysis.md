![[Principal Component Analysis Slides.pdf]]
So what is the principle analysis called a PCA? PCA is a very popular statistical procedure that it will use an orthogonal transformation. So you can see there is a transformation here to convert a set of observations, that of observations of possibly correlated variables into a set of values of linearly uncorrelated variables called principle components. So what do we do? What do we mean this? So we have a set of attributes. But those attributes maybe, maybe, OK, we are not sure, but maybe correlated, or maybe it has some correlation. 

So the principle components are first uncorrelated variables, OK? So we will represent, then we use those uncorrelated variable, we call the principle component, then represent the data instead of using the original variable. OK, so that's the main idea. So what do you do is you project those original data into those principle components. And then secondly, we want those principle components become smaller. Because if you get a principle component, they are the same number of the original variable, then we don't have a dimension reduction, OK? So what do you do? 

You will take the covariance matrix of the original data, find the eigenvector, OK? So use this eigenvector to define the new space, OK? So what is the eigenvector? So let's get to the point, OK? Think about it. You have n data, OK? This is the method. 

So give it n data vectors from n dimensions. So your data matrix is the n by d, OK? n by capital N by lowercase n dimension. So then we want to find the orthogonal vectors, which is called the principle component. And we are not going to find the n orthogonal vectors. We are going to find the k only, OK? OK? 

And then those k orthogonal vectors best used to represent the data. So what do we do? First of all, we will normalize the inputted data. So that all the attributes, because you have different attributes, right? So we want the attributes to fall into the same range. Because once they are falling into the same range, we can compute, compare, OK? Secondly, we compute the k orthogonal vector. 

So the orthogonal vector is the unit vector, OK? So now each inputted data, the original vector, becomes a linear combination of this k principle component vector, OK? So basically, it's a linear combination of those components, OK? OK. And then we sort those orthogonal, the principle component, based on their significance in order of, in a decreasing order, or in the strength in a decreasing order, OK? And then, so what do we do, OK? If you go to the Wikipedia, and you will find the method basically is like, you have the same thing, OK? 

Then you have the covariance matrix, OK? You find an orthogonal vector in which the direction of that vector represents where the data has largest covariance, OK? Like this. So you start this data, OK? Say I want this space, I want on this right side, OK? So your original space is x and y. However, the data, we will take the data, build the covariance matrix, and then we will find, hey, there is an eigenvector on this, the first and longer vector here, which is on that direction, has the largest covariance. 

And then you get, so your next vector is, has 80, 90 degree of angle with this first vector. Therefore, we will make sure that all those vectors are independent, OK? So your next, if you get another component, that component must be having a 90 degree angle with those two components, OK? So you do the same, continue to do the same, OK? So you sort the component, the size of data therefore can be reduced. If you get some weak component, you can remove that. Because you get the largest variance, then on the second largest variance, then the, so what do you do? 

The method, if you think about it, the method is straightforward. By finding the largest variance, get the largest component, then you remove that component, and then take the rest of them, you map, OK? And then you do the second one as you're on the other direction, OK? Basically, orthogonal direction. So you do this, and then you come up with all other vectors, OK?