![[Data Cleaning Noisy Data Slides.pdf]]And sometimes the attribute, the data is there, but they are noise because the random error or variance in measured variable, okay? So of course incorrect attribute value maybe happened because of several reasons. The first reason is faulty data connection instruments where you have a sensor and that sensor is broken and then it may not even record the data or even record the data every time it records the constant value, okay? And then the data entry problem and somebody mistype your name or mistype your birth date and then causing the problem, okay? Causing the inconsistent of your birth date and your age, something like that, okay? And then data transmission problem when you send the data through the internet or you enter the data at the terminal and then the data is recorded in the data back in the database and then during the transmission, the data changed, so the data incorrect, okay? Of course, there is a technology limitation, for example, at a certain time, you only have 8-bit data and then now you need 32-bit data value. 

So you have to, you know, there is no way the data is correct. And also, for example, you take an image, now your video camera only has, say, 64 KB and then you want 1024x8 or you need a higher resolution, right? Then in this case, you have a problem. And there's another issue, like inconsistency in naming convention, okay? So sometimes you're using this name convention and then another one, another time you use another naming convention, okay? So for example, even for the same name in different country, and then you put the first name and the last name, and then you put last name and first name. So there, you say just the name, and then my name is James Wang, and then another country is Wang James, okay? 

Now of course, there are other data problems, such as duplicated records, incomplete data, inconsistent data. Those we can call noisy data. So to handle the noisy data, there are several popular methods, okay? First is called binning. Binning is trying to first sort the data and partition them into, say, equal width or equal frequency bins. Then you can smooth by bin means, or smooth by bin median, or smooth by bin boundary, et cetera. And the next way to handle the noisy data is called the regression. 

So you try to fit the data into a certain regression function, then smooth the data to remove the noise. And then clustering is another way to remove the noise or outline this. So what you do is you use a clustering analysis algorithm to identify the clusters of the data, and then for those outside any clusters, and be viewed as outliners. So outliners be just removed. And then of course, sometimes computers cannot remove or correct all the noisy data, and then what you can do is you need to combine the computer and the human inspection, okay? So basically what you do is you use the computer and norm the human for the potential missing value or noisy value, say, a suspicious value. And then human will double-check, verify, okay?