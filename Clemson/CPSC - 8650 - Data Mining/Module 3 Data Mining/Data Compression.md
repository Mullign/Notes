![[Data Compression Slides.pdf]]Our next data reduction approach is called the data compression, okay? So what is a data compression? Data compression is trying to reduce the number of, in our discretization method, we try to reduce the data size by putting them into different groups, right? And data compression is a way to actually reduce the data size by compressing the data, okay? So for the string compression, there are many methods, primarily based on the entropy-based approach, where you have Shannon-Fano or Huffman or some other method, or LZW. So those methods are taking a string, trying to compress them based on the probability of the characters appearing in the string to compress the data, okay? So you take an entropy-based approach, and so those string compressions are typically noisy, okay? 

But the compression rate is very limited, usually, okay? So for that, audio and the video compression is different, because audio and the video, first of all, has a large data set, data size. Secondly, audio and the video can tolerate small data loss. For example, you cannot tell the difference with, say, for example, the color with RGB, okay? And so if you're giving you 100, 100, 100, that's that color. And then you change, okay, so say you change one of these values from 100 to 80, human eye may not be able to tell the difference. The same goes to the audio. 

If you have a large explosion happen nearby, most of the other voice around this time will not be heard. So we can use those methods intentionally to remove those data which may not affect the expression or may not affect the human experience, okay? So we come up with the audio and the video compression, okay? And also, you can do the video-audio compression by conversion. You can reconstruct data into a smaller, into a different format. For example, you take an audio, you can use the first Fourier transformation to transform into the frequency domain, and then you take the major components, okay? And then you take the major, say, I take the sub-bond, and then we do the compression. 

We remove some less intense values, and then we take that, recover the data, the data is different, but they have almost the same quality as the original audio. We can do the same as video, say, for example, we can use the DCT method to do the compression. Or you can use DWT, okay, method to convert the data from the spatial domain into the frequency domain. Then you only keep major components, say, for example, we take the LL, because we, for the DWT, we convert it to LL, LD, LH, HL, HH. So we may just ignore the HH, LH, we just use the LL, which will reduce the size, but contain the majority of the information there. Or we can do some other method, okay? Now audio and video can be compressed using the Nozick compression, however, don't try to use this time sequence, okay? 

And first of all, time sequence is not the video, first of all, they're typically short, so the data size is not that large. And secondly, they vary slowly, for example, the stock price goes day by day, right? So you cannot just, oh, you grow some data, or you do compression there, so that's not very good, okay? So if you look at the right figure here, that it represents the Nozick compression, so the red box is the compressed data, right? So the original data and compressed data, and then the Nozick compression, when you decompress the data, it can recover the original data, however, if you use the Nozick compression algorithm, you can only come up the approximate original data. You can never get to the original, okay? Now data reduction and dimensionality reduction may also be considered as a form of data compression, even though we are not corning it, but still, they reduce the data size, right? 

So this is the one method that automatic concept hierarchy generation here, we discussed already, right? So based on analysis, the number of distinct value attributes in the data set, so you can see the attribute with the most distinct values will be the bottom for the straight, and then this is the second one, and then it goes higher. And then also, so the attribute with the most distinct values placed at the lowest, okay? Yeah. Now, of course, there are some exceptions, like weekdays, months, quarterly, years, you may not get a distinct value like that, okay? So yeah, it depends on the data itself.