![[Introduction to Preprocessing Slides.pdf]]Okay, in this module, we are going to discuss the data preprocessing task. So what is data preprocessing? Data preprocessing is trying to clean the data, integrate data, or transform data because the data quality may not be good enough. And also, sometimes the data dimensions are too high, are not suitable for data mining algorithms, or maybe the data mining algorithm runs too slow with the dimensions. We need to do the dimensionality reduction. Okay, so let's get to the first issue. What is the data quality, right? 

So data quality includes, like, the data is not correct, okay? We have missing data, smooth, the noise data, or outliners, okay, or inconsistencies. So for that, we have data cleaning tasks. Then we may have data from different data sources, then we have data integration issues as well. And of course, dimensionality issue is another important issue to be handled. We want to reduce the dimension, okay? And then also, sometimes the data value is too large, and then we have numerosity reduction problem. 

And then sometimes the data is too large and needs too big storage space, and we have to do data compression. And data transformation and data discretization include the normalization or concept hierarchy generation. So why do we need the preprocessed data? Because the quality issue, as we mentioned. Now the quality of the data can be viewed from multiple dimension views, okay? And the first one, we can view that from the accuracy point of view, where if the data are correct or wrong, or are accurate or not. And then we can view from the completeness, okay? 

Maybe like some data were not recorded, or some data may not be available, okay? And then the consistency view, where some modified, but some not, or there is a dangling point. So for example, the foreign key, and then the primary key is removed, the foreign key is dangling. There is no reference to record, okay? And the timeliness, where the data may not be timely updated. Then another point of view is the believability, so how trustable the data are correct, right? Then finally, the interpretability, so how easily the data can be understood by humans. 

All right, so those are data quality issues. So data preprocessed is to address those quality issues.