![[Linear-vs.-Nonlinear Slides.pdf]]Now, let's look at this stem data set, and then we run the different KPM, rerun the different PCA methods. The first one you run the linear PCA, so the PCA transformation, and then on this one, you can see those two data sets are not input data, those two clusters are not linearly separate. So even if you run the PCA, because linear PCA is doing the linear transformation, right? So the result is still linearly inseparable. However, if you use the KPCA, the graph figure C, you can see those two data sets become linearly separate. I use the T-SNE, and then they are also linearly separable, okay? So you can see that's the difference. 

So looking at the heat map of the proximity matrix of linear or nonlinear matrix, you can see that for the PCA, the two diagonal box indicates the proximity within the two clusters represented. So it means the similarity, so the data within the two clusters, they are similar, but they're not that similar, okay? However, the two off-diagonal boxes indicate the proximity between the data from two different clusters. You can see that these two different clusters, they still have some, a lot of similarity. So there's values there, similarity values there. However, you can, from using the RBF KPCA, and you can see the diagonals, the diagonal box within the cluster has a high similarity, and then the off-diagonal box indicates that between the cluster has a very low similarity, or very low proximity, okay? And then the same goes with the TSA, okay? 

Because you can see a lot of white space there, and then that's zero, okay?