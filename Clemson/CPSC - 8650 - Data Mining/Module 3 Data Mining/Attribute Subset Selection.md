![[Attribute Subset Selection Slide.pdf]]
Attribute subset connection is another way to reduce dimensionality of the data. So assume that we know that in our database, they also have redundant attributes, OK? So we want to duplicate much of all the information contained in one or more attributes and reduce into one or more, one or a small set of attributes. For example, if you think about it, you have a purchase price and the tax paid. Actually, they are duplicated value, right? Because tax depends on the tax rate, so the purchase price. And once the purchase price is fixed, your sales tax rate is fixed. 

And then your sales tax paid will be the same. So when you use any data mining method, they should be able to follow the same pattern. So you don't have to do it. So you may just leave the sales price, which is important for attributes. Or you can do some, OK? So you don't have to worry about sales tax because they follow the same trend, right? And also, there are some irrelevant attributes. 

For example, those content, for example, ID. ID contains no information that is useful for your data mining task, right? Because it doesn't matter what ID you're using, and it doesn't matter affect your GPA or affect your ranking, right? So that's just an ID. So for attribute selection, because you have the attribute, then the combination of possible selected attributes are two power of D, right? You can select the one attribute only, and then you can select the two attributes only. So it depends on how many attributes, then you have two power of D possible combinations. 

So the typical heuristic attribute selection method is by best single attribute selection. So what you do is you assume those attributes are independent, OK? Then you apply the significant test, OK? Significant test on each attribute. And then you choose the best significant test feed attribute first. So doing this, you can do a best stepwise feature selection, OK, or best stepwise attribute elimination. So the best stepwise feature selection is like you find the best single attribute, then pick it first. 

Then you take out this, then you compare the rest of attributes based on the significance, and then test again. So you take the next, and then go on until you say, oh, I choose enough, OK? Or you do a stepwise attribute elimination, where you repeat it, eliminate the worst attribute. So you remove the most irrelevant attribute, right? Based on heuristic method. For example, you do like assume those attributes are independent, you remove those id first, and then you run the significant test to find that this is the significant attribute, remove it, right? Or you can do both. 

You choose the best, remove the worst, and then you run, take the remaining, and then you run the same significant test, choose the best, remove the next. And then you could do that, OK? And you can also do an optimal branch and the bound using the attribute elimination, and then do a backtracking. OK, so those are the typical attribute selection method. Now, sometimes, you may need to create a new attribute or feature, which can capture the important information in the data set more effectively than the original one, OK? So those methods, for example, you do an attribute extraction. So those are domain-specific, OK? 

And because you extract the features, you need to have domain knowledge about the data, OK? So that depends on different domains. You have different methods to do the attribute extraction, OK? And then, for example, you have the geographic data. Probably you want to map them into the city, county, and state, something like that, the attributes, OK? Or you can map the data into the new data space. For example, you have voice data, right? 

Voice data are a sample of voice intensity values. Then you can use the Fourier transformation to convert into the sub-bound frequency data. Or you use the wave transformation on the radio data. Now you can do manifold approaches, not covered in this course, OK? And then you can also combine the attributes into a new attribute, OK? And for example, you do the attribute construction using the frequent pattern map mining, OK? And then also, you can use the method studied before for the data discretization to reduce the attribute, OK? 

So you view the attribute as a set of nominal data, or each category of data. Then you try to discretize them. So there are many different methods, OK?