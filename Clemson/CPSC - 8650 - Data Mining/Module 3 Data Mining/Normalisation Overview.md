![[Normalization Overview Slides.pdf]]The first one is called the min-max normalization. So min-max normalization tries to map all the data, original data, to a new range. The range is represented by new min-a and the new max-a. So the formula is like this. You take the value, the old value, minus the minimum-a, which is the old minimum. Then divide the max-a minus the minimum-a. So max-a is the old maximum value, and the min-a is the old minimum value. 

So you can see that v minus min-a, well, divided by max-a minus min-a gave the proportion of the base value in the original data range. Then times the new data range, which is the new max-a minus new min-a. And then plus the minimum value, which is the starting point of the data range. So the starting point of the data range plus the proportion of the original data in the old data range times the new data range. And we get v prime, which is the new value. OK. For example, given an income range from 12,000 to 98,000, now we want to normalize the range to 0 and 1. 

So given any data value such as 73,600, we can map it by 73,600 minus the minimum-a 12,000 divide the maximum-a 98,000 minus minimum-a 12,000 times the new range, which is 1 minus 0 plus the new minimum, which is the starting point. So our result is 0.716. OK. So z-score normalization is another method. The purpose of z-score normalization is try to normalize the data to the population mean. So find the distance between the raw score and the population mean in the unit of the standard duration. So that is why you take the old value minus the attribute mean divide the attribute standard duration. 

OK. For example, given the mean value of mean salary is 54,000, standard duration is 16,000. Then the z-score of 73,600 become 73,600. OK. Minus 54,000 divided by 16,000 equals 1.2245. So what does that mean? It means that this 73,600 is 1.225 standard duration above the mean value. 

And normalization by decimal scaling is try to convert the data into a decimal. And where the value of the maxima normalized data has a value less than 1, less than 1. So the maxima normalized value is less than 1. OK. So you would divide by 10 power of j. j is the smallest integer, such that maxima value of the new data value is less than 1. For example, if you have a set of values, you need to find the maximum value. OK. 

And then you want to find the smallest derivative value. Therefore, your max divide of 10 power of j is just less than 1. OK. I'll use an example here. OK. For example, you have 1, 5, 10, 1,000. OK. 

So the max value is 1,000, right? So your max value is 1,000. So your j is the smallest integer of j. Make 1,000 next to 1 is 4. It's not 3. Because 1,000 divided by 1,000 is equal to 1, which is equal to 1. So equal to 1 does not fit the definition. 

OK. So j must be 4. However, if you have a 1, 5, 10, 999, then in that case, your j can be 3. Because 3, 10 power of 3, and then v is 999 divided by 1,000 equal to 0.999, which is less than 1, right? So normalization, decimal scaling, you have to make sure that j is the smallest integer such that maximum value of your v prime is less than 1. Now, this maximum, OK, is not meaning that the maximum is only going to positive maximum, OK? So if you have a positive value and a negative value, and so you need to make sure that the absolute value of your new data set. 

So for example, you have a negative 999, and then your maximum value, positive value, is 200. Then you need to use 3, j equal to 3. If you have a negative 1,000, but your maximum value is 200, that maximum value is not the value you will use. You need to use the absolute value as the maximum, OK? So that's a negative 1,000, and then divided by 10 to the power of 4. That's the negative 0.1, OK? So that's the tricky part.