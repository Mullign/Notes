![[Sampling Slides.pdf]]
OK, so our next method to do the data transformation is sampling. Sampling is trying to obtain a smaller sample as to represent the whole data set N. So by sampling, it allows the mining algorithm to run in complexity that is potentially become sublinear to the size of data. So if you have a complex data, you have a large data set. With the sample, you have a smaller data set. So you tremendously reduce the data set, and then your algorithm runs much, much faster. So key principle is to choose a representative subset of the data. 

OK, now there are many sampling approaches. We will just study the simpler ones, OK? And first of all, we will study the simple random sampling, OK? Even though that method performs very poor sometimes if the data is highly skewed, but we can also adjust that. Given that every sampling method such as stratified sampling, OK? Now, sampling is trying to reduce the data in the algorithm. So you still have to go into the database to read the data. 

Now, sampling may not reduce the database IO. For example, because database IO is reading one page in the IO. Within one page, it has many, many data. So if your sampling requires you to read all the pages, then you will not save the database IO, OK? Even though you do the sample first. However, you reduce the complexity of later data mining algorithms, OK? All right, so let's get to those type of samplings. 

The first one is called a simple random sampling. So what you do is you give each item an equal probability being taken out from the raw data, OK? So that's the simple random sampling. You take it out. Simple sampling, you have two methods, actually, OK? One is called simple random sample without replacement. So the S-R-S-W-O-R, simple random sample without replacement. 

What it does is you take the data from raw data, the raw data bucket, and then you put it aside. And then that data, in your subsequence, the connection will never be connected because it is taken out, OK? And then your next method is called a simple random sample with replacement. Now, this method actually will take the object out of your original population. Then put that data object back to the original population. So your next data sample may grab the same object. So it is possible two objects being sampled twice. 

Now, why we do that? And we will discuss how we use that simple sample with replacement and why we do that, OK? And later on, when we actually get into the classification algorithm and simple sample with replacement, our method will be used. And why we use it, how we use it, and we will discuss later on, OK? But at this moment, you just need to know, hey, this is two different methods. And then, of course, there is a problem with those two simple sample, simple random sampling method because when your data is highly skewed, you may somehow take data from only the majority groups because your sample is totally random, right? So what if you have five categories of data and only two categories of data, two categories of data, say one category of data having 80% of the data, the other four has only 20% of the data? 

So when you do the sampling to reduce the size by one tenth to only 10% in that case, it is highly possible you take all the data from the same group which occupy the 80% of population, right? So in such case, we can do a stratified sampling, OK? So the stratified sampling, first, we need to partition the data set, right? Or you can cluster the data, OK? And then for each partition, we will draw the equal proportion of data based on the entire proportion, the total proportion we take out as sample. For example, we want to have five groups of data. One group has 80% of data. 

The other four groups has 5% of the data each, right? So what you do is then I want to take 20% of sample data out. Then what you can do is you partition those groups first, right? So for that 80 group of 80% of the data, we will take 20% out of this 80% so that your new sample data contains 16% of the data. And then we take 20% out of that 5%. And then you have that group has 1% of representation. And then another three will have 1% of representation. 

So we call this as a stratified sampling, where each partition has approximately the same percent of data as the original data population, OK?