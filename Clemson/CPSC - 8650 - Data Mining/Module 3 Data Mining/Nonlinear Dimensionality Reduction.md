![[Nonlinear Dimensionality Reduction Slides.pdf]]Now, the nonlinear dimensionality reduction method is different in a way that in the PCA you assumption is that you want to find some orthogonal vectors is a combination of the original space, original data value is a linear combination of the original, right? So, it is a linear method for dimensional reduction because each principal component is the linear combination of the original input, right? So, it works well if the input data approximately follow the Gaussian distribution, okay? Or it forms a few linearly separable clusters. So, it only works there. However, if the input data is linearly inseparable, okay? Or you cannot, first of all, they are, maybe they are not Gaussian, follow the Gaussian distribution, you cannot use the covariance measure to find the, you know, build it. Or you cannot, the original data cannot form a linear separable cluster. 

Then, we need to construct a proxy matrix. So, the proximity matrix. And then, you will, then, based on the original data on the D spatial space, and your proxy matrix is an n by n proxy matrix into output data is the n tuples of data with the k features. So, now, your D is much larger than your k, then you will get a dimensionality reduction. So, of course, your output data, okay, that will preserve the proximity matrix, okay? Okay. So, your first step is to construct the proximity matrix. Then, by preserving the proximity values, and then you output a data with a small set of features. 

And it keeps the same number of tuples. So, each row is represented by other set of, each row represented by a k dimension, okay? So, there are two kinds of non-linear PCA, okay? First one is called the kernel PCA. Kernel PCA goes, you use a kernel function, okay, to construct the kernel matrix. So, kernel, so for each original data value, you build a proximity matrix P, called a kernel matrix P, okay? And then, we estimate proximity matrix P hat, so that it is as close as possible to the kernel matrix P, okay? Then, we use, so the idea is, we use the kernel to convert into a matrix. Then, your next step is to find an approximate, okay, matrix, or P, approximate the matrix P, is very close to the kernel matrix P, okay? 

So, what do you do is that, think about this, the original data is linearly separate, and then, you are, after you apply this kernel function, your new data become kernel, kernel new data set become linearly separate. Then, so your first step is to apply the kernel function, your next step is to apply the PCA, okay? To, you know, you can use a least square method, therefore, you know, your kernel proximity is the same, is very close to the, your kernel matrix, all right? So, that's what you do. So, it's using this kernel function, so there are two different kernel functions, you can use the polynomial kernel function, you can use the radial-based kernel function. Now, in, later on, we will learn that you can use the linear kernel to apply the linear kernel to some, say, SVM, or some other algorithm, but using the linear kernel, essentially, that your PCA degenerated back to the standard PCA, okay? So, another nonlinear method is called SNE. 

SNE is called Stochastic Neighborhood Embedding. So, you construct a proxy matrix P using this formula. This formula is actually represent the probability of two values are the neighborhood, okay? So, two attributes are values are neighborhood. So, you do that, so you will get approximate value equal to this first formula, where each d i j is their distance by x i minus j and take the square divided by the standard deviation of this, okay? So, okay, so this is how you do that, okay? So, assume we have learned the non-dimensional representation of x hat, then we can compute another approximate matrix in a similar way as P hat of i j. So, now, now, now, our objective is to make sure P and the P hat is the same, or very close, okay? So, because those two, we are probability, right? So, they, they are distributions. So, we want to use the KL divergence as we discussed before, right? We use the KL divergence, we minimize the KL divergence of P i and P i hat. So, your x i hat is the value in which it will minimize the P i. So, once we get this probability minimized, we get this KL divergence, the overall KL divergence minimized, then we reduce the dimension, dimension, okay? So, because we, our P i is has a smaller size, okay? Because with no dimension, okay?