![[Discretization Overview Slides.pdf]]Now we talk about the discretization, okay? And discretization, usually that we have three types of attributes, nominal value, okay, and values from an order of set, they are already discretized, right? For example, you have color, you have given color, they are discretized, right? And then profession, so you have different profession, you have given occupation, and then they are already discretized. Then ordinal data, now ordinal data, that value from an order of set, for example, military or academic rank, you can see they are discretized. So you have assistant professor, assistant professor, full professor, you have freshman, sophomore, and junior, and senior, right? And so discretization often applies to the numerical value where real numbers, okay? 

And it can be applied to the integer or real number, okay? Because the reason we want to do discretization is try to divide the range of a continuous attribute into an interval. So now the interval can be large, can be small, okay? And then we use the interval label as the replacement of the actual data value, okay? So discretization can reduce the data size. For example, if you have a group of values 1 to 100, now you can discretize them from 0 to 10, okay? 0 to 9, 10 to 19, and then go on, okay? 

Now discretization can be supervised, which you can train based on example. There are some supervised methods or unsupervised. So supervised must have example, okay? And how to discretize this example. Unsupervised, well, just based on the data characters and to do the unsupervised method, okay? And discretization can be top-down, where you take the entire data set and then split it into different ranges. Or you can do the merge, where you take the entire data set and assume that each individual value is a range, and then you merge those data values together to form the larger range. 

In that case, you get a smaller data size. And you can also perform the discretization recursively on the same attribute. For example, you create a data range, right? So you create those, divide into this range. And within a certain range, because they have a lot of data points, you can also take that data range to further divide, okay? Into the smaller range, okay? Now, data discretization is often used for further analysis, such as classification, okay? 

And to make new data mining algorithms easier, because that way you can apply the algorithm, okay? And so classification algorithms usually require you to discretize the data. Okay, let's get to the method. The first is the simple method, like a bin. So bin is a top-down split, and it is unsupervised. So what it does is try to split the data into different groups, okay? Into the different groups, binning. 

And then the next one is called the histogram, and it is also unsupervised. And it is a top-down approach, okay? And clustering analysis is also unsupervised, and it's a top-down split or bottom-up merge, depending on your clustering algorithms. We will learn those algorithms later on in our clustering analysis lecture, okay? And then decision tree analysis is a supervised method. It is a top-down split, and you build a decision tree, and then split the data into the range, okay? And you can also use the coronation analysis, such as chi-merge. 

It is a bottom-up approach. Well, you calculate the chi-square, chi-coronation of chi-square value between the data points, and then from that point, you try to merge the neighbors of those data, who has the largest coronation, okay? So based on that, you create a new set of data range, okay? Now, of course, you can apply this method recursively. For example, you create a larger range, you can also further do the merge. Or you can split, you can further split on the certain range. So it depends on how you do it, okay? 

You can take a subset of the data to do the merge, or you can take one range, already created the range, and you do the further divide, okay?