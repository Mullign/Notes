![[Classification and Correlation Analysis Slides.pdf]]Okay. Our next method is called supervised method. It's called classification. Okay. Now classification is, for example, you can use a decision tree. Okay. It is a supervised method. So you must have class neighbor. For example, you have a cancerous or benign. Okay. 

Then the decision tree analysis will use the information gain or use information gain ratio. Basically you compute the entropy to decide the split point. Okay. That is the point where your discretization happens. So you start with the entire data set as one group, then split based on the splitting point. Okay. Now we will discuss this later on in the classification method. Of course we can use a chi-merge. It's a chi-based discretization method. Okay. So this is a supervised method because you use the class information. Okay. You use the class information. 

So it is the bottom-up merge. And so what do you do is you take the value to calculate that neighbor chi-square value. Okay. So for those having similar distribution of the class, for example, no chi values, then you merge them together. Then your merge can perform recursively. Okay. Until a predefined stop condition matches. So you have two conditions. First of all, you need some similar class information. Okay. Well, you need to make sure that they find the data having the similar distribution of classes. Secondly, you need a predefined stop condition. Okay. And chi-merge is a correlation analysis method. Okay.