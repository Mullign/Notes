![[Histograms, Clustering, Sampling Slides.pdf]]
And histogram, we already studied several times, okay? You divide the data into buckets and store the sum for each bucket or average of each bucket or store the total number. So it depends on how you do it. You use, for example, if you want to do a data transformation, then you can store the frequency there. If you want to just do the data reduction, you can use the average of those buckets or use the sum of those buckets, okay? Now there are two partitioning approaches. One is called the equal weight. 

So your range of each bucket is equal, called equal weight, or you use an equal frequency. So within each bucket, you have the same number of values. So you can see that this is also similar to the bin, okay? The bin difference, like a bin used for smoothing, then you choose the mean or median value or mean or boundary value to represent the original value, and then smooth the data, so you didn't reduce the data. In this situation, you reduce the data by taking each bucket has only one value now, okay? And the value can be a sum or an average. Clustering is another method. 

So you partition data set into clusters based on the similarity, and then store only the cluster representation, such as centroid, diameter, or radius, or maybe like the cluster. So you create this cluster. You can also store the density of density value, okay? Centroid, density value, diameter, et cetera, okay? But this approach requires that data is clustered. If the data is not clustered, the method is not effective, okay? Now you can also apply the hierarchical clusters, okay? 

And then you store these clusters at each hierarchical level, okay? So you create a multidimensional index tree structure. There are many clustering definitions and clustering algorithms. Later on, we will study them, okay? So we are going to study them in the later chat.