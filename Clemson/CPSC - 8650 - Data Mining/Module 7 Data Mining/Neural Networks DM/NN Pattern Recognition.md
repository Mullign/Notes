Neural networks are studied in the pattern recognition. An important application of neural networks is the pattern recognition. So pattern recognition can be implemented by using a feedforward neural network that has been trained accordingly. So we'll discuss what is called a feedforward neural network, okay? During the training, the network is trained to associate the output with the input pattern. So when the network is used, it identifies the input pattern and tries to associate the output pattern. When the input pattern has no output associated with it, the network gives the output that corresponds to a taught input pattern that is this different from the given pattern. 

So that's how a neural network is applied in the pattern recognition. Let's use an example to demonstrate what a neural network will recognize the pattern, okay? So assume we have two inputs, okay? For input, the T and output will be all black, one black eye. And then for input, the edge will have an all-white output, okay? So this is a network that is trained to recognize the pattern T and edge. The associated patterns are all black and all-white respectively as shown above, okay? 

Now, on the left side, if you have an input that has missing one black on the right corner, so then the output should be T because on every row, they are more like T than an edge because in the first row, you have three blacks. And then in the original, T is here. And then there's two out of three black boxes, so you have black. And then the other one is a white, black, white, the second row, so it's black. And instead, there was a white, black, white, so the output is black. So your output is the closest resembled to T, so that output said it is a T. So the pattern recognition, the neural network will recognize as a T. 

On the other hand, on the right side here, that you have the top is black, white, black, so it's exactly as an edge, right, as an edge, so the output is white. And then the second row is black, black, black. It's also similar to exactly as an edge, so the output is white. And then the very bottom here, the very bottom is only the middle block is black. The edge, the middle block is white. So it's very close to an edge, okay? So they're just a very, just a slight difference. 

So the input here is closely resembled edge with just a slight difference. The network in this case will classify it as edge even though he has not seen it or there's no association, associated patterns there. So you output as an edge or white, right? So this is simple. How about this? What if this pattern here, so your input is the first row's white, black, white. So edge, white, black, white, and it's not, first of all, if you look at this pattern here, right, this pattern here, okay, and white, black, white, okay, white, black, white. 

And so the first one, the T is black, now it's white. And then the second one, the T is black, it is black. The third one is the T is black, it is white. So the one out of three blocks are similar to T. However, for the edge, the first one should be black, the second one should be white, the third one should be black. So none of them are similar to edge, okay? So, of course, it is more similar to T than edge because T will give you only two errors, but the edge has three errors. 

So we will output it black, which means we think that is T, okay? Then on the second one, okay, on the bottom, let's look at bottom one. The bottom one is the same again that for the T, for the edge, okay, for the edge is black, black, white, okay? So that the black, white, black, that's the edge pattern. Now, the first column here, the last row or first column should be, for T, should be white, and then for T, should be black, and for T, should be white, now it is black. So there is one error, one mismatch on the last row for T. And then for the edge, the first one is white, and that's the mismatch. 

And then the second one is black, but the edge should be white, so another mismatch. And then third one is black, the match. So it's only one out of two matches with the edge. And two out of three matches with T, so this one should output edge as well, okay? So, how about the middle one? The middle one, the edge is black, black, edge is black, black, black. So two out of three matches, right? 

No, no, no, T, okay, T, look at the T. T is white, black, black. T is white, black, white. This is the T, right? White, black, white, black, white. So only the third row mismatched with T. So you can output T, they're all black. 

Or you can, and on the edge is F, on the edge is black, white. On the edge is black, black, black. So you have also two matches. So on both, on edge or T, there is only one mismatch. For the T, it's mismatched on the last column. And for the edge, it's mismatched on the first column. So the output can be either black or white, okay? 

So you can see that all over, our output is all black or two thirds of black. So we can see the output resembles T more than H. So usually in that case, we should put this as T, because you have more Ts. You have three Ts. If your output is this, all blacks, then it is T. Even if your output is two blacks, one white, it's still more closer to T. So the network will recognize it as a T, okay?