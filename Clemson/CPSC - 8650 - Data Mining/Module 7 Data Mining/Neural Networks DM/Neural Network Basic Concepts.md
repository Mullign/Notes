In this module, we are going to study neural networks. So we will start with the basic concepts and then we will learn perceptual learning and artificial neural networks, and then we'll study the backpropagation algorithms. Okay, so what is neural network? Neural networks are a new method of programming computers. They are exceptionally good at performing pattern recognition and other tasks that are very difficult to program using conventional techniques. So programs that employ neural nets are also capable of learning on their own and adapting to changing conditions. It actually started in early 1940s. It became popular in 1980s due to the advances of computer technologies. Now, much of the inspiration of the field of neural networks comes from the desire to produce artificial systems capable of sophisticated, perhaps intelligent computations similar to those that the human brain routinely performs, and thereby possibly to enhance our understanding of the human brain. 

So, what is the difference between neural network techniques and the traditional computer techniques? Computers have to be explicitly programmed to analyze the problem to be solved. So you have to write the code in programming language, okay? And neural networks are named from examples. So there is no requirement for an explicit description of the problem, and no need for a programmer, okay? The neural computer adapts itself during the training period based on examples of similar problems, even without a desired solution to each problem. Okay, after sufficient training, the neural computer is able to relate the problem data to the solutions, and relate inputs to the output, and then be able to offer a viable solution to a brand new problem. And also neural networks are able to generalize or to handle incomplete data. 

So this is a comparison of neural network and a traditional computer, okay? And traditional digital computer is a deductive reasoning. So we apply known rules to input the data to produce output, and neural network is an inductive reasoning. So you're given an input and output data. Those are training examples. We construct the rules, okay? And computation is centralized, synchronized, and the series in digital computing. And the computation in the neural network is collective, asynchronous, and parallel, okay? 

And in the digital computers, the memory is the packeted, literally stored, and then located addressable. The neural network memory is distributed, internalized, and the short-term content addressable, okay? And digital computers have no fault tolerance. You have to design the specific fault tolerance system to help the digital computer become fault-tolerant. So one transistor goes bad, it no longer works, so you have to do the high availability. High availability is a very popular business in traditional digital computer systems. And the neural networks are designed to be fault-tolerant, redundant, and then all the cells are sharing the same responsibilities. So even some cells or some neurons broken, the system can still work, okay? 

And the digital computers will give the exact result, and oftentimes neural networks will have an inexact result. And digital computing has static connectivities, and neural networks may have dynamic connectivities, okay? So overall, the neural computer is ethical if well-defined rules with precise input, and the neural computer is ethical if rules are unknown, or complicated, or if data are noisy, or partial, okay? So there are many applications that neural computers can be used to solve the problems, like classification. In telemarketing, you can study the consumer spending patterns, so doing the pattern classification with the neural network. In defense, like a radar or solar image classification, okay? In agriculture and fishing, you can use the neural network to do the fruit or catch grading. In medicine, you can do the ultrasound or electrical audio program image classification, EEG, medical diagnosis. And you can use a neural network to do the recognition and identification. In general computing and telecommunication, you can do a speech recognition, vision, and handwriting recognition. In finance, you can do a signature verification and banknote verification. And you can also use the neural network on the assessment. In the engineering part, you can do the product inspection, monitoring, and control. And in the defense, you can do the targeting tracking. In security, you can do the motion detection, surveillance, image analysis, and the fingerprint matching. 

And neural networks can be used for forecasting and prediction. In finance, for example, you can do the foreign exchange rate forecasting or stock marketing forecasting, okay? And in agriculture, you can use the neural network to do the crop yield forecasting. In marketing, you can do the sales forecasting. And in meteorology, you can do the weather predictions. So all of those applications are a very popular application nowadays that you can use neural networks to solve. Now there are some limitations though, okay? In principle, neural networks can compute any computable functions, such as, for example, that they can do everything normal digital computing can do. But in practice, neural networks are especially useful for classification and function approximation problems. Neural networks are difficult to apply successfully to problems that concern the manipulation of symbols and memory. Although there are no methods for training neural networks that can magically create information that is not contained in the training data. So it is very important to provide sufficient training data for neural networks to learn. 

In this module, we are going to study neural networks. So we will start with the basic concepts and then we will learn perceptual learning and artificial neural networks, and then we'll study the backpropagation algorithms. Okay, so what is neural network? Neural networks are a new method of programming computers. They are exceptionally good at performing pattern recognition and other tasks that are very difficult to program using conventional techniques. So programs that employ neural nets are also capable of learning on their own and adapting to changing conditions. It actually started in early 1940s. It became popular in 1980s due to the advances of computer technologies. Now, much of the inspiration of the field of neural networks comes from the desire to produce artificial systems capable of sophisticated, perhaps intelligent computations similar to those that the human brain routinely performs, and thereby possibly to enhance our understanding of the human brain.  Play video starting at :1:43 and follow transcript1:43 So, what is the difference between neural network techniques and the traditional computer techniques? Computers have to be explicitly programmed to analyze the problem to be solved. So you have to write the code in programming language, okay? And neural networks are named from examples. So there is no requirement for an explicit description of the problem, and no need for a programmer, okay? The neural computer adapts itself during the training period based on examples of similar problems, even without a desired solution to each problem. Okay, after sufficient training, the neural computer is able to relate the problem data to the solutions, and relate inputs to the output, and then be able to offer a viable solution to a brand new problem. And also neural networks are able to generalize or to handle incomplete data.  Play video starting at :3:8 and follow transcript3:08 So this is a comparison of neural network and a traditional computer, okay? And traditional digital computer is a deductive reasoning. So we apply known rules to input the data to produce output, and neural network is an inductive reasoning. So you're given an input and output data. Those are training examples. We construct the rules, okay? And computation is centralized, synchronized, and the series in digital computing. And the computation in the neural network is collective, asynchronous, and parallel, okay?  Play video starting at :3:57 and follow transcript3:57 And in the digital computers, the memory is the packeted, literally stored, and then located addressable. The neural network memory is distributed, internalized, and the short-term content addressable, okay? And digital computers have no fault tolerance. You have to design the specific fault tolerance system to help the digital computer become fault-tolerant. So one transistor goes bad, it no longer works, so you have to do the high availability. High availability is a very popular business in traditional digital computer systems. And the neural networks are designed to be fault-tolerant, redundant, and then all the cells are sharing the same responsibilities. So even some cells or some neurons broken, the system can still work, okay?  Play video starting at :5:13 and follow transcript5:13 And the digital computers will give the exact result, and oftentimes neural networks will have an inexact result. And digital computing has static connectivities, and neural networks may have dynamic connectivities, okay? So overall, the neural computer is ethical if well-defined rules with precise input, and the neural computer is ethical if rules are unknown, or complicated, or if data are noisy, or partial, okay? So there are many applications that neural computers can be used to solve the problems, like classification. In telemarketing, you can study the consumer spending patterns, so doing the pattern classification with the neural network. In defense, like a radar or solar image classification, okay? In agriculture and fishing, you can use the neural network to do the fruit or catch grading. In medicine, you can do the ultrasound or electrical audio program image classification, EEG, medical diagnosis. And you can use a neural network to do the recognition and identification. In general computing and telecommunication, you can do a speech recognition, vision, and handwriting recognition. In finance, you can do a signature verification and banknote verification. And you can also use the neural network on the assessment. In the engineering part, you can do the product inspection, monitoring, and control. And in the defense, you can do the targeting tracking. In security, you can do the motion detection, surveillance, image analysis, and the fingerprint matching.  Play video starting at :7:41 and follow transcript7:41 And neural networks can be used for forecasting and prediction. In finance, for example, you can do the foreign exchange rate forecasting or stock marketing forecasting, okay? And in agriculture, you can use the neural network to do the crop yield forecasting. In marketing, you can do the sales forecasting. And in meteorology, you can do the weather predictions. So all of those applications are a very popular application nowadays that you can use neural networks to solve. Now there are some limitations though, okay? In principle, neural networks can compute any computable functions, such as, for example, that they can do everything normal digital computing can do. But in practice, neural networks are especially useful for classification and function approximation problems. Neural networks are difficult to apply successfully to problems that concern the manipulation of symbols and memory. Although there are no methods for training neural networks that can magically create information that is not contained in the training data. So it is very important to provide sufficient training data for neural networks to learn.  In this module, we are going to study neural networks. So we will start with the basic concepts and then we will learn perceptual learning and artificial neural networks, and then we'll study the backpropagation algorithms. Okay, so what is neural network? Neural networks are a new method of programming computers. They are exceptionally good at performing pattern recognition and other tasks that are very difficult to program using conventional techniques. So programs that employ neural nets are also capable of learning on their own and adapting to changing conditions. It actually started in early 1940s. It became popular in 1980s due to the advances of computer technologies. Now, much of the inspiration of the field of neural networks comes from the desire to produce artificial systems capable of sophisticated, perhaps intelligent computations similar to those that the human brain routinely performs, and thereby possibly to enhance our understanding of the human brain.  Play video starting at :1:43 and follow transcript1:43 So, what is the difference between neural network techniques and the traditional computer techniques? Computers have to be explicitly programmed to analyze the problem to be solved. So you have to write the code in programming language, okay? And neural networks are named from examples. So there is no requirement for an explicit description of the problem, and no need for a programmer, okay? The neural computer adapts itself during the training period based on examples of similar problems, even without a desired solution to each problem. Okay, after sufficient training, the neural computer is able to relate the problem data to the solutions, and relate inputs to the output, and then be able to offer a viable solution to a brand new problem. And also neural networks are able to generalize or to handle incomplete data.  Play video starting at :3:8 and follow transcript3:08 So this is a comparison of neural network and a traditional computer, okay? And traditional digital computer is a deductive reasoning. So we apply known rules to input the data to produce output, and neural network is an inductive reasoning. So you're given an input and output data. Those are training examples. We construct the rules, okay? And computation is centralized, synchronized, and the series in digital computing. And the computation in the neural network is collective, asynchronous, and parallel, okay?  Play video starting at :3:57 and follow transcript3:57 And in the digital computers, the memory is the packeted, literally stored, and then located addressable. The neural network memory is distributed, internalized, and the short-term content addressable, okay? And digital computers have no fault tolerance. You have to design the specific fault tolerance system to help the digital computer become fault-tolerant. So one transistor goes bad, it no longer works, so you have to do the high availability. High availability is a very popular business in traditional digital computer systems. And the neural networks are designed to be fault-tolerant, redundant, and then all the cells are sharing the same responsibilities. So even some cells or some neurons broken, the system can still work, okay?  Play video starting at :5:13 and follow transcript5:13 And the digital computers will give the exact result, and oftentimes neural networks will have an inexact result. And digital computing has static connectivities, and neural networks may have dynamic connectivities, okay? So overall, the neural computer is ethical if well-defined rules with precise input, and the neural computer is ethical if rules are unknown, or complicated, or if data are noisy, or partial, okay? So there are many applications that neural computers can be used to solve the problems, like classification. In telemarketing, you can study the consumer spending patterns, so doing the pattern classification with the neural network. In defense, like a radar or solar image classification, okay? In agriculture and fishing, you can use the neural network to do the fruit or catch grading. In medicine, you can do the ultrasound or electrical audio program image classification, EEG, medical diagnosis. And you can use a neural network to do the recognition and identification. In general computing and telecommunication, you can do a speech recognition, vision, and handwriting recognition. In finance, you can do a signature verification and banknote verification. And you can also use the neural network on the assessment. In the engineering part, you can do the product inspection, monitoring, and control. And in the defense, you can do the targeting tracking. In security, you can do the motion detection, surveillance, image analysis, and the fingerprint matching.  Play video starting at :7:41 and follow transcript7:41 And neural networks can be used for forecasting and prediction. In finance, for example, you can do the foreign exchange rate forecasting or stock marketing forecasting, okay? And in agriculture, you can use the neural network to do the crop yield forecasting. In marketing, you can do the sales forecasting. And in meteorology, you can do the weather predictions. So all of those applications are a very popular application nowadays that you can use neural networks to solve. Now there are some limitations though, okay? In principle, neural networks can compute any computable functions, such as, for example, that they can do everything normal digital computing can do. But in practice, neural networks are especially useful for classification and function approximation problems. Neural networks are difficult to apply successfully to problems that concern the manipulation of symbols and memory. Although there are no methods for training neural networks that can magically create information that is not contained in the training data. So it is very important to provide sufficient training data for neural networks to learn.  In this module, we are going to study neural networks. So we will start with the basic concepts and then we will learn perceptual learning and artificial neural networks, and then we'll study the backpropagation algorithms. Okay, so what is neural network? Neural networks are a new method of programming computers. They are exceptionally good at performing pattern recognition and other tasks that are very difficult to program using conventional techniques. So programs that employ neural nets are also capable of learning on their own and adapting to changing conditions. It actually started in early 1940s. It became popular in 1980s due to the advances of computer technologies. Now, much of the inspiration of the field of neural networks comes from the desire to produce artificial systems capable of sophisticated, perhaps intelligent computations similar to those that the human brain routinely performs, and thereby possibly to enhance our understanding of the human brain.  Play video starting at :1:43 and follow transcript1:43 So, what is the difference between neural network techniques and the traditional computer techniques? Computers have to be explicitly programmed to analyze the problem to be solved. So you have to write the code in programming language, okay? And neural networks are named from examples. So there is no requirement for an explicit description of the problem, and no need for a programmer, okay? The neural computer adapts itself during the training period based on examples of similar problems, even without a desired solution to each problem. Okay, after sufficient training, the neural computer is able to relate the problem data to the solutions, and relate inputs to the outp: Added to Selection. Press [⌘ + S] to save as a note Like Dislike Report an issue : Added to Selection. Press [⌘ + S] to save as a note Like Dislike Report an issue : Added to Selection. Press [⌘ + S] to save as a note