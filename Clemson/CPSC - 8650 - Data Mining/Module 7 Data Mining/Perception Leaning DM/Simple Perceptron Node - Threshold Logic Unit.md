So this is a simple threshold logic unit, which does not contain a non-linearity activation function. So it uses a threshold data to determine if its output is 1 or 0. So it means 0 is prohibited, and then 1 is activated. So the output is equal to 1 if the weighted sum of the input is greater than or equal to the threshold, and otherwise will be 0. So this is the basic concept of perceptual learning for a perceptual as it is a simple threshold logic unit. So let's see how it gets trained, OK? So the length of weights. 

So the perceptual node, we need to learn the weights, like W1, W2, Wn. So initially, we basically don't know what weight we should use, OK? So our training process is to learn the Ws, OK? And so how do you learn? So our goal is to learn the different weights such that an objective function is maximized. So first, what is the objective function, OK? And what is our learning algorithm for such a threshold logic unit? 

Because we know that firing is based on the weights, but we don't know how we should apply the weights to each input unit. So the learning process is to learn those weights, actually. OK, so let's get to the basics here. So assume we have two learning tuples, OK? The first tuple is 0.8, 0.3 for the input x1 and x2. The target is 1, OK? So based on this threshold unit here and the theta, OK, so assume we have the initial weight is 0.4 and negative 0.2. 

So we have an initial weight, initial, OK? And then we also have an initial theta value of 0.1. So those are just the initial weights. We can put it randomly there, OK? So this is our initial weight on the perceptual net. That's a perceptual node. That's one single, one neuron, OK? 

Now taking the first training example here, 0.8, 0.3. So 0.8 times 0.4 plus negative 0.2 times 0.3. So we get a weighted summation. Then we will test against the 0.1, OK? So we applied 0.8, 0.3, and then we get the net value is 0.26. And then we compare against this theta, 0.1. So the output should be z, should be 1 because that, OK? 

So that's the first training instances, and the output is 1. And then when the output is 1, then there's no error because your training target is 1, OK? So our training law is we want to change the delta Wi equal to c is called the learning rate, and t is target minus z, the output, times xi. So we change the weight based on the xi's and the t minus t. So using the c is a training. So this is our perceptual, simple perceptual learning rule, OK? So you have a learning. 

The learning rate, and then the t minus z times xi is the objective, OK? So we want to change the delta Wi. Now since t was 1 and the output was 1, so this delta Wi is 0, OK? So our new Wi, W1, W2 is not changed. Now we get to the second unit here, OK? The second training sample, 0.4, 0.1 come in. So 0.5 times 0.4 plus 0.1 times negative 0.2 equals 0.14. 

Still greater than the threshold of 0.1, OK? Still greater than the 0.1. So our output is 1 again. Output is 1 again, OK? Now since our output is 1, then our target is 0. Then we have a t minus z is different. So it's 0 minus 1, OK? 

And so our perceptual learning rule learning is basically changing the delta Wi equal to c t minus z. So where W is the weight from input i to perceptual node, and c is the learning rate, and t is the target of the current instance, z is the current output, OK? xi is the current. So the perceptual rule learning, you use the following principles. First of all, only to change this weight if there is an error. So if t and z are different, OK? So delta Wi becomes non-zero only if you have a t and 0 difference, OK? 

And also you want to use a small c, rather than change the weights sufficiently to make current pattern correct. So you don't want to adjust it immediately, OK? If you do this, your network will alter it a lot. Basically, what you do is the change is, say, for example, a very small value. So every time you change the delta Wi a little bit, OK? And then your change will be scaled by the xi's, by the input, OK? So the different input, well, the value of input decides your delta Wi. 

That means your input actually affects your change. So this is the perceptual learning rule, OK? Delta Wi equal to c, t minus zi. OK, so perceptual learning rule, you create a perceptual node with an input, OK? And iteratively apply a pattern from the training set, OK? And then you apply the perceptual. OK, each iteration through the training set is an approach. 

So you call it the approach, OK? And then you continue to train until the total training set error ceases to improve. So every time you change the delta W, and then your error improve, like your error has value, so error value, and then you improve, re-improve, and until the time that there's no. So you may train the same training set for several approaches. So every approach is an iteration through the training set, OK? So there is a theorem called the perception convergence theorem. It's guaranteed to find a solution in finite time if the solution exists. 

So basically, you're through many, many, or maybe several, or maybe many, through a finite time of approaches, epochs, you will be able to find a solution. So you will be able to find a W i's, a set of W i's that make the error be as small as possible, OK? So basically, to maximize, the objective is to minimize the error and maximize the accuracy, OK? So that's the perceptual learning rule. 

So this is a simple threshold logic unit, which does not contain a non-linearity activation function. So it uses a threshold data to determine if its output is 1 or 0. So it means 0 is prohibited, and then 1 is activated. So the output is equal to 1 if the weighted sum of the input is greater than or equal to the threshold, and otherwise will be 0. So this is the basic concept of perceptual learning for a perceptual as it is a simple threshold logic unit. So let's see how it gets trained, OK? So the length of weights.  Play video starting at :1:9 and follow transcript1:09 So the perceptual node, we need to learn the weights, like W1, W2, Wn. So initially, we basically don't know what weight we should use, OK? So our training process is to learn the Ws, OK? And so how do you learn? So our goal is to learn the different weights such that an objective function is maximized. So first, what is the objective function, OK? And what is our learning algorithm for such a threshold logic unit?  Play video starting at :1:55 and follow transcript1:55 Because we know that firing is based on the weights, but we don't know how we should apply the weights to each input unit. So the learning process is to learn those weights, actually. OK, so let's get to the basics here. So assume we have two learning tuples, OK? The first tuple is 0.8, 0.3 for the input x1 and x2. The target is 1, OK? So based on this threshold unit here and the theta, OK, so assume we have the initial weight is 0.4 and negative 0.2.  Play video starting at :2:56 and follow transcript2:56 So we have an initial weight, initial, OK? And then we also have an initial theta value of 0.1. So those are just the initial weights. We can put it randomly there, OK? So this is our initial weight on the perceptual net. That's a perceptual node. That's one single, one neuron, OK?  Play video starting at :3:21 and follow transcript3:21 Now taking the first training example here, 0.8, 0.3. So 0.8 times 0.4 plus negative 0.2 times 0.3. So we get a weighted summation. Then we will test against the 0.1, OK? So we applied 0.8, 0.3, and then we get the net value is 0.26. And then we compare against this theta, 0.1. So the output should be z, should be 1 because that, OK?  Play video starting at :4:10 and follow transcript4:10 So that's the first training instances, and the output is 1. And then when the output is 1, then there's no error because your training target is 1, OK? So our training law is we want to change the delta Wi equal to c is called the learning rate, and t is target minus z, the output, times xi. So we change the weight based on the xi's and the t minus t. So using the c is a training. So this is our perceptual, simple perceptual learning rule, OK? So you have a learning.  Play video starting at :5:10 and follow transcript5:10 The learning rate, and then the t minus z times xi is the objective, OK? So we want to change the delta Wi. Now since t was 1 and the output was 1, so this delta Wi is 0, OK? So our new Wi, W1, W2 is not changed. Now we get to the second unit here, OK? The second training sample, 0.4, 0.1 come in. So 0.5 times 0.4 plus 0.1 times negative 0.2 equals 0.14.  Play video starting at :6:3 and follow transcript6:03 Still greater than the threshold of 0.1, OK? Still greater than the 0.1. So our output is 1 again. Output is 1 again, OK? Now since our output is 1, then our target is 0. Then we have a t minus z is different. So it's 0 minus 1, OK?  Play video starting at :6:36 and follow transcript6:36 And so our perceptual learning rule learning is basically changing the delta Wi equal to c t minus z. So where W is the weight from input i to perceptual node, and c is the learning rate, and t is the target of the current instance, z is the current output, OK? xi is the current. So the perceptual rule learning, you use the following principles. First of all, only to change this weight if there is an error. So if t and z are different, OK? So delta Wi becomes non-zero only if you have a t and 0 difference, OK?  Play video starting at :7:32 and follow transcript7:32 And also you want to use a small c, rather than change the weights sufficiently to make current pattern correct. So you don't want to adjust it immediately, OK? If you do this, your network will alter it a lot. Basically, what you do is the change is, say, for example, a very small value. So every time you change the delta Wi a little bit, OK? And then your change will be scaled by the xi's, by the input, OK? So the different input, well, the value of input decides your delta Wi.  Play video starting at :8:21 and follow transcript8:21 That means your input actually affects your change. So this is the perceptual learning rule, OK? Delta Wi equal to c, t minus zi. OK, so perceptual learning rule, you create a perceptual node with an input, OK? And iteratively apply a pattern from the training set, OK? And then you apply the perceptual. OK, each iteration through the training set is an approach.  Play video starting at :9:4 and follow transcript9:04 So you call it the approach, OK? And then you continue to train until the total training set error ceases to improve. So every time you change the delta W, and then your error improve, like your error has value, so error value, and then you improve, re-improve, and until the time that there's no. So you may train the same training set for several approaches. So every approach is an iteration through the training set, OK? So there is a theorem called the perception convergence theorem. It's guaranteed to find a solution in finite time if the solution exists.  Play video starting at :10:1 and follow transcript10:01 So basically, you're through many, many, or maybe several, or maybe many, through a finite time of approaches, epochs, you will be able to find a solution. So you will be able to find a W i's, a set of W i's that make the error be as small as possible, OK? So basically, to maximize, the objective is to minimize the error and maximize the accuracy, OK? So that's the perceptual learning rule.  So this is a simple threshold logic unit, which does not contain a non-linearity activation function. So it uses a threshold data to determine if its output is 1 or 0. So it means 0 is prohibited, and then 1 is activated. So the output is equal to 1 if the weighted sum of the input is greater than or equal to the threshold, and otherwise will be 0. So this is the basic concept of perceptual learning for a perceptual as it is a simple threshold logic unit. So let's see how it gets trained, OK? So the length of weights.  Play video starting at :1:9 and follow transcript1:09 So the perceptual node, we need to learn the weights, like W1, W2, Wn. So initially, we basically don't know what weight we should use, OK? So our training process is to learn the Ws, OK? And so how do you learn? So our goal is to learn the different weights such that an objective function is maximized. So first, what is the objective function, OK? And what is our learning algorithm for such a threshold logic unit?  Play video starting at :1:55 and follow transcript1:55 Because we know that firing is based on the weights, but we don't know how we should apply the weights to each input unit. So the learning process is to learn those weights, actually. OK, so let's get to the basics here. So assume we have two learning tuples, OK? The first tuple is 0.8, 0.3 for the input x1 and x2. The target is 1, OK? So based on this threshold unit here and the theta, OK, so assume we have the initial weight is 0.4 and negative 0.2.  Play video starting at :2:56 and follow transcript2:56 So we have an initial weight, initial, OK? And then we also have an initial theta value of 0.1. So those are just the initial weights. We can put it randomly there, OK? So this is our initial weight on the perceptual net. That's a perceptual node. That's one single, one neuron, OK?  Play video starting at :3:21 and follow transcript3:21 Now taking the first training example here, 0.8, 0.3. So 0.8 times 0.4 plus negative 0.2 times 0.3. So we get a weighted summation. Then we will test against the 0.1, OK? So we applied 0.8, 0.3, and then we get the net value is 0.26. And then we compare against this theta, 0.1. So the output should be z, should be 1 because that, OK?  Play video starting at :4:10 and follow transcript4:10 So that's the first training instances, and the output is 1. And then when the output is 1, then there's no error because your training target is 1, OK? So our training law is we want to change the delta Wi equal to c is called the learning rate, and t is target minus z, the output, times xi. So we change the weight based on the xi's and the t minus t. So using the c is a training. So this is our perceptual, simple perceptual learning rule, OK? So you have a learning.  Play video starting at :5:10 and follow transcript5:10 The learning rate, and then the t minus z times xi is the objective, OK? So we want to change the delta Wi. Now since t was 1 and the output was 1, so this delta Wi is 0, OK? So our new Wi, W1, W2 is not changed. Now we get to the second unit here, OK? The second training sample, 0.4, 0.1 come in. So 0.5 times 0.4 plus 0.1 times negative 0.2 equals 0.14.  Play video starting at :6:3 and follow transcript6:03 Still greater than the threshold of 0.1, OK? Still greater than the 0.1. So our output is 1 again. Output is 1 again, OK? Now since our output is 1, then our target is 0. Then we have a t minus z is different. So it's 0 minus 1, OK?  Play video starting at :6:36 and follow transcript6:36 And so our perceptual learning rule learning is basically changing the delta Wi equal to c t minus z. So where W is the weight from input i to perceptual node, and c is the learning rate, and t is the target of the current instance, z is the current output, OK? xi is the current. So the perceptual rule learning, you use the following principles. First of all, only to change this weight if there is an error. So if t and z are different, OK? So delta Wi becomes non-zero only if you have a t and 0 difference, OK?  Play video starting at :7:32 and follow transcript7:32 And also you want to use a small c, rather than change the weights sufficiently to make current pattern correct. So you don't want to adjust it immediately, OK? If you do this, your network will alter it a lot. Basically, what you do is the change is, say, for example, a very small value. So every time you change the delta Wi a little bit, OK? And then your change will be scaled by the xi's, by the input, OK? So the different input, well, the value of input decides your delta Wi.  Play video starting at :8:21 and follow transcript8:21 That means your input actually affects your change. So this is the perceptual learning rule, OK? Delta Wi equal to c, t minus zi. OK, so perceptual learning rule, you create a perceptual node with an input, OK? And iteratively apply a pattern from the training set, OK? And then you apply the perceptual. OK, each iteration through the training set is an approach.  Play video starting at :9:4 and follow transcript9:04 So you call it the approach, OK? And then you continue to train until the total training set error ceases to improve. So every time you change the delta W, and then your error improve, like your error has value, so error value, and then you improve, re-improve, and until the time that there's no. So you may train the same training set for several approaches. So every approach is an iteration through the training set, OK? So there is a theorem called the perception convergence theorem. It's guaranteed to find a solution in finite time if the solution exists.  Play video starting at :10:1 and follow transcript10:01 So basically, you're through many, many, or maybe several, or maybe many, through a finite time of approaches, epochs, you will be able to find a solution. So you will be able to find a W i's, a set of W i's that make the error be as small as possible, OK? So basically, to maximize, the objective is to minimize the error and maximize the accuracy, OK? So that's the perceptual learning rule.  So this is a simple threshold logic unit, which does not contain a non-linearity activation function. So it uses a threshold data to determine if its output is 1 or 0. So it means 0 is prohibited, and then 1 is activated. So the output is equal to 1 if the weighted sum of the input is greater than or equal to the threshold, and otherwise will be 0. So this is the basic concept of perceptual learning for a perceptual as it is a simple threshold logic unit. So let's see how it gets trained, OK? So the length of weights.  Play video starting at :1:9 and follow transcript1:09 So the perceptual node, we need to learn the weights, like W1, W2, Wn. So initially, we basically don't know what weight we should use, OK? So our training process is to learn the Ws, OK? And so how do you learn? So our goal is to learn the different weights such that an objective function is maximized. So first, what is the objective function, OK? And what is our learning algorithm for such a threshold logic unit?  Play video starting at :1:55 and follow transcript1:55 Because we know that firing is based on the weights, but we don't know how we should apply the weights to each input unit. So the learning process is to learn those weights, actually. OK, so let's get to the basics here. So assume we have two learning tuples, OK? The first tuple is 0.8, 0.3 for the input x1 and x2. Th: Added to Selection. Press [⌘ + S] to save as a note Like Dislike Report an issue : Added to Selection. Press [⌘ + S] to save as a note Like Dislike Report an issue : Added to Selection. Press [⌘ + S] to save as a note