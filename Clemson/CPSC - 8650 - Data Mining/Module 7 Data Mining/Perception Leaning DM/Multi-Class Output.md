Now, this perceptron is actually rule, and you can see it handles the binary classification problem. It's just out of the pattern, either one or zero, right? And there's an issue with any learning model which only supports binary classification like perceptron SVM. Perceptron is the threshold. If the threshold is above the threshold, the file is zero or one. File one is not file zero, right? So to handle the multiclass output, you can create a perceptron for each output class, okay? 

And the training set considers all other classes to be negative examples. And then you run all the perceptron on NOVA data set up to be the class of perceptron which outputs high. Then if there's a tie, choose the perceptron with the highest net value. So this is how you do it with the multiclass output, right? So if you have three classes, now binary is easy at zero, output is just use one perceptron. If you have three perceptrons, then you create three perceptrons, okay? And then each perceptron, each class is a perceptron, okay? 

And then that class is, for the training sample, if the training sample is one on that, the first class, then that class will output one on that first perceptron. All the others will output as a negative for that perceptron, okay? Then you run all the perceptrons on the new data, and then the output, if that perceptron output one, then you say this is the class one, otherwise, okay? And then you go, unless you see the tie, say, oh, you have two outputs, then you check the net values on that neuron. And if the net value is higher, you use that as the output. Of course, you can do it another way. You create a perceptron for each pair of output classes, treat them as binary on each pair. 

Then you have, well, the training set only contains examples from those two classes. So only take the examples which contain those two classes, and then you ignore all the others. So you have those pairs, right? Create for each pair of output classes, right? And then, so you run all the perceptrons on novel data, and then set output to be the class with the most wins from those perceptrons, okay? So whoever has the most wins, because they're against each other, so you have many, many pairs. So if you have three classes, you have only six pairs, you have, yeah, six pairs, okay? 

A combination of three over two is three pairs. Then you have three pairs, and then you will see which pair has the most wins, okay? Now the problem is that number model grows by the square of output classes. Unlike the previous one, you only have one perceptron for each output class. Now you have paired them together. So basically it's a combination, combinatorial of number of n three over two, so that's three classes. And then if you have a six one, a six two is a six times five divided by two times one. 

So that's 15 nodes, okay? So you have 15 models of binary, and then you use all of those perceptrons to name. So again, it's easy to handle multiclass output by using one perceptron for each class, or use a pair for each pair of output class for one perceptron. And in all cases, you use the majority votes to decide the output, okay? Now what is the objective function? Of course, the error on accuracy, all right? So that correctly number of over the total instances, classification of accuracy is the number of correct over the total instances, or classification error is the number of misclassified over the total of instance. 

It's basically one minus accuracy. You can see in our previous training sample, we achieved the 100% of accuracy, and there's zero error because all the training sample produced delta w i not change. The output is the same, exactly the same, right? Okay, so that is our objective functions. So we maximize accuracy, minimize the errors, okay? So how do you achieve the objectives, right? And oftentimes, we call the error another name called cost. 

So we want to minimize the loss function or cost function or cost function errors, okay? And so for the pattern errors, the target minus output, okay? So the target minus output, as we said, okay, error, because the error could be canceled each other, so for example, you have a positive error in the beginning and the negative in the first row, first training sample, and the second training sample with a negative error, then you may cancel each other. If you alternate those positive and negative, you end up with a zero error, actually, you always have the error in every training sample. That's not good. So what we can do is we can take the absolute error. So that's a sum of all the pattern errors and the absolute value. 

So we call it a normal one loss, L1 loss, okay? And then the common approach is using the squared error. Squared error basically should reduce that, you know, try to avoid that errors cancel each other, but take the square root. The reason we take a squared error and L2 loss is, we call it a sum of squared error, okay, is later on, we will learn that it is easier for us to do the back propagation in the neural network, okay? And then that's for the real value input, that's continuous values, right? For nominal data, pattern error is typical one for a mismatch or zero for a match. So a nominal data, actually, binary data is also a special case of nominal, right? 

It just has two states. Nominal data may have more than two states, okay? So for nominal output and a target, sum of squared error and the classification error are equivalent, basically, the number of classifiers, because it matches one, not matches zero. So basically, the sum of squared error equal to classification error, because matches one and one squared is one and you add them together, you know, the number of matches, okay? And so basically, it's equal to the classification error. 

Now, this perceptron is actually rule, and you can see it handles the binary classification problem. It's just out of the pattern, either one or zero, right? And there's an issue with any learning model which only supports binary classification like perceptron SVM. Perceptron is the threshold. If the threshold is above the threshold, the file is zero or one. File one is not file zero, right? So to handle the multiclass output, you can create a perceptron for each output class, okay?  Play video starting at ::50 and follow transcript0:50 And the training set considers all other classes to be negative examples. And then you run all the perceptron on NOVA data set up to be the class of perceptron which outputs high. Then if there's a tie, choose the perceptron with the highest net value. So this is how you do it with the multiclass output, right? So if you have three classes, now binary is easy at zero, output is just use one perceptron. If you have three perceptrons, then you create three perceptrons, okay? And then each perceptron, each class is a perceptron, okay?  Play video starting at :1:49 and follow transcript1:49 And then that class is, for the training sample, if the training sample is one on that, the first class, then that class will output one on that first perceptron. All the others will output as a negative for that perceptron, okay? Then you run all the perceptrons on the new data, and then the output, if that perceptron output one, then you say this is the class one, otherwise, okay? And then you go, unless you see the tie, say, oh, you have two outputs, then you check the net values on that neuron. And if the net value is higher, you use that as the output. Of course, you can do it another way. You create a perceptron for each pair of output classes, treat them as binary on each pair.  Play video starting at :2:55 and follow transcript2:55 Then you have, well, the training set only contains examples from those two classes. So only take the examples which contain those two classes, and then you ignore all the others. So you have those pairs, right? Create for each pair of output classes, right? And then, so you run all the perceptrons on novel data, and then set output to be the class with the most wins from those perceptrons, okay? So whoever has the most wins, because they're against each other, so you have many, many pairs. So if you have three classes, you have only six pairs, you have, yeah, six pairs, okay?  Play video starting at :3:56 and follow transcript3:56 A combination of three over two is three pairs. Then you have three pairs, and then you will see which pair has the most wins, okay? Now the problem is that number model grows by the square of output classes. Unlike the previous one, you only have one perceptron for each output class. Now you have paired them together. So basically it's a combination, combinatorial of number of n three over two, so that's three classes. And then if you have a six one, a six two is a six times five divided by two times one.  Play video starting at :4:44 and follow transcript4:44 So that's 15 nodes, okay? So you have 15 models of binary, and then you use all of those perceptrons to name. So again, it's easy to handle multiclass output by using one perceptron for each class, or use a pair for each pair of output class for one perceptron. And in all cases, you use the majority votes to decide the output, okay? Now what is the objective function? Of course, the error on accuracy, all right? So that correctly number of over the total instances, classification of accuracy is the number of correct over the total instances, or classification error is the number of misclassified over the total of instance.  Play video starting at :5:53 and follow transcript5:53 It's basically one minus accuracy. You can see in our previous training sample, we achieved the 100% of accuracy, and there's zero error because all the training sample produced delta w i not change. The output is the same, exactly the same, right? Okay, so that is our objective functions. So we maximize accuracy, minimize the errors, okay? So how do you achieve the objectives, right? And oftentimes, we call the error another name called cost.  Play video starting at :6:38 and follow transcript6:38 So we want to minimize the loss function or cost function or cost function errors, okay? And so for the pattern errors, the target minus output, okay? So the target minus output, as we said, okay, error, because the error could be canceled each other, so for example, you have a positive error in the beginning and the negative in the first row, first training sample, and the second training sample with a negative error, then you may cancel each other. If you alternate those positive and negative, you end up with a zero error, actually, you always have the error in every training sample. That's not good. So what we can do is we can take the absolute error. So that's a sum of all the pattern errors and the absolute value.  Play video starting at :7:46 and follow transcript7:46 So we call it a normal one loss, L1 loss, okay? And then the common approach is using the squared error. Squared error basically should reduce that, you know, try to avoid that errors cancel each other, but take the square root. The reason we take a squared error and L2 loss is, we call it a sum of squared error, okay, is later on, we will learn that it is easier for us to do the back propagation in the neural network, okay? And then that's for the real value input, that's continuous values, right? For nominal data, pattern error is typical one for a mismatch or zero for a match. So a nominal data, actually, binary data is also a special case of nominal, right?  Play video starting at :9:1 and follow transcript9:01 It just has two states. Nominal data may have more than two states, okay? So for nominal output and a target, sum of squared error and the classification error are equivalent, basically, the number of classifiers, because it matches one, not matches zero. So basically, the sum of squared error equal to classification error, because matches one and one squared is one and you add them together, you know, the number of matches, okay? And so basically, it's equal to the classification error.  Now, this perceptron is actually rule, and you can see it handles the binary classification problem. It's just out of the pattern, either one or zero, right? And there's an issue with any learning model which only supports binary classification like perceptron SVM. Perceptron is the threshold. If the threshold is above the threshold, the file is zero or one. File one is not file zero, right? So to handle the multiclass output, you can create a perceptron for each output class, okay?  Play video starting at ::50 and follow transcript0:50 And the training set considers all other classes to be negative examples. And then you run all the perceptron on NOVA data set up to be the class of perceptron which outputs high. Then if there's a tie, choose the perceptron with the highest net value. So this is how you do it with the multiclass output, right? So if you have three classes, now binary is easy at zero, output is just use one perceptron. If you have three perceptrons, then you create three perceptrons, okay? And then each perceptron, each class is a perceptron, okay?  Play video starting at :1:49 and follow transcript1:49 And then that class is, for the training sample, if the training sample is one on that, the first class, then that class will output one on that first perceptron. All the others will output as a negative for that perceptron, okay? Then you run all the perceptrons on the new data, and then the output, if that perceptron output one, then you say this is the class one, otherwise, okay? And then you go, unless you see the tie, say, oh, you have two outputs, then you check the net values on that neuron. And if the net value is higher, you use that as the output. Of course, you can do it another way. You create a perceptron for each pair of output classes, treat them as binary on each pair.  Play video starting at :2:55 and follow transcript2:55 Then you have, well, the training set only contains examples from those two classes. So only take the examples which contain those two classes, and then you ignore all the others. So you have those pairs, right? Create for each pair of output classes, right? And then, so you run all the perceptrons on novel data, and then set output to be the class with the most wins from those perceptrons, okay? So whoever has the most wins, because they're against each other, so you have many, many pairs. So if you have three classes, you have only six pairs, you have, yeah, six pairs, okay?  Play video starting at :3:56 and follow transcript3:56 A combination of three over two is three pairs. Then you have three pairs, and then you will see which pair has the most wins, okay? Now the problem is that number model grows by the square of output classes. Unlike the previous one, you only have one perceptron for each output class. Now you have paired them together. So basically it's a combination, combinatorial of number of n three over two, so that's three classes. And then if you have a six one, a six two is a six times five divided by two times one.  Play video starting at :4:44 and follow transcript4:44 So that's 15 nodes, okay? So you have 15 models of binary, and then you use all of those perceptrons to name. So again, it's easy to handle multiclass output by using one perceptron for each class, or use a pair for each pair of output class for one perceptron. And in all cases, you use the majority votes to decide the output, okay? Now what is the objective function? Of course, the error on accuracy, all right? So that correctly number of over the total instances, classification of accuracy is the number of correct over the total instances, or classification error is the number of misclassified over the total of instance.  Play video starting at :5:53 and follow transcript5:53 It's basically one minus accuracy. You can see in our previous training sample, we achieved the 100% of accuracy, and there's zero error because all the training sample produced delta w i not change. The output is the same, exactly the same, right? Okay, so that is our objective functions. So we maximize accuracy, minimize the errors, okay? So how do you achieve the objectives, right? And oftentimes, we call the error another name called cost.  Play video starting at :6:38 and follow transcript6:38 So we want to minimize the loss function or cost function or cost function errors, okay? And so for the pattern errors, the target minus output, okay? So the target minus output, as we said, okay, error, because the error could be canceled each other, so for example, you have a positive error in the beginning and the negative in the first row, first training sample, and the second training sample with a negative error, then you may cancel each other. If you alternate those positive and negative, you end up with a zero error, actually, you always have the error in every training sample. That's not good. So what we can do is we can take the absolute error. So that's a sum of all the pattern errors and the absolute value.  Play video starting at :7:46 and follow transcript7:46 So we call it a normal one loss, L1 loss, okay? And then the common approach is using the squared error. Squared error basically should reduce that, you know, try to avoid that errors cancel each other, but take the square root. The reason we take a squared error and L2 loss is, we call it a sum of squared error, okay, is later on, we will learn that it is easier for us to do the back propagation in the neural network, okay? And then that's for the real value input, that's continuous values, right? For nominal data, pattern error is typical one for a mismatch or zero for a match. So a nominal data, actually, binary data is also a special case of nominal, right?  Play video starting at :9:1 and follow transcript9:01 It just has two states. Nominal data may have more than two states, okay? So for nominal output and a target, sum of squared error and the classification error are equivalent, basically, the number of classifiers, because it matches one, not matches zero. So basically, the sum of squared error equal to classification error, because matches one and one squared is one and you add them together, you know, the number of matches, okay? And so basically, it's equal to the classification error.  Now, this perceptron is actually rule, and you can see it handles the binary classification problem. It's just out of the pattern, either one or zero, right? And there's an issue with any learning model which only supports binary classification like perceptron SVM. Perceptron is the threshold. If the threshold is above the threshold, the file is zero or one. File one is not file zero, right? So to handle the multiclass output, you can create a perceptron for each output class, okay?  Play video starting at ::50 and follow transcript0:50 And the training set considers all other classes to be negative examples. And then you run all the perceptron on NOVA data set up to be the class of perceptron which outputs high. Then if there's a tie, choose the perceptron with the highest net value. So this is how you do it with the multiclass output, right? So if you have three classes, now binary is easy at zero, output is just use one perceptron. If you have three perceptrons, then you create three perceptrons, okay? And then each perceptron, each class is a perceptron, okay?  Play video starting at :1:49 and follow transcript1:49 And then that class is, for the training sample, if the training sample is one on that, the first class, then that class will output one on that first perceptron. All the others will output as a negative for that perceptron, okay? Then you run all the perceptrons on the new data, and then the output, if that perceptron output one, then you say this is the class one, otherwise, okay? And then you go, unless you see the tie, say, oh, you hav: Added to Selection. Press [⌘ + S] to save as a note Like Dislike Report an issue : Added to Selection. Press [⌘ + S] to save as a note Like Dislike Report an issue : Added to Selection. Press [⌘ + S] to save as a note