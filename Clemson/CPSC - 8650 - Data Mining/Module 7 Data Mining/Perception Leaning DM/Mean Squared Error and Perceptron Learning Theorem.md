And also, you may train n times, because you have n numbers. So oftentimes, you take the mean squared of MSC. The MSC is the SSE over the n. Because it will normalize the error for the data set of different sizes, because oftentimes, you have a different training size. Mean squared error is the average square error per pattern. And as we say that the mean squared error is take the square, right? Square is the value is the square unit. 

So you often use the RMSE, called the root mean squared error. So it's the square root of the MSC. This puts the error value back into the same unit as the features, OK? So that is making it be more intuitive, OK? Because in the SSE, we square them up already, OK? RMSE is the average distance or average error of targets from output in the same scale as the features, OK? So RMSE is also most used, especially when you measure the accuracies, OK? 

Because it's basically scaled down to the same scale as the feature space, OK? Now, a perceptron can learn anything that can be represented in a linear separate space with the hyperplane. For example, anything linearly separable with a hyperplane can be. So this is called a threshold unit perceptron. We are not talking about the activation functions, OK? We talked about the original spectrum, OK? It can learn anything that can be represented, can represent. 

That means anything linearly separable with a hyperplane, like all functions, OK? Like you can draw a line, OK? For all functions, 0, 0, 0, 0, 1 is 1, 1, 0 is 1, 1, 1, OK? However, however, it is not separable if you have an XOR problem here, OK? Think about XOR problem. So for XOR problem, we need more than one perceptron, OK?