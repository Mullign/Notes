Our question is, as in your previous situation where theta was determined as a point 1, so should we also adjust the theta? Yes, in most cases, now unless you put the theta as a zero, then just the simple threshold as a zero, but usually you need to adjust the theta value to make that is the bias. Because adjusted theta is also necessary. So in most cases, and you put that theta as the input so that you can adjust the theta. So your model becomes something like that. So the initial input was 1, and it keeps as a constant as a 1. And then your Wn plus 1 is the negative theta. 

So you can see that your theta, now your threshold becomes zero. So it looks like if you put summation of Wi Xi plus W2 X2 plus Wn Xn plus Wn Xn greater than or equal to theta, you move theta to the left side of the equation, well, basically become a negative theta, right? So basically you have Wi Ni and then Wi summation of Wi Xi negative theta greater than or equal to zero. So this is, theta is an actual weight now. So we call it WNi. So we can train that theta as an actual weight. Let's use a pattern recognition. 

So for example, we want to train a pattern based on the perceptron output 0 or 1. So for example, it's 1, 1, 0, 1, 0, output 1. So it's all augmented version by adding a threshold theta value there as a 1 as an input because we want to train the threshold there, right? And then we have a 1, 0, 1, 1, the output 0, 1, 0, 0, 1, output 1, okay? So we treat the threshold like any other weight, okay? Therefore, there's no special case for the theta itself. And we call it a bias since it biases the output up and down with a constant value weight input 1, okay? 

So since we started with a random weight anyway, so you can ignore the negative theta notion, okay? Basically, it's like you can treat it as any positive or negative, okay? You just think of bias as an actual available weight, okay? Or of course, some other, you know, like the author used the negative 1 as an input. That's another one. Treat that as a positive. That's fine. 

Either way, because it is a random value anyways, to start with, you can treat it as a 1, negative theta or negative 1, positive theta, okay? All right. So you always use a bias weight to start with.