Okay, so let's get it to the example here, okay? So again, assume we have three input perception plus a bias, okay? So if output is one, if the net is greater than zero, else it is zero, okay? And then we also assume a landing rate of C of one and the initial weights of all zeros, okay? So initial WI plus that data is all zero. So you can see this is the initial, okay? The first training set is 001, 1111, 1110111, okay? 

So initial 001 is output zero. So we use the augmented patterns, okay? And so the initial is 001 and zero. The target is zero, okay? 001 and then the theta is one and output is zero. And then initial weight is zero, okay? So based on the first example, what is our output? 

Our output is zero times zero, zero times one, one times zero, one times zero, one times zero. So our net is zero. So our output should be zero, okay? Net is zero, output is zero. Then what is our delta WI? Delta W is C times T minus zero times XI. Basically it's zero, zero, zero, because T equal to zero, okay? 

So that's the first one. Then what do we have? Our weight. Our weight does not be changed, okay? Our weight is the same, because zero, zero, zero, zero. And then our pattern becomes one, one, one. That's the next training sample. 

And then the one with the augmented version. And then our target is one, because our target T, okay? Then we compute WWW, zero, zero, zero. And then because of zero, one times zero, one times zero, one times zero, they are all zeros. And then zero is not greater than zero. Net is greater than zero. So the output is still zero, okay? 

Then output is still zero. And then our net is zero, output is zero. And you can see where our W should be changed, because T is one, Z is zero, okay? And then all the X ones include the theta, the initials of one. And then times the C, so delta WI is equal to one, one, one, one. So delta WI is equal to one, one, one, one. We change the weight to one, one, one, one. 

So our next training sample is one, zero, one, output target is one. So one, zero, one, one, one, zero, zero, one, one, target is one. Now we calculate one, zero, one, one times the weight is equal to net value is three. Then our output is one, because the net value is three greater than zero, so output is one. Now look at the targeted value is one. So one, so our delta WI is CT minus zero XI is zero, zero, zeros. So our weight is still zero, zero, zero, or one, one, one, one. 

Then our next training sample zero, one, one, output is zero. Now next training sample is zero, one, one, one, one times one, one, one, one, one is the three, okay? And then output is one, okay? So in this case, we have issues because the T and zero are different. So T minus zero is T minus T is zero, one is output is one. So you have a C times XI, so X zero is zero, this one, one, so it's a negative one. So your weight, it becomes zero, negative one, negative one, negative one. 

So we change our Ws. So we add this delta W to this W become one, zero, zero, zero. Now we run out of our training sample. So we finish one epoch, okay, one epoch. And then we start again, our second epoch. Our second epoch started zero, zero, one, output zero. So you can say zero, zero, one, one, zero, output, okay, is zero. 

And then our weight is this, okay? And then we train and output become vector, the net is zero, output is zero. And then nothing changes, zero, zero, two. So you get a zero, zero, zero, okay? Now then we train the next one. We take the next one, the next examples, okay? One, one, one, and one with this, and output is one. 

Because the one times one, and then the net is one, output is one. So they are correct, so the delta W is zero, okay? And then we train another sample, one, one, one, and then one, one, one, and then the one, and the one, the output, the delta W is zero. And then we take the same thing with delta W. So in our second epoch, delta W is not changed. And at this stage, we can stop because if our objective is not improved anymore, we stop here, we always get the perfect result, accuracy is great. So we achieve our objective, the error becomes zero now, okay? 

So we take two epochs to finish the training. So our weight becomes one, zero, zero, zero. And that is our rule, okay? So for any unknown input, okay? Now for this, a known input, we know the result is like this. For any unknown input, for example, one, one, zero, okay? Given the input of one, one, zero, so then let's see what is the actual output. 

One, one, zero, based on this weight, one, zero, one, one, zero, and a one, so our output should be one times, then there's a net is one, output one. So this perceptron, this single neuron node with this weight will output a one, okay? That'll be easy, given the one. Now for the others, for example, zero, zero, one, and then you apply the rule, and then it becomes zero. So zero, zero, one, output is zero, and one, one, zero, output one. Of course, for zero, one, one, output zero, one, one, remember we already trained this output one. So this is what happens with the perceptron rule, okay? 

Okay, so let's get it to the example here, okay? So again, assume we have three input perception plus a bias, okay? So if output is one, if the net is greater than zero, else it is zero, okay? And then we also assume a landing rate of C of one and the initial weights of all zeros, okay? So initial WI plus that data is all zero. So you can see this is the initial, okay? The first training set is 001, 1111, 1110111, okay?  Play video starting at ::56 and follow transcript0:56 So initial 001 is output zero. So we use the augmented patterns, okay? And so the initial is 001 and zero. The target is zero, okay? 001 and then the theta is one and output is zero. And then initial weight is zero, okay? So based on the first example, what is our output?  Play video starting at :1:35 and follow transcript1:35 Our output is zero times zero, zero times one, one times zero, one times zero, one times zero. So our net is zero. So our output should be zero, okay? Net is zero, output is zero. Then what is our delta WI? Delta W is C times T minus zero times XI. Basically it's zero, zero, zero, because T equal to zero, okay?  Play video starting at :2:9 and follow transcript2:09 So that's the first one. Then what do we have? Our weight. Our weight does not be changed, okay? Our weight is the same, because zero, zero, zero, zero. And then our pattern becomes one, one, one. That's the next training sample.  Play video starting at :2:30 and follow transcript2:30 And then the one with the augmented version. And then our target is one, because our target T, okay? Then we compute WWW, zero, zero, zero. And then because of zero, one times zero, one times zero, one times zero, they are all zeros. And then zero is not greater than zero. Net is greater than zero. So the output is still zero, okay?  Play video starting at :3:9 and follow transcript3:09 Then output is still zero. And then our net is zero, output is zero. And you can see where our W should be changed, because T is one, Z is zero, okay? And then all the X ones include the theta, the initials of one. And then times the C, so delta WI is equal to one, one, one, one. So delta WI is equal to one, one, one, one. We change the weight to one, one, one, one.  Play video starting at :3:52 and follow transcript3:52 So our next training sample is one, zero, one, output target is one. So one, zero, one, one, one, zero, zero, one, one, target is one. Now we calculate one, zero, one, one times the weight is equal to net value is three. Then our output is one, because the net value is three greater than zero, so output is one. Now look at the targeted value is one. So one, so our delta WI is CT minus zero XI is zero, zero, zeros. So our weight is still zero, zero, zero, or one, one, one, one.  Play video starting at :4:42 and follow transcript4:42 Then our next training sample zero, one, one, output is zero. Now next training sample is zero, one, one, one, one times one, one, one, one, one is the three, okay? And then output is one, okay? So in this case, we have issues because the T and zero are different. So T minus zero is T minus T is zero, one is output is one. So you have a C times XI, so X zero is zero, this one, one, so it's a negative one. So your weight, it becomes zero, negative one, negative one, negative one.  Play video starting at :5:42 and follow transcript5:42 So we change our Ws. So we add this delta W to this W become one, zero, zero, zero. Now we run out of our training sample. So we finish one epoch, okay, one epoch. And then we start again, our second epoch. Our second epoch started zero, zero, one, output zero. So you can say zero, zero, one, one, zero, output, okay, is zero.  Play video starting at :6:18 and follow transcript6:18 And then our weight is this, okay? And then we train and output become vector, the net is zero, output is zero. And then nothing changes, zero, zero, two. So you get a zero, zero, zero, okay? Now then we train the next one. We take the next one, the next examples, okay? One, one, one, and one with this, and output is one.  Play video starting at :6:57 and follow transcript6:57 Because the one times one, and then the net is one, output is one. So they are correct, so the delta W is zero, okay? And then we train another sample, one, one, one, and then one, one, one, and then the one, and the one, the output, the delta W is zero. And then we take the same thing with delta W. So in our second epoch, delta W is not changed. And at this stage, we can stop because if our objective is not improved anymore, we stop here, we always get the perfect result, accuracy is great. So we achieve our objective, the error becomes zero now, okay?  Play video starting at :7:58 and follow transcript7:58 So we take two epochs to finish the training. So our weight becomes one, zero, zero, zero. And that is our rule, okay? So for any unknown input, okay? Now for this, a known input, we know the result is like this. For any unknown input, for example, one, one, zero, okay? Given the input of one, one, zero, so then let's see what is the actual output.  Play video starting at :8:33 and follow transcript8:33 One, one, zero, based on this weight, one, zero, one, one, zero, and a one, so our output should be one times, then there's a net is one, output one. So this perceptron, this single neuron node with this weight will output a one, okay? That'll be easy, given the one. Now for the others, for example, zero, zero, one, and then you apply the rule, and then it becomes zero. So zero, zero, one, output is zero, and one, one, zero, output one. Of course, for zero, one, one, output zero, one, one, remember we already trained this output one. So this is what happens with the perceptron rule, okay?  Okay, so let's get it to the example here, okay? So again, assume we have three input perception plus a bias, okay? So if output is one, if the net is greater than zero, else it is zero, okay? And then we also assume a landing rate of C of one and the initial weights of all zeros, okay? So initial WI plus that data is all zero. So you can see this is the initial, okay? The first training set is 001, 1111, 1110111, okay?  Play video starting at ::56 and follow transcript0:56 So initial 001 is output zero. So we use the augmented patterns, okay? And so the initial is 001 and zero. The target is zero, okay? 001 and then the theta is one and output is zero. And then initial weight is zero, okay? So based on the first example, what is our output?  Play video starting at :1:35 and follow transcript1:35 Our output is zero times zero, zero times one, one times zero, one times zero, one times zero. So our net is zero. So our output should be zero, okay? Net is zero, output is zero. Then what is our delta WI? Delta W is C times T minus zero times XI. Basically it's zero, zero, zero, because T equal to zero, okay?  Play video starting at :2:9 and follow transcript2:09 So that's the first one. Then what do we have? Our weight. Our weight does not be changed, okay? Our weight is the same, because zero, zero, zero, zero. And then our pattern becomes one, one, one. That's the next training sample.  Play video starting at :2:30 and follow transcript2:30 And then the one with the augmented version. And then our target is one, because our target T, okay? Then we compute WWW, zero, zero, zero. And then because of zero, one times zero, one times zero, one times zero, they are all zeros. And then zero is not greater than zero. Net is greater than zero. So the output is still zero, okay?  Play video starting at :3:9 and follow transcript3:09 Then output is still zero. And then our net is zero, output is zero. And you can see where our W should be changed, because T is one, Z is zero, okay? And then all the X ones include the theta, the initials of one. And then times the C, so delta WI is equal to one, one, one, one. So delta WI is equal to one, one, one, one. We change the weight to one, one, one, one.  Play video starting at :3:52 and follow transcript3:52 So our next training sample is one, zero, one, output target is one. So one, zero, one, one, one, zero, zero, one, one, target is one. Now we calculate one, zero, one, one times the weight is equal to net value is three. Then our output is one, because the net value is three greater than zero, so output is one. Now look at the targeted value is one. So one, so our delta WI is CT minus zero XI is zero, zero, zeros. So our weight is still zero, zero, zero, or one, one, one, one.  Play video starting at :4:42 and follow transcript4:42 Then our next training sample zero, one, one, output is zero. Now next training sample is zero, one, one, one, one times one, one, one, one, one is the three, okay? And then output is one, okay? So in this case, we have issues because the T and zero are different. So T minus zero is T minus T is zero, one is output is one. So you have a C times XI, so X zero is zero, this one, one, so it's a negative one. So your weight, it becomes zero, negative one, negative one, negative one.  Play video starting at :5:42 and follow transcript5:42 So we change our Ws. So we add this delta W to this W become one, zero, zero, zero. Now we run out of our training sample. So we finish one epoch, okay, one epoch. And then we start again, our second epoch. Our second epoch started zero, zero, one, output zero. So you can say zero, zero, one, one, zero, output, okay, is zero.  Play video starting at :6:18 and follow transcript6:18 And then our weight is this, okay? And then we train and output become vector, the net is zero, output is zero. And then nothing changes, zero, zero, two. So you get a zero, zero, zero, okay? Now then we train the next one. We take the next one, the next examples, okay? One, one, one, and one with this, and output is one.  Play video starting at :6:57 and follow transcript6:57 Because the one times one, and then the net is one, output is one. So they are correct, so the delta W is zero, okay? And then we train another sample, one, one, one, and then one, one, one, and then the one, and the one, the output, the delta W is zero. And then we take the same thing with delta W. So in our second epoch, delta W is not changed. And at this stage, we can stop because if our objective is not improved anymore, we stop here, we always get the perfect result, accuracy is great. So we achieve our objective, the error becomes zero now, okay?  Play video starting at :7:58 and follow transcript7:58 So we take two epochs to finish the training. So our weight becomes one, zero, zero, zero. And that is our rule, okay? So for any unknown input, okay? Now for this, a known input, we know the result is like this. For any unknown input, for example, one, one, zero, okay? Given the input of one, one, zero, so then let's see what is the actual output.  Play video starting at :8:33 and follow transcript8:33 One, one, zero, based on this weight, one, zero, one, one, zero, and a one, so our output should be one times, then there's a net is one, output one. So this perceptron, this single neuron node with this weight will output a one, okay? That'll be easy, given the one. Now for the others, for example, zero, zero, one, and then you apply the rule, and then it becomes zero. So zero, zero, one, output is zero, and one, one, zero, output one. Of course, for zero, one, one, output zero, one, one, remember we already trained this output one. So this is what happens with the perceptron rule, okay?  Okay, so let's get it to the example here, okay? So again, assume we have three input perception plus a bias, okay? So if output is one, if the net is greater than zero, else it is zero, okay? And then we also assume a landing rate of C of one and the initial weights of all zeros, okay? So initial WI plus that data is all zero. So you can see this is the initial, okay? The first training set is 001, 1111, 1110111, okay?  Play video starting at ::56 and follow transcript0:56 So initial 001 is output zero. So we use the augmented patterns, okay? And so the initial is 001 and zero. The target is zero, okay? 001 and then the theta is one and output is zero. And then initial weight is zero, okay? So based on the first example, what is our output?  Play video starting at :1:35 and follow transcript1:35 Our output is zero times zero, zero times one, one times zero, one times zero, one times zero. So our net is zero. So our output should be zero, okay? Net is zero, output is zero. Then what is our delta WI? Delta W is C times T minus zero times XI. Basically it's zero, zero, zero, because T equal to zero, okay?  Play video starting at :2:9 and follow transcript2:09 So that's the first one. Then what do we have? Our weight. Our weight does not be changed, okay? Our weight is the same, because zero, zero, zero, zero. And then our pattern becomes one, one, one. That's the next training sample.  Play video starting at :2:30 and follow transcript2:30 And then the one with the augmented version. And then our target is one, because our target T, okay? Then we compute WWW, zero, zero, zero. And then because: Added to Selection. Press [⌘ + S] to save as a note Like Dislike Report an issue : Added to Selection. Press [⌘ + S] to save as a note Like Dislike Report an issue : Added to Selection. Press [⌘ + S] to save as a note