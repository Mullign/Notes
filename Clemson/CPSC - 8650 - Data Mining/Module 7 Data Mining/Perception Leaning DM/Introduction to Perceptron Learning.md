After we learn the basic concept for artificial neural networks, we will learn how to train a perceptron to learn, okay? That train a neuron to learn, train a neuron to file. So to train a neuron to file, we have training mode, okay? So at training mode, the neuron can be trained to file or not for a particular input patterns. In the user mode, when a taught input is detected at the input, its associated output becomes the current output. However, if the input pattern does not belong in the taught list of input patterns, then we should use a file rule. The filing rule will be used to determine whether to file or not. 

So the filing rule is an important concept in neural networks. It accounts for their high flexibility. A filing rule determines how one calculates whether a neuron should file for any input pattern. It relates to all the input patterns, not only the ones that have been trained, okay? Or the one the node was trained on previously. You should have the rule to file on any input patterns, okay? So perceptron learning, perceptron is a predecessor of a neural network. 

So it starts with a single neuron, okay? So a perceptron is a sum of weighted sum of input, okay? So basically, that is a weighted sum of input plus a bias. So that bias is a measure of how easy it is to get the perceptron to output a 1, okay? And then, sometimes you can apply an activation function to add a nonlinearity to the model. So essentially, it is a calculation of a weighted sum of all the inputs, okay? Now, the original perceptron model does not have a nonlinear activity function, okay? 

It was invented back in the 1950s.