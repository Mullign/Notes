So look at this backpropagation, and as we know that our net value, okay, on the jth level, assume that i and j are connected, okay, so the previous level is i, the next level is j, okay, so equals wj, okay, net j is the wj, the net, the width of j0, j0 is the bias, okay, and then, then, equal time, summation of wji, okay, wji, level i and level j, and then each w, okay, and then times oi, oi is like an input, output of oi is an input of, of, of perceptual nodes j, okay, or the layer j, layer i's output is layer j's input, so net j is just the summation, okay, and then function output of j is a function of j and net j, this is the function is the activation function, right, okay, so our delta wji equal to is, do you remember that on the changes, right, the delta error, delta error over delta wji, okay, the error, and then in this case, delta error over delta ji, that was we wanted to calculate, as we said, right, in the previous case, we, we left this question there, where we want to calculate this, this j theta is the error term, okay, so we want to calculate this error here, and then we stand, anyway, and then, okay, so partial derivative of wji, well, why partial derivative, we don't care about all the other wj, so wji, okay, and then we add the net, okay, partial derivative net delta e, delta net j, delta net j, and delta wiji, now, now, look at this delta e over delta net j, okay, and the error over the net value is actually, okay, is delta j, and then the derivative of net over the partial derivative of the net over the wji is o i, that's what you calculate, because your net is here, right, and then, now, now, look at this, this term is delta j, that's the error over the net value, how the error over the net value, okay, and then we further going on with relate to this delta j with the activation, okay, that, that, because we know that w, delta wj equal to alpha, this linear rate, and then delta j, and times o i, o i is this partial derivative value, okay, and well, delta j equals negative delta e, and add a partial derivative o j, and partial o j over net, so this is another chain rule, so we connected to that output j, now, delta wiji is related to the output of the previous net, and then with the output of this current layer j, and over the net, is the derivative of this function, right, o j equal to f j, and also, we want to calculate that the partial derivative of the error over the partial, over the o j itself, o j is the output of the j, to the j, okay, okay, so here, we know this formula, so, okay, so what do we need to know, we have, we need to calculate o j, since we, okay, so now, we get to o i, and then we need to o j, and we need to know the errors, okay, now, let's go forward, so this delta j is the change of delta wij, okay, so that's, we call the credit assignment, okay, now, if j's node is an output unit, and then, we have an error function called error equal one over half t j over o j, okay, so basically, target minus z, that's what a square, we use the squared error, okay, then, what do we get, we take delta e, or a partial derivative of e over o j, equal to negative t j minus o j, now, our delta j, we put this, this, we use this value, negative t j minus o j, replace this partial derivative, okay, then, our delta j here, delta j here, equals t i, t j minus o j, times f net j, okay, so what is the f net j, and f net j is, we already calculated, right, okay, so our delta j, of course, we have partial derivative on each one, delta j's, and then, you should sum of all the effect, if, okay, so delta e over delta o j, okay, partial derivative over o j, is summation of every one of net k, and everyone from one to k, you know, you have many, as many parameter you have, okay, so it's actually each one of them, the weight, you just add them together, okay, so delta j is f j on the net j, times this delta k w j, because you add them all, okay, so the w i j, and it is become related to the delta j on the current term, and also the output of the current term, and then, give a momentary weight, basically, it's just the rho is a variable, okay, and times delta j i t minus one, then, we get delta j i t, and equals delta j i t minus one, plus, no, w j i t equals w j i t, previous w, and plus delta w j i, so we calculated, we get it, we solve the problem, because we can calculate delta j here, and we calculate, we know that, we know the o i here, okay, and then, so we just change this delta w i j output, because once we calculate this, we update the weight, so this is how back propagation, okay, given those back errors, and then we back forward, and then, we can go one every step, okay, going backwards, based on this chain error, okay, chain rule.