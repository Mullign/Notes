So let's explain the gradient descendant, okay? So assume that we have a loss function j, okay? So this is the error, okay? Which is a function of a model parameter w, okay? It's a model parameter theta, okay? Theta can be a w-weight or any bias, okay? It's a function of theta, okay? 

So and then there's many thetas, okay? Say theta j, theta i, theta 1, okay? So we want to change the theta value, okay? So the change of theta is a partial derivative of the error over the theta j, okay? So the gradient that we measure, so our objective is given the global cost function j theta, we want to minimize this j function, okay? And then with the j, we need to do the theta, where j theta, partial derivative of j theta over the theta j, okay? And the j's value is basically is the change of the errors. 

So our theta j, the weight itself is adjusted based on a learning rate times this, the slope, this the slope, the gradient. The gradient time at all is the learning rate, control the step size of the optimization. So you change it and then adjust the limit. So you take that. So what do we end up need to compute this partial derivative of j theta over theta j, okay? So that's the main idea. So if we can learn the gradient, okay? 

And then with the gradient descendants, we come to the optimum. Well, how do we calculate that partial derivative of j theta over the weight? So that's the main problem, how, okay? Okay, so as we getting into the network, okay? And then we have this error function j theta, okay? And so the output we know, the input we get now, okay? For the multi-layer neural network. 

And so we have a predicted output, we have a ground truth yi, okay? So we have a loss function e equal to j yj yi y hat i over yi. So this is the loss function or error function, okay? So this is a fade forward, okay? So we get an input, we get an output, okay? And then to compute the gradient of weight of each parameter, we basically, we want to compute the gradient of the partial derivative of j theta over j theta j on that parameter, particular parameter. So the partial derivative of that particular parameter. 

And then if we can compute that, if we can compute that, of course, we don't know how to compute it yet, okay? And then we can update this, update the parameter of theta j equal to theta j minus rho times that gradient, okay? Now rho is the learning rate, okay? A small learning rate, we control how big that stuff changes, okay? So this is the main idea of learning the neural network parameters, okay? Well, the key question is how to learn the gradient, right? So how do you learn the gradient? 

We can use back propagation, okay? Now looking at these layers, so back propagation, basically you started with the output layer and going backwards, okay? So and then every layer, you get the length from the layer and then the delta of this previous layer, okay? So for example, gradient of wij in the l's layer, basically corresponding to the unit j in the layer l, connected to unit i in layer l minus 1, okay? That's a delta jl, okay? We call it the delta jl. So and it's a function of all error term in the layer l plus 1, all error term, okay? 

The error l1, so from delta 1, delta 2, delta 3, okay? So and so of course, the output from unit i in layer l1 is oi. So basically is delta j is a function of all the delta 1 to n, whether in the layer, in the lower layer and also the object i, okay? Because they are related, because they through the unit j and you get the function, okay? And unit j is the summation of everything and then getting out and then you get the error. So the error term and then the input term, they reflect on the weight. So that's the main idea with the back propagation. 

So if you know the error on the l plus 1 layer and then you know the input on the l minus 1 layer, then you can compute w in the layer l, okay? So that's what we can compute. That's called the back propagation. So what is the error? The error term in delta j l actually is a function of all l delta k l plus 1 in layer l1, if layer l is a hidden layer, okay? Now, if the l layer l is an open layer and basically we get the overall loss function value. So in that case, the overall loss function value in the final error, we can start it now. 

So now we know that already. Then from that point going backwards and then we step by step, hopefully we can compute each one of delta j l, okay? So if we already know how to compute the error at the output, then we just distribute the backwards through the network layers. This is the main idea of back propagation, okay? Layer by layer. Started with the output layer going backwards because there is a relation. Okay, let's look at how they can be related, okay?