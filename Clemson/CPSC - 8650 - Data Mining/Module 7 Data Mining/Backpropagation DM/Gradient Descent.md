As we said, we use the gradient descent, right? So a commonly used optimization function that adjusts the weight according to the area they cause. So gradient is another words of a slope, as we said. And it is a typical form of an x-y graph. So it's a coordinate graph, two-dimensional coordinate, right? Represent those two variables, how they are related to each other. So in the neural network, the slope we care about describes the relationship between the network's error and a single weight. 

So how does the error vary as the weight is adjusted? Now basically, we want to answer the question, which weight will produce the least error? So we want to descend the gradient, right? And which one correctly represents the signals contained in the input data, and then trust them to a correct classification. If we can't answer which weight will produce the least error, then that weight should correctly represent the signal contained in the input data, and trust them to the correct classification. So it is like, which one can see the eyes and the nose in the input image, I know that should be labeled as face and not a dish. So those are the questions we can answer based on changing the weights, okay? 

Now the problem is, how do we compute, okay? Well, as a neural network, it slowly adjusts the many weights so that it can be mapped signal-to-meaning correctly. So the relationship between the network's error and each of those weights is a derivative, dE over dW, that measures the degree of which a slight change in the weight causes a slight change in the error, okay? So in the network, there are many weights. Each weight is just one factor in the deep network that involves many transforms, right? So the signal of weight pass through activation, and then sum over several layers. So we have to find a way to address this multiple layer of activations. 

And here, we can use the chain rule of calculus to match backwards through the network activation and the output, and finally arrive at the weight in question, okay? And of course, that backwards matching process eventually going back to the final overall error. So that's how we did, and like in our previous explanation, that backpropagation, right? We started with the overall errors and then going backwards. Well, so how do we... And then, as we said, if you go back to here, that OI and the delta J, L, is all related to the data, all the data values on the L plus one with the backpropagation. So let's get to the chain rules, okay? 

And the chain rules in the calculus is dz over dx equal to dz over dy times dy over dx, okay? You add a term dy here from the chain rule. So in the feed forward network, the relationship between the net error and the single weight, we can add the deactivation as the intermediate term. So d error over d weight equal to d error over deactivation times deactivation over d weight. So as we know, we want to form the error to learn the weight, okay? So we use the third variable called activation, through which weight is passed on, okay? Then we can calculate how change in weight affects the change in error by actually first calculating how change in activation affects the change in error. 

And also understand how a change in weight affects the change in the activation, okay? So we added the activation. Now why we add the activation? Because our output goes through the activation function here. So we add that function, okay?