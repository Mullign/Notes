Okay, so we have learned that multi-layer neural networks can solve any kind of problem. Unlike the single-layer perceptual network, you can only solve the linear separable problem. You need hidden layers to increase the non-linearity, therefore you can solve the non-linearly separable problem. The problem is that how do you know the weight? How do you learn the weight on the hidden layers? So here comes the backpropagation algorithm. So the objective here is to efficiently learn the weights. 

Because networks without hidden units are very limited in the input-output mapping they can model. More layers of linear units do not help, it is still linear, right? So the fixed-output non-linearities are not enough. We need multiple layers of adaptive non-linear hidden units. This gives us a universal approximation. But how can we train such networks? Because we need an efficient way to adapt all the weights, not just the last layer. 

As we know, in our perceptual network, when you have an input, directly map the output, given the output and then the target, you can use a linear rate, change the delta w with the c, delta w equal to c, and then t minus z, and then times xi. You relate this error with this input and then to change the w, right? But how about the hidden unit? Because there is no way you know the output of this hidden unit and how they change it. So it is hard to learn the weight going into the hidden unit, okay? And nobody tells directly what the hidden unit should do. So learning all the weights is hard. 

Of course, the naive approach to learn the hard weight is by perturbing the weights, okay? So what do you do? You randomly perturb one weight and see if it improves the network performance. If so, you save the change and then you perturb another one. But this is very inefficient because we need to do it many, many times, okay? And one representative set of training data just changes one weight. And then the problem is that when you change one weight, and then you change another weight, it may affect this one you already changed. 

And then towards the end of learning, large weight perturbations will nearly always make things worse. Because you change this and then fix the others, and then you perturb this, the other fixed, and then you make performance worse. Well, then somebody said, oh yeah, because you cannot change just change one, let's change everything together. So you randomly perturb all the weights in parallel. Well, then the problem is like, you don't know what is good. So you correlate the performance gain with the weight changes, but it is not any better because you need a lot of trial and error, and then these perturbations can become exponentially large trials, okay? And then there is no way you know what weight change causes the noise, and what weight change causes the problems. 

Now, so far, as a perceptual network, learning the hidden to output weight is easy, because since you have an output, and you're given the learning rate plus t minus z times related to the hidden output there, then you can name that part of weight, back weight, okay? And however, there is no way you know the features on the output, the hidden weight, and then going back to the input, the hidden weight is hard now, because there is no output measured or output observed for the hidden layers. Nobody knows what the hidden layer has done so far. So we need a back propagation algorithm. So the idea behind the back propagation is, we don't know what the hidden unit ought to do, but we can compute how fast the error changes once we change the hidden activity. So instead of using desired activities to train the hidden unit, we use the error derivatives with regard to the hidden activities. So each hidden activity can affect the many output units, right? 

And then it can therefore have many separate effects on the error. So we will combine those efforts together to see how they affect the final errors, okay? So instead of calculating exactly how they affect, we can compute the error derivative for all the hidden units, because they can be computed efficiently if we apply a back propagation. So once we have an error derivative for the hidden activities, then we can easily get the error derivative for the weight going into the hidden unit. So, what is that? How do we do this? Well, we have the gradient descender. 

Okay, so we have learned that multi-layer neural networks can solve any kind of problem. Unlike the single-layer perceptual network, you can only solve the linear separable problem. You need hidden layers to increase the non-linearity, therefore you can solve the non-linearly separable problem. The problem is that how do you know the weight? How do you learn the weight on the hidden layers? So here comes the backpropagation algorithm. So the objective here is to efficiently learn the weights.  Play video starting at :1:6 and follow transcript1:06 Because networks without hidden units are very limited in the input-output mapping they can model. More layers of linear units do not help, it is still linear, right? So the fixed-output non-linearities are not enough. We need multiple layers of adaptive non-linear hidden units. This gives us a universal approximation. But how can we train such networks? Because we need an efficient way to adapt all the weights, not just the last layer.  Play video starting at :1:47 and follow transcript1:47 As we know, in our perceptual network, when you have an input, directly map the output, given the output and then the target, you can use a linear rate, change the delta w with the c, delta w equal to c, and then t minus z, and then times xi. You relate this error with this input and then to change the w, right? But how about the hidden unit? Because there is no way you know the output of this hidden unit and how they change it. So it is hard to learn the weight going into the hidden unit, okay? And nobody tells directly what the hidden unit should do. So learning all the weights is hard.  Play video starting at :2:56 and follow transcript2:56 Of course, the naive approach to learn the hard weight is by perturbing the weights, okay? So what do you do? You randomly perturb one weight and see if it improves the network performance. If so, you save the change and then you perturb another one. But this is very inefficient because we need to do it many, many times, okay? And one representative set of training data just changes one weight. And then the problem is that when you change one weight, and then you change another weight, it may affect this one you already changed.  Play video starting at :3:46 and follow transcript3:46 And then towards the end of learning, large weight perturbations will nearly always make things worse. Because you change this and then fix the others, and then you perturb this, the other fixed, and then you make performance worse. Well, then somebody said, oh yeah, because you cannot change just change one, let's change everything together. So you randomly perturb all the weights in parallel. Well, then the problem is like, you don't know what is good. So you correlate the performance gain with the weight changes, but it is not any better because you need a lot of trial and error, and then these perturbations can become exponentially large trials, okay? And then there is no way you know what weight change causes the noise, and what weight change causes the problems.  Play video starting at :4:57 and follow transcript4:57 Now, so far, as a perceptual network, learning the hidden to output weight is easy, because since you have an output, and you're given the learning rate plus t minus z times related to the hidden output there, then you can name that part of weight, back weight, okay? And however, there is no way you know the features on the output, the hidden weight, and then going back to the input, the hidden weight is hard now, because there is no output measured or output observed for the hidden layers. Nobody knows what the hidden layer has done so far. So we need a back propagation algorithm. So the idea behind the back propagation is, we don't know what the hidden unit ought to do, but we can compute how fast the error changes once we change the hidden activity. So instead of using desired activities to train the hidden unit, we use the error derivatives with regard to the hidden activities. So each hidden activity can affect the many output units, right?  Play video starting at :6:35 and follow transcript6:35 And then it can therefore have many separate effects on the error. So we will combine those efforts together to see how they affect the final errors, okay? So instead of calculating exactly how they affect, we can compute the error derivative for all the hidden units, because they can be computed efficiently if we apply a back propagation. So once we have an error derivative for the hidden activities, then we can easily get the error derivative for the weight going into the hidden unit. So, what is that? How do we do this? Well, we have the gradient descender.  Data Mining Module 1 Module 1: Introduction to Data Mining Module 2 Module 2: Data and Measurements Module 3 Module 3: Data Preprocessing Module 4 Module 4: Pattern Mining Concepts & Typical Methods Module 5 Module 5: Classification & Selected Methods Module 6 Module 6: Cluster Analysis & Selected Methods Module 7 Module 7: Neural Networks Neural Networks Connecting Course Learning Objectives Reading. Duration: 5 minutes5 min Neural Network: Basic Concepts Video. Duration: 9 minutes9 min NN Pattern Recognition Video. Duration: 10 minutes10 min Artificial NN and Biological Inspiration Video. Duration: 9 minutes9 min Practice Quiz #1 Practice AssignmentGrade: 100% Perceptron Learning Introduction to Perceptron Learning Video. Duration: 3 minutes3 min Simple Perceptron Node - Threshold Logic Unit Video. Duration: 10 minutes10 min Do We Need to Adjust? Video. Duration: 4 minutes4 min Perceptron Rule Example Video. Duration: 9 minutes9 min Multi-Class Output Video. Duration: 9 minutes9 min Mean Squared Error and Perceptron Learning Theorem Video. Duration: 3 minutes3 min Practice Quiz #2 Practice AssignmentGrade: 100% Artificial Neural Networks Introduction to Artificial Neural Networks Video. Duration: 4 minutes4 min Simple Quadric Example and MLP Video. Duration: 6 minutes6 min Key Elements of NN Video. Duration: 9 minutes9 min What's Needed to Use NN Video. Duration: 4 minutes4 min Practice Quiz #3 Practice AssignmentGrade: 100% Backpropagation Introduction to Backpropagation Video. Duration: 7 minutes7 min Gradient Descent and Learning NN Parameters Video. Duration: 10 minutes10 min Gradient Descent Video. Duration: 6 minutes6 min Backpropagation Algorithm Video. Duration: 10 minutes10 min Design Considerations Video. Duration: 5 minutes5 min Practice Quiz #4 Practice Assignment. Duration: 5 minutes5 min Module 7 Summary & Homework Module 7 Summary Video. Duration: 1 minute1 min Practice Quiz #5 Practice Assignment. Duration: 5 minutes5 min Module 7 Homework Graded Assignment. Duration: 2 hours2h Module 7 Homework Solutions Reading. Duration: 10 minutes10 min Module 8 Module 8: Deep Learning Module 9 Module 9: Outlier Detection Module 10 Module 10: Data Mining Trends & Research Frontiers 0:27 / 7:43 Introduction to Backpropagation Save note Transcript Notes Downloads Language: English (auto)​Interactive Transcript - Enable basic transcript mode by pressing the escape key You may navigate through the transcript using tab. To save a note for a section of text press ⌘ + S. To expand your selection you may use ⌘ + arrow key. You may contract your selection using shift + ⌘ + arrow key. For screen readers that are incompatible with using arrow keys for shortcuts, you can replace them with the H J K L keys. Some screen readers may require using ⌘ in conjunction with the alt key Play video starting at :: and follow transcript0:00 : Added to Selection. Press [⌘ + S] to save as a note Like Dislike Report an issue : Added to Selection. Press [⌘ + S] to save as a note