Of course, not only we need to learn the parameters, but we still have other design considerations. Like what is the activation function should we use? That's F net, right? And in the previous ways, what is the F net? Now you can use different error functions, because we need to calculate this. So we want to know what activation function should we use. And then also, how many inputs does the network need? 

This question should be easier to answer, for example, based on the number of features, you can have the input as the number of features, that's dimension, okay? How many hidden layers? This is a hard question, actually, because we need to evaluate and change the number of hidden layers to decide, you know, we may need trial and error. At least you need one hidden layer. Now once you decide the hidden layer, and then on each layer, how many hidden neurons you have? And then now another question is that how many outputs should the network have? Actually, this question becomes a little bit easier, depending on how many classes you have. 

So if you have a binary classification problem, you may just need one output layer, or one output, okay? Now if you have multiple, like three classes, you may need three, you may use the three outputs, okay? And then you use one versus the others. Or you can use the combination of, like you trade every pair of output as one over the others. Then you need maybe three, you have three classes, then you may need to have more output, okay? Three over, because you do the, you just trade each classes as 1, 0, 1, and then 0, 1, 0, 1, 0, 1, as the class. So then you get a different number of output. 

Maybe you end up having three, and you have a combinatory of a number of outputs, based on the n classes. Say you have k classes, k of 2, okay? Because you pair each, you take each pair of classes as the output, and put them into the concentration, okay? So overall, there is no standard method to determine these values, okay? Even there is some heuristic point, final values are determined by the trial and error procedure. Basically, that you have to try, okay? And also, as we know, neural network that can take any kind of input, but still we have to transform data to the neural network input, okay? 

So we can apply the mathematical or statistical functions, or we can encode in the texture data from the database into that data that neural network will accept. So most likely we will try to change the input into a numerical value from 0 to 1, okay? And also, we want to select the most relevant data, therefore, we want to remove the outliers, okay? And oftentimes, you want to minimize the network input by feature extraction, or we buy principal component analysis, and we can do the waveform transformation, or we can apply the image analysis so that we extract the image features, input the image features into the neural network, okay? And then we're coding those preprocessing data to the network input. So preprocessing is a very important step for the neural network input as well, not just the input raw data, okay?