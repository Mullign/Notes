Let's look at a simple quadratic problem and how they change a quadratic surfaces while actually making a linearity inseparable problem become separable, okay? Say for example, a perceptron with just the feature f1 cannot separate this data, okay? There is only f1 feature, there's one feature, okay? One dimension and now we could add another feature to our perceptron f2 as a second order, f2 equal to f1 square, okay? By doing this, our data become in the two-dimensional space, okay? We have a two-dimensional space, right? So f1 and f2. Now you can see we can draw a line, horizontal line to separate this green with the red, while in the original space we cannot draw it. Of course, in the original space we can use a quadratic surface to separate it, like we draw a round surface here, right? 

Okay, so we could also think this is just using feature f1 but now in our quadratic surface to separate the data, okay? But here with the separation we use the perception on the two-dimensional space to do it. So as we know for a problem that non-linearly separable problem, we could using the quadratic machine or using a higher order features to increase the dimensions. Could we solve the problem without adding any input dimensions? Well, this gave us our answer here, that adding the multiple layers, okay, of perceptron. This was proposed in 1969 by Minsky and Pepper. They offered the solution to XOR problem by combining perceptron unit responses using a second layer of unit. 

So in the first layer, what they do is they use one perceptron load to draw a linear line to separate the top, left top circle with the rest of objects, okay? And then on the bottom number two, another perceptron draw a line to separate the bottom one from zeros. But then with those two lines as the input, okay, so the features in this number one, the output of the number one has the features and the output number two has the features. These two features input into another perceptron and with the linear threshold and finally they get a separable problem now. So basically it is a piecewise linear classification using a multi-layer perceptron with threshold unit. There is no need to do a pre-process of input space. We are solving the problem with the original two-dimensional space. So this is the key that adding multiple layers, okay? So multi-layer perceptron network is actually make the sophisticated decision-making surfaces. So, well, single-layer perceptron always fail in the problem such as XOR problem, okay? And so for a multi-layer perceptron network, it includes at least one hidden layer, okay? And so there is a website you can try to play around with multi-layer perceptron network. Well, basically you can use the different number of input with a different number of hidden layers, different number of layers, and with different output. And I suggest you go to this website, pregrunt.tensorflow.org to play with this MLP, okay? So multi-layer neural networks will allow us to discover higher-order features automatically from the original input space without any pre-process or any increased dimensions of original input, okay? So that's what happens. So that comes with our neural network, okay?