Okay, so we have learned that a perceptron node can solve a linearly separable problem. And so, what about the other problem that is not a linear solver, for example, an XOR problem. In this case, you cannot draw a linear line to solve this problem. You cannot draw a linear line to separate 1 from 0. There is no way you draw a linear line to separate them. So apparently, perceptron cannot represent this exclusive OR problem since it is not linearly separable. So how do we address such a problem? 

Well, look at our solution on the perceptron. We are using a linearly weighted linear submission of input, right? So we have used that. But we could pre-process the input in a nonlinear way by adding the second order like we did in the SVM, okay? For example, a problem with two inputs x and y plus the bias, right? So if you do the weighted summation of x and y, it is still, it can only solve the linear separable problem. But if we add extra inputs like x square, y square, and xy, then with these five inputs, then we will use the perceptron to solve the problem in this five-dimensional space, okay? 

So this five-dimensional task is linearly separable. Now of course, what kind of decision surfaces would it allow for the original two-dimensional input space? As we said, a hyperplane cannot separate this XOR problem at all. However, in the original two-dimensional input space, actually, a quadratic machine can solve it, okay? That all quadratic surfaces can solve that XOR problem, like ellipsoid, parabola, and et cetera. There are a bunch of quadratic surfaces that can separate these two-dimensional XOR problems. With the quadratic surfaces, it significantly increases the number of problems that can be solved, but even with the quadratic surfaces, there are still many problems which are not quadratic separable. 

Well, our solution, we can go to the third or higher-order features. For example, given the x and y as the original two-dimensional space, we can increase the dimension to the third degree. For example, we get x, y, and x, y, and x, y, x square, y square, x square, y, x, y square, x cube, y cube, and we go on, right? But by doing these higher-order features, the number of possible features grows exponentially.