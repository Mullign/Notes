The key element of our neural network requires a number of neurons, okay, and it can be connected together into a neural network, and those neurons are arranged in layers, okay, so each neuron is a weighted summation plus the bias, and then with an activation function. Now given the threshold function like we did, the threshold logic unit, for a single node, we can only solve linearly separable problems, okay, and so at each neuron, every input has an associated weight which modifies the strength of each input, okay, and then the neurons simply add all the inputs with this such a weight that calculates the output to be passed on to the next layer. Of course, it is based on the activation function, okay, so the property of a neural network is that there is no connection within a layer, so in the same layer, you don't connect those neurons, you only connect the neurons with your next layer, okay, no direct connection between input and output, okay, so the input and output layer should not connect, and also between the layers, there must be a full connection. What do you mean? That it means each node in the layer i minus one should be fully connected with the nodes in layer i, okay, and usually it is more than three layers because it should include at least one hidden layer, okay, and without a hidden layer, it is just a single layer perceptual network, okay, and also the number of input units are different, usually different from the number of output units, okay, why? Because the number of input units represent the input features, okay, or input variables, and then the number of output units decide the number of classes or decide the number of output, okay, or a number of features, output features, okay, so they should not be equal or they need not to be equal, and also the number of hidden units per layer can be more or less than input or output unit, that depends on how your problem and how complex your problem is, okay, and also we often put the bias as an extra weight so that we can uniform the computation of this weight, okay, because if you put the bias in the threshold unit as a threshold, then you may, you have to adjust that as well, but you put as an extra weight with a constant input of one or negative one there, you don't have such a problem, you do the threshold, okay, and basically you just adjust that bias as a weight, all right, so this is a neural network, okay, so this is a typical four bit forward neural network, okay, you can see you have a n input, okay, n input, and then that's input node, and then you have a layer of three hidden nodes, and then you only have two output layers, so this is an input, okay, this is a neural network. Now, why do we have this, do you need these multiple layers? Because with those multiple layers, we have automatic feature representation learning, by the first layer, we learn the feature, and each, you know, for example, with a different weight, with a hidden layer, we learn the feature, for example, in this LMP on the B, right, with the input, okay, x1, okay, and x2, and two variables, and the input 2 will be 0 0 0 1 1 0 1 1. Now, then your hidden layer u3, okay, actually is a sign of, given those parameters, negative 1, negative 1, 1, 1, is a hidden layer, will equal to sign x2 minus x1 minus 0.5, okay, so that's the result of u3, the output 0 1, output 0 1, and then the same goes output 0 2 is the unit of u4, okay, sign of x2 minus x1 plus 0.5, okay, so you can see, you learn the feature, and those features combined into u5 is 0 2 minus 0 1 minus 0.5, okay, so this is how we output of this hidden unit back into this result of y. Of course, you can do this, okay, with this, so basically that it learned the complex non-linear mapping functions, okay. 

Now, the feed-forward neural network does not have a backwards connection, you cannot connect from the layer backwards, you only get connected from left to right, going one direction, okay, so you can see from this neural network here, and in each layer, you learn the different features, okay, and so you learn the different features, you get a different result, okay, the o1 has a result 0 0 1, so that's the feature, and the o2 is the result 1 1 0 1, so this is the other set of features, okay, so this is a feed-forward neural network. Now, look at the neural network, and we haven't touched the part where what is the activation function, there are, and as we said, we can change the input in the pre-process, change the input with adding the non-linear narrative, right, by adding the input, well, actually, you can add non-linear activation function, okay, further adding, so it's just at the output level to add the non-linear narrative, well, there are several activation functions, sigmoid and tangent renal, leaking renal, you know, those are examples of activation functions, okay, and we will learn later on in our deep learning and our neural network, okay, deep learning, but right now, we just gave it this here, and we will learn later on, okay.