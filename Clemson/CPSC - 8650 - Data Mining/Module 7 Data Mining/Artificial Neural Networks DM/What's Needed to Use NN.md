Given the neural network, what should we learn? What do we need to use the neural network? First of all, we need to determine the number of inputs, right? So usually, given the dimensions or given the number of features of input, we can use the number of input nodes as equal to the number of input features or input units, okay? And then we need to know how to connect the data to learn or to test. So we need to convert the data into the data that is suitable for the neural network. Now the neural network, you know, for example, you have images, you have text, and you have videos. 

How do we convert those data that can fit into the neural network, okay? And then with those, how do we test those data with the neural network? So those are the problems we have to answer later on, okay? And also, how many hidden names and how many hidden nodes? We need to find an optimal number, because with more hidden nodes, it will increase the complexity of the network, but if you use a smaller number of hidden nodes, you may not solve the problem, because the problem itself is already complex, and you need that many hidden nodes, okay? And finally, look at those Ws, right? The W was learned during the process of the training. 

So how do we estimate those parameters? How do we learn these weights? This is the learning process, okay? Of course, we also need to know how to evaluate the performances of the net. Of course, evaluate the network performance based on the accuracy, right? Based on the speed of the training, right? And based on the classification accuracy or feature extraction accuracy. 

Now, if we evaluate the performance of the constructed network, and then we find that the performance is not satisfactory, then we have to go back again to review all the preceding points, which include determining the input, maybe at this moment say, oh, yeah, we can increase the number of dimensions, or we can determine if we collect the correct data. We may just, you know, select the wrong data, or for the learning and the testing, and then we can change the number of hidden nodes, or we have a problem in our learning phase. So those are the cycle back again. So all of those will tell us how to, you know, build a neural network. And building an efficient neural network is not an easy task, it is very hard, okay?