​The next trend is the network as a contact. ​So network of X, ​each node represent an entity, ​a dataset or data mining models. ​For example, network of time series. ​So basically, the network in here is ​network of something. ​Okay, so that's the context. ​The key idea of mining network of serious data, ​for example, is to model each time series ​by either traditional secretive process approach ​or deep neural network. ​Then you leverage the contextual network ​to recognize different time series model. 

​Another network as a context example is ​each node represent an entity, ​a dataset or data mining model. ​This data mining model can be a network, right? ​So you can have a network of networks. ​So each X in this network of X, ​the node itself is a contextual network, ​which represent another domain specific network. ​The main advantages of the network of network model is ​to expressly encode the hierarchical structure of the network. ​With the contextual network, ​it provide additional regularization. ​And network of X can be a network of data mining models. 

​So because the X can be an entity, a dataset or model, right? ​So we already talked about entity, dataset or model. ​And then data mining models, ​so each X itself is a data mining model. ​And different performance prediction models ​borrow data from each other. ​And then they mutually boost each other's prediction performance. ​It is like our like a bagging approach ​or just to use the small base like ensemble, right? ​As a network, as a contact, there are a few future directions ​such as contextual network construction. 

​This can be applied to more applications. ​For example, you can apply it to construct a knowledge graph, ​heterogeneous network generation. ​We can also integrate other data mining problems. ​For example, adversarial learning, fairness explainable learning. 

​The next trend is the network as a contact. ​So network of X, ​each node represent an entity, ​a dataset or data mining models. ​For example, network of time series. ​So basically, the network in here is ​network of something. ​Okay, so that's the context. ​The key idea of mining network of serious data, ​for example, is to model each time series ​by either traditional secretive process approach ​or deep neural network. ​Then you leverage the contextual network ​to recognize different time series model. ​Another network as a context example is ​each node represent an entity, ​a dataset or data mining model. ​This data mining model can be a network, right? ​So you can have a network of networks. ​So each X in this network of X, ​the node itself is a contextual network, ​which represent another domain specific network. ​The main advantages of the network of network model is ​to expressly encode the hierarchical structure of the network. ​With the contextual network, ​it provide additional regularization. ​And network of X can be a network of data mining models. ​So because the X can be an entity, a dataset or model, right? ​So we already talked about entity, dataset or model. ​And then data mining models, ​so each X itself is a data mining model. ​And different performance prediction models ​borrow data from each other. ​And then they mutually boost each other's prediction performance. ​It is like our like a bagging approach ​or just to use the small base like ensemble, right? ​As a network, as a contact, there are a few future directions ​such as contextual network construction. ​This can be applied to more applications. ​For example, you can apply it to construct a knowledge graph, ​heterogeneous network generation. ​We can also integrate other data mining problems. ​For example, adversarial learning, fairness explainable learning. ​The next trend is the network as a contact. ​So network of X, ​each node represent an entity, ​a dataset or data mining models. ​For example, network of time series. ​So basically, the network in here is ​network of something. ​Okay, so that's the context. ​The key idea of mining network of serious data, ​for example, is to model each time series ​by either traditional secretive process approach ​or deep neural network. ​Then you leverage the contextual network ​to recognize different time series model. ​Another network as a context example is ​each node represent an entity, ​a dataset or data mining model. ​This data mining model can be a network, right? ​So you can have a network of networks. ​So each X in this network of X, ​the node itself is a contextual network, ​which represent another domain specific network. ​The main advantages of the network of network model is ​to expressly encode the hierarchical structure of the network. ​With the contextual network, ​it provide additional regularization. ​And network of X can be a network of data mining models. ​So because the X can be an entity, a dataset or model, right? ​So we already talked about entity, dataset or model. ​And then data mining models, ​so each X itself is a data mining model. ​And different performance prediction models ​borrow dat: Added to Selection. Press [⌘ + S] to save as a note : Added to Selection. Press [⌘ + S] to save as a note