Then the next type of data is spatial and temporal data. ​How do you mine the spatial and temporal data? ​The case properties in the spatial and temporal data is auto-correlation. ​The dependency and correlation in approximate location and time. ​Basically, there will be similar traffic studies in adjacent intersections, for example. ​Also, the data has heterogeneity. ​Heterogeneity and non-stationary distribution is the natural of spatial and temporal data. 

​There are several types of spatial and temporal data. ​First is the event. ​Basically, event is what happens at a spatial location and at a time point. ​The operation is trying to identify the intersections or find the similarity or difference. ​So you want to find the intersection of forest fire ​and a rainfall return and the overlapping area and time. ​Then trajectory is another data type. ​Basically, it defines a path of an object over a space and time. 

​The operation is to find the relationships between two trajectories. ​The example is in transportation. ​Well, for example, you want to trace a vehicle or you can use it in ecology. ​And then another data type is the point reference data. ​And the point reference data is connected using discrete reference points. ​The operation is to reconstruct the fields and modeling the non-stationary random process. ​So, for example, you use the mobile sensor to connect the temporal data in a certain region. 

​Another data type is the raster data. ​You record the observation data at fixed locations and fixed time. ​You can convert a raster to a finer or coarser resolution. ​For example, you estimate the traffic volume using data from nearby sensors and cameras. ​How do you mine the spatial temporal data? ​There are several data models for spatial data. ​You can use an object model where you use the point, line, and polygons to represent the object. 

​For example, you use the point to represent the vehicle and the line to represent the road. ​The polygon references structure. ​And the field model, you can use the spatial information as a function. ​Well, for example, temperature in the area. ​You can also use spatial network model where graphs for relationship, for example, the road network. ​The data models for temporal data, well, you can use the temporal snapshot model. ​Okay, the multiple spatial layers associated with time. 

​Or you can use the temporal change model. ​And this is a spatial theme using a start time and the incremental changing. ​For example, moving of a vehicle with initial location and particular speed. ​You can also use the event process model. ​You have multiple events and processes over the time.