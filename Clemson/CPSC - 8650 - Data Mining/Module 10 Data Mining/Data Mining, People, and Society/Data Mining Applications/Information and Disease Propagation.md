​Now we study the applications in the information and the disease propagation. ​So information and the disease propagation, the research problem is to predict the propagation ​and optimize the propagation. So there's two problems, the prediction problem and optimization ​problem. For example, in the social media we want to predict which piece of the information ​is likely to go viral and also we can want to detect the rumor sources who started a ​misinformation campaign and then we want to neutralize the propagation of the misinformation. ​So those are what we want to do in the social media. ​And also in the computational epidemic biology where you want to ​determine the critical network conditions under which an epidemic is likely to happen. ​Therefore we prevent the disease to spread. 

​So the prediction problem of propagation is you can use the classification or regression to ​determine whether information being popular and gave the popularity score. ​And also we want to predict around the publication, we want to predict the information before ​or after the information is published. And then we also want to predict at the different ​granularity. For example, we want to predict on the individual or groups of information. ​In the social network, we want to optimize the propagation such as ​create a maximized influence. So that's influence maximization by choosing a set of initials to ​maximize the number of infected nodes. So you want to spread the information as quickly as ​possible and which nodes we want to start to spread the information first. 

​And also like a source localization to identify the source of a cascade over the network with ​partial observation to identify the source dot from the source. ​And also we want to study the activity shaping where you try to change the user's activities by ​reshaping the user's activities and misdirect or misdirect the users. ​And you can also use graph connectivity optimization. ​You use the graph connectivity optimization to manipulate the graph topology ​so that you can affect the information propagation results. ​There are several models for information diffusion problems. ​And like a feature-based model, the idea is you use the supervised models for information ​diffusion predictions. So you can use the temporal, local, global structure, ​and the user and items, etc. as their features for feature-based model. 

​And then you can also use the generative models. The idea is to take information diffusion as ​event sequence in continuous temporal domain and formulate as statistically ​generative approaches. So for example, you can use the Poisson process, ​survival analysis, Ork-Hawking process, and epidemic models. ​Of course, deep learning-based model is very popular currently. ​The idea is that there's no assumption on the information diffusion process. ​And we feed the multi-model data with the various neural network modules. ​And also for the disease propagation, we have computational epidemiology models such as SAIS, ​that's a susceptible, infected, and susceptible model. And SIR, ​susceptible, infected, and recovery model. Or you can do SEIR, which is susceptible, ​exploded, and infected, and recovered model. And then there are other variations of models.