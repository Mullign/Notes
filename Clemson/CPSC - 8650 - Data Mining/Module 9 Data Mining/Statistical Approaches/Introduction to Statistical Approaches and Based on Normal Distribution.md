​Now we study the statistical approaches for outline detection. ​There are two kinds of methods for outline detection using the statistical approaches. ​One is called the parametric method and another is non-parametric method. ​The general idea behind the statistical method for outline detection is to learn a generative model fitting the given data set, ​then identify those objects in low probability regions of the model as outliners. ​A parametric method assumes that normal data objects are generated by a parametric distribution with a finite number of parameters. ​The probability density function of the parametric distribution f x data gives the probability that object x is generated by the distribution. ​The smaller this value, the more likely x is an outliner. 

​A non-parametric method tries to determine the model from the input data because we cannot simply use a small set of parameters to determine the model. ​Once we get the model, then we judge whether any data point is an outliner or not. ​For example, we can use a histogram to model the data. ​Our first outline detection method for using the univariance outline is based on normal distribution. ​The assumption is that data are generated from a normal distribution. ​So what we need to do is learn the parameters for normal distribution based on the input data. ​Then we take the data point and then check whether the data point has a low probability. 

​And then those with the low probability will be outliners. ​In the past 10 years, the stated average temperature values in July is from 24 to 29.4. ​So those are 10 values, right? ​So as we know, a normal distribution is determined by two parameters, the mean, mu, and the standard derivation, sigma. ​So we can use the maximum likelihood method to estimate the parameter mu and sigma. ​Actually, mu is the summation of all the values divided by the n. ​And then the sigma is the summation, is the difference between the value with the mean square. 

​And then take the divided by n. ​So that is easy to calculate. ​Now after we calculate this, we use the mu and the sigma as the parameters. ​And we apply to the probability function, say, f mu, sigma, fx over mu and the sigma square. ​But we can also use the z-score of those values. ​For example, we calculate the 24 based on mu and sigma. ​The z-score is 24 minus 28.6 divided by 2.2. 

​Divided by the square root of 2.21. ​Is that 1.5? ​So probably around 3, okay? ​Around 3. ​So the value is a negative 3. ​Is 24 minus 28. ​So the z-score is negative 3. 

​So if we take the z-score and check the z-table, ​actually we can tell that 24 is actually beyond the 15% of that. ​That means the value of 24 is less than 20.15% ​in that very tail end on the normal distribution curve, okay? 

​Now we study the statistical approaches for outline detection. ​There are two kinds of methods for outline detection using the statistical approaches. ​One is called the parametric method and another is non-parametric method. ​The general idea behind the statistical method for outline detection is to learn a generative model fitting the given data set, ​then identify those objects in low probability regions of the model as outliners. ​A parametric method assumes that normal data objects are generated by a parametric distribution with a finite number of parameters. ​The probability density function of the parametric distribution f x data gives the probability that object x is generated by the distribution. ​The smaller this value, the more likely x is an outliner. ​A non-parametric method tries to determine the model from the input data because we cannot simply use a small set of parameters to determine the model. ​Once we get the model, then we judge whether any data point is an outliner or not. ​For example, we can use a histogram to model the data. ​Our first outline detection method for using the univariance outline is based on normal distribution. ​The assumption is that data are generated from a normal distribution. ​So what we need to do is learn the parameters for normal distribution based on the input data. ​Then we take the data point and then check whether the data point has a low probability. ​And then those with the low probability will be outliners. ​In the past 10 years, the stated average temperature values in July is from 24 to 29.4. ​So those are 10 values, right? ​So as we know, a normal distribution is determined by two parameters, the mean, mu, and the standard derivation, sigma. ​So we can use the maximum likelihood method to estimate the parameter mu and sigma. ​Actually, mu is the summation of all the values divided by the n. ​And then the sigma is the summation, is the difference between the value with the mean square. ​And then take the divided by n. ​So that is easy to calculate. ​Now after we calculate this, we use the mu and the sigma as the parameters. ​And we apply to the probability function, say, f mu, sigma, fx over mu and the sigma square. ​But we can also use the z-score of those values. ​For example, we calculate the 24 based on mu and sigma. ​The z-score is 24 minus 28.6 divided by 2.2. ​Divided by the square root of 2.21. ​Is that 1.5? ​So probably around 3, okay? ​Around 3. ​So the value is a negative 3. ​Is 24 minus 28. ​So the z-score is negative 3. ​So if we take the z-score and check the z-table, ​actually we can tell that 24 is actually beyond the 15% of that. ​That means the value of 24 is less than 20.15% ​in that very tail end on the normal distribution curve, okay? ​Now we study the statistical approaches for outline detection. ​There are two kinds of methods for outline detection using the statistical approaches. ​One is called the parametric method and another is non-parametric method. ​The general idea behind the statistical method for outline detection is to learn a generative model fitting the given data set, ​then identify those objects in low probability regions of the model as outliners. ​A parametric method assumes that normal data objects are generated by a parametric distribution with a finite number of parameters. ​The probability density function of the parametric distribution f x data gives the probability that object x is generated by the distribution. ​The smaller this value, the more likely x is an outliner. ​A non-parametric method tries to determine the model from the input data because we cannot simply use a small set of parameters to determine the model. ​Once we get the model, then we judge whether any data point is an outliner or not. ​For example, we can use a histogram to model the data. ​Our first outline detection method for using the univariance outline is based on normal distribution. ​The assumption is that data are generated from a normal distribution. ​So what we need to do is learn the parameters for normal distribution based on the input data. ​Then we take the data point and then check whether the data point has a low probability. ​And then those with the low probability will be outliners. ​In the past 10 years, the stated average temperature values in July is from 24 to 29.4. ​So those are 10 values, right? ​So as we know, a normal distribution is determined by two parameters, the mean, mu, and the standard derivation, sigma. ​So we can use the maximum likelihood method to estimate the parameter mu and sigma. ​Actually, mu is the summation of all the values divided by the n. ​And then the sigma is the summation, is the difference between the value with the mean square. ​And then take the divided by n. ​So that is easy to calculate. ​Now after we calculate this, we use the mu and the sigma as the parameters. ​And we apply to the probability function, say, f mu, sigma, fx over mu and the sigma square. ​But we can also use the z-score of those values. ​For example, we calculate the 24 based on mu and sigma. ​The z-score is 24 minus 28.6 divided by 2.2. ​Divided by the square root of 2.21. ​Is that 1.5? ​So probably around 3, okay? ​Around 3. ​So the value is a negative 3. ​Is 24 minus 28. ​So the z-score is negative 3. ​So if we take the z-score and check the z-table, ​actually we can tell that 24 is actually beyond the 15% of that. ​That means the value of 24 is less than 20.15% ​in that very tail end on the normal distribution curve, okay? ​Now we study the statistical approaches for outline detection. ​There are two kinds of methods for outline detection using the statistical approaches. ​One is called the parametric method and another is non-parametric method. ​The general idea behind the statistical method for outline detection is to learn a generative model fitting the given data set, ​then identify those objects in low probability regions of the model as outliners. ​A parametric method assumes that normal data objects are generated by a parametric distribution with a finite number of parameters. ​The probability density function of the parametric distribution f x data gives the probability that object x is generated by the distribution. ​The smaller this value, the more likely x is an outliner. ​A non-parametric method tries to determine the model from the input data because we cannot simply use a small set of parameters to determine the model. ​Once we get the model, then we judge whether any data point is an outliner or not. ​For example, we can use a histogram to model the data. ​Our first outline detection method for using the univariance outline is based on normal distribution. ​The assumption is that data are generated from a normal distribution. ​So what we need to do is learn the parameters for normal distribution based on the input data. ​Then we take the data point : Added to Selection. Press [⌘ + S] to save as a note : Added to Selection. Press [⌘ + S] to save as a note : Added to Selection. Press [⌘ + S] to save as a note