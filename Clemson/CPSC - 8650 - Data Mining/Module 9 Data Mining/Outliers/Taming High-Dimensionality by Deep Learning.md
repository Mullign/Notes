​And now we discuss how to use the deep learning method to determine the high dimensionality. ​As we know that deep learning method produces a vector representation of input data tuple ​with a much smaller number of dimensions than the original one. So if we can do that, that will ​naturally elevate the high dimensionality challenge itself. Now those deep learning methods such as ​auto encoder is very good or fade forward neural network we can actually create the ​much smaller feature space or you do the CNN you can come with a much smaller feature space as well. ​Well because the deep learning has the strong ability to learn the semantically meaningful ​representation, basically the produced embedding often capture the ​complex interaction between the different input features. Therefore it is capable of detecting ​outliers that might be overlooked by the alternative methods such as linear ​matrix factorization based approaches. ​So how do we use deep learning method to do this high dimensional data detection? 

​Well we can use deep learning as a pre-process. ​What you can do is you feed the input data set into an auto encoder for example. ​The output from the encoder will produce an embedding of the input data tuple in a much ​lower dimensional space. With such embedding as input then we can use the outline detector methods ​such as approximate based approaches or reconstruction based approaches to detect ​the outlining tuples. So this approach is simple and flexible. So this is advantageous because ​basically you apply the deep learning method on the original data generated feature space or ​encoding to create the embedding of the input data tuple. Then you apply any ​approaches to do the outlining detection. So you can try different approaches to do the ​pre-process and then you combine the different approaches for facial detection or outlining ​detecting. Now there is a disadvantage because this pre-processing approach will separate the ​embedding learning stage with the outlining detection stage. Therefore it may lead to a ​sub-optimal detection result. Ideally we should actually integrate the deep learning method ​with the outlining detection. So the next approach is to integrate embedding learning and outlining ​detection. So you only need to change the embedding of the input data tuples to ​to produce this embedding. Basically what you can do is that you take the embedding of the ​input data tuple result as the result of determining the outline. Therefore you ​simultaneously produce the embedding and the detection result. 

​With the integration, it will lead to a higher detection accuracy. ​For example, you can use a one-class neural network. In a one-class neural network, ​you have a hyperplane output in the feed-forward neural net. ​So you replace that output with an outlining score. ​So then the last hidden layer will produce the embedding of the input data. ​So this is how you get ​the outlining detection integrated with the embedding. ​Another example is derivation networks. 

​In the traditional learning deep learning method, you will come up with the ​feature space. So basically you generate the feature space. ​So you replace the raw feature value x by an outlining score f of x data. ​f of x data is produced by a deep learning model such as a feed-forward neural network ​with a linear output layer. ​And so take the x as an input and theta is the model parameter of the linear learning model. ​So basically you have the outlining score of each ​x value. ​Okay, then we use the mu and the standard derivation in the z-normalization ​by the sample mean. You take the sample mean and the sample standard derivation of outlining score. 

​Okay, and so for the normal tuples, okay, and then we can calculate that ​we can calculate the outlining score of any object.