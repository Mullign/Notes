​Let's discuss the Ensembl method for outline detection. ​Given the input as a dataset X with n data tuples, ​that means we have n rows. ​Then this data tuple has n d-dimension. ​So we have a data matrix of Xn by d. ​For each face detector, it represents the input dataset in a random subspace Xi. ​Okay, we have a k random subspace, so i equals from i1 to k, ​whose dimensionality d' is much smaller than the original facial dimensionality. ​Okay, then we can use an off-the-shelf outliner detector method, ​such as local outliner vector, to assign each data tuple with an outliner score. 

​Okay, then we represent the outliner scores of all n data tuples in the form ​of a vector Yi of length n. ​So basically, each data tuple has an outliner score based on that detector. ​Okay, then represent the outliner score of all n tuples in the form of a vector of Yi of length n. ​And then we have k vectors, okay, y1, y2, yk, because we have a k-based detector. ​Okay, then we aggregate the detected result from the k-based detectors ​to obtain the overall outliner scores for all the input tuples from another vector Y. ​Then we can sort this vector, or we can identify the highest overall outliner scores ​in this Y vector as the outliners. So this is the main idea. Let's use a ​diagram to explain this. So given the input data with n tuples, with n rows, and d features, ​we do the sampling first to sample out some subspace with the d prime features. 

​So we just do a random sample of the features with the smaller features, okay. ​Then we use the smaller feature to do a base detector. Then given this data tuple, ​then each base detector will find the y1, say. So after the subsample, ​then we do a base detector, we come with y1, that's the vector. Then x2, we get the y2. ​Then we aggregate this y1, y2, okay, and then yk for overall y. ​So there are some issues with this outline detector ensemble method. First, ​how to represent the input data in the random subspace? Well, we can do the feature bagging. 

​So we can randomly select a few original features to form the representation of the input data ​in the random subspace. Or we do a rotated bagging. So what you can do is we first generate ​a random subspace of dimensionality d prime, then we project the input data into this subspace. ​And our second question is, like, how do you aggregate the detecting result of base detectors? ​Well, we can do a mean aggregation. So what you do is that the overall outlining score of a given ​data tuple is the average of its outlining scores of the k base detectors. ​Or you can do a maximum aggregation, where the overall outlining score of a given data tuple ​is the maximum of the outlining scores of the k base detectors. 

​Of course, it is important to you to normalize the outlining scores of the base detector before ​you do the aggregation. You can either use the minimum-maximum normalization or z-score ​normalization. The reason we do that is that you don't want any single outlining score of a base ​station to overwhelm the other score of the base detectors. So you have to normalize them. ​Otherwise, one outlining score of one base detector has a very high value, ​and it will actually overwhelm all the other scores and dominate it. 

​Let's discuss the Ensembl method for outline detection. ​Given the input as a dataset X with n data tuples, ​that means we have n rows. ​Then this data tuple has n d-dimension. ​So we have a data matrix of Xn by d. ​For each face detector, it represents the input dataset in a random subspace Xi. ​Okay, we have a k random subspace, so i equals from i1 to k, ​whose dimensionality d' is much smaller than the original facial dimensionality. ​Okay, then we can use an off-the-shelf outliner detector method, ​such as local outliner vector, to assign each data tuple with an outliner score. ​Okay, then we represent the outliner scores of all n data tuples in the form ​of a vector Yi of length n. ​So basically, each data tuple has an outliner score based on that detector. ​Okay, then represent the outliner score of all n tuples in the form of a vector of Yi of length n. ​And then we have k vectors, okay, y1, y2, yk, because we have a k-based detector. ​Okay, then we aggregate the detected result from the k-based detectors ​to obtain the overall outliner scores for all the input tuples from another vector Y. ​Then we can sort this vector, or we can identify the highest overall outliner scores ​in this Y vector as the outliners. So this is the main idea. Let's use a ​diagram to explain this. So given the input data with n tuples, with n rows, and d features, ​we do the sampling first to sample out some subspace with the d prime features. ​So we just do a random sample of the features with the smaller features, okay. ​Then we use the smaller feature to do a base detector. Then given this data tuple, ​then each base detector will find the y1, say. So after the subsample, ​then we do a base detector, we come with y1, that's the vector. Then x2, we get the y2. ​Then we aggregate this y1, y2, okay, and then yk for overall y. ​So there are some issues with this outline detector ensemble method. First, ​how to represent the input data in the random subspace? Well, we can do the feature bagging. ​So we can randomly select a few original features to form the representation of the input data ​in the random subspace. Or we do a rotated bagging. So what you can do is we first generate ​a random subspace of dimensionality d prime, then we project the input data into this subspace. ​And our second question is, like, how do you aggregate the detecting result of base detectors? ​Well, we can do a mean aggregation. So what you do is that the overall outlining score of a given ​data tuple is the average of its outlining scores of the k base detectors. ​Or you can do a maximum aggregation, where the overall outlining score of a given data tuple ​is the maximum of the outlining scores of the k base detectors. ​Of course, it is important to you to normalize the outlining scores of the base detector before ​you do the aggregation. You can either use the minimum-maximum normalization or z-score ​normalization. The reason we do that is that you don't want any single outlining score of a base ​station to overwhelm the other score of the base detectors. So you have to normalize them. ​Otherwise, one outlining score of one base detector has a very high value, ​and it will actually overwhelm all the other scores and dominate it. ​Let's discuss the Ensembl method for outline detection. ​Given the input as a dataset X with n data tuples, ​that means we have n rows. ​Then this data tuple has n d-dimension. ​So we have a data matrix of Xn by d. ​For each face detector, it represents the input dataset in a random subspace Xi. ​Okay, we have a k random subspace, so i equals from i1 to k, ​whose dimensionality d' is much smaller than the original facial dimensionality. ​Okay, then we can use an off-the-shelf outliner detector method, ​such as local outliner vector, to assign each data tuple with an outliner score. ​Okay, then we represent the outliner scores of all n data tuples in the form ​of a vector Yi of length n. ​So basically, each data tuple has an outliner score based on that detector. ​Okay, then represent the outliner score of all n tuples in the form of a vector of Yi of length n. ​And then we have k vectors, okay, y1, y2, yk, because we have a k-based detector. ​Okay, then we aggregate the detected result from the k-based detectors ​to obtain the overall outliner scores for all the input tuples from another vector Y. ​Then we can sort this vector, or we can identify the highest overall outliner scores ​in this Y vector as the outliners. So this is the main idea. Let's use a ​diagram to explain this. So given the input data with n tuples, with n rows, and d features, ​we do the sampling first to sample out some subspace with the d prime features. ​So we just do a random sample of the features with the smaller features, okay. ​Then we use the smaller feature to do a base detector. Then given this data tuple, ​then each base detector will find the y1, say. So after the subsample, ​then we do a base detector, we come with y1, that's the vector. Then x2, we get the y2. ​Then we aggregate this y1, y2, okay, and then yk for overall y. ​So there are some issues with this outline detector ensemble method. First, ​how to represent the input data in the random subspace? Well, we can do the feature bagging. ​So we can randomly select a few original features to form the representation of the input data ​in the random subspace. Or we do a rotated bagging. So what you can do is we first generate ​a random subspace of dimensionality d prime, then we project the input data into this subspace. ​And our second question is, like, how do you aggregate the detecting result of base detectors? ​Well, we can do a mean aggregation. So what you do is that the overall outlining score of a given ​data tuple is the average of its outlining scores of the k base detectors. ​Or you can do a maximum aggregation, where the overall outlining score of a given data tuple ​is the maximum of the outlining scores of the k base detectors. ​Of course, it is important to you to normalize the outlining scores of the base detector before ​you do the aggregation. You can either use the minimum-maximum normalization or z-score ​normalization. The reason we do that is that you don't want any single outlining score of a base ​station to overwhelm the other score of the base detectors. So you have to normalize them. ​Otherwise, one outlining score of one base detector has a very high value, ​and it will actually overwhelm all the other scores and dominate it. ​Let's discuss the Ensembl method for outline detection. ​Given the input as a dataset X with n data tuples, ​that means we have n rows. ​Then this data tuple has n d-dimension. ​So we have a data matrix of Xn by d. ​For each face detector, it represents the input dataset in a random subspace Xi. ​Okay, we have a k random subspace, so i equals from i1 to k, ​whose dimensionality d' is much smaller than the original facial dimensionality. ​Okay, then we can use an off-the-shelf outliner detector method, ​such as local outliner vector, to assign each data tuple with an outliner score. ​Okay, then we represent the outliner scores of all n data tuples in the form ​of a vector Yi of length n. ​So basically, each data tuple has an outliner score based on that detector. ​Okay, then represent the outliner score of all n tuples in the form of a vector of Yi of length n. ​And then we have k vectors, okay, y1, y2, yk, because we have a k-based detector. ​Okay, then we aggregate the detected result from the k-based detectors ​to obtain the overall outliner scores for all the input tuples from another vector Y. ​Then we can sort this vector, or we can identify the highest overall outliner scores ​in this Y vector as the outliners. So this is the main idea. Let's use a ​diagram to explain this. So given the input data with n tuples, with n rows, and d features, ​we do the sampling first to sample out some subspa: Added to Selection. Press [⌘ + S] to save as a note : Added to Selection. Press [⌘ + S] to save as a note : Added to Selection. Press [⌘ + S] to save as a note