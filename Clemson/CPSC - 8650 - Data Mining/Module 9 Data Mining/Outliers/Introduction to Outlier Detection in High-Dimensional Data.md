​Now, we study how to detect the outlines in high-dimensional data. ​We'll first study how to extend the conventional outliner detection into high-dimensional data ​space. ​Then, we'll study how to find outliners in subspaces, and we'll also discuss the outliner ​detection ensemble methods. ​We'll also study how to use deep learning methods to address the high-dimensionality ​problem. ​We then discuss how to directly model the high-dimensional outliners. ​What are the challenges in the outliner detection with the high-dimensional data? ​First, the interpretations of outliners in high-dimensional data are very hard. 

​Secondly, the data sparsity, where distances between objects become heavily dominated by ​noise, as dimensionality increases. ​Third, the outliner detection method in high-dimensional data has to be adaptive to the data subspaces. ​Finally, the scalability is always an issue with respect to the dimensionality, like in ​any other data mining problem and data mining algorithms. ​Now, we'll first study how to extend the conventional outliner detection method into the high-dimensional ​data space. ​So we can still use conventional proxy-based models for outliners. ​As we know, when the data dimension becomes very high, the proxy measures become deteriorating. ​So instead of using the direct distance or proximate measures, we can use the alternative ​measures, or we can construct the subspaces and then detect the outliner from the subspaces. 

​So let's see how we directly use the proximate measures, and instead of using the absolute ​distance in the outliner detection, we can use the ranks of distance as the measure of ​the outliner list. ​Like in the high-out algorithm, it's still using the distance-based outliners, then we ​use the rank instead of a direct absolute distance to determine the outliner. ​So the idea here is you find the k-nearest neighbors of object O, and then instead of ​only finding the k-nearest neighbor, we can find the 1-nearest neighbor, 2-nearest neighbor, ​up to k-nearest neighbor. ​Then we weigh the object by sum of the distance to the k-nearest neighbor. ​Then we rank all the objects based on their weight in the descending order, then we take ​the top i object with the weight as the output of the outliner. ​So i is another parameter that we have to specify as well.