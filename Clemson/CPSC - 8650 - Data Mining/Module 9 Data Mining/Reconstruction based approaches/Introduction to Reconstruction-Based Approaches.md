​Now we learn the reconstruction-based approaches for outliner detection. ​We have two kinds of methods for reconstruction-based approaches. ​One is metric factorization-based methods. ​These methods are primarily for numerical data, and we'll also learn the pattern-based ​compression method for categorical data. ​And let's use this example to demonstrate how reconstruction-based approaches work. ​In a publication database, there are research and publication values. ​So each research will list their publication values. 

​Those publication values are in two fields. ​One is data mining, and the other is software engineering. ​Now if we compress the data into a succinct representation where using the publication ​areas to represent their publication values, then we end up having Johan publish in data ​mining and Kao publishing in software engineering, especially for Johan, for Kao, because the ​majority of the publication value is in software engineering. ​So we put Kao in the software engineering area. ​When we reconstruct the data to the original form where we include the publication values, ​you can see there is a difference between the reconstructed data of Kao, which is FSE ​and ICSE, and missing the ICDF, there is an error there. ​So we can say Kao is the outlander because there is a problem with the reconstructed data. ​So we have to answer those questions. 

​The key question we have to answer is how to find the succinct representation, and how ​to use the succinct representation to reconstruct the original data sample. ​And of course, after reconstruction, how to measure the quality or goodness of the reconstruction. ​So those are the questions we have to answer in the reconstruction-based outlander detection method. ​The first kind of reconstruction method is a mesh factorization-based method. ​It is used for numerical data. ​So basically, in this approach, our data sample is represented as an attribute vector. ​So the entire input data set is then represented as a data matrix with rows of different data ​samples, columns of different attributes. 

​So in each data entry of this data matrix indicates the attribute values of the corresponding ​data samples. ​So the metric factorization is trying to approximate the data matrix by two or more low-rank metrics. ​So instead of using the n by d, n is the number of samples, d is the dimension, we come up ​with two or more low-rank metrics to approximate the original data matrix. ​Then when we reconstruct the original data, we will find there are errors on data samples. ​We use the error of each data sample as the indicator of outlander risk. ​Then the higher the reconstruction error, the more likely the given data sample is outlander. ​For example, this is an example here. 

​Now given the data set, we already discussed on that previous example, and then each row ​is the author names, and then each attribute, each column is the conference values. ​So you will see you put a 1 if it is published in KDD, ICDM, then we put a 1 if it doesn't ​publish anything in FSD, put a 0, and then put a 0. ​So we get this data matrix. ​Using the factorization, we can use two smaller metrics, which is matrix F and matrix G. ​So then when we reconstruct the original data with these two metrics F and G, which is F ​is times G, and our result will be missing the conference for ICDM for cow, because that ​one converted back to 0. ​So there's an error there. ​And all the others are the exact same. 

​There's no error there. ​So we can say cow may be an outlander. ​So the algorithm is very simple. ​So given a data set sample, each of which is represented by an m-dimensional numerical ​attribute vector, and then we want to find the K outlanders, then R is the rank. ​So what you do is to represent the input data as the matrix of X of n by m, okay? ​Then approximate the matrix by two rank R metrics, where X, make the X approximate to ​F times G. ​So by doing that, then we compute the reconstruction area by time F and G, where Fij is the element ​of F at i-th row and the j-th column, and then Gj is the j-th row in G. 

​Then we do that reconstruction, we take the Xi minus X, the reconstruction of X, okay, ​X hat. ​Essentially, Xi minus the sigma of j from R Fij times Gj. ​So we find that the reconstructed errors, well, we use the square error to make an error, ​you know, not negative error or positive error, so the same. ​Then we rank those errors, okay, and we find the K samples with largest reconstructed error ​of the R i, then those are the outlanders. ​And actually, instead of using two rank metrics, you can also use the singular value decomposition ​method. ​So given the data matrix, the SVD will approximate by multiplication of three rank R metrics, ​the U and the Epsilon and V prime, okay. ​So U and V are orthonormal metrics, and their columns are called the left singular vectors ​and the right singular vectors for data metrics, respectively. 

​So basically, U is the left singular vectors, V is the right singular vectors, okay. ​And then we also have the Epsilon is a diagonal with non-active diagonal elements called the ​singular values. ​So we have those three smaller metrics, okay. ​And then with this, we can also do the reconstruction, find the error, and then get the outlanders. ​So this is a similar approach, like the vectorization approach, okay. 

​Now we learn the reconstruction-based approaches for outliner detection. ​We have two kinds of methods for reconstruction-based approaches. ​One is metric factorization-based methods. ​These methods are primarily for numerical data, and we'll also learn the pattern-based ​compression method for categorical data. ​And let's use this example to demonstrate how reconstruction-based approaches work. ​In a publication database, there are research and publication values. ​So each research will list their publication values. ​Those publication values are in two fields. ​One is data mining, and the other is software engineering. ​Now if we compress the data into a succinct representation where using the publication ​areas to represent their publication values, then we end up having Johan publish in data ​mining and Kao publishing in software engineering, especially for Johan, for Kao, because the ​majority of the publication value is in software engineering. ​So we put Kao in the software engineering area. ​When we reconstruct the data to the original form where we include the publication values, ​you can see there is a difference between the reconstructed data of Kao, which is FSE ​and ICSE, and missing the ICDF, there is an error there. ​So we can say Kao is the outlander because there is a problem with the reconstructed data. ​So we have to answer those questions. ​The key question we have to answer is how to find the succinct representation, and how ​to use the succinct representation to reconstruct the original data sample. ​And of course, after reconstruction, how to measure the quality or goodness of the reconstruction. ​So those are the questions we have to answer in the reconstruction-based outlander detection method. ​The first kind of reconstruction method is a mesh factorization-based method. ​It is used for numerical data. ​So basically, in this approach, our data sample is represented as an attribute vector. ​So the entire input data set is then represented as a data matrix with rows of different data ​samples, columns of different attributes. ​So in each data entry of this data matrix indicates the attribute values of the corresponding ​data samples. ​So the metric factorization is trying to approximate the data matrix by two or more low-rank metrics. ​So instead of using the n by d, n is the number of samples, d is the dimension, we come up ​with two or more low-rank metrics to approximate the original data matrix. ​Then when we reconstruct the original data, we will find there are errors on data samples. ​We use the error of each data sample as the indicator of outlander risk. ​Then the higher the reconstruction error, the more likely the given data sample is outlander. ​For example, this is an example here. ​Now given the data set, we already discussed on that previous example, and then each row ​is the author names, and then each attribute, each column is the conference values. ​So you will see you put a 1 if it is published in KDD, ICDM, then we put a 1 if it doesn't ​publish anything in FSD, put a 0, and then put a 0. ​So we get this data matrix. ​Using the factorization, we can use two smaller metrics, which is matrix F and matrix G. ​So then when we reconstruct the original data with these two metrics F and G, which is F ​is times G, and our result will be missing the conference for ICDM for cow, because that ​one converted back to 0. ​So there's an error there. ​And all the others are the exact same. ​There's no error there. ​So we can say cow may be an outlander. ​So the algorithm is very simple. ​So given a data set sample, each of which is represented by an m-dimensional numerical ​attribute vector, and then we want to find the K outlanders, then R is the rank. ​So what you do is to represent the input data as the matrix of X of n by m, okay? ​Then approximate the matrix by two rank R metrics, where X, make the X approximate to ​F times G. ​So by doing that, then we compute the reconstruction area by time F and G, where Fij is the element ​of F at i-th row and the j-th column, and then Gj is the j-th row in G. ​Then we do that reconstruction, we take the Xi minus X, the reconstruction of X, okay, ​X hat. ​Essentially, Xi minus the sigma of j from R Fij times Gj. ​So we find that the reconstructed errors, well, we use the square error to make an error, ​you know, not negative error or positive error, so the same. ​Then we rank those errors, okay, and we find the K samples with largest reconstructed error ​of the R i, then those are the outlanders. ​And actually, instead of using two rank metrics, you can also use the singular value decomposition ​method. ​So given the data matrix, the SVD will approximate by multiplication of three rank R metrics, ​the U and the Epsilon and V prime, okay. ​So U and V are orthonormal metrics, and their columns are called the left singular vectors ​and the right singular vectors for data metrics, respectively. ​So basically, U is the left singular vectors, V is the right singular vectors, okay. ​And then we also have the Epsilon is a diagonal with non-active diagonal elements called the ​singular values. ​So we have those three smaller metrics, okay. ​And then with this, we can also do the reconstruction, find the error, and then get the outlanders. ​So this is a similar approach, like the vectorization approach, okay. ​Now we learn the reconstruction-based approaches for outliner detection. ​We have two kinds of methods for reconstruction-based approaches. ​One is metric factorization-based methods. ​These methods are primarily for numerical data, and we'll also learn the pattern-based ​compression method for categorical data. ​And let's use this example to demonstrate how reconstruction-based approaches work. ​In a publication database, there are research and publication values. ​So each research will list their publication values. ​Those publication values are in two fields. ​One is data mining, and the other is software engineering. ​Now if we compress the data into a succinct representation where using the publication ​areas to represent their publication values, then we end up having Johan publish in data ​mining and Kao publishing in software engineering, especially for Johan, for Kao, because the ​majority of the publication value is in software engineering. ​So we put Kao in the software engineering area. ​When we reconstruct the data to the original form where we include the publication values, ​you can see there is a difference between the reconstructed data of Kao, which is FSE ​and ICSE, and missing the ICDF, there is an error there. ​So we can say Kao is the outlander because there is a problem with the reconstructed data. ​So we have to answer those questions. ​The key question we have to answer is how to find the succinct representation, and how ​to use the succinct representation to reconstruct the original data sample. ​And of course, after reconstruction, how to measure the quality or goodness of the reconstruction. ​So those are the questions we have to answer in the reconstruction-based outlander detection method. ​The first kind of reconstruction method is a mesh factorization-based method. ​It is used for numerical data. ​So basically, in this approach, our data sample is represented as an attribute vector. ​So the entire input data set is then represented as a data matrix with rows of different data ​samples, columns of different attributes. ​So in each data entry of this data matrix indicates the attribute values of the corresponding ​data samples. ​So the metric factorization is trying to approximate the data matrix by two or more low-rank metrics. ​So instead of using the n by d, n is the number of samples, d is the dimension, we come up ​with two or more low-rank metrics to approximate the original data matrix. ​Then when we reconstruct the original data, we will find there are errors on data samples. ​We use the error of each data sample as the indicator of outlander risk. ​Then the higher the reconstruction error, the more likely the given data sample is outlander. ​For example, this is an example here. ​Now given the data set, we already discussed on that previous example, and then each row ​is the author names, and then each attribute, each column is the conference values. ​So you will see you put a 1 if it is published in KDD, ICDM, then we put a 1 if it doesn't ​publish anything in FSD, put a 0, and then put a 0. ​So we get this data matrix. ​Using the factorization, we can use two smaller metrics, which is matrix F and matrix G. ​So then when we reconstruct the original data with these two metrics F and G, which is F ​is times G, and our result will be missing the conference for ICDM for cow, because that ​one converted back to 0. ​So there's an error there. ​And all the others are the exact same. ​There's no error there. ​So we can say cow may be an outlander. ​So the algorithm is very simple. ​So given a data set sample, each of which is represented by an m-dimensional numerical ​attribute vector, and then we want to find the K outlanders, then R is the rank. ​So what you do is to represent the input data as the matrix of X of n by m, okay? ​Then approximate the matrix by two rank R metrics, where X, make the X approximate to ​F times G. ​So by doing that, then we compute the reconstruction area by time F and G, where Fij is the element ​of F at i-th row and the j-th column, and then Gj is the j-th row in G. ​Then we do that reconstruction, we take the Xi minus X, the reconstruction of X, okay, ​X hat. ​Essentially, Xi minus the sigma of j from R Fij times Gj. ​So we find that the reconstructed errors, well, we use the square error to make an error, ​you know, not negative error or positive error, so the same. ​Then we rank those errors, okay, and we find the K samples with largest reconstructed error ​of the R i, then those are the outlanders. ​And actually, instead of using two rank metrics, you can also use the singular value decomposition ​method. ​So given the data matrix, the SVD will approximate by multiplication of three rank R metrics, ​the U and the Epsilon and V prime, okay. ​So U and V are orthonormal metrics, and their columns are called the left singular vectors ​and the right singular vectors for data metrics, respectively. ​So basically, U is the left singular vectors, V is the right singular vectors, okay. ​And then we also have the Epsilon is a diagonal with non-active diagonal elements called the ​singular values. ​So we have those three smaller metrics, okay. ​And then with this, we can also do the reconstruction, find the error, and then get the outlanders. ​So this is a similar approach, like the vectorization approach, okay. ​Now we learn the reconstruction-based approaches for outliner detection. ​We have two kinds of methods for reconstruction-based approaches. ​One is metric factorization-based methods. ​These methods are primarily for numerical data, and we'll also learn the pattern-based ​compression method for categorical data. ​And let's use this example to demonstrate how reconstruction-based approaches work. ​In a publication database, there are research and publication values. ​So each research will list their publication values. ​Those publication values are in two fields. ​One is data mining, and the other is software engineering. ​Now if we compress the data into a succinct representation where using the publication ​areas to represent their publication values, then we end up having Johan publish in data ​mining and Kao publishing in software engineering, especially for Johan, for Kao, because the ​majority of the publication value is in software engineering. ​So we put Kao in the software engineering area. ​When we reconstruct the data to the original form where we include the publication values, ​you can see there is a difference between the reconstructed data of Kao, which is FSE ​and ICSE, and missing the ICDF, there is an error there. ​So we can say Kao is the outlander because there is a problem with t: Added to Selection. Press [⌘ + S] to save as a note : Added to Selection. Press [⌘ + S] to save as a note : Added to Selection. Press [⌘ + S] to save as a note