​OK, let's look at the distance based on a detection method. ​So given r, which is r is greater than or equal to 0, ​be a distance threshold. ​And then pi be a fraction threshold. ​We have two parameters. ​Object O is a distance based outliner regarding r and pi ​if the number of objects that has a distance less than r ​or equal to r divided by the total number of objects ​in the data set is less than pi. ​So that's simple. ​So the algorithm is simple. 

​We have an input data set, and then the threshold r and pi, ​and then we just count how many number of objects ​are within the distance of r or less than or equal ​to distance r. ​So then if the count is greater than or equal to, ​greater than the pi times the total number, ​then that object should not be considered as an outliner. ​If it's less than pi times n, then that's the outliner. ​So that's a simple algorithm. ​OK?