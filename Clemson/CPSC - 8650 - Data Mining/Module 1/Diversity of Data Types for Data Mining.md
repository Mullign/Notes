![[Diversity of Data Types for Data Mining slides.pdf]]Data mining involves a diversity of data types. It can be a structured data or unstructured data. The structured data actually are uniform recorded in the table-like structures, and it may be defined as a data dictionaries or database schemas, so with a fixed set of attributes. Each attribute has a fixed set of value there, and then the attribute is fixed semantic meaning. For example, the most popular structured data is the relational database. Then in a data warehouse has a data cube, and then we can also have a data matrix. Another data set is unstructured data. 

Usually, the text data, modern media data such as audio, image, and video data. There is a mix of structured and unstructured data is called the semi-structured. The data object contain a set of values, and then this set of value has a heterogeneous type of value. Or maybe the structures are nested, and now the nested structure within the structure that are unstructured data. So it has a certain structure, but in the detail, it become unstructured data. Then for example, graph data, network data, so basically, they are more sophisticated type of semi-structured data set. It has some structure, but it is not a fixed structure. 

Maybe it has a large unstructured natural within this structured data, but it has certain structure. So in the real world, data can often be a mixture of structured, semi-structured, and unstructured data. Different data associated with the different application. For example, in the sequence data, you can have biological sequences, and you can also have a shopping transaction sequence. For time series data, you can have a set of ordered numerical values with equal time intervals, and then you can also have spatial, temporal, or spatiotemporal data. For the graph and network data, such as social network, author network, computer communication network, biological network, information network. So they are different networks, carries various different semantics. 

In the same data set, you can also find the different kinds of patterns. So you need different mining methods. For example, software programs can find pejoratized modules, and you can also find, copy, and paste the bugs. And also, the data can be stored, and then with a finite amount of data, with large data repository. But you can also have the data coming continuous without end. We call that as a streaming data. For example, you have a radio surveillance or remote sensing, where you have dynamically incoming data, infinitely in real time. 

Okay? So, different data prevent you mining the data effectively, because the environment. Okay? So they have different challenges.