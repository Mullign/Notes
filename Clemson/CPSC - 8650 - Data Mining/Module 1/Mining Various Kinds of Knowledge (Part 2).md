![[Mining Various Kinds of Knowledge (Part 2) slides.pdf]]
Our next knowledge discovery method is called the clustering analysis. So classification, we often call it a supervised learning because, like we said here, that we construct the models based on some training examples. Cluster analysis is called unsupervised learning. So we are not having clustered neighbors, but we want to group data to form new categories or clusters. So for example, we want the cluster houses to find the distribution patterns, okay? The cluster analysis has a principle where you try to make sure they maximize the inter-class similarities. What does this mean? 

It means within the cluster, the objects are very similar. And then you want to minimize the inter-class similarities, where if you have two objects in different clusters, they should be very dissimilar, okay? Of course, there are many methods and applications with this cluster analysis, okay? Our current popular data mining method is called deep learning. Deep learning is a fast-expanding dynamic frontier in the machine learning. So basically, deep learning started with the neural network. It has feed-forward neural network, conventional neural network, convolutional neural network, recurrent neural network, graph neural network, and the transform. 

And those deep learning methods have built the foundation for our AI applications. Deep learning has broad applications in computer vision, natural language processing, machine translation, social network analysis, and then a lot of other AI applications. Actually, deep learning has reshaped a variety of data mining tasks, okay? It can be applied to classification, clustering, outliner detection, and reinforcement learning. And the next method is called outliner analysis. So what is an outliner? So an outliner is an object that does not comply with the general behavior of this group of data, okay? 

So of course, we have to make sure that this outliner is not something like a noise or an exception, okay? So one person's garbage could be another person's treasure. So to you, you think that some noise you can ignore, then some other people maybe identify that as a very important information. For example, you can use as a fraud detection, the outliner, or you can identify some real event, okay? So you can either use cluster analysis or use regression analysis to identify outliner. Now you can also use some statistic learning method just to come up with the outliner detection, okay?