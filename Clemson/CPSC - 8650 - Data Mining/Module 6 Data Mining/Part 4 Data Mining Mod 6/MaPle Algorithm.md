So in mock play algorithm, basically it started with a proposed efficient method to eliminate of delta p cluster. So it used the same idea as the pattern growth in frequent pattern mining, and it based on the downwards closure property, because for each conditional combination of j, find the maximum subset of gene i, such that i and j is a delta p cluster. If i and j is not a sub-matrix of another delta p cluster, then ij is the maximum delta p cluster, right? So that is what we do. We studied two methods, one is delta p by cluster and delta p by cluster, okay?