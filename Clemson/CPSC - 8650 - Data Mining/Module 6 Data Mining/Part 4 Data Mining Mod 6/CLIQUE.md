KeyCurve is another grid-based approach. Basically what it does is try to address the high dimensionality curves with the same algorithm that it uses a density-based approach and tries to do a subspace clustering with the grid. So what it does is it discretizes the data space through a grid and estimates the density by counting the number of points in the grid. And then it is also density-based in which a cluster is the maximum set of connected dense units in a subspace. So a unit is dense if the fraction of the total data point content in the unit exceeds the input model parameter. So you can set a parameter and then given the data point, given this unit, by proportion, the number of data points should be proportional to the area that in the total space. Then if it is dense, then the number of points within that space, how many per square meter or how much within the radius, right? So because you have a cell, that cell probably is a centimeter, square centimeter, or square millimeter. So if you have that fraction of the total data point contained in that unit exceed the input model parameter, then we call it a dense unit. So what you can do is you find the connected dense unit that you form, put them together as a cluster, okay? So the subspace cluster is the set of neighboring dense cells in an arbitrary subspace, okay? And by doing that, it automatically identifies the subspaces of a high-dimensional data space, okay? And it is now better clustering than original space using the F-priority principle. Because when you find the main idea here is like, if you have a cluster in the original space, there must be a cluster within the subspace first. So that's the main idea. So you find the subspace and then you put all the subspace clusters together and then create the final cluster, okay? So it starts with the one-dimensional space, okay? And then you actually discretize the numerical intervals in each axis into grids. 

And then you find the dense regions, find the clusters in each subspace, okay? And then you generate the minimum descriptions, okay? And then you use these dense regions to find the the candidates in the 2D space based on the F-priority principle. Because if the region on the 2D space contains a non-dense region in the 1D space, then we will not treat that as a candidate. Then at the 2D space, we also compute the statistical information, the density factors, and then we get a 2D dense region. Then we go up to 3D, and then by combining the 2Ds into 3D, so we further go up, okay? So this is the main idea. It's using grids and use the dense parameters and doing the subspace clustering. Look at this example here, right? 

Now you have three dimensions, okay? You have center ring, you have edge, and you have a vacation, right? So what do you do is you put in each subspace as a grid, and then you find give a dense parameter, so you find the dense point, so you do like this, this purple color, then kind of color on this side. So we started with one dimension, okay? In the center range, we form the cluster. So we will say, hey, this 2 to 3, 3 to 4, 4 to 5k, those are the area is dense enough, okay? And then the other, the age area is 30 to 50 dense, okay? Then those two are grouped together, and then form an area, is a big area, okay? And this area is from 30 to 50, and then 2 to 5. So this is a rectangle area. 

Then we compute this density on this area, and then we, because this cell, this first cell, because of the grid, right? In this rectangle area, the top grid is contained 3, 3, so assume like the threshold is 2 for each rectangle, each grid. Then this one is only one, so they are not in there. So our new grid, a new two-dimensional dense region becomes this, okay? And then on the other hand, you have a center on the location, and you also, based on the one dimension, you have a 2 to 5, this area, and then the location is a 2 to 4, and then you form a rectangle, and then in the two-dimensional phase, you do, you find that each cell has a 4, 4, a 6, a 6, 4, and then the other two is a 1, so these two will not be included, right? And then you go to the three-dimension. Three-dimension is a combination of this area and this. So the visualization here is not that great, it's not that easy to identify, but at least we know that on this cube, okay, the cube that contains this and this one point grid, or this one point grid should not belong to that candidate, okay? Directly because we use a priori algorithm, a priori principle. So you form a shape of 3D shape, okay? And then you compute that proportion, okay, that within this 3D shape, how many number of points based on the fraction of points that be dense, and then you may form a shape of 3D. So this is how a clique algorithm works, okay? 

So the major steps for a clique algorithm is like identifying subspaces that contain clusters. So you partition the data space, finding the number of 0.9 inside each cell of the partition, then identify the subspace regions that contain clusters using the a priori principle, okay? And then you determine dense units in all subspaces of interest, okay? And then you find the connected dense units in all subspace interests. So that way you get the clusters now. And then you determine the maximum regions that cover a cluster of connected dense units for each cluster. And you also determine the minimum cover of each cluster. That way you generate the minimum discretion of the cluster, okay? And then you go to a higher and higher space, okay? 

So the pros and cons of clique algorithm is you automatically find the subspace of the highest dimensionality as long as high density clusters exist in those subspaces, okay? And then it is insensitive to the order of records in input, okay? And you don't presume some canonical data distribution because you find the data distribution by the small cell. And it's scaling anyways the size of the input. It has good scalability as the number of dimensions in the data increases. But the problem, because you have the grade, okay? So the grade, the quality of the result is critically dependent on the approximate, appropriate choice of the number and the ways of the partitions. The number and the ways of the partitions and the grade cells. 

So basically you may end up having a non-natural grade, a non-natural cluster because of your your grades, size and your partition, okay? So this is the clique algorithm. And so we have done the density-based and the grade-based approach, and the combination of density-based and the grade-based approach. 

KeyCurve is another grid-based approach. Basically what it does is try to address the high dimensionality curves with the same algorithm that it uses a density-based approach and tries to do a subspace clustering with the grid. So what it does is it discretizes the data space through a grid and estimates the density by counting the number of points in the grid. And then it is also density-based in which a cluster is the maximum set of connected dense units in a subspace. So a unit is dense if the fraction of the total data point content in the unit exceeds the input model parameter. So you can set a parameter and then given the data point, given this unit, by proportion, the number of data points should be proportional to the area that in the total space. Then if it is dense, then the number of points within that space, how many per square meter or how much within the radius, right? So because you have a cell, that cell probably is a centimeter, square centimeter, or square millimeter. So if you have that fraction of the total data point contained in that unit exceed the input model parameter, then we call it a dense unit. So what you can do is you find the connected dense unit that you form, put them together as a cluster, okay? So the subspace cluster is the set of neighboring dense cells in an arbitrary subspace, okay? And by doing that, it automatically identifies the subspaces of a high-dimensional data space, okay? And it is now better clustering than original space using the F-priority principle. Because when you find the main idea here is like, if you have a cluster in the original space, there must be a cluster within the subspace first. So that's the main idea. So you find the subspace and then you put all the subspace clusters together and then create the final cluster, okay? So it starts with the one-dimensional space, okay? And then you actually discretize the numerical intervals in each axis into grids.  Play video starting at :3:32 and follow transcript3:32 And then you find the dense regions, find the clusters in each subspace, okay? And then you generate the minimum descriptions, okay? And then you use these dense regions to find the the candidates in the 2D space based on the F-priority principle. Because if the region on the 2D space contains a non-dense region in the 1D space, then we will not treat that as a candidate. Then at the 2D space, we also compute the statistical information, the density factors, and then we get a 2D dense region. Then we go up to 3D, and then by combining the 2Ds into 3D, so we further go up, okay? So this is the main idea. It's using grids and use the dense parameters and doing the subspace clustering. Look at this example here, right?  Play video starting at :4:53 and follow transcript4:53 Now you have three dimensions, okay? You have center ring, you have edge, and you have a vacation, right? So what do you do is you put in each subspace as a grid, and then you find give a dense parameter, so you find the dense point, so you do like this, this purple color, then kind of color on this side. So we started with one dimension, okay? In the center range, we form the cluster. So we will say, hey, this 2 to 3, 3 to 4, 4 to 5k, those are the area is dense enough, okay? And then the other, the age area is 30 to 50 dense, okay? Then those two are grouped together, and then form an area, is a big area, okay? And this area is from 30 to 50, and then 2 to 5. So this is a rectangle area.  Play video starting at :6:22 and follow transcript6:22 Then we compute this density on this area, and then we, because this cell, this first cell, because of the grid, right? In this rectangle area, the top grid is contained 3, 3, so assume like the threshold is 2 for each rectangle, each grid. Then this one is only one, so they are not in there. So our new grid, a new two-dimensional dense region becomes this, okay? And then on the other hand, you have a center on the location, and you also, based on the one dimension, you have a 2 to 5, this area, and then the location is a 2 to 4, and then you form a rectangle, and then in the two-dimensional phase, you do, you find that each cell has a 4, 4, a 6, a 6, 4, and then the other two is a 1, so these two will not be included, right? And then you go to the three-dimension. Three-dimension is a combination of this area and this. So the visualization here is not that great, it's not that easy to identify, but at least we know that on this cube, okay, the cube that contains this and this one point grid, or this one point grid should not belong to that candidate, okay? Directly because we use a priori algorithm, a priori principle. So you form a shape of 3D shape, okay? And then you compute that proportion, okay, that within this 3D shape, how many number of points based on the fraction of points that be dense, and then you may form a shape of 3D. So this is how a clique algorithm works, okay?  Play video starting at :9: and follow transcript9:00 So the major steps for a clique algorithm is like identifying subspaces that contain clusters. So you partition the data space, finding the number of 0.9 inside each cell of the partition, then identify the subspace regions that contain clusters using the a priori principle, okay? And then you determine dense units in all subspaces of interest, okay? And then you find the connected dense units in all subspace interests. So that way you get the clusters now. And then you determine the maximum regions that cover a cluster of connected dense units for each cluster. And you also determine the minimum cover of each cluster. That way you generate the minimum discretion of the cluster, okay? And then you go to a higher and higher space, okay?  Play video starting at :10:17 and follow transcript10:17 So the pros and cons of clique algorithm is you automatically find the subspace of the highest dimensionality as long as high density clusters exist in those subspaces, okay? And then it is insensitive to the order of records in input, okay? And you don't presume some canonical data distribution because you find the data distribution by the small cell. And it's scaling anyways the size of the input. It has good scalability as the number of dimensions in the data increases. But the problem, because you have the grade, okay? So the grade, the quality of the result is critically dependent on the approximate, appropriate choice of the number and the ways of the partitions. The number and the ways of the partitions and the grade cells.  Play video starting at :11:32 and follow transcript11:32 So basically you may end up having a non-natural grade, a non-natural cluster because of your your grades, size and your partition, okay? So this is the clique algorithm. And so we have done the density-based and the grade-based approach, and the combination of density-based and the grade-based approach.  KeyCurve is another grid-based approach. Basically what it does is try to address the high dimensionality curves with the same algorithm that it uses a density-based approach and tries to do a subspace clustering with the grid. So what it does is it discretizes the data space through a grid and estimates the density by counting the number of points in the grid. And then it is also density-based in which a cluster is the maximum set of connected dense units in a subspace. So a unit is dense if the fraction of the total data point content in the unit exceeds the input model parameter. So you can set a parameter and then given the data point, given this unit, by proportion, the number of data points should be proportional to the area that in the total space. Then if it is dense, then the number of points within that space, how many per square meter or how much within the radius, right? So because you have a cell, that cell probably is a centimeter, square centimeter, or square millimeter. So if you have that fraction of the total data point contained in that unit exceed the input model parameter, then we call it a dense unit. So what you can do is you find the connected dense unit that you form, put them together as a cluster, okay? So the subspace cluster is the set of neighboring dense cells in an arbitrary subspace, okay? And by doing that, it automatically identifies the subspaces of a high-dimensional data space, okay? And it is now better clustering than original space using the F-priority principle. Because when you find the main idea here is like, if you have a cluster in the original space, there must be a cluster within the subspace first. So that's the main idea. So you find the subspace and then you put all the subspace clusters together and then create the final cluster, okay? So it starts with the one-dimensional space, okay? And then you actually discretize the numerical intervals in each axis into grids.  Play video starting at :3:32 and follow transcript3:32 And then you find the dense regions, find the clusters in each subspace, okay? And then you generate the minimum descriptions, okay? And then you use these dense regions to find the the candidates in the 2D space based on the F-priority principle. Because if the region on the 2D space contains a non-dense region in the 1D space, then we will not treat that as a candidate. Then at the 2D space, we also compute the statistical information, the density factors, and then we get a 2D dense region. Then we go up to 3D, and then by combining the 2Ds into 3D, so we further go up, okay? So this is the main idea. It's using grids and use the dense parameters and doing the subspace clustering. Look at this example here, right?  Play video starting at :4:53 and follow transcript4:53 Now you have three dimensions, okay? You have center ring, you have edge, and you have a vacation, right? So what do you do is you put in each subspace as a grid, and then you find give a dense parameter, so you find the dense point, so you do like this, this purple color, then kind of color on this side. So we started with one dimension, okay? In the center range, we form the cluster. So we will say, hey, this 2 to 3, 3 to 4, 4 to 5k, those are the area is dense enough, okay? And then the other, the age area is 30 to 50 dense, okay? Then those two are grouped together, and then form an area, is a big area, okay? And this area is from 30 to 50, and then 2 to 5. So this is a rectangle area.  Play video starting at :6:22 and follow transcript6:22 Then we compute this density on this area, and then we, because this cell, this first cell, because of the grid, right? In this rectangle area, the top grid is contained 3, 3, so assume like the threshold is 2 for each rectangle, each grid. Then this one is only one, so they are not in there. So our new grid, a new two-dimensional dense region becomes this, okay? And then on the other hand, you have a center on the location, and you also, based on the one dimension, you have a 2 to 5, this area, and then the location is a 2 to 4, and then you form a rectangle, and then in the two-dimensional phase, you do, you find that each cell has a 4, 4, a 6, a 6, 4, and then the other two is a 1, so these two will not be included, right? And then you go to the three-dimension. Three-dimension is a combination of this area and this. So the visualization here is not that great, it's not that easy to identify, but at least we know that on this cube, okay, the cube that contains this and this one point grid, or this one point grid should not belong to that candidate, okay? Directly because we use a priori algorithm, a priori principle. So you form a shape of 3D shape, okay? And then you compute that proportion, okay, that within this 3D shape, how many number of points based on the fraction of points that be dense, and then you may form a shape of 3D. So this is how a clique algorithm works, okay?  Play video starting at :9: and follow transcript9:00 So the major steps for a clique algorithm is like identifying subspaces that contain clusters. So you partition the data space, finding the number of 0.9 inside each cell of the partition, then identify the subspace regions that contain clusters using the a priori principle, okay? And then you determine dense units in all subspaces of interest, okay? And then you find the connected dense units in all subspace interests. So that way you get the clusters now. And then you determine the maximum regions that cover a cluster of connected dense units for each cluster. And you also determine the minimum cover of each cluster. That way you generate the minimum discretion of the cluster, okay? And then you go to a higher and higher space, okay?  Play video starting at :10:17 and follow transcript10:17 So the pros and cons of clique algorithm is you automatically find the subspace of the highest dimensionality as long as high density clusters exist in those subspaces, okay? And then it is insensitive to the order of records in input, okay? And you don't presume some canonical data distribution because you find the data distribution by the small cell. And it's scaling anyways the size of the input. It has good scalability as the number of dimensions in the data increases. But the problem, because you have the grade, okay? So the grade, the quality of the result is critically dependent on the approximate, appropriate choice of the number and the ways of the partitions. The number and the ways of the partitions and the grade cells.  Play video starting at :11:32 and follow transcript11:32 So basically you may end up having a non-natural grade, a non-natural cluster because of your your grades, size and your partition, okay? So this is the clique algorithm. And so we have done the density-based and the grade-based approach, and the combination of density-based and the grade-based approach.  KeyCurve is another grid-based approach. Basically what it does is try to address the high dimensionality curves with the same algorithm that it uses a density-based approach and tries to do a subspace clustering with the grid. So what it does is it discretizes the data space through a grid and estimates the density by counting the number of points in the grid. And then it is also density-based in which a cluster is the maximum set of connected dense units in a subspace. So a unit is dense if the fraction of the total data point content in the unit exceeds the input model parameter. So you can set a parameter and then given the data point, given this unit, by proportion, the number of data points should be proportional to the area that in the total space. Then if it is dense, then the number of points within that space, how many per square meter or how much within the radius, right? So because you have a cell, that cell probably is a centimeter, square centimeter, or square millimeter. So if you have that fraction of the total data point contained in that unit exceed the input model parameter, then we call it a dense unit. So what you can do is you find the connected dense unit that you form, put them together as a cluster, okay? So the subspace cluster is the set of neighboring dense cells in an arbitrary subspace, okay? And by doing that, it automatically identifies the subspaces of a high-dimensional data space, okay? And it is now better clustering than original space using the F-priority principle. Because when you find t: Added to Selection. Press [⌘ + S] to save as a note Like Dislike Report an issue : Added to Selection. Press [⌘ + S] to save as a note Like Dislike Report an issue : Added to Selection. Press [⌘ + S] to save as a note