And what is the probabilistic model-based clustering? Okay. So in the probabilistic model-based clustering, the cluster analysis is to find the hidden categories. Well, assume that there is a hidden category of probabilistic clusters that is a distribution over the data space, which can be mathematically represented using the probability density function or distribution function. For example, the camera, right? You have two camera lines. One is consumer line. 

One is professional line. So they have different density function in terms of their price. Okay. So density function F1 maybe is for consumer line and density function F2 for professional line. Okay. So these two can be obtained by probabilistic clustering algorithm. So if we have a mixture model that assumes that a set of observed objects is a mixture of instances from multiple probabilistic clusters, and then conceptually each observed object is generated independently, then our task is to infer a set of k probabilistic clusters that is mostly likely to generate the data set D using the above data generation process. 

So a set of k probabilistic clusters C1 to Ck with the probability of density function F1 to Fk respectively, and their probabilities W1 and Wk. That's what we have. Okay. Then the probability of an object O generated by cluster Cj is the probability of O under the Cj condition of Cj equal to Wj times the probability function of object Fk. Fj, O. Then probability of O generated by the set of cluster C, the set of cluster C including C1 to Ck equals sum of Wj, Fj, O. So for a model-based clustering, since objects are assumed to be generated independently, right? 

So given the data set D, we have the cluster C of, under the probability of data set is generated by the cluster C, okay, is equal to the object probability generated by this cluster. And then you multiply that. So because we assume the independency, right? So we already know how to calculate the Toi on the C. So that is the sigma of Wj, Fj, Oi, right? So our task is to find a set of C of k probability clusters such that P, D, and C is maximized. However, maximized P, D, or C is often intractable since probability-dense function of a cluster can take an arbitrarily complicated form. 

Okay? So to make it computationally feasible, as a compromise, you can assume the probability-dense function being a sum-parameterized distribution. Then with that parameterized distribution, we need only observe the estimated parameter. Okay? So for example, we can assume the data, okay, generated by univariant Gaussian mixture model, okay, given n observed objects. And then we have k distributions, and each distribution is defined by the parameters of data. Okay? 

Then the probability of J, O, I, you know, probability J, O, I, is generated by theta of J, is the probability that O, I, is generated from J's distribution with the parameter theta. Okay? And then probability of object I as generated by this entire k distribution is sigma of W, J, P, J, object I, theta J. And because we assume the independency, so the probability of entire data set, object, n objects, n observed objects is generated by this theta set of parameters is multiplication of each individual object's probability. So that's what we get. Now we have this theory, okay? Then if we assume the probability density function of each class that follow the one-dimensional Gaussian distribution, then we can use mu J and sigma J, which is a standard derivation and mean value to estimate the probability. 

Okay? So this is the probability estimation function here. Okay? For each object J, I, the probability of object I belong to class J equal to square root of two pi sigma J exponential negative OI minus mu J, two times standard derivation. Okay? Or square. Then, of course, the object A belong to the entire data set equal to all the sigmas added together. 

You see the added together from I, J to K. And then the probability of this entire data set was generated by this theta is the multiplication of each individual object belong to the classes. Okay? So you multiply and then you calculate the individual probability. So that is how you come up with the probability. Now you simplify the computation by using this probability distribution function, Gaussian distribution. Okay?