Now what do we do with this probability computation? Actually, we can use an EM algorithm. The first EM algorithm is actually K-means algorithm. If you look back into the K-means algorithm, it has two steps. The first step is called expectation step. Given the current cluster center, each object is assigned to the cluster whose center is closest to the object. Then the object expected to belong to this closest cluster. 

Then the next step is called the M-step. Given the cluster assignment for each cluster, the algorithm adjusts the center so that the sum of distance from object assigned to this cluster and the new center is minimized. So the object assigned to this cluster and then the new center. To maximize the step, we make sure that the object to the new center is minimized. As a framework, the EM algorithm is approached to maximize the likelihood of maximum posteriori estimates of the parameters in the statistical models. So in the E-step, you assign objects to the clusters according to the current fuzzy clustering or parameters of probabilistic clusters. In the M-step, you find the new clustering or parameters that minimize the sum-of-square error or the expected likelihood. 

So it has two steps, E-step and M-step. Let's use an example to demonstrate how we run the fuzzy clustering using the EM algorithm. So we have those data points A, B, C, D, E, F. And then in the E-step, the first step, initially we assign two initial clusters. One is cluster 1, center is C1 equal to A, and then C2 equal to B. Then the first step is to assign object O to C1 with its member width. That is the fuzzy clustering idea. 

So the member width is based on their distance to the center C1. So use this formula. The member width is distance O C2 square divided by distance O C1 square plus distance O C2 square. So this weight tells how much weight this object should belong to C1. Again, you can compute the same for weight, that the member weight for object O belonging to C2 is distance O C1 divided by distance O C1 square plus distance O C2 square. Now, based on this, we calculate A to C1. Because A to C1, the distance O C1 equal to 0, right? 

A is 1, so A C1 equal to 0. So distance O, distance A C2 divided by distance A C2 equal to 1. We do the same, and then distance A to C2 is 0, because A to C2, because O to C2 is 0. O C1 is 0, O C2 is 0, because the distance they are the same for the B class. So B to C1, A to C1 is 0, B to C2 is 0, because they are the same. And then C to C1, C to C1 is C to C1, C to A, okay? So it's a 9 minus 3 equal to 6, 6 square equal to 36, 6 minus 3 equal to 3, 3 square equal to 9, 36 plus 9 is 45. 

So that's the distance O C1 is 45. And then distance O C2, that's a 9 minus 4 equal to 5, 5 square equal to 25, plus 4, 10 minus 6 is a 4 square. So it's 25 plus 16 is 41, okay? So you have 41 over 45 plus 41 equal to 0.48. And then we do the same, compute the DC, WDC, and then WDC is 14 minus B, 4, okay? That's the 10, 100, and 10 minus 8 is a 4, so 104. And divided by 14 minus 3, that's the 11, 121. 8 minus 3 is 5, 25. 

So 121 plus 25, 146. So you can see, 104 divided by 146 plus 104. So it equals 0.416. So we do this computation, and then we create this fuzzy matrix here, right? This matrix where you have, now this is, the matrix we have two rows is an empty, so that's transformed, okay? So 1, 0, 48, 42, because it's rounded up to two digits. So it's a 42, 41, 47. 

So it's a 1, 0, 48, 42, 41, 47. And then we can calculate another column with a 0, 1, 0.52, 0.58, 0.59, 0.53. Okay, so that is an expectation step. Then we calculate the new center, okay? Center equals to the, for each object, okay? And we use their weight to calculate their contribution to the center, okay? So the center, you take each point O with their weight, and square, and then times the object coordinate, or object attribute. 

And divide it by the total, divide it by the sum of those weights. And then after that, we get a new center called a C1 is 8.47, 5.12. And then you can calculate the C2 is equal to each point with the O, C2, and then basically on this weight, and then times this object, and then divide it by this O, C2. Okay, so you then get another one. C2 is 10.42 and 8.99. So after this, you get to the E step again, and then you do the same again, you calculate this weight. Great, and then you adjust the weight, okay? 

And after you adjust the weight, then you go back to calculate the center. Now, and it goes on iteratively compute this weight until the cluster center converges. Well, after a while, your cluster center will not change, or the change is very small. For example, every time you make a change, the cluster center will just make a little bit of change. In that case, you can stop. So this is the EM algorithm, okay?