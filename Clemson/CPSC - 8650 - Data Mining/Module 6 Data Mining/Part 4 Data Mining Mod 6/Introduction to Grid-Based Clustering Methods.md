What is the grade-based clustering method? Grade-based clustering method tries to explore the multiple resolution grade data structure in clustering. What it does is it tries to partition the data space into a finite number of cells to form a grade structure. So you find the cell, then you take each cell to find the dense cell. When you find the cluster with a dense region, well, based on each grade structure, form this cell. So it tries to connect those dense cells to form the dense region. So this algorithm, first it is efficient because you are now looking at each cell as the unit. 

Okay, each cell as the clustering unit. Usually the number of cells are much less than the number of data points. However, because you form the cell, your cluster may have those cell structures there. You cannot identify the cluster in a natural shape or arbitrary shape. Because if you have a very highly irregular data distribution, then the cell cannot form that shape. Especially if your cell is large, then you always have a rectangular shape on the edge. And so, the non-quality is also limited by the predefined cell size or the borders, and also the density threshold. 

Because the grade-based clustering method also uses the density as the parameter. So you use to connect those dense cells, the dense cells, and then the threshold is how do you define those dense cells. And then also, because you form the grade, right? And then if you have a high-dimensional space, then how do you get those high-dimensional cells? The cells are in a high dimension, it's very hard to form. So how to cluster high-dimensional data? If you have one-dimensional or two-dimensional data, now in such case, probably what you can do is you do a subspace clustering first, okay? 

Okay, there are several methods. And the first method is called a string, and another method is called a clicker. Clicker is actually a grade-based and also a density-based. And we will discuss later on. Now we first look at the STAIN algorithm called a Statistic Information Grade Approach. 

What is the grade-based clustering method? Grade-based clustering method tries to explore the multiple resolution grade data structure in clustering. What it does is it tries to partition the data space into a finite number of cells to form a grade structure. So you find the cell, then you take each cell to find the dense cell. When you find the cluster with a dense region, well, based on each grade structure, form this cell. So it tries to connect those dense cells to form the dense region. So this algorithm, first it is efficient because you are now looking at each cell as the unit.  Play video starting at :1:19 and follow transcript1:19 Okay, each cell as the clustering unit. Usually the number of cells are much less than the number of data points. However, because you form the cell, your cluster may have those cell structures there. You cannot identify the cluster in a natural shape or arbitrary shape. Because if you have a very highly irregular data distribution, then the cell cannot form that shape. Especially if your cell is large, then you always have a rectangular shape on the edge. And so, the non-quality is also limited by the predefined cell size or the borders, and also the density threshold.  Play video starting at :2:48 and follow transcript2:48 Because the grade-based clustering method also uses the density as the parameter. So you use to connect those dense cells, the dense cells, and then the threshold is how do you define those dense cells. And then also, because you form the grade, right? And then if you have a high-dimensional space, then how do you get those high-dimensional cells? The cells are in a high dimension, it's very hard to form. So how to cluster high-dimensional data? If you have one-dimensional or two-dimensional data, now in such case, probably what you can do is you do a subspace clustering first, okay?  Play video starting at :3:50 and follow transcript3:50 Okay, there are several methods. And the first method is called a string, and another method is called a clicker. Clicker is actually a grade-based and also a density-based. And we will discuss later on. Now we first look at the STAIN algorithm called a Statistic Information Grade Approach.  What is the grade-based clustering method? Grade-based clustering method tries to explore the multiple resolution grade data structure in clustering. What it does is it tries to partition the data space into a finite number of cells to form a grade structure. So you find the cell, then you take each cell to find the dense cell. When you find the cluster with a dense region, well, based on each grade structure, form this cell. So it tries to connect those dense cells to form the dense region. So this algorithm, first it is efficient because you are now looking at each cell as the unit.  Play video starting at :1:19 and follow transcript1:19 Okay, each cell as the clustering unit. Usually the number of cells are much less than the number of data points. However, because you form the cell, your cluster may have those cell structures there. You cannot identify the cluster in a natural shape or arbitrary shape. Because if you have a very highly irregular data distribution, then the cell cannot form that shape. Especially if your cell is large, then you always have a rectangular shape on the edge. And so, the non-quality is also limited by the predefined cell size or the borders, and also the density threshold.  Play video starting at :2:48 and follow transcript2:48 Because the grade-based clustering method also uses the density as the parameter. So you use to connect those dense cells, the dense cells, and then the threshold is how do you define those dense cells. And then also, because you form the grade, right? And then if you have a high-dimensional space, then how do you get those high-dimensional cells? The cells are in a high dimension, it's very hard to form. So how to cluster high-dimensional data? If you have one-dimensional or two-dimensional data, now in such case, probably what you can do is you do a subspace clustering first, okay?  Play video starting at :3:50 and follow transcript3:50 Okay, there are several methods. And the first method is called a string, and another method is called a clicker. Clicker is actually a grade-based and also a density-based. And we will discuss later on. Now we first look at the STAIN algorithm called a Statistic Information Grade Approach.  What is the grade-based clustering method? Grade-based clustering method tries to explore the multiple resolution grade data structure in clustering. What it does is it tries to partition the data space into a finite number of cells to form a grade structure. So you find the cell, then you take each cell to find the dense cell. When you find the cluster with a dense region, well, based on each grade structure, form this cell. So it tries to connect those dense cells to form the dense region. So this algorithm, first it is efficient because you are now looking at each cell as the unit.  Play video starting at :1:19 and follow transcript1:19 Okay, each cell as the clustering unit. Usually the number of cells are much less than the number of data points. However, because you form the cell, your cluster may have those cell structures there. You cannot identify the cluster in a natural shape or arbitrary shape. Because if you have a very highly irregular data distribution, then the cell cannot form that shape. Especially if your cell is large, then you always have a rectangular shape on the edge. And so, the non-quality is also limited by the predefined cell size or the borders, and also the density threshold.  Play video starting at :2:48 and follow transcript2:48 Because the grade-based clustering meth: Added to Selection. Press [⌘ + S] to save as a note Like Dislike Report an issue : Added to Selection. Press [⌘ + S] to save as a note Like Dislike Report an issue : Added to Selection. Press [⌘ + S] to save as a note