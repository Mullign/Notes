Since points belong to a cluster have a low reachability distance to their nearest neighbor, and then what is the reachability distance? Then you can see there are values in the distances. So what do we do? How do we define the reachability distance? Let's get to the reachability distance. Before we define the reachability distance, we'll come back to this figure, okay? And then this visualization results. 

Now, first we define the core distance of an object. A core distance of the object is the smallest value, epsilon, such that the epsilon-neighborhood of P has at least the minimum point. Okay, so you remember that we defined EPS already. That EPS is the requirement for the DBSCAN, okay? However, we also define the minimum point. So however, set an area below the EPS, below the EPS, okay, the radius below the EPS. Look at this figure 10.16 here. 

So given the epsilon, 6 mm, okay, and then give a point P, okay, and then for Q1, the point Q1, okay, okay, and they reach the core distance, okay, the core distance of epsilon and minimum point of P is undefined if, if, okay, core distance P is undefined if the epsilon P neighborhood number of points is less than minimum point. That means P is not a core point at all. If P is not a core point, there is no core distance for P. However, if P is a core point, what does this mean? Suppose P has more than minimum point of number neighborhood around that, okay, then we find, we find an epsilon prime. The value, well, that radius, however, a number of points, okay? And then that number of point is just as, that number of point is just as sufficient object that closes to P. We start epsilon, okay? 

So you started with epsilon, then you lower the threshold to epsilon, say, 1, 2, 3, 4, and then until epsilon prime, and then at that point, and then the number of point around P is still above the minimum point, then that epsilon prime is called a core distance. So P's core distance may not equal to the defined epsilon value, okay? Because P, because your defined epsilon value or given epsilon value is 6 millimeter, but you have minimum number of point or greater than minimum, at least, you know, minimum number of point has a distance less than or equal to 3 millimeter, okay? So that 3 millimeter is called a core distance. Using the core distance, we can further define the reachability distance, okay? So the reachability distance of Q from the core point P is minimum radius value, okay? That makes PQ density reachable from P. So actually reachability distance is where actually you can reach P, okay, reach P using that minimum radius, okay? 

So that's the key point, okay? So of course, the idea is that P must be a core point. If P is not a core point, you cannot have a core distance, okay? However, if P is a core point, okay, and then, of course, P has a core distance, that's epsilon, okay? And also, P is density reachable from Q, then there is a distance between P and Q. So whoever is the maximum, that's the distance of P to Q, okay? We call the reachability distance. 

Reachability distance, the maximum distance that either the core point P or core distance P or the distance from P to Q, okay? So that's it. So if you look at this figure here, like, okay, for Q1, okay, Q1, because P is the core point, because it's less than epsilon prime, then because your core distance is defined this, whoever satisfy that minimum point, right? So for Q1's reachability distance, in this case, is the maximum of this epsilon prime and the distance between Q and P and Q1, and then, of course, it's equal to epsilon. So the minimum reachability distance of P to any of their neighborhood point is epsilon value, prime value. That is their core distance value. However, Q2 is not within their core distance. 

So, but the peak Q2 is density reachable from P, and P is a core point. So then, the reachability distance of Q2 is distance between P and Q2. So that's how you define. So then the cost order of the object can be defined by those reachability distances, okay? For anybody below the core distance, for anybody below the core distances, we will cost first, they are below the core distance, okay? And then anybody that in this has a reachability distance defined can become the part of the clusters, because they are reachable, okay? So that's the idea. 

So with that, the clustering structure is obvious. You are looking for the values, like this. You're looking for the values, okay? Looking for the values. The deeper the value, the denser the clusters, okay? So this algorithm is extension from the DBSCAN based on the density and their distance. The only thing that they did is added a core distance and the reachability distance, and then order the object based on their reachability distance, and then using the reachability distance order to finding the cluster. 

So under compact city, if you have an index, and then it's a log n log n, right? Because n is the number one. But if there's no index, it is n squared, because you have to calculate all the reachability distances, like what we did. The only thing you did is added a core distance and then provide an order. And you store this core distance, reachability distance of this data point, okay? So that's the optics. 

Since points belong to a cluster have a low reachability distance to their nearest neighbor, and then what is the reachability distance? Then you can see there are values in the distances. So what do we do? How do we define the reachability distance? Let's get to the reachability distance. Before we define the reachability distance, we'll come back to this figure, okay? And then this visualization results.  Play video starting at ::47 and follow transcript0:47 Now, first we define the core distance of an object. A core distance of the object is the smallest value, epsilon, such that the epsilon-neighborhood of P has at least the minimum point. Okay, so you remember that we defined EPS already. That EPS is the requirement for the DBSCAN, okay? However, we also define the minimum point. So however, set an area below the EPS, below the EPS, okay, the radius below the EPS. Look at this figure 10.16 here.  Play video starting at :1:45 and follow transcript1:45 So given the epsilon, 6 mm, okay, and then give a point P, okay, and then for Q1, the point Q1, okay, okay, and they reach the core distance, okay, the core distance of epsilon and minimum point of P is undefined if, if, okay, core distance P is undefined if the epsilon P neighborhood number of points is less than minimum point. That means P is not a core point at all. If P is not a core point, there is no core distance for P. However, if P is a core point, what does this mean? Suppose P has more than minimum point of number neighborhood around that, okay, then we find, we find an epsilon prime. The value, well, that radius, however, a number of points, okay? And then that number of point is just as, that number of point is just as sufficient object that closes to P. We start epsilon, okay?  Play video starting at :3:51 and follow transcript3:51 So you started with epsilon, then you lower the threshold to epsilon, say, 1, 2, 3, 4, and then until epsilon prime, and then at that point, and then the number of point around P is still above the minimum point, then that epsilon prime is called a core distance. So P's core distance may not equal to the defined epsilon value, okay? Because P, because your defined epsilon value or given epsilon value is 6 millimeter, but you have minimum number of point or greater than minimum, at least, you know, minimum number of point has a distance less than or equal to 3 millimeter, okay? So that 3 millimeter is called a core distance. Using the core distance, we can further define the reachability distance, okay? So the reachability distance of Q from the core point P is minimum radius value, okay? That makes PQ density reachable from P. So actually reachability distance is where actually you can reach P, okay, reach P using that minimum radius, okay?  Play video starting at :5:49 and follow transcript5:49 So that's the key point, okay? So of course, the idea is that P must be a core point. If P is not a core point, you cannot have a core distance, okay? However, if P is a core point, okay, and then, of course, P has a core distance, that's epsilon, okay? And also, P is density reachable from Q, then there is a distance between P and Q. So whoever is the maximum, that's the distance of P to Q, okay? We call the reachability distance.  Play video starting at :6:40 and follow transcript6:40 Reachability distance, the maximum distance that either the core point P or core distance P or the distance from P to Q, okay? So that's it. So if you look at this figure here, like, okay, for Q1, okay, Q1, because P is the core point, because it's less than epsilon prime, then because your core distance is defined this, whoever satisfy that minimum point, right? So for Q1's reachability distance, in this case, is the maximum of this epsilon prime and the distance between Q and P and Q1, and then, of course, it's equal to epsilon. So the minimum reachability distance of P to any of their neighborhood point is epsilon value, prime value. That is their core distance value. However, Q2 is not within their core distance.  Play video starting at :8:14 and follow transcript8:14 So, but the peak Q2 is density reachable from P, and P is a core point. So then, the reachability distance of Q2 is distance between P and Q2. So that's how you define. So then the cost order of the object can be defined by those reachability distances, okay? For anybody below the core distance, for anybody below the core distances, we will cost first, they are below the core distance, okay? And then anybody that in this has a reachability distance defined can become the part of the clusters, because they are reachable, okay? So that's the idea.  Play video starting at :9:38 and follow transcript9:38 So with that, the clustering structure is obvious. You are looking for the values, like this. You're looking for the values, okay? Looking for the values. The deeper the value, the denser the clusters, okay? So this algorithm is extension from the DBSCAN based on the density and their distance. The only thing that they did is added a core distance and the reachability distance, and then order the object based on their reachability distance, and then using the reachability distance order to finding the cluster.  Play video starting at :10:34 and follow transcript10:34 So under compact city, if you have an index, and then it's a log n log n, right? Because n is the number one. But if there's no index, it is n squared, because you have to calculate all the reachability distances, like what we did. The only thing you did is added a core distance and then provide an order. And you store this core distance, reachability distance of this data point, okay? So that's the optics.  Since points belong to a cluster have a low reachability distance to their nearest neighbor, and then what is the reachability distance? Then you can see there are values in the distances. So what do we do? How do we define the reachability distance? Let's get to the reachability distance. Before we define the reachability distance, we'll come back to this figure, okay? And then this visualization results.  Play video starting at ::47 and follow transcript0:47 Now, first we define the core distance of an object. A core distance of the object is the smallest value, epsilon, such that the epsilon-neighborhood of P has at least the minimum point. Okay, so you remember that we defined EPS already. That EPS is the requirement for the DBSCAN, okay? However, we also define the minimum point. So however, set an area below the EPS, below the EPS, okay, the radius below the EPS. Look at this figure 10.16 here.  Play video starting at :1:45 and follow transcript1:45 So given the epsilon, 6 mm, okay, and then give a point P, okay, and then for Q1, the point Q1, okay, okay, and they reach the core distance, okay, the core distance of epsilon and minimum point of P is undefined if, if, okay, core distance P is undefined if the epsilon P neighborhood number of points is less than minimum point. That means P is not a core point at all. If P is not a core point, there is no core distance for P. However, if P is a core point, what does this mean? Suppose P has more than minimum point of number neighborhood around that, okay, then we find, we find an epsilon prime. The value, well, that radius, however, a number of points, okay? And then that number of point is just as, that number of point is just as sufficient object that closes to P. We start epsilon, okay?  Play video starting at :3:51 and follow transcript3:51 So you started with epsilon, then you lower the threshold to epsilon, say, 1, 2, 3, 4, and then until epsilon prime, and then at that point, and then the number of point around P is still above the minimum point, then that epsilon prime is called a core distance. So P's core dista: Added to Selection. Press [⌘ + S] to save as a note Like Dislike Report an issue : Added to Selection. Press [⌘ + S] to save as a note