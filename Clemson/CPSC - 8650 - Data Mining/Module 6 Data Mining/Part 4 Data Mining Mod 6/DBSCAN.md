Okay, so let's get to the first density-based approach called the DBSCAN. It's called as a density-based spatial clustering algorithm. So it was proposed by Dr. Elster in 1996. There are two parameters in this density-based approach. The first parameter is called the minimum points. The second parameter is called the EPS as epsilon value, okay? 

So a cluster in this algorithm is defined as a maximum set of density-connected points. So what is the density-connected point? So before we get to this density-connected point, the definition of density-connected point, we want to define an epsilon-neighborhood of a point first. Given a data point Q, the epsilon-neighborhood of Q is the point in the database which has a distance nested EPS from this point. So for example, you have a data point P, and the distance between the P and the Q are nested EPS, then P is in the neighborhood, okay? So of course, you need to satisfy two conditions, okay? Now if a neighborhood has a number of minimum points, okay, at least a number of minimum points, or larger than a minimum point, we call P as a core point. 

And then we call P's neighborhood, epsilon-neighborhood, as a dense neighborhood. In the other case, if a point has some of those, has a neighborhood, has some data point, but the number of those data points is less than the minimum point, then we call that as a border point, okay? So let's get to the definition of the density-connected point. Before we make the definition on the density-connected point, we first define a directly density-reachable, okay? A point P is directly density-reachable for a point Q with, regarding the EPS and the minimum point, if P belongs to the epsilon-neighborhood Q. So it is the epsilon-neighborhood for the Q. And also, Q, okay, Q satisfies the core point condition. 

That means the epsilon-neighborhood of Q has a larger number of points, then larger than or equal to minimum points. Okay, for example, on the right top corner figure, we have two points, P and Q. Now, P is directly density-reachable from point Q, because first, P belongs to neighborhood, epsilon-neighborhood Q, okay? Then, secondly, the number of the current knowledge of neighborhood, the number of points within the epsilon-neighborhood Q is greater than the minimum point, which is defined as 5. So that we have 1, 2, 3, 4, 5, 6, 7, 9, 8, 10 points, right? So we have 10 points, it's greater than 5, so actually, neighborhood P is a dense neighborhood, okay? A neighborhood of Q, okay, epsilon-neighborhood of Q is a dense neighborhood. 

So this way, we define a directly density-reachable, okay? So that means, that's the first definition. And then, based on this, we further define the density-reachable. So a point P is density-reachable from a point Q, there exists a chain of density-reachable points, starting from Q, follow the chain, end at the P. So what is that? Given the Q equal to P1, then the next point is PQ, the next point is P3, and then final point is PN, well, PN equal to P. So well, and then Pi plus 1 is directly density-reachable from Pi. 

So in this case, you can see, Q equals to P1, and then there's a P2, and then P equal to P3. So P2 is directly reachable from Q, and then P is directly reachable from P2, okay? So that means P is directly, P is density-reachable, okay, is not the directly density-reachable, because there is no direct connection. So you can see there are several dense neighborhoods connected, overlap each other, and then the dense neighborhood, the P2 has one, two, three, four, five, six points, larger than number of point, far minimum number of point P, and then the P2 has, is another dense neighborhood, has one, two, three, four, five, six, seven, eight, nine, 10, 11, 12, so has 12 points, and then us, again, then another one, and then P has another density neighborhood, so that's called the density-reachable, okay, follow one chain of core point. Then density-connected, now there might be situation that P and the Q are not the density-reachable, that means from P goes to the Q, and there is no direct chain overlap of this point, a core point, however, there is one core point, O, in which from the O can reach P, density-reach P, and from O can reach density-reach Q as well. So in these two cases, then we should put the P and the Q in the same group, and they should be able to form a cluster, because they got, the both can be reached, be connected by O, okay, with the density assumption that the EPS and the minimum point, okay, so a point P is density-connected to a point Q, with regarding this EPS and the minimum point, if there is a point O such that both P and Q are density-reachable from O, with the same EPS and the minimum point, okay. With those definitions, especially the final definition on the density-connected, and or density-reachable and density-connected, so we can define the clusters now, and then our algorithm is to find the density-connected point, the data set, okay, the density-connected data set. 

Okay, so let's get to the first density-based approach called the DBSCAN. It's called as a density-based spatial clustering algorithm. So it was proposed by Dr. Elster in 1996. There are two parameters in this density-based approach. The first parameter is called the minimum points. The second parameter is called the EPS as epsilon value, okay?  Play video starting at ::52 and follow transcript0:52 So a cluster in this algorithm is defined as a maximum set of density-connected points. So what is the density-connected point? So before we get to this density-connected point, the definition of density-connected point, we want to define an epsilon-neighborhood of a point first. Given a data point Q, the epsilon-neighborhood of Q is the point in the database which has a distance nested EPS from this point. So for example, you have a data point P, and the distance between the P and the Q are nested EPS, then P is in the neighborhood, okay? So of course, you need to satisfy two conditions, okay? Now if a neighborhood has a number of minimum points, okay, at least a number of minimum points, or larger than a minimum point, we call P as a core point.  Play video starting at :2:27 and follow transcript2:27 And then we call P's neighborhood, epsilon-neighborhood, as a dense neighborhood. In the other case, if a point has some of those, has a neighborhood, has some data point, but the number of those data points is less than the minimum point, then we call that as a border point, okay? So let's get to the definition of the density-connected point. Before we make the definition on the density-connected point, we first define a directly density-reachable, okay? A point P is directly density-reachable for a point Q with, regarding the EPS and the minimum point, if P belongs to the epsilon-neighborhood Q. So it is the epsilon-neighborhood for the Q. And also, Q, okay, Q satisfies the core point condition.  Play video starting at :3:46 and follow transcript3:46 That means the epsilon-neighborhood of Q has a larger number of points, then larger than or equal to minimum points. Okay, for example, on the right top corner figure, we have two points, P and Q. Now, P is directly density-reachable from point Q, because first, P belongs to neighborhood, epsilon-neighborhood Q, okay? Then, secondly, the number of the current knowledge of neighborhood, the number of points within the epsilon-neighborhood Q is greater than the minimum point, which is defined as 5. So that we have 1, 2, 3, 4, 5, 6, 7, 9, 8, 10 points, right? So we have 10 points, it's greater than 5, so actually, neighborhood P is a dense neighborhood, okay? A neighborhood of Q, okay, epsilon-neighborhood of Q is a dense neighborhood.  Play video starting at :5:17 and follow transcript5:17 So this way, we define a directly density-reachable, okay? So that means, that's the first definition. And then, based on this, we further define the density-reachable. So a point P is density-reachable from a point Q, there exists a chain of density-reachable points, starting from Q, follow the chain, end at the P. So what is that? Given the Q equal to P1, then the next point is PQ, the next point is P3, and then final point is PN, well, PN equal to P. So well, and then Pi plus 1 is directly density-reachable from Pi.  Play video starting at :6:25 and follow transcript6:25 So in this case, you can see, Q equals to P1, and then there's a P2, and then P equal to P3. So P2 is directly reachable from Q, and then P is directly reachable from P2, okay? So that means P is directly, P is density-reachable, okay, is not the directly density-reachable, because there is no direct connection. So you can see there are several dense neighborhoods connected, overlap each other, and then the dense neighborhood, the P2 has one, two, three, four, five, six points, larger than number of point, far minimum number of point P, and then the P2 has, is another dense neighborhood, has one, two, three, four, five, six, seven, eight, nine, 10, 11, 12, so has 12 points, and then us, again, then another one, and then P has another density neighborhood, so that's called the density-reachable, okay, follow one chain of core point. Then density-connected, now there might be situation that P and the Q are not the density-reachable, that means from P goes to the Q, and there is no direct chain overlap of this point, a core point, however, there is one core point, O, in which from the O can reach P, density-reach P, and from O can reach density-reach Q as well. So in these two cases, then we should put the P and the Q in the same group, and they should be able to form a cluster, because they got, the both can be reached, be connected by O, okay, with the density assumption that the EPS and the minimum point, okay, so a point P is density-connected to a point Q, with regarding this EPS and the minimum point, if there is a point O such that both P and Q are density-reachable from O, with the same EPS and the minimum point, okay. With those definitions, especially the final definition on the density-connected, and or density-reachable and density-connected, so we can define the clusters now, and then our algorithm is to find the density-connected point, the data set, okay, the density-connected data set.  Okay, so let's get to the first density-based approach called the DBSCAN. It's called as a density-based spatial clustering algorithm. So it was proposed by Dr. Elster in 1996. There are two parameters in this density-based approach. The first parameter is called the minimum points. The second parameter is called the EPS as epsilon value, okay?  Play video starting at ::52 and follow transcript0:52 So a cluster in this algorithm is defined as a maximum set of density-connected points. So what is the density-connected point? So before we get to this density-connected point, the definition of density-connected point, we want to define an epsilon-neighborhood of a point first. Given a data point Q, the epsilon-neighborhood of Q is the point in the database which has a distance nested EPS from this point. So for example, you have a data point P, and the distance between the P and the Q are nested EPS, then P is in the neighborhood, okay? So of course, you need to satisfy two conditions, okay? Now if a neighborhood has a number of minimum points, okay, at least a number of minimum points, or larger than a minimum point, we call P as a core point.  Play video starting at :2:27 and follow transcript2:27 And then we call P's neighborhood, epsilon-neighborhood, as a dense neighborhood. In the other case, if a point has some of those, has a neighborhood, has some data point, but the number of those data points is less than the minimum point, then we call that as a border point, okay? So let's get to the definition of the density-connected point. Before we make the definition on the density-connected point, we first define a directly density-reachable, okay? A point P is directly density-reachable for a point Q with, regarding the EPS and the minimum point, if P belongs to the epsilon-neighborhood Q. So it is the epsilon-neighborhood for the Q. And also, Q, okay, Q satisfies the core point condition.  Play video starting at :3:46 and follow transcript3:46 That means the epsilon-neighborhood of Q has a larger number of points, then larger than or equal to minimum points. Okay, for example, on the right top corner figure, we have two points, P and Q. Now, P is directly density-reachable from point Q, because first, P belongs to neighborhood, epsilon-neighborhood Q, okay? Then, secondly, the number of the current knowledge of neighborhood, the number of points within the epsilon-neighborhood Q is greater than the minimum point, which is defined as 5. So that we have 1, 2, 3, 4, 5, 6, 7, 9, 8, 10 points, right? So we have 10 points, it's greater than 5, so actually, neighborhood P is a dense neighborhood, okay? A neighborhood of Q, okay, epsilon-neighborhood of Q is a dense neighborhood.  Play video starting at :5:17 and follow transcript5:17 So this way, we define a directly density-reachable, okay? So that means, that's the first definition. And then, based on this, we further define the density-reachable. So a point P is density-reachable from a point Q, there exists a chain of density-reachable points, starting from Q, follow the chain, end at the P. So what is that? Given the Q equal to P1, then the next point is PQ, the next point is P3, and then final point is PN, well, PN equal to P. So well, and then Pi plus 1 is directly density-reachable from Pi.  Play video starting at :6:25 and follow transcript6:25 So in this case, you can see, Q equals to P1, and then there's a P2, and then P equal to P3. So P2 is directly reachable from Q, and then P is directly reachable from P2, okay? So that means P is directly, P is density-reachable, okay, is not the directly density-reachable, because there is no direct connection. So you can see there are several dense neighborhoods connected, overlap each other, and then the dense neighborhood, the P2 has one, two, three, four, five, six points, larger than number of point, far minimum number of point P, and then the P2 has, is another dense neighborhood, has one, two, three, four, five, six, seven, eight, nine, 10, 11, 12, so has 12 points, and then us, again, then another one, and then P has another density neighborhood, so that's called the density-reachable, okay, follow one chain of core point. Then density-connected, now there might be situation that P and the Q are not the density-reachable, that means from P goes to the Q, and there is no direct chain overlap of this point, a core point, however, there is one core point, O, in which from the O can reach P, density-reach P, and from O can reach density-reach Q as well. So in these two cases, then we should put the P and the Q in the same group, and they should be able to form a cluster, because they got, the both can be reached, be connected by O, okay, with the density assumption that the EPS and the minimum point, okay, so a point P is density-connected to a point Q, with regarding this EPS and the minimum point, if there is a point O such that both P and Q are density-reachable from O, with the same EPS and the minimum point, okay. With those definitions, especially the final definition on the density-connected, and or density-reachable and density-connected, so we can define the clusters now, and then our algorithm is to find the density-connected point, the data set, okay, the density-connected data set.  Okay, so let's get to the first density-based approach called the DBSCAN. It's called as a density-based spatial clustering algorithm. So it was proposed by Dr. Elster in 1996. There are two parameters in this density-based approach. The first parameter is called the minimum points. The second parameter is called the EPS as epsilon value, okay?  Play video starting at ::52 and follow transcript0:52 So a cluster in this algorithm is defined as a maximum set of density-connected points. So what is the density-connected point? So before we get to this density-connected point, the definition of density-connected point, we want to define an epsilon-neighborhood of a point first. Given a data point Q, the epsilon-neighborhood of Q is the point in the database which has a distance nested EPS from this point. So for example, you have a data point P, and the distance between the P and the Q are nested EPS, then P is in the neighborhood, okay? So of course, you need to satisfy two conditions, okay? Now if a neighborhood has a number of minimum points, okay, at least a number of minimum points, or larger than a minimum point, we call P as a core point.  Play video starting at :2:27 and follow transcript2:27 And then we call P's neighborhood, epsilon-neighborhood, as a dense neighborhood. In the other case, if a point has some of those, has a neighborhood, has some data point, but the number of those data points is less than the minimum point, then we call that as a border point, okay? So let's get to the definition of the dens: Added to Selection. Press [⌘ + S] to save as a note Like Dislike Report an issue : Added to Selection. Press [⌘ + S] to save as a note Like Dislike Report an issue : Added to Selection. Press [⌘ + S] to save as a note