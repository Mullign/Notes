Those are the ideal cases. In the real world, the data is noisy. So we're trying to find approximate biclusters. And there are two methods to find those biclusters. The first one is called optimization-based method. The second one is called enumeration-based method. Now the optimization-based method is trying to find a submatrix at a time that achieves the best significance as a bicluster. So, of course, if you want to do it, find the best significance as a bicluster of submatrix to achieve the best significance as a bicluster, you need to check all the different submatrix, right? So the cost of computation will be very high. Therefore, we use a greedy search to find the local optimal biclusters. So a typical algorithm is called the Delta Cluster Algorithm, published in ISMB 2000, okay, by Chen and Church. Another set of methods is called the enumeration method. So what it does is it uses a tolerance threshold to specify the degree of noise allowed in the biclusters to be mined. Then try to enumerate all submatrixes as biclusters that satisfy the requirement. 

Now, because you set a threshold, and then it is like you can use the a priori property to achieve enumeration without doing exhaustive search. And this is a typical algorithm called the Delta P Cluster Algorithm. And then later on, there's a MAPRE algorithm. And those algorithms were published in 2002 and 2003 in different conferences, okay? Alright, so let's get to each one of those, okay? And look at this microarray data analysis, okay? So you have three genes, object one, object two, object three, okay? Like in the left figure, okay? And it is really hard to find the pattern on those three genes, okay? In those different dimensions, you have multiple dimensions, okay? However, on the right two, you can find this pattern. It's up and down, okay? So of course, like there's no globally defined similarity or distance measures for finding the bicluster, okay? And then clusters may not be excluded, say, because an object can appear in multiple clusters because you're doing the bicluster, okay?