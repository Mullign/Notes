Our algorithm is to find the density connected point, the data set, okay? The density connected data set, the form a maxima set of data point density connected. They form the cluster, right? So the algorithm becomes very simple. You started with the database, you select one point, right? And then you retrieve all the point that density reachable from P with that EPS, okay? Now from P, the density reachable, from P, density reachable. 

So you find all the point. Then you test if P is a core point or not. If P is a core point, right? So you go from density reachable one point, then go to another, all the other density reachable point. So basically follow a core neighborhood chain, core neighborhood chain, all the way to both side, then those, all the data point on this chain form a cluster. Then we get a cluster now, right? That's it. 

And then any point within this cluster are density connected. Okay, that's it. However, if P is not a core point, say for example, P is a border point, right? It form a epsilon neighborhood, but the size of epsilon neighborhood is less than minimum point. So this is not a dense area or not a dense neighborhood in such case. Then we just jump to another point, which has not been examined yet, starting from another point, right? So if we continue this process until all the data points were examined, then we eventually form all these different clusters with regard to this EPS and the minimum point. 

Okay, so if we look at this algorithm, the complexity, computational complexity is n times n, because from each point, you need to compute all the distance, those other points, or follow a chain of those distance of the other point to compute and to find the density reachable point, right? And then you need to repeat n times, because in the first time, you may not find the, you know, if the first time the border point, and then you cannot reach it, and then you need to do another one. So you need to repeat n times. So each search is n, so n times n, that's the big O n squared. Now, well, sometimes, because we are discovering the process within a special, with the special feature, or just like a inner space, right? So if you can create the special index, and then the data point is indexed with a special index, then you can, your search of density reachable point become not exhaustive search all the endpoints, you just follow that index. So that's the log n complexity. 

So n times log n is a big O n log n is the computational complexity. Okay, so you look at this DBSCAN algorithm, right? And first of all, you need two parameters, EPS and the minimum point. So there is some problem. If you look at this, the results are very sensitive to the settings of parameters. For example, they, if you set the minimum point at the four, okay, and EPS at the point of five, then in the figure eight, you will get the cluster like in this figure 8a has one, two, three, four, five clusters, only if you assume this, the top two clusters are connected, then at least you have four clusters. However, if you reduce the EPS, okay, so you reduce the distance of a neighborhood size, reduce the neighborhood size radius, reduce the neighborhood radius, and you get a smaller cluster, okay, and then you get a larger number of clusters. 

Within the original cluster, for example, in this a round circle cluster or big ball has so many smaller clusters now, okay? And then the same goes with the figure nine. You can see when the EPS is larger at five, you have only two clusters, or maybe have some other smaller clusters in the middle of that area. And then you have two major clusters. However, when you decrease the EPS to 3.5, within each major cluster, you have a larger number of smaller clusters, and then you further reduce the EPS, you get more smaller clusters. So that said, DBSCAN is sensitive to the setting of parameters. So how do we know? 

How do we know? How do we know what parameters are the best? Okay, you know, like in our, by looking at the visualized data, we believe both a, figure 8a and figure 9a has a very good representation of the clusters already. So you don't need to reduce the EPS, right? However, how do you know that?