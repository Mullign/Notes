So the first bicare strain algorithm is called the delta bicare strain. So you want to find the submatrix i and j, okay? So we define the mean of i's row, the mean of i's row as e, i, and j, so and then equal to summation of all the values divided by the size of j, right, for each row, okay? Then the mean of j's column does this, and you add all of them and divide by the number of rows, okay? So then the mean of all the elements is summation of all of them divided by i times j, okay, the same number i and j, okay? Now, and then you can equal to e, i, j, which is the mean, and then divided by size of i, okay? Sum of this mean, sum of all the mean values on each column, and all, and sum of, on each row, and then divided by the number of rows, or the sum of the mean values on each column, and divided by the number of columns, right? 

So this is e, i, j. Then the quality of a submatrix S by cost can be measured by the mean square residue, okay? So we define the mean square residue value as that e, i, j minus e, i, j, j is the mean of each rows, and then the mean of the column, and then plus the mean of everybody, okay? And then take the square, right? Of course, divided by i and j, so that is the mean square residue values, okay, right? So a submatrix i and j times j is a delta by cost if h, i, and j less than or equal to a certain threshold delta, where delta is greater than zero, okay, it is a threshold. When delta equal to zero, and i and j is perfect by cost with coherent values, if delta is set to greater than zero, then you can specify the tolerance of average noise element against a perfect by cost, okay? 

And then you can see the residue of e, i, j equal to e, i, j minus the mean value of column, mean value of row, minus mean value of column, plus the mean value of entire matrix, okay? And then, maximal delta by cluster is a delta by cluster i, j, such that there does not exist another by delta by cluster i prime times j prime which contains this i, j, okay? So of course, finding the maximal delta by cluster is computational costly. So we try to use a heuristic gradient search to obtain the local optimal clusters. In this situation, we have two-phase computation. The first phase is to delete, and then the add addition, okay? So in the delete phase, you start from the entire matrix, right? 

You try to reduce the size of matrix, find the submatrix, okay? So you start from the whole matrix, you iteratively remove the rows and the columns, okay, where the mean square residue of the matrix is over delta. That's the threshold, okay? So in each iteration, for each row or column, compute the mean square residue of that row or the column. Now if that row's residue is greater than the delta, you said, the threshold, or that column is greater than the mean square residue is greater than the value, then you remove that row or the column, okay? That with the largest mean square residue. You're not removing, because you have many of them that get greater than that over, okay? 

But you start to remove the one with the largest mean square residue, okay? Then after you remove, then you do the computation again, you remove the largest, okay? Continue doing this, okay? Now, of course, at the certain time, you cannot find any one of them is, you know, over the delta value, then you finish the deletion phase, okay? Then in addition phase, okay, and you, during the deletion phase, you reach the, you know, small set, dot matrix look, right? And then you get this, you get this set, but like i and j, by cluster, maybe too small. So what do you want to do is to try to add columns or add rows, okay? 

Okay, so you consider that all the rows or columns not involved in the current by-class ij. Basically, those are the ones you already removed or in the removal phase, right? And then you calculate their mean square residue, okay? And then you find out the row or column of the smallest mean square residue, then you add them into the current data value, data by-cluster, okay? Now when you add them in, then you test the entire larger by-cluster to see if it's still below the threshold, right? Then continue doing that. Now you can see that, hey, you will eventually grow to the certain place, you cannot add any more. 

If you add more, you will over the threshold, okay? Now in this case, it finds only one delta by-cluster, right? So the problems depend on the sequence how you remove or add, right? So to get the best by-cluster, you might try to replace the element in the output by-cluster with random number to run several times to see if you get the optimal delta by-cluster, okay?