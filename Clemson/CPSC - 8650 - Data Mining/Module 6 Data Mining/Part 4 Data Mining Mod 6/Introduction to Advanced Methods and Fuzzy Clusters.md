In this video, we are going to study advanced methods for cluster analysis. We will first study probabilistic model-based clustering to deal with fuzzy clusters, and then we will introduce the probabilistic model-based clusters and their expectation maximization algorithm. So what is a fuzzy cluster? So far, every data object is assigned to exactly one cluster. So all these clustering methods will assign the data object to exactly one cluster. However, in some applications, you may need a soft cluster assignment or fuzzy cluster assignment. For example, an e-game could be done to both entertainment and software, right? 

So fuzzy clusters and probabilistic model-based clusters will address such problems. A fuzzy cluster is a fuzzy set with probabilistic values between 0 and 1. For example, in these camera cells, you have an ABCD camera, and so the 50, 130, 1320, 860, 270 units, and the popularity of camera is defined as a fuzzy map from the sales unit to their probability or to a fuzzy probability value, okay? So what it does is, if the unit sold more than 1,000 or at least 1,000, then we call the popularity as 1, okay? Otherwise, the popularity equal to i divided by 1,000. So since camera sold only 50, so the popularity value is 0.5, okay? So that is the fuzzy set for 0.5, 1, 0.86, 0.27. Now, what is the fuzzy cluster? Assume we have two clusters. 

One cluster is C1 includes digital camera and lens. And then another cluster is computer. So we have a user review, okay? For reviewer ID, they have keywords digital camera and lens. So reviewer ID belongs to C1, okay? And then reviewer number two has a digital camera also 100% belongs to class C1, okay? And lens belongs to C1. So we have one, two, three, top three reviewers all belong to cluster one. However, reviewer four has keywords digital camera, lens, and computer. And two sets of those keywords are in class one number C1 and one set of in class C2. So they have the probability of two over three and one over three, okay? So you can see the matrix of this matrix. 

And basically represent the partition of the keywords between different clusters, okay? So fuzzy cluster is trying to generate the k fuzzy clusters represented as a partition matrix M and V-U-H. M is has a V-W-I-J, okay? And for each object, for example, each object, okay? And each each reviewer in this case in the top example here, then under a class C-J, okay? And so you have two clusters C1 and C2, okay? And the W-I-J are between zero and one. 

So you form a fuzzy set. And then also for each object, okay? For each object, they total weight, okay? Total W-I-J, okay? The participation, the partition matrix. And they added each row should equal to one. Each row, okay? For each cluster and go from top to bottom, okay? 

They must guarantee the sum of those weight or sum of those partition matrix values must be greater than zero. That means there is no one column or no one clusters has zero for participation, okay? And also that the summation of that column should be less than N. It means no one column or no one class will take all the participants. So that way you ensure either no empty cluster and also no one cluster is dominated by, okay? Everybody has one participant and then all the others become zero. So that is the those are three properties for fuzzy clustering, okay? And also assume we have a C1 to Ck as the centers of the clusters for each object O-I. The sum of the square error SSE is SSE object O-I equal to the W-I-J-P times distance O-I-C-J square, okay? So now W-I-J-P, the reason we have a P here is given this partition matrix, we want to be able to adjust this P. This is an adjustable parameter, okay? So that's a sum of those distance, particularly on object for each for object O-I to each cluster. So that's the SSE for object O-I to each cluster, okay? So SSE object. And then given a class C-J, the sum of square error of object C-J then equal to all the objects to this C-J. The distance of all the objects to this C-J, okay? So all the objects in the previous formula is the object O-I to all the objects, all the clusters. Now for a cluster C-J, it is all the objects to all the objects to this cluster, okay? Center. And then for a cluster analysis algorithm C, it contains C-1, C-2, C-K clusters. Then the sum will be object and the k. So all the objects to all the clusters. So sum i equal to n, sum j equal to k, W-I-J distance O-I-C-J, okay? So this is the definition of square error, okay? Sum of square error.