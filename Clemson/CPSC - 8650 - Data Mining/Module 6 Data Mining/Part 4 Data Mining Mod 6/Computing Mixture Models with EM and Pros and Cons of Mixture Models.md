So with the EM algorithm, if we have a set of parameters theta with the distribution, we want to find the parameter such that p o theta is maximized, okay? Where theta j is defined by the mu and the sigma, well, standard derivation, okay, of j's invariant Gaussian distribution. So the algorithm with this Gaussian mixture model is we started randomly assign the value to say theta j, okay? Then we conduct the EM steps until the center converges or there is sufficiently small changes on the center, okay? So that is our Gaussian mixture model based EM algorithm. So at each step, the probability is calculated by this, given that theta on the objective belongs to the theta j is the probability of theta j divided by all the k probabilities, okay? That's the probability of all j belongs to each distribution, okay? Then the EM step, you adjusted a parameter theta, mu, sigma, so that they expected an active for the p object, the object set over the theta the theta parameter set is equal to sigma of p theta j over object j under the theta j, and then divided by sigma theta j, and then you do it with all the objects, and that essentially is one over k of probability of each probability of object i on the probability, okay? And then your sigma, that's u, okay? And then that's the mean value, and then you can also calculate the standard derivation as well. So with these two, and then you do the another round of each step, okay? And with the same computation, and then you do the adjustment, okay? 

Okay, so there are advantages and disadvantages of mixed models. The strength of this model is they are more general than partition, ORM, and fuzzy clustering, okay? And also the clusters can be characterized by a small number of parameters, okay? And the result may satisfy the statistic assumption of generative models. However, however, there are some weaknesses, because initially you set the theta parameters as random, right? So it may converge to normal optimal. To overcome this, you might have to run multiple times with different random initialization. 

And also it is computationally expensive if the number of distributions is large, or the data set contains very few of the data points, okay? Because then that will use computation, your computation, okay? And then also you need a large data set to have the reasonable observation observed distribution, because if you don't have large enough data set, then your data set obtains very few observed data points. Then getting that expected mixed model becomes very hard, because you're learning the convergence becomes very hard, because you may go this way, and you may go that way with the different distributions. And also the k is hard, because you don't know how many distributions actually have entered inside the data. So these are the weaknesses, okay? All right, so this is our first, okay? And we will study the next one later, okay?