Then, using a statistical information grid, okay, so the spatial area is divided into rectangular cells at different levels of resolution. Okay, so for example, the first nail, for example, we put, we divided the entire area into four, or this two-dimensional data, right, into four areas. Then, within each four small areas, and then we further divide. Okay, and then a cell at a higher level contains a number of smaller cells of the next, the lower level. The lower level has a higher resolution. Okay, so that's the main idea. Now, so what kind of statistical information? So the statistical information, each cell is calculated, stored beforehand, is used to answer the queries. So, parameters at the higher-level cell can be easily calculated from that of lower-level cell by, you know, add, addition. Say, for example, so the higher-level cell contains the summation of lower-level cell total count, and then the mean error value, the standard derivation, okay, and then the minimum and the maximum. So those can be computed. Okay, and also the distribution of the data within the cell, we can compute. Okay, so in such that we actually store all those information there. Okay, so any query to a region is started at the root. Okay, and because we use this statistical information stored in the different level, and then we naturally build as state index. Okay, so given the query, we start from the root, and then we say if we use, we compute the likelihood that cell is relevant to the query at some confident level, because we already have the statistical information there. We just, we can compute, okay, using this thing. Okay, then we will reach to the child, children node, children node. Okay, and if that children node is likely relevant to the cells to the query, likely relevant to the query, then we will only look at those cells, relevant cells. Okay, then, of course, this cell has their children. Then we look for the children's children to see if they are relevant. Now, if they are relevant, okay, then we include them. Eventually, that we will get all the notice the level of cells which contains, you know, the notice the level of cells that is relevant to your query. Okay, so that is the main idea. Okay, now, because you are based on the probabilistic measure, the likelihood, so you may lost some some accuracy, and especially you have the cell there, right? And so, and of course, the complexity is actually very good, because you are going to the each level, right? 

And then the number of queries you go to recursively going down, so the actual query will reach the to the number of great cells. And that is the number of great cells. So your complexity is big O of number of great cells at the notice the level, which is okay. And then usually, the number of great cells at the notice level is much less than n, because you form the cells now. Okay, so that's the advantage.