So, given the optics, you can find a hierarchically nested cluster structure with different thresholds. The dense clusters are included in the nested density cluster. Of course, the cluster ordering compares information equivalent to the density-based clusters corresponding to a broad range of parameter settings. You can change the cluster settings, identify the different sizes of the clusters by increasing the threshold. And given the minimum point, you just change the epsilon value. So, it is very good for automatic and interactive cluster analysis, because you have a hierarchy, you can move the threshold to find a hierarchically nested cluster structure. So, this is a very good algorithm that is also a very good extension of the Believer's Guide. 

Well, this is an example that with different parameter settings, you increase the threshold and then you get a different cluster and different size, okay, with a different parameter setting and you get a different cluster, okay?