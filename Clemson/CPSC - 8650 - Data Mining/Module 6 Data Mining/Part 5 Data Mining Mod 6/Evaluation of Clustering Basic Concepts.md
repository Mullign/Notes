Okay, we have studied the cluster analysis concept and the different methods to conduct the cluster analysis on the dataset, including the partitioning, hierarchical, density-based and grid-based methods, and some advanced methods. Now, how will those cluster analysis methods perform? We need to evaluate the clustering result. So evaluating the clustering result includes two parts. The first, before we apply a cluster analysis algorithm, we want to assess the feasibility of the clustering analysis on a dataset, because if a dataset itself does not have any inherited group structure, it doesn't matter how well you design the algorithm, you couldn't get clusters, or you couldn't discover the clusters. And secondly, we want to evaluate the quality of the result generated by your clustering method, okay, by your algorithm. And there are several issues on clustering assessment and validation, okay? 

First, that assesses the feasibility of cluster analysis on the dataset, we call it a clustering tendency. So you assess the suitability of clustering, whether the data has any inherited grouping structure. Then, our next task is to determine the number of clusters, because in many of our algorithms, like k-means, k-model, MEDOIDS, we have to predefine the value k, right? So we want to determine for a dataset the right number of clusters that may lead to a good quality clustering, okay? Finally, after we run the cluster analysis algorithm, we need to evaluate the quality of the clustering results.