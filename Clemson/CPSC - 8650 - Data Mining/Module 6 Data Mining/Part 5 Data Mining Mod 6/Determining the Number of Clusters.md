Now, our next question is, how do you determine the number of clusters? Because the appropriate number of clusters control the proper granularity of the cluster analysis. Like in our hierarchical approach where you have many different levels of pot points or thresholds, then you generate different number of clusters. But which one is the best, right? So we need to find a good balance between the compressibility and accuracy in the cluster analysis. So of course, there are two undesirable extremes. The whole data set is one cluster, there is no value of clustering. 

Or you achieve each point as a cluster, there is no data summarization because everybody is divided itself. We want to find a balance in between, right? We want to compress the data set into sample groups and will also maintain the accuracy that maintain the good quality on those clusters. So of course, you go from very extreme, everybody is by itself to everybody in the same group. So you need to find something in between. So there are several ways to determine that. One is called an empirical method. 

Well, given the number of data points, you take as n divided by 2, take a square root. That's the total k clusters, okay? That's the empirical, okay? So it is not optimal, but it is empirical. So each cluster is expected to have about square root of 2n points in this case, okay? Another approach is called the ELBO method. Now think about this, when you have everybody in the same cluster, there is a large variation within the cluster, right? 

So when you increase the cluster, so actually you reduce the sum of within cluster variance of each cluster, because when everybody is the same, there are large variance between those samples. Then you split them, then their variance becomes smaller, right? Well, when you increase the number of clusters, and then you go to a point where you continue to increase, then the average within cluster sum of distance actually cannot improve, cannot reduce too much, okay? From, like when split from 1 to 2, you get a big drop, because within cluster variance becomes small, you can use various sum of squares, right? And then when you split, give a smaller, and then you, because when you split, and then you reduce this variance, then you further split, because their variance is already small, you further split, you may get a much, much smaller reduction, so there must be an ELBO place where your curve become flat, so you choose that point as the number of clusters, okay? So that's ELBO method. And you can also use cross-validation method, okay? 

So you assume like you have the data, you divide them into n parts, okay? Then you use n minus 1 part to obtain a clustering model, okay? And then you still have one part available, that you use that part to test the quality of clustering, okay? For example, each point in the test set find the closest centroid, and use the sum of square distance between all points in the test set and their closest centroids to measure how well the model fit the test set, okay? And then for any k greater than 1, you repeat n times, right, compare the overall quality measure with regarding different k's, then, because every time you change the k, right, so you find the number of clusters that fit the data the best, okay? Basically what you did, you use the given k, you find, and then you use the different partition, and then you use the test. The test is based on how close your testing data fit into the cluster, okay?