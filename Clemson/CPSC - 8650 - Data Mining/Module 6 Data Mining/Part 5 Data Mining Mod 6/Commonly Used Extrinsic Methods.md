Actually, you have three approaches. The first is called the matching-based method. You examine how well the clustering result match the ground truth in partitioned object in the dataset. And then you can also use the information theory-based method. You compare the distribution of clustering results of the ground truth. So you use the information to quantify the comparison. So basically, it's similar to the matching-based approach. 

The matching-based approach, you count the values. Now you're using the information, right? Now, for example, you can use the conditional entropy or you can use to normalize the mutual information. So you use the information theories to quantify the comparison. And then you can also do a pairwise comparison-based method. So you treat each group in the ground truth as a card. Then check the pairwise consistency of object in the clustering result. 

So that way, you have true positive because that's the, you know, if the cluster is getting into the right place or false negative, false positive, true negative, right? So basically, based on that possibility, you can use a Jaccard coefficient. The reason you use a Jaccard coefficient is because we want to avoid the true negative. So this is asymmetric measure. All right, so let's get to each one of these results, this method, okay? First is the matching-based method, okay? The matching-based method compare cluster in the cluster result and the groups in the ground truth, okay? 

So suppose a cluster method partition into m clusters, okay? C1 to Cm. Then the ground truth G partitioned into i groups, okay? G1 to Gi. So we compute the purity of the cluster, okay? That measures the extent that cluster Ci contains the point only from one ground truth partition. So the purity of the cluster Ci equals Ci and intersects with Gj. 

Okay, that's the number of matching objects divided by Ci, okay? Where Ci matches Gj, maximum Ci and Gj. So what does it mean? So Ci may contain the object from different ground truths. You find the one with the maximum number of matches to measure their purity, okay? Let's see this. Like C1 with the ground truth, look at C2, okay? 

C2 may have a part from C1 and a part from C2. So C2 has more content. So C2 matches more with G2, then you use that as the maximum, okay? That's a C2 divided by C1. And then G3, C3 matches more with C3, G3, then you use that. Now it is possible C2 also matches G1 more. In that case, then you use the G1, okay? 

Now look at this. This is, so the total purity of a cluster C, include the C1, C2, C1, Cm, is the Ci over the total n, that's the proportion of Ci with each, the proportion of the object in times the maximum J equal the E to L, you know, whoever has the maximum Ci with GJ, okay? So those are, and then that, and then you take this Ci, you get a maximum Ci under GJ with the n minus one, that's the total purity, okay? So look at this example here, okay? The purity of cluster, okay, one, C1. If you look at C1, given a cluster C1, the C1 matches with G1, okay? So that's the four, okay? 

So the total purity is one over 11 times the matches with the G1, because it matches with G1 more, so that's the four. And C2 matches also with C1, G1, so the total matches two, okay? C3 matches primarily with G2, four, C4 matches with G3, one, so that's purity of cluster C1 is one, it's a very good example, result. On the other hand, the purity of cluster C2 is one over 11 times C1 matches two, okay, matches with G1, two. C2 matches with G1 is three, okay? Now look at it, C1 also matches with G2, matches with G2, okay? But it is also two, so it does not exceed C1, so it's the same. 

So the maximum is either with the G1 or G2, so that's two. And then C2 matches with G1 is three, C2 matches with G2 is two, so you use the three, because you use the maximum of the purity with the ground truth, one set, one ground truth, okay? But C3 matches with G1 is one, C3 matches with G3 is one, so it's one. So you can see the purity of cluster C2 is six over 11. Of course, we want, you know, the pure, the better, okay? And of course, you can use other methods such as maximum matching and F-measure, okay?