Another approach is called Information Normalized Mutual Information. In the previous situation where you used entropy, it cannot detect the issue that C1 split object in G into two clusters. Mutual information may be able to detect that situation. Mutual information given C and G, well, basically, the quantify amount of shared information between the cluster C and ground-truth, it measured the dependency between the observed joint probability Pij of C and G, and the expected joint probability of Pci and PGj under the independent assumption. Of course, when C and G are independent, then you have I(CG) =0. However, the mutual information goes, there is no upper bound because if you use this probability of IJ and over P, PCI, PG, and you can go very large. What you do is given the mutual information, you normalize it. 

To normalize the mutual information is taken the I(CG) and over the entropy of C and entropy of H, and then take a square root. By normalize the mutual information, it bound the value range from 0-1. If you have a value close to zero, close to zero means the clustering are very good. Of course, when you have a value close to one, that means the cluster is not good. Then another approach is called Pairwise Comparison is thematic, you use a Jaccard coefficient. You treat each group in the ground-truth and as a class. They are a class. 

You have N class, for each pair of object OI and OJ in the data, if they are assigned to the same cluster or group, the assignment is regarded as positive, otherwise, negative. There is a total number of pairs and times -1/2. For each OIJ there are two positive, and assigned to the same ground-truth group, they are positive. We call the true positive. Then given the group in the same ground-truth but in the cluster, they send into a different cart, we call the negative, but they are false negative because they should belong to the same group. Then you do the same is in the ground-truth they are not the same, but then you ascend into the same group, we call the false positive. Then for the others, not in the same group, I also not sign into the same group. 

Because this nag number because when you do the pairwise comparison, there is n number of not in the same clusters, but assign not into the same clusters. The true negatives are often dominated. We want to ignore these true negatives. That's why we want to Jaccard coefficient as the TP/LP TP + FN +FP. When the Jaccard=1, it is a perfect clustering. Now you can also based on Pairwise comparison using other measures such as Rand statistic, Fowlkes mallows measures.