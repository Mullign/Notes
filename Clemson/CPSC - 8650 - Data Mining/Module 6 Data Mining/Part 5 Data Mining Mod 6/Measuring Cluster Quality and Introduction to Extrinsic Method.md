And now we discuss how to evaluate the clustering quality. Now, we know how to measure the clustering tendency. And then we also get the method to determine the best case. Now, how do we evaluate the clustering result? There are two kinds of methods. The first is called intrinsic, extrinsic, and then there's another one called intrinsic. Depends on whether there is ground truth. 

So if you have ground truth, and the ground truth should be built by a human expert, or at least you know exists the clusters. So extrinsic is basically a supervised method. So you will employ criteria not inherent to the data set itself. So you compare the clustering against the prior or expert specific knowledge, the ground truth, using certain clustering quality measure. Intrinsic is an unsupervised approach. You use the criteria derived from data itself. So you evaluate the goodness of a clustering by considering how well the clusters are separated and how compact the clusters are, such as St. Luke's coefficient. 

So if the clusters are compact, and then different clusters are far away from each other. So those are two typical methods. So let's study the extrinsic methods first. Given the ground truth CG, and the quality of C compared with CG is the quality measure for clustering C. QC, CG. So QC, CG is good if it satisfies the following four essential criteria. Cluster homogeneity. 

So the purer, the better. So we need to define the purity of the cluster. And then the cluster completeness. So if you assign the object benignly to the same category in the ground truth to the same cluster, it means in a ground truth, you need to assign them to the same cluster. And a rag bag better than aliens. So you put a heterogeneous object into pure cluster should be penalized more than put it into a rag bag, put it into others. So if you have a ground truth, then you put some object into a pure cluster. 

That cluster is just contained in the same ground truth. You would rather just put those into others, not belong to any ground truth. OK? And also, you want to preserve the small clusters. OK? Because you have small categories, you split into pieces. It is more harmful than splitting the large categories into pieces. 

OK? So look at this right figure here. OK? You can see we have two ground truths. OK? The orange and the blue. OK? 

That's the G1 and the G2. Now, you can see C1 cluster 1 is contained in G1. But C2 has its majority of C2 is in some of them in G1, and then half of them in G2. OK, this figure is not accurate, because it should be a majority of G2, and then small amount of G1. OK? So now, let's look at extrinsic methods. OK? 

And now we discuss how to evaluate the clustering quality. Now, we know how to measure the clustering tendency. And then we also get the method to determine the best case. Now, how do we evaluate the clustering result? There are two kinds of methods. The first is called intrinsic, extrinsic, and then there's another one called intrinsic. Depends on whether there is ground truth.  Play video starting at ::55 and follow transcript0:55 So if you have ground truth, and the ground truth should be built by a human expert, or at least you know exists the clusters. So extrinsic is basically a supervised method. So you will employ criteria not inherent to the data set itself. So you compare the clustering against the prior or expert specific knowledge, the ground truth, using certain clustering quality measure. Intrinsic is an unsupervised approach. You use the criteria derived from data itself. So you evaluate the goodness of a clustering by considering how well the clusters are separated and how compact the clusters are, such as St. Luke's coefficient.  Play video starting at :2:8 and follow transcript2:08 So if the clusters are compact, and then different clusters are far away from each other. So those are two typical methods. So let's study the extrinsic methods first. Given the ground truth CG, and the quality of C compared with CG is the quality measure for clustering C. QC, CG. So QC, CG is good if it satisfies the following four essential criteria. Cluster homogeneity.  Play video starting at :3:1 and follow transcript3:01 So the purer, the better. So we need to define the purity of the cluster. And then the cluster completeness. So if you assign the object benignly to the same category in the ground truth to the same cluster, it means in a ground truth, you need to assign them to the same cluster. And a rag bag better than aliens. So you put a heterogeneous object into pure cluster should be penalized more than put it into a rag bag, put it into others. So if you have a ground truth, then you put some object into a pure cluster.  Play video starting at :3:48 and follow transcript3:48 That cluster is just contained in the same ground truth. You would rather just put those into others, not belong to any ground truth. OK? And also, you want to preserve the small clusters. OK? Because you have small categories, you split into pieces. It is more harmful than splitting the large categories into pieces.  Play video starting at :4:24 and follow transcript4:24 OK? So look at this right figure here. OK? You can see we have two ground truths. OK? The orange and the blue. OK?  Play video starting at :4:38 and follow transcript4:38 That's the G1 and the G2. Now, you can see C1 cluster 1 is contained in G1. But C2 has its majority of C2 is in some of them in G1, and then half of them in G2. OK, this figure is not accurate, because it should be a majority of G2, and then small amount of G1. OK? So now, let's look at extrinsic methods. OK?  And now we discuss how to evaluate the clustering quality. Now, we know how to measure the clustering tendency. And then we also get the method to determine the best case. Now, how do we evaluate the clustering result? There are two kinds of methods. The first is called intrinsic, extrinsic, and then there's another one called intrinsic. Depends on whether there is ground truth.  Play video starting at ::55 and follow transcript0:55 So if you have ground truth, and the ground truth should be built by a human expert, or at least you know exists the clusters. So extrinsic is basically a supervised method. So you will employ criteria not inherent to the data set itself. So you compare the clustering against the prior or expert specific knowledge, the ground truth, using certain clustering quality measure. Intrinsic is an unsupervised approach. You use the criteria derived from data itself. So you evaluate the goodness of a clustering by considering how well the clusters are separated and how compact the clusters are, such as St. Luke's coefficient.  Play video starting at :2:8 and follow transcript2:08 So if the clusters are compact, and then different clusters are far away from each other. So those are two typical methods. So let's study the extrinsic methods first. Given the ground truth CG, and the quality of C compared with CG is the quality : Added to Selection. Press [⌘ + S] to save as a note Like Dislike Report an issue : Added to Selection. Press [⌘ + S] to save as a note