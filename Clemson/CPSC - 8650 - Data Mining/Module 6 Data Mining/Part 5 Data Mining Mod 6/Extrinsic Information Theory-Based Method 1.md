The next approach is using an information theory-based method called conditional entropy. So a clustering can be regarded as a compressed representation of a given set of objects. So basically, a cluster is a compressed representation of a set of objects. So the better the clustering result approach to the grand choice, the less amount of information is needed. So this idea is to use the conditional entropy. So what is the conditional entropy? Now the entropy of a cluster is the ci over the total n log ci over, that's the information contained in that cluster, right? 

We already talked about before the total number of objects, the probability of object in that cluster. So we have m clusters, so entropy of c equals ci over n log ci over n, okay? Then same thing, the grand choice is i over l sum over l gi over n log gi over n, okay? The conditional entropy of g given the cluster ci equals to the ci. So because your conditional, so the conditional entropy of g given the cluster ci under the conditional ci, so you will find the matching of ci under the ci, okay? And with the gi matching. It is like the purity computation, okay? 

You find the matching, but this one you calculate the entropy, okay? So negative of j equal to 1, j equal to 1 to l, sum all the matches, log matching, okay? Eventually you get an entropy, conditional entropy g given the cluster ci. And conditional entropy given the cluster ci is the sum of each entropy, each cluster, and conditional entropy of each cluster, and times the proportion, and you add them together, okay? So basically that's the formula here, right? This is the conditional entropy of grand choice over cluster ci, and then the ci divided by n, the proportion for really the number of ci over the total n, and then you have m clusters, right? So you sum up them together. 

And again, based on this formula, your Hg ci is sum of j equal to l, log n. So you can compute Hg on the condition of clusters, right? Let's look at how we compute, okay? Look at it. The purity of the cluster c1, we already computed in the previous example, well, equal to c1, 1, and then c2 equal to 6 over 11, right? And then the entropy of conditional entropy on the c1, on the grand choice, is now you have c1, with c1 has the four matches, okay? Now you can see four matches, 4 over 4, and then 4 over 11, and then 2 over 2, and 2 over 11, and then three of them is 4 over 11, not 4 over 4, and 1 over 11. 

So the c1 equal to 0. And then that means it's good, because the information needed is 0 for under the condition of c1. So that means c1 is good clustering, okay? And then clustering c2 here, and you have a c1, c1 matches with g1, so it's a 2, okay? And then the c1 has 1, 2, 3, 4, so it's a 2 over 4. And then with the proportion of this total is a 2 over 11. And then plus c2, okay, c1 matches with the g2, okay? 

Matches with the g2 is a 2, and then g over c1 has a 4, so 2 over 4, and then 2 over 11. And then c2 matches with g1 is a 3, and c2 has 1, 2, 3, 4, 5. So 3 over 5, 0, and 3 over 11. And then plus the c2 matches g2, okay, 2 over 5, and then 2 over 11. And then look at the c3, we have 2, 3, c3, 1, okay, that matches 1 over 2, and then 1 over 11. And then c3 matches with g3, 1 over 2, 1 over 11. So the entropy is 0.297, okay? 

Now you can see that even though the entropy-based approach here giving a perfect result of zero, but actually it cannot detect the issue that c1, okay, c1 splits the object in g2 into two groups, okay? Because the first half is c1, and then the second half is c2. Even though they match, but they split, so in reality, c1, c2 should be together, okay? And it has more than, you know, has four clusters, and the grand choose has only three clusters, okay?