So, our first task is clustering tendency, that means whether the data contains inherent grid grouping structure, okay? So to assess the suitability of clustering, that means we want to determine the data if the data has any inherent grouping structure, okay? There's a non-random structure that may lead to meaningful clusters. Now how do we determine the clustering tendency or clusterability? It is a hard task because there are many different definitions of clusters, with different definitions, with different ways of applying the clustering algorithm, and like you can apply a partitioning-based method or hierarchical method. So with the hierarchical method, you have a different level of clusters, and then the density-based and the graph-based method, they were based on the local optimals. So it is hard to determine whether a dataset has a clustering tendency or is clusterable. 

Even given a fixed type, it is still hard to define an approximate null model for the dataset because you want to say this data is clusterable or has the clustering tendency. Now then you need to know what kind of data doesn't have a clustering tendency, right? So we call that a null model. So then if we can make a null hypothesis on the data and then we reject the null hypothesis, then that data is a clustering tendency or it is clusterable, okay? Still, we developed several clusterability assessment methods, such as special histogram. In this method, we will compare the histogram of the data with the data that is generated from random samples, okay? Or we can use a distance distribution. 

You compare the pairwise point distance from the data with those from the random generated samples, okay? That we see the differences. And then you can also use the Hopkins statistic where a sparse sampling test for a spatial randomness, okay? Again, it is still compared with the random generated data sample. So you can see that you always try to compare your current data with different measures on with the random generated samples. So if you see the difference means, hey, you have a clusterability, right? Or you have a clustering tendency. 

Okay, so let's get to the first approach called the spatial histogram approach, right? You basically try to contrast the d-dimensional histogram of input data set D with the histogram generated from random samples, okay? So data set D is clusterable if the distributions of two histograms are different, okay? So that's how you come up, okay? That's based on spatial histogram approach. So for example, this A is input data, and then the data generated from the random sample on this two-dimensional space. Then you draw the histogram, spatial histogram on each one of them. 

You can see the distribution differences, right? For example, you can see the input data set A has a large number of data distributed in from negative two, between the negative two and one, there's a large number of data. And then there's another larger number of data from 1.5 to 2.5. So you can see that histogram frequency over there is high. Now then, on these random samples, it is all over the place, so every bar is very similar, right? So you can see the difference. So in this, based on that, you can tell how there is a clustering tendency. 

And then you can do this on the different dimensions, because we are, you know, you have two dimensions here, so you can get there. So divide each dimension into equal-width bins, count how many 0.9 in each cell, and obtain empirical joint probability mass function. And then you do the same on the random sample data, all right? And then you compute the KL-diversity values, find their differences. So this is called the special histogram approach. You are doing the bin-based, you know, equal-width, okay? And then you create the histogram bars, and then you calculate their difference using the KL-divisions.