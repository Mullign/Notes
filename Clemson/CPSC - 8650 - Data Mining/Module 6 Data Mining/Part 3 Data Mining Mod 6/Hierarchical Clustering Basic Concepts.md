Okay, in this video, we are going to study hierarchical methods for cluster analysis. Hierarchical clustering is trying to generate a clustering hierarchy which can be drawn as a dendrogram. We will learn a little bit later on. So it's not required to specify a k. So you don't have to specify the number of clusters. It finds the number of clusters through the hierarchy during the clustering hierarchy. It is more deterministic. 

So you don't have to go through an iterative refinement to find the optimal. It just goes through based on certain thresholds. So there are two categories of algorithms. One is called agglomerative, where it starts with a singleton cluster and then continues to merge two clusters at a time to build a bottom-up hierarchy of clusters. Or a divisive approach where you start with a huge macro cluster, that entire data set as one group, and then split it continuously into two groups and generate a top-down hierarchy for clusters. So those are two categories. One starts from every single point as a cluster and then group them into, you know, merge two clusters together and build up. 

So this is a demonstration of how agglomerative and divisive algorithms work. And the agglomerative works start with a singleton of clusters, like A, B, C, D, E, and five data points. Then you start the first step. For example, you merge A and B together. And then the second step, you merge the D and the E together. So you're the first step, and you have four clusters. The initial, you have five clusters. And then the second step, after step one, you have four clusters, the A, B, C, D, and E. And then you find D can be merged. 

In the next step, then you have A, B, C, and D. You have three clusters. So your number of clusters are determined by your criteria of merge. And then your next step, you merge clusters C and D together as a CD as one cluster. And then you have two more, two clusters, the A, B, and C, D, E. And after step three, then in step four, you merge A, B, C, D, E. So this is called the agglomerative approach. And another approach is started with the entire dataset as one big, big cluster, and A, B, C, D, E as one cluster. And then you find the point where you can split these clusters. Say, for example, you use certain criteria to split them into two clusters, and one is A, B, another is C, D, E. So that's the step one. 

Then you measure, you take the same criteria to check if we can split A, B, or we can split C, D, E. Then we find, okay, we can split C, D, E in step two and become C and D, E. Okay. And then our next step is to try to split A, B, or split D, E. And then we found that split D, E in the next point, and then you become A, B, C, D, E. And then your next step is step four, split A, B into A and B. Now every single point becomes its own cluster. Then you stop there. 

So you build a hierarchy of the tree, like a hierarchy where you have this cluster. At each step, you get a different number of clusters. Okay. So you can draw this as a dendrogram. Okay. And so it is a tree of cluster. Okay. At a different level, you have a different level of number of clusters. For example, if you take a cutting point from this angle here, going at the very bottom, so if you cut at the very top, everybody is in one cluster. And then you cut at the next level, then you have two clusters, three on the left and six on the right. Then you go down one more level, and you have three clusters. So basically, you draw a horizontal line on this tree to split those trees. So you have three on the left cluster, and then you have four in the middle cluster, and then you have two points in the right cluster. 

Then you further go down and cut another line here. You have three on the left cluster, then the second cluster is the three points, and then one point as one single cluster, and then two points as another cluster. Then you go down another level, you make a line cut here, you have two clusters here, one here, two points as a cluster, and then another one point as a cluster, and then you have another one as a cluster, and then two points as a cluster. Then you finally cut on the bottom and split all the roots, and all the leaves become their own clusters. Okay, so this is called the danger ground.