Now, the weakness of aggro-magic or divisive IRQ cluster method is, first of all, it cannot revisit. Once you split or once you merge, you cannot go back, so you may not reach the global optimum. And secondly, it has a scalability bottleneck, where every merge and split need to examine many possible options. So the time complexity is at least O squared N squared, N is the number of total objects. So in the past years, there are several other hierarchical clustering algorithms proposed to address those weaknesses, such as BIRCH, it uses a CF tree and increments to adjust the quality of sub-clusters, and it uses a curl to represent clusters using a set of well-scattered representative points, or Chamberlain uses a graph partition method on the 10-year neighbor graph of the data. So those are hierarchical clustering algorithms that can help to address those weaknesses. So let's study the first one called the BIRCH. 

BIRCH is called the Balanced Iterative Reducing and Clustering Using Hierarchies. It was developed in 1996, and actually it was one of the best hierarchical clustering algorithms that impacted many new clustering methods and applications. So the major innovation is it integrates hierarchical clustering with other clustering methods. So initially you do the micro-clustering phase, where you create a clustering feature tree, and then you do other clustering methods at the higher level. So for example, you use the hierarchical clustering first at the lower level, and then you do the sake means, right? So it has two phases. In the initial phase, the micro-clustering phase, where you scan the DBE to build the initial CFP tree. 

So once you build the initial CFP tree, you get a compression of the database. And therefore, this database will preserve and inherit the clustering structure of the data. And then later on, you can use the partitioning or any other algorithm to partition those CF trees, deep nodes. So you're not just using all strict hierarchical clustering methods going up. You're using maybe other methods to do it. Therefore, you can build a better algorithm, which maintains the natural cluster unlike in the hierarchical clustering, where you only split at a certain point or based on certain criteria to do the splitting. Here you can do any shape of clustering now. 

So the key point here is to build a CF tree, so-called a clustering feature tree, okay? So what is the cluster feature? A cluster feature of the cluster is a 3D vector summarizing information about the clusters of objects, okay? So it basically tries to compute the first and second moments of the cluster. It means the number, okay, the linear sum and the square sum of the cluster, okay? So the number of data points within the cluster and the linear sum of the endpoint and the square sum of this endpoint. For example, you have five points, right? 

And 3, 4, 2, 6, 4, 6, 4, 7, 4, 5, 4, 7, 3, 8. So you take the zeroth momentum, which is the number of points, is the five points, and then you take the sum of each dimension, the 16 on first dimension and then 30 on the second dimension. Then you take the square sum of this, or total, so basically it's 3, 3 squared plus 2 squared plus 4 squared plus 4 squared plus 3 squared plus 4 squared, and then you add them together is 2, 4, 4, 4, 2, 4, 4, okay? So this CF1 is a new data vector here, and I use that to represent this cluster, okay? So eventually what you do is you only store the cluster features, okay? And you can see those cluster features are addictive, okay? Because CF1 plus CF2, okay? 

If you have two clusters, then you can add them together. That's why your SS are computed as all the squares and then add them together. Because if you have this, all the squares, think about this, and CF1 and CF2, and then the computation becomes much simpler, so CF1 plus CF2 becomes a new larger group, a larger cluster, is just equal to N1 plus N2, and then LS1 plus NLS2. And then SS1 plus SS2, right? That's computation is linear. And with this CF features, the measure of the centroid radius and the diameter can become very simple. The centroid is basically the LS divided by N, right? 

So you have the CF3, CF4, CF, then X0, that's the centroid, is LS divided by N. The radius is Xi minus X0. So essentially, if you take this formula here, you will see that it's equal to SS divided by N minus LS squared divided by N squared. So you can see that computation, you don't need to revisit each data point. You can compute the radius. You can also compute the diameters with the 2N times SS minus 2 LS squared and N times N minus 1. You can see the diameter and the radius, well, you don't have to revisit the database. 

That's the most important part, okay? So you get this measure of each cluster, then based on this, you can compute the cluster distances and you can do the merge, you can do anything, okay? You can use this as the data point to do the k-means algorithm, okay? Once you have the CF features, okay? The cluster features built, okay? And so you can have this.