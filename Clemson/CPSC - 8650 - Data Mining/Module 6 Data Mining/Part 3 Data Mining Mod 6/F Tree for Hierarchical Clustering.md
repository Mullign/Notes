So, the main idea is how you build a CF tree, okay? How you build a CF tree. So, you scan the database to build a CF tree, okay? So, you started from the first data point, okay? And then you insert the data into this tree. Your initial tree priority is nothing. So, you get this first tree point. 

So, and then if you already have the tree there, okay, then you find the point in the input, and then you find the closest leaf entry, okay? So, because each leaf is a CF, okay? It's a CF. So, you find this data point to this leaf entry, okay? So, and then you put it in there. You base the distance criteria. Then you update this entry. 

So, once you update entry, basically you add the sum, add the number, right? Add one number. So, from n become n plus one, and then you add the linear sum, and then you add the square sum. Okay, after you update, then you calculate their damage, okay? So, this entry, now you set a threshold for the maximum diameter. So, if you have the CF tree, and you say, oh, the maximum diameter cannot be greater than 100. So, if you compute this, okay, and then you find, oh, greater than 100, then you split these two CFs, okay? 

So, once you split, then it form a new parent. Now, that new parent equal to the sum of those two. Then you compute the new parent. If that new parent still exceed the maximum diameter, then you split that new parent as well, okay? So, this is what I do, okay? So, how do you have this tree? This CF tree has two parameters, okay? 

First parameter is that each branch, because you build the parent, then your parent has the number of, has more, after you split the child, your parent has more child. So, then, for example, you have n children, right? The n children, and then once you split one child, you become n plus one child. So, for CF tree has two parameters. The first parameter is called the branching factor, the maximum number of child, okay? And then the second one is the maximum diameter on the leaf node, okay? So, maximum branches, maximum diameter, okay? 

And then the entire CF tree is height balanced, where every CF are stored on the leaf node and has the same distance to the root if you traverse from one by one going up, okay? And then the non-leaf nodes will store the sum of their CFs in their child nodes, okay? So, for example, the second node, then you store the sum of their, so from the leaf node, then their immediate parent will store all their child's sum. We already know how to calculate their sum, right? Sum is n plus one, n1 plus n2, and then s1 plus, ls1 plus ls2, and ss1 plus ss2, right? So, you can see the computation is very easy, okay? Your build process is a linear building because you are going to, the only time you need to do actual computation is when you do splitting. 

So, the key is those two parameters. One is the branching factor, the maximum number of child and all, and then the diameter, okay? The sub-cluster stored on the leaf node, how big your micro-cluster is, okay? So, this is, you can see, this is a leaf node, okay? And the leaf node, and you can also form a link among the leaf node, then every time you go to the leaf node, leaf node, you can have how many for the leaf node, and then you store the micro-clusters, and then each micro-cluster has the diameter. Then, there is child root, for example, in this tree, each non-leaf node has seven branches, okay? And then leaf node can store six micro-clusters, okay? 

And then every time you find one, try to insert until the point where either that cluster needs to split, and then after you split, you can still store on the leaf node, you keep in the leaf node, then at the point you cannot store in the leaf node, then you have to split, and then when you do the split, then you go up to split. So, this tree is balanced, okay? Now, after you build this, then your next step for clustering is on those micro-clusters, not the actual data point anymore. Those micro-clusters are represented by the NLS assets, okay?