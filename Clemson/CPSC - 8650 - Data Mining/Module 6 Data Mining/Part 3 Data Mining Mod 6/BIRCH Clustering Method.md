BIRCWH actually is an integration of agglomerative clustering with other clustering methods. So at the NOAA level, you explore the CF features and the BIRCWH tree structures at the NOAA level micro clustering process. And then it preserves the inherited clustering structure of the data because the data point is going to the nearest clustering. And then at the higher level, you can use any other clustering method to work. Now the strength is that it get a very good quality of clustering. It doesn't matter what kind of shape of clustering you can create. And also it is a linear scalable in a large and also because you are linearly increment, right? And also every time you do a sum, everything is just one computation and you generate a larger cluster. And also it is incremental and also can be dynamically handle the dynamic data entry. If you have a stream data, because when you have a new data, you insert into the clustering. 

Now the problem is you have to fix the size of the deep nodes. Okay. So depending on your size of deep nodes, say for example, you have the maximum diameter. So sometimes you may arbitrarily put the data into the unnatural boundary. Okay. For example, a certain data point is put in another spree to another micro cluster. So you need to be careful to adjust those parameters, especially that diameter of the micro cluster. 

And also because you form the small micro cluster, it is a spherical shape. So your cluster tend to be spherical because you are given the radius diameter measure to determine the micro cluster. Okay. So that's the weakness.