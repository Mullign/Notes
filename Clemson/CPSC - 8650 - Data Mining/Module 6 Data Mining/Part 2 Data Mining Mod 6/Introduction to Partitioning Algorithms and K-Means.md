In this video, we are going to study partitioning method for cluster analysis. So, let's get the basic concept. Partitioning method discovers the groups in the data by optimizing a specific objective function and iteratively improving the quality of those partitions. The typical method is called k-partitioning method. It partitions a data set D of an object into a set of k clusters so that objective function is optimized. So, here we have one parameter. We call it k. k is predefined. 

Then we have an objective function. For example, you can take the sum of square distance of those clusters and then try to minimize the sum of square distances. Now the square distance is a typical objective function like a square distance sum of square distance SSE is like you take every data object and measure their center to measure the data point to their center within the clusters and you sum all those distances. So for each group, each cluster, you get a sum of square distance from every other point to the center and then you take all the sum of square distance because you have k clusters. So this is the objective function, okay? So our goal is to minimize these objective functions, okay? So the problem becomes given any k, find a partition of k clusters that optimizes the chosen partition criteria. 

In the previous example here is the sum of squared errors. So if you look at these objective functions, to reach a global optimal position, you need to exhaustively enumerate all the partitions to calculate their sum of square distances. So this will be expensive in terms of computation complexity, okay? So in most cases, people will develop heuristic methods such as grading algorithms like k-means, k-median, k-MEDOIs, k-modes, k-prototypes, etc. So let's get the first algorithm, k-means algorithm. It was developed in early 1960s, okay? So k-means algorithm uses cluster center as the representation of each cluster, okay? 

So the input is a k, the number of clusters, okay? And then the initial centroids are selected, okay? So now those initial centroids were randomly selected actually. Then you assign each data point to those centroids, those initial centroids, okay? So each point has a distance to those k center points, right? And then you measure their distances. So you assign each point to its closest centroid, therefore you form the groups, okay? 

You form k clusters. And then after that, you recalculate the centroids, okay? You recalculate the centroids, that is the mean point of each cluster. So for example, one cluster has m objects, then you take the mean of those m objects, okay? And then you get the new centroids. Of course, for each cluster, you get a new centroid. Then after you get a new centroid, then you remeasure the distance of all the points to those k new centroids, and to see if you can form a new cluster, a k new cluster, okay? 

Now you continue this process, okay? You repeat, continue this process until at the time you cannot find a new k group. That means all the data points will stay in the same cluster, even though after you calculate the new centroid. At that time, the algorithm converges, okay? Now here, you need to measure the distance from the data point to the centroid, okay? And then you can use Euclidean distances, Manhattan distances. Now you can also, if you have a vector, you can, your data point is a vector, you can use a cosine similarity. 

Now for different kind of data, you can use different similarity measures, okay? The key point here is that you need to have a similarity measure or distance measure, okay? And then also, you need to predefine the k, okay?