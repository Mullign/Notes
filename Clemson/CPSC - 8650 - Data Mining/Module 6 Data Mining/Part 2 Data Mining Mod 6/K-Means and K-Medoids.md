K-means algorithm is sensitive to outliners. So an object with an extremely large value may substantially discourage the distribution of the data because we are calculating the mean, that extreme large value dominates the mean value there, right? So in such a situation, you might use K-method, okay, instead of taking mean value, you can use a median, or you can use a data point as the reference point, okay? So you find the most centrally located object as a cluster. Now you can also take a median, as we will discuss later on, okay? So the K-method is, you select the K points. Now those data points are actual data points, okay? 

As the initial representative object, okay? So we call it initial K-method. And then you assign each point to the cluster with the closest method. So each point will measure the distance with those K data points. And then whoever they are closest to them, they will be assigned to that group. So you form K groups, right? And then your next step, because you already have K points as the method, your next step is to randomly select any object, which is not one of those K points, select it as the representative object, and then you measure the distance, okay, again, so you use that object as the new central, you replace one new MEDOID, okay? 

You will replace one new MEDOID, so for example, like a MEDOID M, okay? You swap this OJ, OI with this M, okay? Then you calculate the cost based on the distance measures, based on, you know, after you swap, you calculate the SSE, for example, okay? Then if the cost decreases, so your penalty, so your cost of swapping improves your quality, right? After you swap and your quality improves, and then you use this OI as your new M, then you randomly select another one, and you do the same, okay? So finally, everything, you cannot improve the quality anymore, so you can see here, your initial selected K MEDOID, right? Then you have to calculate the distance of N minus K with this K, so K times N minus K times. 

Then after that, you will select any of those, randomly select any of those N minus K points to compare the distance with all those other N minus K points, okay? Because you select one, and then you select another one, so you have total of N minus K selections, so you calculate the cost, right? So you can see that the complexity is actually big O, K, NK squared, okay? So this is a demonstration of the algorithm. So initially, you select this blue, and this blue as your MEDOID, and then after that, you assign all the other remaining to these groups, you form two groups, and then you select a non-MEDOID point, for example, I select this one, then I compute the total cost of swapping, okay? And then if the total cost is less, then we use that as the new MEDOID, and then compute it, okay? If it is not, then we select another one, for example, you select this one, and you calculate the cost, and then if the cost of swapping decrease, then you move on. 

So after that, you're done, okay? So this is how, of course, eventually, you will get different groups. So this is if your cost drops, okay? So eventually, after you select this, and you swap, and then you finally get these two groups, instead of the initial groups, okay?