And then, all of those need to be able to compute the numerical value of distance, right? And for non-numerical data, such as categorical data, distance may not mean much. Or distance may not satisfy the triangular property. So if you just simply map the categorical value to 1 or 0, you may not be able to generate a high-quality cluster when you have high-dimensional data. For example, your data value has a lot of colors, so you may have only a few ones. Then you convert them using either a cosine distance measure or any other distance measure, you may get a very bad distance representation, okay? So K-means is extension to K-means by replacement of the cluster with the mode, okay? 

So your dissimilarity measure between object X and the center of a cluster Z is represented as how many matches with the Z, okay? So that's a phi Xj with Zj equal to 1 when there is a feature match between the Z center and the Zj. Now the cluster Z, the center, represents the most number of features in this cluster, okay? Now of course, the algorithm is just that you change this distance dissimilarity measure, okay? And the other algorithm doesn't change. So your measure is based on how many, your data to the center point is how many categorical data matches or features matches with the center point, okay? So continue to do this centroid reassignment and then readjustment and then get to a convergence point. 

Now this exact matching may not give you the best cluster result and sometimes some objects may belong to different groups, may, you know, okay, they're partially belong, say, for example, one categorical data may have many different features and then make a half red, half other colors or say, then half blue, then somehow you need to have a fuzzy K-mode method where you calculate the fuzzy cluster membership value for each object to each cluster. So say, okay, this data point 50% belong, 80% belong to that cluster and the 50%, 20% belong to this part, okay? And then there's also, your data may not just the categorical data, okay? And sometimes we combine with a numerical value, say, for example, age and then for age and then their hobby or age, their selected fashion styles. So you may include the numerical data and the categorical data, right? So another method is called the K-prototype method is proposed where your distance measure actually include the value, include the categorical data and the numerical value, okay? So you calculate by using the mixture of those two data, okay? 

So we already discussed how to calculate the dissimilarity of a mixed data type, okay?