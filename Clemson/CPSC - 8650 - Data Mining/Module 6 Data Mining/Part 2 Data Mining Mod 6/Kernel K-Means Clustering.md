And then kernel k-means clustering algorithm is another method that to handle the situation you have a non-convex data shape like this on the right corner, okay? So because your k-means can only detect clusters that are linearly separable, in this situation if you run the k-means, you cannot draw a line to separate these two clusters, okay? Because one cluster is embedded inside another cluster within the center of that cluster and then the other cluster actually forms a circle, okay? So you draw a line, no matter where you draw the line, you always have some blue line in one with the group of red, okay? So as we already learned in classification method, we can project, solving the non-linearly separable data set, we can expand projected data onto the high-dimensional kernel space, right? Like we did in SVM, we use the kernel functions, okay? Then after map the data, projected data onto the high-dimensional kernel space, we run the traditional k-means algorithm, okay? 

So, of course, because you run the projected data into the higher-dimensional kernel space, for example, from the two dimensions you run into five dimensions, of course your computational complexity is higher, okay? And also, you need to compute and store n times n kernel matrix. Generated from the kernel function on the original data, okay? Now another approach is like you do a spectrum clustering. Again, it's another way to transform the data into another dimension, okay? So you can also consider this as a variation of kernel k-means algorithm, okay? So how do we run this? 

And the typical kernel function like in the SVM, we have polynomial kernel, okay? Or Gaussian radian-based kernel, RBF, and the sigmoid kernel, okay? Tangent-based computation. So the idea is you compute a kernel matrix k for any two data points. Now look at this. In this case, you calculate the matrix first of phi xi times phi xj. Now look at this. 

You may not need the data point now. You calculate the k, xi, xj, okay? Then your next step is to calculate the SSE of a cluster, okay? Of a cluster analysis result equal to phi xi minus the ck squared. So that is, okay? And then the centroid is calculated for each phi divided by the, you know, xi to the center. You apply and then you get divided by the distance ck, okay? 

Ck is the distance that you can call the size of ck, okay? So you can see your computation may not need that projection restored as long as you have the k matrix here. Let's use the example to demonstrate, okay? And if you use the Gaussian gradient-based function kernel k, xi, xj equal to e exponential of two sigma squared and then xi minus xj squared, okay? So you have, originally you have five two-dimensional data, 0, 0, 4, 4, minus 4, 4, okay? So this data in the original space is not linearly separable, okay? So we set sigma to 4, then we can compute each one of their kernels, okay? 

So for example, you take the kernel x1, x2 is you apply, so the 0, 0, minus 4, okay? And then there's a square, okay? So you calculate 0, minus 4, 0, minus 4, 2 squares, okay? Then you add them together to 32, so your kernel function is ekx1, x2 is an exponential of negative 32 over 2 times 4, 2 power of 2, equal to e a power of negative 1. And then you do all this calculation for x1 to 1 equal to 1, because x1 minus x1 equal to 0, e negative 0, and it's 1, okay? So you get an e negative 1, and then you compute an e negative 1, e negative, so you calculate all those points, okay? So you map the original two-dimensional data into the kernel space, okay? And then you run the algorithm to do this SSEs, and eventually, now in the original data point, you run the traditional K-means algorithm, you know, the best result looks like this, okay? 

Now you separate, and then red is some, okay, some blue, and outside circle, and some of this red, most of the red in this middle in a group, and then the rest on the other side, of course, these are not two best groups, and then you use the Gaussian kernel, you actually get these two groups, those are on the right Gaussian kernel K-means cluster, okay? So that is a much better quality cluster, okay? All right, so our next step is to discuss the hierarchical method.