So, as we talked about, the computational complexity is O, big O, k, n minus k squared. So instead of p, k, n, it is quite expensive, okay? So this is called partitioning, the first algorithm for partitioning around the medoids, okay, was proposed in 1987, okay? So the representative objects are medoids, not the mean or centroids, okay? So there are algorithms that are proposed to improve the PAM approach, like Clara. Clara was proposed three years later. So the idea is that instead of taking the entire data set to find the medoid, it takes a sample of the data set first, okay? 

Then you run the PAM on the sample. For example, assume your sample size is much smaller, then you have k as square as your sample. Then you use the sample to find the medoid as the starting point, okay? You run against all as the centroid point, and then you measure the distance around the reassignment there, readjustment. So your cost will be k as square, s square plus k times n minus k, because that you calculate all the points and then find the final groups, okay? And then another method was later proposed as Clara's, because the Clara may have, again, because you take the sample, and then it depends on your sample size and also your sample quality, so you may not get the best result. So in Clarins, we'll use a randomized resampling approach. 

Therefore, it will further ensure the quality as well as, ensure the efficiency as well as quality, okay? All right, so, so far we've studied the k-means, k-medoids. And then there's another method called the k-medians, because the k-means, k-medoids, well, you use the distance measure to find the distance measure to the center there, right? And then medium is less sensitive to the outlier than means, okay? So, for example, the median center is the median, is not the mean, because if you have a few top executive take a median, and your majority of employees only 100,000, then that median is a much better representation of your center, okay? So, so k-medians, well, instead of picking the mean value of the object in the class as the reference point, medians are used, okay? For example, use the L1 norm as the distance measure, okay? So the criteria function for the k-median algorithm is become, like, take the sum of each standpoint to the median, okay? That's absolute distance to that median, okay? And then, again, you take the sum of total clusters, so this is like a new SSE, this is a new objective function, okay? 

So the algorithms start selecting the randomly k-point, okay, initial k-medians, then you assign each point to the nearest median, right? And then you form the groups, clusters, then from that point, you calculate which point is at the median, okay? All right, so by doing that, you continue until no reassignment needed, then the algorithm converges, okay? All right, so we studied the means, medoid, and the median, okay?