Okay, in this study, we are going to focus on distance-based methods. We will study the k-means, k-medians, k-MEDOIs, and then we will study the hierarchical algorithms such as aggregative and divisive methods. Then we will get into density-based and grid-based methods. So for the density-based method, we will try to explore the data space at a higher level of granularity, then put together the dense regions into arbitrary shapes. The grid-based, basically what you do is you individualize the regions of data points from a grid-like structure, then you group them into a region of interest. Then the probabilistic and generative models, we will try to model the data from a generative process. So we may assume a specific form of generative model, for example, Gaussian mixture model, and then we model the parameters with the expectation and maximization algorithm. 

So therefore, we group the data based on their maximum likelihood. So the key is to estimate the generative probability of the underlying data point, okay? We also study the high-dimensional clustering. For high-dimensional clustering, we include subspace clustering that finds the clusters on various subclasses. We can do a bottom-up clustering, top-down clustering, and we can do a coordination-based method or seed-cluster method. And then we also use the dimensionality reduction method, okay, where you cluster data on certain smaller dimensions. Basically you can view it as a vertical form of clustering, right? 

Because you find those groups, and then you reduce the dimension. Or you can do a co-clustering where you identify the clusters, columns, and include the rows, such as a bi-cluster, right? And you can do a probabilistic latent semantic indexing, okay? Basically you model the data, text data, to identify the topics, okay? So we call it topic modeling. You do non-active metric factorization. This is like a co-clustering, okay? 

You also can apply the spectrum clustering. So use a spectrum of similarity metrics of data to reduce the dimension, knowledge. And then, therefore, you can cluster it in a lower dimension, basically into fewer dimensions. You don't have to, because you already convert the similarity metrics data into a certain spectrum data, okay?