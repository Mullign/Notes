In this module, we are going to study cluster analysis and the synaptic method. We will first study the basic concept and then discuss different methods to conduct cluster analysis. First, we'll study the partitioning-based method, then hierarchical method. We will study density-based and the grid-based method. We will also introduce some advanced methods. We will also study how to evaluate cluster analysis results. Okay, so what is cluster analysis? When we fly over a city, we can easily identify fields, forests, commercial areas, and residential areas based on their features. 

You don't have to explicitly train anybody. Cluster analysis is trying to identify the group without any training process. Okay, so in this module, we will systematically study cluster analysis methods and help answer the following questions. What are the different proximity measures for effective clustering? Can we cluster a massive number of data points efficiently? Can we find clusters of arbitrary shape? Can we find the clusters at multiple levels of granularity? 

How can we judge the quality of the clusters discovered by our method? So, what is cluster analysis? To answer this question, we need to know what is a cluster. A cluster is a collection of data objects, which are similar or related to each other within the same group. We call this group a cluster. On the other hand, the objects in different groups or different clusters are dissimilar. Okay, so cluster analysis is to try to identify those clusters given a set of data points. 

So, the cluster analysis method should partition these data points into a set of groups. We call it clusters. Okay, so that data points within these clusters are as similar as possible. Cluster analysis is unsupervised learning because there is no predefined cluster. We don't even know how many clusters it will discover given a data set. Unlike the classification, we call it a supervised learning. We already know a set of predefined clusters. And also, we provide the training data, which is pre-enabled with those predefined clusters. 

In data mining, we can use cluster analysis as a standalone tool to get insight into the data distribution. Or, we can use it as a pre-processing tool to generate intermediate results for other algorithms. For example, we generally reduce the data size, or we identify clusters to do the data partition, to categorize data, or hierarchical generation. Therefore, we can input those data into the other algorithms. Okay, and as we already studied in the classification, we can do the data reduction, dimensional reduction, data compression, data binning, data discretization, and we can use cluster analysis. Right? So, what is the value of cluster analysis? Cluster analysis helps you partition massive data into groups based on its features. Okay? And it will help subsequent data mining processes, such as pattern discovery, classification, and outline analysis. 

So, in the data mining specialization, cluster analysis has played a very important role. Okay? So, in this module, we will learn various scalable methods to find clusters for massive data. We will learn how to mine different kinds of clusters effectively. We will also learn how to evaluate the quality of the cluster you find. And also, we will go back to review how cluster analysis will help with classification, outline analysis, or other data mining tasks.