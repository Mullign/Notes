And to cluster different data types, we will first focus on the numerical data, because most earliest clustering algorithms were designed for numerical data. Then we will study the categorical data, how to handle the categorical data. And the categorical data usually are discrete data, there is no natural order. And then we will study how to cluster the text data. Okay, now cluster data, text data are popular in social media, web, and the social network. They have some special features, they are high dimensional, they are sparse, okay, and they have values corresponding to word frequencies. And there are typical methods such as k-means, but you still need to combine with the algorithmic methods to provide topic modeling or co-clustering. 

Or you may do some data reduction because of the high dimension. And then we should be able to handle the multimedia data such as imagery, audio, video. Now you have YouTube, you have Instagram. So for multimedia data, you often have a multi-modal data, because it has audio-video combined with text data, okay. And then they are often contextual-based, because it contains the behavior or contextual attributes, such as the position, timing, and also the event, okay. And sometimes the video and music data may have a particular meaning, a word, because a different video presented a different semantic meaning. And then when we cluster the time series data, you may have sensor data, stock market, and temporal tracking, forecasting. So there is a timing. Data are temporarily dependent, okay. So time provides a contextual attribute, okay, and also your date value is a behavioral attribute. So it is a multi-dimension. For example, you do a correlation-based online analysis, okay. You can cluster the stocks that are related, okay, in the stock market. You can provide a shape-based offline analysis. 

You can provide a cluster ECG based on overall shapes. For the sequence data, such as the web block, so based on the web blocks, you will identify the trend. Biologic sequences, you can find interesting biologic sequences, okay. And the system of command sequences, you can identify certain groups of data sequences, command sequences, which is a tag, okay. Now, for those sequence data, you can combine with the contextual attribute. You need to have good similarity functions different from the other methods, other data types, such as timing distance, added distance, or longest command sequences, okay. So you can also use the different data structures or models. For example, for the sequence clustering, you use a suffix tree, or you can use a model, a generative model, using a hidden marker model, okay. 

For the stream data, they have some important features, such as their real time, and the evolution, and the concept of drifting. And because the stream data comes in, in a stream form, so you can only run the algorithm as a single path, okay. And the stream data can also use the cluster analysis to generate a micro clustering, which is an intermediate representation of the data. With that, you can store them for further analysis, such as doing the cluster analysis, or further pattern discovery, okay. And also, you should be able to cluster graphs or homogeneous networks, okay. So in which you can provide the edges and the data points, nodes, okay. You can use the generative models, or you use the combinatorial algorithms, such as graph cut method, and you do a spectrum method, or you do the non-active matrix vectorization method. So those are used for like a social network, you know, graphs, okay. And then you may also have a heterogeneous network, okay. 

For example, a network consists of multiple types of data nodes and edges. For example, p-period algorithmic data. You should be able to cluster different types of data or links together. For example, you can use the net class. The net class is a very good tool to cluster different types of data nodes. Now you should be able to handle the uncertain data, identify the noise, okay, and all the data values are approximate values, and then they have multiple possible values, okay. Therefore, we may need to incorporate the probabilistic information to improve the quality of clustering. And we should be able to handle the big data, for example, a large amount of web logs, a lot of stream data, and the censoring, like, say, video surveillance data, okay. 

So some specific framework may be used, such as map-reduce, okay, method where using the map-reduce format you can distribute all the data into different computation nodes, okay. You can use the data reduction method, okay. You can also, based on map-reduce framework, well, you compute the data from different machine or different computation nodes, and then you use the reduce function to aggregate the result obtained from the map steps. So those are the ways to handle the big data.