And case-based reasoning is another one where you use a database of solutions to solve the new problem. So you already have the problem solutions for the similar problems to solve the similar problems. Okay. And if you have a new problem, then what do you need to do is find out how similar these new problems with the existing problem. Okay. And then come up with the similarity measure. Okay. 

Because each instances will be represented by the symbolic discretion or for example user function graphs. And then you, within the database, you already have those similar cases, right? And then you retrieve those cases out are similar to the current case. And then you infer the decision based on the similar case. So we are talking about the similar cases, then you need a good similarity metric to measure the similarity between two cases, right? And also you need to know how to index this syntactic similarity measure, okay? Because therefore you can quickly search, otherwise you do it overall, you know, you compare every one of them, all the knowledge base, I mean, the entire knowledge base. 

That's not good, okay? And so if you index it, and then you can quickly identify the similar measures, identify the cases with the similar measures. So if we cannot find out, then we can search the other branches, okay? And then, so indexing is another challenge. So those two, you have near k-nearest neighbor and case-based reasoning. Those are laziness, typical laziness.