Well, we have a Bayesian belief network, so Bayesian belief networks will use, so the assumptions like independent of the value of between the attributes on the naive Bayesian classifier is not always true, okay? This assumption is too simple and too naive, okay? That cannot model the real world situation. So Bayesian networks will all call the belief network or Bayesian probability model or probabilistic directed asymptotic graph model, okay? So it's a probabilistic graphic model where you have the probability of dependency between the attributes, okay? So given a set of random variables and also their conditional dependency using a directed acyclic graph, okay? So for example, the rain is dependent on the season. So some season has a higher chance of rain, right? And other season has a less chance of rain. And then the sprinkler, some season you use the sprinkler and some other season you don't use the sprinkler. Then the grass is wet or not is depend on if you use a sprinkler or not, or if it's rain or not. Then the grass surface is slippery depend on whether the grass is wet. 

So you can see this is some kind of dependency. Now, given this symptom, network can be used to compute the probabilities of presence of various disease. If you have the symptoms that have the dependency, say like a cold, a coffee, a cold, high temperature and then a cough or some other symptoms, right? Okay. So you can see the probabilistic graphic model must be a directed acyclic graph, okay? So look at this. On the left side, this is a directed acyclic graphic model. A and C depend on B, right? And then C depends on A. However, the middle figure where A depends on B, C depends on A, B depends on C. They form a circle. Then we cannot call that as a Bayesian network, okay? So Bayesian belief network allows class conditioning dependency between the subset of the variables, okay? For example, the non-cancer depend on the family history and also depend on the smoker, okay? And then the positive x-rays depend on the non-cancer, okay? So you can see that, hey, those build a link of dependency, right? And the nodes are the variables, okay? So if you can say like ABC, the probability that ABC exists is equal to probability B times probability of A on the condition B, okay? Times probability of C on the condition A and B because they are dependent. You cannot, you know, remove the dependency. 

Therefore, we can build a conditional probability table like the right corner, bottom corner here. So we need two components. One is a directed acyclic graph, a deck. So we call that a network structure. And then two, we need to come up with a set of conditional probability tables, okay? We want to build the table to know the probability. So that's the key to derive this because with the network, with the tables, then we can use the Bayesian theorem to compute the probability, compute the posterior probability, posterior probability, prior probability, and based on the prior probability, okay? 

Well, we have a Bayesian belief network, so Bayesian belief networks will use, so the assumptions like independent of the value of between the attributes on the naive Bayesian classifier is not always true, okay? This assumption is too simple and too naive, okay? That cannot model the real world situation. So Bayesian networks will all call the belief network or Bayesian probability model or probabilistic directed asymptotic graph model, okay? So it's a probabilistic graphic model where you have the probability of dependency between the attributes, okay? So given a set of random variables and also their conditional dependency using a directed acyclic graph, okay? So for example, the rain is dependent on the season. So some season has a higher chance of rain, right? And other season has a less chance of rain. And then the sprinkler, some season you use the sprinkler and some other season you don't use the sprinkler. Then the grass is wet or not is depend on if you use a sprinkler or not, or if it's rain or not. Then the grass surface is slippery depend on whether the grass is wet.  Play video starting at :2:25 and follow transcript2:25 So you can see this is some kind of dependency. Now, given this symptom, network can be used to compute the probabilities of presence of various disease. If you have the symptoms that have the dependency, say like a cold, a coffee, a cold, high temperature and then a cough or some other symptoms, right? Okay. So you can see the probabilistic graphic model must be a directed acyclic graph, okay? So look at this. On the left side, this is a directed acyclic graphic model. A and C depend on B, right? And then C depends on A. However, the middle figure where A depends on B, C depends on A, B depends on C. They form a circle. Then we cannot call that as a Bayesian network, okay? So Bayesian belief network allows class conditioning dependency between the subset of the variables, okay? For example, the non-cancer depend on the family history and also depend on the smoker, okay? And then the positive x-rays depend on the non-cancer, okay? So you can see that, hey, those build a link of dependency, right? And the nodes are the variables, okay? So if you can say like ABC, the probability that ABC exists is equal to probability B times probability of A on the condition B, okay? Times probability of C on the condition A and B because they are dependent. You cannot, you know, remove the dependency.  Play video starting at :5:6 and follow transcript5:06 Therefore, we can build a conditional probability table like the right corner, bottom corner here. So we need two components. One is a directed acyclic graph, a deck. So we call that a network structure. And then two, we need to come up with a set of conditional probability tables, okay? We want to build the table to know the probability. So that's the key to derive this because with the network, with the tables, then we can use the Bayesian theorem to compute the probability, compute the posterior probability, posterior probability, prior probability, and based on the prior probability, okay?  0:06 / 6:20 1.25x Introduction to Bayesian Belief Networks Save note Transcript Notes Downloads Language: English (auto)​Interactive Transcript - Enable basic transcript mode by pressing the escape key You may navigate through the transcript using tab. To save a note for a section of text press ⌘ + S. To expand your selection you may use ⌘ + arrow key. You may contract your selection using shift + ⌘ + arrow key. For screen readers that are incompatible with using arrow keys for shortcuts, you can replace them with the H J K L keys. Some screen readers may require using ⌘ in conjunction with the alt key Play video starting at :: and follow transcript0:00 : Added to Selection. Press [⌘ + S] to save as a note Like Dislike Report an issue : Added to Selection. Press [⌘ + S] to save as a note