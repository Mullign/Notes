So, the first classification method based on the Bayes theory is called the naive Bayesian Bayes classifier, okay? And naive Bayesian classifier, well, based on the theory, right, we want to compute the posterior probability of classify an object to a class C as given the evidence X and the probability for evidence X belongs to C is proportional to P probability of under this class situation, how much the probability X exists, times probability C. So, that's the likelihood and this is the prior probability. So, we want to compute the posterior probability, okay, posterior probability. Now, so, X has different attributes, okay? So, we can compute the under the class because we compute the PX over C, right? That's the likelihood. So, we start under the PX1 over C. That's if we can compute the probability X1 over C, okay? And then we will be able to compute that X2 under the condition of X1 under C, X3 under the condition X2, X3, and then finally times probability for C. So, we will fix one variable attributes and then we will then we check the probability of fix this one variable and then we check the probability of fix one this variable with that class, probability class, and then the probability for X2 and then assume that X1, X2 are fixed and then probability. So, we basically that probability X over C equal to probability X1 over C times probability by X2 over X1, X3, X1, C and then probability X times probability X3 and with the condition of X1, X2 fixed under C and then code continue and then times PC. So, that's how you compute. We assume like you have this X1, X2, X3 and they are created or they are not totally independent in that case, okay? Now, a Naive Bayes assumption is try to simplify this last part where the probability is conditional Bayes, where you have computed probability X2 under the condition of X1 under C. What you do is assume that condition that X1, X2, Xn those attributes are totally independent, totally independent then they are not be in the consideration of as the condition at all. 

So, your probability computation will become PX1 only depend on the C, PX2 only depend on the C, the condition of C and then times PC, okay? So, then the maximum value of this computation will give you the, so given this for each C, so you have many many C, say you have two classes then C1, C2. So, you compute a PC X1X, PC2X, PC3X then C1, C2, C3 whoever as the largest P value probability value and then you will get the prediction based on whoever gave a maximum value, right? So, this is the Naive Bayes assumption and the previous, this top formula here you have the dependency between variables you are not know, now you assume that they have no dependency at all, okay? So, if you simplify it to the make this naive assumption then each feature only conditions to on the classes because not condition to other features, okay? So, you only need to count the probability within a sample on that attribute within that within that class. So, that is very efficient to compute and it turned out it is surprisingly good in terms of accuracy. Okay, let's get to some details, okay? 

First of all, Naive Bayes classification is focused on started with the categorical data, okay? But it can also measure used on the continuous value. In a continuous value features, we may make some assumption on the continuous value distribution, okay? So, for the category data as we know like each feature xk is categorical then g xk equal to v xk p probability for ci each xk vk, okay? Because each probability, okay? So, you just multiply all the probability on that different categorical data because you have those different number of tuples in the series on each value, okay? The vk is the value, okay? And then if it is a continuous, we can make the assumption that, hey, the value follows the Gaussian distribution with the mean and the standard derivation. Then we calculate that probability of p v on the value k is a standard derivation of 2 pi square root of of sigma and the e negative 2 square root of x minus u ci. 

Now, sigma ci is on that the standard derivation under that ci condition and the mu is a mu under that condition. There's a mu value. So, you take the average value on that class and you compute the standard derivation of that on that class and you calculate the probability, okay? Given the x value. So, that's how you handle the continuous value feature. But either way that you will use the same formula there, okay? That the p x vk and time, okay?