A Naive Bayes classifier is very good in terms of performance in comparison with the decision tree or some other neural network classifiers. They are as good. Okay. And also, the training can be incremented. So each training example will change when you have a new training example come in and you just increment or decrease the probability. Okay. So the prior knowledge can be combined with the current observed new data. Once you have the new data, you just change because you predict when the data are all the available data. So you can start with a small data set, you can predict it, and then when you add more data, you can do the prediction as long as you can compute the probability. The weakness is also obvious. There's an assumption on the attribute, the conditional independence, right? So therefore, you may not get the accuracy you expected. So because, for example, when you calculate the p, c, x, and then with x2, then you need the real probability is the p, c, x2 under the condition of x1 and c. And then p, c, p, x3 under the condition x2, x1, x2, and c. Now you simplify into a p, x1, c, p, x2, c, p, x3, c, right? And in reality, attributes may be dependent. 

For example, given the patient, right, their age, their family history, will impact their diagnosis. And the patient's symptoms, for example, fever and cough, often come together, right? So the diseases may be dependent, okay? Say like if you have a diabetes with a high blood pressure or with other heart disease, just, you know, dependent, okay? So the dependencies among these cannot be modeled by the naive patient's characteristics. So how do you deal with those dependencies?