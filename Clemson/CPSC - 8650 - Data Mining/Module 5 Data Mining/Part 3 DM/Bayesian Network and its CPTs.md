If, for example, this is our fire, smoke, fire will result in an arm, tempering the fire will result in an arm, right? So the conditional probability table will show the fire, smoke, both are true, and then their probability is 0.9, then the fire falls, smoke is 2.1, and then you can take another example, for an arm, 2 has fire 2, tempering 2, an arm 2, and then the probability is 5, and then you can see this probability. Then given the probability, you can, for each probability for x, say for an arm, then equal to the probability of their parents, a condition of their parents on this x, okay? Okay, so for example, probability of fire and smoke and an arm and t equals to the probability of x times probability of tempering and times the probability of smoke on the fire condition and times probability of an arm on the fire and tempering, okay? Okay, so the key is to build this structure under the conditional table, then after that, we can compute those probabilities, when we can compute the probabilities, our idea of calculating the patient's network here, that we want to calculate the the posteriori probability, and then we have a prior probability already there, so the likelihood will be calculated based on the conditional of CPB, conditional probability table. Without the conditional probability table, we cannot compute the negative, so that's the key, okay? Now, the key is, like, the eventual result we want to calculate the posteriori probability, okay? Posteriori probability is the goal we want to achieve, and then, of course, we need, first, we need a structure to know what dependency they exist in, and then, secondly, we need to know this conditional probability table. 

So a patient's believing network becomes two parts. The first part is the constructor, the network. Secondly, is to build the CPB, CPTs, right? So, scenario one, if we know the network structure and all variable observables, then we only need to compute the CPT entries. That's the first scenario, okay? That's easy. The second is that we know the network structure, and there are some variables hidden, and then we can use the gradient descent method by searching the solution along the steeply descent of a criteria function, okay? So this actually was used in the neural network, okay? 

So the idea is, like, so you give the weight on different random probability values, and then every iteration, you try to move towards what appears to be the best solution at that moment. Now, there's no backtracking, okay? So then you update, okay? And then you, so weight are updated at each iteration, and so that's based on the local optima, okay? So after the weight updated, you find a local optima, then you get a result, okay? So this is a scenario two. And then there is another scenario, hey, I don't know the structure. And so in such case, and also we have all the variables, in such case, you need to search the model space to reconstruct the network protocol, okay? The network topology. 

And then another scenario is no structure. Variables are all hidden. In such case, you don't know the structure, so you don't know how to do gradient-descending algorithm, because you don't know which direction you follow, okay? So, and then variables are hidden. Unlike if you have the variable, you can search through the model space to find the structure. Well, there is a paper, and there is a tutorial, and it's called Learning with the Bayesian Network, okay? And so this paper is published by MIT first. 

If, for example, this is our fire, smoke, fire will result in an arm, tempering the fire will result in an arm, right? So the conditional probability table will show the fire, smoke, both are true, and then their probability is 0.9, then the fire falls, smoke is 2.1, and then you can take another example, for an arm, 2 has fire 2, tempering 2, an arm 2, and then the probability is 5, and then you can see this probability. Then given the probability, you can, for each probability for x, say for an arm, then equal to the probability of their parents, a condition of their parents on this x, okay? Okay, so for example, probability of fire and smoke and an arm and t equals to the probability of x times probability of tempering and times the probability of smoke on the fire condition and times probability of an arm on the fire and tempering, okay? Okay, so the key is to build this structure under the conditional table, then after that, we can compute those probabilities, when we can compute the probabilities, our idea of calculating the patient's network here, that we want to calculate the the posteriori probability, and then we have a prior probability already there, so the likelihood will be calculated based on the conditional of CPB, conditional probability table. Without the conditional probability table, we cannot compute the negative, so that's the key, okay? Now, the key is, like, the eventual result we want to calculate the posteriori probability, okay? Posteriori probability is the goal we want to achieve, and then, of course, we need, first, we need a structure to know what dependency they exist in, and then, secondly, we need to know this conditional probability table.  Play video starting at :3:21 and follow transcript3:21 So a patient's believing network becomes two parts. The first part is the constructor, the network. Secondly, is to build the CPB, CPTs, right? So, scenario one, if we know the network structure and all variable observables, then we only need to compute the CPT entries. That's the first scenario, okay? That's easy. The second is that we know the network structure, and there are some variables hidden, and then we can use the gradient descent method by searching the solution along the steeply descent of a criteria function, okay? So this actually was used in the neural network, okay?  Play video starting at :4:24 and follow transcript4:24 So the idea is, like, so you give the weight on different random probability values, and then every iteration, you try to move towards what appears to be the best solution at that moment. Now, there's no backtracking, okay? So then you update, okay? And then you, so weight are updated at each iteration, and so that's based on the local optima, okay? So after the weight updated, you find a local optima, then you get a result, okay? So this is a scenario two. And then there is another scenario, hey, I don't know the structure. And so in such case, and also we have all the variables, in such case, you need to search the model space to reconstruct the network protocol, okay? The network topology.  Play video starting at :5:41 and follow transcript5:41 And then another scenario is no structure. Variables are all hidden. In such case, you don't know the structure, so you don't know how to do gradient-descending algorithm, because you don't know which direction you follow, okay? So, and then variables are hidden. Unlike if you have the variable, you can search through the model space to find the structure. Well, there is a paper, and there is a tutorial, and it's called Learning with the Bayesian Network, okay? And so this paper is published by MIT first.  If, for example, this is our fire, smoke, fire will result in an arm, tempering the fire will result in an arm, right? So the conditional probability table will show the fire, smoke, both are true, and then their probability is 0.9, then the fire falls, smoke is 2.1, and then you can take another example, for an arm, 2 has fire 2, tempering 2, an arm 2, and then the probability is 5, and then you can see this probability. Then given the probability, you can, for each probability for x, say for an arm, then equal to the probability of their parents, a condition of their parents on this x, okay? Okay, so for example, probability of fire and smoke and an arm and t equals to the probability of x times probability of tempering and times the probability of smoke on the fire condition and times probability of an arm on the fire and tempering, okay? Okay, so the key is to build this structure under the conditional table, then after that, we can compute those probabilities, when we can compute the probabilities, our idea of calculating the patient's network here, that we want to calculate the the posteriori probability, and then we have a prior probability already there, so the likelihood will be calculated based on the conditional of CPB, conditional probability table. Without the conditional probability table, we cannot compute the negative, so that's the key, okay? Now, the key is, like, the eventual result we want to calculate the posteriori probability, okay? Posteriori probability is the goal we want to achieve, and then, of course, we need, first, we need a structure to know what dependency they exist in, and then, secondly, we need to know this conditional probability table.  Play video starting at :3:21 and follow transcript3:21 So a patient's believing network becomes two parts. The first part is the constructor, the network. Secondly, is to build the CPB, CPTs, right? So, scenario one, if we know the network structure and all variable observables, then we only need to compute the CPT entries. That's the first scenario, okay? That's easy. The second is that we know the network structure, and there are some variables hidden, and then we can use the gradient descent method by searching the solution along the steeply descent of a criteria function, okay? So this actually was used in the neural network, okay?  Play video starting at :4:24 and follow transcript4:24 So the idea is, like, so you give the weight on different random probability values, and then every iteration, you try to move towards what appears to be the best solution at that moment. Now, there's no backtracking, okay? So then you update, okay? And then you, so weight are updated at each iteration, and so that's based on the local optima, okay? So after the weight updated, you find a local optima, then you get a result, okay? So this is a scenario two. And then there is another scenario, hey, I don't know the structure. And so in such case, and also we have all the variables, in such case, you need to search the model space to reconstruct the network protocol, okay? The network topology.  Play video starting at :5:41 and follow transcript5:41 And then another scenario is no structure. Variables are all hidden. In such case, you don't know the structure, so you don't know how to do gradient-descending algorithm, because you don't know which direction you follow, okay? So, and then variables are hidden. Unlike if you have the variable, you can search through the model space to find the structure. Well, there is a paper, and there is a tutorial, and it's called Learning with the Bayesian Network, okay? And so this paper is published by MIT first.  If, for example, this is our fire, smoke, fire will result in an arm, tempering the fire will result in an arm, right? So the conditional probability table will show the fire, smoke, both are true, and then their probability is 0.9, then the fire falls, smoke is 2.1, and then you can take another example, for an arm, 2 has fire 2, tempering 2, an arm 2, and then the probability is 5, and then you can see this probability. Then given the probability, you can, for each probability for x, say for an arm, then equal to the probability of their parents, a condition of their parents on this x, okay? Okay, so for examp: Added to Selection. Press [⌘ + S] to save as a note Like Dislike Report an issue : Added to Selection. Press [⌘ + S] to save as a note Like Dislike Report an issue : Added to Selection. Press [⌘ + S] to save as a note