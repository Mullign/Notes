And let's get to the first approach called the k-nearest neighbor algorithm, okay? So first, the instances, the samples are represented as points in the n-d space, d is number of dimensions, okay? So the nearest neighbors are defined in terms of Euclidean distance, okay? So for every pair of points, they have a distance, okay? And the target function could be discrete, or real value, because the distance value there, okay? For discrete values, because that's the target function, the classification, okay? k-nearest neighbor algorithm returns the most common value among the k training examples, nearest to xq. xq is the query sample. So this is a discrete value, because you have neighbors and with different classes. So you find the one with the most neighbors with the same class, and then that is the most common value, okay? Most common class. So this is called a discrete value. And then, of course, if you have a real value, then you can use the mean value of the neighbor, okay? And a typical example is the Vennari diagram, where the decision step is induced by the one nearest neighbor for a typical set of training examples, okay? 

So each cell is one, only one. Okay, one nearest neighbor. So when it goes, it goes for every new instance, and when it comes in, it falls into the cell where there's a nearest neighbor, and then use that as a decision, okay? So just to say, oh, it comes to the top one, and then the decision was made to the same as the top one, okay? And for the k-nearest neighbor algorithm, you must have, first of all, decide the k. Secondly, you need to find the distance measure, okay? And we use the Euclidean distance. However, using Euclidean distance will have the same weight for all the neighbors in the same group, okay? This k neighbor has the same weight. So you can further refine the algorithm so that the weight of the contribution for each neighbors are proportional to the reverse proportion of the distance. 

The further away from this new instance, they have less weight, okay? So the closer the neighbors are, the greater weight they have, okay? So k-nearest neighbor algorithm is robust to noise data, because you average the k-nearest neighbors. But the distance measure is based on a Euclidean distance of a d-dimension. So if you have attributes that are irrelevant, then it could cause the curse of dimensionality problem, because the decision space is too sparsely, okay? So to overcome this, you can do an access stretch, or you can do attribute selection by eliminating the least relevant attributes, focus on those attributes that are most relevant, okay? And also, the selection of k will determine the quality of your algorithm, okay? For example, if you use a smaller k, then it may cause a high variance, right? But there's no bias, because you are not biased, because you have very small k. 

So like when your k is equal to 3, k is equal to 5, k is equal to 1, 17. So when you have a big k, you have too many points involved in the decision, right? So the variance will be low, but it is highly biased, okay? So there are two differences, okay? You can see the decision is not, so you need to find a way to find the optimal k as the number of neighbors, okay?