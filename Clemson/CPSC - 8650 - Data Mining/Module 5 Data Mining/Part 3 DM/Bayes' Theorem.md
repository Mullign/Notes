In this video, we are going to study our second classification method, Bayes classification method. So the Bayes classification method was based on the Bayes theorem. Okay, so first of all, we have the total probability theorem where for a probability of class B with each attribute, right, and A, they have many variables. Okay, so the total probability B is probability of A i, probability of B under the condition of A i times probability of A i. So this is called the conditional probability, right? And the Bayes theory is like this. Given the hypothesis, the probability given the evidence X, okay, given a data sample evidence X, and then the probabilities that X satisfy to the hypothesis, which X belongs to a class, a certain class, that's the hypothesis, okay? 

So given the evidence of X, the probability of X belongs to class C is a hypothesis. The probability of that hypothesis is called equal to the probability of that hypothesis exists in the entire data set times the, under that hypothesis exists, the probability of evidence happens in the data set. So probability X over H divided by probability of evidence X, okay? So the probability of X over H hypothesis is the likelihood of X being in that hypothesis that we already see. The probability of hypothesis hold in the whole data set is called the prior probability, which means what we knew previous. So then given that X evidence, we want to determine the probability of X hold, right? So this is how we predict evidence, okay? 

So the idea is we want to classify based on those posterior probabilities, okay? So if you have H as class 1, class 2, class 3, so whoever, so if you have a class C1, C2, C3, so you, if you can compute probability C1 over X, C2 over X, C3 over X, and then we will check the largest probability as the prediction, because it has the maximum posterior probability, okay? And then, of course, if you look at this, PX is a constant. If we want to check the PH over X and for different classes, H, different edges, right, different hypotheses, H belongs to C1, H belongs to C1, H belongs to C2, well, X belongs to C1, X belongs to C2, X belongs to C3, and it doesn't matter. PX is a constant in that situation. It's all the same. So we often take, ignore the PX. We only compute the PX over H times PH, so the likelihood times prior probability, okay? So the posterior, posterior probability, okay, posterior probability is proportional to the likelihood times prior probability. So that's the main theories that support the Bayes classification method, okay?