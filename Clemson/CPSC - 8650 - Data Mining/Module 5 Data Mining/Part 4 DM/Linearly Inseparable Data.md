Okay, for the linearly inseparable problem, okay? Linearly inseparable data, then we must try to convert into the linear inseparable data first by increased dimension. And also, it may not be, you may not always be able to separate the data, even if you try to use many, many ways, okay? So in such case, we can soften the boundary, okay? So we use a soft margin, where you allow some data point falling within the boundary, okay? So you use a snack variable size to actually given that value, okay? So you provide the penalized point, penalize the point in the wrong side according to the distance to the margin boundary, okay? 

So anything is greater than 1, that is greater than 1, that'd be okay. But anything less than 1, or between the negative 1 and 1, then we need a penalty, okay? So you gave a value, a constant value c to control the trade-off between the penalty and the margin, okay? So you give a smaller c, right? You give a smaller c, and you allow more mistakes. And then you give a larger c, you allow less mistakes. So this is widely used for soft margin SVM. 

So this is the one way where you cannot generate a linearly separable hyperplane, or you cannot transform into a higher enough dimension, you still cannot do a linearly separable, okay? Then you allow data within the boundary. So we will penalize that, okay? So you can see this, the penalty string c decides how much you penalize. The smallest c, you allow more in the decision boundary, right? You have, and then you have a larger c, you allow less in the decision boundary, okay? So let's see how, so in your SVM algorithm, there is a way you try to move, try to find the optimum c, which separates the data, okay? 

So that is the algorithm, and you can do the search for the c in this SVM algorithm, okay? That's called a penalty string variable. And for the linearly inseparable data, as we said before, we can also map them to a higher dimensional space so that you can separate, because it doesn't matter how you do the penalize, some problem may not be separable with your soft margin. For example, x all, there is no way you provide a linearly soft margin that cover, that separates them, okay? Eventually, the margin goes in a nudge, and then both of them will be in the same class. Okay, so to solve this x all problem, you have to use a transform function. You transform x into a higher degree, a higher dimensional space, like from two dimension to three dimension. 

Then with the three dimension data, you can draw a hyperplane to separate them, or you can go higher, okay? So usually, you will use the kernel function to do it, okay? But, you know, when data is linearly inseparable, you run the kernel function on the original database. The typical kernel functions, such as polynomial kernel, or Gaussian radial based function kernel, or sigmoid kernel, okay? So all those kernels will efficiently transform the original data into a higher dimensional data, okay? So SVM can efficiently perform a nonlinear classification using the kernel function, because kernel function implicitly mapping the input into the higher dimensional spatial space, okay? And for more details, you can check those kernel functions for machine learning application on this website, and you can also check the YouTube video there, okay?