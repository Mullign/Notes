In this video, we're going to learn two advanced classification methods. One is the Nasal Nanner and another is the Pod Vector Machine. Okay, so what is the Nasal Nanner? So far, we have studied the decision tree and the Bayesian classification, right? So those two classification methods will build the model first based on a set of training tuples, okay? Then when you have new data coming in, you will actually feed the data into the model to come up with the classification result. On the other hand, Nasal Nanner will simply store the training data. 

It does not do anything or not build a model, okay? Or only do some minor process such as normalization or compute the distances or compute the radius or maybe sometimes do some other process, right? Then, when a test tuple comes in, you make the decision. You use the test data against the existing training data to make the decision. So you can see Nasal Nanner is used less time in training, but during the prediction, you may need more time, okay? So the Nasal method effectively uses a richer hypothesis space since it uses many local linear functions to form an implicit global approximation to the target function. While IgNet must commit to a single hypothesis that covers the entire instance space. 

So there is a difference because Nasal Nanner can look around, okay? So you can use the local approximation to come up with a global approximation, okay? So Nasal Nanner is essentially an instance-based method, okay? So you store all the training examples there, delay the process, delay the evaluation process called the Nasal Evaluation until the new instance comes in and needs to be classified. There are several typical approaches. One is called the k-nearest neighbor approach, okay? So instances are represented as points in the Euclidean space. 

And then when a new instance comes in, you find the k-nearest neighbor and then use the mean value of the nearest neighbor as the k-nearest neighbor as the cost of the new instance. Or you can do a local weighted regression. You construct a local approximation, okay? And then you can also do a case-based reasoning where you have symbolic representations and knowledge base. And then use those symbolic representations to infer the classification result. Okay.