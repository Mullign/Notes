So, of course, you may not need to do, if the data is already linearly separable, you don't have to transform the data to a higher space. That's the simple case when data is linearly separable, okay? Then you just find the support vector, okay, where you have the maximum margin between the support vectors, okay? However, if it is linearly inseparable, you need to transform data into a higher space, okay? So, let's deal with the linearly separable data first. So, first of all, a separating hyperplane can be written as WTX plus B, okay? So, WT is a weight vector and B is a bias, X is the attribute, okay? 

So, for example, you have two-dimensional data, then the attribute is X1, X2, so W1, X1, W2, X2 plus B, okay? You write this as equal to zero, okay? So, the hyperplane defines the size of the margin in which W0 plus W1, because the bias we call that, for simplicity or for uniform, we pick the bias as the W0, okay? So, W0, W1, X1, okay? And then W2, X2, greater than or equal to one. And then, for, means, this is plus Yi equal to plus one. For anything greater than one, we make it plus one. 

And then hypothesis two is anything less than minus one, Yi equal to minus one. So, any training tuple that falls on hyperplane H1 or H2, what is that? Means any tuples that make W01 plus W1 times X1 plus W2 times X2 equal to one, or equal to negative one, we call them as support vectors, okay? So, essentially those points, now, there are not many points that can make it equal to one or negative one. Only a few of them. Or maybe you only have one. You can see from this example here. 

You can see, if you do this SVM, the support vector on the red one, right at the bottom, red one is the support vector. And then this one is a support vector. And the blue one, like a second blue one, is from the second from the top, near this dotted line, is a support vector, okay? So, the distance from any point to the separated plane is really as distance AX plus BX plus C equal to zero and X zero and Y zero, okay? Equal to the length of AX zero plus BY zero plus Z divided by the square root of A square B square. So, that's the width, the width distance, okay? So, you can see our objective is to maximize the distance of the closest data point to the hyperplane. 

Because on both sides, they have one closest data point or a group of closest data points to the hyperplane, okay? So, you adjust the hyperplane, the AB, adjust the AB so that the hyperplane has a different angle. But eventually, we want to find the hyperplane where this distance becomes the maximum. Well, solving this problem with that gap is hard because you adjust the line, right? Randomly or just, you know, scan through. So, we should convert it into an easier problem, okay? An easier problem where, first of all, we need to get the minimum first. 

For a minimum W and B, such that a Y will be greater than or equal to one for this, okay? So, that way, the problem becomes much easier, okay? So, this is the basic form of SVM. It can be solved by using quadratic programming, okay? So, that means we find the minimum distances of W and such that Yi is greater than or equal to one. Well, for Yi is from one to N, okay? So, that's the idea. 

So, you can see, Wxi, there's a W1, X1, W2, X2, possibly equal to one is the support vector, okay? Any point on this line is the support vector. Again, any point on this line is also the support vector. And the middle is the splitting point here. So, the margin is the distance between these two, okay? It's two times of two over the width W. That's the margin, okay? 

So, that's easy. The W is the square root of W1 squared plus W2 squared plus W3 squared, okay?