So, SVM can be used in many applications, okay? First of all, SVM, the training is very slow, okay? But the accuracy is very high, because it has ability to model complex nonlinear decision boundaries with margin maximization, okay? And so, it can be used for classification and numerical prediction as well, okay? And SVM can also be used for classifying multiple cases. You can do a one versus rest, or you can do a one versus one. You can also use SVM to do the regression analysis. 

Of course, you need additional parameters to do the regression analysis. So some typical applications such as handwriting digital recognition, disease diagnosis, object recognition, speaker identification, and time series prediction, and we can use the SVM algorithm, okay? Overall, SVM is very good, okay? It is trained well on a very small dataset. You don't need a large dataset, because as long as you can find the essential or critical data point, where it serves as the vector, the support vector, and it can solve the linearly inseparable problem with the kernel function itself. It has an elegant mathematical formula, okay? So it guarantees global optimal with the optimization. 

And you can do the cluster first and then do the classification. So it conforms with the semi-supervised training. The only problem it has is that when the data point becomes very large, it is hard to get these support vectors all identified, because they may have not support vectors, okay? And for the details on the SVM, we suggest you read this website, okay? There are several representative implementations of SVM. The most popular one is the libsvm. So it is an efficient implementation of SVM, and it includes a lot of features. 

Multicast classification, or some new SVM 1 class. It is implemented using different programming languages, such as Java, Python. Then you also have SVM 9. It provides a simpler, but its performance is not that good as libsvm, okay? And also, it supports only the binary classification and also the languages in C only. SVM Podge is another recent implementation, also written in C. You can just take this existing implementation of SVM and then adjust the parameters, training the parameters, so that you come up with your own model, okay?