So, as we said, SVM was originally proposed to solve the binary classification problem where you have two classes. What happens if you have multiple classes? How do you handle that? Because the reason they use the binary classification is because they use a hyperplane to separate. So the hyperplane was separate and then it goes to one side of the plane and another side of the plane, right? So for multi-class classification problem where you have more than two classes, then your hyperplane can never come up because this is a 9, right? The hyperplane is a 9, then you cannot separate them with a 9, even if it goes to a higher dimension. 

So what do you do is you use multiple classifiers. For example, you use 1 versus the rest. So for example, you have three classes in this dataset on the net. Then what you can do is you use one classifier to identify red against the green and the blue. Then you use another classifier to identify the blue versus red and the green. Then you do a green versus odd. So in this case, the number of classes you have determines the number of classifiers. 

If you have five classes, then you need five classifiers. On the other hand, what you can do is we will only do a one-on-one. So we will only separate green, red, and blue. We don't care about the green. Or we only do the green and blue without considering the red. So this requires n times n minus 1 divided by two classifiers. So those are the ways to handle the multiclass classification with SVM. 

The question, SVM is effective on the high-dimensional data because we're using the support vectors. So the complexity of training classifier is actually characterized by the number of support vectors, so the number of points on the edge, rather the entire dataset. So it does not matter with the dimensionality of the data. So as long as we can find out the essential or critical training examples to serve as the decision boundary that maximizes the minimum distance hyperplane, right? So SVM with a small number of support vectors can have a very good generalization, even when the dimensionality of data is very high. However, SVM is not scalable to the number of total data points because when the number of total data points is getting higher, you have to search for all the data points to identify this hyperplane, okay? You solve a quadratic problem with a large number of data inputs, right? 

So you need a lot of training time and require a lot of memory. So there is an approach by doing the SVM using a hierarchical micro-clustering approach, where instead of using all the data points to do the classification, it tries to create a micro-cluster of data and then use the micro-cluster data as the input point to do the classification. And for the detail, you can check the KDD paper by Dr. Han, where the title is called Classifying Large Data Sets Using SVM with Hierarchical Clusters. So you're building the micro-clusters first, and then classify the cluster instead of using the traditional SVM algorithm. And these micro-clusters represent the data in the nearby, okay? Now, with the micro-cluster approach, well, you do the clustering on the micro-clusters, right? 

And then the clusters, after that, then you further do it. Once you find the clusters, the boundary clusters, then you do the finding. You only search the support vectors within the clusters, not going to other, not on the micro-cluster, not on the boundary. So that will save you a lot of training time and a lot of memory, because most of the data should not be on the boundary, should not be support vectors. Only that within the micro-clusters, on the support boundary, be the actual support vectors, okay?