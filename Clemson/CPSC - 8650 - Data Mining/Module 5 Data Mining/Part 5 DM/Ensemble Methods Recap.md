And so far, we studied random forest, XGBoost, and those two are most commonly used algorithms for tabular data. And they are good for tabular data because you can take a smaller, you know, random forest, you make a smaller tree, right? You can, your base, every time you use a weak learner. So it requires no data scaling and it doesn't matter when your data, because your learner is weak, you didn't make a complex training, so you will get, you can scale it to a larger data set. And even if you have missing data, they can still, it's still be okay because you're doing the random sampling, right? The problem is you train the same, when you do a random sampling with the replacement that you may over feed the training data because you repeat several times, many times. And another problem is like when you build a random forest or XGBoost because you have many, many weak learners, you don't know which learner make the decisions, right? So it is hard to interpret, okay? 

In terms compared to the decision, you know exactly what rules make decision, you follow the paths, right? You say, oh, the rainy weather, the rainy humidity, right? So you know exactly what happened, but here you don't know because you have so many branches and then so many weak learners and each weak learner based on a set of a subset of features to make a decision.