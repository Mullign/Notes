And another method is called the gradient boosting, and so it uses a diffractional loss function, okay, and it uses a weak learner to make the prediction, usually give a twig, okay, and then every time you're not using some advanced method such as SVM, okay, so you add a new weak learner, okay, and every time you add a new weak learner, the idea is to minimize that diffractional loss function, okay, and so there's a scalable implementation called the XGBoost, okay, and so you're given a current model, you always add a new weak model, okay, and that weak model, and then you try to, by adding a new weak model in to minimize the loss function, okay, now, when the loss function reaches a certain threshold, then error, error, getting to the certain threshold, below a certain threshold, you stop there, okay. 

And another method is called the gradient boosting, and so it uses a diffractional loss function, okay, and it uses a weak learner to make the prediction, usually give a twig, okay, and then every time you're not using some advanced method such as SVM, okay, so you add a new weak learner, okay, and every time you add a new weak learner, the idea is to minimize that diffractional loss function, okay, and so there's a scalable implementation called the XGBoost, okay, and so you're given a current model, you always add a new weak model, okay, and that weak model, and then you try to, by adding a new weak model in to minimize the loss function, okay, now, when the loss function reaches a certain threshold, then error, error, getting to the certain threshold, below a certain threshold, you stop there, okay.  0:06 / 1:49 1.25x Gradient Boosting Save note Transcript Notes Downloads Language: English​Interactive Transcript - Enable basic transcript mode by pressing the escape key You may navigate through the transcript using tab. To save a note for a section of text press ⌘ + S. To expand your selection you may use ⌘ + arrow key. You may contract your selection using shift + ⌘ + arrow key. For screen readers that are incompatible with using arrow keys for shortcuts, you can replace them with the H J K L keys. Some screen readers may require using ⌘ in conjunction with the alt key Play video starting at :: and follow transcript0:00 : Added to Selection. Press [⌘ + S] to save as a note Like Dislike Report an issue : Added to Selection. Press [⌘ + S] to save as a note