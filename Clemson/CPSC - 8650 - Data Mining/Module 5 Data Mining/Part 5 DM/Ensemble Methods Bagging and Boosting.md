So let's get to the basic algorithm of back-end. The name of back-end algorithm is basically called bootstrap aggregation. So it is analogy of diagnosis based on multiple doctors' majority vote, actually, okay? So for the training process, you may have a 1-2k, you train 1-2k times, okay? But those 1-2k times are not sequential. You can do it in parallel. But every time, you create a bootstrap sample, DI. 

Now that bootstrap sample actually is the same size as the original sample, D. Okay, that's the first. But the sample will be with the replacement. So when you do a sample with the replacement, what do you get? What you get is probably like a 60, 63, 64%, 65% of the data, of the original data, and then there are some overlap of the sample data, okay? Some data sample will be replaced, repeated in the sample data set. Then you use that sample, use that bootstrap sample to train the model, okay? 

So that's MI. So given, so you train k models. So given an unknown sample X, okay, then you net each model k to make a classification. And then you take the majority of those k models result to, as the final classification result. Now, in terms of using the prediction, you can take an average prediction, but you want to predict the numerical value, you take an average, okay? So that's called the bagging. And boosting, okay, boosting is like another way, well, for example, if you go to see the doctor, you go to see your family doctor, they have experience on every different kind of disease, okay, to diagnose any kind of disease, but they are not the expert on any specific one, okay? 

So you go to your family doctor, they do the initial diagnosis, then they introduce you to the specialty doctors, and they do further diagnosis. So what you do is, in the end, you gave a smaller weight on your initial doctor's diagnosis, and they gave a higher weight to your specialty doctor, because they are expert on that field has a very good track record on that particular disease, right? So how do we generate such a scenario that we can come up with a good, come up with specialty doctors in this case? So boosting, well, started with initial classifier, okay? So you train a classifier iteratively, okay? So you're not trained in parallel, okay? You train one by one. 

So you started from the first classifier, okay? Then you get the prediction result. Some tuple may be misclassified by this initial classifier, okay? Then your next classifier will pay more attention to these training tuples that were misclassified by the previous classifier. So the MI is then, then your subsequent classifier MI plus one will pay more attention to the training tuple that were misclassified by MI. Then your final classification is the combination of each individual classifier from M1 to MK. And then weight each classifier's vote as a function of its accuracy. 

Given the classifier with a higher accurate, higher weight, okay? Of course, you can use this weighted average to adjust, to extend this boosting algorithm for the numerical prediction as well, okay? Okay, so this is boosting. 

So let's get to the basic algorithm of back-end. The name of back-end algorithm is basically called bootstrap aggregation. So it is analogy of diagnosis based on multiple doctors' majority vote, actually, okay? So for the training process, you may have a 1-2k, you train 1-2k times, okay? But those 1-2k times are not sequential. You can do it in parallel. But every time, you create a bootstrap sample, DI.  Play video starting at ::54 and follow transcript0:54 Now that bootstrap sample actually is the same size as the original sample, D. Okay, that's the first. But the sample will be with the replacement. So when you do a sample with the replacement, what do you get? What you get is probably like a 60, 63, 64%, 65% of the data, of the original data, and then there are some overlap of the sample data, okay? Some data sample will be replaced, repeated in the sample data set. Then you use that sample, use that bootstrap sample to train the model, okay?  Play video starting at :1:47 and follow transcript1:47 So that's MI. So given, so you train k models. So given an unknown sample X, okay, then you net each model k to make a classification. And then you take the majority of those k models result to, as the final classification result. Now, in terms of using the prediction, you can take an average prediction, but you want to predict the numerical value, you take an average, okay? So that's called the bagging. And boosting, okay, boosting is like another way, well, for example, if you go to see the doctor, you go to see your family doctor, they have experience on every different kind of disease, okay, to diagnose any kind of disease, but they are not the expert on any specific one, okay?  Play video starting at :3:8 and follow transcript3:08 So you go to your family doctor, they do the initial diagnosis, then they introduce you to the specialty doctors, and they do further diagnosis. So what you do is, in the end, you gave a smaller weight on your initial doctor's diagnosis, and they gave a higher weight to your specialty doctor, because they are expert on that field has a very good track record on that particular disease, right? So how do we generate such a scenario that we can come up with a good, come up with specialty doctors in this case? So boosting, well, started with initial classifier, okay? So you train a classifier iteratively, okay? So you're not trained in parallel, okay? You train one by one.  Play video starting at :4:41 and follow transcript4:41 So you started from the first classifier, okay? Then you get the prediction result. Some tuple may be misclassified by this initial classifier, okay? Then your next classifier will pay more attention to these training tuples that were misclassified by the previous classifier. So the MI is then, then your subsequent classifier MI plus one will pay more attention to the training tuple that were misclassified by MI. Then your final classification is the combination of each individual classifier from M1 to MK. And then weight each classifier's vote as a function of its accuracy.  Play video starting at :5:58 and follow transcript5:58 Given the classifier with a higher accurate, higher weight, okay? Of course, you can use this weighted average to adjust, to extend this boosting algorithm for the numerical prediction as well, okay? Okay, so this is boosting.  So let's get to the basic algorithm of back-end. The name of back-end algorithm is basically called bootstrap aggregation. So it is analogy of diagnosis based on multiple doctors' majority vote, actually, okay? So for the training process, you may have a 1-2k, you train 1-2k times, okay? But those 1-2k times are not sequential. You can do it in parallel. But every time, you create a bootstrap sample, DI.  Play video starting at ::54 and follow transcript0:54 Now that bootstrap sample actually is the same size as the original sample, D. Okay, that's the first. But the sample will be with the replacement. So when you do a sample with the replacement, what do you get? What you get is probably like a 60, 63, 64%, 65% of the data, of the original data, and then there are some overlap of the sample data, okay? Some data sample will be replaced, repeated in the sample data set. Then you use that sample, use that bootstrap sample to train the model, okay?  Play video starting at :1:47 and follow transcript1:47 So that's MI. So given, so you train k models. So given an unknown sample X, okay, then you net each model k to make a classification. And then you take the majority of those k models result to, as the final classification result. Now, in terms of using the prediction, you can take an average prediction, but you want to predict the numerical value, you take an average, okay? So that's called the bagging. And boosting, okay, boosting is like another way, well, for example, if you go to see the doctor, you go to see your family doctor, they have experience on every different kind of disease, okay, to diagnose any kind of disease, but they are not the expert on any specific one, okay?  Play video starting at :3:8 and follow transcript3:08 So you go to your family doctor, they do the initial diagnosis, then they introduce you to the specialty doctors, and they do further diagnosis. So what you do is, in the end, you gave a smaller weight on your initial doctor's diagnosis, and they gave a higher weight to your specialty doctor, because they are expert on that field has a very good track record on that particular disease, right? So how do we generate such a scenario that we can come up with a good, come up with specialty doctors in this case? So boosting, well, started with initial classifier, okay? So you train a classifier iteratively, okay? So you're not trained in parallel, okay? You train one by one.  Play video starting at :4:41 and follow transcript4:41 So you started from the first classifier, okay? Then you get the prediction result. Some tuple may be misclassified by this initial classifier, okay? Then your next classifier will pay more attention to these training tuples that were misclassified by the previous classifier. So the MI is then, then your subsequent classifier MI plus one will pay more attention to the training tuple that were misclassified by MI. Then your final classification is the combination of each individual classifier from M1 to MK. And then weight each classifier's vote as a function of its accuracy.  Play video starting at :5:58 and follow transcript5:58 Given the classifier with a higher accurate, higher weight, okay? Of course, you can use this weighted average to adjust, to extend this boosting algorithm for the numerical prediction as well, okay? Okay, so this is boosting.  So let's get to the basic algorithm of back-end. The name of back-end algorithm is basically called bootstrap aggregation. So it is analogy of diagnosis based on multiple doctors' majority vote, actually, okay? So for the training process, you may have a 1-2k, you train 1-2k times, okay? But those 1-2k times are not sequential. You can do it in parallel. But every time, you create a bootstrap sample, DI.  Play video starting at ::54 and follow transcript0:54 Now that bootstrap sample: Added to Selection. Press [⌘ + S] to save as a note Like Dislike Report an issue : Added to Selection. Press [⌘ + S] to save as a note Like Dislike Report an issue : Added to Selection. Press [⌘ + S] to save as a note