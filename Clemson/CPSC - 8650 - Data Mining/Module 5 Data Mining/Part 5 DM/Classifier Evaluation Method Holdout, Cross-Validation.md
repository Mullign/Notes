Evaluation. As we said, the evaluation cannot use the training data set to evaluate. You can see the training data set to evaluate because your data is already, your model remember the training data set, so you may cause the overfitting problem there, right? Your complexity goes higher and then you get an overfitting, then you use the training data set to test it, you get a very good result, but in the end, your test sample gave you a very bad result. Okay. So what do you do is you want to hold out the samples. So you gave the labeled data, you will split them into two parts. 

For example, you use a majority of this portion as the training data and then a smaller set as the testing data. For example, you use the 66%, 33%, or you use the 80% or 20%. Okay. So this is called a hold automatic. Well, oftentimes you use the hold automatic, depend on your partition. Now what if your partition, the test partition has a different distribution with the training partition, then your test result is very bad because you use another distribution, a data with a different distribution to build the model, then you test with a different distribution data. So people will improve that by randomly sub-sample data. 

Well, we take 5%, okay, say 20% out, but then we randomly take 20% out. We hold out the rest of 80% to do the training, then we test, but we are not done yet. Then we random sample again, and then take that 20% out, and then the rest of the 80% that's training. So we test that again. And then we do this k-time, okay? So we call it a repeated sub-sample validation, okay? So basically it's a variation of holdout. 

So this way, because we repeated the holdout k-times, then the classification accuracy is the average of the accuracy we obtained in this k-time. That will give you a much better estimate, it will not be affected by this, at least the average out, so that a one-time random peak result may cause a bad estimate, okay? So that avoids that problem. And so it goes to an extreme situation, but it brings us another method called the validation, okay? So validation is just like this, like you randomly pick out the random holdout data. But instead of random holdout at a certain percentage, well, in this case, it's like you randomly partition into k subsets first. And then, because in the previous situation, you randomly picked out a certain percent, but in this situation, you just put them into k mutually exclusive subsets, okay? 

And then make them each as an equal set. And then you do the k-time. So first of all, your percentages decide 1 over k for each partition. Each partition is 1 over k, size is approximately 1 over k. And then you also repeat the k-time, okay? So in each iteration, you use that particular... So for example, iteration 1 uses partition 1 as the test set, the rest as the training set, okay? 

And then in iteration 2, you use the D2 as the testing set and the other as the training set. By doing that, you have a total of k-times, then you estimate the accuracy as the average of these k-times, okay? Now, in a very real situation, when you have a very small dataset, okay? And then, for example, you only have 20 data samples, well, you may just leave one out. So you take one sample as the test, the rest of them as the training. Then that only handles the small-sized data. And also, the cross-validation has a problem if your data distribution, say, you take a k tenfold cross-validation, you want to make sure that each fold will have the same cost distribution as the entire population, okay? 

So we also call it a stratified cross-validation. Now for the stratified cross-validation, you must make sure each partition will contain the cost distribution will be approximately the same as the initial data, because that test data, if it doesn't contain certain costs, for example, one partition is dominated by one cost, and then you use all the other training data that doesn't even contain this one cost, and then that model will not be good. It will not give you a good result on that particular cost, okay? So stratified cross-validation tries to make the partition has the same distribution in terms of the cost, okay, in each folder, okay? Each folder must have the same distribution of cost with the original data. 

Evaluation. As we said, the evaluation cannot use the training data set to evaluate. You can see the training data set to evaluate because your data is already, your model remember the training data set, so you may cause the overfitting problem there, right? Your complexity goes higher and then you get an overfitting, then you use the training data set to test it, you get a very good result, but in the end, your test sample gave you a very bad result. Okay. So what do you do is you want to hold out the samples. So you gave the labeled data, you will split them into two parts.  Play video starting at ::55 and follow transcript0:55 For example, you use a majority of this portion as the training data and then a smaller set as the testing data. For example, you use the 66%, 33%, or you use the 80% or 20%. Okay. So this is called a hold automatic. Well, oftentimes you use the hold automatic, depend on your partition. Now what if your partition, the test partition has a different distribution with the training partition, then your test result is very bad because you use another distribution, a data with a different distribution to build the model, then you test with a different distribution data. So people will improve that by randomly sub-sample data.  Play video starting at :1:55 and follow transcript1:55 Well, we take 5%, okay, say 20% out, but then we randomly take 20% out. We hold out the rest of 80% to do the training, then we test, but we are not done yet. Then we random sample again, and then take that 20% out, and then the rest of the 80% that's training. So we test that again. And then we do this k-time, okay? So we call it a repeated sub-sample validation, okay? So basically it's a variation of holdout.  Play video starting at :2:35 and follow transcript2:35 So this way, because we repeated the holdout k-times, then the classification accuracy is the average of the accuracy we obtained in this k-time. That will give you a much better estimate, it will not be affected by this, at least the average out, so that a one-time random peak result may cause a bad estimate, okay? So that avoids that problem. And so it goes to an extreme situation, but it brings us another method called the validation, okay? So validation is just like this, like you randomly pick out the random holdout data. But instead of random holdout at a certain percentage, well, in this case, it's like you randomly partition into k subsets first. And then, because in the previous situation, you randomly picked out a certain percent, but in this situation, you just put them into k mutually exclusive subsets, okay?  Play video starting at :4:16 and follow transcript4:16 And then make them each as an equal set. And then you do the k-time. So first of all, your percentages decide 1 over k for each partition. Each partition is 1 over k, size is approximately 1 over k. And then you also repeat the k-time, okay? So in each iteration, you use that particular... So for example, iteration 1 uses partition 1 as the test set, the rest as the training set, okay?  Play video starting at :4:56 and follow transcript4:56 And then in iteration 2, you use the D2 as the testing set and the other as the training set. By doing that, you have a total of k-times, then you estimate the accuracy as the average of these k-times, okay? Now, in a very real situation, when you have a very small dataset, okay? And then, for example, you only have 20 data samples, well, you may just leave one out. So you take one sample as the test, the rest of them as the training. Then that only handles the small-sized data. And also, the cross-validation has a problem if your data distribution, say, you take a k tenfold cross-validation, you want to make sure that each fold will have the same cost distribution as the entire population, okay?  Play video starting at :6:19 and follow transcript6:19 So we also call it a stratified cross-validation. Now for the stratified cross-validation, you must make sure each partition will contain the cost distribution will be approximately the same as the initial data, because that test data, if it doesn't contain certain costs, for example, one partition is dominated by one cost, and then you use all the other training data that doesn't even contain this one cost, and then that model will not be good. It will not give you a good result on that particular cost, okay? So stratified cross-validation tries to make the partition has the same distribution in terms of the cost, okay, in each folder, okay? Each folder must have the same distribution of cost with the original data.  Evaluation. As we said, the evaluation cannot use the training data set to evaluate. You can see the training data set to evaluate because your data is already, your model remember the training data set, so you may cause the overfitting problem there, right? Your complexity goes higher and then you get an overfitting, then you use the training data set to test it, you get a very good result, but in the end, your test sample gave you a very bad result. Okay. So what do you do is you want to hold out the samples. So you gave the labeled data, you will split them into two parts. : Added to Selection. Press [⌘ + S] to save as a note Like Dislike Report an issue : Added to Selection. Press [⌘ + S] to save as a note