So far we have learned the basic concept of classification and several classification methods and also we learned how to evaluate those classification algorithms and determine which models are the best. All of those classification methods have their weaknesses. So in this video we are going to discuss how to improve the classification accuracy based on the current training data set. Okay, so we'll focus on the ensemble method including bagging, boosting, and random forest and also we go back to study how to improve the classification result for the imbalanced data set where you have a minority class and one class with the minority samples. All right, so what is an ensemble? An ensemble is trying to use a combination of several models to increase the accuracy. It is like when we try to get a medical diagnosis, we ask several doctors and we get several opinions. Then we combine those opinions together to make a better decision. Okay, so an ensemble method is basically we train multiple models. For example, we train k models. 

Okay, so we want to combine these models, the vote of those combined models to make the prediction. Okay, so that's the key. So you can see from this diagram where you have data and then you train first model 1, model 2k, and then when you have a new tuple, we use model 1 to get a 1 result, model 2 get a 1 result, model k get a 1 result, and then we combine those results from those models to make the final decision. Okay, so what are the requirements to generate the improved model? First of all, we should use a majority voting, right? Because when you do an ensemble, and if you just make a decision randomly, it doesn't give you the best result, right? So if you take the majority voting, it should give you a better result. Okay, for example, you have model 1, model 2, model 3. So model 1 predicts x1 as positive and model 3 gives you a good estimate. Then model 2 misclassifies x1. Then we take both of them, then we get the best result. Okay, and then the same thing goes to model 2, model 3. So ensemble will give you the positive effect in this case. On the other hand, in case number two, if all the models give you the same result, then ensemble does not give you the effect, any effect, because it is the same as the base model. 

Or in the worst case, okay, in the worst case, model 1 is positive, the data x1 is positive, model 1 is predicted as positive or correct, but model 2, model 3 says no, they are not correct. Then if you take the majority voting, hey, you get x1 is not positive. Then you get the wrong. So the worst case scenario where you have the majority of them giving the wrong result, then the ensemble will have a negative effect. So for generating and improving the model, first, you need the base model to be accurate. If your base model is wrong, no matter how you ensemble, it doesn't work. Secondly, your base models should be diverse, diverse, because if they have a consistent judgment, you don't have to do an ensemble because there is no ensemble effect, okay? Now, so there are two popular ensemble methods. One is called the backing, okay? And another is called the boosting. Now, backing, well, you train the model, the smaller model, okay, with a subset of training set, okay? And then each one has a subset. So you get this model is based on a subset of the data. 

Then when you do the test, you put the data into this model to do the majority vote. So that's one situation here, right? So how do you get this subset of the training set? You can do a resampling, okay? So that's the one called the backing. And so those models, because after you resample, you get a separate training data set. They may have some overlap, but they are trained as separate data sets. 

Another method is you boost it. Well, you use the same data set, okay? Okay? And gave the data with a different weight. So you initially gave the data as the same weight, okay, when you train the model. Then after one iteration, you check whether certain data item was misclassified. So for those your classifier makes mistakes, then you focus on this data. So your next training round, you give a higher weight to the data sample, okay? So you run this model again on the data, but with a different weight. 

So eventually, it is called boosting because every time you adjust the weight, so every time you adjust the weight, in all the series, you generate a series of models. Finally, each model may have different accuracy, right? So you gave the model with the highest accuracy, with the highest weight in your final majority vote. Okay, so this is for the boosting. 

So far we have learned the basic concept of classification and several classification methods and also we learned how to evaluate those classification algorithms and determine which models are the best. All of those classification methods have their weaknesses. So in this video we are going to discuss how to improve the classification accuracy based on the current training data set. Okay, so we'll focus on the ensemble method including bagging, boosting, and random forest and also we go back to study how to improve the classification result for the imbalanced data set where you have a minority class and one class with the minority samples. All right, so what is an ensemble? An ensemble is trying to use a combination of several models to increase the accuracy. It is like when we try to get a medical diagnosis, we ask several doctors and we get several opinions. Then we combine those opinions together to make a better decision. Okay, so an ensemble method is basically we train multiple models. For example, we train k models.  Play video starting at :2:12 and follow transcript2:12 Okay, so we want to combine these models, the vote of those combined models to make the prediction. Okay, so that's the key. So you can see from this diagram where you have data and then you train first model 1, model 2k, and then when you have a new tuple, we use model 1 to get a 1 result, model 2 get a 1 result, model k get a 1 result, and then we combine those results from those models to make the final decision. Okay, so what are the requirements to generate the improved model? First of all, we should use a majority voting, right? Because when you do an ensemble, and if you just make a decision randomly, it doesn't give you the best result, right? So if you take the majority voting, it should give you a better result. Okay, for example, you have model 1, model 2, model 3. So model 1 predicts x1 as positive and model 3 gives you a good estimate. Then model 2 misclassifies x1. Then we take both of them, then we get the best result. Okay, and then the same thing goes to model 2, model 3. So ensemble will give you the positive effect in this case. On the other hand, in case number two, if all the models give you the same result, then ensemble does not give you the effect, any effect, because it is the same as the base model.  Play video starting at :4:40 and follow transcript4:40 Or in the worst case, okay, in the worst case, model 1 is positive, the data x1 is positive, model 1 is predicted as positive or correct, but model 2, model 3 says no, they are not correct. Then if you take the majority voting, hey, you get x1 is not positive. Then you get the wrong. So the worst case scenario where you have the majority of them giving the wrong result, then the ensemble will have a negative effect. So for generating and improving the model, first, you need the base model to be accurate. If your base model is wrong, no matter how you ensemble, it doesn't work. Secondly, your base models should be diverse, diverse, because if they have a consistent judgment, you don't have to do an ensemble because there is no ensemble effect, okay? Now, so there are two popular ensemble methods. One is called the backing, okay? And another is called the boosting. Now, backing, well, you train the model, the smaller model, okay, with a subset of training set, okay? And then each one has a subset. So you get this model is based on a subset of the data.  Play video starting at :6:51 and follow transcript6:51 Then when you do the test, you put the data into this model to do the majority vote. So that's one situation here, right? So how do you get this subset of the training set? You can do a resampling, okay? So that's the one called the backing. And so those models, because after you resample, you get a separate training data set. They may have some overlap, but they are trained as separate data sets.  Play video starting at :7:36 and follow transcript7:36 Another method is you boost it. Well, you use the same data set, okay? Okay? And gave the data with a different weight. So you initially gave the data as the same weight, okay, when you train the model. Then after one iteration, you check whether certain data item was misclassified. So for those your classifier makes mistakes, then you focus on this data. So your next training round, you give a higher weight to the data sample, okay? So you run this model again on the data, but with a different weight.  Play video starting at :8:36 and follow transcript8:36 So eventually, it is called boosting because every time you adjust the weight, so every time you adjust the weight, in all the series, you generate a series of models. Finally, each model may have different accuracy, right? So you gave the model with the highest accuracy, with the highest weight in your final majority vote. Okay, so this is for the boosting.  So far we have learned the basic concept of classification and several classification methods and also we learned how to evaluate those classification algorithms and determine which models are the best. All of those classification methods have their weaknesses. So in this video we are going to discuss how to improve the classification accuracy based on the current training data set. Okay, so we'll focus on the ensemble method including bagging, boosting, and random forest and also we go back to study how to improve the classification result for the imbalanced data set where you have a minority class and one class with the minority samples. All right, so what is an ensemble? An ensemble is trying to use a combination of several models to increase the accuracy. It is like when we try to get a medical diagnosis, we ask several doctors and we get several opinions. Then we combine those opinions together to make a better decision. Okay, so an ensemble method is basically we train multiple models. For example, we train k models.  Play video starting at :2:12 and follow transcript2:12 Okay, so we want to combine these models, the vote of those combined models to make the prediction. Okay, so that's the key. So you can see from this diagram where you have data and then you train first model 1, model 2k, and then when you have a new tuple, we use model 1 to get a 1 result, model 2 get a 1 result, model k get a 1 result, and then we combine those results from those models to make the final decision. Okay, so what are the requirements to generate the improved model? First of all, we should use a majority voting, right? Because when you do an ensemble, and if you just make a decision randomly, it doesn't give you the best result, right? So if you take the majority voting, it should give you a better result. Okay, for example, you have model 1, model 2, model 3. So model 1 predicts x1 as positive and model 3 gives you a good estimate. Then model 2 misclassifies x1. Then we take both of them, then we get the best result. Okay, and then the same thing goes to model 2, model 3. So ensemble will give you the positive effect in this case. On the other hand, in case number two, if all the models give you the same result, then ensemble does not give you the effect, any effect, because it is the same as the base model.  Play video starting at :4:40 and follow transcript4:40 Or in the worst case, okay, in the worst case, model 1 is positive, the data x1 is positive, model 1 is predicted as positive or correct, but model 2, model 3 says no, they are not correct. Then if you take the majority voting, hey, you get x1 is not positive. Then you get the wrong. So the worst case scenario where you have the majority of them giving the wrong result, then the ensemble will have a negative effect. So for generating and improving the model, first, you need the base model to be accurate. If your base model is wrong, no matter how you ensemble, it doesn't work. Secondly, your base models should be diverse, diverse, because if they have a consistent judgment, you don't have to do an ensemble because there is no ensemble effect, okay? Now, so there are two popular ensemble methods. One is called the backing, okay? And another is called the boosting. Now, backing, well, you train the model, the smaller model, okay, with a subset of training set, okay? And then each one has a subset. So you get this model is based on a subset of the data.  Play video starting at :6:51 and follow transcript6:51 Then when you do the test, you put the data into this model to do the majority vote. So that's one situation here, right? So how do you get this subset of the training set? You can do a resampling, okay? So that's the one called the backing. And so those models, because after you resample, you get a separate training data set. They may have some overlap, but they are trained as separate data sets.  Play video starting at :7:36 and follow transcript7:36 Another method is you boost it. Well, you use the same data set, okay? Okay? And gave the data with a different weight. So you initially gave the data as the same weight, okay, when you train the model. Then after one iteration, you check whether certain data item was misclassified. So for those your classifier makes mistakes, then you focus on this data. So your next training round, you give a higher weight to the data sample, okay? So you run this model again on the data, but with a different weight.  Play video starting at :8:36 and follow transcript8:36 So eventually, it is called boosting because every time you adjust the weight, so every time you adjust the weight, in all the series, you generate a series of models. Finally, each model may have different accuracy, right? So you gave the model with the highest accuracy, with the highest weight in your final majority vote. Okay, so this is for the boosting.  So far we have learned the basic concept of classification and several classification methods and also we learned how to evaluate those classification algorithms and determine which models are the best. All of those classification methods have their weaknesses. So in this video we are going to discuss how to improve the classification accuracy based on the current training data set. Okay, so we'll focus on the ensemble method including bagging, boosting, and random forest and also we go back to study how to improve: Added to Selection. Press [⌘ + S] to save as a note Like Dislike Report an issue : Added to Selection. Press [⌘ + S] to save as a note Like Dislike Report an issue : Added to Selection. Press [⌘ + S] to save as a note