So far, we have studied the concept of classification and also introduced several typical classification algorithms include decision tree induction, Bayes classification method, Bayesian believing network, and lazy learners, and SVM. Now we study how to evaluate those classification model. To evaluate a classification model, we need to have a set of metrics, right? Classification is trying to predict the neighbors of unknown data, new data set, new data, okay? So the accuracy of the prediction is the measure, because if we predict the neighbor in the wrong class, that's not accurate, right? And also we need many other metrics. We will define those metrics in this video. And also that when you evaluate the model, we must make sure that we are not going to use the training set to access, assess the accuracy. Because the training data set was used to construct the model. So the model may already remember the training data set. So the test results may be misleading. So we need to make sure we have fresh new test data, other than the training data, because that test data must not be seen by the model already. That is why we have a holdout method, which hold a portion of data out as the test data. Then we can also have a cross validation method. 

Now another way to build the test set is use the bootstrap. We're not going to cover here, okay? And then also, we may have other methods to evaluate the models, such as ROC curves, we will discuss here too. 

So far, we have studied the concept of classification and also introduced several typical classification algorithms include decision tree induction, Bayes classification method, Bayesian believing network, and lazy learners, and SVM. Now we study how to evaluate those classification model. To evaluate a classification model, we need to have a set of metrics, right? Classification is trying to predict the neighbors of unknown data, new data set, new data, okay? So the accuracy of the prediction is the measure, because if we predict the neighbor in the wrong class, that's not accurate, right? And also we need many other metrics. We will define those metrics in this video. And also that when you evaluate the model, we must make sure that we are not going to use the training set to access, assess the accuracy. Because the training data set was used to construct the model. So the model may already remember the training data set. So the test results may be misleading. So we need to make sure we have fresh new test data, other than the training data, because that test data must not be seen by the model already. That is why we have a holdout method, which hold a portion of data out as the test data. Then we can also have a cross validation method.  Play video starting at :2:38 and follow transcript2:38 Now another way to build the test set is use the bootstrap. We're not going to cover here, okay? And then also, we may have other methods to evaluate the models, such as ROC curves, we will discuss here too.  0:08 / 3:10 1.25x Introduction to Model Evaluation and Selection Save note Transcript Notes Downloads Language: English (auto)​Interactive Transcript - Enable basic transcript mode by pressing the escape key You may navigate through the transcript using tab. To save a note for a section of text press ⌘ + S. To expand your selection you may use ⌘ + arrow key. You may contract your selection using shift + ⌘ + arrow key. For screen readers that are incompatible with using arrow keys for shortcuts, you can replace them with the H J K L keys. Some screen readers may require using ⌘ in conjunction with the alt key Play video starting at :: and follow transcript0:00 : Added to Selection. Press [⌘ + S] to save as a note Like Dislike Report an issue : Added to Selection. Press [⌘ + S] to save as a note