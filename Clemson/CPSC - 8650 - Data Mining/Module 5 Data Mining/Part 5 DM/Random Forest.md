Another method is called the random forest. Now the basic concept is that you use decision tree as base model, and you run the back-in algorithm. Okay, the back-in algorithm. The back-in can be two-front. Back-in, as we said, you do random sampling, right? In the bootstrapping aggregation algorithm, you do a random sample of the data. In this random forest, you do the back-in, you do the same sample with replacement, okay? 

You use a subset of Trinity, only a subset of Trinity, but again use a sample with replacement. So this is called the data back-in. But with data back-in, you can also use a facial back-in, okay? And the facial back-in, at each node, you're not going to use all the attributes. You only use a random selected attribute to train the candidate, as a candidate to do the tree spree, okay? So if you build the entire tree, you need to go through all the attributes. Now you just take a subset of the attributes, so this is called the facial back-in, okay? 

So by doing that, you generate a big number of trees, okay? So a big number of trees, what do we call it? We call it a forest, right? So this is called a random forest, because you are randomly selecting either the Trinity data, and also randomly selecting all attributes, okay? So in the final classification, all those base trees will give you the result, and then you take a majority vote. So now, random forest, you can, based on how you take the random selection of data or random selection of features, you have different methods to build a random forest. The first one is called the forest RI, so it is random input selection, okay? 

So at each node, you randomly select the F attribute as candidate for spree at that node. And also, as we know, in our decision tree method, we use, based on how many branches to do the, based on the entropies, information gain or information ratio to make a decision. In this forest RI method, you use the classification and regression tree methodology, where you use binary decision, okay? You split into two parts, okay, yes or no. It is like in the Gini index. So that split point, you always split into two parts, okay? You find the best split point, okay, among these, and then you split them, okay? 

So that's card methodology. For the more details on card methodology, you can go check. So the idea is that I can use the Gini index or use the Gini impurity to decide the splitting, the best splitting point. Therefore your tree becomes simpler, okay? Then you grow the tree to the maximum size, eventually, okay? You can also do a forest random combination, so the random linear combination. So what you do is you create a new attribute, that linear combination of existing attributes. 

Therefore, you reduce the correlation between the individual classifiers, okay? So you have two methods, okay? Random forest has the comparable performance in accuracy as the AdaBoost, but it is more robust to the errors and the alternate. The reason is because the random combination of smaller or weak randoms, okay? And also, it is insensitive to the number of attributes selected for consideration at each split, and it is faster than typical bagging, because you randomly select the features splitting with a simple card methodology, okay? 

Another method is called the random forest. Now the basic concept is that you use decision tree as base model, and you run the back-in algorithm. Okay, the back-in algorithm. The back-in can be two-front. Back-in, as we said, you do random sampling, right? In the bootstrapping aggregation algorithm, you do a random sample of the data. In this random forest, you do the back-in, you do the same sample with replacement, okay?  Play video starting at ::58 and follow transcript0:58 You use a subset of Trinity, only a subset of Trinity, but again use a sample with replacement. So this is called the data back-in. But with data back-in, you can also use a facial back-in, okay? And the facial back-in, at each node, you're not going to use all the attributes. You only use a random selected attribute to train the candidate, as a candidate to do the tree spree, okay? So if you build the entire tree, you need to go through all the attributes. Now you just take a subset of the attributes, so this is called the facial back-in, okay?  Play video starting at :1:55 and follow transcript1:55 So by doing that, you generate a big number of trees, okay? So a big number of trees, what do we call it? We call it a forest, right? So this is called a random forest, because you are randomly selecting either the Trinity data, and also randomly selecting all attributes, okay? So in the final classification, all those base trees will give you the result, and then you take a majority vote. So now, random forest, you can, based on how you take the random selection of data or random selection of features, you have different methods to build a random forest. The first one is called the forest RI, so it is random input selection, okay?  Play video starting at :3:14 and follow transcript3:14 So at each node, you randomly select the F attribute as candidate for spree at that node. And also, as we know, in our decision tree method, we use, based on how many branches to do the, based on the entropies, information gain or information ratio to make a decision. In this forest RI method, you use the classification and regression tree methodology, where you use binary decision, okay? You split into two parts, okay, yes or no. It is like in the Gini index. So that split point, you always split into two parts, okay? You find the best split point, okay, among these, and then you split them, okay?  Play video starting at :4:27 and follow transcript4:27 So that's card methodology. For the more details on card methodology, you can go check. So the idea is that I can use the Gini index or use the Gini impurity to decide the splitting, the best splitting point. Therefore your tree becomes simpler, okay? Then you grow the tree to the maximum size, eventually, okay? You can also do a forest random combination, so the random linear combination. So what you do is you create a new attribute, that linear combination of existing attributes.  Play video starting at :5:10 and follow transcript5:10 Therefore, you reduce the correlation between the individual classifiers, okay? So you have two methods, okay? Random forest has the comparable performance in accuracy as the AdaBoost, but it is more robust to the errors and the alternate. The reason is because the random combination of smaller or weak randoms, okay? And also, it is insensitive to the number of attributes selected for consideration at each split, and it is faster than typical bagging, because you randomly select the features splitting with a simple card methodology, okay?  0:06 / 6:21 1.25x Random Forest Save note Transcript Notes Downloads Language: English​Interactive Transcript - Enable basic transcript mode by pressing the escape key You may navigate through the transcript using tab. To save a note for a section of text press ⌘ + S. To expand your selection you may use ⌘ + arrow key. You may contract your selection using shift + ⌘ + arrow key. For screen readers that are incompatible with using arrow keys for shortcuts, you can replace them with the H J K L keys. Some screen readers may require using ⌘ in conjunction with the alt key Play video starting at :: and follow transcript0:00 : Added to Selection. Press [⌘ + S] to save as a note Like Dislike Report an issue : Added to Selection. Press [⌘ + S] to save as a note