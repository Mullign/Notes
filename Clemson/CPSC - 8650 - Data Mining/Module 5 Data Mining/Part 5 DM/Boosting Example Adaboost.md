Now, let's look at an example, a typical example of boosting called the add-up boosting, okay? So for the add-up boosting algorithm, you assign the initial weights to each training tuple. For example, you assign an equal weight. At this moment, you don't know which which tuple will be more important or which tuple is hard to be classified, right? And then, so you have a w1, w2, wn, whatever, okay? And then train the first model. So, because you train the base classifier on the weighted data set, right? 

You assign the weights to each training sample, okay? Or you can also, so you give the weight, then you train with the whole data set. Or what you can do is you sample the original data set based on certain weight distribution as well, right? So either way, this is a two different weighted strategy. But the most popular one is that I just assign weights to each training example and then when you build the model, use the weight of the example to build the model, okay? The base model. So you get a base model number one. Then you evaluate base model number one, okay? And then after you evaluate, some of them will be misclassified. So some tuples are misclassified. In that case, you need to increase the weight on those, okay? Increase the weight on those. Then your next training sample, training model, next base model will focus on that. 

And then you continue going like that. Eventually, going through a series of these, each one of them has an accuracy value. Then you take the majority vote, but based on the weight. So M1's opinion is weighted by its accuracy, test accuracy, okay? So you get the final result. So this is the AdaBoost algorithm, okay? The training set D is X is the attribute, Y1 is the neighbors, classification neighbors. So you gave an initial weight of those N values, and okay, each one of them is 1 over N. So each sample is 1 over N, right? Okay? So then what you do is you fit a classifier MI based on the weighted error function. So because when you do the errors, when you do the errors, you gave a weighted error function. So for example, so the number of values, okay? So give the weight, the Jm, okay, is equal sigma N from N1 to N, and weight on which that instant MI on XN is not equal to YN. Okay, so basically, it's a misclassified, okay? So you give the weight. So the total weight is a weighted of an individual error. So the error rate is Jm divided by the weight, okay? 

So you add all the weights together, so 1. So you're doing this, and if your error rate, okay, your weighted error rate is below certain threshold, you don't need to continue, okay? And also, and you can also, based on this error rate, to calculate the model I's, MI's vote as alpha I equals 1 over 2, so half of log N1 minus error rate divided by error. So this is their model's voting power, okay? Then, after that, you train the next block, okay? Say, after you train the two, you train the, run the three. So your weight is the previous rate times exponential of alpha I times I of other MI that misclassified. Okay, so wherever misclassified, you increase the weight by exponential of I, only on that weight. 

The other weights will not change, okay? Increase that. So after you apply these weight changes, and your training model finally gets all the k models are ready, or, you know, and then your final model will be based on the alpha 1 to N, because each one of them has an alpha, okay? So that's voting rate, okay? You know, they have their own error rate. They have their own weight power, okay? So this is called the Adaboom. 

Now, let's look at an example, a typical example of boosting called the add-up boosting, okay? So for the add-up boosting algorithm, you assign the initial weights to each training tuple. For example, you assign an equal weight. At this moment, you don't know which which tuple will be more important or which tuple is hard to be classified, right? And then, so you have a w1, w2, wn, whatever, okay? And then train the first model. So, because you train the base classifier on the weighted data set, right?  Play video starting at :1:8 and follow transcript1:08 You assign the weights to each training sample, okay? Or you can also, so you give the weight, then you train with the whole data set. Or what you can do is you sample the original data set based on certain weight distribution as well, right? So either way, this is a two different weighted strategy. But the most popular one is that I just assign weights to each training example and then when you build the model, use the weight of the example to build the model, okay? The base model. So you get a base model number one. Then you evaluate base model number one, okay? And then after you evaluate, some of them will be misclassified. So some tuples are misclassified. In that case, you need to increase the weight on those, okay? Increase the weight on those. Then your next training sample, training model, next base model will focus on that.  Play video starting at :2:35 and follow transcript2:35 And then you continue going like that. Eventually, going through a series of these, each one of them has an accuracy value. Then you take the majority vote, but based on the weight. So M1's opinion is weighted by its accuracy, test accuracy, okay? So you get the final result. So this is the AdaBoost algorithm, okay? The training set D is X is the attribute, Y1 is the neighbors, classification neighbors. So you gave an initial weight of those N values, and okay, each one of them is 1 over N. So each sample is 1 over N, right? Okay? So then what you do is you fit a classifier MI based on the weighted error function. So because when you do the errors, when you do the errors, you gave a weighted error function. So for example, so the number of values, okay? So give the weight, the Jm, okay, is equal sigma N from N1 to N, and weight on which that instant MI on XN is not equal to YN. Okay, so basically, it's a misclassified, okay? So you give the weight. So the total weight is a weighted of an individual error. So the error rate is Jm divided by the weight, okay?  Play video starting at :4:57 and follow transcript4:57 So you add all the weights together, so 1. So you're doing this, and if your error rate, okay, your weighted error rate is below certain threshold, you don't need to continue, okay? And also, and you can also, based on this error rate, to calculate the model I's, MI's vote as alpha I equals 1 over 2, so half of log N1 minus error rate divided by error. So this is their model's voting power, okay? Then, after that, you train the next block, okay? Say, after you train the two, you train the, run the three. So your weight is the previous rate times exponential of alpha I times I of other MI that misclassified. Okay, so wherever misclassified, you increase the weight by exponential of I, only on that weight.  Play video starting at :6:31 and follow transcript6:31 The other weights will not change, okay? Increase that. So after you apply these weight changes, and your training model finally gets all the k models are ready, or, you know, and then your final model will be based on the alpha 1 to N, because each one of them has an alpha, okay? So that's voting rate, okay? You know, they have their own error rate. They have their own weight power, okay? So this is called the Adaboom.  Now, let's look at an example, a typical example of boosting called the add-up boosting, okay? So for the add-up boosting algorithm, you assign the initial weights to each training tuple. For example, you assign an equal weight. At this moment, you don't know which which tuple will be more important or which tuple is hard to be classified, right? And then, so you have a w1, w2, wn, whatever, okay? And then train the first model. So, because you train the base classifier on the weighted data set, right?  Play video starting at :1:8 and follow transcript1:08 You assign the weights to each training sample, okay? Or you can also, so you give the weight, then you train with the whole data set. Or what you can do is you sample the original data set based on certain weight distribution as well, right? So either way, this is a two different weighted strategy. But the most popular one is that I just assign weights to each training example and then when you build the model, use the weight of the example to build the model, okay? The base model. So you get a base model number one. Then you evaluate base model number one, okay? And then after you evaluate, some of them will be misclassified. So some tuples are misclassified. In that case, you need to increase the weight on those, okay? Increase the weight on those. Then your next training sample, training model, next base model will focus on that.  Play video starting at :2:35 and follow transcript2:35 And then you continue going like that. Eventually, going through a series of these, each one of them has an accuracy value. Then you take the majority vote, but based on the weight. So M1's opinion is weighted by its accuracy, test accuracy, okay? So you get the final result. So this is the AdaBoost algorithm, okay? The training set D is X is the attribute, Y1 is the neighbors, classification neighbors. So you gave an initial weight of those N values, and okay, each one of them is 1 over N. So each sample is 1 over N, right? Okay? So then what you do is you fit a classifier MI based on the weighted error function. So because when you do the errors, when you do the errors, you gave a weighted error function. So for example, so the number of values, okay? So give the weight, the Jm, okay, is equal sigma N from N1 to N, and weight on which that instant MI on XN is not equal to YN. Okay, so basically, it's a misclassified, okay? So you give the weight. So the total weight is a weighted of an individual error. So the error rate is Jm divided by the weight, okay?  Play video starting at :4:57 and follow transcript4:57 So you add all the weights together, so 1. So you're doing this, and if your error rate, okay, your weighted error rate is below certain threshold, you don't need to continue, okay? And also, and you can also, based on this error rate, to calculate the model I's, MI's vote as alpha I equals 1 over 2, so half of log N1 minus error rate divided by error. So this is their model's voting power, okay? Then, after that, you train the next block, okay? Say, after you train the two, you train the, run the three. So your weight is the previous rate times exponential of alpha I times I of other MI that misclassified. Okay, so wherever misclassified, you increase the weight by exponential of I, only on that weight.  Play video starting at :6:31 and follow transcript6:31 The other weights will not change, okay? Increase that. So after you apply these weight changes, and your training model finally gets all the k models are ready, or, you know, and then your final model will be based on the alpha 1 to N, because each one of them has an alpha, okay? So that's voting rate, okay? You know, they have their own error rate. They have their own weight power, okay? So this is called the Adaboom.  Now, let's look at an example, a typical example of boosting called the add-up boosting, okay? So for the add-up boosting algorithm, you assign the initial weights to each training tuple. For example, you assign an equal weight. At this moment, you don't know which which tuple will be more important or which tuple is hard to be classified, right? And then, so you have a w1, w2, wn, whatever, okay? And then train the first model. So, because you train the base classifier on the weighted data set, right? : Added to Selection. Press [⌘ + S] to save as a note Like Dislike Report an issue : Added to Selection. Press [⌘ + S] to save as a note Like Dislike Report an issue : Added to Selection. Press [⌘ + S] to save as a note