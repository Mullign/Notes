So let's get to some computation here. Now given this confusion matrix, so you can see the sensitivity is the true positive over the actual positive. True predict the positive over the total ground truth positive, so 90 over 300. And specificity is the true negative rate over the actual negative rate. Accuracy is true positive plus true negative over the total. Error rate is the false negative plus false positive divided by the total. It is also the 1 minus accuracy. 

The precision is the true positive over the predicted positive. OK, recall is the same as sensitivity. F1 measure is 2 times precision times record divided by precision plus record, as in this, OK? OK, so you can see those are the results, OK? And F1 is a single value including the P and R. Well, so we get all the matrix so far, OK?