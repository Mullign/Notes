Let's start with our matrix, OK? So for a binary classification, we can use a confusion matrix to represent the classification test result, OK? So in this confusion matrix, each row represents the actual ground truth, OK? And then each column represents the predicted classes. So for example, for class 1, right? So your prediction says, given the class 1, you predict it as a class 1. Then it is true positive. 

So if for class 1, you predict it as not class 1, then that's called the false negative because the ground truth says this is a class 1. So on the other hand, for all those not class 1 results, if you predict it as class 1, your prediction value is a class 1, then they are false positive. And then if you predict the not class 1 case as not class 1, then it is true negative, OK? So in the confusion matrix, if you have m classes, that Cmij, actually the count of mij indicates the number of tuples in class i that were enabled by the classifier as class j, OK? So to be more clear, you may add an extra row or column to provide the total, OK? So let's see why we want to provide this total here, OK? Look at the bottom table here. 

And so as we said, each row represents the ground truth, right? So you have a pre-golf or not pre-golf. So the ground truth is in this population. We have 7,000 pre-golf, OK? And then 3,000 not pre-golf. Then on each column, the prediction, OK? So among these 7,000, we predicted 6,954 pre-golf. 

So this is called the true positive, OK? You predict the positive, and they are actually positive. And then you predict the 46,000 not pre-golf. So you predict the not positive, but they are actually positive. So it's called the false negative, OK? On the other hand, the second row, as you know, you have 3,000 negative cases, 3,000 people not pre-golf, not pre-golf. Then your prediction said, hey, we have 412 pre-golf. 

So that means you bring in the false positives because they are negative. On the other hand, you have negative 2,588. That is true negatives, OK? So with the total, it is much clearer on the values, OK? And also, with the total, it is easier to visualize the accuracy, OK? For example, if you take the accuracy, that means for the positive cases, if you predict it as yes, then that's the true positive. And for the negative cases, if you predict that's negative, that's the true negative. 

So the accuracy is the true positive plus true negative divided by this total, right? Total is 10,000. OK, so let's get to the definition part. 

Let's start with our matrix, OK? So for a binary classification, we can use a confusion matrix to represent the classification test result, OK? So in this confusion matrix, each row represents the actual ground truth, OK? And then each column represents the predicted classes. So for example, for class 1, right? So your prediction says, given the class 1, you predict it as a class 1. Then it is true positive.  Play video starting at :1:19 and follow transcript1:19 So if for class 1, you predict it as not class 1, then that's called the false negative because the ground truth says this is a class 1. So on the other hand, for all those not class 1 results, if you predict it as class 1, your prediction value is a class 1, then they are false positive. And then if you predict the not class 1 case as not class 1, then it is true negative, OK? So in the confusion matrix, if you have m classes, that Cmij, actually the count of mij indicates the number of tuples in class i that were enabled by the classifier as class j, OK? So to be more clear, you may add an extra row or column to provide the total, OK? So let's see why we want to provide this total here, OK? Look at the bottom table here.  Play video starting at :2:50 and follow transcript2:50 And so as we said, each row represents the ground truth, right? So you have a pre-golf or not pre-golf. So the ground truth is in this population. We have 7,000 pre-golf, OK? And then 3,000 not pre-golf. Then on each column, the prediction, OK? So among these 7,000, we predicted 6,954 pre-golf.  Play video starting at :3:30 and follow transcript3:30 So this is called the true positive, OK? You predict the positive, and they are actually positive. And then you predict the 46,000 not pre-golf. So you predict the not positive, but they are actually positive. So it's called the false negative, OK? On the other hand, the second row, as you know, you have 3,000 negative cases, 3,000 people not pre-golf, not pre-golf. Then your prediction said, hey, we have 412 pre-golf.  Play video starting at :4:18 and follow transcript4:18 So that means you bring in the false positives because they are negative. On the other hand, you have negative 2,588. That is true negatives, OK? So with the total, it is much clearer on the values, OK? And also, with the total, it is easier to visualize the accuracy, OK? For example, if you take the accuracy, that means for the positive cases, if you predict it as yes, then that's the true positive. And for the negative cases, if you predict that's negative, that's the true negative.  Play video starting at :5:12 and follow transcript5:12 So the accuracy is the true positive plus true negative divided by this total, right? Total is 10,000. OK, so let's get to the definition part.  Let's start with our matrix, OK? So for a binary classification, we can use a confusion matrix to represent the classification test result, OK? So in this confusion matrix, each row represents the actual ground truth, OK? And then each column represents the predicted classes. So for example, for class 1, right? So your prediction says, given the class 1, you predict it as a class 1. Then it is true positive. : Added to Selection. Press [⌘ + S] to save as a note Like Dislike Report an issue : Added to Selection. Press [⌘ + S] to save as a note