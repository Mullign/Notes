In this module, we'll study classification and the selected methods. First, let's get to the basic concepts. Okay, so what is classification? Classification is a supervised learning. So supervised means there is a supervision process. So we must have training data, such as observations or measurements are accompanied by labels to indicate the classes which they belong. So new data is classified based on the model that was built from the training set. 

So we have a training. The training process will build the model. And then we use the test instances to fit into this built model and to predict its classes. Okay, so predict the class label. So, for example, on this left corner table, you have the training data. It is already labeled with play, golf, okay, no, and yes. So the variables or attributes outlook, temperature, humidity, and the windy. 

And decide whether you play golf or not. So we want to train a model based on this training data. Okay, and then later on when a new data comes in, include outlook, temperature, humidity, and windy without the label. Without the label, and then you want to fit those variables into the model. And then the model will produce a label as an outcome. The label is either a no or yes, based on those other attributes or variables. So this is called the supervised learning. 

And of course, there is another learning process called unsupervised learning. We will discuss later on in the cluster analysis. So the unsupervised learning, there is no training data set. Okay, no labels for the training data. Okay, or there's no training data at all. So you only have a set of observation or measurement. You want to establish the possible existence of classes or clusters in the data. 

So, of course, classification is trying to predict the categorical class label. So the class labels in general are discrete or nominal. So to construct a model based on this training set and a class label, then later on use this model to classify the new data. A second problem is for numerical prediction. Instead of deciding the classes or determining the class labels, you may want to model a continuous value of the functions to predict or unknown or missing value. For example, you predict the temperature of a five-day snack or predict the stock value after a year. So those are called numerical predictions. 

And they are similar problems. One will decide the class labels. It is a categorical or discrete label. And another is a continuous value where you predict a value. In this module, we will focus on the classification. Now, the classification involves three phases. The first phase is model construction and training. 

So model construction means we want to decide what kind of model we want to build. And then secondly, we want to train the model to recognize the training sample. So that it learns from the training sample to adjust the parameters of the model. So then the next phase is called validation and testing. So testing is trying to estimate the accuracy of the model. So you use the test sample. Now, this test sample must have a label. 

So you cannot say, oh, test the sample without a label. That's the prediction. So you need the test sample with the set of variables which has the fit and then existing labels. Those satisfy those steps as a ground truth. Then we feed those other variables into the model and then predict the label. And then we compare the label with the non-label to see if they are the same. So if we find they predict the same label every time, then the accuracy is 100%. 

So accuracy is percent of test samples that are correctly classified by your model. Make sure that test set is independent of the training set. Now, validation is a test set. And if the validation is used to select or refine the models, we call it validation. Basically, it is in the development phase. Well, you are not so sure about your model. You want to adjust the parameter or you want to select. 

So if you build a model and they have a set of parameters, you don't know which parameter set are the best. In that case, you use the test set to do a select, select different parameter or adjust the parameter. Or you use the algorithm to dynamically adjust the parameter and then continue feeding this test set into this new parameter based models. And then come up with a different accuracy and then you choose the best one, right? So that's called validation. After you choose the model, you decide the accuracy is acceptable. Then you can deploy model into the real application to classify the new data.