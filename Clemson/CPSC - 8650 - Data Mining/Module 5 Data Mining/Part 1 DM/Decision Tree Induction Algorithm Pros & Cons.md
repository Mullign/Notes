If you look at this decision tree algorithm, it is easy to explain because, say, Outlook is which condition, so you can come up with rules. Outlook is a code, Outlook is a forecast, or Outlook is raining, and then the windy condition, we are not going to play. So for anybody can understand based on that tree. Also, it is easy to implement because you just need to test the variables attributes. Then now the key is the criteria to decide which variable to test. There are many algorithms to implement it. Also, the decision process is very efficient. 

Also, can follow the missing data. If certain attribute is missing but there are other branches, you can still do the decision. So to the user, it is a white box. All the decision is laid out to the user. Unlike some black box classifier, where you feed in the data, you don't know what happens inside the black box, and then you get an output. So there's no way you can reason which attributes decide what. Then also, there's no normalization need for the data because the data can be in original form. 

Also, you don't need to make any assumption on the data distribution. You don't have to make this assumption attribute independency because you're doing that by each variable attributes individually make the decision, and then the decision can be combined because after you use the attribute A, make a decision, and then still can know that you can combine attribute B to do the same thing in the next level, right? So it can work with the category or nominal data as well as continuous data. That is the problem. The problem is it is unstable, okay? The reason it is unstable is because it's sensitive to noise. For example, when you do the splitting, a noise data may affect your splitting point, or when you make some adjustment, some splitting, like some outline will affect the decision tree branch a lot, okay? 

And sometimes when your data is dirty, the accuracy may not be good enough. And finding the optimal splitting is an NP problem, okay? So we want to use the usually grading algorithms I use. And so because as long as you have attributes, you continue to build a test, so you exhaustively test all the branches, all the attributes on each test. So you may have the overfitting problem, okay, in which your decision trees are very good on the training dataset because you test all the possible situations in the training data. So your tree is adapted to the training data. But when you have new data coming in, it may not be able to decide. 

So it causes some overfitting problems. Okay, so those are the problems, cons for this decision tree algorithm. Okay, so, so far, we still not answer the question, how do you split, or how do you decide which one we want to test, right?