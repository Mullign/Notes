![[Mining Frequent Itemsets and Association Rules Slides.pdf]]Mining frequent items and associated rules, that is, first of all, we need to give it a minimum support, and then secondly, we need a minimum confidence, okay? So our goal is to find all the rules such that satisfy these two conditions, the support level and the confidence level are all greater than or equal to minimum support and minimum confidence, and then we can have that rule. For example, if we have a minimum support of 50%, right, and then we also assume that the minimum confidence level is 50%, then we have frequent one-item set, right, beers, and then frequent two-item set, beer and a diaper, right? So those are frequent, no others, and we already in the previous slides we discussed that they are all comprehensive, right? So based on those, for the one-item set, we don't have a rule, or like association rule, but for two-item set, we have the association rules, right, like beer, and buy beer, and we'll buy diaper, or buy diaper, we'll buy beer. Now what is their confidence level, okay? So we need to calculate another metric, which is confidence level. 

The confidence level is for beer, diaper is equal to beer, diaper together, the support of a beer and a diaper divided by the support of beer, so the three divided by three equal to 100%, so the confidence level is 100%, and on the other hand, beer and diaper is three divided by the diaper, four is 75%, right? So the question, are those rules, two rules, satisfy those two conditions? Of course it is, they are, all right, because 50%, 50%, there's a 75% and 100%. So mining association rules and the mining frequent patterns are very close problem. If we can mine the frequent patterns, then if we can mine the frequent patterns, then it is easy to derive the association rules, right? The big question is that we need to find the scalable method to mine the large dataset. In this small database, we can visually identify those frequent item set, right? 

So for this database, first of all, it contains only five transactions, and then the largest transaction only contains five items. What if you have a transaction contain 100 items? Think about how many K item set, or one item, two item, three item, four item goes on, right? So how do you list all of them, or store all the item set together, right? So that's a big question. Look at this, right, look at this transaction, right? So we're looking at, we're not looking at a large number of transactions, we just look at this database that has two transactions. 

However, these two transactions contain, one has a 1 to 50 items, another transaction contains 1 to 100 transactions, right? So a non-pattern actually contains a combinatorial number of sub-patterns, right? For example, the pattern A1 to A100, it actually contains so many combinations of sub-patterns, whether 1, just take a 1, so 100 combined, 1, 100, 2, 100, 3, 100, 100, right? It's total of 2, 2 to the power of 100 minus 1, different number of item set. Given the minimum support equal to 1, then all those combinations are all frequent items. Then if we wanted to store and list all of them, then there'll be a big storage space, and also the computation power is huge, right? So that's our big problem. 

Okay, so to handle such a challenge, we can introduce another compressed form of pattern, we call it closed pattern. So the closed pattern basically is identify the largest pattern, okay, at the same support level. Okay, so a pattern x is closed if x, first of all, there must be frequent, because when we mind association rules or frequent items set, we are only interested in the frequent pattern, we are not interested in the infrequent type. So given the minimum threshold of x on that db1, and then there are two closed patterns. The first one is the p1, and the support level of 2. So because with the closed pattern is defined as a pattern is closed if x is frequent, right? So p1 is frequent, and then there exists no super pattern y include x with the same support of x. 

So p1 is a1 to a50. That pattern is closed pattern because it has a support level of 2, no other pattern that supersedes this, or a superset of a1 to a50, and has more support level. Okay, however, you have another pattern p2 as well. p2 is a1 to a100 at the threshold of 1, which is also a support level of 1 is also above the minimum support threshold. So this is another closed pattern. You can see this is those are two closed patterns, okay? And with those two closed patterns, it contains all the frequent patterns with all the support levels, okay? So closed pattern is a lossless comparison of frequent patterns, okay? 

So because it reduces the number of patterns but does not lose the support information because the support level information is still there, right? Because we can still tell that a2 and a5, a40 support level is 2, and then we can still tell that a5 and a51, the support level is only 1. So the support level information does not lose. So we call it a compression form of pattern, closed pattern, but the compression, the lossless compression of frequent patterns. And talking about lossless compression, what is the lossless compression? We may have another solution, okay? Another solution is the max pattern. So you can see in that transaction database, we have two patterns, closed patterns, okay? P1 is a1 to a50 as a 2 support level, a1 to a100 as 1 support level, right? 

So a pattern x is a max pattern. If x is frequent, there exists no frequent super pattern y greater than x. So we are not limiting that they have the same support level. So in such a case, we have two transactions. We have two patterns, a1 to a50 is the largest pattern, right? But another pattern P2 is a1 to a100. a1 to a100 is a frequent pattern. So a1 to a100 is greater than, is a superset of a1 to a50. So we use a1 to a100, okay? This is called the max pattern. So in this max pattern, we actually, we know a1 to a40 is frequent. So if we use a1 to a max pattern to represent all the frequent patterns, we know a1 to a50, a40 is frequent. But we do not know how many support, support, okay, the real support level of a1 to a40, right? So in many applications, maybe closed pattern is more desirable than mining the max pattern. Mining the max pattern, you can find the frequent item. But mining the closed pattern, you will find the support level, which will give you more information and then you can derive more information from this, right? 

So, okay, so far we have all the, all the basic concepts and discussed. 

Mining frequent items and associated rules, that is, first of all, we need to give it a minimum support, and then secondly, we need a minimum confidence, okay? So our goal is to find all the rules such that satisfy these two conditions, the support level and the confidence level are all greater than or equal to minimum support and minimum confidence, and then we can have that rule. For example, if we have a minimum support of 50%, right, and then we also assume that the minimum confidence level is 50%, then we have frequent one-item set, right, beers, and then frequent two-item set, beer and a diaper, right? So those are frequent, no others, and we already in the previous slides we discussed that they are all comprehensive, right? So based on those, for the one-item set, we don't have a rule, or like association rule, but for two-item set, we have the association rules, right, like beer, and buy beer, and we'll buy diaper, or buy diaper, we'll buy beer. Now what is their confidence level, okay? So we need to calculate another metric, which is confidence level.  Play video starting at :1:54 and follow transcript1:54 The confidence level is for beer, diaper is equal to beer, diaper together, the support of a beer and a diaper divided by the support of beer, so the three divided by three equal to 100%, so the confidence level is 100%, and on the other hand, beer and diaper is three divided by the diaper, four is 75%, right? So the question, are those rules, two rules, satisfy those two conditions? Of course it is, they are, all right, because 50%, 50%, there's a 75% and 100%. So mining association rules and the mining frequent patterns are very close problem. If we can mine the frequent patterns, then if we can mine the frequent patterns, then it is easy to derive the association rules, right? The big question is that we need to find the scalable method to mine the large dataset. In this small database, we can visually identify those frequent item set, right?  Play video starting at :3:41 and follow transcript3:41 So for this database, first of all, it contains only five transactions, and then the largest transaction only contains five items. What if you have a transaction contain 100 items? Think about how many K item set, or one item, two item, three item, four item goes on, right? So how do you list all of them, or store all the item set together, right? So that's a big question. Look at this, right, look at this transaction, right? So we're looking at, we're not looking at a large number of transactions, we just look at this database that has two transactions.  Play video starting at :4:36 and follow transcript4:36 However, these two transactions contain, one has a 1 to 50 items, another transaction contains 1 to 100 transactions, right? So a non-pattern actually contains a combinatorial number of sub-patterns, right? For example, the pattern A1 to A100, it actually contains so many combinations of sub-patterns, whether 1, just take a 1, so 100 combined, 1, 100, 2, 100, 3, 100, 100, right? It's total of 2, 2 to the power of 100 minus 1, different number of item set. Given the minimum support equal to 1, then all those combinations are all frequent items. Then if we wanted to store and list all of them, then there'll be a big storage space, and also the computation power is huge, right? So that's our big problem.  Play video starting at :6:15 and follow transcript6:15 Okay, so to handle such a challenge, we can introduce another compressed form of pattern, we call it closed pattern. So the closed pattern basically is identify the largest pattern, okay, at the same support level. Okay, so a pattern x is closed if x, first of all, there must be frequent, because when we mind association rules or frequent items set, we are only interested in the frequent pattern, we are not interested in the infrequent type. So given the minimum threshold of x on that db1, and then there are two closed patterns. The first one is the p1, and the support level of 2. So because with the closed pattern is defined as a pattern is closed if x is frequent, right? So p1 is frequent, and then there exists no super pattern y include x with the same support of x.  Play video starting at :8:2 and follow transcript8:02 So p1 is a1 to a50. That pattern is closed pattern because it has a support level of 2, no other pattern that supersedes this, or a superset of a1 to a50, and has more support level. Okay, however, you have another pattern p2 as well. p2 is a1 to a100 at the threshold of 1, which is also a support level of 1 is also above the minimum support threshold. So this is another closed pattern. You can see this is those are two closed patterns, okay? And with those two closed patterns, it contains all the frequent patterns with all the support levels, okay? So closed pattern is a lossless comparison of frequent patterns, okay?  Play video starting at :9:19 and follow transcript9:19 So because it reduces the number of patterns but does not lose the support information because the support level information is still there, right? Because we can still tell that a2 and a5, a40 support level is 2, and then we can still tell that a5 and a51, the support level is only 1. So the support level information does not lose. So we call it a compression form of pattern, closed pattern, but the compression, the lossless compression of frequent patterns. And talking about lossless compression, what is the lossless compression? We may have another solution, okay? Another solution is the max pattern. So you can see in that transaction database, we have two patterns, closed patterns, okay? P1 is a1 to a50 as a 2 support level, a1 to a100 as 1 support level, right?  Play video starting at :10:51 and follow transcript10:51 So a pattern x is a max pattern. If x is frequent, there exists no frequent super pattern y greater than x. So we are not limiting that they have the same support level. So in such a case, we have two transactions. We have two patterns, a1 to a50 is the largest pattern, right? But another pattern P2 is a1 to a100. a1 to a100 is a frequent pattern. So a1 to a100 is greater than, is a superset of a1 to a50. So we use a1 to a100, okay? This is called the max pattern. So in this max pattern, we actually, we know a1 to a40 is frequent. So if we use a1 to a max pattern to represent all the frequent patterns, we know a1 to a50, a40 is frequent. But we do not know how many support, support, okay, the real support level of a1 to a40, right? So in many applications, maybe closed pattern is more desirable than mining the max pattern. Mining the max pattern, you can find the frequent item. But mining the closed pattern, you will find the support level, which will give you more information and then you can derive more information from this, right?  Play video starting at :13:3 and follow transcript13:03 So, okay, so far we have all the, all the basic concepts and discussed.  Mining frequent items and associated rules, that is, first of all, we need to give it a minimum support, and then secondly, we need a minimum confidence, okay? So our goal is to find all the rules such that satisfy these two conditions, the support level and the confidence level are all greater than or equal to minimum support and minimum confidence, and then we can have that rule. For example, if we have a minimum support of 50%, right, and then we also assume that the minimum confidence level is 50%, then we have frequent one-item set, right, beers, and then frequent two-item set, beer and a diaper, right? So those are frequent, no others, and we already in the previous slides we discussed that they are all comprehensive, right? So based on those, for the one-item set, we don't have a rule, or like association rule, but for two-item set, we have the association rules, right, like beer, and buy beer, and we'll buy diaper, or buy diaper, we'll buy beer. Now what is their confidence level, okay? So we need to calculate another metric, which is confidence level.  Play video starting at :1:54 and follow transcript1:54 The confidence level is for beer, diaper is equal to beer, diaper together, the support of a beer and a diaper divided by the support of beer, so the three divided by three equal to 100%, so the confidence level is 100%, and on the other hand, beer and diaper is three divided by the diaper, four is 75%, right? So the question, are those rules, two rules, satisfy those two conditions? Of course it is, they are, all right, because 50%, 50%, there's a 75% and 100%. So mining association rules and the mining frequent patterns are very close problem. If we can mine the frequent patterns, then if we can mine the frequent patterns, then it is easy to derive the association rules, right? The big question is that we need to find the scalable method to mine the large dataset. In this small database, we can visually identify those frequent item set, right?  Play video starting at :3:41 and follow transcript3:41 So for this database, first of all, it contains only five transactions, and then the largest transaction only contains five items. What if you have a transaction contain 100 items? Think about how many K item set, or one item, two item, three item, four item goes on, right? So how do you list all of them, or store all the item set together, right? So that's a big question. Look at this, right, look at this transaction, right? So we're looking at, we're not looking at a large number of transactions, we just look at this database that has two transactions.  Play video starting at :4:36 and follow transcript4:36 However, these two transactions contain, one has a 1 to 50 items, another transaction contains 1 to 100 transactions, right? So a non-pattern actually contains a combinatorial number of sub-patterns, right? For example, the pattern A1 to A100, it actually contains so many combinations of sub-patterns, whether 1, just take a 1, so 100 combined, 1, 100, 2, 100, 3, 100, 100, right? It's total of 2, 2 to the power of 100 minus 1, different number of item set. Given the minimum support equal to 1, then all those combinations are all frequent items. Then if we wanted to store and list all of them, then there'll be a big storage space, and also the computation power is huge, right? So that's our big problem.  Play video starting at :6:15 and follow transcript6:15 Okay, so to handle such a challenge, we can introduce another compressed form of pattern, we call it closed pattern. So the closed pattern basically is identify the largest pattern, okay, at the same support level. Okay, so a pattern x is closed if x, first of all, there must be frequent, because when we mind association rules or frequent items set, we are only interested in the frequent pattern, we are not interested in the infrequent type. So given the minimum threshold of x on that db1, and then there are two closed patterns. The first one is the p1, and the support level of 2. So because with the closed pattern is defined as a pattern is closed if x is frequent, right? So p1 is frequent, and then there exists no super pattern y include x with the same support of x.  Play video starting at :8:2 and follow transcript8:02 So p1 is a1 to a50. That pattern is closed pattern because it has a support level of 2, no other pattern that supersedes this, or a superset of a1 to a50, and has more support level. Okay, however, you have another pattern p2 as well. p2 is a1 to a100 at the threshold of 1, which is also a support level of 1 is also above the minimum support threshold. So this is another closed pattern. You can see this is those are two closed patterns, okay? And with those two closed patterns, it contains all the frequent patterns with all the support levels, okay? So closed pattern is a lossless comparison of frequent patterns, okay?  Play video starting at :9:19 and follow transcript9:19 So because it reduces the number of patterns but does not lose the support information because the support level information is still there, right? Because we can still tell that a2 and a5, a40 support level is 2, and then we can still tell that a5 and a51, the support level is only 1. So the support level information does not lose. So we call it a compression form of pattern, closed pattern, but the compression, the lossless compression of frequent patterns. And talking about lossless compression, what is the lossless compression? We may have another solution, okay? Another solution is the max pattern. So you can see in that transaction database, we have two patterns, closed patterns, okay? P1 is a1 to a50 as a 2 support level, a1 to a100 as 1 support level, right?  Play video starting at :10:51 and follow transcript10:51 So a pattern x is a max pattern. If x is frequent, there exists no frequent super pattern y greater than x. So we are not limiting that they have the same support level. So in such a case, we have two transactions. We have two patterns, a1 to a50 is the largest pattern, right? But another pattern P2 is a1 to a100. a1 to a100 is a frequent pattern. So a1 to a100 is greater than, is a superset of a1 to a50. So we use a1 to a100, okay? This is called the max pattern. So in this max pattern, we actually, we know a1 to a40 is frequent. So if we use a1 to a max pattern to represent all the frequent patterns, we know a1 to a50, a40 is frequent. But we do not know how many support, support, okay, the real support level of a1 to a40, right? So in many applications, maybe closed pattern is more desirable than mining the max pattern. Mining the max pattern, you can find the frequent item. But mining the closed pattern, you will find the support level, which will give you more information and then you can derive more information from this, right?  Play video starting at :13:3 and follow transcript13:03 So, okay, so far we have all the, all the basic concepts and discussed.  Mining frequent items and associated rules, that is, first of all, we need to give it a minimum support, and then secondly, we need a minimum confidence, okay? So our goal is to find all the rules such that satisfy these two conditions, the support level and the confidence level are all greater than or equal to minimum support and minimum confidence, and then we can have that rule. For example, if we have a minimum support of 50%, right, and then we also assume that the minimum confidence level is 50%, then we have frequent one-item set, right, beers, and then frequent two-item set, beer and a diaper, right? So those are frequent, no others, and we already in the previous slides we discussed that they are all comprehensive, right? So based on those, for the one-item set, we don't have a rule, or like association rule, but for two-item set, we have the association rules, right, like beer, and buy beer, and we'll buy diaper, or buy diaper, we'll buy beer. Now what is their confidence level, okay? So we need to calculate another metric, which is confidence level.  Play video starting at :1:54 and follow transcript1:54 The confidence level is for beer, diaper is equal to beer, diaper together, the support of a beer and a diaper divided by the support of beer, so the three divided by three equal to 100%, so the confidence level is 100%, and on the other hand, beer and diaper is three divided by the diaper, four is 75%, right? So the question, are those: Added to Selection. Press [⌘ + S] to save as a note Like Dislike Report an issue : Added to Selection. Press [⌘ + S] to save as a note Like Dislike Report an issue : Added to Selection. Press [⌘ + S] to save as a note