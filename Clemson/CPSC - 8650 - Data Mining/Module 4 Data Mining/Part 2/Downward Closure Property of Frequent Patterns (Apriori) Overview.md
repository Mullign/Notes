![[Downward Closure Property of Frequent Patterns (Apriori) Overview Slides.pdf]]
So first, what is done was property of frequent item type. So this is used to design the a-priority algorithm. If you look at it, from the observation if you have a frequent item set A1 to A50, and you know all the subsets are all frequent, okay? That's it. So the downwards closure, we also call it a-priority. Property of frequent patterns is given any frequent item set, all its subsets must be frequent, okay? So if we have a beer, diaper, not frequent, then beer and a diaper must be frequent, okay? 

And every transaction containing beer, diaper, not also contain diaper. So the efficient mining method will use this downward closure property, okay? So if any subset of an item set S is infrequent, then there's no chance of S itself to be frequent. So in such a situation, S should never be considered because its sub item is already identified as infrequent. So this is a way to not consider some longer patterns because its short pattern does not satisfy the frequent criteria, right? Okay, so this is a sharp knife for pruning. So you can remove some items from consideration before going further to do the combination for the larger item set. 

Okay, so the a-priority algorithm uses the a-priority pruning principle to improve the efficiency of brute force of just the least odd item, right? So if there is any item set which is infrequent, then its subset should not even generate. That's the main idea. And then also there are some other approaches, okay, like that utilize this a-priority pruning principle to further improve or at least provide a better solution than the original a-priority algorithm, such as Eclat FP growth, right? Okay, so let's first study the original a-priority algorithm. For the a-priority algorithm, okay, so the first step is to scan the database once to get all the frequent one item set. So you scan for each item set, you count the existence or occurrence of this one item set in the database, right? 

Then you repeatedly form the k item set, okay, k item set. So from one item set, you generate two item sets, okay? Okay, so you generate length k plus one candidate item set. Candidate item set is like formed for length k frequent item set. So what do you mean? You only count the frequent one item set, combine the frequent one item set to generate the one, two item set. Combine the two item, frequent two item set, generate the three item set. Combine the three item set, frequent three item set, and then generate the candidate for four item set. 

Then we test those candidates against the database. You go back to the database to see if those candidates, so now we are not testing any other combination that if frequent items formed, candidates will not be generated at all. So we only check those candidates against database to see if those k plus one item sets are frequent or not. Now if they are frequent, then they become the k plus one frequent item set, right? Then we continue row, okay, increase k by one and then continue until no frequent or candidate set can be generated. Then our return will contain all the frequent item set. This is complete. So we answered our question about complete set, okay.