![[Mining FP-Tree Slides.pdf]]So, we will be mining the FPTree now instead of going back to the database. We started with the, we gave it this calendar tree. We partitioned the tree based on this calendar pattern. So patterns contains P, for example, in the first leaf node and include F, C, A, M, they have a count of two, frequent of two, count of two. And then P here, you follow the P to this node, C, B is a one, okay? So and then P's conditional database, well, the database, the partition, the subset of the database under the condition that P exists, okay? And basically is the prefix passes of item P, that means F, C, A, M, C, B, okay? 

Then we can find M, started M is a leaf, right? We can go to M item, M is F, C, A, right? And also M is F, C, A, B, okay? P is this, F, C, A to F, C, A, B. And then we can go to the B, B has F, C, A, F and C. We can go to A, A has F, C, that's it. And then for C, we have F, C is F, okay? 

And then that C doesn't have it. And then this F and this C is already getting to the end, so there's no prefix. So the conditional database is given each item, okay? And what does this mean? Conditional database of each pattern is given an item C, A, B, M, P exists in the database and the above items also in the database. All right, so that's a conditional database. So we are partitioning the data, so we only focus on this. 

Then with this conditional database, okay? And we can mine immediately, mine the pattern, okay? For each conditional pattern, mine a single item pattern. And then construct the FPTree, and then we get P's conditional database. And then based on P's conditional database, we get these rules. And then we get B's conditional database, and then so we get P, M, B, A, okay? And then so you can see that each conditional database has their own FPTree, okay? 

And then we can derive those rules directly or frequent patterns directly from FPTree. For each branch, all the frequent patterns can be constructed. For example, M, we know M is three, and then we know FM is three, and then CM is three, AM is three, because there are three, right? So we can build it. So instead of going back to the database, we go to this conditional database to do the FP, directly derive the frequent item set, okay? And then we can, based on this, we can come up with the association rules. Now, a special case is that there is a single prefix in the FPTree, right? 

So pay, post, and there are several items share the same. We can further reduce that, replace that single prefix path into one node. That, because we store the prefix first, and then keep the one node, okay? So in that case, your mining result actually can carry two parts. The first part is the prefix, which is a single path. That path, the longer the path, the better for us to reduce the tree size, okay? And then we will concurrently use the result of each two paths together. 

So this is another special case to handle the single prefix paths. In this previous one, we don't have a single prefix. Here, but for example, if we don't have this B, C, B, C, B, P paths there, it is possible you have a F, C, A, and then M, P, and B, M. And then that, we have one, two, three, four, five, six, seven, seven nodes. But that single prefix path, single path can be reduced into one node, and we only need five nodes to build the tree, because they are single paths, okay? Store the tree. So we're mining these databases recursively and build the, for the special cases, we build, actually, one node to represent the prefix path. 

And then we mine the pattern, okay? So let's summarize, okay? So single frequent items, we find it, okay? Okay, and based on, okay, and then recursively grow the frequent pattern by doing that, each partition database, called the partition conditional base, okay? And we build the FPT, okay, to do this. And then we, our data mining becomes recursively constructed mining the FPTs, conditional FPTs, okay? And until the FPT is empty, because every time we take a conditional FPT, we take the branch out. 

And then, or until it contains only one path, then we generate the compile, you know, put them into one node. Then from that, all the, follow the paths, everything is a frequent pattern, or frequent item set, okay?