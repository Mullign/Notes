![[Mining Closed Patterns Slides.pdf]]Okay, so far, we are mining the frequent balance, okay? And as we discussed before, that closed item set, the largest item set is a compression of, it's called the noiseless compression of the frequent pattern. So there is an algorithm based on the pattern groups to directly mining the closed item, okay? So it is efficient, right? The idea is if a FPT contains a single branch as shown in the map, like A1 to A3, so A1 to A3 should be merged, okay? And so if Y appears in every occurrence Y, X, then Y is merged with X. That's the item merging approach, right? 

So instead of doing this, finding each individual frequent, smaller frequent pattern, it tried to find the closed item set. So what it does is, for example, in this right transaction table here, well, you have ACDE, AVE, CEFG, ACDEF, assume the minimum support is 2. So you build the F list. So based on the A is 3 times, C is 3 times, DA is 2 times, E is 3 times, F is 3 times. So the F list is A, C, E, F, D. Now what you do is, you do a projection on the database. Based on the D-projected database, we will contain the AC, D-projected database will contain the ACEF, okay, and the ACF, okay? 

And then you, ACF, ACEF, then you can see ACF and ACEF has a common ACF, okay, ACF. You can see this, this D-projected database contains the ACF, right, ACEF contains ACF. And then another item comes in, ACF. Then you take ACF as one, merge with D, become ACD-projected database, which contains only E now, okay? So your closed item set then is ACFD, which is 2, at the support, minimum support level of 2, because it's the longest item, okay? So that's how you find it. Now of course, for the details, you should go to this paper, and there are many other tricks given it, okay, and that to try to build a closed item set, because once you find a closed item set, you get a lossless compression of entire frequent items set for the entire database.