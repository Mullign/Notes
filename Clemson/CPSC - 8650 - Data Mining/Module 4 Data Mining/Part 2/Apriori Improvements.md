![[Apriori Improvements Slides.pdf]]Okay, so after the first algorithm to discover the frequent item set, a priori algorithm published, there are many more papers published to try to improve the original a priori algorithm. Okay, so we will get to those different ideas and alternative approaches. Now, the first approach is to try to reduce the passes of transaction database scans, right? So we will discuss one partitioning algorithm. Then the next approach is try to shrink the number of candidates and we will discuss another algorithm called the direct hash-based approach. Okay, now there are other approaches such as dynamic item set counting, right? By priori counting the item set, when one item set is finished or like a lower level item set finishes, you start the higher level item set because the item set becomes frequent. 

So that's dynamic counting, okay? Or you do borrowing by support lower boundary or you use sampling to shrink the number of candidates. Now, of course, for some special data, you can use the tree projection h minor hypercube decomposition to explore the special data structures to help to improve the original a priori algorithm, okay? So let's get to the first improvement of a priori algorithm partitioning, right? So the theorem is that if any item is potentially frequent in the entire transaction database, it must be frequent in at least one of the partitions of TDB, right? So here is the proof, okay? For example, if we take the database partition them into k partitions, right? So then if the supported level of x is less than the minimum threshold, that's the relative threshold, okay? Times the total size, then you add them together, the support level of x must be less than the minimum threshold of the entire database, right? Because it's just a proportion. So they must be at least be above that support level, okay? Maybe TDB2, maybe TDBi, at least one, okay? So that's the theory. Based on this theory, so what we can do is we can scan the database tries to find the frequent item set. The first scan is to partition the database so that each partition can fit in the main memory. Why do we need to fit in the memory? 

Because we want to find the frequent item set within that partition, okay? We want to find the frequent item set within the partition. If we cannot fit in the memory, then we have to read the database tries many times. Because within the partition, we need to scan several times to find the frequent item set. Even though they are small, but we still need to run the priority algorithm to go through several phases. But if they are in the memory, then just doing the study from one item set to two item sets, and then it goes on, goes on, we can find all the item sets, right? So for those local frequent patterns in this current partition, then we keep them, use them as the candidate, right? 

And then we can connect all the candidates from all the partitions. Then we try to consolidate into the frequent pattern. So we will go to the entire database to see if any one of those frequent item set in the local partition is also frequent in the global, okay? We have to scan one more time to count, okay? And by counting this, we will get there. First, we get a true frequency, okay? Or we also get the confidence level if we need to drive the association rules, okay? 

So this is a partition-based approach. There's another approach called direct hashing and pruning. So what it does is you hash the item set. You take every item set, you hash it into the bucket, okay, using the hash function. So for different item sets, you may actually hash into the same bucket, okay? For example, A, B, A, D, C, E were hashed into the same bucket, okay? Now, what we can do is we at first scan, we count one item set, okay? 

And then, based on one item set, we hash that two item set to calculate the bucket count. We are not calculating individual count. We hash it, so in the bucket, we calculate the bucket count. Calculate the bucket count. So the total bucket count, right, if the total bucket count exceeds the threshold, right, then we will keep it. We will keep it. If it is less than, we will prune it. 

Okay. Because if a K8 item set cannot be frequent, if its corresponding hash bucket count is below the minimum threshold, because that hash bucket has different item sets, right? If it's like three different item sets, their total count is less than the threshold, they should not be, okay? So by doing this, we may prune another item set. You know, for the more details, you can go to the DHP paper published in the SIGMOD 1995. So this is another way to reduce the candidate's number, okay?