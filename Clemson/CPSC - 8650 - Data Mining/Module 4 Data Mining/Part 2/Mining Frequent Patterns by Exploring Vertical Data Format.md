![[Mining Frequent Patterns by Exploring Vertical Data Format Slides.pdf]]And there's another paper trying to explore the vertical data format called Econet. The Econet format is called Equivalence Cost Transformation. It uses a depth-first algorithm to identify the frequent patterns. So what it does is it transforms the original database from a horizontal data format into a vertical data format. So the horizontal data format starts with transaction ID with the transaction's items. What it does is you transform it into starting with each item, then find the item in the transaction ID. For example, A in transaction 10 and 20, then we will have A with a transaction ID 10, 20. 

So you will have, instead of you have a transaction ID item B, you have the item name and transaction ID. So you can see, for example, for E, we have 10 transactions contain E. So E is 10, 20, three transactions are contained. So E has 10, 20, 30. So by doing that, we have this transaction ID. So that item count equals to number of transaction ID. So the size will tell the count. 

Based on this, we will immediately know that A, so if we gave a threshold of three, right? Then we know one item set, E, is the frequent. If you give a minimum threshold of two, then A, B, C, E are all frequent, right? So there are some properties that give you this, for example, if transaction X and transaction item, okay, if item set X and Y in a transaction set, TX equal to TY means X and Y always happen together. For example, if you look at T, okay, so look at TAC and TD. They all happen together in 10. Then you have TAC equal to TD, okay? 

And also another property is that TX is included in TY. That means transaction having X always has Y. For example, TAC, okay, is included in TCE, but TCE included 10 and 30, and then TAC is only 10, right? So we have these kind of properties, okay? So to find the frequent patterns, what we do is basically is to find the, you know, the one with the largest transaction IDs, okay? So the frequent pattern might become a vertical intersection, okay? Now, to accelerate the mining, because we are not going to list all the different TAC, TAE, TAE, TAE, right? 

For example, TEA, TEA equals T10, T20, T30, TA equals T10, T20. So instead of, then we have a TAE equals T10, T20. So those are the intersections, right? Well, instead of keep the intersection, we can only keep the div set, because div set is the intersection. Intersection is the main set equal to the main set minus the difference. So we use the difference, okay? Actually, if you go to the set, and actually the set intersection can be defined by a minus, okay, operation. 

So we only keep the div set, okay? And in this situation, for example, if you have a TE, and then TCE, and then div set of CE and E is T20. And for this one, the div set of TE and TAE is, div set is T30, okay? So, doing this, we can grow our, find out our frequent item set at a different length, okay?