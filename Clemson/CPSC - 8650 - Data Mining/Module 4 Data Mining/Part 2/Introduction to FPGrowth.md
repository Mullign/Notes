![[Introduction to FPGrowth Slides.pdf]]And there's another approach that does not even use the count by counting those frequent item sets. It tries to grow the frequent patterns directly. And then it is a breadth-first search mining algorithm for the F-priority, because you do go in, scan once, first find or complete the first frequent K item set, and then derive from the K item set to derive the K plus 1 item set candidate, and then scan. So this approach requires a lot of computation, as well as access the database so many times. That is why we use different methods to try to improve this efficiency of F-priority algorithms, such as reduce the passes, reduce the candidates, or use the vertical set, vertical data format. Can we mine the patterns directly with a depth-first search mining algorithm? For example, if we identify as a frequent item P, can we just search the frequent item that contains P, and only within those transactions that contains P? 

So such thinking leads to a frequent pattern growth approach called FD growth. That means they will find the frequent item set P first, and then you grow those frequent patterns based on those transactions that contain only P. Well, how do you do that? So this is their algorithm, okay? And first of all, given the transaction ID, the transactional database like this, right? So it scans the database first, it will find the single-item frequent pattern, where each item, so for example, F appears four, and A appears three times, and then P appears three times, okay? And then you get this. 

And after that, so basically it finds one item set, okay? Then we're only given a minimum support of three, so we want to find the frequent one item set. So we use the F, A, C, B, M, P, then we sort these. We sort this frequent one item set, okay? In a descending order, we call it a F list, so we get F, C, A, B, M, P, and then we scan the database again. In this time, in this scanning, when we scan, we will build a FP tree. We will take this F list to try to match into each transaction, each transaction, okay? 

For example, F, C, A, F, C, A, B, M, P, right? So we will take this, try to map into this transaction 100. You can see B is not there. So well, then we will get F, C, A, M, P is item D, items, items, items, frequent item, one item. So that is, and then we use that to build a branch from F, C, A, M, P, a branch of this FP tree. Then we scan, get another transaction, 200. It contains F, C, A, B, M, then we build another branch. 

For those with the same, for F, C, A, they have the same item Bs, then we merge them with an incremented count. So let's get to that part, okay?