![[Construct FP-Tree Slides.pdf]]Okay, so we already get this, right? From this, we come up with those ordered frequent item deeds. So this is the item in the transaction, and then we get the deeds. Because those are based on FRIST, and then we take it out. So with these deeds, we will build our FPTree. We start, okay, for each transaction, insert the ordered frequent item deeds into FPTree with shared sub-branches merged, and accumulate their counts. Okay, so that's what I do. 

To do that, we will use a head table. This head table contains the item, one item, and then frequency in order. And then there is a header pointer to each tree node, where you can trace the F list. For example, F item, you can trace, follow the header. Let's go get it, okay? So we build a header table. Now, then we take the first transaction ID, then we build the first branch, FCAM, okay? 

After you set the first frequent item deed set, we get, and then now we will build a header from the header pointing to FCAV. And then on the side, you have the account, okay? Okay, then you go to the next 200 transactions, FCABM. After you set the second frequent item deed, FCABM, you will get, basically, you insert FCABM. Now, FCA, you can insert at this root node, from the root. Root is an empty set, okay? From the root, insert another one. 

Then you find that the FCA are the same, you merge them and increment their count. So and then you have this. And then at this time, B has no point, right? Because there was no B in the first branch. Now B has started. And then M is here, so you follow the link. The reason we do this, this thing, is because we can use starting from the, from each, from the head table, from the M, and find the each item on the tree, okay? 

Find that node directly, otherwise you have to search through the entire tree to find this F. All right, then we get to the next one, FB. Then FB, F get increment, and then we get a new branch at the B. So F increment by one, and we have the new branch B, and then with the one. And then F equal to three. Then you do the CBP, right? So actually, F is not there, and C, we start with the C here, and then you build another B, P. 

So eventually, after you set all the frequent item list, what do you get is, you can see your next is FB, you set it here, right? And then you start to insert the CBP, because CBP, you build another branch, because CBP is not there, so you build another branch. You can see like, so this is the C, right? This is BM, and then FB, FB is starting the F, and then B. So F increment by one, I get CBP, you get a CBP one by one. And then FCAMP, and then the F again, the F count as four now. And then C, C count as three, A, A count as three, and then M, P count by two. 

So we build this tree, okay? So the question, we have the tree, what do we do with the tree? So our next task is to, as we said, we want to find the frequent item P, you can subsequent search to only those transactions that contain name P. So we know that for frequent item M, okay, the frequent item, non-frequent item, FCAVM, and on that path, all contains transaction M. Okay, BM, AM, CM, FA, FCM, FBM, all contain M.