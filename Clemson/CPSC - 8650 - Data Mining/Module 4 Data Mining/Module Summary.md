![[Module Summary Slides.pdf]]Okay, let's summarize. In summary, we studied the concept on the pattern discovery and discussed why it is important. And also, we understand how to mine the frequent patterns and association rules. And also, we discussed the closed pattern and the max pattern to compression of frequent patterns. The closed pattern will be frequent patterns at the different support levels. And the max pattern is the largest pattern at the frequent support level. It doesn't matter which frequent support level, we just find the longest maximum pattern. 

And for the efficient pattern mining method, we studied down-closure property for the frequent patterns. Well, if an item set is frequent, then its subset should also be frequent, right? So using that, we can derive a priori algorithm in which we can generate the candidate set from the k plus one item set from k item set using the join of k item set. And then we can clone the candidates by removing the k plus one item set that contains infrequent k item set. Because the downward closure property, which says frequent item set should have all the subset must be frequent as well. And then we also discussed how to improve the a priori algorithm by reduce the number of scans, reduce the candidates generate or parallelize the candidates generalization. And we also discussed using the mined frequent pattern by exploring the vertical data format where you use the item as the list and then list all the transaction IDs. So in the item as the ID of the rows and then with all the transaction list on each item, then we do it using the set join to find this join to form the higher-level association, right? Higher-level more items with the frequency, okay? And then we also discussed the depth-first search method like FPGrowth where you build the FPTree, okay? And then you find the frequent pattern from the pattern growth approach where you grow the patterns on the tree, use the conditional base, frequent base database, conditional frequent database, okay? And then furthermore, we can use the mined cluster pattern which is the Gnostic compression of frequent pattern and then to generate, to gather all the different patterns, all the frequent patterns, okay? 

Then we study how to evaluate the interest needs of patterns. Well, use the LIFT and the chi-square and we also may discuss that all these chi-square are not suitable for the database which contains a lot of transactions because they are not now invariant invariant. Then we discuss the non-invariant measures include the Jakarta coefficient, the max confidence, all confidence, and then Kuczynski, and then, and the cosine, okay? And then we find Kuczynski plus the irre, a imbalance ratio, a good measure of interest needs, okay? And then we finally studied how to mine various kinds of patterns include the multi-level association, multi-dimensional association, quantitative association, and the rare patterns or mine the negative patterns.