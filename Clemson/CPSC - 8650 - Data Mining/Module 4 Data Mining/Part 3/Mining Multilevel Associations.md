![[Mining Multilevel Associations Slides.pdf]]What is the multi-level frequent balance? And given a data item, right, so the item often form hierarchies because, for example, one product may have different branch, brand, and may have different level, okay? For example, milk has 2% of milk and bread has wheat bread or other kind of bread, okay? So you can see, oh, they have different category and then there are lower level categories. So they just, you know, further divide into a hierarchy, right? So sometimes the customer or the store manager, okay, only want to know if after the customer purchased the 2% of milk, what they will do next. Or if they want to identify what people buy skim milk with what product. 

So you can see, not just by the milk sale, right? So you have a multiple-level frequent pattern. They also want to find the same, buy milk with eggs or buy milk with something else, okay? So mind this frequent patterns at multiple levels. Should we use the same uniform support level for all the levels, all levels? The same uniform threshold? Of course, it may cause some problem because uniform support level will remove some lower level product or item and lower support because they supposed to have a lower support. 

Looking at this figure, the milk has a support 10%. 2% of milk has support, 6% is popular, right? Skim is the next support, has only 2%. If you use the minimum support level of 5% for all the product there, then skim milk will never become a frequent item. So skim, then you cannot discover this rule of skim milk purchased with somebody else, right? So you should use a level-reduced support threshold, right? Items at the lower level are expected to have lower support, so you need to give a lower support threshold. If you mine the frequent patterns in a different level, you may try to mine them with the same algorithm on the different level. 

For example, I want to run the first level, level 1, then we do a milk-reduced support and then level 1, then we set the minimum threshold at 5%. Then when we do the level 2, or when we do the 2% milk, we may set a threshold at 4%. And then for the skim milk, what we can do is set the threshold at 1%, right? So then we run the same algorithm three times. This is not very efficient, okay? So another approach is like we share multi-level mining, okay? We run the same algorithm. 

We started with the lowest minimum support threshold, and then we get the candidate set to fill out all milk that does not go past the threshold of 1% level, okay? And then we feed those candidates above the 1% to the higher level, okay? Give it, say, 6% or 5%, and then to 4%. So by doing that, you don't have to repeat checking multiple times, okay? And also, there are possible rules are redundant because of the ancestor rule. Because ancestor rules or ancestor relationship, okay? Because like milk, well, you buy milk, we'll buy wheat bread, and then support is 8% and confidence 70%, that's the number one. 

And then second rule is 2% of milk, wheat bread, support 2%, confidence of 72%. So suppose 2% of milk sold is about a quarter of milk sold in a store. In such a case, we expect that 2% of milk has a support level only one quarter of the total support level of milk. So in such case, those two rules, rule number one and rule number two are very similar. And actually, you can directly derive rule number two from rule number one. So in this case, those two rules should be redundant, right? Because you can derive one from another. 

So in general, a rule is redundant if its support is close to the expected value, right? So if you generate a rule number two, and then support level is expected value, because you already have a milk support level at 8%, then 2% of milk, you know that's number two, right? And because you have a support level of 2%, then in that case, you don't need rule number two, you just directly generate my milk to 2% of milk. Or if somehow, you know the 2% of milk and then has this confidence level, can you derive the milk at the higher level? Well, maybe not, okay? Because there is a confidence level, okay? You cannot directly derive, okay? 

But if they have the similar confidence, then you can say rule one is the ancestor of rule two. But which one should you prune? Well, in my opinion, you should prune number two, because number two rule can be derived from number one. But of course, you know that after you run the mining, you identify and then you can do that, okay? Or if you're only interested on the 2% of milk rule, then you can just remove the milk to wait for a rule, okay? So that depends on what you are interested in, okay? And also, because sometimes some items are rare in the database, so you may need a customized minimum support for the different kinds of items. 

Because so far, we have used the same minimum support threshold for all items in each association mining, no matter what happens. However, some products like jewelry, diamonds, watches are valuable, but they are less frequent in the database, right? So they're supposed to have less support, okay? So it is necessary to have a customized minimum support that is for different kinds of items. So how do you use the next different threshold? Well, we have to group those data together, okay? So we use group-based individualized minimum support. 

For example, taking a diamond and a watch, you use a 0.05%. For bread and milk, you use 5%, right? And then, how do we mine such rules efficiently? Like when we talk about the shared multi-level association mining, we can start it with the lowest threshold. In this case, it may not be a good idea. Well, so far, we already studied different ways to improve the association rule mining, right? And one of the methods is doing the partition, right? 

So you partition the database. For example, we do the partition database on the diamond and watch. So we only have the partition that contains diamond and watches. And then in that case, we run algorithms against that database. So there are many ways. Of course, you can do other approaches, okay? Like generate the p-condition or FPTrees, something like that, right? 

So, so far, all those algorithms that we already studied for scalable mining algorithms can be easily adapted to cover such cases, okay? Thank you.