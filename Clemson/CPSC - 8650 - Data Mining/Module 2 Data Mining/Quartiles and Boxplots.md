![[Quartiles and Boxplots Slides.pdf]]We use the quartile and the boxplot to measure the dispersion of the data. The quartile has q1, quartile 1, and q3, quartile 3, and each represent 25 percentile and 75 percentile respectively. And the interquartile range is Iqr equal to q3 minus q1. So we can use a five-number summary to represent the dispersion of the data. The minimum, the q1, median, q3, max, OK? And we use the graphic display to represent this five-number summary, which we can have a box, which is Iqr, OK? And then at the bottom box, it is q1, top of the box, then, is q3. 

Then the median is drawn as a line in the box, OK? So the height of the box is the Iqr, OK? And then beyond the box, you draw two whisker lines, OK? And then two lines outside, OK? Extend to the maximum and to the minimum, OK? Oftentimes, we can use the Iqr to do outline detection. For example, you can specify a threshold. 

Oftentimes, we can use a threshold such as 1.5 Iqr. So if you take 1.5 Iqr as the threshold, means you take anything outside 1.5 Iqr is online. So you take one box, OK? That's a box. And then take a half box up, half box down. And anything beyond those, you can see that's outline, OK? Then for the outliner, you can use a cross to point out those outliner points, OK?