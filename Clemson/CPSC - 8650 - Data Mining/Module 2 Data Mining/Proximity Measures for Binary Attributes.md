![[Proximity Measures for Binary Attributes Slides.pdf]]We can also measure the binary attributes with contingent table. So as we said that we can use the Hamming distance to measure the binary distance, right? So oftentimes binary vector is small, it is very, you know, simple and easy to calculate and it is meaning something. But the most popular proximity measure for binary attribute is actually represent into the contingency table first. So it uses the contingency table to represent if the data are the same or not. So in this contingency table, okay, and you take object i and take object j, okay? So object i has a binary vector, object j has a binary vector. 

At each point position, you compare the binary value of object i and object j. If both of them are 1, you count as 1. So eventually, so you get the Q, okay? You sum of those 1, 1's position, you get the Q. And then if object i equal to 0 and the object j equal to 1 on those positions, you sum the number of those positions, you get the S. You do the same if object i equal to 1, object j equal to 0, then you sum them together, you get R. And then if the object i and object j both equal to 0 at those positions, you sum the number of positions, you get the T, okay? 

So the contingency table is QSRT here, okay? So for your Hammond distance is the differences between those two objects, then basically it's equal to R plus S. That's simple. And so, of course, the Hammond distance is not normalized, right? Because the number is increased when the vector size goes up. So what we can do is we can normalize it with the total number of B, okay? So that is why we divide it by QRS plus T for the symmetric binary variable, okay? 

So symmetric binary variable means the symmetric binary variable, we will not distinguish the 1 and the 0, 1 and 0 has the same meaning in the data, okay? However, in our application such as medical diagnosis, a positive value often has different meaning from the negative value. Often positive value means more importantly than the negative value. For example, if you go to the diagnosis, go to the medical doctor and do a lot of tests, and then you tell the doctor a lot of symptoms, the doctor will look at specifically on those positive ones, and then often just equal all those large number of negative ones. So if you use that distance measure as a symmetric, it cannot emphasize the positive, okay? So that's why oftentimes we take out this T, which is both negative, to find, okay, so if both object has a lot of zeros, okay, that means they are normal people. We don't care too much. 

But we will only care the symptom differences, or, you know, some symptom difference will tell the difference, okay? So this is a symmetric distance will remove the T that's both negative, okay? Now sometimes you want the similarity, see? So the similarity is 1, because since we have normalized that into 0, 1, we can use 1 minus D to represent the similarity, okay? And of course, here is the same thing, okay, if the symmetric data is a Q plus T, because they are the same on those positions, divided by the total P position, okay? And Jaccard coefficient is used for a similar measure for asymmetric binary variables, where you take the Q, you only count the positive similarity, okay? Ignore the negative similarity, so that's a Q over Q plus R plus S, T is gone. 

This will avoid situation where there is a lot of both negative situation, so then T becomes dominated. When T becomes dominated, the similarity of Jaccard of I and J become very large, almost to 0, or almost to 1. But actually they are different if you remove those T, okay? Well Jaccard coefficient is also the same as the coherence, which is another measurement that we will discuss later on in the pattern discovery.