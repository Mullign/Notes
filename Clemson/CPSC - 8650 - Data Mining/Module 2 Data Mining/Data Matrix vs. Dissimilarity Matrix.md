![[Data Matrix vs. Dissimilarity Matrix Slides.pdf]]So, before we calculate the dissimilarity or similarity, we have to understand the data matrix. The data matrix is a matrix that represents the data point, okay? So each row represents one data point, okay? Each column represents the attributes of that data point. So for example, you have n points with l dimensions, okay? So x11 to x1l represent l attributes for object 1. And then x21 to x2l represent l attributes for object 2, okay? 

So dissimilarity matrix is the measure of distances between those objects, okay? It will register the distances, okay, typical matrix. So if you take this n object, so it should have n row and n column. Because it is symmetric, because distance between object 2 and object 1 is the same as distance between object 1 and object 2. So we often present it as a triangle. So it is a triangular matrix with the diagonal line as zero, meaning that distance between object 1 and object 1 is the same, and then it's a zero. And then distance between the object 2 and 2 is zero, distance between object 3 and 3, object n and n is zero. 

So the size of those two matrix are different. You can see that the data matrix where you have the dimension size is the column number, okay? And then in the dissimilarity matrix, you have n, that's the number of points. The n by n, okay? Now you can use distance functions to measure the real, boring, categorical, or ordinary and ratio and vector variables, and we will discuss those distance functions later on, okay? And also the weights can be associated with different variables based on applications and data semantics, okay? Now let's get to the formulas, okay? 

Sometimes before we get to the more formula, we want to standardize the numerical data into a certain range. For example, you can do a z-score, which is a very popular standardization for the data. Because when you have a value of x, right? So a sequence value, and so we try to normalize them so that it represents the distance between the raw score to the mean value. The mean value is the expected population mean. And then divided by standard derivation, we use that standardization as the units, okay? So that normalization will just put the data in the unit of standard derivation, okay? 

So z-score means, hey, the value of a data is how many standard derivations? 3.5 standard derivations, something like that, right? So when z-score is positive, it means the data value is above the population mean. When z-score is negative, it means the data value is below the population mean. And of course, you can also use another way to standardize the data. That is using the mean absolute derivation instead of the standard derivation, okay? So the mean absolute derivation is take the absolute of mean, and then the difference from the mean value between those absolute of mean. 

Usually, the mean absolute derivation is more robust than using the standard derivation. And basically, if you look at the formula, you can see because they take the differences, and then take the absolute of values, and divide it by n. So when you take a square, it basically increases the variance, okay? So that's the only thing that you can get using the mean absolute derivation is more robust. And then this is an example of data matrix and the dissimilarity matrix. You can see there are four data points. So the data matrix has four rows, and then each data point has two attributes, attribute 1 and attribute 2. 

So attribute 1 represents in the coordinated space as 1 and 2. And x2 is coordinated in the 3 and 4, okay? So you can see this is, so if you have five, then you add one more row. So the data matrix, if the attribute numbers are fixed, then the data matrix can grow only by the row, okay? And then the dissimilarity matrix, in this case, use the Euclidean distance here. Well, it's the Euclidean distance between each object is x1 and object x2, and between x2 and other 0. So the diagonal point are 0s. 

So we use a triangular matrix here, because this cell from x1, x2 cell is also 3.61. So they are symmetric with this x1, 2, 1, okay? So we are not going to do that, okay? There's a distance 2, 1, and then is equal to distance 1, 2. And then distance 3, 1 equal to distance 1, 3. So we are not going to just, you know, produce, just make it empty there. Now, how do we calculate Euclidean distance? 

Let's get to this point, alright?