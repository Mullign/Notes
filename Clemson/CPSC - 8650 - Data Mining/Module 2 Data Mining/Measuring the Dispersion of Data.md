![[Measuring the Dispersion of Data Slides.pdf]]
Data are not always symmetrically distributed, okay? And if the data are symmetrically distributed, it often means medium and mole are all together. If it is a unit, it is a unit model data set, okay? For the unit model data set, it may be skewed, where you have more larger values, less smaller values, or more smaller values, less larger values. So we will use the mean and the median relationship to determine if it is skewed. Now if the median and the mean are all together, we can call it symmetric. If the median is less than the mean, okay, that means it is a positive skewed data set. 

If the median is greater than the mean, it is a negative skewed data. Why is that? Because if the mean is less than the median, that means you have smaller numbers, okay? Then the mean value is smaller than the median value, okay? So it is called a negative skewed. And there is a very popular distribution we used to assume some data, okay? Like a model of the data. 

We call it a normal distribution, okay? So if we have a large data set, we always say, hey, they follow a certain distribution, okay? Normal distribution is one of those distributions we often use to model data sets, okay? So the normal distribution has some very interesting characters. First of all, it is a unit model distribution, okay? So it has a median and a mean and a uniform, so it's asymmetric, okay? So at the center. 

So the mean value, okay? Then there are some other interesting characters where you have 68% of the data are within one standard derivation of the mean. So 95% of the data within the two standard derivations of the mean. So the question, okay? The question is, what is the standard derivation? Okay, that we will measure. And let's measure the data distribution, what is the variance and the standard derivation. 

So standard derivation is the square root of the variance. Now, so what is the variance? So let's first define the variance, okay? To define the variance, the variance, we need to have the mean value. So for the sample data, okay? Say you have a sample size, say S. Then standard deviation is S square equal to 1 over N minus 1, sum of Xi minus X bar. 

So what does this mean? Xi minus X bar, take a square. So Xi minus X bar is the difference of each data value away from the mean value, right? So this value, the larger, means the data has more variance, right? So this is how you measure the variance, okay? Then you will notice there are two different formulas. One is the variance sigma square, well, divided by N. 

And in this case, N is the entire population size, okay? And then the lower case N is the sample size. So why there is a 1 divided by N minus 1, another is 1 divided by N, capital N? Well, this is because if the sample size is large, but not very large, not sufficient large, then the variance using the sample to calculate the variance is always, okay, is always have a little bit different from the population value. And that variance happens to be a little bit larger, okay? Because of that, if you take the N minus 1 as the denominator, then you will get a much better estimate, okay? Okay, so remember, this is the case when you have a large enough sample, but not a very large sample, okay? 

When you have a very, very large sample, okay, sufficient sample size, you can use 1 over lower case N, that's good, because that bias, that error, or that difference is very small, okay? And then when you have a very small sample, it actually doesn't matter if you use N or use N minus 1, the variance computation is always a little bit different from the entire population, because you only take a small sample, okay?