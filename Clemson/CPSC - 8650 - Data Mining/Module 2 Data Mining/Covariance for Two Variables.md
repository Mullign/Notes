![[Covariance for Two Variables Slides.pdf]]
Now, let's get to the next topic called covariance, because we talk about the single variable. So based on that single variable, and then the covariance is that you take the mean. So x1 over, so you have two variables, and then each variable has a group of numbers, and then they are paired. So number x11, x21, x11 measure with x21, x22, x12 with x22, x13 with x23, so like this. So what you do is you take each x value minus the first distribution mean, that's expected. And then times x2 minus x2 minus mu, mu2, and then so you time that again. And then you take E of this, E of this expected mean, okay, of this mean value. 

And then that is called the covariance, okay? And the covariance actually can be written as the other formulas, like Ex1 times x2 minus mu1, mu2, or Ex1 x2 minus Ex1 Ex2, okay? And mu1 is, Ex1 is the respective mean or expected value of x1. And then mu2 is the same for the x2, okay? So this is the covariance. Now we assume that we have the entire population, okay? And the covariance will be different, a little bit different, okay, that we just take n between these two, okay, we divide by n, that's it, not that different. 

So we get a covariance hat of sigma 1, 2, okay? Now if you take it, you take the two data sets from the same value. So you take the same data set and then apply the covariance on this same data set. That means Ex1 minus mu1 times x1 minus mu1, that's x1 squared, that means Ex1 minus mu1 squared. So what does this mean? This is a covariance, right? This is a variance of x1, so the covariance is a generalization of the same variance. 

Because you have a sample variance, the same sample variance, you take the covariance. So for the variance, the covariance, if the covariance value is greater than zero, it's called a positive covariance. If the covariance value is less than zero, it's called a negative covariance, okay? So that's it. And so what this covariance for two variables can be used in our statistical measure, okay? You can measure independence or correlation between those two variables, okay, if they are independent, okay? It means sigma 1, 2 equal to zero, okay, but you cannot say even the sigma 1, 2 equal to zero, it means the x1 and x2 are independent, okay? 

This is not the reverse. So if they are independent, they must be equal to zero, okay? But if they are, the sigma 1, 2 equal to zero, they may not be independent, right? And so some random variables, basically some pairs of random variables, they are independent variables, but may have some random variables have a covariance of zero, but they're not independent, right? So you cannot call it independent, okay? So you need to have some additional assumptions. For example, you can make an assumption about the data, say, those data follow the multivariant normal distribution, okay? 

Otherwise, you cannot say covariance of zero implies the independence, okay? So in this example, you can see that the covariance, and calculate the covariance of 1 and 2, so the data variable x1 and variable x2, and then the covariance, if you calculate them, and you, so the ex, what is the ex1, ex1 equal to zero, right, ex2 equal to zero, and what is the ex1 times x2, right, they are zero, okay, but no, ex, so ex1, no, this one, ex1, zero, zero, and ex1, ex2, yeah, and then what do you get? You are getting zero, because ex1 equal to zero, right, ex1, one plus minus one equal to zero, divided by whatever n number, and then ex2, and then zero, one minus one equal to zero, okay? So you take ex1, so times x2, so one times two, one times zero equal to zero, negative one times one, plus negative one times one, so you add some of them equal to zero, so ex1, ex2 also equal to zero, so this means, this is zero minus zero, so sigma 12 equal to zero, right, but you cannot say these two variables are independent, okay?