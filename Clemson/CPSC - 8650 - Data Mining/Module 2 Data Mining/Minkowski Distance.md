![[Minkowski Distance Slides.pdf]]
Euclidean distance is a special case of Minkowski distance. So before we calculate the Euclidean distance, let's get to know the Minkowski distance first. Minkowski distance is a popular distance measure where you take the difference of each attribute of the two data objects, and then take the epsilon value, and power by p. p is 1, 2, 3, 4, 5, and infinite. And then you take this root, p root. So p is root, the power p, and then p is root. So you can see that the object i and object j, object i is xi1 to xil. Object j is xj1 to xjl. 

So there are two l-dimensional data. So p is the order. So for that, we call this distance as the lp norm. So the p is 1, then the l1 norm. If p equal to 2, then it's the l2 norm. Now, Minkowski distance has three special properties. The first property is called positivity. 

So d, i, j greater than 0 if i and j are different. Now, of course, if i and j are the same, say d, i, i, they are 0. So the value of two objects are either 0 or greater than 0. Then the distance is symmetric. d, i, j equal to d, j, i. So they have a symmetry. And then they also have a property called the triangle inequality or triangular inequality. So the triangle inequality means if you know the geometry that in the triangle, the length of one line is always less than or equal to the summation of the other two lines. 

So this is the same case. The distance from i to j is always equal to or smaller than the distance from i to another point k and then from that point k to the point j. So a distance measure that satisfies those properties is called a metric. OK, a metric. Of course, there are non-metric dissimilarities. For example, set differences because they cannot satisfy all those three properties. So only the distance measures that satisfy those three properties can be called a metric. 

Now, as we said, Euclidean distance is a special case of Minkowski distance. So let's get into those special cases. We'll see. 

Euclidean distance is a special case of Minkowski distance. So before we calculate the Euclidean distance, let's get to know the Minkowski distance first. Minkowski distance is a popular distance measure where you take the difference of each attribute of the two data objects, and then take the epsilon value, and power by p. p is 1, 2, 3, 4, 5, and infinite. And then you take this root, p root. So p is root, the power p, and then p is root. So you can see that the object i and object j, object i is xi1 to xil. Object j is xj1 to xjl.  Play video starting at :1:15 and follow transcript1:15 So there are two l-dimensional data. So p is the order. So for that, we call this distance as the lp norm. So the p is 1, then the l1 norm. If p equal to 2, then it's the l2 norm. Now, Minkowski distance has three special properties. The first property is called positivity.  Play video starting at :1:55 and follow transcript1:55 So d, i, j greater than 0 if i and j are different. Now, of course, if i and j are the same, say d, i, i, they are 0. So the value of two objects are either 0 or greater than 0. Then the distance is symmetric. d, i, j equal to d, j, i. So they have a symmetry. And then they also have a property called the triangle inequality or triangular inequality. So the triangle inequality means if you know the geometry that in the triangle, the length of one line is always less than or equal to the summation of the other two lines.  Play video starting at :3:3 and follow transcript3:03 So this is the same case. The distance from i to j is always equal to or smaller than the distance from i to another point k and then from that point k to the point j. So a distance measure that satisfies those properties is called a metric. OK, a metric. Of course, there are non-metric dissimilarities. For example, set differences because they cannot satisfy all those three properties. So only the distance measures that satisfy those three properties can be called a metric.  Play video starting at :4:4 and follow transcript4:04 Now, as we said, Euclidean distance is a special case of Minkowski distance. So let's get into those special cases. We'll see.  Euclidean distance is a special case of Minkowski distance. So before we calculate the Euclidean distance, let's get to know the Minkowski distance first. Minkowski distance is a popular distance measure where you take the difference of each attribute of the two data objects, and then take the epsilon value, and power by p. p is 1, 2, 3, 4, 5, and infinite. And then you take this root, p root. So p is root, the power p, and then p is root. So you can see that the object i and object j, object i is xi1 to xil. Object j is xj1 to xjl.  Play video starting at :1:15 and follow transcript1:15 So there are two l-dimensional data. So p is the order. So for that, we call this distance as the lp norm. So the p is 1, then the l1 norm. If p equal to 2, then it's the l2 norm. Now, Minkowski distance has three special properties. The first property is called positivity.  Play video starting at :1:55 and follow transcript1:55 So d, i, j greater than 0 if i and j are different. Now, of course, if i and j are the same, say d, i, i, they are 0. So the value of two objects are either 0 or greater than 0. Then the distance is symmetric. d, i, j equal to d, j, i. So they have a symmetry. And then they also have a property called the triangle inequality or triangular inequality. So the triangle inequality means if you know the geometry that in the triangle, the length of one line is always less than or equal to the summation of the other two lines.  Play video starting at :3:3 and follow transcript3:03 So this is the same case. The distance from i to j is always equal to or smaller than the distance from i to another point k and then from that point k to the point j. So a distance measure that satisfies those properties is called a metric. OK, a metric. Of course, there are non-metric dissimilarities. For example, set differences because they cannot satisfy all those three properties. So only the distance measures that satisfy those three properties can be called a metric.  Play video starting at :4:4 and follow transcript4:04 Now, as we said, Euclidean distance is a special case of Minkowski distance. So let's get into those special cases. We'll see.  Euclidean distance is a special case of Minkowski distance. So before we calculate the Euclidean distance, let's get to know the Minkowski distance first. Minkowski distance is a popular distance measure where you take the difference of each attribute of the two data objects, and then take the epsilon value, and power by p. p is 1, 2, 3, 4, 5, and infinite. And then you take this root, p root. So p is root, the power p, and then p is root. So you can see that the object i and object j, object i is xi1 to xil. Object j is xj1 to xjl.  Play video starting at :1:15 and follow transcript1:15 So there are two l-dimensional data. So p is the order. So for that, we call this distance as the lp norm. So the p is 1, then the l1 norm. If p equal to 2, then it's the l2 norm. Now, Minkowski distance has three special properties. The first property is called positivity.  Play video starting at :1:55 and follow transcript1:55 So d, i, j greater than 0 if i and j are different.: Added to Selection. Press [⌘ + S] to save as a note Like Dislike Report an issue : Added to Selection. Press [⌘ + S] to save as a note Like Dislike Report an issue : Added to Selection. Press [⌘ + S] to save as a note