![[Quantile-Quantile Plot (Q-Q) Slides.pdf]]Our next one is called a quantile-quantile plot. It's called a Q-Q plot. So in this plot, you have two univariate distributions. One is on the x-axis, and the x-axis is represented with the value, right? The value is increased. And then you order another axis, y-axis, also to order the value. Then you take the first value on both sequences, on both data sets at the minimum. So you take the minimum, OK? So the minimum value on this x-axis and the minimum value on this y-axis can be used as the origin, OK? That's the 10-quantile point, or 0.1-quantile point, depending on how many data points in these two distributions, OK? 

So whatever that value is. And then you take the second one, and then you draw that quartile-quantile point, OK? So the same quantile point will be represented as a point. So for example, if you take the median of the data set, like this, middle black, black, dark, dark black, dark diamond, the middle dark diamond. So represent at the median of branch 1's unit price is 80, and branch 2's unit price is 90. So it means you have two branches, OK? The price, the unit price of the item, so those two branches, the median price is higher for branch 2, and median price for branch 1 is lower. However, the max price in here in branch 1 is 120, and the max price in branch 2 is less than 120, OK? And also, you can see some point that at about 20 percent point, well, the price at the 20 percent quantile, well, you have branch 1 price is about 58, and then branch 2 probably about 55. So there is also a line. That line is a diagonal line. 

Well, it's a red diagonal line. The diagonal line represents where you have equal values on each branch. So you draw that line. On the line, any point of that line has equal values on branch 1 and branch 2, OK? So any data above that line represents branch 2 has a price larger than branch 1, and lower than that line means branch 1 has a price larger than branch 2. Now, also, this is two univariate distributions plotted on the same graph, OK? And oftentimes, we have only one univariate distribution, so your univariate distribution of data. 

So, and then you want to compare with some non-distribution, such as normal distribution. So we want to say, hey, if this data follows the normal distribution, then you can also draw the same thing. So you can plot the data against the normal distribution so that on the x-axis is your observed value, and then your y-axis is expected the normal distribution value, normal value, OK? So that's what happens.