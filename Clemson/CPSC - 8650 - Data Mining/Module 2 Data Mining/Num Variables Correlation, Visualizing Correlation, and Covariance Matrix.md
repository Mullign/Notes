![[Num Variables Correlation, Visualizing Correlation, and Covariance Matrix SLides.pdf]]
And so for the correlation between the two numerical variables, OK, is the standard covariance obtained by normalized covariance with standard derivation of each variable. So you take the covariance, divide, so rho 1, 2 equals variance, covariance divided by each their own standard derivation, OK? So the sample correlation of two attributes can also be calculated like this. It's just that the only difference is that you use the sample, OK, where n is the number of tuples in the data set. And then the mu 1, mu 2 are the means, and sigma 1 and sigma 2 are the standard derivations, and sigma 1, 2 are their covariance, OK? So if rho 1, 2 greater than 0, and then these two numerical variables are positively coordinated, OK, and that means x1's value rise as x2's value, OK? And then the larger the rho tuple, the stronger the correlation. 

If rho 12 equal to 0, it means they are independent, OK? Now we, under the same assumption as discussed in the covariance, means that they follow the same distribution, they follow this. And then if rho 12 less than negative, and then they are negative, less than 0, and then they are negatively correlated. So if the rho equal to 0, they're independent, OK? They're independent. We need to make some assumptions, OK? Our assumption is that the data follow the multivariance normal distribution, OK? 

That's it. OK, so if we visualize the correlation coefficient, you will see that correlation coefficient value range goes from negative 1 to plus 1. Why is it from negative 1 to plus 1? Because you have normalized here, OK? You get normalized by the variance, OK? So when it's a negative 1, you can see these two variables are going negatively correlated, and from a high, so when one value goes higher, another value goes lower. And then if the correlation is from negative 9, maybe less, and then negative 0.8, now when it is going to 0.0, 0.0, it is just randomly no correlation at all, independent. 

And then going correlated again to 1.0, OK? So this plot is called the scattered plot. So we just plot the data on these two-dimensional space. And we will discuss the scattered plot later on, OK? And then the variance and the covariance information for two variables can be summarized in a 2 by 2 covariance matrix like this, OK? So at the diagonal point, you have a variance, and then on the other end has two covariance. And then if you have a, this is a 2 by 2, if you have a d dimensions, we can come up with the covariance matrix like this, OK? 

So that's a d1, sigma 1, sigma 2 square, and then sigma d square, that's on the diagonals has the variance. And then all the other covariance pair of those d dimensions, OK, the 1 with 2, 2 with 3, 3 with 4, OK, we can come up with this covariance matrix, OK?