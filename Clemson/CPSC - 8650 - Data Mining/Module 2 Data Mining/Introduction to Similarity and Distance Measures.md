![[Introduction to Similarity and Distance Measures Slides.pdf]]In this video, we're going to study dissimilarity and distance measures for data objects. And we'll first study the data matrix and the difference between the data matrix and the dissimilarity matrix. And then we will study the proximity measures for nominal attributes. We'll also study proximity measures for binary attributes. And we study dissimilarity of numerical data, specifically Minkowski distance and their special set cases. And then we'll also study the proximity measure for ordinal attributes. And then we will study how to measure the attributes dissimilarity of mixed types. 

And we also learn how to calculate the document dissimilarity using cosine dissimilarity measure. And we also study how to measure the dissimilarity distributions using KL divergence. And also we touch base on the hidden semantics and how the hidden semantics of data objects are different from the similarity measures and why similarity measures may not be able to capture the hidden semantics. All right, so what is the similarity? Similarity, dissimilarity, and proximity are the terms used to describe the difference or similarity of the object. So similarity measure or similarity function is a function that produces a real value that quantifies the similarity between two objects. So it principally measures two data objects and how they are alike. 

The higher the similarity value, the more alike these objects. So oftentimes we normalize this similarity measure into the range from 0 to 1, in which 0 means no similarity and 1 means completely similar. And dissimilarity or distance measure. The dissimilarity is the numerical measure of how different two data objects are. So in some sense, it can be viewed as the inverse of similarity. So when it is lower, then these objects are more alike because they are close. So the minimum dissimilarity is often 0, which means those two objects are completely the same or completely similar. 

Oftentimes we can put it into a range 0 to 1 and oftentimes, now of course, if you cannot put the range into 0 and 1, it may go from 0 to infinity, depending on the definition. Proximity usually refers to either similarity or dissimilarity.