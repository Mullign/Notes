![[Proximity Measures for Categorical (Nominal) Attributes and Ordinal Attributes, and Attributes of Mixed Types Slides.pdf]]Okay, we will study the approximate measure for categorical attributes. So as we know that categorical attributes, also called the nominal attributes, they represent as a group of names. So the data itself, say color, red, yellow, blue, green. So if you have the data, for example, you have only four colors, then one data point has a red, yellow, and no blue and green. And another data point has a red, blue, and without yellow and green. So what you can do is you can find out the total number of matches in each data object. And then divide by the total variables. 

Okay, that's it. So the total matches divided by the matches, the variables, that's a seminary. So if you take the total variable minus the matches divided by the point, it's the differences, so called a disseminary or distances, okay? Now another method to do it is to try to use a large number of binary attributes. So instead of just finding the matches, you take the total variables available in this dataset. Say, for example, you have each, for example, one data point has a red, yellow, okay? And then your total variable has four red, yellow, blue, green. 

Then you take a red, yellow, blue, green, so and then without the blue and green, with the red, you put a one and a one, and yellow, red is one, yellow is one, and then blue is zero, and green is zero. So you convert each nominal data into a vector. So this vector is a fixed-size vector, and the position has one represented as a nominal value at that position, the state of that variable is on, okay? So basically, you create a large number of states. That number of states equals to the total available nominal variable, okay? And then once you convert them into the binary vectors, then you can use the binary similarity measure or dissimilarity measure to measure the distance and the similarity. On the ordinal variables, an ordinal variable can be discrete or continuous, okay? 

And so don't confuse the ordinal variable with the nominal variable. By writing, it looks like, you know, for example, you have a freshman, sophomore, junior, senior, looks like they are nominal variables, but they are different, because the freshman, sophomore, junior, senior has the ranking inherited, okay? The freshman has a lower rank than sophomore, junior has a higher rank than sophomore and senior. And you can see that assistant professor, assistant professor, and the full professor, they are different. So in the nominal data, they are the same value and name, just represent name, there's no rank. So since there is inherited ranking in the ordinal variables, then we can trade it, convert it into an interval scale. So we replace an ordinal variable value by its rank. 

So say freshman rank is one, sophomore rank is two, and up to like an MF. So M is the maximum rank or highest rank in this data set. So then for each variable, we will map its current rank into a fraction, okay? So then we, for example, in this one, for example, sophomore or freshman, so freshman, sophomore, junior, senior, well, their rank is one, two, three, four. And then we use the value of RIF minus one divided by MF minus one. So the highest rank minus one as the denominator, okay? So we use the rank minus one. 

So freshman rank is one, so one minus one is equal to zero, and divided by four minus one is three, so the freshman becomes a numerical value of zero. And sophomore becomes a numerical value of one over three, and junior becomes a value of two over three, senior becomes a value of three over three, so one. So the distance between the freshman and the senior becomes very easy. We just find the distance, you know, one minus zero, take an absolute, okay? And for the junior, between, and the sophomore becomes two over three minus one over three equal to one over three. So we just use a simple interval-scale variable computation to calculate the dissemination, okay? So this is for ordinal variables. 

Because a dataset may contain all kinds of attribute types. It may have, at a certain position, some attribute as a nominal, some other attribute as a symmetric binary, some other attribute as a symmetric binary, some numerical values, and then it may also have ordinal values. This is happening if you have a database record, right? You have a professor, you have a salary, you have your rankings professor or full professor or assistant professor, and then you have age, you know, there's a lot of data, and then you have your addresses, right? So one may use a weighted formula to combine all the effects. So the weighted formula is that we gave each data type a weight, okay? So for the numerical value, okay, then we can use a normalized distance. 

Because the reason we use the normalized distance is that we're not going to use a real distance, and therefore, it will overweight everything else. Because most of the other distances are normalized between zero and one, okay? So basically, it's like taking the distance of each one and take a weight, okay? So that's, and then divide the total weight, okay? So that's how they use an attributor of mixed type.