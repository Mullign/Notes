![[Numeric Attribute Types and Discrete vs. Continuous Attributes Slides.pdf]]And the most popular data is a numerical attribute, where you have quantitative data, where you have the integer or real-value data, okay? You have two different kinds, okay? And one is called the interval scale. It measures on the scale of equal-sized units, for example, values like temperature and the dates, okay? And for this interval scale data, there is no true zero point. The ratio based upon ratio scale data has an inherent zero point. You can speak of values as being an order of magnitude larger than the unit of measure, for example, like 10K degree is twice as high as 5K degree because they measure as a ratio, okay, to a unit. 

That's 1K, okay? So that is the inherent zero point. Now attribute can be discrete or continuous, okay? And the discrete data has only infinite or countable infinite set of values, for example, zip code, profession, and then the word, okay, in a document. Sometimes you can represent as integer variables. Now, of course, binary attributes are a special case of discrete attributes, again. Then the continuous attribute has real numbers as attribute value, for example, temperature, height, weight. 

Practically, real value can only be measured and represented using an infinite number of digits. So you can argue that, hey, this is also a discrete attribute. But with the computer technology improved, you may get more and more digits. So in this case, we can call it a continuous attribute. You never know in one day we can represent all values, okay? All infinite number of values. So in the real-world scenario, we often represent the continuous attribute as a floating-point variable. 

And then the discrete attribute often represents, if you want to use a number, you can use an integer variable, okay? So that's another difference.