## 1. Compare and contrast partitioning, hierarchical, density-based, and grid-based clustering methods. For each method, provide a brief description of its core principles and give a practical example of its application.

## 2. Suppose that the data mining task is to cluster points (with (x, y) representing location) into three clusters, where the points are:

A1(2, 10), A2(2, 5), A3(8, 4), B1(5, 8), B2(7, 5), B3(6, 4), C1(1, 2), C2(4, 9)

### The distance function is Euclidean distance. Suppose initially we assign A1, B1, and C1 as the center of each cluster, respectively. Use the k-means algorithm to show only

- **(a)** The three cluster centers after the first round of execution.
    
- **(b)** The final three clusters.
    

## 3. Explain the challenges of determining the optimal number of clusters, k, in K-Means clustering. Propose and describe two different methods that can be used to address this problem.

## 4. Describe the K-Modes algorithm and explain why it is necessary for clustering categorical data, as opposed to using K-Means.

## 5. Explain the difference between **agglomerative** and **divisive** hierarchical clustering approaches, using the concepts of bottom-up and top-down hierarchies.

## 6. Explain the concept of a **dendrogram** and how it is used to visualize the results of a hierarchical clustering algorithm.

## 7. Explain the core concepts of **core objects**, **density-reachable**, and **density-connected** in the context of the DBSCAN algorithm. Use these concepts to describe how DBSCAN can discover arbitrarily shaped clusters.