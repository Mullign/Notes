![[Linear Models Overview Slides.pdf]]Welcome back everybody, let's talk about some linear regression. So last time we talked about the comparison of models and how we determine the fitness of them to our data set that we are evaluating on. Here we need to talk about the evaluating significance of predictors and how well do we actually know what f hat is. So starting with evaluating the significance of predictors, we start off with a linear model. There are different types of models that we'll be using in the future, but this is one of the building blocks so it's very important to see. When we built our K nearest neighbors model to predict the sales of the TVs last time, remember we had the radio the TV sales data sets table, we didn't actually have a closed form for f hat. So how do we actually answer the question how much more sales do we expect if we double the TV advertising budget? We can put this into a mathematical form here with y equals f of x plus epsilon or beta sub 1 of x plus beta sub 0 plus epsilon. So it follows from our mathematical formula that y hat equals f hat of x or beta-sub-1-hat-of-x times x plus beta-sub-naught-hat, where beta sub 1, beta sub 0 hat are estimates of beta 1 and beta null respectively that we are using to compute our observations. So the idea behind a linear model is really pretty simple. You just have a series of betas that you are trying to use to estimate the error and make a linear model. 

How do we actually estimate the regression coefficients though? That's our betas. So we have a given data set here, TV budget and sales, but what is a good line here? We have a guess. This one kind of looks like it's going through the middle of it. Maybe we can tweak it a little bit here, tweak it a little bit more, I don't know, like I'm just drawing on these slides. Instead we need to have some sort of way to measure the error and calculate the residuals to actually find out which of these lines is the best. So again we're going to use our mean squared error as our loss function. Here we have our y-sub-i minus y-hat-sub-i. 

This is the difference between the actual and estimated values squared, added up, and then divided by the number of observations. When we use our linear regression form though, we have our beta sub 1, beta sub 0, and hat in order to minimize the predictive errors made by our model. So if we're looking for the optimal values of these, the optimal values is just whatever the arg min l, the minimum argument, is on these values. We have to find these in some way though. One possible solution is to do it in a brute force way. If we have some sort of loss function graph that we can make here, and it has this parabola shape to it, or instead of brute forcing this, we can take a more calculus oriented approach and take some partial derivatives of l with respect to our residuals, set them to zero, and find the solution to that equation. So this gives us an explosive formula for this and a way that we can make a regression line. 

So if we actually run through this math here, we can see beta-sub-naught is y-bar minus beta-sub-1 x-bar, and running through our other partial derivative, letting the algebra smoke clear, we can actually bring ourselves into explicit forms for beta sub 0 and beta sub 1. "Confidence intervals for the predictor's estimates". We need to have an interpretation of our predictors. So what do you think a predictor coefficient actually means here? Sales equals 7.5.04 TV. What does the 7.5 and the 0.04 actually mean? If we increase TV by a thousand dollars, we expect that to have some sort of corresponding by this equation effect on sales, if our numbers are actually good. 

That's what we need to talk about with our confidence intervals. So we interpret the epsilon term in our observation, y equals f of x plus epsilon, those are the two forms of the same symbol, it's just a different font between them there, to be the noise introduced by random variations in natural systems or imprecisions of our scientific instruments. If you measure something with the same yardstick twice, you're very likely to see a different ever so slightly value be interpreted back out. So if we knew the exact form of f of x, for example, f of x equals beta plus beta 1 x and there was no epsilon, then estimating the beta hats would have been exact. So we need to think about what acceptable error can we have in the models that we are fitting. Three things can happen though, which results in us having less faith in our beta hats. Epsilon is always there, there is always some kind of error being introduced, observed, it's part of the natural world. We do not know the exact form of f of x, or we have a limited sample size. So if we talk about epsilon first, this is the irreducible error. 

Since even predictors made with the actual function f will not match observed values of y, we're working with real data here and the real world is messy, so we often have these irreducible errors that we can't get rid of. Let's think about a model. For some values x, y, and f we have these particular points, but because of error, every time we measure on our model there's some sort of interval that we can get a different observation. One set of observations or a realization, a sample of a data set perhaps, we obtain one set of y's as red crosses here on our model. For each one of these realizations we could fit a model and estimate our betas and we would obtain some sort of approximation. We have 0.2 plus 2.13 x approximating our line here, but for a different set of realizations we can have different models giving different lines, different outputs, different regressions. So we just have one set of measurements. Our estimates are just for this particular realization. We have a single time that we can actually go and gather this information, so we are doing the best with what we have. If this is just one realization though of the reality, how do we actually know the truth? How do we deal with this irreducible error that exists in the natural world? Well we can use some imagination, magic realism. We have parallel universes and we repeat this experiment on each of the other universes all the same time and we get these different models coming out of it from universe A, B, and C. So now we can sample multiple times. With more sampling we can build up these probabilities and understand the likelihood of where this can be happening at. Eventually we can do a standard error form of our epsilons and have a statistical understanding of where things are likely to be. Finally from this normal distribution, of error we can calculate the confidence intervals which are the ranges of values such that the true value of whatever's in the middle, in this case beta-sub-one, is contained in this interval point with n percent probability. Which one of these is important? How significant are the predictors? 

We need to answer one other question first though.