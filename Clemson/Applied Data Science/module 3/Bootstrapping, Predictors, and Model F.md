![[Bootstrapping, Predictors, and Model F Slides.pdf]]
In the lack of active imagination, parallel universes and all the fun stuff that comes along with that, it can be a difficult sale to your boss to talk about these things. We need an alternate way of producing fake data that resembles the parallel universes. That's bootstrapping. Bootstrapping is the practice of sampling from the observed data in estimating statistical probabilities. So if we have a bucket of two balls, we first pick up randomly a ball and replicate it. This is called sampling with replacement. We remove the replicated ball to another bucket. 

Notice we have now a black 8 ball in both buckets, it was replaced. We then randomly pick another ball and again we replicate it. We move the replicated ball to the other bucket. This time we got a 6 and we keep going, 3, another 6, until the other bucket has the same number of balls as the original one. This new bucket represents one of the parallel universes that we are trying to sample from again to understand our confidence interval for our error in our model. We can repeat this again and again and again, get as many parallel universe readings as we want. These new buckets all represent one of those parallel universes. 

So we have our definition. Bootstrapping is the practice of estimating properties of an estimator by measuring these properties by, for example, sampling from the observed data. Definitions like this can tend to be a bit word salad-y, so think about our parallel universes or our buckets to give a concrete example to latch onto. Estimating confidence intervals for predictors, estimates, and a standard error, we can empirically measure the standard errors represented with the function SE. For each bootstrapped sample, the estimated betas are beta-hat-sub-zero-comma-i for the i-th parallel universe. Or if we know the variance of the noise, so this is sigma squared sub epsilon of the noise epsilon, we can compute the standard error of beta-hat-sub-zero, say the standard error of beta-hat-sub-one, analytically using the formula below, and we don't need to bootstrap. We want a low standard error, so if we have more data, we can calculate a lower standard error. 

If we have a larger coverage, we know more about the data distribution itself, we can also lower our standard error, or if we just have better data. In practice, though, we don't know the theoretical value, we don't actually know the full scope of distribution that our data holds, so we don't know the exact distribution. We tend to use the other two methods more frequently. But if we make the following assumptions, the error is epsilon-sub-i is the difference between our actual and estimated y-value sub i's, and the epsilon-sub-j is similarly, if they are uncorrelated, the different points in our data sets, and we make the assumption that epsilon-sub-i has a mean of zero and a variance of sigma squared sub epsilon, then we can empirically estimate sigma squared, something that we want to know for theoretical purposes of confidence intervals, from the data in our regression line. So we have a formula down here to sort of use the mean squared error as an estimation for our sigma value. A better model also reduces standard error. So what happens to the beta values, beta hat values, under these scenarios? 

Considering the significance of predictors, which predictors are actually contributing to the decisions made by the model? Okay, we're back on our question. How can we answer this question? Which of these are important? The newspaper advertising, the TV advertising, or the radio advertising? Hypothesis testing. Hypothesis testing is a formal process through which we evaluate the validity of a statistical hypothesis by considering evidence for and against the hypothesis gathered by random sampling of the data. 

So you state the hypothesis. Typically this is the null hypothesis, represented H sub naught, and an alternative hypothesis H sub one. That is the negation of the former. Here's a type of analysis, how to use the sample data to evaluate the null hypothesis. Typically this involves choosing a single test statistic, and then you sample the data and compute your test statistic, and use that value to reject or not reject the null hypothesis. How well do we actually know the model F hat? Our confidence in F is directly connected with the confidence in our betas. 

So for each bootstrap sample, we have one beta, which we can use to determine the model. We have a blue set of X's in data zero, and we have a gray set that was just added on top of with different fitting coefficients. There's one line for every bootstrapped sample. Every time you sample every parallel universe, every bucket, there's a different line. So if we line up thousands of lines for bootstrap samples, we can have this sort of fuzzy line, this gray area of smudged regression, that we can pull out our probabilities of good as the fit. For every X, we can calculate the mean of the models, F hat, with a dotted line showing here in the middle, and the 95% confidence intervals of these models in a shaded area. So we have this estimated thing, the exact mean of all of our regression models, and we have a shaded area of where things typically were with a random bootstrap distribution. 

How confident are we in this? So for a given X, we have a distribution of models, F of X, and for each of these F of X, we have a prediction for Y. The normal distribution, the function big N, with a mean of the output of F, and the standard deviation of the error term for the standard deviation. We can use that normal distribution to make the prediction confidence intervals, and drawing our Y hat prediction with 95% confidence interval. We talked a lot about regressions and some of the math that goes into them. Post questions you have in the discussion forums, or send them to our email.