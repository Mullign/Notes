Step two, we need to find the frequent item sets that we will be using in our algorithms. So, back on actually finding these frequent item sets, typically the data is kept in flat files rather than a database system. Database systems are nice, but they are just not always going to be there, and even if you have it in a database system, converting into a flat file is not too much work, so we can deal with this form of data as a standard. These things are stored on disks, and they're typically stored basket by basket, so if you have, for example, customers at an online store checking out with things, and then you just keep a log in some sort of file in a directory, you can keep everything and still use it from there. Baskets are typically really small, but we have many baskets and many items, so in order to actually expand all these into pairs, triples, quadruples, whatever, you need to use k nested loops to generate all sets of size k. In order to find the frequent item sets, we have to count them. To count them, we have to enumerate them. 

That tends to be a very heavy disk I O bottleneck. Resident data is usually in the number of disk I O operations, especially if you have anything that is not a solid state data storage device. I O is just very slow, and even with solid state devices, I O tends to be much slower than CPU cost, so in practice, association rule algorithms read the data in passes, and all baskets are read in turn. This cost is measured by the number of passes an algorithm makes over the data, and this turns out to be a critical resource of the main memory. As we read in the baskets, we need to count something. The occurrence of pairs of items, for example, the number of different things we count is limited by the main memory, and swapping counts in and out of disk is just off the table. It's way too slow, especially at scale. 

It turns out that the hardest problem is finding frequent pairs of items. Frequent pairs of items are all over the place, but frequent triples of items are rare. Frequent quadruples of items are even more rare. This is just because as the number of items that are frequently associated with each other increases, the set of frequent quadruples, quintuples, whatever, just exponentially drops. Let's talk about pairs, because that is the hardest one first, and this can be extended into larger sets. We need to generate all the item sets, enumerate them, but we only want to count the item sets that in the end turn out to be frequent. We don't have this pre-existing knowledge of which item sets are the most frequent. 

We have to actually know what they are, begin counting, and go from there. So imagine we need to identify frequent pairs of items, so we need to enumerate all of them, and rather than keeping a count for every pair, we hope to discard a lot of pairs and only keep track of the ones that will in the end turn out to be frequent. So we start off with a naive algorithm here. Each basket B of n sub B items generates its pairs by using nested loops. We will have a square being formed here of size n sub B times n sub B minus 1, all divided by 2. A data structure that keeps count of every pair is then used to count all of these. We make our square matrix, add the counts in, and this fails if the number of items squared exceeds main memory. 

These number of items can be really big, really fast. We use Walmart here as an example, but having a single data structure take up n squared space is pretty expensive. Suppose 10 to the 5th items count our 4 byte integers and each pair of items. Just using some napkin math here, for 100,000 items, we're looking at 20 gigabytes of memory being needed. So it's possible that your laptop can handle that, but that's possible that that's all your laptop can handle. So instead, we want to count pairs in memory, and we have two approaches for this. Counting the number of occurrences in each pair of items, we can keep a matrix and optimize it a little bit from our naive algorithm just now, or we can keep a table of triples. 

So the count of the pair of items ij is c. We have a trade-off here, though. If integers and item IDs are 4 bytes, then we need approximately 12 bytes for each pair. With count greater than 0, we only make an entry into this table if there is a present count. Plus, there's some additional overhead for the hash table. So before we completely abandon the matrix, we do have a trade-off to consider. We can think about these item set counts as triangles. 

When we have our first sort of square matrix that we're working with, our naive algorithm, we don't need half of that matrix. We can work all of it off of the lower half of the triangle here because it's symmetrical across the main axis. So we have 4 bytes per pair, and allocated all of the memory for the whole triangle to add up. With the triples, we only keep certain holes here, and we trade a cost of having for each entry 12 bytes per current pair instead of having everything allocated at once at a lower cost per pair. When we're actually comparing these two approaches on our first approach, we have our triangular matrix that's for n being the total number of items, and we have a count a pair of items i, j, only if i is less than j. You are working on the lower half of the matrix triangle. We can keep the pairs in a lexicographic order to structure it out, and with that, we can calculate the position of our data structure with this formula here. 

Knowing the number of items and the index you're looking for, we can calculate an efficient indexing algorithm. Total number of pairs is still n squared. N squared over 2 still means that we have a big O of n squared, and this would grow pretty quickly with a byte consideration. The triangular matrix requires 4 bytes per pair, and when we compare this to our approach 2 using 12 bytes per occurring pair, but only for pairs with counts greater than 0, approach 2 beats approach 1 if there's less than one third of the possible pairs occurring. While there is the overhead of the hash computation, and we might not have as clean of a pair position equation here, when we can meet a certain characteristic of our data, it is better to use approach 1 or approach 2. Because our ratio is 4 bytes for approach 1 to 12 bytes for approach 2, approach 2 wins if less than one third of the possible pairs occur.