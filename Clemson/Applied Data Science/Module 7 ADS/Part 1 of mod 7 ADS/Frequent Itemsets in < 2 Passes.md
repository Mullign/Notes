Cool, so we have the a priori and the PCY algorithms helping us find our frequent item sets, but they can require multiple passes to have a high degree of accuracy, and sometimes the biggest bottleneck in running those algorithms is in the number of passes. So we have a few algorithms here that we can go over about finding the frequent item sets with a smaller number of passes. So taking k passes to find frequent item sets of size k is a great way to find frequent items, maybe frequent pairs, but getting higher and higher in that becomes untenable when we have particularly large data sets or when the actual accessing of our items is very slow. For example, if it's a network operation or something, you have to wait for the whole internet pipeline to get back to you with your actual data. So we're going to be talking about these three different approaches, random sampling, SUN, and TOVON. Random sampling, don't throw away because it's just random. Randomness is actually a very useful technique for like curing having large data sets. 

We are very fortunate in data science to often have the problem of too large of data sets. That's a really great problem to have and we can leverage random sampling to help us with that. So first point about random sampling, take a random sample of the market baskets. We have some large number of baskets and we just randomly take 15%, 10% of them. We don't pay for the disk IO each time because it is a priority to run in main memory. Using support threshold proportionally to match the sample size. So if instead of having a support threshold of three, we would have one of three over 100 if we're only taking a 1% sample of our full data set. 

So you might get unlucky and have a random sample pulled that would indicate a false positive. We can optionally verify the candidate pairs are truly frequent in the entire data set in our second pass, but you don't catch the sets that are frequent in the whole data, but not in the sample. So if you have the smaller threshold, it helps catch more truly frequent item sets, but it requires more space. So it's all about trade offs between where the bottleneck actually is in the data you're working on. If it is in reading particular items, if it is in a memory bottleneck, if it is in a compute bottleneck, using these different techniques have different applications. Our Sun algorithm next, we'll talk about that a little bit more. So the Sun algorithm takes in chunks of the full data set that can fit in memory. 

If the full data set is 96 gigabytes large and you only have 16 gigabytes of RAM to run it in, if you find the frequent items within each 16 gigabyte chunk, you can have a nice performance boost by using the in-memory application of the Sun algorithm here. So when we're doing that, we're doing an a priori on our first pass. The item set becomes a candidate if it is found to be frequent in any one or more subsets of the basket. So if it's going to be frequent globally, it's likely going to be frequent locally too. On the second pass, we count all the candidate item sets and determine which are frequent in the entire set. So we have individual chunks that are frequent to that chunk. But because of our monotonicity idea, the item set cannot be frequent in the entire set of baskets unless it is frequent in at least one subset. 

So when you have a frequent item set globally, at least one chunk of your data will have it being frequent there too. However, even with the Sun algorithm, we still don't know whether we found all the frequent item sets. And that's where our next algorithm comes in. All right, Tony Voning's algorithm. Pass one, start off with a random sample. So there's our randomness coming back. It's still good for us. 

But we lower the threshold slightly for the sample. So instead of taking 1% of the baskets and using 1 over 100, we can use 1 over 125 to make a slightly lower threshold for identifying frequency. We find the frequent item sets in that sample. And then we add the negative border idea to the item sets that are frequent in the sample. We're going to have a visual of this on the next slide. But the negative border is an item set if the item is not frequent in the sample, but all of its immediate subsets are. So if you delete any element from a frequent set, it becomes frequent. 

But with all of its elements, it is not frequent. Okay, so we have a lovely MS Paint picture here. We have all of the sets ABC, BCD, all the triplets are frequent. So they belong in the green frequent items set there. But ABCD itself is not frequent. So the ABCD would be on the negative border. Okay, both passes here. 

We have the random sample up front with a slightly lowered threshold for the subsetting. Add to the item sets that are frequent in the sample the negative border of these item sets. So we are including the negative border of item sets that we're finding in the frequent items. On pass two, we count all the candidate frequent item sets from the first pass, and also count the sets in their negative border. If no item set from the negative border turns out to be frequent, then we found all the frequent item sets. So the idea is that we're including this negative border and then checking all the sort of subsets to identify when there's a breach in the border. So if we find out that something is in the negative border is frequent, we start all over again with another sample. 

Not that big of a deal because we're doing a random sample and has this lower threshold. Try to choose the support threshold to be so the probability of failure is low while the number of item sets checked on the second pass fits in main memory. You want to use as much main memory as possible because that is the fastest memory. And you want to have a configured threshold that can be effective without setting the probability of failure so high. That's a tunable parameter that we as data scientists get paid to make. This does raise a pretty immediate question though that if something in the negative border is frequent, how far does the problem actually go on to? We can visualize it as this eruption from our negative border from the frequent item sets, and we're not quite sure how far up the chain it goes of frequent item sets. 

So this leads us to our last slide of our video here, a nice little theorem. If there is an item set S that is frequent in the full data but not frequent in the sample, then the negative border contains at least one item set that is frequent in the whole. So we can prove this with a nice little proof by contradiction. Suppose not. So suppose there is an item set S frequent in the full data but not frequent in the sample, and suppose nothing in the negative border is frequent in the full data. So if we're going to think about the smallest subset of S that is not frequent in the sample but every subset is, we'll call that one T, then T is frequent in the whole. So that's the S is frequent and the idea of monotonicity sort of cascading up the frequency. 

But then T is in the negative border, and that goes against our assumptions. So we have a proof by contradiction.