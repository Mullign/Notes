We can extend these frequent item set finding algorithms using a priori algorithm using these concepts of monotonicity or frequent. A notion of candidate pairs and an extension to larger item sets is enabled so more than two indices can be used when we're using a priori. So the first thing to be thinking about with a priori algorithms here is monotonicity. If a set of items i appears at least s times, so does every subset j of i. If we have a, b, c being a frequent item set, then a, b will also be frequent. b, c will also be frequent. a, c will also be frequent. So the contrapositive for this is if item i does not appear in s baskets then no pair including i can appear in s baskets. We'll build up on this. 

So in our first pass we read the baskets and count in main memory the number of occurrences of each individual item. So instead of using a big O of n squared amount of space here we're using big O of n. It is directly proportional to the number of items. Items that appear more than a certain s number of times are frequent items. And then on our next pass we read baskets again and keep track of the count only of those pairs where both elements are frequent. This requires the memory for the counts proportional to the square of the number of frequent items not the square of total number of items plus a list of the frequent items so you know what has to be counted. Here's a visualization. 

In pass one we have our item counts taking up space in main memory and in pass two we have the frequent items so we've cut down a little bit from the item counts and then we also have the counts of pairs of frequent items candidate pairs that could be frequent. We can use the triangular matrix method with n equals the number of frequent items here. It may save space compared to storing triples depending on characteristics of the data as discussed in our last video. So we can renumber the frequent items one two three so on and keep a table relating the new numbers to the original item numbers. That way we're not actually losing track of which items are talking about and where they are in the original data set. So for each k we construct two sets of k-tuples sets of size k sets of size one two three singles doubles triples. T sub k is the candidates k-tuples those that might be frequent sets with support greater than some threshold based on information from the pass for k minus one. 

So lk is the set of truly frequent items. We can think about this as filtering and constructing iteratively as we go through so all the items start with c1 we count the items we construct the pairs of items from l1 using a triangle method perhaps count those pairs and filter it down and we keep going on. We'll explain the further example here in a second. So we're going to start with c1 and we're going to we'll we'll explain the further example here in a second.