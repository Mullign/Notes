Cool, so last time we talked about a priori. Here we'll be talking about the Park-Chen-Yu algorithm, which came around in like 1995 and uses a hash map and this idea of frequent buckets to make a significantly more memory efficient algorithm. So some observations from last time, most of the memory was idle, we had this great big triangle of data points that were being mostly unused and we were storing only the individual item counts. So can we use that memory in a more efficient way? In addition to the item counts, we can maintain this hash table to keep account of the bucket as things are hashed. When we have significantly slower IO operations, maybe a spinning disk or a network operation, the efficient hashing is going to become an immense speed up as well. So it's memory and time saving here. 

So here's our PCY algorithm for each basket. We go through and we get the items in each basket and add one to that item's count. So we still have to go through and make that initial each item gets a count pass, but new this time for each pair of items, we hash that into a bucket and add a count to that bucket. So for example, if we have one and two in a pass, we would additionally hash and increment the one two pairing. So these pairs need to be generated from the input file. They're not present in the file, so we're not sure which pairs are going to be making exactly. This is where a lot of the space saving comes from, from the a priority algorithm, where instead of having that great big triangle that was full of mostly empty space, we have exactly the hash pairs that we need. 

We're not just interested in the presence of a pair, but we need to see some support if it is frequent or not. So incrementing that count into the hash is going to actually tell us things that we need we want to know between passes. So just a couple observations about the buckets, i.e. the hash map being used to make the pairs. If a bucket contains a frequent pair, then the bucket is surely frequent. If we have a frequent pair that is in there, it will be represented in this bucket. However, even without any frequent pair, a bucket can still be frequent. So we can't use the hash to eliminate any member of a frequent bucket, but for a bucket with total count less than S, as in we don't have enough support of its frequency, call it frequent, none of its pairs can be frequent. 

So if the entire bucket is below the threshold, then none of the pairs in that bucket can be frequent. But if the bucket has a frequent pair, it will be frequent as a whole bucket. On pass 2, only count the pairs that hash to frequent buckets, so we can pare down really quickly into which counts we are actually maintaining as we go through our passes. Let's talk about the between pass step here, where we replace the buckets with a bit vector. Bit vectors we can think about as a very compact data structure that uses a single bit to represent a presence or absence of something, instead of using the full 4-byte integer counts that were previously being used. So already we're at 1 32nd of the memory being used, also deciding which items are frequent, so we can use this for the second pass to make that even faster. On our second pass of PCY, you count all the pairs, for example IJ that meet the conditions for being a candidate pair, i.e. both of them are frequent items, and the pair hashes to a bucket who's in the bit vector. 

So we need to know that both of the smaller items are frequent and that it has survived the first reduction. Both conditions are necessary for the pair to have a chance of being frequent because of monotonicity. So here's just a common visualization of the main memory used by PCY. On our first pass, we have our item counts where we're taking ABC, having a count for each one, and then we have the hash table for the pairs taking up likely more space. In the second pass, the frequent items are eliminated by the item counts, and the bitmap is created from the hash table. So we have a significantly smaller amount of memory being used because of both of these space-saving techniques. The leftover, well, not leftover, but the rest of the space used by PCY in pass 2 goes into counts of the candidate pairs and so on for further passes.