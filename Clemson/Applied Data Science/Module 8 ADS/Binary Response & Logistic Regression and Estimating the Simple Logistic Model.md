So our binary response and logistic regressions we can define in this way. Logistic regressions address the problem of estimating a probability to be outside the range of 0, 1 and the logistic regression model uses a function called the logistic function to model that probability. Here it is. It uses Euler's number and so we have e to the beta 0 plus beta 1. It's just in a mathematical form here. So as a result of using this prediction with an s-shaped curve, which is the general shape of a logistic function, we have beta 0 shifts the curve right or left by negative beta 0 over beta 1 and beta 1 controls how steep it is. We have ways that we can change how these s-shaped curves look. 

I think this is really well summarized in this figure. We have moving left and right by the negative beta 0 over beta 1. We have an approximation of the slope at the highest derivative at 2 beta 1 and the value of the curve at that point for beta 1 over 4. This becomes really important when we are choosing our beta values to find logistic regressions that most accurately represent our data. So if we have a ton of data that is on the left half of our x-axis, for example, and we know all of that is going to be in 0 and it's only until x is equal to 80 or higher that it becomes 1, we need to be able to fit our s-curve around the data we have. Here we can see changing the beta 1 is changing the angle in the middle of the s-curve and when we go into a negative value it flips it around. So we're not restricted to having the left side being 0 and the right side being 1. We can mirror shift as much as we want. 

With a little bit of algebra work, our logistic model can be written in this form and the value inside the natural log function is called the odds. Thus the logistic regression is said to model the log odds with a linear function of the predictors or features aka x. This gives us the natural interpretation of the estimates similar to linear regression. A one unit change in x is associated with a beta 1 change in the log odds that y is equal to 1 or a one unit change in x is associated with a e to the beta 1 change of the odds that y equal 1. Having this logistic understanding will help when you are looking at a model that is not fitting very well and you're trying to find out what needs to be tuned to actually figure out how to adjust your model to be more useful. Okay so we want to estimate and actually do one of this. This is applied data science. We want to actually do it. We have a probability of y equaling 1, that's p, so the probability that y equals 0 should be everything that is not in that p value, 1 minus p. 

So we can write this in terms of exponents here where p is the probability that y equals 1 and x where p is the probability that y equals 1 and x is x and therefore p depends on x. Thus not every p is the same for each individual measurement. We don't have to have the same output, the same probability for every measurement. Keeping all of these separate is just like us having all of our parallel universes for the linear regressions. Okay so looking back at our heart data set here with our logistic estimation we'd like to predict whether or not a person has heart disease and here are data points down below. How do we actually visualize this data? With a logistic equation we can sort of have this s curve going on where it goes from 1 to 0, left to right, to kind of best fit we can see on on the bottom maybe it's a little bit further to the right clustered out, but the the more important thing is that we're not telling anybody that they have a over 100% chance that they have heart disease. Using logistic estimations like this let us tune these curves but we're still motivated by having models that accurately and naturally represent our data. 

So there are lots of ways to fit these curves. The most straightforward one is to use a sklearn which I believe you'll be seeing in lab as well. When we are using this in particular we want to be thinking about this this 10,000 variable to actually be able to tune how many data points we are using here and this will give us back our beta 1 and beta 0 values to interpret with our logistic equation. The uncertainty for estimates for beta 0 and beta 1 can be quantified and used to calculate both confidence intervals and hypothesis tests for distributions and the estimate of these standards of these estimates are likelihood based which is based on something called Fisher's information. That is beyond the scope of this class unfortunately but do get a google if you're interested in the math that is driving this even deeper. Due to the nature of the underlying Bernoulli distribution if you estimate the underlying proportion p sub i you get the variance for free. That's cool. Because of this the inferences will be based on the normal approximation and not t distribution based. We could always bootstrap the results to perform these inferences as well. That is once again unfortunately outside the scope of this particular class.