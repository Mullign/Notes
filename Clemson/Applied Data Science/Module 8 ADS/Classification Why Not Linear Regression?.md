This week we're going to begin classification with logistic regressions. So just to start off, we're going to be talking about why not linear regressions. We've already had a lot of really useful tools given to us through linear regressions, so why do we even need to be thinking about logistic? A lot of data does not have a continuous response variable. So here we have a dataset with age, sex, chest pain, lots of data, and then a response variable on the end, yes, no, is there heart disease? So we have binary outcome for heart disease for 303 patients who presented with chest pain. An outcome value of yes would indicate that the presence of the heart disease is based on an angiographic test, and no means no heart disease. 

More no's please. There are 13 predictors for this that are taking different forms. We have age being a continuous value, sex being zero or one in this particular dataset, and lots of other heart and lung function measurements still making a categorical response variable. So we've seen a lot of models for the prediction of quantitative response variables, number of taxi pickups, number of, number of, number of, whatever. Linear regressions perform really well for that. That's what they're good at, that's what they're made for. But when the response variable is categorical, then the problem is no longer called a regression problem but is instead labeled a classification. 

Sometimes you don't need to guess a number of something, you just need to know the type of thing that will be coming out of it. So the goal here is to attempt to classify each observation into a category, class, cluster, whatever you want to call it, any of these nebulous terms, defined by Y and based on a set of predictor variables X. So the motivating examples for this lecture, however it's being broken up, is mostly based on medical datasets, heart disease, cancer, et cetera, trying to determine where the cutoff for some of these diagnostic tests, pregnancy, prostate, or breast cancer screen tests, is a difficult problem. Trying to determine if a cancer has gone into remission based on treatment, trying to classify patients into types or classes of diseases based on various genomic markers. So we can think about a simple classification example. Given a dataset, data pairs X1, Y1, X2, Y2, where there are Y categorical, sometimes referred to as qualitative, we would like to be able to predict the category Y takes on any given X. A categorical variable, Y, could be encoded to be quantitative, sure. 

For example, if Y represents a concentration of Clemson undergraduates, then Y could take on one of the values, one if it's a computer science major, two if it's statistics, three mathematics, four agrarian sciences, otherwise. Classification does not work well or is inappropriate in this session because there's no relationship strictly between one and two. Statistics is not the average of computer science and math. It's a whole separate thing. So a linear regression model could be used to predict Y from X, but that would be a problem here. The model would imply a specific ordering of the outcome and would treat that one unit changed in Y equivalent. So the move from computer science to statistics just doesn't make sense. 

It's not the same difference of majors as statistics to whatever comes after it. So the response variable could be reordered such that Y represents statistics and two represents computer science and nothing really changes. So we need a whole separate model to account for this when we have what's known as ordinal data. If the categorical response variable was ordinal, has a natural ordering, then a linear regression would be better, but still not great. Because when we have ordinal data, there's an ordering, but it's not necessarily the same amount of steps between each point. For example, going one, three, four, six, eleven, twelve, that sequence is ordinal, but the difference between the values would leave us wanting for what we could actually apply to that in useful data terms. So, binary responses are one of the most common uses for classifications when the response has only two categories and then an ordering of the categories is natural. 

For example, an upperclassman and a Clemson student could be categorized as one if they live in the quad and zero if otherwise. Linear regression could be used to predict Y directly from a set of covariates. And our approximation would be better than half if we could predict the student lives in quad and predict other houses at less than 0.5 probability. Having a catch-all like this really does simplify it down. It's very useful, but it is not always the most natural way to represent our data. When we're making models, we want to find natural representations because those tend to make very clean computations. So, what could go wrong with this particular model? 

We have some data up top and we have some data down below, and we have a linear regression that is drawing a straight line straight down. We have all of these predictions in the middle where our probability is more or less completely irrelevant to the data that we are feeding into our model. It's nonsense. When we have nonsensical values for Y, since the modeling for the probability that Y is equal to 1, the values for our model below 0 and above 1 would be at odds with the natural measure. It just doesn't agree with our data. So instead, we need to have a prediction function that looks something like this. We start off on the bottom and we just shoot right up to 0 to 1 for our probability at some point in some way. 

Comparing this to a linear regression model, we have a linear straight line or a logistic curve. This is why it's called logistic regressions.