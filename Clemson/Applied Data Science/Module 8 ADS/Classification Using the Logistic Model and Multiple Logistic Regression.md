Okay so we have talked about estimating using simple logistic models and we need to actually get into classification using these logistical models. So how can we actually use a logistic regression model to perform our classification, i.e. how can we predict when y is 1 versus y is 0, our response variable. We mentioned before we can classify all observations for which our probability model for y equals 1 has to be greater than or equal to 0.5 to be in a group associated with y equals 1 or on the other side less than 0.5 to be in a group associated with 0. This is known as a Bayes classifier, just having this model classification such that we either go all the way up to 1 or down to 0. So the Bayes classifier takes the approach that assigns each observation to the most likely class given the predictor values. This, like all models, has good use cases and bad use cases. The Bayes classifier is the one that minimizes the overall classification error rate. So just like how we would do a mean square error and minimize the error with that, we can also minimize the error here using this last function. So think back to our heart data scatter plots with the, you know, some of the data points on the top and some on the bottom. 

Consider if this would be a good function to minimize or not. This has the potential to be a good classifier if the predicted probabilities are on both sides of 0 and 1. So we need to extend this into more than two categories eventually, but our general use case is if the probabilities happen on both sides. That brings us into our multiple logistic regression points, which we need to consider because it's really easy to think about these simple binary yes-no's, but this more or less easily generalizes to the situation where there are multiple predictors. A lot of the same details as linear regression still holds into logistic regressions, so like interactions can be considered, multicollinearity is a concern, and overfitting, all the goods and the bads. So how do we actually correct for such problems? We can have the regularization and checking through the training test and cross-validation just like with our linear regressions. We'll get into the details of this along with other extensions of logistic regressions in the next lecture, but let's go through a step-by-step of the classifier with two predictors. 

So how can we estimate a classifier based on the logistic regression? So how can we estimate a classifier based on a logistic regression for the following plot? We have these two kind of groups of sort of yellowish points in the top left and bluish points in the bottom right, and there's a non-linear division line between them pretty much. We have some blue and yellow crossing over into the other areas as well. Earlier we saw the general form of this simple logistic regression, meaning there was just one predictor using the model. So in terms of linear predictors, that can be mathematically represented here. Log of the probability that y is one over one minus probability that y is one could be seen as beta sub naught plus beta sub one of x. Multiple logistic regressions generalization using multiple predictors just like with their linear regressions. So specifically we can define our multiple multiple logistic regression model to predict the probability that y is one generalized here below. 

The estimation procedure is identical to that we used in the simple logistic regression, so a likelihood approach is taken and the function is maximized across all the parameters using some sort of iterative method like Newton-Raphson. The actual fitting of the logistic model is really easy using software. We have sklearn, we have Python, we get to use the computers. This is applied data science. So we get to use these packages as the iterative maximization of the likelihood has already been hard-coded and forced. So this is the sklearn linear model package. You just have to create your multi-dimensional design and use it as predictors for the logistic regression function. We'll see some of that here and you'll see it again in lab. Interpreting this is where the human comes back in. Interpreting the coefficients in a multiple logistic regression is similar to a linear regression. Since there are other predictors in the model, our coefficient estimate is the association between the jth predictor and the response on a logon scale. So do we have to say controlling for our other predictors in the model? 

We're trying to attribute the partial effects of each model controlling for the others. So we have these large sets of data coming in that are possibly confounding the data and when we have our beta values come back out, that can indicate to us which of our input variables are actually contributing to the model. Okay, so getting back into the heart data, we have our, you know, top and bottom data points. We are attempting to predict whether someone has heart disease based on the max heart rates and whether the person is female or male. The simultaneous effect of these two predictors can be brought into the model here and when we're using our logistic interpretation here, we have our max heart rate on the top log and our gender on the bottom, giving us different beta values to interpret. So when we put this into our Python code with rsk.learn, we have the max heart rate and sex input variables in our first line here and running through our code, we have our estimated betas down below. Just like in a linear regression, interaction terms can be considered in a logistic regression. 

So our interaction term is incorporated into the model in the same way and the interpretation is very similar just on a logarithmic scale, logistic regression. Write down the heart data for the two predictors plus the interactions term like I'm going to show on the next slide. So it's the exact same code except on our first line we have our df heart interaction term, which is the heart rates times the sex, and we have the interaction term also added into our data x before it is added into our full sklearn evaluation. So we have these logistic regression tools that can help us make useful interpretations and insights from data that linear models would fail on. When we have a categorical variable, when we have these like bins that are perhaps ordinal but not evenly spaced as continuous data would be, logistic regressions are the more appropriate choice. Leave any questions in emails, in forum posts, and have fun in lab.