​Okay, so we're going to start off with a single neuron network. This is a single perceptron. ​So when we're thinking about our logistic regression, we have a formulation something ​like this. We have for every data point x sub i our affine function, which is a linear function ​plus a constant, so it's a straight line. We have our activation function, which is our logistic ​s-curve, which allows us to engage in classification problems, and this leads to a loss function, ​which is showing how good of an estimation this is for our data set. For each data point, we have ​x sub i, x sub k, and so on and so forth. We can bring this together and drop our subscript ​notation into a big X for the full vector of the data notation, leading to full activation ​and a full loss function using the sigma summation notation. So we take that previous ​logistic regression condensed notation here, and we bring it into another series of ​equations that we can now break into our estimation to model this particular logistic ​regression set. So we have our set of data, big X coming in at the top, which we can replace down ​below with our w and our y hat for our model that we can train and then learn the appropriate ​weights on each set. This is a single neuron network, so logistic regression can be thought ​as a single neuron in a network. This particular function is the sigmoid activation. 

​It takes all of this work to get to that point that we then once again can collapse using ​notation into the sigmoid activation function of xw input set x to response variable y. ​We can layer these perceptrons on top of each other, the single neuron networks, into ​multi-layer perceptrons, and let's see this be built up by the heart data example set. ​So we have our max heart rate and our heart disease, yes, no response, but we can see that ​in the top here it's kind of clustered towards the middle, and on the bottom it's split into ​the left and right sets. So we can choose a w such that we get this particular set. The w is our ​training set, and using that we can make a single perceptron which can approximate sort of the right ​half of this. We are completely losing the left half of the no side of our data, ​but we can hit the right side pretty well. ​Similarly, we can hit the left side using a different w, just another set that we're pulling ​out. So if we take both of these together, we have our left and our right, we can combine them ​to have an even more accurate representation. We want the best of both worlds here. 

​So to combine these neurons together, we start off with the same input data x, ​and we split into our two sigmoid functions xw1, xw2, to make our sets y and y'. Remember, ​this is the right hand and left hand sides of our function that we're trying to estimate, ​and we combine them back together with each other using an affine function, ​just some sort of weighted linear function to make the graph have the lower on the left, ​high in the middle, low on the right function shape. But this isn't a probability. So when we ​have this q equals h1 plus h2, the outputs of our sigmoids, we have this linear function here. It's ​still not a probability. So in order to have a probability come through, we have our same ​introduction here. We have our sigmoid with our two training sets, w1, w2, leading these functions, ​and sigmoid activation function at the end as well to have h values come into play. And so when we ​pass this through, now we have our probability back, and it maintains the structure of our ​neural network. So here we can have our loss function be seen with our natural logs. Lovely. 

​We still don't know what w1, w2, and w3 are. We have to learn these weights so that we can ​actually go through and combine these things. We're working on the assumption that having these ​weights are arbitrarily chosen to have these shapes, but we do need to find them eventually. ​As we're looking through and looking for these different weights, we can see how this changes ​the shape of our function, and different weights change the shape and position. We have, you know, ​this darker lower node representing the left-hand side, and the upper sort of lighter ​node on the right-hand side. When we combine them together, we can have this very severe, ​very sharp peak in the middle. Or if we have an additional layer of our neurons, ​we can actually model any reasonable function. So reasonable meaning it's not some sort of exotic ​function. We're not trying to trick you with, you know, mathematical definitions and such, ​just like normal functions that you're going to be seeing in the world. 

​So having these different architectures can allow us to model increasingly complex functions. ​Normal does not mean, you know, simple, but having these tools to build our functions together ​lets us more accurately model the data that is given to us in the real world, and make useful ​insights and conclusions about the data that we can gather. ​Okay, quick summary. A single neuron is a logistic regression unit. We'll soon see other choices. ​A neural network is a combination of logistic regressions or other types of units, and a neural ​network can approximate non-linear functions. So remember the Cartesian versus radial coordinate ​pairs. We'll be talking about the activations, the neurons, how to put these things together ​into the architectures in the next lecture, as well as how to estimate the weights and biases ​to make sure that we are actually producing a useful model, and how to regularize these ​neural networks for standardization purposes. Send along any questions you have, and have fun in lab. 

​Okay, so we're going to start off with a single neuron network. This is a single perceptron. ​So when we're thinking about our logistic regression, we have a formulation something ​like this. We have for every data point x sub i our affine function, which is a linear function ​plus a constant, so it's a straight line. We have our activation function, which is our logistic ​s-curve, which allows us to engage in classification problems, and this leads to a loss function, ​which is showing how good of an estimation this is for our data set. For each data point, we have ​x sub i, x sub k, and so on and so forth. We can bring this together and drop our subscript ​notation into a big X for the full vector of the data notation, leading to full activation ​and a full loss function using the sigma summation notation. So we take that previous ​logistic regression condensed notation here, and we bring it into another series of ​equations that we can now break into our estimation to model this particular logistic ​regression set. So we have our set of data, big X coming in at the top, which we can replace down ​below with our w and our y hat for our model that we can train and then learn the appropriate ​weights on each set. This is a single neuron network, so logistic regression can be thought ​as a single neuron in a network. This particular function is the sigmoid activation.  Play video starting at :2:1 and follow transcript2:01​It takes all of this work to get to that point that we then once again can collapse using ​notation into the sigmoid activation function of xw input set x to response variable y. ​We can layer these perceptrons on top of each other, the single neuron networks, into ​multi-layer perceptrons, and let's see this be built up by the heart data example set. ​So we have our max heart rate and our heart disease, yes, no response, but we can see that ​in the top here it's kind of clustered towards the middle, and on the bottom it's split into ​the left and right sets. So we can choose a w such that we get this particular set. The w is our ​training set, and using that we can make a single perceptron which can approximate sort of the right ​half of this. We are completely losing the left half of the no side of our data, ​but we can hit the right side pretty well. ​Similarly, we can hit the left side using a different w, just another set that we're pulling ​out. So if we take both of these together, we have our left and our right, we can combine them ​to have an even more accurate representation. We want the best of both worlds here.  Play video starting at :3:32 and follow transcript3:32​So to combine these neurons together, we start off with the same input data x, ​and we split into our two sigmoid functions xw1, xw2, to make our sets y and y'. Remember, ​this is the right hand and left hand sides of our function that we're trying to estimate, ​and we combine them back together with each other using an affine function, ​just some sort of weighted linear function to make the graph have the lower on the left, ​high in the middle, low on the right function shape. But this isn't a probability. So when we ​have this q equals h1 plus h2, the outputs of our sigmoids, we have this linear function here. It's ​still not a probability. So in order to have a probability come through, we have our same ​introduction here. We have our sigmoid with our two training sets, w1, w2, leading these functions, ​and sigmoid activation function at the end as well to have h values come into play. And so when we ​pass this through, now we have our probability back, and it maintains the structure of our ​neural network. So here we can have our loss function be seen with our natural logs. Lovely.  Play video starting at :4:49 and follow transcript4:49​We still don't know what w1, w2, and w3 are. We have to learn these weights so that we can ​actually go through and combine these things. We're working on the assumption that having these ​weights are arbitrarily chosen to have these shapes, but we do need to find them eventually. ​As we're looking through and looking for these different weights, we can see how this changes ​the shape of our function, and different weights change the shape and position. We have, you know, ​this darker lower node representing the left-hand side, and the upper sort of lighter ​node on the right-hand side. When we combine them together, we can have this very severe, ​very sharp peak in the middle. Or if we have an additional layer of our neurons, ​we can actually model any reasonable function. So reasonable meaning it's not some sort of exotic ​function. We're not trying to trick you with, you know, mathematical definitions and such, ​just like normal functions that you're going to be seeing in the world.  Play video starting at :5:51 and follow transcript5:51​So having these different architectures can allow us to model increasingly complex functions. ​Normal does not mean, you know, simple, but having these tools to build our functions together ​lets us more accurately model the data that is given to us in the real world, and make useful ​insights and conclusions about the data that we can gather. ​Okay, quick summary. A single neuron is a logistic regression unit. We'll soon see other choices. ​A neural network is a combination of logistic regressions or other types of units, and a neural ​network can approximate non-linear functions. So remember the Cartesian versus radial coordinate ​pairs. We'll be talking about the activations, the neurons, how to put these things together ​into the architectures in the next lecture, as well as how to estimate the weights and biases ​to make sure that we are actually producing a useful model, and how to regularize these ​neural networks for standardization purposes. Send along any questions you have, and have fun in lab. ​Okay, so we're going to start off with a single neuron network. This is a single perceptron. ​So when we're thinking about our logistic regression, we have a formulation something ​like this. We have for every data point x sub i our affine function, which is a linear function ​plus a constant, so it's a straight line. We have our activation function, which is our logistic ​s-curve, which allows us to engage in classification problems, and this leads to a loss function, ​which is showing how good of an estimation this is for our data set. For each data point, we have ​x sub i, x sub k, and so on and so forth. We can bring this together and drop our subscript ​notation into a big X for the full vector of the data notation, leading to full activation ​and a full loss function using the sigma summation notation. So we take that previous ​logistic regression condensed notation here, and we bring it into another series of ​equations that we can now break into our estimation to model this particular logistic ​regression set. So we have our set of data, big X coming in at the top, which we can replace down ​below with our w and our y hat for our model that we can train and then learn the appropriate ​weights on each set. This is a single neuron network, so logistic regression can be thought ​as a single neuron in a network. This particular function is the sigmoid activation.  Play video starting at :2:1 and follow transcript2:01​It takes all of this work to get to that point that we then once again can collapse using ​notation into the sigmoid activation function of xw input set x to response variable y. ​We can layer these perceptrons on top of each other, the single neuron networks, into ​multi-layer perceptrons, and let's see this be built up by the heart data example set. ​So we have our max heart rate and our heart disease, yes, no response, but we can see that ​in the top here it's kind of clustered towards the middle, and on the bottom it's split into ​the left and right sets. So we can choose a w such that we get this particular set. The w is our ​training set, and using that we can make a single perceptron which can approximate sort of the right ​half of this. We are completely losing the left half of the no side of our data, ​but we can hit the right side pretty well. ​Similarly, we can hit the left side using a different w, just another set that we're pulling ​out. So if we take both of these together, we have our left and our right, we can combine them ​to have an even more accurate representation. We want the best of both worlds here.  Play video starting at :3:32 and follow transcript3:32​So to combine these neurons together, we start off with the same input data x, ​and we split into our two sigmoid functions xw1, xw2, to make our sets y and y'. Remember, ​this is the right hand and left hand sides of our function that we're trying to estimate, ​and we combine them back together with each other using an affine function, ​just some sort of weighted linear function to make the graph have the lower on the left, ​high in the middle, low on the right function shape. But this isn't a probability. So when we ​have this q equals h1 plus h2, the outputs of our sigmoids, we have this linear function here. It's ​still not a probability. So in order to have a probability come through, we have our same ​introduction here. We have our sigmoid with our two training sets, w1, w2, leading these functions, ​and sigmoid activation function at the end as well to have h values come into play. And so when we ​pass this through, now we have our probability back, and it maintains the structure of our ​neural network. So here we can have our loss function be seen with our natural logs. Lovely.  Play video starting at :4:49 and follow transcript4:49​We still don't know what w1, w2, and w3 are. We have to learn these weights so that we can ​actually go through and combine these things. We're working on the assumption that having these ​weights are arbitrarily chosen to have these shapes, but we do need to find them eventually. ​As we're looking through and looking for these different weights, we can see how this changes ​the shape of our function, and different weights change the shape and position. We have, you know, ​this darker lower node representing the left-hand side, and the upper sort of lighter ​node on the right-hand side. When we combine them together, we can have this very severe, ​very sharp peak in the middle. Or if we have an additional layer of our neurons, ​we can actually model any reasonable function. So reasonable meaning it's not some sort of exotic ​function. We're not trying to trick you with, you know, mathematical definitions and such, ​just like normal functions that you're going to be seeing in the world.  Play video starting at :5:51 and follow transcript5:51​So having these different architectures can allow us to model increasingly complex functions. ​Normal does not mean, you know, simple, but having these tools to build our functions together ​lets us more accurately model the data that is given to us in the real world, and make useful ​insights and conclusions about the data that we can gather. ​Okay, quick summary. A single neuron is a logistic regression unit. We'll soon see other choices. ​A neural network is a combination of logistic regressions or other types of units, and a neural ​network can approximate non-linear functions. So remember the Cartesian versus radial coordinate ​pairs. We'll be talking about the activations, the neurons, how to put these things together ​into the architectures in the next lecture, as well as how to estimate the weights and biases ​to make sure that we are actually producing a useful model, and how to regularize these ​neural networks for standardization purposes. Send along any questions you have, and have fun in lab. ​Okay, so we're going to start off with a single neuron network. This is a single perceptron. ​So when we're thinking about our logistic regression, we have a formulation something ​like this. We have for every data point x sub i our affine function, which is a linear function ​plus a constant, so it's a straight line. We have our activation function, which is our logistic ​s-curve, which allows us to engage in classification problems, and this leads to a loss function, ​which is showing how good of an estimation this is for our data set. For each data point, we have ​x sub i, x sub k, and so on and so forth. We can bring this together and drop our subscript ​notation into a big X for the full vector of the data notation, leading to full activation ​and a full loss function using the sigma summation notation. So we take that previous ​logistic regression condensed notation here, and we bring it into another series of ​equations that we can now break into our estimation to model this particular logistic ​regression set. So we have our set of data, big X coming in at the top, which we can replace down ​below with our w and our y hat for our model that we can train and then learn the appropriate ​weights on each set. This is a single neuron network, so logistic regression can be thought ​as a single neuron in a network. This particular function is the sigmoid activation.  Play video starting at :2:1 and follow transcript2:01​It takes all of this work to get to that point that we the: Added to Selection. Press [⌘ + S] to save as a note Like Dislike Report an issue : Added to Selection. Press [⌘ + S] to save as a note Like Dislike Report an issue : Added to Selection. Press [⌘ + S] to save as a note