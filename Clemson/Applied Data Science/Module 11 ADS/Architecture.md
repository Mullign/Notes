​Okay, finally we have the architecture. With all of these data considerations, with the continuous, ​with the discrete, with the distributions, with the outputs, with the loss functions, ​how do we actually hook together our nodes to produce our network? Well, with one hidden ​node here, this is not some sort of generalized distribution here, this is one input, ​one hidden node, and one output, we can approximate something about like this. ​If we're predicting the value of a sine wave, ​we have the sine wave here in dots and the prediction in solid. ​Adding together another hidden node in the middle can give us sort of two bends in our prediction. ​Adding together three gives us sort of three bends. ​Adding together n number of hidden nodes can give us n bends in this model, which ​may or may not be, you know, feasible. So we need to think about how we can sort of ​elongate and how having these chains of hidden nodes can change where these bends happen in our ​model. So previously we had one hidden node giving us one bend, now we have two hidden nodes, ​so we still get two bends. But instead of having it happen sort of like we did previously where it ​was focused on the sort of right hand side of this, we can have two bends in the middle with ​two hidden nodes in the middle. When we actually fully connect these, each hidden node, we have ​sort of a two-by-two configuration here. Each hidden node is connected to each other. We can ​get very, very accurate stuff very quickly. So with a three-by-three we can get even more accurate. 

​It's honestly amazing. This is very cool to see how quickly we can converge onto an accurate model. ​So with how accurate these models can become on model sets, we can have the universal ​approximation theorem make an appearance. If we think about a neural network as a function ​approximation, remember way back at the very beginning where we have some sort of output ​based on an input and an error, one hidden layer is enough to represent an approximation of any ​function to an arbitrary degree of accuracy. So why do we go deeper? We can think about the shape ​of these networks having a width and a depth. Here the train data is going up, starting with an input ​and going up through our weights into a final output. So a shallow net may need some perhaps ​exponentially more width, and a shallow net may overfit more. So having a deep net with an ​appropriately wide number of nodes is how we get to actually good and reasonable models to ​make useful insights into data. Fortunately, we have a large number of researchers investigating ​how accurate these things get in very fast ways. So Goodfellow here in 2017 ​showed that the accuracy percent does grow with depth, but it's a sort of logarithmic ​return here. With more layers we can get faster at more accurate pretty quickly, ​but it tapers off with too many layers. 

​When we have a shallow net here, a three convolutional, three fully connected, or 11 ​convolutional, the number of parameters does have an impact in how quickly these things ​train and how accurate they can achieve. So it's not just the depth. Having more parameters, ​having a better encoded input, having better architecture can produce more accurate ​neural networks, and how these things sort of train and perhaps even go down in accuracy with ​too many parameters is an ongoing field of research. The 11 layer net generalizes better ​on the test set when controlling for number of parameters, but the three layer nets perform worse ​even with a similar number of parameters. So it really is an ongoing field. Don't worry about what ​convolutional means. Every neural net that we've seen so far has been fully connected. 

​It's just a special type of neural network. It's used for images. We'll talk about that soon. ​Write down any questions you have. Have fun in lab. It's some cool stuff in this module.