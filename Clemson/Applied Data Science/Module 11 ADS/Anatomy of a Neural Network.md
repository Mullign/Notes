​So last time we talked about perceptrons and single neuron networks, so how are we going ​to actually be building these things into full networks? ​We call that the anatomy of a neural network. ​So we have a singular neuron node here, we have an input, we have the neuron node, and ​then the output. ​This is an affine transformation and an activation function that takes us from the inputs to ​the output. ​And we'll talk later about how the choice of activation function, so far we've only ​talked about the sigmoid, but there are lots of other choices for better training on particular ​datasets that we can identify. ​So this is a simple artificial neural network. ​We have an input layer, a hidden layer, and an output layer. 

​When we have our weights, we have our inputs, our X, our weights, our W, and our output ​is Y. ​Remember the hat means it's a model, so it's our approximation of the actual Y. ​We represent our weights with a matrix for a reason that we'll see in a moment here. ​We can have our W1 here up top with W sub 1, you can see in the Z1 function or the Z2 ​function, W sub 2. ​So we are indexing into W with two indices, W sub 2, 1 for example, for a particular location ​in a matrix. ​Finally, we have our Y model, which is a affine of our H's that we calculate through our weighted ​training. ​That's our output function. 

​From our output function, we can make our loss function and figure out if the model ​is actually any good or not. ​So we'll talk about this choice of output layer. ​There are other types of output layers. ​Essentially every single component of this neural network can be swapped out with different ​tunables, and that's why data scientists get paid the big bucks. ​Okay, so we have added in another hidden layer. ​We have hidden layer 1, hidden layer 2, in the middle here. ​It's still fully connected, every hidden layer, it goes to every other hidden layer, left ​to right. 

​So we can generalize our sort of hidden layers here by having n hidden layers and using this ​dot dot dot notation to indicate that there's a gap there of some variety following the ​same pattern as the input layer to the hidden layer. ​The choice of how many hidden layers you have is something that we can tune. ​We have an overfitting issue or just a computational training issue. ​You don't need as many hidden layers as you can compute all the time. ​If you can save the computation and still have the same accuracy, you get more models, ​you get more work done. ​Similarly, we can also talk about how many nodes are in each hidden layer. ​So we have the n layers, left to right, but also n nodes, top to bottom. 

​So we have three nodes here, and we can use a similar dot dot dot notation to show the ​sort of number of nodes here represented with m. ​Number of inputs we call d. ​It's specified by the data. ​Sometimes you have, you know, 14 input variables that you want to consider. ​Sometimes you have 3,000, sometimes you have 8 million, sometimes you have one. ​Having the input data to your model be encoded to best fit the input layer is the job of ​the data scientist and is often handled by sort of the natural and tidy cleaning of your ​data. ​Overall, we have the input layer, we have hidden layers, and we have the output layer. 

​The input layer is defined by the data. ​So we as data scientists have control over the hidden layers and the output layer. ​Why do we even have layers? ​Because the representation matters. ​Remember our Cartesian coordinate versus polar coordinates distinction? ​Having a number of hidden layers is what allows these networks to actually learn these representations ​and can unlock new representations of data to produce insights for us or save on efficiency ​and give the appropriate insight for the time we need it in. ​So with these learning multiple components, we have rule-based systems. 

​This is sort of your traditional program, hello world, programming as it was in the ​50s. ​You know, we have the input, the hand-designed program that you write, and then the output. ​We have the classical machine learning. ​So if you think about, well, ironically, also the 50s, there's the inputs, the hand-designed ​features that are cleaned up by hand and made as pristine, shiny as they can be. ​Then the mapping from the features is learned and output is calculated. ​Representation learning, however, we have the input, the features, and the mapping from ​the features are both learned before the output is interpreted. ​And then when we add one more in here, we have deep learning, which extracts the simple ​features, has additional layers for more abstract features, those are mapped, and then ​it's output. 

​So we have a highly learned process of going from input to output. ​And the canonical example that every single person sees when they're learning neural networks ​is the mince data sets about handwritten digit recognition. ​So we have some numbers here. ​The first row is zeros, the second row is ones, but we can see that already in the ones, ​we have some people writing one with a single straight line. ​Some people write a one with serifs on the top and the bottom. ​Some people's ones are really straight up and down, and some are angled, but we can ​all recognize that they're ones. ​Having the repeated compositions, the depth of the neural network, can allow us to extract ​the features of this data set to make a useful interpretation of this data. 

​So the input pixels, outside the scope of this particular slide deck, but formatting ​these pixels as a series of black and white, 0, 1, we can have an input layer of the visible ​layer of the text. ​The first hidden layer could be identifying the edges. ​The second hidden layer could be identifying the corners and contours. ​The third hidden layer, the object parts, until eventually, we have our output layer, ​which is 0, 1, 2, 3. ​Which number do we think it is? ​And all of these will have some sort of percentage chance calculated by the neural network. ​And hopefully, if you're lucky, the output node will have the highest percentage chance ​correlating to the value that the author of the number was intending in the first place. 

​So linear models can be fit very efficiently. ​We have a convex optimization, we have strategies for this, but it has a limited modeling capacity. ​As an alternative, we have this function, where our f is the weight times the phi phi ​function where phi is a nonlinear transform. ​This is what traditional machine learning is. ​You manually engineer the phi to a domain-specific problem set. ​It's a huge human effort. ​We have a set of generic transformations that map to a higher dimensional space. 

​For example, having three layers of your neural network brings you into a higher dimensional space. ​Kernel methods, RBF kernels. ​This does lead to overfitting very quickly, though, because it is domain-specific. ​It's very often the case that we will fit to the data we have in the domain, and we ​can't generalize very well outside of that domain. ​We cannot encode enough prior information because of the human effort required to manually ​construct these transform functions. ​It's almost impossible, practically impossible, to have enough human-labeled data to actually ​train our models to produce useful insights. ​This is why deep learning came around. 

​We can directly learn this transform function where we have two inputs to the function of ​phi. ​We have x and theta, and it's an automatically learned representation of x. ​We don't need to know about the contours and the edges and the serifs of ones if we can ​already learn it by learning the hidden layers of the network. ​For a deep network, phi is a function learned by the hidden layers of the network. ​Theta here are the learned weights. ​Non-convex optimization, though, so it's harder to train, it's harder to learn. ​It can encode prior beliefs, but it generalizes well. 

​This one is really important because the prior beliefs can include things like systemic oppressions ​of racism and sexism. ​If we allow the data to be learned as is, if the data we hand it has systems of inequality ​baked into it, then it will be generalized into every other situation.