​Okay, last output units design choice here for our output type to be binary one node ​yes no. ​Our output distribution is going to be a Bernoulli distribution, our loss function is going to ​be a binary cross entropy. ​But what would the actual output layer look like? ​This was one of our decisions of how many layers we have and how we interpret the numbers ​coming out of them. ​So the output unit for a binary classification, we have input X, some hidden layers, and then ​an output unit. ​We have a single output unit, so our model y hat is the probability that y is one. ​We have our phi giving the same output unit. 

​So we have the probability that y is one is one over one plus e to the negative phi on ​x. ​Running through another row of this, we have an output type that is discrete. ​So this is a generalized Bernoulli known as a multinulli, I know. ​Cost function here is cross entropy generalized off of binary cross entropy. ​So what would our output layer look like here? ​We have our same X leading into our hidden layers in the middle. ​This time we have multiple output nodes in our output layer. 

​So we have our output unit is P1, P2, P3 coming out as a matrix. ​We have our A score, B score, and B score. ​So if we think back to our slide on the Mints handwriting training set, this is what was ​going on there. ​We have the score that the number coming out was written down as a one, was written down ​as a two, was written down as a three. ​And with the probabilities that these numbers are being put together, we can show this with ​a softmax function. ​And we have a slightly more complicated model function coming out of this. ​Overall, that softmax can take in all this data and give us the probability of these ​things. 

​And we have our final model approximation with a softmax e to the phi sub k on X over ​the sum of all of the k's of that probability unit. ​So softmax is our output layer. ​If we have a continuous output type, so we're back in the space of linear regressions and ​having a continuous ordinal data set output, this is a Gaussian output distribution. ​So our cost function is mean squared. ​Once again, what does this look like? ​Well, we start off with our totally typical output unit, single output unit, some hidden ​layers in the middle, giving us our model. ​And we simplify that into a neural network phi function. 

​So we have the weights coming out of this as W phi on X. ​That is our model approximation of our data set, W phi leading to our model. ​The output layer here is just linear. ​It's a linear regression. ​Maybe the simplest one to interpret here. ​When we have a continuous output type, but we don't know what the output distribution ​is going to be, it's not this normal distribution, it's not Gaussian, it's just an arbitrary ​thing, we need to use a GANs cost function. ​I'll talk about here. 

​So for example, a sigmoid output, sigmoid output plus a squared loss. ​We can show this here with Y minus sigma of X squared. ​We have these flat surfaces at the top of these things. ​But with the cross entropy, we have these losses that sort of shoot up and they don't ​actually level off like a sigmoid function would. ​So instead, we combine them together using these logarithmic functions, and we can have ​the cross entropy loss shown here between the predicted value, the true value, and it ​eventually tapers off.