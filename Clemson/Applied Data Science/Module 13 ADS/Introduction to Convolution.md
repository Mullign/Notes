​For this module, we'll be talking about convolutional neural networks. ​So we talked about the architecture of neural networks, and most of the examples that we've ​seen so far have been fully connected, and we have these very, very dense feedforward ​networks going on here. ​Every node is connected to every node in the subsequent layers. ​But we also know that it is good to learn a small model, because training costs are ​significantly smaller. ​And if we can get away with making better models faster, then we should. ​So do we really actually need all of these edges from our fully connected, or can some ​of these be shared? ​Are any of these edges not performing entirely unique functions to the useful output of our ​model? 

​So let's think about learning an image. ​Some patterns are much smaller than the whole image. ​For example, a beak as a part of a bird is a smaller portion of the image, but every ​picture of a bird will have a beak somewhere in there. ​So can we represent a small region with fewer parameters? ​Can we have a beak detector section of our network? ​These can be compressed. ​So if we think about having our fully connected network, and we realize that some edges are ​all detecting beaks just in different ways, that's redundant. 

​So instead, we can compress together these edges into these smaller detectors and let ​the detector move around. ​So we have one beak in the sort of top left here. ​We have an upper left beak detector portion of our network. ​We have a sort of more middle of the space beak detector. ​Using these together can be compressed to save trading space, computational time, all ​the great things to make these networks even better. ​So this is what's known as a convolutional layer. ​CNN is a neural network with some convolution layers and other hidden layers. 

​A convolutional layer has a number of filters that does the convolutional operation. ​So we'll talk about that in a second. ​It's perhaps not the most well-known matrix operation, but it's very powerful. ​So let's say that we have a 6x6 image just shown with ones and zeros for our pixels here. ​To have a convolution, we need to have a set of network parameters. ​These are the sort of subsections that we are looking for. ​Imagine if the top filter here is for a beak and the lower filter here is for a wing, perhaps. 

​Essentially, just each filter detects a small pattern, and so we have broken up this 6x6 ​image into 3x3 matrices that we are looking for inside of it. ​So when we're doing a convolution, let's start with just the first filter. ​Every convolution operation needs the original matrix and then a filter, sometimes also called ​a kernel. ​Starting at the top left, we just do a dot product between the corresponding sized subset ​of the original image and our filter, and here our dot product comes out to 3. ​Remember, a dot product is doing an element-by-element multiplication, then aggregation of these ​two matrices. ​So because our filter 1 is looking for this one in the top left, one in the center, and ​one in the bottom right, our dot product here turns out to be 3. ​And we move around inside of our original matrix with what's known as a stride. 

​So with a stride 1, we just move over 1. ​When we do a dot product here, we don't have as large of an outcome. ​Our dot product value is negative 1. ​And as a quick side, sometimes we can tune the performance of these models by the convolution ​stride length. ​So instead of doing a stride 1, we can have a stride 2, where we look at two points over. ​And then from two points from here, we would go over and down to the next index. ​When training on actual images, the pixel size is significantly larger than 6x6, so ​optimizations and tuning like this are excellent tweakables to improve performance. 

​Back on a stride of 1, we have our 3 in the top left, and we have a negative 1 for the ​next subsection. ​We keep going and we compute all these values out for the entire matrix. ​And we can see that every time we have a match of our filter, we have a 3 in the top as a ​result of the dot product. ​So when we have our top left convolutional subsect, we have the 3 in the bottom left. ​We can also see our pattern of going straight down across this diagonal. ​We have another 3 there. ​So we found a few matches. 

​That's great. ​We're writing that down inside of our output matrix. ​So for our filter 2, we repeat this process for every filter. ​We have a negative 1 as our first dot product, a negative 1 as our second dot product, and ​so on and so forth. ​A lot of negative 1s in here, but we do it for the whole thing. ​And until we get to our very last convolutional subset here in the bottom right, this is where ​we have our first 3, the maximum positive value, indicating that we have a match of ​the middle column here. ​This collection of dot product calculations is called the feature map. 

​So it's 2 4x4 images forming a 2x4x4. ​This is determined by the size of your filter and the size of your original image. ​So as it turns out, RGB has 3 channels, R, G, and B, that can be broken up into our filters. ​So in particular, this set of RGB filtering techniques turns out to be very efficient ​on images. ​So if we're thinking about a convolution versus a fully connected network, we have ​these convolutions interconnecting our layers of our network, it's a convolutional layer ​of a network, as opposed to the fully connected. ​So if we think about the number of edges and connections going on between these, the convolutional ​layer has 9 inputs. ​For example, if we have our first filter level coming out here, we have our convolution, ​we know that this set of inputs, we have our filter 1 and then a subset of our image going ​to produce this 3 value, then we can place our 3 into our neural network layer and connect ​in the values from our filter and the original image. 

​So we only need to have 9 inputs, it's not fully connected. ​Instead of having all 6x6 36 connections here, we can have 9 only because we are filtering ​into only these values. ​Way fewer parameters, way fewer training computations, way fewer execution computations. ​So we can keep doing this with the other steps of our convolution, we have our 3 here as ​our first node of this layer, but adding the negative 1 as the second node of this convolution ​layer. ​This is a shared weight as well. ​As we go through our matrix operations to train the entire network, these can be updated ​in the same batch style as the other layers, it's a totally normal neural network layer. ​It's even fewer parameters, so it's just even more efficient, it's great. 

​So if we have some sort of image input into our model here, we can have these convolution ​layers interlaced between max pooling layers, which we'll talk about next. ​Repeating this many, many times can filter down into the specific characteristics of ​our images, where this computation is then flattened into a matrix, put into a fully ​connected but significantly smaller feedforward network, and have a probability indicator ​of if the image is representing a cat or a dog or a banana or a fish. 

​For this module, we'll be talking about convolutional neural networks. ​So we talked about the architecture of neural networks, and most of the examples that we've ​seen so far have been fully connected, and we have these very, very dense feedforward ​networks going on here. ​Every node is connected to every node in the subsequent layers. ​But we also know that it is good to learn a small model, because training costs are ​significantly smaller. ​And if we can get away with making better models faster, then we should. ​So do we really actually need all of these edges from our fully connected, or can some ​of these be shared? ​Are any of these edges not performing entirely unique functions to the useful output of our ​model? ​So let's think about learning an image. ​Some patterns are much smaller than the whole image. ​For example, a beak as a part of a bird is a smaller portion of the image, but every ​picture of a bird will have a beak somewhere in there. ​So can we represent a small region with fewer parameters? ​Can we have a beak detector section of our network? ​These can be compressed. ​So if we think about having our fully connected network, and we realize that some edges are ​all detecting beaks just in different ways, that's redundant. ​So instead, we can compress together these edges into these smaller detectors and let ​the detector move around. ​So we have one beak in the sort of top left here. ​We have an upper left beak detector portion of our network. ​We have a sort of more middle of the space beak detector. ​Using these together can be compressed to save trading space, computational time, all ​the great things to make these networks even better. ​So this is what's known as a convolutional layer. ​CNN is a neural network with some convolution layers and other hidden layers. ​A convolutional layer has a number of filters that does the convolutional operation. ​So we'll talk about that in a second. ​It's perhaps not the most well-known matrix operation, but it's very powerful. ​So let's say that we have a 6x6 image just shown with ones and zeros for our pixels here. ​To have a convolution, we need to have a set of network parameters. ​These are the sort of subsections that we are looking for. ​Imagine if the top filter here is for a beak and the lower filter here is for a wing, perhaps. ​Essentially, just each filter detects a small pattern, and so we have broken up this 6x6 ​image into 3x3 matrices that we are looking for inside of it. ​So when we're doing a convolution, let's start with just the first filter. ​Every convolution operation needs the original matrix and then a filter, sometimes also called ​a kernel. ​Starting at the top left, we just do a dot product between the corresponding sized subset ​of the original image and our filter, and here our dot product comes out to 3. ​Remember, a dot product is doing an element-by-element multiplication, then aggregation of these ​two matrices. ​So because our filter 1 is looking for this one in the top left, one in the center, and ​one in the bottom right, our dot product here turns out to be 3. ​And we move around inside of our original matrix with what's known as a stride. ​So with a stride 1, we just move over 1. ​When we do a dot product here, we don't have as large of an outcome. ​Our dot product value is negative 1. ​And as a quick side, sometimes we can tune the performance of these models by the convolution ​stride length. ​So instead of doing a stride 1, we can have a stride 2, where we look at two points over. ​And then from two points from here, we would go over and down to the next index. ​When training on actual images, the pixel size is significantly larger than 6x6, so ​optimizations and tuning like this are excellent tweakables to improve performance. ​Back on a stride of 1, we have our 3 in the top left, and we have a negative 1 for the ​next subsection. ​We keep going and we compute all these values out for the entire matrix. ​And we can see that every time we have a match of our filter, we have a 3 in the top as a ​result of the dot product. ​So when we have our top left convolutional subsect, we have the 3 in the bottom left. ​We can also see our pattern of going straight down across this diagonal. ​We have another 3 there. ​So we found a few matches. ​That's great. ​We're writing that down inside of our output matrix. ​So for our filter 2, we repeat this process for every filter. ​We have a negative 1 as our first dot product, a negative 1 as our second dot product, and ​so on and so forth. ​A lot of negative 1s in here, but we do it for the whole thing. ​And until we get to our very last convolutional subset here in the bottom right, this is where ​we have our first 3, the maximum positive value, indicating that we have a match of ​the middle column here. ​This collection of dot product calculations is called the feature map. ​So it's 2 4x4 images forming a 2x4x4. ​This is determined by the size of your filter and the size of your original image. ​So as it turns out, RGB has 3 channels, R, G, and B, that can be broken up into our filters. ​So in particular, this set of RGB filtering techniques turns out to be very efficient ​on images. ​So if we're thinking about a convolution versus a fully connected network, we have ​these convolutions interconnecting our layers of our network, it's a convolutional layer ​of a network, as opposed to the fully connected. ​So if we think about the number of edges and connections going on between these, the convolutional ​layer has 9 inputs. ​For example, if we have our first filter level coming out here, we have our convolution, ​we know that this set of inputs, we have our filter 1 and then a subset of our image going ​to produce this 3 value, then we can place our 3 into our neural network layer and connect ​in the values from our filter and the original image. ​So we only need to have 9 inputs, it's not fully connected. ​Instead of having all 6x6 36 connections here, we can have 9 only because we are filtering ​into only these values. ​Way fewer parameters, way fewer training computations, way fewer execution computations. ​So we can keep doing this with the other steps of our convolution, we have our 3 here as ​our first node of this layer, but adding the negative 1 as the second node of this convolution ​layer. ​This is a shared weight as well. ​As we go through our matrix operations to train the entire network, these can be updated ​in the same batch style as the other layers, it's a totally normal neural network layer. ​It's even fewer parameters, so it's just even more efficient, it's great. ​So if we have some sort of image input into our model here, we can have these convolution ​layers interlaced between max pooling layers, which we'll talk about next. ​Repeating this many, many times can filter down into the specific characteristics of ​our images, where this computation is then flattened into a matrix, put into a fully ​connected but significantly smaller feedforward network, and have a probability indicator ​of if the image is representing a cat or a dog or a banana or a fish. ​For this module, we'll be talking about convolutional neural networks. ​So we talked about the architecture of neural networks, and most of the examples that we've ​seen so far have been fully connected, and we have these very, very dense feedforward ​networks going on here. ​Every node is connected to every node in the subsequent layers. ​But we also know that it is good to learn a small model, because training costs are ​significantly smaller. ​And if we can get away with making better models faster, then we should. ​So do we really actually need all of these edges from our fully connected, or can some ​of these be shared? ​Are any of these edges not performing entirely unique functions to the useful output of our ​model? ​So let's think about learning an image. ​Some patterns are much smaller than the whole image. ​For example, a beak as a part of a bird is a smaller portion of the image, but every ​picture of a bird will have a beak somewhere in there. ​So can we represent a small region with fewer parameters? ​Can we have a beak detector section of our network? ​These can be compressed. ​So if we think about having our fully connected network, and we realize that some edges are ​all detecting beaks just in different ways, that's redundant. ​So instead, we can compress together these edges into these smaller detectors and let ​the detector move around. ​So we have one beak in the sort of top left here. ​We have an upper left beak detector portion of our network. ​We have a sort of more middle of the space beak detector. ​Using these together can be compressed to save trading space, computational time, all ​the great things to make these networks even better. ​So this is what's known as a convolutional layer. ​CNN is a neural network with some convolution layers and other hidden layers. ​A convolutional layer has a number of filters that does the convolutional operation. ​So we'll talk about that in a second. ​It's perhaps not the most well-known matrix operation, but it's very powerful. ​So let's say that we have a 6x6 image just shown with ones and zeros for our pixels here. ​To have a convolution, we need to have a set of network parameters. ​These are the sort of subsections that we are looking for. ​Imagine if the top filter here is for a beak and the lower filter here is for a wing, perhaps. ​Essentially, just each filter detects a small pattern, and so we have broken up this 6x6 ​image into 3x3 matrices that we are looking for inside of it. ​So when we're doing a convolution, let's start with just the first filter. ​Every convolution operation needs the original matrix and then a filter, sometimes also called ​a kernel. ​Starting at the top left, we just do a dot product between the corresponding sized subset ​of the original image and our filter, and here our dot product comes out to 3. ​Remember, a dot product is doing an element-by-element multiplication, then aggregation of these ​two matrices. ​So because our filter 1 is looking for this one in the top left, one in the center, and ​one in the bottom right, our dot product here turns out to be 3. ​And we move around inside of our original matrix with what's known as a stride. ​So with a stride 1, we just move over 1. ​When we do a dot product here, we don't have as large of an outcome. ​Our dot product value is negative 1. ​And as a quick side, sometimes we can tune the performance of these models by the convolution ​stride length. ​So instead of doing a stride 1, we can have a stride 2, where we look at two points over. ​And then from two points from here, we would go over and down to the next index. ​When training on actual images, the pixel size is significantly larger than 6x6, so ​optimizations and tuning like this are excellent tweakables to improve performance. ​Back on a stride of 1, we have our 3 in the top left, and we have a negative 1 for the ​next subsection. ​We keep going and we compute all these values out for the entire matrix. ​And we can see that every time we have a match of our filter, we have a 3 in the top as a ​result of the dot product. ​So when we have our top left convolutional subsect, we have the 3 in the bottom left. ​We can also see our pattern of going straight down across this diagonal. ​We have another 3 there. ​So we found a few matches. ​That's great. ​We're writing that down inside of our output matrix. ​So for our filter 2, we repeat this process for every filter. ​We have a negative 1 as our first dot product, a negative 1 as our second dot product, and ​so on and so forth. ​A lot of negative 1s in here, but we do it for the whole thing. ​And until we get to our very last convolutional subset here in the bottom right, this is where ​we have our first 3, the maximum positive value, indicating that we have a match of ​the middle column here. ​This collection of dot product calculations is called the feature map. ​So it's 2 4x4 images forming a 2x4x4. ​This is determined by the size of your filter and the size of your original image. ​So as it turns out, RGB has 3 channels, R, G, and B, that can be broken up into our filters. ​So in particular, this set of RGB filtering techniques turns out to be very efficient ​on images. ​So if we're thinking about a convolution versus a fully connected network, we have ​these convolutions interconnecting our layers of our network, it's a convolutional layer ​of a network, as opposed to the fully connected. ​So if we think about the number of edges and connections going on between these, the convolutional ​layer has 9 inputs. ​For example, if we have our first filter level coming out here, we have our convolution, ​we know that this set of inputs, we have our filter 1 and then a subset of our image going ​to produce this 3 value, then we can place our 3 into our neural network layer and connect ​in the values from our filter and the original image. ​So we only need to have 9 inputs, it's not fully connected. ​Instead of having all 6x6 36 connections here, we can have 9 only because we are filtering ​into only these values. ​Way fewer parameters, way fewer training computations, way fewer execution computations. ​So we can keep doing this with the other steps of our convolution, we have our 3 here as ​our first node of this layer, but adding the negative 1 as the second node of this convolution ​layer. ​This is a shared weight as well. ​As we go through our matrix operations to train the entire network, these can be updated ​in the same batch style as the other layers, it's a totally normal neural network layer. ​It's even fewer parameters, so it's just even more efficient, it's great. ​So if we have some sort of image input into our model here, we can have these convolution ​layers interlaced between max pooling layers, which we'll talk about next. ​Repeating this many, many times can filter down into the specific characteristics of ​our images, where this computation is then flattened into a matrix, put into a fully ​connected but significantly smaller feedforward network, and have a probability indicator ​of if the image is representing a cat or a dog or a banana or a fish. ​For this module, we'll be talking about convolutional neural networks. ​So we talked about the architecture of neural networks, and most of the examples that we've ​seen so far have been fully connected, and we have these very, very dense feedforward ​networks going on here. ​Every node is connected to every node in the subsequent layers. ​But we also know that it is good to learn a small model, because training costs are ​significantly smaller. ​And if we can get away with making better models faster, then we should. ​So do we really actually need all of these edges from our fully connected, or can some ​of these be shared? ​Are any of these edges not performing entirely unique functions to the useful output of our ​model? ​So let's think about learning an image. ​Some patterns are much smaller than the whole image. ​For example, a beak as a part of a bird is a smaller portion of the image, but every ​picture of a bird will have a beak somewhere in there. ​So can we represent a small region with fewer parameters? ​Can we have a beak detector section of our network? ​These can be compressed. ​So if we think about having our fully connected network, and we realize that some edges are ​all detecting beaks just in different ways, that's redundant. ​So instead, we can compress together these edges into these: Added to Selection. Press [⌘ + S] to save as a note : Added to Selection. Press [⌘ + S] to save as a note : Added to Selection. Press [⌘ + S] to save as a note