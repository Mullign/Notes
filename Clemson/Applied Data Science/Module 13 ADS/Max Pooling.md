​So the idea for max pooling is really pretty simple. ​We have a filter and we have our feature map here that has been built out of the original ​image and the feature. ​We break the feature map into different pools and we just use the value from this pool that ​is the largest. ​This reduces the dimensionality of our feature map. ​So it saves memory, increases computational speed. ​We can see it breaking down here. ​And so now we have gone from a 1, 2, 3, 4 by 4 into a 2 by 2 feature map layer. 

​And this ends up still giving us a lot of the same advantages. ​We'll talk about the why on the next slide. ​So when we are subsampling the pixels, i.e. taking the pools of these feature maps, it ​doesn't change the object. ​If you take an image of a bird and just subsample it into a smaller picture of a bird, you can ​still tell that it's a bird. ​And a lot of the features remain the same. ​For example, seeing the beak is still a pointy, triangular object here near the head of the ​bird. ​This smaller image gives us fewer parameters to characterize the image, improving the performance ​of our models. 

​So CNNs compress a fully connected network in two main ways. ​It reduces the number of connections, which also is the shared weights on the edges. ​So fewer edges means more sharing of weights, easier matrix operations. ​And then the max pooling reduces the complexity even further by having fewer parameters. ​So we can see this in the max pooling sense here. ​We start with our original image, our 6 by 6 example image with a convolution and then ​a max pooling. ​It produces for us a 2 by 2 image that is new but smaller. 

​Each filter is a channel and we have compressed this image to be easier to work with, easier ​to train on, easier to use. ​This process can be repeated many times. ​If you have a 4K image of a kitten that has about the same amount of information as a ​1080 image, same information-ish as a 480, you can keep going as many times as you need ​to make a sufficiently small image that you can use. ​In these pairs of convolutions and max poolings, we have new images being created that is smaller ​and the number of channels is the number of filters. ​Repeating this as many times as you need. ​Okay, so that just leaves the last step of this process, the flattening and the fully ​connected feedforward network. ​We'll talk about flattening next. 

​We have our feature map and we flatten it into a single vector. ​This is entirely an organizational layer. ​It is not doing a large amount of math to get here. ​All we are doing is just taking our values and putting them from stacked on top of each ​other and in a matrix into a vector. ​This means that we can put it into a fully connected feedforward network really easily. ​Thankfully, Keras has implemented this already for us. ​We modified the network structure and the input format, meaning that it goes from vector ​to 3D tensor in Keras. 

​We have our inputs. ​The convolution has the convolution2D function. ​We have the 25 3x3 filters that are formed out of this. ​Then we have our input shapes with the 28x28 pixels. ​In this case, we're only doing black and white, so negative one, one, as opposed to ​a RGB full channel system with three values. ​Then in our MaxPooling, we have a MaxPooling2D. ​So when we put together these convolution and MaxPooling layers, if we have an input ​image that is in grayscale, this 28x28, we can have, for example, something like this. 

​Modern versions of Keras might have updated this convolution2D function to be conv2D. ​We have 25 layers, 25 filters of 3x3 size with an input shape of a 28x20 image in grayscale, ​leading to a feature map of 25x26x26. ​If we pull this down into a 2x2s, we can cut our feature map size to 25x13x13. ​From here, having another convolution layer with 50 layers can bring our feature map to ​a 50x11x11, and pulling that down once more gives us a 50x5x5. ​So if we need to consider how many parameters are on each filter, we need to think about ​how the convolution layers are being connected into the larger network. ​So we have nine parameters for each filter here, because each filter is a 3x3 matrix, ​so we don't need to be thinking about the whole thing, but making sure that our networks ​are connected properly between layers is how we have a fully functioning network. ​It's kind of like type checking for normal sequential programming. 

​Down here we have a significantly larger number of parameters, because we've picked up a few. ​We have 25x9, we still have our 3x3 giving our 9 factor, but we also have the previous ​pooling 25 filters coming through, so now we have nine parameters with 25 filters, meaning ​we have 225 parameters now total. ​Another convolution layer means that we do have a larger number. ​It's still far fewer than a fully connected network. ​So we can put all this together into a full CNN. ​We flatten out our feature map now with just the flatten function, thank you Keras, giving ​us 1250 nodes in this vector that we've put into a fully connected feedforward network. ​Here are the Keras functions for that, for you to get more experience with in the lab. 

​But we can have a just Relu or a Softmax activation functions on these, totally typical neural ​network activation functions. 

​So the idea for max pooling is really pretty simple. ​We have a filter and we have our feature map here that has been built out of the original ​image and the feature. ​We break the feature map into different pools and we just use the value from this pool that ​is the largest. ​This reduces the dimensionality of our feature map. ​So it saves memory, increases computational speed. ​We can see it breaking down here. ​And so now we have gone from a 1, 2, 3, 4 by 4 into a 2 by 2 feature map layer. ​And this ends up still giving us a lot of the same advantages. ​We'll talk about the why on the next slide. ​So when we are subsampling the pixels, i.e. taking the pools of these feature maps, it ​doesn't change the object. ​If you take an image of a bird and just subsample it into a smaller picture of a bird, you can ​still tell that it's a bird. ​And a lot of the features remain the same. ​For example, seeing the beak is still a pointy, triangular object here near the head of the ​bird. ​This smaller image gives us fewer parameters to characterize the image, improving the performance ​of our models. ​So CNNs compress a fully connected network in two main ways. ​It reduces the number of connections, which also is the shared weights on the edges. ​So fewer edges means more sharing of weights, easier matrix operations. ​And then the max pooling reduces the complexity even further by having fewer parameters. ​So we can see this in the max pooling sense here. ​We start with our original image, our 6 by 6 example image with a convolution and then ​a max pooling. ​It produces for us a 2 by 2 image that is new but smaller. ​Each filter is a channel and we have compressed this image to be easier to work with, easier ​to train on, easier to use. ​This process can be repeated many times. ​If you have a 4K image of a kitten that has about the same amount of information as a ​1080 image, same information-ish as a 480, you can keep going as many times as you need ​to make a sufficiently small image that you can use. ​In these pairs of convolutions and max poolings, we have new images being created that is smaller ​and the number of channels is the number of filters. ​Repeating this as many times as you need. ​Okay, so that just leaves the last step of this process, the flattening and the fully ​connected feedforward network. ​We'll talk about flattening next. ​We have our feature map and we flatten it into a single vector. ​This is entirely an organizational layer. ​It is not doing a large amount of math to get here. ​All we are doing is just taking our values and putting them from stacked on top of each ​other and in a matrix into a vector. ​This means that we can put it into a fully connected feedforward network really easily. ​Thankfully, Keras has implemented this already for us. ​We modified the network structure and the input format, meaning that it goes from vector ​to 3D tensor in Keras. ​We have our inputs. ​The convolution has the convolution2D function. ​We have the 25 3x3 filters that are formed out of this. ​Then we have our input shapes with the 28x28 pixels. ​In this case, we're only doing black and white, so negative one, one, as opposed to ​a RGB full channel system with three values. ​Then in our MaxPooling, we have a MaxPooling2D. ​So when we put together these convolution and MaxPooling layers, if we have an input ​image that is in grayscale, this 28x28, we can have, for example, something like this. ​Modern versions of Keras might have updated this convolution2D function to be conv2D. ​We have 25 layers, 25 filters of 3x3 size with an input shape of a 28x20 image in grayscale, ​leading to a feature map of 25x26x26. ​If we pull this down into a 2x2s, we can cut our feature map size to 25x13x13. ​From here, having another convolution layer with 50 layers can bring our feature map to ​a 50x11x11, and pulling that down once more gives us a 50x5x5. ​So if we need to consider how many parameters are on each filter, we need to think about ​how the convolution layers are being connected into the larger network. ​So we have nine parameters for each filter here, because each filter is a 3x3 matrix, ​so we don't need to be thinking about the whole thing, but making sure that our networks ​are connected properly between layers is how we have a fully functioning network. ​It's kind of like type checking for normal sequential programming. ​Down here we have a significantly larger number of parameters, because we've picked up a few. ​We have 25x9, we still have our 3x3 giving our 9 factor, but we also have the previous ​pooling 25 filters coming through, so now we have nine parameters with 25 filters, meaning ​we have 225 parameters now total. ​Another convolution layer means that we do have a larger number. ​It's still far fewer than a fully connected network. ​So we can put all this together into a full CNN. ​We flatten out our feature map now with just the flatten function, thank you Keras, giving ​us 1250 nodes in this vector that we've put into a fully connected feedforward network. ​Here are the Keras functions for that, for you to get more experience with in the lab. ​But we can have a just Relu or a Softmax activation functions on these, totally typical neural ​network activation functions. ​So the idea for max pooling is really pretty simple. ​We have a filter and we have our feature map here that has been built out of the original ​image and the feature. ​We break the feature map into different pools and we just use the value from this pool that ​is the largest. ​This reduces the dimensionality of our feature map. ​So it saves memory, increases computational speed. ​We can see it breaking down here. ​And so now we have gone from a 1, 2, 3, 4 by 4 into a 2 by 2 feature map layer. ​And this ends up still giving us a lot of the same advantages. ​We'll talk about the why on the next slide. ​So when we are subsampling the pixels, i.e. taking the pools of these feature maps, it ​doesn't change the object. ​If you take an image of a bird and just subsample it into a smaller picture of a bird, you can ​still tell that it's a bird. ​And a lot of the features remain the same. ​For example, seeing the beak is still a pointy, triangular object here near the head of the ​bird. ​This smaller image gives us fewer parameters to characterize the image, improving the performance ​of our models. ​So CNNs compress a fully connected network in two main ways. ​It reduces the number of connections, which also is the shared weights on the edges. ​So fewer edges means more sharing of weights, easier matrix operations. ​And then the max pooling reduces the complexity even further by having fewer parameters. ​So we can see this in the max pooling sense here. ​We start with our original image, our 6 by 6 example image with a convolution and then ​a max pooling. ​It produces for us a 2 by 2 image that is new but smaller. ​Each filter is a channel and we have compressed this image to be easier to work with, easier ​to train on, easier to use. ​This process can be repeated many times. ​If you have a 4K image of a kitten that has about the same amount of information as a ​1080 image, same information-ish as a 480, you can keep going as many times as you need ​to make a sufficiently small image that you can use. ​In these pairs of convolutions and max poolings, we have new images being created that is smaller ​and the number of channels is the number of filters. ​Repeating this as many times as you need. ​Okay, so that just leaves the last step of this process, the flattening and the fully ​connected feedforward network. ​We'll talk about flattening next. ​We have our feature map and we flatten it into a single vector. ​This is entirely an organizational layer. ​It is not doing a large amount of math to get here. ​All we are doing is just taking our values and putting them from stacked on top of each ​other and in a matrix into a vector. ​This means that we can put it into a fully connected feedforward network really easily. ​Thankfully, Keras has implemented this already for us. ​We modified the network structure and the input format, meaning that it goes from vector ​to 3D tensor in Keras. ​We have our inputs. ​The convolution has the convolution2D function. ​We have the 25 3x3 filters that are formed out of this. ​Then we have our input shapes with the 28x28 pixels. ​In this case, we're only doing black and white, so negative one, one, as opposed to ​a RGB full channel system with three values. ​Then in our MaxPooling, we have a MaxPooling2D. ​So when we put together these convolution and MaxPooling layers, if we have an input ​image that is in grayscale, this 28x28, we can have, for example, something like this. ​Modern versions of Keras might have updated this convolution2D function to be conv2D. ​We have 25 layers, 25 filters of 3x3 size with an input shape of a 28x20 image in grayscale, ​leading to a feature map of 25x26x26. ​If we pull this down into a 2x2s, we can cut our feature map size to 25x13x13. ​From here, having another convolution layer with 50 layers can bring our feature map to ​a 50x11x11, and pulling that down once more gives us a 50x5x5. ​So if we need to consider how many parameters are on each filter, we need to think about ​how the convolution layers are being connected into the larger network. ​So we have nine parameters for each filter here, because each filter is a 3x3 matrix, ​so we don't need to be thinking about the whole thing, but making sure that our networks ​are connected properly between layers is how we have a fully functioning network. ​It's kind of like type checking for normal sequential programming. ​Down here we have a significantly larger number of parameters, because we've picked up a few. ​We have 25x9, we still have our 3x3 giving our 9 factor, but we also have the previous ​pooling 25 filters coming through, so now we have nine parameters with 25 filters, meaning ​we have 225 parameters now total. ​Another convolution layer means that we do have a larger number. ​It's still far fewer than a fully connected network. ​So we can put all this together into a full CNN. ​We flatten out our feature map now with just the flatten function, thank you Keras, giving ​us 1250 nodes in this vector that we've put into a fully connected feedforward network. ​Here are the Keras functions for that, for you to get more experience with in the lab. ​But we can have a just Relu or a Softmax activation functions on these, totally typical neural ​network activation functions. ​So the idea for max pooling is really pretty simple. ​We have a filter and we have our feature map here that has been built out of the original ​image and the feature. ​We break the feature map into different pools and we just use the value from this pool that ​is the largest. ​This reduces the dimensionality of our feature map. ​So it saves memory, increases computational speed. ​We can see it breaking down here. ​And so now we have gone from a 1, 2, 3, 4 by 4 into a 2 by 2 feature map layer. ​And this ends up still giving us a lot of the same advantages. ​We'll talk about the why on the next slide. ​So when we are subsampling the pixels, i.e. taking the pools of these feature maps, it ​doesn't change the object. ​If you take an image of a bird and just subsample it into a smaller picture of a bird, you can ​still tell that it's a bird. ​And a lot of the features remain the same. ​For example, seeing the beak is still a pointy, triangular object here near the head of the ​bird. ​This smaller image gives us fewer parameters to characterize the image, improving the performance ​of our models. ​So CNNs compress a fully connected network in two main ways. ​It reduces the number of connections, which also is the shared weights on the edges. ​So fewer edges means more sharing of weights, easier matrix operations. ​And then the max pooling reduces the complexity even further by having fewer parameters. ​So we can see this : Added to Selection. Press [⌘ + S] to save as a note : Added to Selection. Press [⌘ + S] to save as a note : Added to Selection. Press [⌘ + S] to save as a note