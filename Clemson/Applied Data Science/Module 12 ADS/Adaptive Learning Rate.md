​There are a few ways that we can learn these sort of gradients and optimize into these ​problem spaces with adaptive learning rates that perform even better than previous averages ​and look-ahead positions can give us. ​So if we're looking at this particular sort of oval-shaped training space, we have the ​oscillations moving along the vertical and the horizontal axes at the same time, but ​we can tell just by, like it's an oval, that the motion in the horizontal space will happen ​much faster than the vertical. ​Look at the total distance that we move from the initial points to the middle along the ​x-axis or the y-axis. ​So we can use this information to sort of split up which of these training directions ​happens faster and adapt to the space that we are in. ​Let's look at this first step here just on its own. ​So we have a green sort of vector here showing how it would be moving up and to the right, ​but because we know that moving on the w1 axis is much faster, we can extend that vector ​and slow down w2 so we can shorten that one to adjust how our next step will be placed ​in our progression. ​And doing the same thing on the next step here, we have a vertical and horizontal component ​that is either being shortened or lengthened to get us closer towards the minimum faster, ​and you can see from our visualization that we are getting there in far fewer steps and ​a more direct path. 

​So in this particular instance, we would get there in about three steps. ​AdaGrad, adaptive gradients, accumulating these squared gradients. ​We can update each parameter, so w1, w2, w3 if we're in three dimensions. ​This gives us greater progress along gently sloped directions and is inversely proportional ​to the cumulative squared gradient. ​So when we have our sort of gradients being taken into account here, we can break up these ​parameters to have more efficient progressions towards the minimums. ​So AdaGrad adaptive gradient, we have our old descent gradient, which uses our consistent ​learning rate of our delta learning function, building up our notation as we go along. ​We want to have our adapted weights be updated by these lambdas not to be the same and inversely ​proportional to the magnitude of our g vector. 

​So we can think about having our updates being impacted individually by these parameters ​in separation from each other, each parameter is distinct. ​But we want these to be inversely proportional to it. ​And here we're just adding in a delta value to make sure that we don't have any divide ​by zeros and making this like explode for it for that reason. ​The new gradient descent then is written here, where instead of having this updated weight ​being just the weight minus our lambda g, it's echo over delta plus the square root ​of R sub i times g sub i, remember where i is the weight matrix parameter that we are ​training for separately. ​An issue with the adaptive gradient algorithm from the previous slide is that it will prematurely ​decrease learning rates as it gets closer to the optimization point. ​In order to make sure that we finish out strong and we get to the optimal weights even ​faster, we can use the root mean square propagation. ​So in this sense, we have a exponentially weighted average for the gradient accumulation. 

​We have two formulas here, there's the gradient accumulation, which keeps the running average ​of the squared gradients on parameter i, where rho here is our decay factor. ​Typically it's around 0.9, we don't want to decay too much off of it. ​We have our current squared gradients, gi squared, and this will keep the exponentially ​decaying average consistent as our steps get closer towards the optimization point. ​Our parameter update, looking at parameter i again. ​We have a learning rate epsilon here, hyperparameter, but we also have our delta, which is a smoothing ​term. ​It's just some small value, remember, to make sure it doesn't blow up. ​We have a root mean square, so R sub i, square root of that, and then quantity times the ​current gradient to update. 

​So this makes sure that it adapts to the scale of the gradients, and it is just another ​efficient algorithm to get towards the optimal point for our data sets. ​And our last optimization algorithm, Adam, applies the adaptive learning rates from our ​RMS prop and the momentum idea from earlier to make an even better algorithm for this. ​So we have an estimation of the first moment, going through with the exponential moving ​average. ​We have our rho hyperparameter, once again typically around 0.9. ​You can change that before you begin the algorithm. ​We have our second rho, and we have a second moment to use when we are updating our parameters. ​So these bias corrections for V and R can also help make this even more an efficient ​algorithm as we go through and update our parameters individually. 

​This works really well in practice, and it's fairly robust with hyperparameters, so we ​can actually set and tune these rho values and other hyperparameters like training time ​to have a very effective algorithm.