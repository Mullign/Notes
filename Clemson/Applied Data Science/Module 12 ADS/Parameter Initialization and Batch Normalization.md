​Okay, so we have the adaptive learning rates and we have all these ways that we can sort ​of reduce the number of steps from our starting position to a local minimum for our training ​weights. ​How do we actually pick our first initial point? ​We placed it randomly on that oval last time, but can we do better? ​So in general, we want to break the symmetry between the units. ​We know that our units are not going to all be training at the same rate, so we don't ​need to necessarily be starting them all in the same position. ​It doesn't make a lot of sense to treat everything the exact same when we know that they're not. ​So we can initialize all the weights randomly using a Gaussian or uniform distribution. 

​The scale here is really important to think of because when we have our large initial ​weights or small, we have these gradient explosions and gradient vanishing problems ​that propagate through our matrices. ​So we want to have some sort of not too big, not too small, not the same, all these not ​not nots into a good parameter initialization that can give us fast training times. ​So Xavier initialization is a good first step here. ​It's heuristic for all outputs to have a unit variance. ​For a fully connected layer with m inputs, we can have a cell in our weight matrix to ​be some value from a normal distribution with a mean of zero and a variance of one over ​m, m inputs, one input, ten inputs, m. ​For a RELU unit, having a normal distribution centered at zero with two over m seems to ​work out pretty well, so it's a good starting point for when you're entering your hyperparameters ​and training your models. ​For a fully connected layer with m inputs and n outputs on a normalized initialization, ​a uniform distribution with this mean and this variance trades off between initializing ​all layers to have the same activation and gradient variance, and is sparse when m is ​really large, which gives non-zero weights in each unit without overloading all the computation. 

​We'll talk about other ways that we can sort of effectively train these models here in ​our next subsection. ​Normalization So it's good practice to normalize these features ​before applying the learning algorithm. ​We have a feature vector, a vector of the mean feature values, and a vector of the standard ​deviation of the feature values. ​If we normalize all of these beforehand, it speeds up our training because it makes all ​of our computation sort of in the same area, and well, you'll see. ​If we have a oblong bowl that we're tossing our marble into, it has all of these weird ​characteristics about how we would consider one parameter over another and how it can ​spiral out in any direction. ​But if we normalize it beforehand and we have a circular bowl to toss our marble into, then ​that can allow us to make more assumptions about our training space and exploit that ​to use these gradient optimization algorithms to the best of their ability. ​So an internal covariance shift. 

​Each hidden layer here is changing the distribution of inputs to the next layer, and this slows ​down our learning quite a lot. ​When we have this fully connected of a network, we have many, many inputs, many computations, ​and we have a lot of, well, matrix operations to compute. ​So if instead we do it by batching. ​We can have a mini batch of activations for each layer to normalize. ​This can speed up our training time quite a bit. ​So we can break down our matrices into some sort of sections. ​Here we have H prime is H. 

​So this gives us two new learnable parameters that we can use to control the range of normalization ​in this process. ​Here we have gamma on the left. ​That is the scale of the normalization, and beta is the shift. ​So thinking about how the overall range of the data is adjusted and moved around. ​Here's where the hyperparameters are put, and we do have one note here that this can ​reduce the expressive power of the normalization, and that makes sense too. ​If you think about the size of data as inherently holding information, if we adjust that size, ​it's possible that we reduce the impact of that information as it relates to the rest ​of the data. ​So we have these normalization operations that we apply per level in our network. 

​Here we have our math, and we have a nice little pipeline showing how it goes from batch ​one down to batch N and back again. ​We do this for layers in the neural network. ​As many layers as you have, the more normalizations you need to do. ​How that impacts your computational time is a thing to keep an eye out for as we want ​to make sure all of our neural networks still execute in a reasonable amount of time.