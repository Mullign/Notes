​So with sparse representation. ​So when we're thinking about our sparse representation technique here, we have our ​neural network sort of structure. We have our input, a couple of hidden layers, and output. ​And we're trying to think about the loss function as it's being computed ​as a whole. We have these vector operations that sort of show the inputs to our W7 output node. ​So if we think about the actual like W7 node, this weight node in here, it has inputs of these ​H edges between it as it is fed into it, and that is sort of this vertical vector that we have ​on our equation. We can think about how those inputs actually fit into our overall omega ​function, our regularization term in our loss function now. So instead of doing a full omega ​W for the full weight matrix, here we only need to actually think about the regularization of ​those particular parts, the relevant aspects of our matrix, i.e. this set of H edges, this vector ​as it is applying into our hidden layer. Instead of doing a full matrix update, we can think about ​the relevant parts of it, a sparse representation. And then our last technique here, we have dropouts, ​not the kind that data scientists daydream about, but instead the kinds that data scientists ​work with. So if we think about how robust our neural networks are to random noise in them, ​it actually turns out that adding in a little bit of network weight buzzing, a little bit of jitter ​in the noise, is equivalent to minimizing the loss with the regularization term. 

​So this encourages a smooth function and leads to small changes in the output, ​but better convergence. So it prevents the pursuit of these hard probabilities ​when we inject with noise in the output labels. So the network weights and the output labels ​that we're just sort of jittering around and adding a little bit of randomization to. ​The dropout here is pretty much what you see in the figure. It is just randomly setting some neurons ​and their connections to zero, i.e. just slumping them right out of the matrix. Prevents the ​overfitting by reducing the co-adaption of neurons. It's like training many random subnetworks ​within the one that we were doing. So instead of doing this fully connected, ​very dense thing, we just kind of randomly chop it up. And as it turns out, ​this is wildly effective. This is really, really good in ways that we can make our classification ​error just plummet by actually doing less training. As it turns out, having a fully ​connected network is not always going to be a more accurate thing, whereas it would always ​be a more expensive thing to train. So fewer computations, better error rates. We love it. 

​This dropout technique is actually effective enough to be considered as an alternative to ​these ensembling techniques, which we'll talk about in the decision trees and random forest ​first section. Those ensembling techniques essentially is just training many different ​versions of the model and having them vote on outputs to your model. But when it comes to neural ​networks, because we were already working in these highly computationally intensive spaces ​with all of these matrices floating around, minimizing our losses and doing all of these ​amazing things, it's very expensive to train lots and lots of neural networks. ​And as it turns out, dropout is a way that we can still achieve a similar type of improvement ​without needing to train a thousand, a hundred thousand neural networks and then having them ​decide amongst themselves for one output. So these neural networks that we've been talking ​about fit a nice little place in our overall concept map of the class. We have both the ​regression and the classification performance uses of these neural networks, just like decision ​trees and k-nearest neighbors and such. It's an amazingly useful technique. 

​So with sparse representation. ​So when we're thinking about our sparse representation technique here, we have our ​neural network sort of structure. We have our input, a couple of hidden layers, and output. ​And we're trying to think about the loss function as it's being computed ​as a whole. We have these vector operations that sort of show the inputs to our W7 output node. ​So if we think about the actual like W7 node, this weight node in here, it has inputs of these ​H edges between it as it is fed into it, and that is sort of this vertical vector that we have ​on our equation. We can think about how those inputs actually fit into our overall omega ​function, our regularization term in our loss function now. So instead of doing a full omega ​W for the full weight matrix, here we only need to actually think about the regularization of ​those particular parts, the relevant aspects of our matrix, i.e. this set of H edges, this vector ​as it is applying into our hidden layer. Instead of doing a full matrix update, we can think about ​the relevant parts of it, a sparse representation. And then our last technique here, we have dropouts, ​not the kind that data scientists daydream about, but instead the kinds that data scientists ​work with. So if we think about how robust our neural networks are to random noise in them, ​it actually turns out that adding in a little bit of network weight buzzing, a little bit of jitter ​in the noise, is equivalent to minimizing the loss with the regularization term.  Play video starting at :2:3 and follow transcript2:03​So this encourages a smooth function and leads to small changes in the output, ​but better convergence. So it prevents the pursuit of these hard probabilities ​when we inject with noise in the output labels. So the network weights and the output labels ​that we're just sort of jittering around and adding a little bit of randomization to. ​The dropout here is pretty much what you see in the figure. It is just randomly setting some neurons ​and their connections to zero, i.e. just slumping them right out of the matrix. Prevents the ​overfitting by reducing the co-adaption of neurons. It's like training many random subnetworks ​within the one that we were doing. So instead of doing this fully connected, ​very dense thing, we just kind of randomly chop it up. And as it turns out, ​this is wildly effective. This is really, really good in ways that we can make our classification ​error just plummet by actually doing less training. As it turns out, having a fully ​connected network is not always going to be a more accurate thing, whereas it would always ​be a more expensive thing to train. So fewer computations, better error rates. We love it.  Play video starting at :3:24 and follow transcript3:24​This dropout technique is actually effective enough to be considered as an alternative to ​these ensembling techniques, which we'll talk about in the decision trees and random forest ​first section. Those ensembling techniques essentially is just training many different ​versions of the model and having them vote on outputs to your model. But when it comes to neural ​networks, because we were already working in these highly computationally intensive spaces ​with all of these matrices floating around, minimizing our losses and doing all of these ​amazing things, it's very expensive to train lots and lots of neural networks. ​And as it turns out, dropout is a way that we can still achieve a similar type of improvement ​without needing to train a thousand, a hundred thousand neural networks and then having them ​decide amongst themselves for one output. So these neural networks that we've been talking ​about fit a nice little place in our overall concept map of the class. We have both the ​regression and the classification performance uses of these neural networks, just like decision ​trees and k-nearest neighbors and such. It's an amazingly useful technique. ​So with sparse representation. ​So when we're thinking about our sparse representation technique here, we have our ​neural network sort of structure. We have our input, a couple of hidden layers, and output. ​And we're trying to think about the loss function as it's being computed ​as a whole. We have these vector operations that sort of show the inputs to our W7 output node. ​So if we think about the actual like W7 node, this weight node in here, it has inputs of these ​H edges between it as it is fed into it, and that is sort of this vertical vector that we have ​on our equation. We can think about how those inputs actually fit into our overall omega ​function, our regularization term in our loss function now. So instead of doing a full omega ​W for the full weight matrix, here we only need to actually think about the regularization of ​those particular parts, the relevant aspects of our matrix, i.e. this set of H edges, this vector ​as it is applying into our hidden layer. Instead of doing a full matrix update, we can think about ​the relevant parts of it, a sparse representation. And then our last technique here, we have dropouts, ​not the kind that data scientists daydream about, but instead the kinds that data scientists ​work with. So if we think about how robust our neural networks are to random noise in them, ​it actually turns out that adding in a little bit of network weight buzzing, a little bit of jitter ​in the noise, is equivalent to minimizing the loss with the regularization term.  Play video starting at :2:3 and follow transcript2:03​So this encourages a smooth function and leads to small changes in the output, ​but better convergence. So it prevents the pursuit of these hard probabilities ​when we inject with noise in the output labels. So the network weights and the output labels ​that we're just sort of jittering around and adding a little bit of randomization to. ​The dropout here is pretty much what you see in the figure. It is just randomly setting some neurons ​and their connections to zero, i.e. just slumping them right out of the matrix. Prevents the ​overfitting by reducing the co-adaption of neurons. It's like training many random subnetworks ​within the one that we were doing. So instead of doing this fully connected, ​very dense thing, we just kind of randomly chop it up. And as it turns out, ​this is wildly effective. This is really, really good in ways that we can make our classification ​error just plummet by actually doing less training. As it turns out, having a fully ​connected network is not always going to be a more accurate thing, whereas it would always ​be a more expensive thing to train. So fewer computations, better error rates. We love it.  Play video starting at :3:24 and follow transcript3:24​This dropout technique is actually effective enough to be considered as an alternative to ​these ensembling techniques, which we'll talk about in the decision trees and random forest ​first section. Those ensembling techniques essentially is just training many different ​versions of the model and having them vote on outputs to your model. But when it comes to neural ​networks, because we were already working in these highly computationally intensive spaces ​with all of these matrices floating around, minimizing our losses and doing all of these ​amazing things, it's very expensive to train lots and lots of neural networks. ​And as it turns out, dropout is a way that we can still achieve a similar type of improvement ​without needing to train a thousand, a hundred thousand neural networks and then having them ​decide amongst themselves for one output. So these neural networks that we've been talking ​about fit a nice little place in our overall concept map of the class. We have both the ​regression and the classification performance uses of these neural networks, just like decision ​trees and k-nearest neighbors and such. It's an amazingly useful technique. ​So with sparse representation. ​So when we're thinking about our sparse representation technique here, we have our ​neural network sort of structure. We have our input, a couple of hidden layers, and output. ​And we're trying to think about the loss function as it's being computed ​as a whole. We have these vector operations that sort of show the inputs to our W7 output node. ​So if we think about the actual like W7 node, this weight node in here, it has inputs of these ​H edges between it as it is fed into it, and that is sort of this vertical vector that we have ​on our equation. We can think about how those inputs actually fit into our overall omega ​function, our regularization term in our loss function now. So instead of doing a full omega ​W for the full weight matrix, here we only need to actually think about the regularization of ​those particular parts, the relevant aspects of our matrix, i.e. this set of H edges, this vector ​as it is applying into our hidden layer. Instead of doing a full matrix update, we can think about ​the relevant parts of it, a sparse representation. And then our last technique here, we have dropouts, ​not the kind that data scientists daydream about, but instead the kinds that data scientists ​work with. So if we think about how robust our neural networks are to random noise in them, ​it actually turns out that adding in a little bit of network weight buzzing, a little bit of jitter ​in the noise, is equivalent to minimizing the loss with the regularization term.  Play video starting at :2:3 and follow transcript2:03​So this encourages a smooth function and leads to small changes in the output, ​but better convergence. So it prevents the pursuit of these hard probabilities ​when we inject with noise in the output labels. So the network weights and the output labels ​that we're just sort of jittering around and adding a little bit of randomization to. ​The dropout here is pret: Added to Selection. Press [⌘ + S] to save as a note Like Dislike Report an issue : Added to Selection. Press [⌘ + S] to save as a note Like Dislike Report an issue : Added to Selection. Press [⌘ + S] to save as a note