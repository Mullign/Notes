![[Statistical-Based & Deviation-Based Detection Slides.pdf]]So let's talk about our first section method, statistical-based. This is probably the oldest method when we have an assumption of the data, for example, on a normal distribution curve. We have the distribution, the mean, the variance. We have this nice bell curve on the right here. Data is assumed to be part of a working hypothesis. And each data object in the data set is compared to the working hypothesis and is either accepted in the working hypothesis or rejected as discordant into the alternative. Those are the outliers. 

So if we have a nice distribution of data points forming our normal distribution curve here, and then we have one little outlier there circled on our graph to the right, it doesn't follow the normal shape. So if we have some sort of range that we expect to see things in following our perhaps clustering-based, but more typically a different detection algorithm, we expect things to be within these two lines kind of, and if they're not, outlier. Depth-based instead, data is organized into layers according to some definition of depth. Shallow layers are more likely to contain outliers than deep layers and can effectively handle computation for very large data sets. We can calculate these extra dimensions of depth to layer on top of data sets and identify outliers in this way. It's pretty easy for our human eyes to look at this particular picture and see where the mountain is that is, you know, showing the discrepancy in data between the outliers and the rest of the data. Most outlier research has been done in this area. 

Many data distributions are known. This is based in just traditional statistics, statistics before computers. We've been doing this for a very long time, and we have very effective ways of doing this. Almost all statistical models are univariate though, and those that are multivariates can only efficiently handle k less than the four, so like up to four-ish dimensions. Because they were developed before computers, they were restricted by the computational power of a human brain, and doing things in larger than four dimensions is difficult. All models assume the distribution is known, and that's just not always the case. Having an outlier detection be completely subjective to this distribution used means that it's relying on this intuition of somebody looking at the depth and saying, yeah, probably that peak, but maybe not that one. 

Deviation-based detection, so we simulate a mechanism familiar to a human. After seeing a series of similar data, an element disturbing the series is considered an exception. So if you see A, A, A, B, you're going to tune into that as a human, recognizing a distinct item in the sequence. Selecting subsets of the data, I sub J from the data set I, compare the dissimilarity of I and I minus I sub J, we can find the minimum subset, I sub J, that reduces the dissimilarity the most. We have a smoothing factor here as part of that, where D is a dissimilarity function and C is the cardinality function. We want to pick the subset that has the smallest dissimilarity, a.k.a. the most similarity, from our subsets. For example, let's take a data set of 1, 4, 4, 4. 

To our human eyes, we can see a sequence of 4, 4, 4, and 1 is the dissimilar item. So if we take a subset of the empty set, we can find the continuity and dissimilarity to be 4 and 1.6, so our smoothing factor is zero. As we build up our subsets to compare against 4, 4, 4, 4, 4, 4, we can see that there is some variation in the smoothing factor, minus 1-ish to 1.5-ish. But when we get to I sub J being just the set of 1, our smoothing factor spikes to 5.7. We have a maximum value, so 1 is the outlier set that has distinguished itself as an outlier. We also have the OLAP data cube technique, where OLAP stands for Online Analytical Processing. Deviation detection process is overlapped with cube computation. 

Precomputed measures indicating the error data exceptions are needed for this, and cell value is considered to be an exception if it is significantly different from the expected value based on the statistical model. So even our sequence-aware outlier detection techniques can integrate statistical models, just building them into a higher dimensionality. Visual cues such as background color to reflect the degree of the exception. Think about like a heat map. You can look at a heat map and find a particular point that's just so much hotter or cooler than others.