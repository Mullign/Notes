![[Distance-Based Detection Slides.pdf]]And finally, we have the distance-based detections. So for an object O in a dataset T, a distance-based outlier is if at least a fraction p of the objects in T are greater than or equal to a distance from 0. A point O in the dataset is an outlier with respect to parameters k and d if no more than k points in the dataset are at a distance of d or less from 0. A relative measurement, a dk of O denotes the distance of the kth nearest neighbor of O, is a measure of how much of an outlier the point is. So this is a step towards mathematizing, specifying, outlining mathematically, our intuition that outliers are things that are far away from most other things. So an index-based algorithm, KN98, indexing structures such as R-trees, or plus-trees, or KDB-trees are built for multidimensional databases. The index is used to search for neighbors of each object O within a radius d around the object. 

So if you consider a leaf on the tree, the neighboring leaf is going to be closer than a leaf that you have to branch off from the root to get to. That's k. Neighbors of an object O are found, O is not an outlier. So the worst-case computational complexity for this is O of k times n squared, where k is the dimensionality and n is the number of objects in the dataset. This scales well with k, but the index construction process may cost a lot of time. And depending on how your dataset is stored, the constant accesses into the tree could be a source of that. So we can make use of some clever caching in a nested loop algorithm to identify these outliers as well. 

We divide our buffer space into two halves, first and second arrays, and break the data into blocks to feed into these two blocks. So we break the data into blocks that can fit into those arrays. We directly compute the distance between each pair of objects inside of the array or between the arrays, and then we decide the outlier at the end. So we'll show an example here. It's the same computational complexity as the index-based algorithm. So avoiding index structure construction is one of our gained advantages here, but we want to try and minimize the IOs to still maintain performance. Okay, so our first stage here, we have a buffer on the left split into two parts, currently filled with A and B, and we have our database on the right, A, B, C, and D, that we've broken up into blocks that can fit inside of our two parts of our buffer. 

So we load A into the first array, and we load B into the second array, the two halves of our buffer. We compare this difference, then we load C in, and we load D in. So we load both of those into the second array, we keep A there to minimize the reads. We've now compared A across A, B, A, C, and A, D, four total reads. We left off stage one with A, D in the buffer, and we've used all of the comparisons for A that we need. So we keep D in the buffer, we load B into the first array, compare B, D, and then we load C into the first array, and compare C, D. So now we have two additional reads, and we've done all of our comparisons with D. 

So now C is the target block for stage three, it's already in the buffer. So we load A into the second array, compute C, A, and then compute C, B to have our total two reads for the end of stage three. B is left on our buffer now, so we have C in the buffer and B in the buffer. We load A into the first array, compare B, A, and then we load D into the first array, compare B, D. Two total reads, and at this point we have compared every combination of our database. Every block is one quarter of the database, so from stages one to four, a grand total of ten blocks were read out, amounting to ten over four passes for the entire dataset. Not the most effective, but it minimizes I.O. by having this clever slip-swapping of what is being compared and what is already loaded in. 

One more, we have cell-based distance detection. So if we divide the dataset into cells of specified length, and K is dimensionality and D is the distance, we define a layer one neighborhood, all the immediate neighbor cells. So layer two neighborhoods are the cells within three cells of a certain cell, and the minimum distance between a cell and the cells inside layer two neighborhood is D. So criteria here, we search a cell internally. If there are M objects inside, all the objects in the cell are not outliers. Searching the layer one neighbors, if there are M objects inside the cell and it's a layer one neighbors, all the objects in the cell are not outliers. Searching the layer two neighbors, if there are less than M objects inside the cell, it's layer one neighbor cells and it's layer two neighbor cells, all the objects in the cells are outliers. 

Otherwise, the objects in the cell could be an outlier, and then need to calculate the distance between the objects in the cell and the objects in the cells in the layer two neighbor cells to see whether the total points within D distance more than M or not. This will make a lot more sense with the visual. Here we go. So we take our space and we draw a grid over it. We have a certain cell that we're looking at here in red, and we have our layer one neighbor cells immediately around it, and then layer two neighbor cells in blue up to three away. So the maximum distance between a point in the red cell and a point in its layer one cell is D, and the minimum distance between a point in the red cell and a point outside that's layer two cells is D. So we have points inside of this, we are overlaying a grid on top of it. 

We can have multiple cells, multiple data points per cell, and we are calculating distances by cell. Our previous outlier detection algorithms typically work with global outliers, but remember we have local outliers as well. So O2 here, this smaller point in the bottom left outside of the denser cluster, would not normally be considered an outlier with regular distance-based outlier detection because it looks at a global picture. But we do want to point out when we have these local outliers as well. Here we can use a local outlier specific method where each data object is assigned a local outlier factor, LOF. Objects which are closer to a dense cluster receive a higher LOF, and LOF varies according to the parameter minimum points. So we have a visual here of a point and its radius of detection. 

When we calculate these values, minimum points equals three in this case, we are given a scalar value back for each data object to assign its local outlier status. We can then overlay this into a depth and have a visual interpretation of which of these points are probably local outliers. We have some points, I'm going to use the laser pointer here one more time. We have some points here and here, some over here as well, that when we put a LOF on them, we can see, hey, these are probably the local outliers. Again, this is an interpretive validation step, this is not an end result that you publish to your stakeholders. This is for us to double check our outliers as we are doing our analysis. So partition-based detections, using a Birch clustering to identify clusters and partitions of non-outliers, prune partitions that do not contain outliers, and use the index nested loop algorithms on the remaining data points. 

Since many data points are removed during pruning, the efficiency is increased significantly. We can have a partition-oriented outlier detection algorithm that is not using k-nearest-neighbors clustering. Instead, using a Birch clustering turns out to be a fairly efficient way to do it. Partitioning the points, computing the upper and lower bounds, identifying the candidate partitions containing outliers, perhaps partitions with the smallest number of points in them, and computing n outliers from points in candidate partitions to give us a base understanding of what the outliers may be. You'll be getting your hands-on experience with this in lab as usual, so post questions in the discussion forums or send emails to the instructional staff. Have fun!