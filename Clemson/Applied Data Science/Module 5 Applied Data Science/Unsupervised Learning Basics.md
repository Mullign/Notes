![[Unsupervised Learning Basics Slides.pdf]]So this week's topic we'll be talking about clustering, this is our first unsupervised learning technique that we're going over. Unsupervised learning as a whole is very often a first step in sort of learning about how the data relates to itself, but not necessarily as they're related to a response variable, not strictly an input-output relationship, but just an amongst-themselves clustering relationship. This is very often our first sort of technique to explore into the data, because we don't need it to be labeled, we don't need to actually know what the response variable is if there even is one. So finding subgroups of breast cancer patients with similar gene expressions, grouping movies according to ratings assigned by viewers, clustering happens all over the place. So here's an actual definition of clustering, it is a technique for finding subgroups or clusters in a multivariate data set. It's very easy to conceptualize this in two dimensions, and that's how most of our examples are going to play out in these slide decks, but it can happen in higher dimensions very simply. We want to partition the data into distinct groups so that observations within each group are similar to each other, and we need to define concretely what it means for observations to be similar or different, which that can be domain-specific. 

The actual metrics used to determine similarity do change with the types of data, the applications of the data, it's very by problem definition. So just for notation's sake, we assume we have n observations which are measured on p features, if x is an n by p matrix of data, and x sub i j for i being 1 to n and j 1 to p, we are interested in finding the subset of rows in x that are similar to each other, which will form a cluster, and we just define that to be a cluster. So here's a really simple example, we have the eruptions of Old Faithful that are collected at the time between eruptions and the duration of the eruption out at Yellowstone. If we use sort of single descriptive statistics to talk about this, we get these means and standards and minimums that don't necessarily reflect the data itself. So here's the data being plotted out, collected on time between eruptions and the duration of eruption, we have eruptions and weighting on our axes here, and we can really visualize with our human eyes the clusters going on. The trick is to get the computer to actually draw these borders for us. So having these clustering similarities and these algorithms are really useful for finding relationships among data.