![[Diagnostics and Optimizing Number of Clusters Slides.pdf]]
Okay, so how do we actually optimize and investigate the number of clusters to put into these things? Even if we aren't choosing the number of clusters straight out, understanding if the number of clusters that was decided by the algorithm is still an important step. There's no single way to do this, unfortunately. We will explore two types of methods as you get further and further into this field. You will see many, many ways to do this. Direct methods involve optimizing a particular criterion. Silhouette method and the ELBO method are the two sort of first picks. 

Silhouette method we've already talked about, the ELBO method we will get to. Or testing methods that evaluate evidence against a null hypothesis, a gap statistic. So having some sort of statistical inference being done about the variation between clusters is also a valid way to be thinking about the number of clusters to get. Okay, so previously we had this wc sub k to be the within cluster sum of squares distances between all pairs for cluster k for a particular clustering. If we let t sub k be the sum of all of these from k equals one to all of the k's, be the total within cluster variation for the clustering of k clusters. So this is a summation of the distances between all pairs of data points for a cluster. For a particular clustering method, let k vary over a range of values, just one to ten for example. 

Probably want to start at one and go to something. So we're going to use t sub k for each k and plot t sub k against k and look for a clear bend in the graph. We're going to get a kind of hockey stick or ELBO looking graph out of this. The value where the bend occurs is considered the correct number of clusters. So we have an ELBO method graph here and we can see that's having something between two and four, probably four is going to be an appropriate number of clusters for our prime data set that we've been thinking about. Similar to the ELBO method, having a particular clustering method, let k vary over a range of values, one to ten, starting at one going to something. For each k, calculate the average silhouette score across all averages. 

And then if we plot this value, the value of k where s sub k is maximized is considered to be appropriate number of clusters. So we get to have a max here instead of eyeballing where the bend happens. And here we have a maximum of two and it goes down for three and comes back up for four. So while we have an overall maximum at two, when we are interpreting this data set, we can decide if having two clusters or four clusters is the most appropriate number of clustering. This is a heuristic for us to help us decide things. This is not an absolute golden rule that must always be followed. It is probably more likely to be useful to have at least four clusters when talking about things than just splitting it down the middle. 

In that same sense, we have the ELBO method suggesting four clusters where the bendish happened and the average silhouette suggested two, the absolute maximum. Interpretations for each method are different enough that inconsistencies are not uncommon. This is why these are interpretations, not golden rules. Both approaches measure global clustering characteristics only and are informal. This is something that you do for yourself as you're doing your analysis. This is not something that you share with your, well, stakeholders. Okay, gap statistic. 

For a particular choice of K clusters, compare the total within cluster variation to be expected within cluster under the assumption that the data have no obvious clustering, just totally random data assumption. The gap statistic detects whether the data clustered into K groups is significantly better than if they were generated at random. So much like if we were to be doing a t-test or a chi-squared or one of these statistical tests, that's the approach that we're trying to take here. These are clustering better than random to reject the null hypothesis. And here's our algorithm for the gap statistic. First, we cluster the data at varying number of total clusters, K. Let T sub K be the total within cluster sum of square distances. 

This is the same T sub K that we were using a moment ago. Generate B reference data sets of size N within the simulated values of variable J uniformly generated over the range of observed variable X sub J. So we typically choose B to be about 500 in order to give us enough observations to determine a statistical conclusion. For each generated data set, B from 1 to perhaps 500, perform the clustering for each K, compute the total within cluster sum of square distances, T sub K, B. So we have these total square distances of generated sets, and we compute the gap statistic as the gap at K, so K is the varying number of clusters, to be the sum from B1 to perhaps 500, capital B, log of TK, B minus log of TK, divided by B. So lots of numbers flying around here. This is one of the glorious things about computers, is that we can write down the equation once and then it just calculates it for us forever. 

So if we think about W bar here as sort of being the average width, bar is typically like a mean or an average, and W perhaps stands for width, we can compute the standard deviation of our K-th variant cluster as this mathematical equation. We define S sub K to be the standard deviation times 1 plus 1 over B to the one-half root radical, however you want to say that. Finally, after we have all of that calculated out, we choose the number of clusters as the smallest K, such that the gap of that K is greater than or equal to the gap of the next K minus this S sub K plus 1. So we have this K that we're looking for that's minimal, the smallest K, that's greater than or equal to the next K minus this S sub K standard deviation informed statistic. We can roll all that together into a table that looks something like this. We have a number of clusters, this would be our K value in our first column, gap value that we've calculated here, we have mostly the same. We have our standard deviation and our differences, plotting it together, we get to pick K equals 2 as our minimum K, such that gap of K is greater than or equal to the next gap minus S sub K plus 1. 

So we have an answer from the gap statistic that K equals 2 is the optimal number of clusters. This is a principled approach for choosing cluster sizes. It's not just totally eyeballing it, it is a statistical method that we can apply generally. Although we have really different conclusions being reached based on the different clustering approaches, the gap statistic is generally understood to be conservative, as in like errors on the side of picking a smaller number of clusters. There's an alternative form of the gap statistic, which replaces the log term with this formula, gap star, which was in the previous table, feel free to see that. This turns out to be a little bit less conservative. Once again, it is up to us as the data scientists to be picking which is the most useful interpretation of our data, while maintaining our principled approaches. 

So we have some data sets that have structure that's obvious to our human eyes that are less obvious to machines. The K-means clustering was randomly assigning all of the clusters together, generating off where each of these are. And it really struggles with this kind of data set. Like it does not make sense to have this inner ring, for example, be three separate clusters. Ideally that inner ring should be its own cluster on its own, and then the next outer ring and then maybe these bottom left two bars and this bottom right. Data sometimes does have these more obvious to us relationships.