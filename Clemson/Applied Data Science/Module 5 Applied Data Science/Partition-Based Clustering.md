![[Partition-Based Clustering Slides.pdf]]Here's where we get into partition-based clustering. We need to specify a number of clusters into which the data will be partitioned and then perform a computation to group the data so that the observations within the clusters are similar, and the observations between the clusters are dissimilar. The actual number of partitions that we are creating is determined in a separate step, and notice that we're using partition here, not division or agglomeration because partitions categorize every single point. Even if our clusters are not necessarily the best as they could be, they must place every point in one of the clusters. For k-means clustering, let's say that we have some set of k clusters, C1 through Ck, denoting the sets containing the indices of observations within the k clusters, such that the union of all of our clusters together is our dataset and the intersection between any two distinct cluster partitions is the empty set. We have all of our data points accounted for and no data point is in more than one cluster partition. We have the cluster variation here defined in a mathematical form, and the goal of k-means clustering is to find these partitions such that the cluster variation on average is minimized. 

We have our goal to minimize the cluster variation from our previous slide and we have an algorithm here of how to actually do it. First, we randomly assign each observation to one of the k clusters, totally random, and repeat following steps until the clusters do not change. For each cluster k, compute the cluster centroid, x bar sub k, variable-wise average of the observations in cluster k. Then given the k centroids, reassign all observations to cluster based on their closeness to the centroids. This will make a lot more sense with a visual. Here we go. We start off with data here in the top left that is unclustered and we randomly assign all of the points into step 1. 

We have different colors that are showing each of these in turn. We calculate the centroid. Here we have in iteration 1, step 2a, these larger circles that we have plotted in the middle. Then in step 2b, we assign the partitions of each data point to the nearest centroid that has been calculated. Then we move the centroid into the now calculated middle of the data partition cluster. In which case, we do not have a second iteration because the centroid has successfully stabilized. Some notes about k-means clustering as a whole. 

It does require you to select the k in advance and the algorithm is locally optimal. You can get these different cluster results depending on the initial cluster assignments. You can get different cluster results based on the initial random variation in the data points. To try and address some of these issues, we have trying different values of k to compare the results between each other, or try different initial cluster assignments in parallel and choose the one with the best within cluster sum of squared deviations. It's very often the case that just running a bunch of random assignments in parallel will get you to a reasonable minimum value of a statistic. That's a totally viable option if you have the compute power. Here we have six results from different initializations. 

It's not only that the different colors can be rotated amongst each other, the distinct data points themselves are placed into different partitions. We can see in our top left here, we have some outliers, these green points. I'm going to actually use the laser pointer here. This point right here is in clustering from this particular initialization, but it changes depending on which initialization happens. This is particularly common for outlier data points where they are not obviously in each cluster. Back on our violent crimes by state, we have our first few observations that are rescaled, and we have done this by subtracting the mean and dividing by the standard deviation. We choose just four clusters, a totally probably small arbitrary guess as to how many clusters is reasonable to put onto this data. 

We can diagnose if four is an appropriate choice using the silhouette plots. Once the clustering has been determined, let a sub i be the average dissimilarity between observation i and the other points in the cluster to which i belongs. It's the average variation of the cluster points. B sub i is the average dissimilarity between the observation i and the other points in the next closest cluster to observation i. It's the next door neighbor. Should I be in this cluster or should I have been in the next best possibility? We define S sub i to be the silhouette score for the observation i. 

Interpreting this statistic, we have observations with S sub i around one are well clustered. They're around zero, they lie on the edge between two clusters, and if it is less than zero, they're probably in the wrong cluster. So just as an interpretation, when we're doing our smell checks along our way, keep this in mind. Here's a silhouette analysis for k-means clustering on just sample data. We have one point here indicated with the arrow that this S sub i value is in the negative. So something could be better with the blue partition in particular, and we can find 0, 1, 2, 3 of our clusters. It's probably going to be one of these outliers down here, or maybe it's this guy up here. 

All we know is that from this graph, something is in the negative, and that's probably a bad thing. In this particular case, it was Missouri. Thanks, Missouri. Love you.