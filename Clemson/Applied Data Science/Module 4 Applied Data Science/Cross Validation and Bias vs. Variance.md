![[Cross Validation and Bias vs. Variance Slides.pdf]]So we know about overfitting and we know about picking between models. And we need to make sure that the particular test set that we're using is not being overfit to exactly that average test set. This is where cross validation comes in. So if we just have some sort of training data set here represented with stars, and we have validation set that we've taken out, you're represented with squares. We can have a polynomial regression with degree one, just a straight line being cut through. And let's just put a few more on there. There's only degree 2 and degree 3 that can become more and more accurate for this particular set of training data being evaluated on the testing data. 

So we have these two different sets of mean square errors that we're talking about the training set and the validation set, stars and squares. Here's the big idea for cross validation. Using a single validation set to select amongst multiple models to be problematic. This is really just to make sure that we're not picking out a particular random subset accidentally and we're overfitting to exactly that one subset of the data. So instead we pull out multiple validation sets and average the validation performance together. We can do this randomly and just selecting through it, but randomly creating that has the possibility of hitting very weird edge cases very quickly. So instead we pull out specific sets systematically. 

And that's the idea of cross validation. We have this one test set that is pulled out here in the first iteration on the right, and then the next one over for the second, the third, until we've moved through the entire training set and we averaged together that error. This is called K fold cross validation. So here we have a mathematical formulation of it given a data set split up into these partitions, where we have j features in each of them. To ensure that every observation in the data set is included in at least one training set and at least one validation, we use k fold validation. We split it uniformly into chunks and we create K number of validation training splits using K chunks for validation and the rest for training. Exactly what you just saw in the last slide. 

So here we have this in a mathematical form. This is just the sort of symbolic representation of what we just saw. Okay, K fold validation is sort of like the base first shot at getting a cross validation between all of our models. So we can extend this pretty well immediately into leave one out cross validation. So we have our validation set and we train on everything else. We have the cross validation score as the performance of the model averaged across all the validation sets shown here again in a symbolic form. So this leads to a question of what is the right ratio of train validate testing sets? 

How do I actually choose K? Do I have one-half of the data being pulled out for the training set? Do I have 5% of the data being pulled out for the validation set? What is the difference in multiple predictors and polynomial regression in a model selection? So we can frame this problem as a degree selection for polynomial models. And here we have a k equals 1, 10, and 7 k nearest neighbors mean squared error data set that we can be looking at. So in our first model here, we saw that it was really, really important to select an appropriate K for the data. 

If the k is too small, the model is really sensitive to noise. k here is how wide to look out for neighbors. And if k is too large, the model tends to be making constant predictions. So a principled way to choose the K here is through K fold validation. We want to pick the K with a optimal. We know too small is causing errors, and too large is also causing errors. We're looking for a happy middle ground. So with K equals 100, we can step through a bunch of these k values and look at the actual error that's being caused in each of these models when we have cross validation. 

And with no cross validation, we can see a difference in the error being generated here. All right, so that error has bias and variance into it. You've probably seen this described as accuracy versus precision in other scientific contexts. This is like the famous darts at a dartboard plot. In an ideal world, we want low variance and low bias. We want to know what we're aiming at and we want to hit it. Ideal worlds are great. 

We don't live in one though. So we need to have plans to actually discuss the trade off between these two things. So our model complexity has some sort of minimum error that we want. And we know that there's in addition to this, a trade off between the bias and the variance, how accurate you are or how precise you are. If we overfit to a data set, we're going to have a very, very precise data set, but not a very accurate data set. So when we have our linear model here, we have the sample data coming through. We have different sample data that's causing our f hat to be producing different error, and so on and so forth until we have some sort of normal distribution of error around these things that is trying its best to approximate the true model. 

If we're going to try and be a little bit clever with our model and use a polynomial degree 20, we have these tails and these valleys and peaks throughout the middle of it. So when we train on the first sample data set here, one, we have this curve, or when we train on sample data number two, we have this curve, it's pretty significantly different to the last one sample data set three, these lines are significantly different from each other as opposed to the degree 1 regression, which was pretty much a straight line and it kind of wiggled and wobbled a little bit in the slope. These have serious differences in how they overlap or don't. So we lose our average approximation of our true model when we move into a degree larger than one regression. So when we think about the best fit here on the left, we have 2000 best fit straight lines, each fitted from a different 20 point training set. And on the right we've done the same thing and we have this huge difference in how these lines will average into each other. We lose an assumption of closely averaged models. 

So we can do some exploratory data analysis on these on the left, thinking about the linear regression coefficients, we have these coefficients being evaluated at significantly lower degrees, significantly lower values when our coefficient is linear. We have these statistical ways that we can detect when our bias and variance are different. And we'll finish off here with the idea of regularization, which is about modifying the loss function. In particular, we add these terms that penalize some specified properties of our model parameters. So if we have a scalar value that gives weights of importance to the regularization term, we can modify how each term is contributing to the model and we can use this to sort of fit our loss function parameters to whatever properties we're looking for specified by R. Post any questions you have in the discussion forums, send emails and have fun in the labs. 

So we know about overfitting and we know about picking between models. And we need to make sure that the particular test set that we're using is not being overfit to exactly that average test set. This is where cross validation comes in. So if we just have some sort of training data set here represented with stars, and we have validation set that we've taken out, you're represented with squares. We can have a polynomial regression with degree one, just a straight line being cut through. And let's just put a few more on there. There's only degree 2 and degree 3 that can become more and more accurate for this particular set of training data being evaluated on the testing data.  Play video starting at ::50 and follow transcript0:50 So we have these two different sets of mean square errors that we're talking about the training set and the validation set, stars and squares. Here's the big idea for cross validation. Using a single validation set to select amongst multiple models to be problematic. This is really just to make sure that we're not picking out a particular random subset accidentally and we're overfitting to exactly that one subset of the data. So instead we pull out multiple validation sets and average the validation performance together. We can do this randomly and just selecting through it, but randomly creating that has the possibility of hitting very weird edge cases very quickly. So instead we pull out specific sets systematically.  Play video starting at :1:38 and follow transcript1:38 And that's the idea of cross validation. We have this one test set that is pulled out here in the first iteration on the right, and then the next one over for the second, the third, until we've moved through the entire training set and we averaged together that error. This is called K fold cross validation. So here we have a mathematical formulation of it given a data set split up into these partitions, where we have j features in each of them. To ensure that every observation in the data set is included in at least one training set and at least one validation, we use k fold validation. We split it uniformly into chunks and we create K number of validation training splits using K chunks for validation and the rest for training. Exactly what you just saw in the last slide.  Play video starting at :2:29 and follow transcript2:29 So here we have this in a mathematical form. This is just the sort of symbolic representation of what we just saw. Okay, K fold validation is sort of like the base first shot at getting a cross validation between all of our models. So we can extend this pretty well immediately into leave one out cross validation. So we have our validation set and we train on everything else. We have the cross validation score as the performance of the model averaged across all the validation sets shown here again in a symbolic form. So this leads to a question of what is the right ratio of train validate testing sets?  Play video starting at :3:19 and follow transcript3:19 How do I actually choose K? Do I have one-half of the data being pulled out for the training set? Do I have 5% of the data being pulled out for the validation set? What is the difference in multiple predictors and polynomial regression in a model selection? So we can frame this problem as a degree selection for polynomial models. And here we have a k equals 1, 10, and 7 k nearest neighbors mean squared error data set that we can be looking at. So in our first model here, we saw that it was really, really important to select an appropriate K for the data.  Play video starting at :4:4 and follow transcript4:04 If the k is too small, the model is really sensitive to noise. k here is how wide to look out for neighbors. And if k is too large, the model tends to be making constant predictions. So a principled way to choose the K here is through K fold validation. We want to pick the K with a optimal. We know too small is causing errors, and too large is also causing errors. We're looking for a happy middle ground. So with K equals 100, we can step through a bunch of these k values and look at the actual error that's being caused in each of these models when we have cross validation.  Play video starting at :4:45 and follow transcript4:45 And with no cross validation, we can see a difference in the error being generated here. All right, so that error has bias and variance into it. You've probably seen this described as accuracy versus precision in other scientific contexts. This is like the famous darts at a dartboard plot. In an ideal world, we want low variance and low bias. We want to know what we're aiming at and we want to hit it. Ideal worlds are great.  Play video starting at :5:22 and follow transcript5:22 We don't live in one though. So we need to have plans to actually discuss the trade off between these two things. So our model complexity has some sort of minimum error that we want. And we know that there's in addition to this, a trade off between the bias and the variance, how accurate you are or how precise you are. If we overfit to a data set, we're going to have a very, very precise data set, but not a very accurate data set. So when we have our linear model here, we have the sample data coming through. We have different sample data that's causing our f hat to be producing different error, and so on and so forth until we have some sort of normal distribution of error around these things that is trying its best to approximate the true model.  Play video starting at :6:29 and follow transcript6:29 If we're going to try and be a little bit clever with our model and use a polynomial degree 20, we have these tails and these valleys and peaks throughout the middle of it. So when we train on the first sample data set here, one, we have this curve, or when we train on sample data number two, we have this curve, it's pretty significantly different to the last one sample data set three, these lines are significantly different from each other as opposed to the degree 1 regression, which was pretty much a straight line and it kind of wiggled and wobbled a little bit in the slope. These have serious differences in how they overlap or don't. So we lose our average approximation of our true model when we move into a degree larger than one regression. So when we think about the best fit here on the left, we have 2000 best fit straight lines, each fitted from a different 20 point training set. And on the right we've done the same thing and we have this huge difference in how these lines will average into each other. We lose an assumption of closely averaged models.  Play video starting at :7:43 and follow transcript7:43 So we can do some exploratory data analysis on these on the left, thinking about the linear regression coefficients, we have these coefficients being evaluated at significantly lower degrees, significantly lower values when our coefficient is linear. We have these statistical ways that we can detect when our bias and variance are different. And we'll finish off here with the idea of regularization, which is about modifying the loss function. In particular, we add these terms that penalize some specified properties of our model parameters. So if we have a scalar value that gives weights of importance to the regularization term, we can modify how each term is contributing to the model and we can use this to sort of fit our loss function parameters to whatever properties we're looking for specified by R. Post any questions you have in the discussion forums, send emails and have fun in the labs.  0:41 / 8:54 1.25x Cross Validation and Bias vs. Variance Save note Transcript Notes Downloads Transcript language: English​Interactive Transcript - Enable basic transcript mode by pressing the escape key You may navigate through the transcript using tab. To save a note for a section of text press ⌘ + S. To expand your selection you may use ⌘ + arrow key. You may contract your selection using shift + ⌘ + arrow key. For screen readers that are incompatible with using arrow keys for shortcuts, you can replace them with the H J K L keys. Some screen readers may require using ⌘ in conjunction with the alt key Play video starting at ::6 and follow transcript0:06 : Added to Selection. Press [⌘ + S] to save as a note Like Dislike Report an issue : Added to Selection. Press [⌘ + S] to save as a note