So just some definitions and working into our margin of a hyperplane here, we have hyperplane h such that the hyperplane is greater than one when the response variable y sub i is equal to one. And on the other side of the plane, it spits out a negative number when it is on the other side of the hyperplane. It doesn't really matter which side is plus and which side is negative. That's symmetrical in that way. So these points on the planes are the support vectors. We have the actual x sub i dot w plus b. Remember, that's just a generalized straight cut through whatever dimensionality you're working on, where we have d plus the shortest distance to the closest positive point and d minus is the shortest distance on the other side. The margin is that separating gap. And, you know, we have all these like vector notations and things floating around here in the in the left, but visualizing this in two dimensional space works out so long as you keep in mind that this can be extended into three, four, five n dimensions. So we have some sort of dividing support vector h that has a distance d positive to h1 and d negative to h2. And that gap is determining our margin. We want to have a maximal margin difference, we want to be able to find the hyperplane that sort of most evenly divides our points, because that will help us have a effective interpretation later on. And this will be the most efficient way to actually divide our data. We have a distance from point to a line, ax plus by plus c hyperplane, we can generalize a Euclidean distance from two dimensions, you know, if you think about your a squared plus b squared, equals c squared distance, we can generalize that distance here with this dot matrix notation and the magnitude of the vector. So in order to maximize the margin, we need to minimize this w magnitude, the weights magnitude with the condition that there are no data points between h1 and h2. This leads into what's known as a constrained optimization problem. It's a fascinating branch of math of how we can actually like find these. But the short of it and for our applied data science purposes, we have a derivative proof of what we are trying to maximize the summation here, under a condition, the below summation. We actually do want to do this even as applied data scientists. Part of this is under the quadratic programming, because for a fixed real number accuracy, we can solve this in pretty efficient time, O of n log n time, that's about the same, or that that's the same as our fastest data sorting algorithms, for example. So we have this positive semi definite programming problem. Great for mathematicians, for data scientists. This is just our proof that this is a pretty efficient algorithm, n log n time, not that bad. Problems though. 

When we were talking about our hyperplanes, we were thinking about these flat cut lines between them, having a line in two dimensions, having a plane in three dimensions. These decision functions are not always linear. Here's one that is sort of radial, we want to have a circle. That's where our kernel trick comes into play. So we have data points that are linearly separable in this space, x1 squared, x2 squared, square root of two, x1, x2. If you project the kernels from your original data sets into higher dimensions, it becomes linearly separable. So maximizing our summation here, we can still do this, like summation maximization problem. And we can define our k, our kernel, is pretty easy to compute. So for a circle, x sub i dot x sub j squared is a pretty efficient algorithm. 

It basically moves from our sort of radial picture down below with the blue circle into our higher dimensional space that we can put a plane across in the top. So radial kernels show up all the time, lots of things happen like kind of in a space easily captured in a circle. But polynomial kernels also are very useful. So we have x dot j x sub j plus one, where p is a tunable parameter representing the polynomial degree. So evaluating k, our kernel here only requires one addition and one exponentiation more than the original dot product. We're still not too far off of efficient algorithm space. This is still maintaining like a reasonable efficiency. Gaussian kernels are a fascinating space of math to study into that, and we just need to be aware that it's another kernel opportunity that projection can produce an efficient result in. Okay, overtraining and overfitting is a big problem with SVNs. Well, machine learning in general, but SVNs in particular. This means that we've learned the training data very well, but we cannot classify unseen examples. So if we think about like our graph on the right here, if we have a highly polynomial dividing hyperplane between our training and test data, we could draw this line through the points and like exactly capture the border of those points. What we want is this maximum distance, maximum margin hyperplane cutting through in a sort of reasonable dimensionality so that it's fast. 

On an intuition level, this example, a botanist thinks they know all trees. Every time he sees a new tree, he claims it's not a tree because it's not in the set of trees that he has seen. You know, what is a cactus? Is that a tree? What is a palm tree? It's not really a tree. Trees are weird. 

Okay, so a measure of the risk of overtraining, and there are lots of other ways to talk about this, but we can show that the portion end of unseen data that will be misclassified is bounded. The number of support vectors over the number of training examples is greater than or equal to the misclassified data. So Occam's razor here, just as a logical way to think about what's going on here, a simpler system is better than a more complex one. Better here, especially in data science terms, better as typically means faster to efficient. So having fewer support vectors means a simpler representation of the hyperplane. And, you know, if we have one gene of information, we want to pick out the important dividing points rather than using 5,000 things and have to compute 5,000 times more data to make the same result.