Cool, let's talk about support vector machines. So we have four main ideas for this one. The first one is this max margin classifier, some sort of separating hyperplane that will distinguish the boundaries of our data. We're going to formalize this notion of the best linear separator. We have Lagrangian multipliers, which is a way to convert the constraint optimization problems to one that is easier to solve. Many problems become easier to find gaps to put hyperplanes in when you project into higher dimensions with a kernel trick. So projecting the data into higher dimensional space makes it linearly separatable, or the complexity of this. 

So depending only on the number of training examples, not the dimensionality of the kernel space, being able to actually do this with real data means that we have to deal with the complexity that comes with having these high dimensionality spaces. And high dimensionality typically means that things get harder, but we try to avoid that with some tricks. Okay, support vector machines are actually a really simple idea. So here's the simplest diagram I think that can be drawn to actually show this idea. We have a humidity continuous variable, and we have a temperature continuous variable, and then we have a play tennis, do not play tennis categorical variable. So three data points, three types of data going on here. We have some sort of line that divides, hey, it's too humid to play, or it's too hot to play, but at some point there's this threshold that is crossed from saying, okay, I'm good to go play, or I no longer want to go play tennis. 

So our first generalization here, removing our tennis example, we have some set of data. Here we have x variables and y variables. We have the single x1, which is just the first continuous was humidity. We have the single vector x2, which is the continuous variable used to be temperature. These place the data sets onto the space that we're looking at. But also we have these indicator variables. So y sub i is either a negative one or one to distinguish them like categorically. 

So just like with the linear regressions, there could be many lines that are dividing these particular points of data. So hyperplanes are a generalized idea of this. We have this rd notation to notate the real numbers in d dimensions. So a typical flat graph like this is in r2. We parameterize that with a vector of weights and a constant b, which can be expressed into a dot product to sort of specify a generalized hyperplane idea. Lines are a hyperplane, just in a low dimensionality. So if you remember a equation of a hyperplane, it in very simple terms could be y equals mx plus b for a straight line. 

It could be z equals ay plus bx plus c for a plane, generalizes up into n dimensions in that way. So we want to find a hyperplane f of x that returns the sine of a point given that hyperplane and correctly classifies our data. So when we have the sine function, all that is is just positive, negative, green, red. Which side of the hyperplane is that point on? That's our goal.