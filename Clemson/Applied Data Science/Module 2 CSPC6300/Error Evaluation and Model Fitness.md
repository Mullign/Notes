 ![[Error Evaluation and Model Fitness Slides.pdf]]So let's go into these error evaluation and model fitness a little bit more. Starting with just a set of data. This is a graph of two-dimensional data that we can work with here. We take out some of the data points and create a train test split. So in order to train our model, we need to have some set of our data reserved so that we can check if the model is any good. This is known as the training set and the testing set, where the training sets are the ones that we use to train and testing we use to test. So if we're going to be estimating y-hat for k equals one, we can just look at these points and move right through. 

We have some sort of function that's moving between them, but if we want to look at the red crosses here, the points not on the line, we look at the data that we haven't used, and here we can use these to estimate our error. In this case, we have what are known as residuals, y-sub-i minus y-hat-sub-i, the difference between the predicted line and the known true set. So our test set comes in here to add up the distances between our known and estimated values, in this case, our error evaluation. When we're thinking about the error evaluation here, in order to quantify how a model performs, we define a loss or error function. So this is a generalized term that we were using in the previous models, we were talking about distance as sort of the loss and error, but when we use more abstract models in the future, talking about the actual loss itself becomes more convenient. A common loss function for quantitative outcomes is the mean squared error. This is exactly what we were doing. 

It's a difference between distance of points and how they add up, well, as an average in your test set. This is called a particular value in here, the y-sub-i minus y-hat-sub-i, how far off your individual guess is, is called a residual, and measures the error at a certain point in your data. So be aware, this is the first one we're talking about, this is not the only, and it's not the best loss function. Even for K and N algorithms, we find often that different data sets require different loss functions to produce useful models. The square root of the mean of squared errors, RMSE, is also really commonly used. We have these different formulations mathematically because of different data sets and different models that we need to make accurate. So when we're thinking about the comparison of two models, how do we actually choose between these? 

"Do the same for all k's and compare RMSEs. k equals 3 seems to be the best model." This graph here is showing the residual mean square error for K equals 1, 2, 3, 4, 5, 6, 7, 8, and it seems like it bottoms out first at K equals 3. So looking at the three nearest neighbors seems to be the best model to predict the particular data sets that we were working with. Overall fitness. How does the model perform? For our slips of the day, we calculate the residual mean square error for K equals 3, but is 5 good enough? Just because it's the best number of neighbors to be looking at in this model, it doesn't mean that it's actually a good model. 

We don't know if the fit is good. This is very sensitive to things like units. If we used cents instead of dollars here, our RMSE is huge, and if all you're looking at is the one number to come out to evaluate the fitness of your model, it's possible that a misleading interpretation is caused by just using cents instead of dollars. So it really does need us to be comparing this to something in order to make an informed decision about if our errors are good and if our model is actually fit. So we'll use the simplest model. This is the full average of all of the data sets. And if our model is as good as the mean value, Y bar, then R squared equals zero. 

If our model is perfect, then R squared is one. R squared can be a negative if the model is worse than the average, and this can happen when you evaluate the model in test set. So we've defined R squared with this formula below. One of the wonderful things about doing this with computers is we don't need to be calculating these by hand. This is definitely the first points that I'm seeing where having these mathematical formulas is great in software, doing it by hand would be a task. So now we've talked about the comparison and the model fitness, evaluating significance of predictors, and how well do we know F hat. These are in the next lecture. 

So I'll see you then, and post any questions that you have in the online forum. We will be in touch. Catch you later.