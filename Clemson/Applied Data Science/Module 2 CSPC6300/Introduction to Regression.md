![[Introduction to Regression Slides.pdf]]
Let's talk about some regression. This is one of the core components of statistical learning and data science. Very, very important, it's really early in the class. So let's get into talking about how we actually predict a variable. So we want to predict the amount of view time that YouTube video will get next week based on video length, the day it was produced, previous number of views. Maybe we want to predict which movies a Netflix user will rate highly based on previous input. Essentially, we're using a set of variables to predict another at least one set of variables. 

So here's a dataset, the advertising dataset which consists of the sales, that product in 200 different markets, along with advertising budgets for the product in each of these markets for three different media, TV, radio, and newspaper. Everything is given in units of $1,000. It comes from this wonderful book, An Introduction to Statistical Learning with applications in R. So if we have TV, radio, newspaper, sales, we can think about TV, radio, newspaper as inputs, and sales as an output here. In data science, we talk about these as response and predictor variables. There's a huge asymmetry to notice when we talk about these problems. The variable we'd like to predict is possibly more difficult to measure, is more important than the others, or maybe directly or indirectly influenced by the values of the other variables. 

So we really define these two categories of variables that we want to predict and values that we use to make our prediction. If you're familiar with these terms, predictor and response, you can infer a input-output relationship, x, y. For math terms, we have a set of predictors and we have our response. Here we have the n observations, 1, 2, 3, 4, 5, rows indicates five observations of 1, 2, 3 predictor variables and one response output. If we're observing p plus 1 number variables and we're making n sets of observations, we call the variable we'd like to predict the outcome or response variable. Typically, this is with a Y and a capital Y and the individual measurements are little y sub i and index into the larger set of y. Then the features of predictors we denote with capital X. 

We have typically many more of these. So just big X is the set of all predictors. Big X sub and index is the particular predictor that we're talking about. Then the observation is X sub i, j to indicate the row and column coordinates in our dataset. There's a true model that's actually being naturally observed here. We have a statistical model that we are trying to estimate that true model because we don't know what it is. We can assume that it's some function on the predictor variables and some error, f of predictors, f of big X plus epsilon (we use for error), that is used to denote a estimated function for the true model. 

F hat is our estimation, Y is the true model. So how do we find F hat? If we have some data floating around in space and we have an X and a Y, is it this one? What is the value of Y at this X? We don't know. We don't have data to verify what this particular output would have been. Similarly, for this position, we don't know what the Y value is. 

So one way that we can make a statistical model is take the mean, the average of all of the Y values, and make the predictor function F hat just be the output of the average value. It gives us a line, it gives us better than nothing. But as you can see, the difference between our red dot line here is pretty great between that and our actual known data points. So this brings up one of the first points about data science that really is a decision that we make as the data scientist. Whether or not we actually care about the form of F hat, or if we just want to make the outputs as similar to the observed values as possible. If we care about the actual form of F hat, that's called an inference problem, and we use Y hat sub I to actually talk about the predicted value. Whereas if we don't really care about the function going on, we can just say, make it accurate. 

That's a prediction problem. So here we have a simple set of data. We can ask, what is Y hat sub Q at some X sub Q? We have X sub Q noted with our circle point here between 150 and 200, and we have a whole range of values that Y hat sub Q could be. So one way to talk about this is define the distances to all their points, distance formula here, and find the nearest neighbor. Whatever is going on nearest to this line, could also be a prediction for what Y hat sub Q is. So what if we know that our dataset here is actually connected in each point? 

We have this estimation of line that goes between the datas. Regardless of what that connection looks like, we know that it's a continuous dataset. So if we're trying to estimate this Y hat sub Q at X sub Q, we can find the distances to all their points just like in our previous method, except we can find the K nearest neighbors and take an average between them and make a hopefully more accurate model. If we have an assumption that our data is continuous, then it reasons that moving from this circled point on the left to the circle point on the right, there will be some middle ground between them, that X hat sub Q would more likely be at. We can extend this through. If we have K equals one, we're just looking at the nearest neighbor. If we have K equals three, we take the averages of three different points. 

If we have K equals 10, we can get a really, really high resolution of average and make a hopefully more accurate prediction. So this is called the K nearest neighbors model. It's a fairly intuitive way to predict a quantitative response variable. Predict a response for a set of observed predictor values, we use the responses of other observations most similar to it. If you're not sure how to guess what something will be, you look around and, hey, whatever else is going on is probably in that range. Strategy can also be applied into classification problems. We'll be talking about the KNN algorithm later in this class, in the context of unsupervised learning and classification. 

Here it is, just in a more mathematical form. For a fixed value of K, the predictor response for the ith observation is the average of the observed responses of the K closest observations. So as we're looking at these first two models to compare between, the first model being looking at the nearest point and just using that value, and the second model being using the K nearest neighbors average value, we need to think about a way that we can actually choose which one we want to use. So the fitness we describe in a model is how the model performs in predicting an actual variable. We evaluate the significance of the predictors, maybe some of the predictor values are outliers and they really mess with our mean value. How well do we actually know what F hat is? The confidence intervals of our F hat can indicate to us when we are confident statistically that our model is providing a useful result.